# CLAUDE.md — חוקי עבודה

## 0. מה הקובץ הזה

מסמך **פקודות**, לא ידע. נטען אוטומטית לכל session של Claude Code.

| קובץ | תפקיד |
|------|--------|
| `CLAUDE.md` (כאן) | חוקי עבודה — מה מותר, מה אסור, מה חובה |
| `CONTEXT.md` | ידע — ארכיטקטורה, סכימה, כללי קוד, היסטוריית ספרינטים |
| `docs/RUNBOOK.md` | נהלי חירום — שחזור DB, קמפיין מייל, התקנת hooks |
| `docs/STATE.md` | מצב חי — מה עכשיו, מה הבא, מה חסום |
| `docs/DECISIONS.md` | החלטות נעולות — מה הוחלט, למה, ומה נפסל |
| `docs/INCIDENTS.md` | יומן אירועים — כל תקלת פרודקשן/CI ולקחה |

**בסתירה בין הקבצים — `CLAUDE.md` גובר.**

---

## 1. כלל הברזל

לפני כל פתרון: **"האם מהנדס גוגל + טריידר וול-סטריט היה בונה את זה?"**
אם לא — מחדש. לא "בינתיים", לא "זה יעבוד".

## 2. הסטנדרט

- **אפס כשל שקט** — כל כשל צועק. `catch` ריק, `|| []` שמסתיר שגיאה, ברירת מחדל שמחליפה נתון אמיתי — כולם באגים.
- **אפס שינוי בלי הוכחה** — "אמור לעבוד" אינו אימות. הוכחה = פלט מודבק.
- **אפס תלות בזיכרון אנושי** — אם הכלל לא כתוב בריפו, הוא לא קיים.
- **אפס מנה בלי מכנה** — כל אחוז, שיעור או "X%" חייב להופיע עם המונה, המכנה, ושם האוכלוסייה: `41/41 סגורות = 100%`, לא `100%`. חל על דוחות, פלט סקריפטים, שאילתות אד-הוק, ועל כל מספר שנמסר בפרומפט. לפני שמדווחים שיעור: **האם המכנה יכול בכלל להכיל את מה שנספר?** אם לא — המספר מודד את גודל האוכלוסייה, לא התנהגות.

## 3. מטרת-העל

כל פתרון נמדד בשאלה אחת: **האם זה מקטין עבודה ידנית לניב לאורך זמן?**
פתרון שמוסיף לניב צעד ידני קבוע הוא פתרון גרוע גם אם הוא נכון טכנית.

---

## 4. Safety gate — לפני כל משימה

```bash
git pull origin main && git status
```

`pull` שנכשל **או** עץ לא נקי → **STOP**. מדווחים לניב ומחכים.
לא "אני אסתדר", לא stash אוטומטי, לא `checkout --` על שינוי לא מוכר —
קובץ לא מוכר עלול להיות עבודה בתהליך של ניב.

## 5. גיט — חוקי ברזל

1. **לעולם לא force-push.** אין "עם אישור", אין `--force-with-lease`. `push` שאינו fast-forward נחסם ב-`.githooks/pre-push`. (מבטל את `CONTEXT.md` §Coding Rules #8 בנוסחו הישן.)
2. **לעולם לא לקומט `HANDOFF*.md`.** ב-`.gitignore` + נחסם ב-`.githooks/pre-commit`.
3. **session אחד בכל רגע על הריפו.** אין worktrees, אין עבודה מקבילה. שתי סשנים במקביל הם מה שהוביל ל-force-push.
4. **"סיימתי" תקף רק עם פלט push שמכיל `..HEAD -> main`.** קומיט מקומי אינו סיום — Vercel מתפרס מ-`main` בלבד. נתפס פעמיים בעבר (ראה `CONTEXT.md` §Working procedures).
5. **עקיפת hook** — `--no-verify` בלבד, למקרה חירום, **ומתעדים ב-`docs/INCIDENTS.md`**.

## 6. התקנת hooks — פעם אחת לכל clone

```bash
git config core.hooksPath .githooks
chmod +x .githooks/*
```

`core.hooksPath` הוא config **מקומי** ואינו עובר ב-`clone`. clone חדש בלי הפקודה הזו = בלי הגנה, בשקט. ראה `docs/RUNBOOK.md`.

## 7. verify לפני כל push

```bash
npm run verify   # test:coach → test:import → test:write → test:settings
                 # → test:hydration
                 # → test:datachain → test:rcontract → test:format → test:fx
                 # → test:tradingstats → test:ocr → test:engine → test:feedback
                 # → test:notify → test:landing → test:digest → test:capability
                 # → test:analytics → test:arch → test:copy → test:horizon
                 # → test:syntax → test:instrument → test:registry → test:dna
                 # → test:shortpct → test:watchlist → test:equitystate
                 # → test:boundary → test:cents → test:lessons → test:diagnosis
                 # → test:tour
                 # → build
```

33 חוליות ואז `build`. **הרשימה נגזרת מ-`package.json` ומתעדכנת באותו קומיט שמשנה את השרשרת** — ראה `docs/DECISIONS.md` 2026-08-08. 🆕 **המספר הזה **נאכף** מ-15.09** — `B-274`: הפרוזה אמרה «23» בעוד הבלוק מנה 24 ו-`package.json` הריץ 24, וזו הייתה הזזת-יד; `D-083` מדד שהמספר הוזז ביד **שבע פעמים** בטווח `23`→…→`29`, בלי ששער יוכל לראות זאת. 🔴 **`B-274` נסגר → `D-083`** (15.09, `62c3a32`, `T3`, **אומת-מקור**): `test:registry` §14.1 גוזר את החוליות מ-`scripts.verify` · §14.2 הוא **שער-מטא** שדורש את העוגן «N חוליות ואז `build`» בדיוק פעם אחת · §14.3 **משווה פרוזה למדוד** ⇒ מספר סחוף **צובע את השרשרת אדום**. ⛔ **אל תפתח מחדש את `B-274` ו⛔ תמחזר את המזהה** — פריט סגור חי ב-`DONE` בלבד (§14). ⚠️ **ומה ש-§14.3 ⛔ עושה הוא לגזור-ולכתוב** — את המספר בפרוזה עדיין מזיזים **ביד**, והשער תופס הזזה **שגויה** ⛔ מייתר את ההזזה עצמה; זו הצורה המוסכמת, ⛔ חוב פתוח. 🔴 **והחוב שכן נשאר בסעיף הזה הוא `B-324`, פתוח** — `11` מניות ה«N assertions» **בפסקה שלמטה** נשארו כתובות ביד ו⛔ נגזרות משום שער.

**להדביק את הפלט המלא בדיווח**, לא סיכום ולא "עבר". `test:coach` (111 assertions) חובה לפני *וגם* אחרי כל נגיעה ב-`src/intelligence/`; `test:import` לפני כל נגיעה ב-`src/import/` או בסכימת העסקה; `test:write` (25 assertions) לפני כל נגיעה ב-`src/lib/tradeWrite.js` או בכל מסלול שכותב ל-`trades` — כולל מסלולי ה-RPC ב-`AdminPanel.jsx`; `test:ocr` לפני כל נגיעה ב-`api/ocr.js`; `test:fx` לפני כל נגיעה ב-`src/lib/fx.js` או ב-`api/fx.js`; `test:digest` (9 assertions) לפני כל נגיעה ב-`scripts/daily-digest.mjs` או ב-`api/_lib/replyLedger.js`; `test:arch` (8 assertions) לפני כל נגיעה ב-`scripts/arch-auditor.mjs` — כולל הוספה לרשימת ה-`ACKNOWLEDGED`; `test:capability` (11 assertions) לפני כל נגיעה ב-`src/lib/imageResize.js`, בכל מסלול שמעלה תמונה ל-`/api/ocr`, או בכל צריכה של `navigator.clipboard` / `matchMedia`; `test:analytics` (17 assertions) לפני כל נגיעה ב-`src/lib/consent.js`, ב-**`src/main.jsx`**, או בכל אתר קריאה של `track*` ב-`SwingEdge_App.jsx` — כולל הוספת פרמטר לאירוע, שמוחזק ברשימה סגורה. 🆕 **אסרציות 15–17 (27.08, `B-184`) שומרות על הערוץ ה*שני* — Vercel Analytics.** ⚠️ 1–14 היו **עיוורות מבנית** אליו: אסרציה 12 היא מילולית `/(?<!window\.)\bgtag\s*\(/` ⇒ היא מודדת את gtag **בלבד**, ולכן `INCIDENTS#12` נסגר בערוץ אחד ונשאר פתוח בשני בלי שאף שער יוכל לראות זאת. ⛔ **`beforeSend` הוא רשימה-לבנה ו⛔ לא מסנן**: הוא **בונה מחדש** `origin + analyticsPath(pathname)` ו⛔ לעולם אינו מחפש `access_token` בשם — מסנן-לפי-שם עובר בשקט ברגע שהספק קורא לפרמטר בשם אחר, וזה נמדד: פרמטר סינתטי בשם `session_key` נפל יחד עם השאר. ⚠️ **וקריאת סקריפט ⛔ אינה קובעת מה נשלח** — הכללה של «CSS סטטי ⛔ אינו קובע מה מרונדר»: החבילה ב-`node_modules` ⛔ אינה שולחת דבר, הלכידה והביקון חיים ב**בייטים ש-Vercel מגישה**, ואימות שנעצר בחבילה מחמיץ את הבאג. ⛔ **ו-Playwright headless ⛔ אינו כלי אימות כאן** — הסקריפט מתאבד על `navigator.webdriver` ומחזיר «אין דליפה» כוזב; האימות הוא מארז מבודד בדפדפן אמיתי מול `dist/`; `test:copy` (16 assertions) לפני כל נגיעה ב-`src/data/tooltips.js`, ב-`colorMap` של `OnboardingScreen.jsx`, או במתג השפה ב-`LandingPage.jsx` — **ואחרי כל עריכת tooltip יש להריץ `npm run glossary` באותו קומיט**, אחרת assertion 5 תתפוס סחיפה בין `tooltips.js` ל-`glossary.json`; `test:horizon` (140 assertions) לפני כל נגיעה ב-`src/lib/tradeHorizon.js`, בספי האופק, או בנקודת האופק בשני מסלולי הרינדור (`SwingEdge_App.jsx` + `src/components/MobileTradeCard.jsx`) — **הספים הם החלטת מוצר ולא ריפקטור**: מספר שזז בבלוק 1 משנה כמה משתמשים רואים את הפיצ'ר ומחייב עצירה, לא עדכון ציפייה; `test:syntax` (4 assertions) לפני כל נגיעה ב-`scripts/` או ב-`api/` — הוא החוליה **היחידה** שפורסת סוכני CI, ו-19 החוליות שלפניו היו ירוקות ביום שסוכן פרודקשן לא התקמפל; `test:instrument` (519 assertions — ⚠️ **המספר הוזז ביד `485 → 519` ב-18.09 (`B-345`, בלוק 21), וזה **`B-324`** — פתוח, ⛔ `B-274` שנסגר ב-`D-083`** — אין שער שגוזר אותו מהריצה. ⚠️ **ההזזה הזו היא הכרעת ניב 18.09 («ב»), ⛔ ברירת מחדל:** harness חדש יכול לגור בחוליה **חדשה** — ואז זזים **שני** מספרים (`32 → 33` בבלוק השרשרת שלמעלה, **ועוד** מנייה חדשה כאן) — או **להתארח** בחוליה קיימת, ואז זז **אחד**. נבחר האירוח, ולכן **השרשרת נשארת `32` ו⛔ זזה**. ⛔ **וזה ⛔ סוגר את `B-324`** — הוא מקטין את ההזזה מ-`2` ל-`1`, ⛔ מבטל אותה. ⚠️ **הפרוזה אמרה «252» ונמדדה 417 ב-06.09**, כלומר נסחפה **לפני** גל `G2` ⛔ ולא בו; זה **`B-324`** בתבנית זהה, על קובץ אחר — ⛔ `B-274`, שנסגר ב-`D-083` על מניית ה**חוליות** בלבד. 🆕 **בלוק 19 (`417 → 452`, 18.09, `B-338`) הוא בית ה*מחשבון*** — 🔴 המסך נקב «`SHARES 2` · `MAX RISK ₪106` · `POS. VALUE ₪1,334.77`» ו**מתחתיו** התרה שהסיכון בפועל `4.3%`, כלומר פי 4.3 מהכלל שלפיו ההוראה חושבה; מחשבון שממליץ על מה שהוא פוסל מאבד את שני המשפטים. השורש הוא `‹R-2›` טהור: `suggestedShares = posSizeTooSmall ? 1 : posSize` — מניה **מומצאת** במקום הודאה. ⛔ **הסרתה לבדה הייתה יוצרת כשל שקט חדש** — `shares: 0` ב-DB, שממנו כל מדד נגזר (P&L · סיכון · DNA) מתאפס בשקט ⇒ `0` + **חסימת שמירה** ב-`handleSubmit` + באנר שכתוב מחדש הם **שלושה חלקים של תיקון אחד**. ⚠️ **שני בלוקים ותיקים קידדו את ההמצאה** (13.1 · 13.9e) והועברו ל-`sharesOverride: "1"` — אותם ערכים byte-identical (`6.54%` · `₪913.29` · `₪163.49`), כלומר **בחירה מוצהרת** במקום רצפה; ⛔ אף ציפייה ⛔ רוככה (`R-4`). ⚠️ **ו-`MAX RISK` ⛔ במחלקת `D-068`/`B-321`** — הוא נשא `.toLocaleString()`, ש⛔ כופה שתי ספרות ויושב ב**זרוע הביקורת** `K1`–`K6` של `test:cents`; הוא תוקן ל-`fmtCapitalAmount` והאסרציה יושבת **כאן**, ⛔ שם) לפני כל נגיעה ב-`src/lib/instrumentCurrency.js`, ב-**`src/lib/positionSizing.js`**, ב-**`fxPairPlan`/`accountAmount`/`spotAmount`/`livePnlAmount`/`makeConvertingCalc` שב-`src/hooks/useFxRates.js`**, בכל שער שמשווה מטבע נייר להון (`SwingEdge_App.jsx` לוח הסיכון · `TradeDNA.measurableRisk` · `GrowthTracker.riskMgmtScore`), או ב-`pnlByCurrency` של `src/lib/tradingStats.js` — **הגזירה רצה בזמן קריאה ולעולם אינה נשמרת**, ובלוק 9 חוסם הוספת עמודת DB נגזרת. ⚠️ שער שמשווה `currencyOf(t)` (התווית השמורה) להון הוא טאוטולוגי ואינו יכול לירות — הבלוקים 5–7 קיימים כדי להוכיח שהשער **יכול** להיכשל. ⛔ **ואסור להכניס `|| 1` או `?? 1` למסלול גודל הפוזיציה** — פלט `sizePosition` הוא הוראת קנייה, ושער מנוחש שם הוא פקודה בגודל שגוי ⛔ ולא מספר מכוער על המסך. ⚠️ **הקו הקפוא של 13.9b הוסר בגל ג׳ ומניית 13.9 ירדה `3 → 1` בגל ד׳** (`B-119`, 13.08): שני אתרי ה-P&L החי הומרו ב-spot, ונותר `riskDollar` בלבד. ⛔ המנייה ⛔ אינה מקבעת התנהגות — היא **אוכלוסייה**; מי שמתקן אתר מוריד אותה **ומצהיר בהערה מה זז**, ⛔ ולא מרכך מספר כדי לעבור. ⚠️ **בלוק 14 (`A1`–`A8`) הוא ההפרדה בין עבר להווה**: `accountAmount` ⛔ אינו מסרב על פוזיציה פתוחה — הוא **מצליח** בשער יום ה**כניסה** (`realizedDayKey` נופל ל-`trade.date`), ולכן שימוש חוזר בו על P&L חי הוא מספר שגוי **בשקט**. `A2` היא האסרציה היחידה שיכולה להבדיל, ו-`A8` שומרת על המצב החמישי (`dispCcy` ≠ `accountCurrency`) שבו ⛔ אין `identity`; `test:registry` (36 assertions) לפני כל נגיעה בשמונת קבצי המרשם (§14) — הוא בודק **צורה בלבד** (כפילות מזהה · מקור+תאריך · **תקציב `STATE` בבתים** · hash · מפעיל לבדיקה · מזהה שנעלם · ❄️ בלי נימוק · `NEXT`/`INBOX` · ⛔ מרשם בשורש · עמודת `T` · **`METRICS` §12** · 🆕 **פרוזה מול מדידה §8.6**) ו**אינו יכול לדעת אם פריט נכון**. 🆕 **§8.6 (27.08, `D-057`) סוגר את `B-250`:** §8.1 **הדפיס** את מספר השיוכים ו⛔ לא השווה אותו לפרוזה בראש שכבת השורשים ⇒ המספר שם נסחף **ארבע פעמים באותה שורה** (13.08 · 19.08 · 20.08 · 26.08) והשער היה **ירוק** בכל אחת. ⛔ **התיקון ⛔ אינו עדכון המספר** — זה מה שנעשה ארבע פעמים. §8.6 קוראת ארבעה מספרים מהפרוזה (סה"כ · עם-שורש · שיוכים · בלי-שורש, כולל **המכנה** של `X/Y`) ומשווה למדוד. ⚠️ **ותבנית שאינה נתפסת בדיוק פעם אחת היא כשל, ⛔ לא דילוג** — ניסוח מחדש של הפרוזה היה הופך את השער לעיוור ומחזיר את `R-4` בתחפושת. ⚠️ נצפתה אדומה בארבעה מוטנטים (כולל מקרה «מונה נכון, מכנה נסחף») ואז **ירתה אדום בשימוש האמיתי הראשון** על ארבעת המספרים בו-זמנית. ⚠️ **§3 עבר משורות לבתים ב-26.08** — `wc -l` החזיק את המספר בשלמות (`100→100`, ואז `68→99`) בזמן שהתוכן גדל ×9.2, והמחזור הראשון הסתיים באובדן 33 פריטים פתוחים; `DECISIONS` 26.08 · `docs/audits/STATE-PRUNE-2026-08-26.md`. 🆕 **§12 מודד טריוּת ⛔ ולא נכונות** — הוא מחשב `נמדד + תוקף` מול היום ומחייב `⏳ פג`; ⛔ **הארכת `תוקף` כדי לעבור אסורה** והיא בדיוק `R-4`. ⚠️ אסרציה `4.1` תפסה בריצתה הראשונה ש**קומיט אינו יכול לשאת את ה-hash של עצמו** ⇒ גל שסוגר את עצמו נרשם ב-DONE **בקומיט הבא**; ⛔ הכלל לא רוכך כדי לעבור, ו-`--amend` נשאר אסור (§5); `test:dna` (27 assertions) חובה לפני כל נגיעה ב-`src/intelligence/core/TradeDNA.js` או ב-`src/intelligence/core/GrowthTracker.js` — בודק ש-4 ציוני ה-DNA וחמשת תת-הציונים של ה-Growth מחזירים `null` על אוכלוסייה לא-נמדדת ⛔ ולא `50`/`100` מומצא (B-156, 20.08). כולל שער תפוס-קפוא (§4) על ג'ורנל מלא — 4 עסקאות מ-D-041 — שחייב להישאר ירוק לפני *וגם* אחרי כל שינוי; מספר שזז שם עוצר, לא מתעדכן בשקט. ⚠️ `consistency` בשער הקפוא זז `53→46` באותו קומיט שהציג את `test:dna` לראשונה — לא רגרסיה: `TradeDNA` ייבא בעבר נוסחת שונות ידנית (population, ÷n) בזמן ש-`GrowthTracker` קרא ל-`stddev()` המשותף (sample, ÷n−1); שני מנועים על אותו ג'ורנל נתנו שני מספרים שונים. התיקון החליף את ההעתק הפנימי בקריאה ל-`stddev()` (מקור-אמת-אחד, §13) — הזזת השער היא ההוכחה שהשער תפס משהו אמיתי. 🆕 `test:hydration` (22 assertions — **17 מבחינות · 5 ⚪ אינווריאנטות**, 29.08 `B-268` + 22.09 `B-361`) לפני כל נגיעה בשער ההידרציה שב-`SwingEdge_App.jsx` (`:1726-1818`), באפקט ההתמדה (`:1824-1848`), באפקט ה-unload (`:1853-1864`), **בשני ה-`useCallback` שכותבים ישירות ל-DB** (`dismissWelcome` · `completeTour`), או ב-`loadSettings`/`migrateFromLocalStorage` שב-`src/lib/userSettings.js` — הוא ה-harness **היחיד** ל-`SwingEdge_App.jsx`, ועד שנולד ⛔ **אפס** מהחיווט בקובץ הזה היה מכוסה (`build` מוכיח **קומפילציה בלבד**). ⚠️ **הוא מריץ את הבייטים שבקובץ**, ⛔ אינו בודק תבנית: 61 `import` חוסמים ייבוא ל-Node ⇒ הוא מחלץ את גופי שלושת ה-`useEffect` לפי עוגן ומריץ ב-`new Function` עם תלות מוזרקת. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`) — שינוי-שם או ריפורמט **עוצר את השרשרת**, ושלושה שערי-מטא (מספר התאמות ≠ 1 · סוגריים לא מאוזנים · זנב שאינו `setHydrationDone(true);`) אוכפים זאת. ⚠️ **חמש האינווריאנטות מודפסות ב-⚪ ו⛔ לא ב-✓ בכוונה** — הן ירוקות בשני העצים ו⛔ **אינן** ראיה לתיקון; קריאתן כראיה היא בדיוק העיוורון של אסרציה 12 ב-`test:analytics`. ⚠️ **ו-`A7`·`A8`·`A9` הן ראיה חלשה יותר מהשאר** — הן אדומות בעץ הישן כי **חוזה התלות המוזרקת עצמו השתנה**, ⛔ לא כי אותו קלט מייצר התנהגות שונה. 🆕 **`A15`·`A16`·`A17` (22.09, `B-361`) הן החלפת משתמש באותו טאב.** 🔴 `hydratedRef` הוא `useRef` ש⛔ אופס **לעולם**, ו-`:4050` הוא early-`return` ⛔ ולא unmount ⇒ אחרי `A`→`B` הוא נשאר `true`, ו**שלושה** מסלולי כתיבה ירו על `B` **בערכים של `A`** — נמדד: `{"capital":49000,…}` לשורה של `u-B`, ועוד `{welcomeSeen:true}`+`{tourDone:true}` על בסיס-מיזוג `{}` ⇒ **החלפת הבלוב כולו**. ⚠️ **הן דרשו חילוץ חדש**: `D-lite` יושב ב**ראש ה-`useEffect`**, לפני שער ה-early-return, ולכן גוף `hydrate` לבדו ⛔ יכול למדוד אותו — בהתנתקות (`authUser = null`) הגוף ⛔ נקרא כלל. ⇒ העוגנים עלו `3 → 5` (`+` שני ה-`useCallback`), ו-`I5` מודד את כולם. ⚠️ **`A16`/`A17` נמדדות *סינכרונית* מיד אחרי גוף האפקט** — וזה בדיוק מה ש-React עושה: אותו commit, ⛔ המתנה למיקרו-טאסק. ⛔ **והכתיבה ל-`localStorage` ב-`completeTour` נשארה *מחוץ* לשער בכוונה** — המכשיר הזה ⛔ יריץ את הסיור שוב גם כשכתיבת ה-DB חסומה. ⛔ **והוא ⛔ אינו מכסה JSX**: הבאנר · שני ה-`onClick` · RTL/ניגודיות · React עצמו · דפדפן אמיתי · פרודקשן — כולם חיים ב-`C-036` **בלבד**, שנשאר פתוח ומצומצם (`B-273`), ו-`B-361` נסגר-במסך רק ב-`C-058`. 🆕 `test:shortpct` (18 assertions, 04.09, `B-186`) לפני כל נגיעה במשפט `const livePnlPct =` שב-`SwingEdge_App.jsx` או בצרכן היחיד שלו (`Math.abs(livePnlPct)` ברוחב הבר) — ה-harness ה**שני** לקובץ הזה, ובאותה תבנית חילוץ-לפי-עוגן של `test:hydration`: **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת ארבעה שערי-מטא (עוגן ≠ 1 · צרכן ≠ 1 · סוגריים לא מאוזנים · זנב שאינו `: null;`). ⚠️ **הענף SHORT חילק ב-`currentPrice` ולא ב-`tr.entry`** ⇒ אחוז שגוי בכל SHORT פתוח; ⛔ **הכסף (`livePnl`) היה נכון** — רק הבר. ⛔ **ואסור להכניס `|| 0` או `?? 0` לשומר `currentPrice && tr.entry`** — `null` הוא הודאה, `0` הוא המצאה (`R-2`). ⛔ **והוא ⛔ אינו מכסה רינדור**: מעל `±10%` שני הברים נחתכים ל-100% ⇒ דלתא-הפיקסל היא **אפס בדיוק**, והמסך חי ב-`CHECKS` בלבד. 🆕 `test:watchlist` (25 assertions + 2 ⚪ אינווריאנטות, 05.09, `B-185`) לפני כל נגיעה בזרע `DEFAULT_WATCHLIST` (`:1641-1645`), ברינדור `const price`/`const changePct` שברשימת המעקב, או באפקט ההתמדה שכותב `watchlist:` בתוך `patch` — ה-harness ה**שלישי** לקובץ הזה, ובאותה תבנית חילוץ-לפי-עוגן: **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת שישה שערי-מטא (ארבעה עוגנים ≠ 1 · סוגריים לא מאוזנים בארבעת המקטעים · זנב שאינו `;`). ⛔ **`46/46` שורות `user_settings` בפרודקשן נשאו ליטרל מ-`SCANNER_DATA` שהוצג כמחיר חי** — הזרע נשא `price`/`change` אמיתיים, הרינדור ניחש מהם כש-`lp` היה `null`, והכתיבה החוזרת שימרה את הליטרל לנצח. ⛔ **ואסור להכניס `|| 0` או `?? 0` למסלול `price`/`changePct`** — `null` הוא הודאה, `0`/הליטרל הישן הם המצאה (`R-2`). ⚠️ **שורת האחוז ב-JSX נושאת שומר `changePct != null` (הכרעת ניב, `DECISIONS` 05.09, «ב2»)** — אינווריאנטה בלתי-נאכפת ב-`priceService.js` אינה הגנה. 🔴 **`priceService.js:136` שורד את הגל הזה** — הוא ממציא `0` כש-`prevClose` falsy, ונרשם כ-`W1` ⚪: ירוק בשני העצים ו⛔ אינו ראיה שהסימפטום `+0.00%` נסגר. ⛔ **והוא ⛔ אינו מכסה רינדור**: JSX · React · דפדפן אמיתי · שהמשתמש כבר רואה `—` על המסך — אותו גבול בדיוק של `C-036`/`C-038`, ונסגר רק ב-`C-039`. 🆕 `test:equitystate` (30 assertions — **14 ערך · 16 חיווט**, 05.09 `B-295` + 06.09 `B-301`) לפני כל נגיעה ב-`src/lib/equityState.js`, בשלושת אתרי ההון שב-`SwingEdge_App.jsx` (כותרת · כרטיס KPI · פוטר), 🆕 **בלוח הסיכון** (`:4746-4880`), או ב-`riskInCapital` שב-`src/hooks/useFxRates.js`. 🆕 **`G2` (06.09, `B-301`) הרחיב אותו `18 → 30`** — `V7`–`V10e` (**ערך**, `deriveRiskState`+`riskFigure`) ו-`S1`–`S4` (**צורה**, לוח הסיכון). 🔴 **הלוח הכריז «בטוח · 0.00%»** על משתמש שנשא סיכון אמיתי בשתי פוזיציות: `matchesCapital` גידר **שוויון קודים** בלי ניסיון המרה, ו-`totalRiskDollar` סיכם `riskDollar ?? 0` ⇒ מי שלא נספר תרם **אפס**. ⛔ **`matchesCapital` ⛔ לא הוסר — `S2` שומר עליו**: ההמרה נכנסת **אחריו**, ⛔ לא במקומו. ⚠️ **האסימטריה היא החוזה:** סכום חלקי הוא **חסם תחתון** ⇒ `over`/`caution` **שורדים** עליו ו-`safe` **נמחק** — הבטחה שלא נמדדה ⛔ אינה מוצגת. ⛔ **ואסור `|| 0`/`?? 0` בכל המסלול** — האוכלוסייה **מסוננת בשם** (`countedRisks`) ⛔ ולא מנוטרלת בשקט (`R-2`). 🔴 **הכותרת הציגה סכום חלקי כסופי** — `$3,000.00` ואז `$3,045.66` באותן הגדרות בדיוק, כש-45.66 הם P&L פתוח שטרם נחת, ⛔ בלי חיווי טעינה; ובחשבון QA שבו ⛔ לא היה מחיר חי, ההון הוצג כסופי. **הכלל: ⛔ אין מספר עד שהוא שלם.** ⚠️ **שני החצאים ⛔ אינם שווי-ערך כראיה**: `A1`–`A6` הן אסרציות **ערך** על המודול, `C1`–`C12` בודקות **צורה בלבד** על בייטים של ה-JSX — הן מוכיחות ששלושת האתרים צורכים מקור אחד ו⛔ אינם מרנדרים `fmtBalance(curEquity` ערום, ⛔ ולא שהתג נראה. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת שלושה שערי-מטא (שלושה עוגנים ≠ 1) ש⛔ **אינם נספרים ב-18**. ⚠️ **הפוטר הוא ה⛔יחיד עם look-back** — `title`/`aria-label` יושבים על ה-`span` העוטף, כלומר לפני עוגן הטקסט; החלון הורחב **אחרי** שנצפה אדום, ואומת שהוא עדיין יורה אדום על מוטנט שממנו הוסרו התכונות. ⛔ **ואסור להכניס ברירת מחדל לחתימת `deriveEquityState`** — מונה שנעדר ונופל ל-`0` היה מסווג `complete` אוכלוסייה שלא נספרה (`R-2`). ⚠️ **גדר ה-`loading` חלה על הנחיתה הראשונה בלבד** (`pricesLoading && pricesLastUpdated == null`) — `setPricesLoading(true)` יורה בכל מחזור ריענון, וגדר על `pricesLoading` לבדו הייתה מוחקת מספר **שלם** מהמסך כל 15 שניות בשוק פתוח. ⛔ **והוא ⛔ אינו מכסה רינדור**: התג · RTL · ניגודיות · קורא מסך אמיתי · React · דפדפן · פרודקשן — אותו גבול בדיוק של `C-036`·`C-038`·`C-039`, ונסגר רק ב-`C-041`. 🆕 `test:boundary` (49 assertions — **4 מטא · 32 ערך · 13 צורה**, 09.09, `B-304`) לפני כל נגיעה ב-`src/components/PanelBoundary.jsx`, ב-`src/lib/alertChannel.js`, בגבול השורש שב-**`src/main.jsx`**, או בחמשת עוטפי `<PanelBoundary>` שב-`SwingEdge_App.jsx`. 🔴 **ב-07.09 `t.riskPct` אחד שהיה `null`, בתא אחד, בטבלה אחת, הפיל את הג'ורנל · הייבוא · המאמן · ההגדרות והמעקב** — כי `main.jsx` עטף את כל האפליקציה ב**גבול אחד**. ⚠️ **המשתנה הוא רדיוס-הפגיעה, ⛔ לא השומר** — שומר שנשכח יישכח שוב. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת ארבעה שערי-מטא (esbuild · קריאת הקובץ · עוגן ברירת-מחדל ≠ 1 · ארבעת הייצואים). ⛔ **`onError` ⛔ אינו `captureException` שני** — `Sentry.ErrorBoundary` **כבר לוכד** בעצמו, ולכידה ידנית לצדה הייתה מתייקת **כל קריסה פעמיים** ומשחיתה בדיוק את המנייה שהגל הזה נבנה כדי לסמוך עליה. לכן ההעשרה עוברת ב-`beforeCapture` (`level: "fatal"` · `tags.boundary` · `contexts`) — בלי `fatal` קריסה **מוכלת** מגיעה כרעש שגרתי, כי הגבול מסמן `mechanism.handled = true` — ו-`onError` נושא את **ערוץ הדיסקורד**, שהוא ערוץ אחר ו⛔ לא כפילות. ⛔ **וה-webhook ⛔ לעולם לא בלקוח** — `V31` אוסרת `discord.com` במודול הלקוח; הוא נקרא ב-`api/alert.js` מ-`SENTINEL_DISCORD_WEBHOOK` בצד השרת בלבד, כי webhook בבאנדל הוא סוד מפורסם שסיבובו מחייב פריסה (`B-310`). ⛔ **ו⛔ אין ענף שמרנדר `null`** (§3.3) — `V25` שומרת על זה: גבול שנכשל בשקט הופך «שבור» ל«נראה תקין», וזה `R-4` בתחפושת. ⚠️ **`S4` — ש⛔ אין שני פאנלים החולקים גבול — הוא מה שמשאיר את האחים בחיים**, ⛔ ו⛔ אינו ניקיון. ⚠️ **13 ה-`S*` הן צורה בלבד** על בייטים של JSX: הן מוכיחות שהעוטפים קיימים ו⛔ לא שהכרטיס נראה. ⚠️ **נצפו 12 אדומות** על עץ שהמודולים בו קיימים והעוטפים ⛔ לא — כלומר האדום מודד את **החיווט**, ⛔ לא היעדר קובץ. 🆕 **`probe:boundary` (26 assertions) ⛔ אינו בשרשרת** — הוא דורש דפדפן אמיתי, כמו `test:smoke`. הוא **תצפית הירי** (⑥①): ה-`PanelBoundary` האמיתי מול `@sentry/react` האמיתי ב-Chromium אמיתי, כשפאנל אחד זורק את צורת 07.09 בדיוק. ⚠️ **`C1`–`C4` הן זרוע ביקורת שחייבת להישאר «מתה»** — היא משחזרת את הצורה הישנה (גבול אחד לכל האפליקציה) ומודדת שהאחים **נעלמו**; ⛔ **ביקורת ירוקה פירושה שהטיפול ⛔ אינו מוכיח דבר**. ⛔ **Tailwind ⛔ אינו מהודר שם** ⇒ נמדד **פריסה**, ⛔ לא צבע · ניגודיות · RTL · קורא מסך — אותו גבול בדיוק של `C-036`·`C-038`·`C-039`·`C-041`, ונסגר רק ב-`C-043`. 🆕 `test:cents` (49 assertions — **20 עוגן · 8 צרכן · 20 חילוץ · 14 מחלקה · 6 ביקורת**; ⚠️ `20+8+20` הם **שערי-מטא** ו⛔ נספרים בציון), 15.09, `B-321`) לפני כל נגיעה ב-`Math.round` על **כסף** ב-`SwingEdge_App.jsx`, או ב-`fmt$`/`fmtMoney`/`fmtBalance`/`fmtAccountAmount` שב-`src/utils.js`. 🔴 **ההגדרה תלויה ב*מעצב*, ⛔ בצורה** — `fmt$`/`fmtBalance`/`fmtAccountAmount` כופים `minimumFractionDigits: 2` ⇒ `Math.round` ערום לפניהם מדפיס `.00` שמבטיח אגורות **שלא נמדדו**; ואילו `fmt$0`/`fmtPrice`/`.toLocaleString()` ⛔ ⇒ `fmtPrice(Math.round(avgWin))` — **צורה זהה** — ⛔ במחלקה, ויושב ב**זרוע הביקורת**. ⚠️ **זרוע הביקורת היא מה שמודד את *רוחב* ההגדרה**: `K1`–`K6` חייבות להישאר ירוקות, ואדום שם פירושו שנגעת מחוץ למחלקה ⇒ **עצור**. ⛔ **הוא מייבא את המעצבים ה*אמיתיים*** מ-`src/utils.js` ואת `accountAmount` ה**אמיתי** מ-`src/hooks/useFxRates.js` — סטאב היה מודד את הסטאב. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת `48` שערי-מטא (`20` עוגן ≠ 1 · `8` עוגן-צרכן ≠ 1 · `20` חילוץ/איזון סוגריים). 🔴 **והלדג׳ר הוא דו-כיווני, וזה החוזה:** `9/14` האתרים **עדיין פגומים בהצהרה** (`8` ב-`B-325` · `A1` ב-`B-321`) ונטענים ב**כיוון ההפוך** — מי שמתקן אחד מהם בלי להזיז את שורת הלדג׳ר **מקבל אדום**. ⛔ **ולכן ⛔ לקרוא את הפלט כ-`14/14` ירוק** — הסיכום מדפיס `CLASS 5/5` · `LEDGER 9/9 פגומים-בהצהרה` · `CONTROL 6/6`, ושורת אזהרה מפורשת אוסרת את הקריאה הזו; «כל האסרציות עברו» על מחלקה ש-`9/14` ממנה פגומים הוא בדיוק `R-4`. ⛔ **והוא ⛔ אינו מכסה רינדור**: JSX · React · דפדפן אמיתי · פרודקשן — אותו גבול בדיוק של `C-036`·`C-038`·`C-039`·`C-041`·`C-043`, ונסגר רק ב-`C-049`. 🆕 `test:lessons` (47 assertions — **6 מטא · 41 ערך/צורה**, 17.09 `B-326` + 18.09 `B-331`·`B-336`) לפני כל נגיעה ב-`generateSmartLessons` שב-`SwingEdge_App.jsx`, בטקסט כרטיס «הסטאפ החזק ביותר», **באריח `Best Setup` שבאנליטיקס**, או בצריכת `stats.bySetup`. 🔴 **הכרטיס המליץ להגדיל את גודל הפוזיציה על סטאפ מפסיד** — «`Unknown` הוא הסטאפ החזק ביותר שלך · 33% הצלחה על פני 3 עסקאות» + «הגדל את גודל הפוזיציה», על אותו מסך שהציג `PF 0.20`. **שני שורשים:** ⓓ המלצת ההגדלה **מרותכת** למחרוזת (⛔ תנאי · ⛔ טרנרי) ⇒ **הסרה, ⛔ ניסוח מחדש**; ⓖ `Unknown` — הליטרל ש-`tradingStats.js:417` מייצר מ-`(raw ?? "").toString().trim() || "Unknown"` — היה **מועמד שווה-זכויות**, ⇒ **סינון לפני ה-`sort`, ⛔ שער אחריו** (שער אחריו מוחק את ה**כרטיס** במקום את ה**מועמד**). ⚠️ **`lang === 'he' ? … : …` הוא בינארי ו⛔ חמש-דרכי** ⇒ `es`·`pt`·`ar` נופלות לענף האנגלי; המחרוזת חיה ב-`2` ליטרלים בדיוק, והסרתם מכסה `5/5` שפות — `V7` מודדת זאת ⛔ מניחה. ⚠️ **זרוע הביקורת `K1`–`K8` חייבת להישאר ירוקה** — היא ירוקה ב**שני** העצים ומודדת את *רוחב* הסינון; אדום שם פירושו שסטאפ אמיתי נעלם ⇒ **עצור**. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת `6` שערי-מטא (שלושה עוגנים ≠ 1 · איזון סוגריים מסולסלים · שתי בניות ב-`new Function`). 🆕 **בלוק 5 (`B-331`, 18.09) סוגר את מה שהפרוזה הקודמת הצהירה כ⛔-נסגר-כאן** — שער `count >= 2` ישב **אחרי** ה-`argmax` ⇒ קבוצה בת עסקה אחת ב-100% זכתה בבחירה, נפלה בשער, וסטאפ בן 5 עסקאות ב-80% שישב מתחתיה ⛔ הוצג כלל; המשתמש ⛔ ראה «⛔ מספיק נתונים» — הוא ראה **כלום**. ⛔ **התיקון הוא הזזת השער אל *תוך* המועמדות, ⛔ ריכוך הסף** — אותה הכרעה בדיוק כמו `Unknown`: מסננים **מועמד**, ⛔ **תוצאה**. ⚠️ **ו-`K12`/`K13` כן מקבעות את סדר הבחירה** (`winRate` ואז `count`) — הפרוזה הקודמת אמרה ש⛔ קיימת אסרציה כזו, וזה היה נכון עד הגל הזה. 🆕 **בלוק 7 (`B-336`) הוא ה-harness ה*שני* בקובץ** — אותו סינון `Unknown` הוחל על אריח `Best Setup` באנליטיקס, שנבחר לפי `winRate` בלי **שום** שער מדגם. ⚠️ **`L1` היא שורת לדג׳ר, ⛔ הישג** — היא מודדת ש⛔ נוסף שער מדגם לאריחים (⑨: `:6309`·`:6318`·`:6330`), ⛔ שהיעדרו נכון; מי שמוסיף אחד מזיז שלושה אריחים בבת אחת. ⛔ **והוא ⛔ אינו מכסה רינדור**: JSX · React · דפדפן אמיתי · פרודקשן — ונסגר רק ב-`C-050`. 🆕 `test:diagnosis` (33 assertions — **5 מטא · 13 לדג׳ר · 5 גזירה · 2 catch · 8 ביקורת**, 18.09, `B-335`) לפני כל נגיעה בקריאת `add()` בשני קבצי ה-sentinel (`tests-sentinel/sentinel-auth.spec.js` · `sentinel-public.spec.js`). 🔴 **ב-17.09 20:35 (ריצה `35271690173`) שער כניסה ב-`deleteRow` זרק, ולכן הלחיצה על «מחיקה» והדיאלוג ⛔ רצו — וההתראה אמרה «בדוק את `handleDeleteTrade` ואת `ConfirmProvider`».** ההתראה האשימה קוד שלא רץ, וניב בנה עליה פרומפט. ⚠️ **חמשת אתרי ה-`got` הם החמורים** — התווית בדיסקורד היא «מה חזר» ⇒ פרוזה שם היא **שקר על מדידה**, `R-2` בצורתו הטהורה; 11 אתרי ה-`reason`/`fix` הם ניחוש **מוצהר**. ⛔ **גוזרים מ-`e.message`, ⛔ מוחקים** — `sentinel.yml:570-573` מרנדר 5 שדות ב-`jq`, ומחיקה משאירה תבליט ריק. ⛔ **והלדג׳ר שומר את המשפט המדויק, ⛔ תבנית** — בדיקת «יש אינטרפולציה» הייתה עוברת על `` reason: `${e.message}` `` שמשכפל את `got` ⛔ ומסביר דבר. ⚠️ **זרוע הביקורת `K1`–`K8` חייבת להישאר ירוקה** — `:394` (ה-`reason`) ו-`:723` הם הניסוח הנכון שכבר היה בעץ, ואדום שם פירושו שנגעת מחוץ למחלקה ⇒ **עצור**. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת `5` שערי-מטא (חתימת `add` ≠ 1 לקובץ · איזון סוגריים · אוכלוסייה `27`/`6` · `13` שורות לדג׳ר ייחודיות). ⚠️ **והוא מודד *צורה*, ⛔ אמת** — ירוק כאן פירושו ש⛔ נותר משפט ידוע-שקרי, ⛔ שההתראה נכונה; המסך והאמת חיים ב-`C-051` בלבד. 🆕 `test:tour` (57 assertions — **12 מטא · 5 ערך · 11 עוגן · 11 ניווט · 10 מקשים · 8 התמדה**; ⚠️ המספר כתוב **ביד** ⇒ `B-324`, פתוח — ⚠️ **ו⛔ תיאורטי: הוא נסחף *בתוך הגל שהציג אותו*.** הפרוזה נקבה «56 · 13 מטא · 10 ניווט» בעוד הריצה מדדה `57 · 12 · 11`, והפירוט עצמו סכם `57` ⇒ סתירה פנימית שאף שער ⛔ ראה. השורש: `N11` נולדה בזרוע הביקורת `CA-N8` **אחרי** שהפרוזה נכתבה, ו-`esbuild` נספר כשער-מטא אף שהוא **תנאי-קדם קשיח** ש⛔ פולט שורת אסרציה), 20.09, `B-350`…`B-353`) לפני כל נגיעה ב-`src/components/OnboardingTour.jsx`, ב-`buildTourSteps`/`NAV_KEYS`/`TOUR_NAV_IDS`/`completeTour` שב-`SwingEdge_App.jsx`, או ב**כל** תכונת `data-tour=` בעץ. 🔴 **הקובץ נשא *אפס* כיסוי (נמדד)** — ארבעה תיקונים בו באותו גל בלי שער הם הימור. 🔴 **`B-351`: `data-tour="add-trade"` ישב על *שני* אלמנטים** — כפתור מצב-ריק שמרונדר רק כש-`trades.length === 0`, וה-FAB; `document.querySelector` לקח את הראשון ⇒ בעץ ריק הסיור הצביע על כפתור שנעלם ברגע שנוצרה עסקה. ⛔ **בלי שגיאת build, בלי מבחן, בלי התרעה** — משפחת `A*` היא השער שהיה תופס אותו. ⚠️ **היא סופרת על פלט `esbuild` ⛔ על המקור** — הערת פרוזה ב-`IntelligenceUI.jsx:97` מכילה `data-tour="trading-dna"` מילולית, וסריקת-מקור הייתה מונה אותה כאלמנט שני; ההמרה מוחקת הערות ומנרמלת `data-tour={anchor}` ⇒ `anchor: "x"`, ולכן **שער-מטא `M7`** דורש **אתר-העברה אחד בדיוק** (`StatCard`) — שני היה מעוור את ספירת צורת-ה-prop. ⛔ **`TOUR_NAV_IDS` נגזר מ-`NAV_KEYS` ⛔ נכתב ביד** (`V2`) — ליטרל היה ממשיך לאשר מזהה ישן ביום ששם טאב זז. ⛔ **ואין `|| "dashboard"`** במסלול הניווט (`N7`): יעד מנוחש הוא `R-2`, וההודאה הרועשת היא התשובה. ⚠️ **`N8`/`N9` מקבעות את התאימות-לאחור כ**בחירה**: `navKeys` חסר/ריק ⇒ ולידציה ⛔ מופעלת. ⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת `12` שערי-מטא (שישה עוגנים ≠ 1 `M1`–`M6` · ארבעה איזוני-סוגריים `M8`–`M11` · זנב שאינו `}, [fwdKey, backKey]);` `M12` · אתר-העברה ≠ 1 `M7`) — ⚠️ **ו-`esbuild` ⛔ ביניהם**: הוא **תנאי-קדם** שקורס את הריצה כולה ו⛔ פולט אסרציה, וספירתו כשער-מטא היא מה שנתן «13». ⛔ **והוא ⛔ אינו מכסה רינדור**: React · DOM · גיאומטריית הזרקור · RTL · ניגודיות · דפדפן אמיתי — אותו גבול בדיוק של `C-036`·`C-038`·`C-039`·`C-041`·`C-043`·`C-049`·`C-050`, ונסגר רק ב-`C-057`. ⚠️ **`test:syntax` ⛔ מכסה את `tests-sentinel/`** — הוא פורס `scripts/` ו-`api/` בלבד.

⛔ **גרש-הפוך (`` ` ``) בהערת SQL בתוך `scripts/` או `api/` — אסור.** `--` הוא הערה ב-SQL ואינו הערה ב-JavaScript; בתוך template literal אין תחביר הערות כלל, והגרש סוגר את המחרוזת. ⚠️ **הכשל אינו תמיד רועש:** ציטוט שפותח בתו-מזהה (`` `date` ``) נופל ב-11/11 בפרסינג, אבל ציטוט שפותח בסימן פיסוק (`` `.length` ``, `` `,x,` ``) עובר ב-8/8 את `node --check` — ומקרה הפסיק מייצר SQL **קטוע** בשקט. לכן `test:syntax` §3 קיימת ואינה כפילות של §2. שמות עמודות נכתבים חשופים או במרכאות כפולות. `INCIDENTS#15`.

`test:smoke` (Playwright) **אינו** בשרשרת — הוא נפרד ואינו חוסם push.

---

## 8. ניתוח השלכות — הרץ על עצמך לפני כל Plan

**רמה 1 — פילטר שקט.** חמש שאלות:

- משנה DB?
- נוגע בכסף או במיילים?
- מוסיף סוד?
- רץ אוטומטית בפרודקשן?
- בלתי הפיך?

כל התשובות "לא" → **המשך, אל תציג כלום.** אין ערך בטבלה על שינוי CSS.

**רמה 2 — רק אם הפילטר תפס.** הצג טבלה:

| ציר | שאלה |
|-----|-------|
| משתמשים | מי מושפע ואיך? |
| נתונים | הפיך? דורש מיגרציה? |
| עלות | Vercel · Supabase · דקות Actions · מכסה |
| תקרות ספק | Gmail ~300/יום · Finnhub · Supabase 500MB |
| אבטחה | סודות · RLS · חשיפת נתונים |
| תחזוקה | מבחן מטרת-העל (§3) |
| הפיכות | איך חוזרים אחורה, ותוך כמה זמן |
| כשל שקט | איך זה ייכשל בלי שנדע? |

**פסק דין:** ✅ בצע · ⚠️ בצע עם הגנה · ✂️ פצל · ⛔ אל תבצע.

### 8.1 נוהל אבחון-לפני-ביצוע

בכל סשן רציני, או בכל מקום עם פוטנציאל לבלבול — **קודם אבחון מקיף, ומיד לאחריו
ביצוע באישור ניב+Claude.** שני שלבים נפרדים:

**שלב 1 — אבחון read-only.** מודד את המצב בפועל · **מפריך או מאשש כל הנחה
שבפרומפט עצמו** · דוח ל-`docs/audits/` שנדחף **לפני כל נגיעה בקוד**.
⛔ **אפס תיקון** — אבחון שמתקן תוך כדי הוא הנחה שמתחפשת לממצא.

**שלב 2 — ביצוע.** נכתב **מהממצאים המדודים** ולא מההשערות · Plan Mode (§9) ·
עצירה לאישור · ואז ריצה.

**מתי? — כשהסיווג (§15) נותן `T3`.** ⛔ הרשימה למטה אינה מבוטלת: היא מונה את
המקרים שהתשובות תופסות ממילא, ומשמשת כבדיקת-שפיות על הסיווג. **מפעילים:**

- נגיעה בכסף · בנתוני משתמש · בסכימה
- סתירה בין הנראה למתועד
- צילומי מסך עם התנהגות לא צפויה
- כל משימה שבה מספרי שורות או הנחות מהזיכרון עלולים להיות מיושנים

⚠️ **Code מזהה בעצמו כשמשימה עומדת בקריטריון ואומר זאת — גם אם הפרומפט לא ביקש.
"הפרומפט לא ביקש" אינו נימוק.**

## 9. נוהל תוכניות

ארבעה שלבים, לפי הסדר הזה בדיוק (ראה `docs/DECISIONS.md` 2026-07-27 לנימוק):

**שלב 1 — Plan Mode.** מציג תוכנית → ניב מאשר יציאה מ-Plan Mode. **אישור זה הוא
רשות לכתוב קבצים בלבד — אינו אישור לביצוע.**

**שלב 2 — הפעולה הראשונה והיחידה אחרי היציאה:** כתיבת התוכנית המלאה ל-
`docs/plans/PLAN-YYYY-MM-DD-<slug>.md`, קומיט **נפרד**:

```
docs(plan): <משימה> — awaiting approval
```

push, **ואז עצירה.** אין נגיעה בקוד, ב-workflows, או הרצת npm בשלב הזה.

**שלב 3 —** Claude מושך מ-origin, קורא את הקובץ שנדחף (לא מהזיכרון), ומחזיר
לניב אישור או תיקונים.

**שלב 4 —** רק אז מתחיל הביצוע בפועל.

## 10. בסיום כל משימה

1. עדכן `docs/STATE.md` — **לפני** ה-push, באותו קומיט.
2. אירוע פרודקשן או CI → גם `docs/INCIDENTS.md`, **באותו קומיט של התיקון**.
3. החלטה ארכיטקטונית חדשה → שורה ב-`docs/DECISIONS.md`.
4. שינוי יכולת פונה-משתמש → עדכן `docs/TRUTH.md` באותו commit.

### 10.1 אין פריט יתום

כל דבר שהוחלט או התגלה ואינו מבוצע בפרומפט הנוכחי חייב להגיע ל-`docs/STATE.md`
**באותו commit** — ל-⏭️ אם הוא משימה, ל-⏸️ אם הוא חסום על ניב, ל-⚠️ אם הוא חוב
מוכר. **"נטפל בזה אחר כך" בלי שורה בקובץ = הפריט לא קיים.**

חל גם על סריקה סביבתית (§11): ממצא שדווח ולא תוקן — שורה ב-⚠️, לא הערה בדיווח בלבד.

בבלוק הסיום של כל פרומפט, לפני ה-push:
**"האם משהו שעלה במשימה הזו נשאר בלי שורה בקובץ?"**

### 10.2 אין פידבק יתום

§10.1 סוגר את מה ש**אנחנו** גילינו. הסעיף הזה סוגר את מה ש**המשתמשים** אמרו.

**כל פידבק משתמש מסתיים באחד משלושה מצבים — אין רביעי:**

| מצב | מה נדרש |
|-----|----------|
| **משימה** | שורה ב-`docs/STATE.md` (⏭️ / ⏸️ / ⚠️), עם ציטוט הפידבק |
| **טופל** | תוקן, **עם hash הקומיט שתיקן** |
| **נדחה** | לא ייושם, **עם נימוק כתוב** — "לא רלוונטי" אינו נימוק |

**`Reviewed` בפאנל הפידבק הוא מצב ביניים, לא סיום.** הוא אומר "נקרא", לא "הוכרע".
פידבק שיושב ב-`Reviewed` בלי אחד משלושת המצבים הוא פידבק יתום: המשתמש טרח לכתוב,
מישהו קרא, ושום דבר לא קרה ואי-אפשר להוכיח אחרת. זה בדיוק מה שמלמד משתמשים
להפסיק לכתוב.

בכל מעבר על פאנל הפידבק: **"איזה מהשלושה זה, ואיפה זה כתוב?"**

## 11. סריקה סביבתית

תוך כדי עבודה תיתקל בבאגים שאינם המשימה. תקן **מיד** רק אם מתקיימים כל השלושה:

- ודאי ב-100% (לא "נראה לי")
- **וגם** קשור למשימה
- **וגם** קטן ובטוח

אחרת → **רשימה נפרדת בדיווח, ניב מחליט.** תיקון "בדרך" הוא איך שדיף נהיה בלתי-ניתן לביקורת.

## 12. גבולות קשיחים

- **מיגרציות:** Claude Code כותב קובץ `.sql` **בלבד**. ניב מריץ ידנית ב-SQL Editor. Code לעולם לא מריץ מיגרציה.
- **סודות:** Code לעולם לא מריץ `gh secret set`, לא מזין ולא מדפיס סודות. ניב בלבד.
- **קוד אפליקציה:** שינוי ב-`SwingEdge_App.jsx` / `src/` / `api/` / `.github/workflows/` רק כשזו המשימה עצמה.
- **מיילים למשתמשים:** כל מייל למשתמשים = HTML ממותג מ-`emails/`, טוקני v3, RTL, CTA ל-swing-edge.com עם utm. לעולם לא טקסט חשוף.
- **סקילים:** נהלים חוזרים יושבים ב-`skills/`. פרומפט שמציין
  `Skills: skills/<שם>/SKILL.md` — קרא אותו לפני שאתה מתחיל.

## 13. מלכודות קוד ידועות

- **Tailwind JIT** — אין אינטרפולציה דינמית של class. כל ענף חייב להיות מחרוזת ליטרלית עם hex. `` `text-${color}-500` `` לא ייבנה.
- **מחרוזות load-bearing** — ערכי `setup` / `emotion` / `market` ב-`src/data/tradeOptions.jsx` מושווים ישירות ב-`DecisionCoach`, `cleanTrades`, `purgeInvalidTrades`, `VALID_EMOTIONS`. **לא משנים `value` בלי אודיט מלא.**
- **מקור-אמת-אחד** ל-`edge` / `capital` / `VALID_*`. חישוב inline = סחיפה.
- **אנטי-flicker** — קומפוננטות ב-module scope + `memo`. הגדרת קומפוננטה בתוך render מרנדרת את העץ מחדש בכל keystroke.
- **back-compat ב-`parseChartResult`** — רק הוספת שדות חדשים. שינוי או הסרה שוברים עסקאות קיימות.
- **`src/index.css` §override (~L86–100)** — ממפה מחדש utilities של צבע טקסט ל-CSS variables. במסכים כהים העדף צבע inline.

## 14. מרשם המשימות

**פריט חי בקובץ אחד בלבד. מעבר בין קבצים = מחיקה משם והוספה כאן, ⛔ לא העתקה.**

| קובץ | מה חי בו |
|------|-----------|
| `docs/NEXT.md` | **ההמלצה — 3 פריטים בדיוק**, ≤40 שורות. מזהה · למה **עכשיו** · דרך יישום · חוסם · **מדד סגירה**. |
| `docs/INBOX.md` | **קליטה גולמית** — תאריך · מה · מקור. ⛔ פריט אינו שורד סשן. |
| `docs/STATE.md` | 🔴 עכשיו · ⏭️ 3 הבאים · ⏸️ חסום · ⚠️ סיכונים. **תקציב 16,000 בתים** (`wc -c`). |
| `docs/BACKLOG.md` | כל פריט פתוח שאינו פעיל. אין תקרה. |
| `docs/DONE.md` | הושלם — מזהה · תאריך · **hash** · **איך אומת** · `T`. |
| `docs/CHECKS.md` | בדיקות ידניות חוזרות — **תדירות או תנאי-גלישה** לכל פריט. |
| `docs/METRICS.md` | **מדידות עם תאריך-תפוגה** (`M-nnn`) — ערך **עם מכנה ועם שם האוכלוסייה** · **הפקודה שמדדה** · `נמדד` · `תוקף` בימים. ⛔ **אינו קובץ משימות.** |
| `docs/audits/STATE-ARCHIVE-*.md` | **סגור בלבד.** |

⚠️ **`NEXT` ו-`INBOX` ⛔ אינם קבצי מזהים.** `NEXT` **מצטט** `B-` קיים ו⛔ אינו
מפרט — פרט שנכתב בו הוא עותק שני שייסחף (`R-6`). `INBOX` הוא מה ש**טרם** קיבל
מזהה. ⛔ **`BACKLOG` נשאר בלי סדר בכוונה** — ההכרעה חיה ב-`NEXT` ובשום מקום אחר.

🆕 **`METRICS` (26.08) הוא הבית של מה שאינו משימה ואינו הכרעה — מדידה.** הוא נבנה
כי כלל 08.08 («מספר שנרשם פעם ולא נמדד שוב ⛔ אינו עובדה») היה **פרוזה בלבד**:
המספרים ישבו ב-`STATE` ובדוחות `audits/`, התיישנו בשקט, והמשיכו להיקרא כאילו
נמדדו היום. ⚠️ **המקרה שהכריח:** «0 עסקאות ב-70.4 שעות» חי ב-`INBOX` — קובץ
שמצהיר «⛔ פריט לא נשאר כאן מעבר לסשן» — ו-`STATE` **ציטט אותו משם** ⇒ ציטוט
שמצביע לקובץ שמוחק את עצמו (`R-5`). `test:registry` §12 מחשב את ההתיישנות
**מהתאריך בשורה** ומחייב `⏳ פג`. ⛔ **התיקון ל-`⏳ פג` הוא מדידה חוזרת, ⛔ לעולם
לא הארכת `תוקף`** — הארכה כדי לעבור היא `R-4`, בדיוק כמו תקרת ה-100 שורות.
⚠️ **מדידה חוזרת מעדכנת את השורה במקום ומורידה את הערך הישן לשושלת** — ⛔ אינה
פותחת `M-` חדש, אחרת המגמה אובדת.

⚠️ **מי שגוזם את `docs/STATE.md` מעביר ל-`BACKLOG` או ל-`DONE` — ⛔ לעולם לא
ל-`ARCHIVE` אם הפריט פתוח.** גיזום כזה איבד 33 פריטים פתוחים (`B-051`…`B-083`),
שחלקם המתינו מ-01.08 והתגלו רק ב-12.08 כי הארכיון נקרא כ"סגור".

⛔ **מזהה לא נמחק ולא ממוחזר לעולם.** מזהים יציבים (`B-001`·`D-001`·`C-001`) הם מה
שמאפשר לצטט פריט בפרומפט **בלי מספר שורה** — מספרי שורות בקובץ עם תקרה נודדים.

**מה חייב לקרות ומתי:**

| מפעיל | מה נדרש |
|--------|----------|
| **תחילת כל סשן** | **triage ל-`docs/INBOX.md`** — כל שורה הופכת ל-`B-` עם `‹R-n›` ומקור · **או** ל-`M-` ב-`METRICS` אם היא **מדידה** ולא משימה · **או** נדחית עם נימוק ב-`DECISIONS`. ⛔ **אין רביעי**, ו⛔ שורה שנשארה = הפריט חוזר לחיות בזיכרון של ניב (`R-5`) |
| בלוק §registration בכל פרומפט | שורה מפורשת **"מה עלה בשיחה ואינו ברפו"** ⇒ `BACKLOG` |
| **גל נסגר** | ⚠️ **עדכון `docs/NEXT.md`** — גל שלא עדכן אותו השאיר את הצ'אט הבא בלי כיוון, ו-**112 פריטים לא-סגורים** (83 פתוח · 18 חסום-על-ניב · 11 ממתין-לאות · מתוך 129 מזהי `B-`, נמדד 13.08) בלי סדר הם מה שאילץ לפתוח אותו |
| גל נסגר | שורה ב-`DONE` עם **hash ואיך אומת**. ⚠️ קומיט אינו יכול לשאת את ה-hash של עצמו ⇒ הגל נרשם ב-`DONE` **בקומיט הבא** |
| גל נוגע במסך פונה-משתמש | פריט ב-`CHECKS` (`C-001`) **לפני** שהגל נסגר |
| לפני כל push | `npm run test:registry` — חוסם |

⚠️ **המרשם הראשון (02.08) נכשל בגלל כפילות ובגלל היעדר שער מכני, ⛔ לא בגלל
קיום.** `STATE` מתעדכן כי §10.1 מכריח; למרשם לא היה מה שיכריח. `test:registry`
הוא ההכרח הזה.

⛔ **קובץ-מרשם מחוץ ל-`docs/` אסור — נאכף** ב-`test:registry` §10. `.md` בשורש
שצבר שורות `| X-nnn |` או כותרת מרשם הוא מרשם שני, ⛔ ותשע האסרציות הראשונות
אינן יכולות לראותו (`B-131`, נמדד 13.08).

⛔ **`|` בתוך תא טבלה במרשם — `\|` תמיד.** גרש-בודד ב-`` `| פתוח |` `` שובר את
התא לשניים ומזיז עמודות בשקט. השער תפס זאת **פעמיים בשבוע** (`M-018` · `C-038`,
04.09) ⇒ תבנית, ⛔ לא מקרה.

## 15. סיווג משימה — T1 · T2 · T3

**סיווג משימה — חמש שאלות בינאריות, בתחילת כל משימה:**
1. **הפיכות** — פעולה שקשה/בלתי-אפשרי לבטל? (כתיבת DB · מייל יוצא · מחיקה · פרסום)
2. **אמון** — משתמש יקבל מספר/תוצאה שגויים בלי לדעת? (כסף · סטטיסטיקה · המלצת מאמן)
3. **אבטחה וגישה** — `auth` · RLS · secrets · `api/` · קלט לא-מסונן?
4. **רוחב בקוד** — >קובץ **קוד** אחד · מסלול כתיבה (ידני/ייבוא/OCR) · מודול שצרכנים רבים תלויים בו? ⚠️ `docs` ⛔ אינם נספרים כאן.
5. **ודאות** — הנחה שלא נמדדה ב-48 שעות?

| כן | רמה | אבחון-קדם | אימות-אחרי |
|----|-----|-----------|-------------|
| 0 | T1 | קריאת הקוד הרלוונטי | אסרציות + verify |
| 1 | T2 | **מדידת כל הנחה** — בדוח | + אסרציית **ערך** שנצפתה אדומה |
| 2+ | T3 | אבחון read-only → audits/ → עצירה לאישור (§8.1) | + **אימות עין/דפדפן** — ⛔ אין סגירה בלעדיו |

· Code מדווח "רמה: T_ · תשובות: _/_/_/_/_" ⛔ לפני שורת קוד ראשונה.
· ⚠️ ספק ⇒ "כן". **שאלות 1·3 הן טריגר-יחיד: "כן" באחת ⇒ `T3` לבד.** ⛔ רמה לא יורדת תוך משימה; עולה — עם דיווח.
· הרמה נקבעת **מהתשובות**, ⛔ לא מ"כמה זה מסובך".
· T3: אימות עין יכול ע"י Code+Chrome — פותח את האתר, מצלם,
  **מחשב את המספר בעצמו** (חיבור, ⛔ לא "נראה סביר"), ניב
  מאשר צילום. בלי דפדפן — ניב מצלם.
· **`T3` בלי מסך מושפע ⇒ האימות הוא קריאת התוצר מ-`origin`** (⛔ לא מהעץ המקומי) + `SELECT`/מדידה חוזרת עצמאית. סמן ב-`DONE`: **`אומת-מקור`** במקום "עין".

**כותרת כל פרומפט נושאת "רמה משוערת: T_"** — הערכת ניב. Code מאמת מול חמש
השאלות ו**מתקן בדיווח** אם היא שגויה; ⛔ ההערכה בכותרת אינה מחליפה את הסיווג.
כל שורת `DONE` נושאת עמודת `T`, נאכף ב-`test:registry` §11.

## 16. הפניות

- **כללי קוד 1–11** (`t.side` ולא `t.direction`, `calcTradeMetrics` ולא `trade.pnl`, NAV_KEYS…) → `CONTEXT.md` §Coding Rules
- **אימות deploy** (למה לא `curl`, אימות מובייל ב-iframe 390px, Atom feed לאימות hash) → `CONTEXT.md` §Working procedures
- **שחזור DB · קמפיין מייל · התקנת hooks** → `docs/RUNBOOK.md`
- **6 אירועים ולקחיהם** → `docs/INCIDENTS.md`
