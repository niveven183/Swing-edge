# AGENT-CHANNELS — מפת הערוצים

> ⚠️ **הקובץ הזה הוא מפה, ⛔ לא תשתית חדשה.** ניב ביקש "סוכן זמין"; המדידה
> אומרת ש**כבר יש**: Claude Code הוא המקודד · **הריפו הוא המוח** —
> `CLAUDE.md` + `CONTEXT.md` + `STATE` + `NEXT` + `BACKLOG` + `DONE` + `CHECKS`
> + `DECISIONS` נטענים בכל סשן · והאפליקציה במובייל היא הגישה.
> **מה שחסר אינו סוכן — אלא לדעת מה כל ערוץ עושה ומה לא.** זה כאן.
>
> ⚠️ **סשן אחד על הריפו, בכל רגע.** מובייל = קליטה ל-`docs/INBOX.md` · קריאת
> `docs/NEXT.md` / `docs/STATE.md` · שאלות.
> ⛔ **אף פעם `push` בזמן שסשן אחר פתוח** — שני סשנים במקביל הם מה שהוביל
> ל-force-push (`DECISIONS` 2026-07-26).

נמדד: 13.08 · 17 workflows · 12 מהם `schedule:`

---

## הערוצים

| ערוץ | מה עושה **היום** | כיוון | מה חסר | פריט קשור |
|-------|-------------------|--------|---------|------------|
| **Discord webhook** | 14/17 workflows + `api/notify.js` + `api/send-invites.js` שולחים אליו | **חד — יוצא** | ⛔ **אין קליטה חזרה.** הודעה בדיסקורד אינה יכולה להגיע לריפו | `B-130` |
| **Gmail (`MAIL_USERNAME`)** | 9/17 workflows שולחים דוח פרטי — הערוץ **היחיד** שמותר לו נתוני משתמש (הריפו ציבורי) | **חד — יוצא** | ⛔ **אין קריאה.** תיבת הדואר נצברת ואיש אינו מנתח אותה | `B-130` |
| **Cowork #7 Dispatcher** | ⛔ **אינו רץ** — רץ מחוץ לריפו, אפס קוד כאן מפנה אליו (`CONTEXT.md` §Cowork) | — | הוא **התוכנית** לקליטה: קורא מייל ⇒ מנסח פרומפט כטיוטת Gmail, ⛔ אינו מבצע | `B-130` |
| **פידבק באפליקציה** (`api/feedback.js`) | הערוץ ה**נכנס היחיד** שקיים — משתמש ⇒ DB ⇒ פאנל | **חד — נכנס** | הכרעה: 5/8 יושבים ב-`Reviewed` בלי אחד משלושת מצבי §10.2 | `C-011` · `B-070` |
| **Sentinel** | `:20` ו-`:50` בכל שעה — עסקת QA `SNTNL` נגד פרודקשן | חד — יוצא | ירוק ב-11.08; ⛔ לא נמדד מאז | `C-008` |
| **Fleet Daily** | 06:00 UTC · **3 שלבי סוכן**: Cost Watchdog · Growth Pulse · Watchdog liveness | חד — יוצא | ⚠️ **המספר שנרשם בפרומפט (5) ⛔ אינו מה שנמדד** | — |
| **Fleet Weekly** | ראשון 07:00 UTC · **שלב אחד**: Weekly Vitals | חד — יוצא | — | — |
| **Failure Alert / Watchdog** | שכבת ההתרעה על **כל** האחרים | חד — יוצא | 🔴 11/17 בלי צופה · Watchdog מודד **התיישנות ולא כישלון** | `B-001` · `B-002` · `B-018` |
| **מובייל** | האפליקציה עצמה + הכתבה ל-`INBOX` דרך סשן פתוח | דו — דרך ניב | ⛔ **אין כתיבה אוטומטית לריפו מהטלפון**, וזו תכונה | `docs/INBOX.md` |
| **Hive agents (Python)** | ⛔ **פורקו 06.08** — 5 קבצים נמחקו, אפס מייבאים | — | ⛔ אין. רשום כאן כדי שלא ייבנה שוב מזיכרון | `docs/archive/HIVE_ARCHITECTURE.md` |

---

## מה המפה אומרת

⚠️ **9 מתוך 10 הערוצים יוצאים.** המערכת יודעת **לדווח** ואינה יודעת **לקלוט** —
ולכן כל ממצא שנולד בדיסקורד או במייל נשען על כך שניב יזכור. `docs/INBOX.md`
הוא הקליטה הידנית שסוגרת את הפער **בלי שרת לתחזק**; Dispatcher/IMAP ייבנה רק
כשה-`INBOX` יוכיח נפח (`DECISIONS` 13.08).
