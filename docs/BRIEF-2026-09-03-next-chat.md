# BRIEF — 03.09 · מסירה לצ'אט הבא

> 🔴 **הקובץ הזה ⛔ אינו משכפל את `docs/STATE.md` — הוא מפנה אליו.**
> מצב חי · שלושת הבאים · חסום · סיכונים ⇒ **`docs/STATE.md`**. פירוט ⇒ `BACKLOG`.
> הנמקה ⇒ `NEXT`. אימות ⇒ `DONE`. ⛔ **ערך שקיים שם ⛔ אינו נכתב כאן** (`R-6`).
>
> ⚠️ **שם הקובץ ⛔ אינו `HANDOFF` בכוונה** — `HANDOFF*.md` חסום ב-`.gitignore:10`
> וב-`.githooks/pre-commit` (`grep -E '(^|/)HANDOFF[^/]*\.md$'`), ו-`--no-verify`
> אסור (`CLAUDE.md` §5 סעיף 2). **הבקשה הייתה `docs/HANDOFF-2026-09-03.md` — היא ⛔ לא
> ניתנת לקומיט.** ⇒ אותו תוכן, שם אחר. ⛔ **הכלל ⛔ לא נעקף.**
>
> ⚠️ **הקובץ הזה הוא זמני-לסשן ו⛔ אינו קובץ מרשם** (§14 מונה שמונה, וזה ⛔ לא אחד
> מהם). ⛔ **⛔ אין לצטט ממנו מספר** — כל מספר כאן נושא את מקורו, ⇒ קרא את המקור.

---

## 1 · הסדר הנעול: `B-186` → **ואז** ההדגמה

**`B-186` ראשון. ההדגמה (מסלול העיצוב) ⛔ אינה עוקפת אותו.**

- מקור: `DECISIONS` 27.08 ⓶ — «**הכנסת `B-246` ל-`NEXT` על חשבון
  `B-184`/`B-185`/`B-186` — נפסלה מפורשות**». P0 של כסף/אבטחה קודמים לעיצוב.
- `NEXT` מקום 1 = `B-186` (`W1b`). מסלול העיצוב חי ב-`STATE` תחת 🎨 ו⛔ **אינו**
  ב-⏭️ — **בכוונה**.
- ⚠️ **מה שהצ'אט הבא עלול לעשות ⛔ ולא צריך:** לקרוא את שלושת התנאים המקדימים
  להדגמה (למטה) כ«שלוש משימות שממתינות» ולהתחיל בהן. **הן ⛔ אינן ממתינות —
  הן חסומות מאחורי `B-186`.**

---

## 2 · שלושת התנאים המקדימים להדגמה — **מספרי שורות נמדדו 03.09**

`C-037` ⛔ **אינו יכול לרוץ** לפני שהשלושה נמדדים; הם הקלט ל-⓵ («מה מרונדר שם
עכשיו»). הסדר הוא הכרעת ניב 27.08: **`B-254` → `B-246` → `B-245`**.
⚠️ **מספר שורה הוא עותק שני של מיקום** (`B-280`) ⇒ נמדד מחדש כאן, ⛔ לא הועתק.

| # | פריט | מיקום — **נמדד 03.09** | מול הרשום ב-`BACKLOG` |
|---|------|------------------------|------------------------|
| ⓵ | `B-254` — `Inter` inline שובר RTL | `SwingEdge_App.jsx:3979` (מעטפת) · `:3959` (מסך טעינה) · `src/index.css:67` | **`3/3` מדויקים** |
| ⓶ | `B-246` — ⛔ אין דירוג ויזואלי | `SwingEdge_App.jsx:954` (`StatCard`) · `:972` · `:4416` (`lg:grid-cols-6`) | **`3/3` מדויקים** |
| ⓷ | `B-245` — ניגודיות AA | `src/design/tokens.css:29` (`--text-muted: #9CA89A`) · `:176` (המערכת השנייה) · `src/priceService.js:445` (`#10b981`) · אתרי badge `:4166`·`:5438`·`:7709`·`:8252` · מחירים קשיחים `:6749-6750` | **`9/9` מדויקים** |

🔴 **אזהרה מדודה לצ'אט הבא — `R-3` שכמעט הפלתי כאן היום:**
`grep -n "badge.color"` החזיר **3** אתרים ⛔ ולא 4, ו-`:8252` נראה «נסחף».
הוא ⛔ **לא נסחף** — האתר הרביעי נושא שם משתנה אחר (`fBadge.color`), ו-`grep`
מדד **מחרוזת** במקום **תפקיד**. ⇒ **⛔ אל תסיק סחיפה מ-`grep` שלא הוצלב לקוד.**
זו בדיוק מחלקת ההפרכה `23` (`AUDIT-refutations-2026-08.md`).

---

## 3 · מסלולי הדריסה של `W-CAP` — ו«`verify` ⛔ אינו מוכיח את החיווט» **כלשונו**

**⛔ הקוד מתוקן ונדחף** (`6fd3106`). מה שפתוח: `C-036` (אימות עין) · `D-063`
(שורה שמורה וריקה) · `B-286` (חוב הרישום של `P7`). הפירוט ⇒ `INCIDENTS#19`.

| מסלול | אתר | מה עשה |
|-------|-----|--------|
| א | `src/lib/userSettings.js:109` | `if (!error && data && data.settings)` — קריאה כושלת נפלה למראה של `localStorage` ו**זרעה בו את מטמון המודול** |
| ב | `SwingEdge_App.jsx:1724` | `if (!s) { hydratedRef.current = true; … }` — **סמכות כוזבת**: ההידרציה נכשלה והשער נפתח בכל זאת ⇒ אפקט ההתמדה כתב את ברירת המחדל מעל הערך האמיתי |
| ג | `src/lib/userSettings.js:160` | `migrateFromLocalStorage` באותה צורה ⇒ בדיקת-קיום שגתה בנתה blob מ-`localStorage` ו-`upsert` מעל שורה **קיימת** |
| **`P7`** | `src/lib/userSettings.js:145` → היום `:192` | `flushSettings` קרא ל-`upsertBlob` **ללא תנאי** ⇒ סגירת טאב כתבה את המראה (או `{}`) מעל השורה |

⚠️ **המנייה ⛔ אינה אחידה בין שני המקורות, וזו הבחנת-אוכלוסייה ⛔ ולא סתירה:**
`INCIDENTS#19` קורא ל-`P7` «**third** path» (מונה **מסלולי כתיבה**: ב · ג · `P7`);
`AUDIT-refutations` §7.3 קורא לו «מסלול דריסה **רביעי**» (מונה גם את א, שהוא
**הרעלה** ⛔ ולא כתיבה). ⇒ **אותה עובדה, שני מכנים.** ⛔ **⛔ אל תתקן אחד מהם
בלי לומר איזו אוכלוסייה נמדדת** — זה `CLAUDE.md` §2.

🔴 **«`verify` ⛔ אינו מוכיח את החיווט» — כלשונו, מ-`AUDIT-refutations-2026-08.md:470`:**

> ⛔ **«`verify` ⛔ אינו מוכיח את החיווט» — כלשונו:** `test:settings` מוכיח את
> **חוזה המודול** בלבד; `build` מוכיח **קומפילציה בלבד**. ⇒ `B-272` ⇒ `test:hydration`
> (`D-064`), שמכסה 3 `useEffect` ⛔ ולא JSX.

⚠️ **ומה ש-`test:hydration` ⛔ אינו מוכיח** (`CLAUDE.md` §7): הבאנר · שני
ה-`onClick` · RTL/ניגודיות · React עצמו · דפדפן אמיתי · פרודקשן — **`C-036`
בלבד**. `5/19` האסרציות הן ⚪ אינווריאנטות שירוקות ב**שני** העצים ⇒ ⛔ **אינן
ראיה לתיקון**; `3/14` («`A7`·`A8`·`A9`») אדומות בעץ הישן מפני ש**חוזה התלות
המוזרקת השתנה**, ⛔ לא מפני שאותו קלט מייצר התנהגות שונה.

---

## 4 · מה הצ'אט הבא ⛔ **לא** יֵדַע מ-`STATE` לבדו

`STATE` נגזם ל-**15,986/16,000 בתים** (`14` פנויים). ⇒ הדברים הבאים **⛔ אינם
נגזרים ממנו**, וכל אחד מהם נושא כאן **מקור** ⛔ ולא ערך:

1. 🔴 **`STATE` ⛔ אינו יכול לקלוט עוד שורה.** `14` בתים. ⇒ הגל הבא **חייב** לגזום
   **עותק** לפני שהוא כותב. ⛔ **אל תעלה את התקרה** — היא `DECISIONS` 26.08, והתקרה
   הקודמת (שורות) איבדה **33 פריטים פתוחים**.
2. 🔴 **`D-063` שמור וריק** ל-`W-CAP`, ו-**`B-287`** (חסום-על-ניב) מודד `0/4` גלים
   בלי שורת `DONE`: `d6ad695` · `b866d30` · `950f279` · `8452a06`. ⛔ **⛔ אל תמציא
   ארבע שורות רטרואקטיביות** — ההכרעה של ניב חסרה, וזה מה שחוסם.
3. ⚠️ **`B-285` הוא הישנות של `B-279` הפתוח** — לא ממצא עצמאי. שני כלי-מדידה,
   שני קבצים, **5 ימים** ⇒ **«נפתח פריט» ⛔ אינו מדד הגנה.** הכלל («כלי-מדידה נכשל
   קשה על ארגומנט שאינו מובן») חייב לחיות ב**שער** (`B-284`) ⛔ ולא בזיכרון.
4. ⚠️ **`B-284` פתוח: `0/25` חוליות `verify` קוראות `.github/workflows/`.** ⇒ תיקון
   `B-277` (`age_s`) **⛔ אינו מוגן ברגרסיה**. מי שנוגע ב-workflow ⇒ ⛔ אין שער.
5. ⚠️ **הרתם `/tmp/b277-harness.sh` ⛔ אינו בריפו ו⛔ אינו בשרשרת `verify`.** הוא
   דורש שורש מפורש (`$1`) ⛔ ואין לו ברירת מחדל — **בכוונה**, זה `B-285`.
6. ⚠️ **`C-037` ⛔ מעולם לא רץ.** ⛔ אין לרשום לו «עבר» על סמך האודיט שהוליד אותו —
   `AUDIT-design-failure` §7 מצהיר שכל מדידותיו על **המקור** ⛔ ולא על **המסך**.
7. ⚠️ **`R-7` נפסל** (`DECISIONS` 29.08, `D1`) — `R-3` מכסה. ⛔ **אל תפתח שורש שביעי.**
8. ⚠️ **תג `‹R-n›` בשורת `BACKLOG` ⛔ אינו ציטוט — §8.3 קוראת אותו כ**חברוּת**.**
   התיקון המתועד הוא **הסרת התג** (תקדים `B-168`·`B-267`·`B-273`·`B-276`),
   ⛔ **לעולם לא הוספת הפריט לרשימת החברים כדי לעבור** — זה `R-4`.
9. ⚠️ **`|` חשוף בתא של `BACKLOG`/`DONE` מזיז עמודות בשקט** — §13.2 מפצל על
   `/(?<!\\)\|/`. חובה `\|`. תפס פעמיים בסשן הזה.

---

## 5 · הצעד הראשון של הצ'אט הבא

**קרא `docs/STATE.md` · `docs/NEXT.md` · ואז `docs/BACKLOG.md` לפריט שנבחר.**
⛔ **אל תתחיל מהקובץ הזה** — הוא נספח, ⛔ לא מקור-אמת.
`npm run test:registry` לפני כל push (38 אסרציות, חוסם).
