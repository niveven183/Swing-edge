# CHECKS — בדיקות ידניות חוזרות

> **חוזה שבעת הקבצים — פריט חי בקובץ אחד בלבד.**
>
> | קובץ | מה חי בו |
> |------|-----------|
> | `docs/NEXT.md` | **ההמלצה — 3 פריטים בדיוק**, ≤40 שורות. **מצטט** `B-` קיים ו⛔ אינו מפרט. |
> | `docs/INBOX.md` | **קליטה גולמית** — תאריך · מה · מקור. ⛔ פריט אינו שורד סשן. |
> | `docs/STATE.md` | 🔴 עכשיו · ⏭️ 3 הבאים · ⏸️ חסום · ⚠️ סיכונים. **תקציב 16,000 בתים** (`wc -c`) — ⚠️ **תוקן 27.08: הנוסח «תקרת 100 שורות» כאן היה שריד ל-`D-054`** (26.08), שהחליף שורות בבתים מפני ש-`wc -l` החזיק `100→100` בזמן שהתוכן גדל ×9.2. `B-266` |
> | `docs/BACKLOG.md` | כל פריט פתוח שאינו פעיל. אין תקרה. |
> | `docs/DONE.md` | הושלם — מזהה · תאריך · hash · איך אומת · **`T`** (רמת הסיווג, `CLAUDE.md` §15). |
> | `docs/CHECKS.md` (כאן) | **בדיקות חוזרות** — מה · תדירות **או** תנאי-גלישה · מתי נבדק · מה נמצא. |
> | `docs/audits/STATE-ARCHIVE-*.md` | **סגור בלבד** — ⛔ פריט פתוח לעולם לא נוחת שם. |
>
> מעבר בין קבצים = **מחיקה משם והוספה כאן**, ⛔ לא העתקה.
> ⛔ **מזהה לא נמחק ולא ממוחזר לעולם.** ⛔ **קובץ-מרשם מחוץ ל-`docs/` אסור.**
> ⚠️ **לכל פריט חייבת להיות תדירות או תנאי-גלישה.** בדיקה בלי מפעיל אינה בדיקה — היא כוונה.
> נאכף מכנית ב-`npm run test:registry` (חוליה 23).

---

## למה הקובץ הזה קיים

⚠️ **11.08: 105 אסרציות ירוקות וגל שלא עבד.** האימות בעין חי רק בראש של Claude —
ולכן לא קרה. `D-011` ב-`docs/DONE.md` הוא הראיה.

⚠️ **בקשת `omrikapara1` ממתינה מאז 31.07 כי שום דבר לא מעיר אותה.** `C-011` הוא
תנאי-הגלישה שהיה מעיר.

---

## 🔴 אחרי גל — תנאי-גלישה

| מזהה | מה בודקים | תדירות / תנאי-גלישה | נבדק לאחרונה | מה נמצא |
|------|-------------|----------------------|---------------|----------|
| C-001 | **צילום מסך של המסך המושפע.** ⛔ הגל אינו נסגר בלעדיו | תנאי-גלישה: **כל גל שנוגע במספר פונה-משתמש** | 11.08 22:35 | הבאג חי בשני אתרים נוספים + `posSize` גדול פי-שער ×4 — אחרי 105 אסרציות ירוקות |
| C-002 | אימות מובייל ב-iframe 390px | תנאי-גלישה: כל גל שנוגע ב-`MobileTradeCard.jsx` או ב-layout | 10.08 (גל האופק) | נקודת האופק מוצגת בשני מסלולי הרינדור |
| C-003 | אימות hash הפריסה מול Atom feed (⛔ לא `curl`) | תנאי-גלישה: כל push ל-`main` שמשנה `src/` או `api/` | 12.08 (`636b490`) | תואם |
| C-004 | SELECT אימות אחרי מיגרציה | תנאי-גלישה: **כל הרצת `.sql` ידנית של ניב** | 09.08 (`b69b295`) | ⚠️ `b69b295` **לא הורץ** — המכנה המסונן ב-`admin_rpcs` עדיין לא בפרודקשן |
| C-005 | מדידה חוזרת של מטריקות חיות לפני דיווח כל שיעור | תנאי-גלישה: **לפני כל מספר שנמסר בדיווח** (`CLAUDE.md` §2 — אין מנה בלי מכנה) | 12.08 | 46 auth.users · 60 עסקאות · 14/60 תווית מטבע שגויה |
| C-021 | **ראיות האירוע נאספו באותו יום** — לוג Vercel · run id · צילום · פלט SELECT, מודבקים ב-`docs/INCIDENTS.md` **לפני** שהיום נגמר | תנאי-גלישה: **כל אירוע פרודקשן/CI** | 13.08 (נכתב) | ⏳ טרם הופעל. **הנימוק נמדד:** שימור הלוגים של Vercel הוא **~24 שעות ולא 7 ימים** — 24h ו-7d החזירו כמעט אותם מספרים (152 מול 151 `/api/quote`, `B-083`) ⇒ ראיה שלא נאספה באותו יום ⛔ אינה קיימת |
| C-022 | **אחרי גל על מסלול כתיבה — ⛔ שני** המסלולים מיושרים, ⛔ לא רק זה שתוקן: ידני (`SwingEdge_App.jsx`) · ייבוא (`src/import/normalizeRow.js`). ⚠️ **הניסוח תוקן ב-16.08:** הגרסה הראשונה מנתה **שלושה** וכללה את `api/ocr.js`. זו הייתה טעות סיווג ⛔ ולא פער — OCR ⛔ **אינו כותב ל-`trades`**; הוא ממלא טופס שהמסלול הידני שומר. מסלול קדם-המילוי עבר ל-`C-027` | תנאי-גלישה: **כל גל שנוגע במסלול שכותב ל-`trades`** | 16.08 (`B-129`) | ✅ **מיושרים.** שניהם חותמים `source` **וגם** `currency_source`: ייבוא — ארבע דרגות מוצהרות (`broker_arithmetic`/`file_cell`/`account_default`/`literal_fallback`); ידני — `manual_capital`. אומת: `test:import` בלוק 33 (20 אסרציות · V8 מודדת 212/212 שורות) · `test:write` 26–29 🆕 **ואומת בפרודקשן 16.08 (`D-040`), ⛔ לא רק באסרציות:** ניב הריץ את **2/2** המסלולים וקרא מה-DB — הידני `manual`+`manual_capital`, הייבוא `import`+`broker_arithmetic`/`file_cell` ב-**2 מנות נבדלות** ⇒ **4 ערכי תעודה נבדלים** על אותה תווית `USD`. שורות הבדיקה נמחקו. |
| C-027 | **OCR הוא קדם-מילוי, ⛔ לא מסלול כתיבה** — ובגלל זה הוא צריך בדיקה משלו: הוא ממלא שדות שהמסלול הידני יחתום עליהם `manual_capital`, כלומר **הנחה שנוצרה ממסך שהמשתמש צילם**. בודקים ששני מסלולי ה-OCR (קובץ · לכידת מסך) גוזרים את המטבע מהטיקר שחזר **ומצהירים** כשלא נגזר, ⛔ ושההצהרה אינה חוסמת שמירה | תנאי-גלישה: **כל גל שנוגע ב-`api/ocr.js`, ב-`handleImageUpload`, או ב-`routeChartOcr`** | 16.08 (`B-129`) | ✅ **נולד עם הפיצול מ-`C-022`.** מכנית: `test:instrument` בלוק 16 — 2/2 מסלולים גוזרים · טיקר מספרי (`604611`) ⇒ `numeric_ticker` ⇒ מצהיר · `NFLX`/`EURUSD` ⇒ ⛔ שקט · ⛔ אין `disabled` שתלוי בהצהרה. ⚠️ **מכסה טיקר מספרי בלבד** — חוזה ה-JSON של Vision נשאר פתוח ב-`B-080`, חסום על `B-110` ❄️ |
| C-023 | **כותרת החשבון נכונה אריתמטית אחרי מעבר מטבע** — הון + P&L חי **באותה יחידה**, בכל אחד מארבעת מצבי (מטבע-הון × מטבע-תצוגה). ⚠️ הבדיקה היא **חיבור**, ⛔ לא "נראה סביר" | תנאי-גלישה: **כל גל שנוגע ב-`openPnL` · `curEquity` · `accountAmount` · `fxPairPlan`** | 15.08 (אחרי `B-142`)  | ✅ **בוצעה 15.08 — התוצאה חיה ב-`D-032` ו-`D-033`**, ⛔ ולא כאן: תוצאה שנכתבת פעמיים נסחפת ‹R-6›. שם: 4/4 המצבים ב**חיבור** 🔁 **ירתה שוב על ההון הסגור, 3+1 מצבי-סירוב שנבדקו בדפדפן בחיבור:** **S5** (אין טבלה כלל) → `₪10,000.00` = 10,000 בסיס **גולמי ב-₪** + **0** (AAPL 100$ מוחרג, `no_table`) + **0** (MSFT פתוח, אין מחיר חי); התג `1/1` פתוחות + `1/1` סגורות. · **S7** (יש fixing ליום, אין spot נדרש) → `₪20,200.00` = 10,000$×2.0 + 100$×2.0; AAPL בעל `currency:"ILS"` שגוי מוחרג `unverified_instrument`; תג `1/2`; תשואה 200/20,000 = **1.0%**. · **S8α** (טווח חסום, spot חי) → `₪20,200.00`; MSFT שנסגר 2026-04-15 מוחרג `no_rate_for_day` — **מעבר להליכה-אחורה בת 7 ימים**; תג `1/2`. 🔴 **S8γ — באג שהתכנון ⛔ לא צפה, נמצא רק על המסך:** אין spot + **יש** fixing ליום הבסיס ⇒ ההכרעה **מצליחה** ומחזירה ₪, בעוד `fxOk` שקר ⇒ `dispCcy` נופל ל-$ ⇒ הכותרת הראתה **`$20,100.00`** = 20,000 **שקלים** ועוד 100 **דולר** תחת "$". ⚠️ **⛔ אינו קצה** — `loadRateTable` מחזיר בדיוק טבלה כזו במסלול ה-`catch`, כי spot ⛔ לעולם אינו נשמר ל-localStorage. תוקן ל-`equityBase = fxOk ? equityBaseD.value : capital`, ואומת על המסך: **`$10,100.00`** = 10,000$ + 100$. `B-142` · `D-033` 🔁 **ירתה שוב 17.08 בגל `B-143`** (תנאי-הגלישה: `curEquity`) — ⚠️ **והמדידה הפכה את המשמעות: `stats.currentEquity` ⛔ אינו מקור הכותרת כלל.** הכותרת מרכיבה `equityBase + closed + live` בעצמה ⇒ 4/4 המצבים נשארו **זהים ל-15.08** ⛔ ולא בגלל שהבדיקה דילגה, אלא כי השדה שתוקן ⛔ אינו נצרך שם. ⇒ אימות-העין של הגל עבר ל-`C-029`, והשדה נשאר **נכון וללא בעלים** (`D-043`) |

---

## 🟠 תשתית — תדירות קבועה

| מזהה | מה בודקים | תדירות / תנאי-גלישה | נבדק לאחרונה | מה נמצא |
|------|-------------|----------------------|---------------|----------|
| C-006 | **Failure Alert** — האם ירה בכלל | שבועי | 11.08 | 🔴 **11/17 workflows אינם מכוסים** · `watchdog.yml:101` מדלג על קבצים עם `workflow_run` בלבד — `B-001` |
| C-007 | **Watchdog** — מודד כשל או רק התיישנות | שבועי | 11.08 | 🔴 מודד התיישנות בלבד · חלון עיוור של 25.1h במקרה הגרוע — `B-002` |
| C-008 | **Sentinel** — ריצה אחרונה ופלט | שבועי | 11.08 | ירוק |
| C-009 | **Restore Drill** — ריצה אחרונה | חודשי | 11.08 | ⚠️ נשען על Watchdog לבדו לגילוי כשל — `B-018` |
| C-010 | **גיבוי DB** — קיים ובר-שחזור | שבועי | 11.08 | קיים · השחזור עצמו לא נוסה מאז |
| C-011 | **פידבק ב-`Reviewed` בלי אחד משלושת מצבי §10.2** | תנאי-גלישה: **כל מעבר על פאנל הפידבק** · ובנוסף שבועי | 12.08 | 🔴 **5/8 פידבקים לא נסגרו** · בקשת `omrikapara1` מ-31.07 ממתינה 12 יום · `B-070` (העלאת קבצים) |
| C-012 | הרצה מתוזמנת של `user-analytics` — הושלמה בלי קריסה | יומי (ריצה מתוזמנת) | 10.08 | ירוק מאז `dea4cc1` · לפני כן קרסה בשקט — `INCIDENTS#15` |
| C-024 | ⚠️ **שלוש שורות `DONE` האחרונות — האם הרמה (`T`) תואמת את שלוש השאלות בדיעבד?** קוראים את השורה, עונים על השאלות **מהתוצאה שהתגלתה בפועל**, ומשווים ל-`T` שנרשם. **סיווג-חסר** (המשימה הייתה עמוקה מהרמה שהוכרזה) ⇒ שורה ב-`docs/INBOX.md`. ⚠️ הבדיקה מודדת את **הפרוטוקול**, ⛔ לא את הגל — `T` שגוי אינו מבטל את הגל, הוא מכייל את השאלות | **דו-שבועי** | טרם | ⏳ נולד 13.08 עם `CLAUDE.md` §15. ⚠️ **29 השורות ההיסטוריות נושאות `(רטרו)` ואינן במדגם** — סיווגן שוחזר מהתיאור ולא מהזמן-אמת (`C-024` הראשון ימדוד את `D-030` ואילך) |
| C-025 | ⛔ **`.md` בשורש שצבר מבנה מרשם** — שורות `\| X-nnn \|` **או** כותרת עם "משימות"/"מרשם"/"TASKS"/"BACKLOG". פטורים: `README` · `CLAUDE` · `CONTEXT`. נמצא ⇒ מוזג ל-`docs/BACKLOG.md` ונמחק | **חודשי** | 13.08 | ✅ 3 קבצי `.md` עקובים בשורש, אף אחד אינו מרשם. ⚠️ **`test:registry` §10 חוסם היווצרות חדשה** — הבדיקה הידנית קיימת ל**סחיפה** שהשער מחמיץ: קובץ שצובר מבנה מרשם **בלי** אף אחד משני הסמנים (רשימה עם `-`, ללא טבלה) |

---

## 🟠 מוצר ומדידה

| מזהה | מה בודקים | תדירות / תנאי-גלישה | נבדק לאחרונה | מה נמצא |
|------|-------------|----------------------|---------------|----------|
| C-013 | **הדייג'סט בבוקר — האם המספר תואם למדידה** | יומי | 12.08 | ⚠️ לא הושווה למדידה עצמאית מאז `ac9b38f` |
| C-014 | `git log -1` מול שורת ה-HEAD ב-`docs/STATE.md` | תנאי-גלישה: תחילת כל session | 12.08 | תאם (`636b490`) |
| C-015 | ספירת `waitlist` — ירידה מחייבת חקירה | שבועי | 12.08 | 61 · ⚠️ ירידה מעולם לא נחקרה כי אף אחד לא מדד פעמיים |
| C-016 | משפך GA4 — נתונים מצטברים | תנאי-גלישה: **אחרי 16.08** (שבוע מלא מאז `c4810f1`) | טרם | ⏳ ממתין לאות |
| C-017 | האם מישהו סגר עסקה מאז 29.07 | שבועי | 12.08 | 🔴 **0 סגירות ב-14 יום** · 26/60 סגורות, כולן לפני 29.07 |
| C-018 | שימור שבוע-1 בקוהורטה | שבועי | 11.08 | 🔴 **0/6** · `de66dc99` מעולם לא היה נמען `why_stopped` ⇒ התוצאה האמיתית 5→0→0 |
| C-019 | 🔴 **גל ג׳ — סכום-בחשבון על המסך.** יומן **שקלי** עם עסקה על נייר **דולרי**: (1) שורת היומן בדסקטופ · (2) הכרטיס במובייל · (3) לוח השנה ← מודל היום · (4) הטוסט בסגירת עסקה — כולם מציגים סכום **מומר** בשער **יום המימוש**, ⛔ לא את הסכום הדולרי תחת `₪`. ובנוסף: אין שער ⇒ `—` עם נימוק ב-`title`, ⛔ לא `0` ו-⛔ לא מספר לא-מומר. (5) `POS. VALUE` — **שתי** ספרות אחרי הנקודה, ⛔ לא שלוש | תנאי-גלישה: **לפני שגל ג׳ נרשם כ"נסגר"** | 13.08 | ✅ 4/4 האתרים מומרים בשער יום-המימוש · `POS. VALUE` = ₪774.88 בשתי ספרות · שער `2.9895` עקבי בכל האתרים · אין-שער ⇒ `—` עם נימוק. 🔴 **ליקוי אחד: P&L חי ⛔ אינו מומר ודולף לכותרת החשבון ⇒ `B-119`**. הגל נסגר כ-`D-024` |
| C-020 | **`92a0…` — שורת `BE` אחרי הרצת `20260812150000_be_row_currency_label.sql`**: `currency='USD'` · `entry`/`stop`/`exit`/`shares` **זהים בדיוק** לפלט ה-SELECT שלפני · צ'יפ "מטבע לא מאומת" נעלם מהשורה | תנאי-גלישה: מיד אחרי שניב מריץ את ה-`.sql` | 13.08 | ✅ `currency='USD'` · `entry`/`stop`/`exit`/`shares` **זהים בדיוק** לפלט שלפני · הצ'יפ "מטבע לא מאומת" נעלם · `be_ils=0 · ils_total=0` · 61 שורות. `D-025` |
| C-026 | 🐤 **תרגיל הקנרית — האם רשת ההתראות באמת מוסרת.** מריצים `🐤 Canary Drill` ב-`mode=fail` (T-A) ומוודאים שהגיעה הודעת דיסקורד **וגם** מייל שנושאו נושא את התג 🐤, ושה-`Delivery gate` הדפיס `channels delivered: 2/2`. מיד אחריו `mode=pass` (T-B) — **בקרה שלילית: ⛔ אסור שתיווצר ולו התראה אחת.** ⚠️ שער שיורה על ירוק גרוע משער שקט, כי הוא מאמן להתעלם | חודשי · **ותנאי-גלישה:** אחרי כל נגיעה ב-`failure-alert.yml` · ב-`canary.yml` · או בסוד `MAIL_*`/`SENTINEL_DISCORD_WEBHOOK` ⚠️ **`2/2` ⛔ אינו ראיית מסירה** — הוא HTTP 204 מ-Discord ונושא-מייל שנבנה; שתיהן מדידות של ה**משגר**. **הבדיקה נסגרת רק כשניב מאשר שההודעה נראתה בערוץ שהוא באמת קורא** ‹R-4›: שומר שמודד את הצלחת השליחה של עצמו מודד נוכחות, ⛔ לא הגעה | 15.08 | ✅ **15.08 — נסגר בשתי השכבות.** **(1) מכנית:** 4/4 ריצות תחת `Delivery gate` הדפיסו `channels delivered: 2/2`, ונושא המייל נשא את התג (`🚨 🐤 Canary Drill — התראת-בדיקה נכשל`). **(2) ✅ אומת בעין — ניב צילם את ערוץ הדיסקורד:** **5/5** הודעות קנרית התקבלו, כל אחת עם 🐤 + שם ה-workflow + URL לריצה. זמנים (מקומי, UTC+3): **16:24:59 · 16:29:05 · 16:37:30 · 16:40:32 · 16:51:56**. האחרונה = ריצת `failure-alert` `31888353149` שנורתה מ-canary **`31888345445`** — ה-T-A **החוזרת שאחרי הבלוק האחרון**. ⚠️ **המכנה 5 ⛔ ולא 4:** ההודעה הראשונה (16:24:59, alert `31887147462` על `8fb8b11`) נמסרה **לפני** שנולד `Delivery gate` ⇒ היא ראיית **ערוץ** ⛔ ולא ראיית שער. T-B: 2/2 ריצות `mode=pass` ⇒ `skipped`, 0 צעדים, ⛔ אפס הודעה. 🔁 **בוצעה שוב 17.08 כ-D4 של `AUDIT-alert-incident-1708.md`** — ⚠️ **⛔ לא כמפעיל של הבדיקה** (הקבצים והסודות ⛔ לא נגעו), אלא כדי למדוד אם הרשת חיה **אחרי** אירוע ה-429. T-A: canary **32058659008** אדומה בכוונה ⇒ `Failure Alert` **32058677953** ירוקה · `discord POST -> HTTP 204` · `channels delivered: 2/2`. ⚠️ **שתי השכבות נרשמות בנפרד, כי `2/2` ⛔ אינו «הגיע»:** **(1) משגר — ✅ 2/2.** **(2) ערוץ — ✅ מייל בלבד:** ניב צילם את התיבה **22:08 מקומי**, הנושא `🚨 🐤 Canary Drill — התראת-בדיקה נכשל` ומפנה ל-`runs/32058659008`. **⏳ דיסקורד של הריצה הזו ⛔ לא אומת בעין**, ו⛔ אין לגזור אותו מ-`2/2` — זו בדיוק ההבחנה שהבדיקה קיימת בשבילה ‹R-4›. ⇒ **מסקנת D4: ה-429 התפוגג והרשת חיה כרגע.** ⛔ **ומצב הכשל שגרם לאירוע (`B-163`) ⛔ אינו נסגר בכך** — התרגיל מודד ג'וב ש**התחיל**, והאירוע היה ג'וב שמת ב-`Set up job` |
| C-028 | **המוצר ⛔ אינו ממציא מספר על יומן ריק — ו⛔ אינו מסרב על יומן מלא.** שני חצאים, ו**חצי אחד לבדו הוא כישלון**: (א) **יומן ריק** (חשבון בלי סגורות) ⇒ סרגל הסטטיסטיקה **⛔ אינו מרונדר כלל**, וכל מקום שכן מציג מספר מציג `"—"` ⛔ ולא `0%`/`$0`/`0.00`; קונסולה **נקייה מ-`TypeError`**. (ב) **יומן מלא** ⇒ ⛔ **אף מספר אמיתי לא הפך ל-`"—"`** — מחשבים ביד לפחות מספר אחד מהמסך ומשווים. (ג) **פילטר שאינו מתאים לאף עסקה סגורה** (למשל טיקר שלא קיים) ⇒ הסרגל **נעלם**, ⛔ ואינו מציג 7 תאים ריקים — זה `B-157`, והמסלול היחיד שבו משתמש **פעיל** ראה `EMPTY_STATS`. | תנאי-גלישה: כל נגיעה ב-`EMPTY_STATS` · בסרגל הסטטיסטיקה · או בכל `formatPct`/`fmt$` שקורא `journalStats` | — | ✅ **בוצע 16.08 · `D-041`** — שלושת החצאים נצפו על `/app` בבנייה של `eb8758b`, כל אחד **לפני ואחרי** (הקבצים הוחזרו ל-`19d1dd0` ובחזרה). (א) ריק: `אחוז הצלחה 0% → "—"`, `▲ 0.00%` על ה-P&L **נעלם**, הסרגל ⛔ לא מרונדר, קונסולה 0 שגיאות. (ב) מלא (4 סגורות): 7/7 ערכי הסרגל חושבו ביד והתאימו (`50%` · `+$150` · `-$160` · `0.94` · `-$220` · `2.8d`), ו-**0 מספרים אמיתיים הפכו ל-`"—"`**. (ג) פילטר `ZZZZ`: לפני — 7 תאים של `0`/`$0.00`/`0.00`/`0.0d` מעל "אין עסקה שתואמת"; אחרי — הסרגל **נעלם**. 🔁 **ירה שוב 16.08 בגל `B-160`** (תנאי-הגלישה: כל נגיעה בלוח הסטטיסטיקה — השער ב-`:4824` שונה ל-`journalStats.total > 0`). אומת בעין בשני מצבי השער: פילטר `ZZZZ` ⇒ הסרגל **נעדר** (זהה לפני/אחרי המוטציה) · יומן אמיתי ⇒ הסרגל **נוכח**, התא הראשון (`total`) חושב ביד ותאם. ⛔ אפס מספר אמיתי הפך ל-`"—"`. `D-042` |
| C-029 | **ה-PDF החודשי — הסכום, ההצהרה, ו**ציר** עקומת ההון.** ⛔ **שלושה חלקים, וכל אחד לבדו אינו מספיק:** (א) **הסכום** — `TOTAL P&L` וה-`equity` האחרון נקובים ב-`dispCcy`, ומחושבים ביד מהעסקאות שבטבלה. (ב) **ההצהרה** — כשקיימת ולו עסקה אחת בלתי-ניתנת להמרה, הערת-השוליים מונה אותה **בשמה** (פתוחה-בלי-מחיר · פתוחה-בלי-שער · **סגורה-בלי-שער**) ⛔ ואינה מסתפקת בסכום חלקי שקט. (ג) 🔴 **הציר** — `maxEq`/`minEq` נגזרים **מאותה** אוכלוסייה מוחרגת: מחשבים ביד את `toY` של שתי נקודות לפחות ומשווים ל-`path` שנפלט. ⚠️ **ציר הוא הדבר שאימות-מספר ⛔ אינו יכול לתפוס** — מחובר בלתי-מומר משנה את **צורת** הגרף גם כשהמספר בכותרת כבר תוקן, ובדיוק זה נמדד: רצפת `minEq` הישנה **9,700** הייתה דולרים שהוחסרו מיתרה שקלית. ⛔ **`C-023` ⛔ אינו מכסה זאת** — הוא בודק את **כותרת המסך**, וה-PDF הוא מסלול רינדור **שני** עם `calc` משלו | תנאי-גלישה: **כל נגיעה ב-`exportMonthlyPDF`** — כולל `equityPoints` · `maxEq` · `equityIncomplete` · או ב-`calcFn` המועבר אליו | 17.08 (`B-143`) | ✅ **בוצעה 17.08 — לפני/אחרי מאותו `exportMonthlyPDF` בדיוק** (המצב הישן שוחזר ע"י עטיפת ה-`calc` כך שתפשיט `fxUnconverted`, ⛔ לא ע"י כתיבת קוד ישן מחדש). **לפני:** `₪9,900.00` · `−₪100.00` · צניחת-V · ⛔ **אפס הצהרה**. **אחרי:** `₪10,100.00` · `+₪100.00` · ⚠️ *«Excludes 2 trade(s) from the totals and the equity curve — 2 closed without an FX rate to ILS»*. 🔴 **ו-`CLOSED TRADES 4` + `WIN RATE 50%` ⛔ לא זזו** — פיצול סוגי-המטריקה נראה בפריים אחד: הכסף צר, הספירה והשיעור ⛔ לא. **(ג) שני הצירים חושבו ביד והתאימו תו-בתו:** *אחרי* — 10,000→10,200→10,100, `max=10,200` `min=10,000`, `toY=150−(eq−10000)·0.75` ⇒ `M0.0,150.0 L300.0,0.0 L600.0,75.0` ✓ · *לפני* — 10,000→9,700→9,800→10,000→9,900, `min=`**`9,700`**, `toY=150−(eq−9700)·0.5` ⇒ `M0.0,0.0 L150.0,150.0 L300.0,100.0 L450.0,0.0 L600.0,50.0` ✓ ⇒ **הרצפה הישנה הייתה ארטיפקט**, ⛔ לא ירידת הון. `B-143` · `D-043` |

| C-030 | 🔴 **באנר הערבוב על המסך — שני חצאים, ו-20.08 נמדד ש⛔ אף אחד מהם ⛔ אינו בר-אימות היום.** ⚠️ **תוקן 20.08 בהכרעת ניב — קודם נכתב כאן שרק (ב) חסום, וזו הייתה קריאה שגויה שלי.** **(א) — טכנית בר-ביצוע, ⛔ אך ריק כראיה.** יומן של 2 ניירות אמריקאיים סגורים שתוויתם השמורה `ILS` ⇒ הבאנר **⛔ אינו מרונדר** — 🔴 **אבל אם `B-172` נכון, "הבאנר נעדר" נכון גם ⛔ בלי הגל**, שכן `mixedCurrency === false` על **כל** קלט. ⇒ הצילום ⛔ אינו מפלה בין «תוקן» ל«מעולם לא יכול היה לירות», ו**תא שמכריז אימות שלא הפלה דבר הוא בדיוק `B-162`**. ⇒ **גם (א) חסום על `B-172`.** 🔴 **(ב) — `B-172` נסגר 20.08 (`D-048`), ⛔ אך (ב) עדיין ⛔ בלתי-נגיש בפרודקשן, ⛔ ולא "בוצע".** `MEASURED` מגיע כעת משני מסלולים: `provider_code` (`trade.instrumentCurrency`, עדיין ⛔ ללא עמודה ב-DB, ⛔ ללא כותב) **וגם** `currency_source_evidence` (חדש, `B-172`) — אבל נמדד 20.08 ש**0/78 שורות פרודקשן** נושאות `currency_source` evidence-backed (`{null:67, manual_capital:11}`, 0×`broker_arithmetic`/`file_cell`) ⇒ הענף השני **מחובר ורדום**, לא פעיל. ⇒ 324/324 הצירופים הישנים עדיין `{"USD"}` בלבד ⇒ `codes.size > 1` עדיין בלתי-אפשרי **היום**. ⇒ **(ב) ייפתח בפועל רק ביום שבו ייבוא `broker_arithmetic`/`file_cell` ירוץ** — לא אירוע חד-פעמי אלא תלוי-קלט; הכיסוי הנוכחי נשאר הפיקסצ'ר ב-`test:tradingstats` 12.T ו-`test:instrument` §17. ⚠️ **וזו בדיוק הסיבה שהחצי הזה ⛔ אינו נמחק:** "הבאנר נעדר" על המסך היום ⛔ אינו מעיד שהגזירה נכונה — הוא מעיד שאין לה איך להידלק | תנאי-גלישה: **כל נגיעה ב-`isMixedCurrency` · ב-`pnlByCurrency` · או ב-`MixedCurrencyBanner`** | — | ⏳ **⛔ לא בוצעה — `B-172` נסגר 20.08 ו⛔ עדיין לא משנה זאת (ענף רדום, ראה למעלה). הכרעת ניב 20.08: הצילום בוטל, ⛔ לא נדחה על עומס.** נימוקו, במלואו: *«אם `B-172` נכון, הצילום מוכיח ⟪הבאנר נעדר⟫ — עובדה שנכונה גם בלי הגל ⇒ אימות ריק. ⛔ עדיף `C-030` פתוח ומוצהר מאשר צילום שנראה כמו ראיה ⛔ ואינו»*. ⚠️ **הזריעה (`ZZUS1`/`ZZUS2`) הוכנה במלואה ⛔ ולא הורצה** — ההוראות וה-`DELETE` הממוקד נמסרו לניב 20.08 ונזנחו בהכרעה זו; ⛔ אין שורות בדיקה ב-`trades`. ⚠️ **ומצב C ⛔ אינו מתקיים אצל ניב, נמדד ⛔ לא הונח:** `capitalCurrency=ILS` אבל `accountCurrency=USD` ⇒ `dispCcy=USD` ⇒ **מצב D**; ⛔ **לא נתבקש לשנות הגדרה כדי לייצר באג** — זה שינוי מצב אמיתי ⛔ לא זריעה. **מה שכן נמדד, ⛔ ולא על המסך:** ההזנה הידנית חותמת `currency: capitalCurrency` ⇒ 2 ניירות US ⇒ `contradicted/null` ×2 ⇒ **לפני:** באנר **נוכח** עם שורת כסף **ריקה** (`""`) · **אחרי:** `(מוסתר)`. שיניים (fixtures בלבד): `+₪50.00 (1)  ·  +$50.00 (1)`. `B-144` · `B-172` |
| C-031 | **`DNACard`/`GrowthChart`/`menteeDNA` על יומן ריק ודליל — הצג נמוך-ביטחון, ⛔ לעולם לא מספר בדוי.** (א) **יומן ריק** ⇒ `DNACard` **⛔ אינו נעלם** (עוגן `data-tour="trading-dna"`) — מציג 4 מקפים + שורת הסבר, ⛔ לא `50`; `GrowthChart` **⛔ אינו מרונדר**. (ב) **יומן דליל** (1–2 עסקאות, מתחת ל-`MIN_SAMPLE_R`) ⇒ ציון לא-נמדד מוצג כ-`null`/מקף, ⛔ לא `0`/`50`; `GrowthChart` מציג תג שבר `measuredCount/totalCount` גלוי ליד הנקודה היחידה | תנאי-גלישה: **כל נגיעה ב-`TradeDNA.js` · `GrowthTracker.js` · `IntelligenceUI.jsx` (`DNACard`/`ScoreBar`/`GrowthChart`) · או ב-`menteeDNA`** | 20.08 (`B-156`) | ✅ **בוצעה 20.08 דרך הרמה הזמנית `EyeVerifyHarness` (נמחקה לפני קומיט) — ⛔ לא "אומת-מקור".** ריק: `DNACard` 4 מקפים + הסבר, לא נעלם; `GrowthChart` לא מרונדר. עסקה בודדת: `DNACard` — `discipline`/`riskMgmt`/`growth`=100, `consistency`=`null` (מתחת ל-`MIN_SAMPLE_R`); `GrowthChart` — נקודה אחת אמיתית + תג "3/5 נמדד". `B-156` · `D-049` |
| C-033 | **דפדפן שאינו Chromium — Safari ו-Firefox.** 🔴 **DEEP-AUDIT 26.08 נמדד ב-Chromium בלבד ⇒ `0/2` מנועים אחרים נבדקו כלל.** ⚠️ **זו ⛔ אינה "בדיקת תאימות" גנרית** — היא מכוונת לשלושה מסלולים שנמדדו כתלויי-מנוע: (א) **פורמט כסף** — `toLocaleString()` חשוף ב-24 אתרים נגזר מלוקאל הדפדפן (`B-188`), ו-Safari עם לוקאל `ar` מדפיס ספרות ערביות-הודיות על סכום; (ב) **`navigator.clipboard`** — נדרש `test:capability`, ו-Safari חוסם ללא מחווה ישירה; (ג) **`matchMedia`/viewport** במובייל Safari עם סרגל דינמי. ⛔ **"נראה תקין" אינו תוצאה** — לכל מסלול נרשם המספר שנצפה, ⛔ לא רושם "עבר" | תנאי-גלישה: **כל נגיעה ב-`src/lib/imageResize.js` · ב-`toLocaleString`/`Intl` · ב-`navigator.clipboard` · ב-`matchMedia`** — **או** לפני כל שחרור פונה-משתמש | ⛔ **מעולם** | ⛔ **טרם בוצעה.** `DEEP-SUMMARY.md` §"מה לא נמדד" · `B-188` |
| C-034 | **רשת חנוקה — Slow 3G ו-offline.** 🔴 **DEEP-AUDIT 26.08 מדד עמידות בכשל ספק (`DEEP-F`) ⛔ אך לא עמידות ב**איטיות**;** כשל מהיר וכשל איטי הם שתי מחלקות שונות. שלושה מסלולים: (א) **תג `🟢 LIVE`** — `B-185` מדד שהוא שעון ו⛔ לא מדידת חיוּת ⇒ ב-Slow 3G הוא ירוק בזמן שהמחיר בן דקות; (ב) **העלאת תמונה ל-`/api/ocr`** — האם יש טיימאאוט וחיווי, או ספינר לנצח; (ג) **`offline` מלא** — האם המשתמש רואה הצהרה או מספר ישן בלי חותמת. ⛔ **המדד הוא מה שהמשתמש רואה, ⛔ לא זמן הטעינה** | תנאי-גלישה: **כל נגיעה במסלול מחיר חי · ב-`api/ocr.js` · או בשכבת ה-fetch** — **או** רבעוני | ⛔ **מעולם** | ⛔ **טרם בוצעה.** `DEEP-SUMMARY.md` §"מה לא נמדד" · `B-185` |
| C-035 | **מגמת heap בסשן ארוך.** 🔴 **`DEEP-E` מדד bundle ו-render, ⛔ אך המדידה ארכה דקות ⇒ דליפה שמצטברת על-פני שעה ⛔ אינה נראית בה.** ⚠️ **הסיכון ⛔ אינו תיאורטי:** יומן מסחר נפתח בבוקר ונשאר פתוח עד הסגירה — 6.5 שעות עם פולינג מחירים, ו-`setInterval` בלי `clearInterval` ב-`useEffect` אחד מספיק. **שיטה:** DevTools → Memory → 3 snapshots בהפרש 20 דקות עם האפליקציה פתוחה ומתרעננת; **נרשם `usedJSHeapSize` בשלוש הנקודות** ⛔ לא "נראה יציב". **ספַּה שלישית > ראשונה ב->50% ⇒ עוצרים ופותחים `B-`** | תנאי-גלישה: **כל הוספת `setInterval`/`setTimeout`/`addEventListener` בקומפוננטה** — **או** רבעוני | ⛔ **מעולם** | ⛔ **טרם בוצעה.** `DEEP-SUMMARY.md` §"מה לא נמדד" |
| C-032 | **`B-171`/D3 — האם `days=30` של TwelveData מכיל את `days=7`/`days=1` כתת-קבוצה מדויקת (הזנב תואם תו-בתו), כמו שהחוזה ב-`PLAN-B171.md` §2.1 מניח.** ⛔ **לא נצפה חי בגל 24.08** — מכסת TwelveData היומית הייתה נעולה לאורך כל הגל (`curl` על `/api/quote?days=30` **וגם** `?days=7` החזירו `{"SPY":null}` ב-17:21 UTC 24.08, אותו יום מדידת ה-1052/800 המקורית) ⇒ `deriveOverviewForRange` נבנה על קריאת-קוד בלבד, ⛔ לא A/B. ⚠️ **הכרעת ניב 24.08: ⛔ לא לסמן "אומת" כשהמכסה תתאפס בלי ריצה חדשה** — הבדיקה הזו **היא** מנגנון-החזרה, כדי שהיא לא תישכח. **תוספת 24.08 (חלק ב', לאחר `AUDIT-B171c`) — תנאי מדידה נקייה ל-`B-171` עצמו, ⛔ לא רק ל-D3, בעקבות `b1243aa`:** `AUDIT-B171c` מדד את הספייק "16=2×8" על **הקוד הישן** (התיקון `2d95218` עלה 20:35 IL, התצפית הייתה 18:51-19:43 IL — **לפני** הפריסה) — ⇒ עדיין אין מדידה נקייה של התיקון עצמו. **M1** — חלון 24 שעות מ-20:35 IL 24.08 עד 20:35 IL 25.08, ⛔ לא פחות; ניב מצלם דשבורד TwelveData אחרי 20:35 IL 25.08. **M2** — המונה היומי של TwelveData מתאפס בחצות **UTC** (03:00 IL), ⛔ לא בחצות IL ⇒ הצילום יראה **17.5 שעות** של קוד חדש בלבד (03:00→20:35), ⛔ לא 24 — יש לציין זאת במפורש כשהסעיף נסגר. **M3** — סף הצלחה **<800/יום**; קו-בסיס קוד-ישן = **2,389**. ~1,200 ⇒ האיחוד עבד (פי-2) אך הבעיה בתדירות, ⛔ עדיין לא סגור. <800 ⇒ נסגר. ~2,400 (ללא שינוי) ⇒ התיקון ⛔ לא עבד באמת, חוזרים לאבחון. **M4** — מקור "הגל הקטן" (1-2 קריאות בין ספייקים, כל ~5 דק') טרם זוהה בוודאות; Sentinel כל 30 דק' ⛔ אינו יכול להסביר קצב ~5 דק' לבדו; `checkTwelveData`/`api/health.js` הוא המועמד המוביל — ⛔ לא להניח, למדוד עד 25.08 | תנאי-גלישה: (D3) **מיד כשמכסת TwelveData היומית מתאפסת** — נבדק ב-`curl 'https://…/api/quote?symbol=SPY&days=30'`; תשובה עם מחיר אמיתי (⛔ לא `null`) היא האות להריץ. **וגם** (M1-M4, `B-171`) **תנאי-גלישה חדש: 25.08 אחרי 20:35 IL** — ניב מצלם דשבורד TwelveData; נמדד מונה-יומי מול סף M3 וסטטוס M4 | — | 🟡 **חלקי — M1-M3 אומתו בעין 25.08 16:25 IL; D3 ו-M4 ⛔ נשארים לא-מוצהרים.** ✅ **M1-M3 (`B-171`/`D-052`):** ניב צילם דשבורד TwelveData — קרדיטים `2,389→440` (**−82%**, קריאת **13.5ש'** מאיפוס-UTC 03:00 IL ⛔ לא 24ש', אומדן-יממה **~550-650**), avg/min `6/8→2/8`, max/min `40/8→17/8`, מכסה-מול-תקרה `300%→55%` ⇒ **סף M3 (<800) הושג**. ⚠️ `17/8` עדיין חוצה 2×/דקה ברגעים בודדים ⇒ ⛔ לא "אפס חריגות" — `B-183`. ⛔ **D3 (30D מכיל 7D/1D כתת-קבוצה) ⛔ עדיין לא נצפה חי** — מכסת TwelveData הייתה נעולה לאורך כל גל 24.08, ⛔ שום A/B לא בוצע; החוזה נשאר מאושר-בקריאת-קוד בלבד (`PLAN-B171.md` §2.1). ⛔ **M4 (מקור "הגל הקטן" בין ספייקים) לא סופק/לא נמדד בצילום 25.08** — נשאר בגדר מועמד (`checkTwelveData`) ⛔ לא זוהה בוודאות. ⇒ **הבדיקה לא נסגרת במלואה — D3+M4 ממתינים לתנאי-גלישה הבא** (איפוס מכסה חדש / מדידת-לוגים תוך-דקתית) |
| C-036 | **שני הבאנרים של `B-268` — מסך פונה-משתמש שנולד בגל `W-CAP` 28.08.** 🆕 **מצומצם 29.08 ע"י `test:hydration`** (19 אסרציות · 14 מבחינות נצפו אדומות על `6fd3106^`). ⛔ **הפריט ⛔ אינו נסגר ו⛔ אינו מרוכך — הוא מאבד את מה שהשרשרת מוכיחה עכשיו, ⛔ ולא יותר.** **⛔ הוסר מכאן** (עבר לשער חוסם): שההידרציה הכושלת ⛔ אינה מסמנת `hydratedRef` (`A1`·`A4`) · שאפקט ההתמדה ⛔ אינו קורא ל-`saveSettings` אחריה (`A12`) · ש-`beforeunload`/`pagehide`/פירוק ⛔ אינם קוראים ל-`flushSettings` (`A13`·`A14`) · ש-`console.error` נושא `[hydrate]` (`A6`) · שהתנאי להרמת באנר-הדריסה יורה ומכבד את מפתח-הביטול (`A10`·`I3`). **✅ מה שנשאר — ⛔ ואין שער בשרשרת שיכול לראותו:** (1) שהבאנר **מרונדר** בפועל כששני ה-state דלוקים — JSX, ⛔ אין רנדרר (`B-273`); (2) ששני הכפתורים עושים משהו — «ההון שלי נכון» כותב `swingEdgeCapitalConfirmed:<uid>` ו⛔ **אינו כותב ל-DB** (פאנל Network ריק, ⛔ לא הקונסולה) · «לשדה ההון» מגיע ל-`settings`; (3) **התנהגות דפדפן ופרודקשן אמיתיים** — DevTools → חסימת `user_settings` → רענון ⇒ הבאנר מופיע וההון נופל לערך המקומי · רענון ⇒ הבאנר ⛔ אינו חוזר · מכשיר אחר ⇒ **כן** חוזר (`B-271`, מקובל ביודעין) · `StrictMode` · `pagehide` במובייל; (4) **RTL ו-LTR · ניגודיות · קריאוּת** — הטקסט inline ו⛔ אינו עובר ב-`tooltips.js` ⇒ `test:copy` ⛔ אינו רואה אותו | תנאי-גלישה: **כל נגיעה ב-`src/lib/userSettings.js` · בשער ההידרציה (`SwingEdge_App.jsx` `:1726-1818`) · או בשני הבאנרים** — **או** רבעוני | ✅ **06.09 — QA** | 🟡 **חלקית — (1)+(3) בלבד; ⛔ הפריט נשאר פתוח.** 3/3 ריצות **זהות** בחלון מבודד עם `user_settings` חסום מ-`t=0` (קריאה **וגם** כתיבה): `4` בקשות נחסמו, **כולן `GET`** ⇒ **`0` `POST`/`PATCH`** — ⛔ **אין אפילו ניסיון כתיבה**, ו-`B-268` ⛔ **אינו משחזר בפרודקשן היום**. **(א)** המסך רונדר במלואו ו**הבאנר הורם** (`guardBanner=true` ב-3/3) · **(ב)** ההון `$9,480.00` = הון `10,000`, ⛔ **לא** `$1,980` שהיה נובע מברירת `2,500` ⇒ **(א)+(ב) בלי כתיבה** ⇒ `W-CAP` נסגר. ⚠️ **סטייה מהציפייה ב-(3):** ההון ⛔ **לא** «נפל לערך המקומי» — הוא נשאר על ההון האמיתי, וזו ההתנהגות **המתוקנת**. ההון חזר ל-`10,000` אחרי הסרת החסימה ⇒ `scripts/2026-08-28-qa-capital-restore.sql` ⛔ **לא הורץ ו⛔ לא נדרש**. 🔴 **⛔ ולא נבדקו:** (2) ששני הכפתורים עושים משהו · (4) RTL/ניגודיות/קריאוּת של הבאנר. `INCIDENTS#19` · `B-268` · `B-273` · `D-063` · `D-070` |
| C-037 | 🔴 **שער-מדידה לפני שינוי עיצוב — ⛔ בדיקה שחוסמת, ⛔ לא בדיקה שמתעדת.** נולד מ-`DECISIONS` 29.08 ו-`AUDIT-design-failure-2026-08.md` §5. **לפני שורת JSX אחת** שנוגעת בהיררכיה · טיפוגרפיה · צבע · מרווח — שלוש תשובות **כתובות** בגוף התוכנית: ⓵ **מה מרונדר שם עכשיו** — `getComputedStyle` בדפדפן אמיתי על האלמנט עצמו (`font-size` · `font-family` · `color` · `background-color`), ⛔ **לא שם מחלקה ו⛔ לא קריאת `tokens.css`** · ⓶ **מהו ציר ההבחנה הקיים, וכמה ממנו צבע** — כמותית, בצורת `X/Y` (⚠️ `StatCard` נמדד `15/15` צבע · `0/15` טיפוגרפי) · ⓷ **כמה ממה שמפריע כבר קיים בבסיס** — `grep` מול `main` **לפני** הסבב (⚠️ נמדד `6/8` הורשו). ⚠️ **מבחן ההפעלה, והוא החלק שנוטים לדלג עליו:** «אילו המסך היה נראה אחרת לגמרי, האם ההצעה שלי הייתה משתנה?» — «לא» ⇒ **עצור.** | **תנאי-גלישה — ⛔ לא תדירות.** ⚠️ **גולש על כל תוכנית שנוגעת בשכבת התצוגה**, כולל «רק צבע» וכולל «רק טוקן»: ארבע הגרסאות שנדחו היו כולן «רק». ⛔ **ואינו גולש** על תיקון באג-רינדור מדוד (`B-254` הוא באג RTL ⛔ ולא עיצוב — `DECISIONS` 27.08 ⓵). ⛔ **אין תדירות קלנדרית** — בדיקה שרצה «כל שבוע» על שכבה שלא זזה היא רעש. | **29.08 — ⛔ לא בוצע. נולד היום, ⛔ אין ריצה.** ⚠️ **⛔ ואסור לרשום כאן «עבר» על סמך האודיט שהוליד אותו:** `AUDIT-design-failure` §7 מצהיר במפורש שכל מדידותיו הן **על קוד המקור** ו⛔ **לא על המסך המרונדר** ⇒ הוא מקיים את ⓶ ואת ⓷, ו⛔ **אינו** מקיים את ⓵. | 🔴 **הבדיקה חסומה עד שלושת התנאים המקדימים נמדדים** — `B-254` (RTL) · `B-246` (היררכיה) · `B-245` (ניגודיות), בסדר של ניב מ-27.08. ⚠️ **⛔ והם ⛔ אינם «הבדיקה»** — הם הקלט ל-⓵ בהרצה הראשונה. ⛔ **ריצה ראשונה ללא דפדפן אמיתי ⛔ אינה ריצה** — ⚠️ ⛔ **ו-Playwright headless ⛔ אינו נפסל כאן**: הפסילה שייכת לערוץ ה-**analytics** (`navigator.webdriver`), ו-`C1` **מדדה ניגודיות חיה בהצלחה** דרך CDP; הכללתה לעיצוב הופרכה במדידה (`AUDIT-design-failure` §2.3). |
| C-038 | 🆕 **פס ה-P&L החי בעסקת SHORT פתוחה — הפיקסל, ⛔ לא המספר.** נולד מ-`B-186` (04.09). `test:shortpct` מוכיח שהביטוי מחזיר את האחוז הנכון, ו⛔ **אינו מוכיח שהפס נראה נכון** — אותו גבול בדיוק כמו `C-036` מול `test:hydration`. ⚠️ **והמסך ⛔ אינו יכול להיבדק בכל ערך:** רוחב הפס הוא `Math.min(Math.abs(pct) * 10, 100)` ⇒ **מעל `±10%` שני הביטויים נחתכים ל-100%** ודלתא-הפיקסל היא **אפס בדיוק** (נמדד 04.09: E=100·C=125 ⇒ `0.00pp` · E=100·C=75 ⇒ `0.00pp`). **מה נבדק, ובאילו ערכים:** ⓵ SHORT פתוח עם `Math.abs(pct) < 10` — E=100·C=95 ⇒ הפס **50%** מרוחב המכל (הקוד הישן נתן 52.63%, Δ=2.63pp) · ⓶ E=100·C=92 ⇒ **80%** (ישן 86.96%, Δ=6.96pp) · ⓷ **הכיוון** — SHORT ברווח צבוע כרווח, בהפסד כהפסד (הכסף היה נכון גם לפני ⇒ הצבע ⛔ אינו ראיה לתיקון) · ⓸ **LONG לא זז** — E=100·C=110 ⇒ 100% לפני ואחרי. ⛔ **הרוחב נמדד ב-DevTools על האלמנט עצמו (`getComputedStyle`/`offsetWidth`), ⛔ לא «נראה בערך חצי»** (`C-037` ⓵). ⚠️ **וצריך SHORT פתוח כדי לבדוק — ו-`M-018` מדד `0/40` פתוחות SHORT** ⇒ ⛔ **אין היום מסך להסתכל עליו**, וזו הסיבה שהגל נסגר ב-`אומת-מקור` ו⛔ לא באימות עין | **תנאי-גלישה — ⛔ לא תדירות.** גולש ב-**הראשון** מבין השניים: (א) **כל נגיעה** במשפט `const livePnlPct =` שב-`SwingEdge_App.jsx` או בצרכן `Math.abs(livePnlPct)` ברוחב הפס · (ב) **הופעת עסקת SHORT פתוחה ראשונה בג'ורנל אמיתי** — נמדד ב-`M-018`, שתוקפו 7 יום; מדידה חוזרת שמחזירה `n/40` עם `n>0` **היא** האות. ⛔ **אין תדירות קלנדרית** — בדיקה שדורשת SHORT פתוח ורצה «כל חודש» על אוכלוסייה ריקה מדפיסה «עבר» בלי לראות דבר, וזה `R-4` | ⛔ **מעולם** | ⛔ **טרם בוצעה — ו⛔ אינה יכולה להתבצע היום.** ⚠️ **⛔ ואסור לרשום כאן «עבר» על סמך `test:shortpct`:** 18/18 מוכיחות את **הביטוי** שרץ ב-`new Function`, ⛔ לא את הפס שנצבע ב-React בדפדפן. ⛔ **וגם שתי בדיקות הצורה על ה-bundle ⛔ אינן סוגרות אותה** — הן מוכיחות שהביטוי הנכון **נשלח לדפדפן**, ⛔ לא שהוא רונדר. `B-186` · `M-018` · `C-036` (אותו גבול) |
| C-039 | 🆕 **פאנל רשימת המעקב — האם משתמש שלא נגע ברשימה רואה `—` ולא מספר מומצא.** נולד מ-`B-185` (05.09), `PLAN-2026-09-05-b185-fallback.md` §5. `test:watchlist` מוכיח ש-(ב)+(ג)+(ד) מייצרים ערכים nullish בזרימת המקור והכתיבה-חזרה, ו⛔ **אינו מוכיח שהמסך מרנדר `—`** — אותו גבול בדיוק כמו `C-036`/`C-038`: JSX · React · דפדפן אמיתי ⛔ אינם מכוסים ב-harness שקורא בייטים בלבד. **מה נבדק, ובשני מצבים:** ⓵ **ספק חי** — משתמש עם `lp` אמיתי מהחיבור למחירים, פריט ⛔ שלא נגע ברשימה ⇒ מחיר+אחוז אמיתיים מרונדרים, ⛔ לא ליטרל ישן · ⓶ **ספק מנותק** (DevTools → חסימת בקשות המחיר) ⇒ `price`/`changePct` שניהם nullish ⇒ `—` במקום המחיר, ושורת האחוז **נעדרת לגמרי** (השומר `changePct != null`, ב2) ⛔ לא `+0.00%` וגם ⛔ לא `NaN%`/`null%`. ⚠️ **וצריך משתמש אמיתי שלא נגע ברשימה כדי לבדוק** — `46/46` ב-`M-019` הם חסם עליון, ⛔ לא ערובה שמשתמש ספציפי עדיין שם; מומלץ חשבון בדיקה טרי או `localStorage`/`user_settings` ריקים | **תנאי-גלישה — ⛔ לא תדירות.** גולש ב-**כל נגיעה** ב-`DEFAULT_WATCHLIST`, בשלושת אתרי הכתיבה `:1642`/`:6750-6751`/`:1831`, או בשורת האחוז ב-JSX של פאנל רשימת המעקב — **וגם חובה לפני שהגל `B-185` נסגר** (§14, T3 ⛔ אינו נסגר בלי אימות עין) | ⛔ **מעולם** | ⛔ **טרם בוצעה.** `B-185` · `test:watchlist` (27/27) · `PLAN-2026-09-05-b185-fallback.md` §5·§9 |
| C-040 | 🆕 **גל D+E1 — האם P&L חי מרונדר עם סנטים (⛔ לא מעוגל) והאם `stop` ריק מרונדר `—` (⛔ לא `$0`).** נולד מ-`B-292`/`D-068` (05.09), `PLAN-2026-09-05-d-e1.md` §7.2 סעיף 8 — התוכנית עצמה דורשת אימות-אחרי בדפדפן לפני סגירת `T3`, וזה **טרם בוצע**: `76716df` פורסם ל-`main` בלבד, ⛔ בלי פתיחת `swing-edge.com` ואימות עין. `test:format` §9-§10 מוכיחות את **הפונקציות** (`fmt$` על ערך לא-מעוגל, `fmtPaperPrice(null)`), ו⛔ **אינן מוכיחות שהערך המתוקן מרונדר במסך** — אותו גבול בדיוק כמו `C-036`/`C-038`/`C-039`: JSX · React · דפדפן אמיתי ⛔ אינם מכוסים ב-harness שקורא בייטים בלבד. **מה נבדק, ובאילו עסקאות:** ⓵ עסקה פתוחה עם P&L חי שאינו עגול (למשל `+$5.20`, ⛔ לא `+$5.00`) בכרטיס העסקה **וגם** בטבלת "כל העסקאות" (שני האתרים, `:4621`+`:5269`) · ⓶ עסקה פתוחה בלי `stop` ⇒ `—` בכרטיס, ⛔ לא `$0` · ⓷ עסקה עם `stop=0` אמיתי (אם קיימת) ⇒ **חייבת** להישאר `$0`, ⛔ לא `—` (אחרת רגרסיה על ההבחנה `0` מול `null`) | **תנאי-גלישה — ⛔ לא תדירות.** גולש ב-**כל נגיעה** ב-`fmt$`/`fmtPaperPrice` בשני אתרי P&L החי (`:4621`/`:5269`) או בשומר `tr.stop`/`t.entry`/`t.exit` ברחבי הקובץ — **וגם חובה לפני שהגל D+E1 נסגר במלואו** (§14, T3 ⛔ אינו נסגר בלי אימות עין) | ✅ **06.09 — QA** | ✅ **אומת-עין — ⓵⓶ עברו, ⓷ ריק לפי הגדרתו; הפריט נסגר.** ⓵ **סנטים בשני האתרים**: כרטיס העסקה (`:4621`) **וגם** טבלת היומן (`:5269`) — `UUUU +$5.27` ו-`NOK +$137.56`; **חושב עצמאית** מהמסך: `(14.47−14.30)×31 = 5.27` · `(10.03−9.27)×181 = 137.56` ⇒ ⛔ לא מעוגל. ⓶ עסקת `TESTC040` **בלי `stop`** ⇒ **`—` בשלושה אתרים** (כרטיס · עמודת `סטופ` בטבלת היומן, נקרא מה-DOM לפי כותרת ⛔ לא לפי פיקסל · לוח הסיכון), ⛔ **לא `$0`**; אחיות באותו מסך הראו `$11.13`/`$8.72` ⇒ ה-`—` **ייחודי** לעסקה חסרת-הסטופ ו⛔ אינו ברירת-מחדל גורפת. ⓷ **ריק לפי «אם קיימת» שבהגדרת הפריט** — ⛔ אין בג'ורנל שורה עם `stop=0`, **וטופס היצירה חוסם אותה**: `entry=100`·`stop="0"`·`target=120` ⇒ `Log Trade` `disabled=true` ⇒ המצב ⛔ **בלתי-נגיש דרך ה-UI** (ייתכן רק מייבוא/OCR/נתון היסטורי). `TESTC040` נמחק דרך ה-UI ואומת: `6→5` רשומות · `3→2` פתוחות, **שרד רענון** ⇒ ⛔ לא אופטימי-בלבד. `B-292` · `D-068` · `D-070` |
| C-041 | 🆕 **שלושת אתרי ההון — האם סכום חלקי נושא תג נראה, והאם `…` מופיע רק בטעינה הראשונה.** נולד מ-`B-295` (05.09), `PLAN-2026-09-05-g3-equity-state.md`. `test:equitystate` מוכיח **ערך** (`A1`–`A6`, המודול) ו**צורה** (`C1`–`C12`, הבייטים של ה-JSX), ו⛔ **אינו מוכיח שהתג מרונדר** — אותו גבול בדיוק כמו `C-036`/`C-038`/`C-039`: JSX · React · דפדפן אמיתי · קורא מסך ⛔ אינם מכוסים ב-harness. **מה נבדק, ובאילו מצבים:** ⓵ **חלקי** — עסקה פתוחה בלי מחיר חי (DevTools → חסימת בקשות המחיר) ⇒ המספר מוצג **עם `⚠`** בכותרת ובפוטר, והבאנר המלא בכרטיס ה-KPI · ⓶ **`aria-label`** — VoiceOver/TalkBack מקריא את המשפט המלא, ⛔ לא רק את המספר (🔴 **זו הסיבה ש-`aria-label` נדרש ו⛔ לא רק `title`** — `title` ⛔ נגיש במובייל, הכרעת ניב `D2`) · ⓷ **טעינה ראשונה** ⇒ `…` ⛔ ולא ספרות · ⓸ 🔴 **ריענון ברקע ⛔ אינו מרוקן כותרת שכבר נחתה** — צפייה **רצופה** של ≥2 מחזורי ריענון (30ש') בשוק פתוח: המספר **⛔ אינו** קופץ ל-`…` (⚠️ זו הסטייה מ-§3.1 של התוכנית, ו-`W1` ב-harness ⛔ אינו ראיה לה — הוא ירוק בשני העצים) · ⓹ **RTL וניגודיות** של ה-`⚠` בעברית · ⓺ **`fxStatus="unavailable"`** ⇒ המספר במטבע ההון עם תג, ⛔ לא היפוך סמל | **תנאי-גלישה — ⛔ לא תדירות.** גולש ב-**כל נגיעה** ב-`src/lib/equityState.js`, בשלושת אתרי ההון, או בבאנר `B-142`/הודעת ה-fx — **וגם חובה לפני שהגל `G3` נסגר במלואו** (§15, `T3` ⛔ אינו נסגר בלי אימות עין) | ✅ **06.09 — QA** | 🟡 **חלקית — ⓵⓶⓷ עברו · 🔴 ⓹ נכשל · ⓸⓺ ⛔ לא ניתנים לאימות היום; ⛔ הפריט נשאר פתוח.** **שלם:** `$9,622.83` בכותרת **וגם** בכרטיס ה-KPI, ⛔ **בלי תג** — `aria-label=null` ו-`0` תווי `⚠` במסמך כולו; **אומת אריתמטית עצמאית:** `10,000−520 = 9,480` ‎+‎ `5.27` ‎+‎ `137.56` ⇒ `9,622.83` **בדיוק** כמרונדר. ⓵ **חלקי:** `⚠` בכותרת **וגם** בפוטר (`text-amber-400`) + המשפט המלא בכרטיס ה-KPI. ⓶ `aria-label` **קיים** ונושא את המשפט המלא ⛔ לא רק מספר; **והמכנה נע עם האוכלוסייה** — `3 מתוך 3` ⇐ `2 מתוך 2` אחרי מחיקת `TESTC040` ⇒ ⛔ **אינו קבוע**. ⛔ **קורא מסך אמיתי ⛔ לא הופעל** ⇒ ⓶ אומת כ**נוכחות התכונה**, ⛔ לא כהקראה. ⓷ **טעינה:** בחלון מבודד עם `/api/quote` תלוי ⇒ `…` **בלי ספרות** ו-`aria-label="טוען..."`. ⚠️ **נדרשה כפייה** — האפליקציה שולחת **`0`** בקשות `/api/quote` בטעינה קרה בשוק סגור, והוכח בביקורת-חיובית (קריאה ידנית **כן** נתלתה) ⇒ `0` היה «אין בקשות», ⛔ לא «התיקון לא עובד». 🔴 **⓹ ניגודיות נכשלת — נמדד, ⛔ לא הוערך:** `amber-400` `rgb(251,191,36)` על `rgb(248,250,247)` = **`1.59:1`** מול **`4.5:1`** נדרש (כותרת 14px/700 · פוטר 10px/400 — **טקסט רגיל**, ⛔ לא רכיב-UI; גם סף `3:1` נכשל). RTL ✅ `dir=rtl`. ⇒ ה-`⚠` הוא הנשא ה**חזותי היחיד** של «הסכום חלקי» ו⛔ אינו עומד ב-AA — הערוץ לקורא-מסך תקין, הערוץ לרואה **חלש**. ⓸ דורש **שוק פתוח** (≥2 מחזורי ריענון רצופים) ⇒ ⛔ לא היום. ⓺ `accountCurrency`=`capitalCurrency`=`USD` (**זהות**) ⇒ ⛔ לא ניתן ב-QA, והמטבע ⛔ **לא שונה** (איסור מפורש). ⚠️ **בלוק ההון בכותרת הוא `hidden sm:block`** ⇒ מתחת ל-640px ⛔ **אין הון בכותרת כלל** (`rect 0×0`), ולכן «`⚠` בכותרת» נבדק ב-≥640px בלבד. `B-295` · `B-245` (אותה מחלקת ניגודיות) · `C-036` (אותו גבול) · `D-070` |
| C-042 | 🆕 **לוח הסיכון — האם התג `בטוח` **נעלם** בסכום חלקי, האם `≥` מרונדר לצדו, והאם השער ותאריכו מוצגים.** נולד מ-`B-301` (06.09), `PLAN-2026-09-06-g2-risk-in-capital.md`. `test:equitystate` מוכיח **ערך** (`V7`–`V10e`, `deriveRiskState`+`riskFigure`) ו**צורה** (`S1`–`S4`, הבייטים של ה-JSX), ו⛔ **אינו מוכיח שהתג נעלם על מסך** — אותו גבול בדיוק כמו `C-036`/`C-038`/`C-039`/`C-041`: JSX · React · דפדפן אמיתי ⛔ אינם מכוסים ב-harness. **מה לבדוק, ובאילו מצבים:** ⓵ **חלקי מתחת לתקרה** — עסקה פתוחה שסירבה (⛔ אין שער / `contradicted`) ⇒ התג `בטוח` **⛔ אינו מרונדר כלל**, המספר הגדול נושא `≥`, וגם שורת הכסף שמתחתיו · ⓶ **חלקי מעל התקרה** — אותו מצב עם סיכון גבוה ⇒ התג `חריגה` **כן** מרונדר (חסם תחתון מוכיח חריגה) · ⓷ **טעינה** — טבלת ה-FX טרם נחתה ⇒ `…` בשני האתרים ו⛔ אין פסק-דין · ⓸ **הצהרת השער** — הון ב-₪ + עסקה דולרית ⇒ «הומר לפי שער 3.3xxx (משער YYYY-MM-DD)» מתחת לכרטיס · ⓹ **RTL** — הקידומת `≥` בעברית ⛔ אינה נדבקת למספר בסדר הפוך · ⓺ **`aria-label`** — קורא מסך מקריא את `partialSumWarn` על המספר הגדול. ⛔ **⓵–⓸ דורשים חשבון עם הון ₪ ועסקה דולרית פתוחה — 1/46 משתמשים בפרודקשן, ⛔ ואין לי גישה אליו** ⇒ ריפרו מקומי בלבד, וזה נאמר במקום להציג צילום של חשבון אחר כראיה. | **אחת** — ⛔ טרם רצה **כבדיקה**. תנאי-גלישה: כל נגיעה ב-`riskInCapital` · `deriveRiskState` · `riskFigure` · או בלוח הסיכון ב-`SwingEdge_App.jsx` | 🔴 **07.09 — בפרודקשן, ⛔ לא ב-QA** | 🔴 **נכשל, ו⛔ לא על ידינו — המשתמש הריץ אותו במקומנו.** התוצאה: Error Boundary על לוח הסיכון כולו (`TypeError: … null … 'toFixed'`, release `51a12d2` · `INCIDENTS#20` · `B-303`). ⛔ **⓵–⓺ כולם ⛔ לא נבדקו — כשהמסך זורק ⛔ אין עליו מה למדוד.** ⇒ הפריט **נשאר פתוח במלואו**. ⚠️ **וה-hotfix ⛔ אינו סוגר אותו:** `R1`–`R4` מוכיחות שהתא ⛔ אינו זורק ומחזיר `—`, ו⛔ **אינן** מוכיחות שהתג `בטוח` נעלם · ש-`≥` מרונדר · שה-RTL תקין · שקורא מסך מקריא. 🔴 **הלקח על הפריט עצמו:** `C-042` נכתב **באותו יום** שבו נסגר `D-071`, ו⛔ לא הורץ לפני הדיפלוי — פריט `CHECKS` שנכתב ו⛔ לא רץ הוא תיעוד של כוונה, ⛔ לא הגנה. ⛔ **⓵–⓸ עדיין חסומים על חשבון `1/46` ש⛔ אין לי גישה אליו** ⇒ ריפרו מקומי, ⛔ לא צילום של חשבון אחר. 🆕 **מדידה שנייה 07.09, אחרי `1ebb559` — המסך חזר, והפריט ⛔ עדיין לא נבדק.** נמדד בדפדפן על `https://swing-edge.com/app`: לוח הסיכון **מרונדר** (⛔ אין Error Boundary), ושבעה מספרים חושבו **עצמאית** והתאימו — `\|1740−1700\|×2=$80` · `\|14.3−11.13\|×31=$98.27` · `\|9.27−8.72\|×181=$99.55` · סכום `$277.82` · `277.82/10,000=2.78%` · `2.78/3.0=93%` · נותר `300−277.82=$22.18`; התג `זהירות` (`2.78% > 1.5%`) ⛔ ולא `בטוח`. ⚠️ **וזה מוכיח שהפריט ⛔ שוב **ניתן** להרצה — ⛔ לא שהורץ:** החשבון נושא `3/3` נספרות ⇒ מצב `complete`, ולכן ⓵–⓷ (סכום **חלקי**) ⛔ אינם קיימים בו כלל, ⓸ ⛔ אינו מרונדר, ו-⓹–⓺ ⛔ אין להם על מה לחול. 🔴 **וגם השורה ה⛔לא-מדודה של `B-303` ⛔ לא נראתה בעין** — `—` והסלייט חיים ב-`R1`–`R4`·`P1`–`P4` **בלבד**, שמריצים ביטוי ⛔ ולא מרנדרים DOM. ⇒ **נשאר פתוח במלואו** |
| C-043 | 🆕 **פאנל שקורס על מסך אמיתי — האם כרטיס השגיאה **נראה**, והאם שאר המסך חי.** נולד מ-`B-304` (09.09), `PLAN-2026-09-07-b304-blast-radius.md`. `test:boundary` (49) מוכיח **ערך** על המודול ו**צורה** על בייטים של ה-JSX; `probe:boundary` (26) מוכיח **פריסה** ב-Chromium אמיתי מול זרוע-בקרה. 🔴 **⛔ ואף אחד מהשניים ⛔ אינו מהדר Tailwind** ⇒ נמדד box לא-אפסי, ⛔ **לא** צבע · ניגודיות · RTL · קורא-מסך. אותו גבול בדיוק של `C-036`/`C-038`/`C-039`/`C-041`. **מה לבדוק:** ⓵ **רדיוס** — הפלת פאנל אחד בדפדפן (זריקה ידנית לתוך אחד מחמשת הפאנלים) ⇒ שאר המסך **מרונדר**, ⛔ והדף ⛔ אינו מוחלף במסך יחיד · ⓶ הכרטיס **נראה**: רקע · גבול · אייקון · שם הפאנל בעברית · ⓷ **ניגודיות AA** של טקסט הכרטיס — **נמדד במספר**, ⛔ לא «נראה בסדר» (⚠️ `C-041`⓹ נכשל `1.59:1` על משפחת amber סמוכה) · ⓸ **RTL** — הטקסט העברי ⛔ אינו נשבר והכפתור בצד הנכון · ⓹ **קורא מסך אמיתי** מכריז `role="alert"` בפועל, ⛔ לא «התכונה קיימת ב-DOM» · ⓺ הכפתור מחזיר את הפאנל **בלי רענון דף** · ⓻ 🔴 **`RootFallback`** — הפלת ה-shell עצמו ⇒ המסך הממותג, ⛔ לא ה-`<p>` החשוף · ⓼ 🔴 **האם הודעה הגיעה לדיסקורד בפועל** (`B-307`) | **תנאי-גלישה — ⛔ לא תדירות.** גולש ב-**כל נגיעה** ב-`src/components/PanelBoundary.jsx` · `src/lib/alertChannel.js` · `api/alert.js` · `RootFallback` שב-`src/main.jsx` · או בחמשת אתרי `<PanelBoundary>` שב-`SwingEdge_App.jsx` — **וגם חובה לפני ש-`B-304` ייחשב מאומת-עין** (§15, `T3`) | 🔴 **⛔ טרם רץ** — 🆕 **`1/8` בלבד נענה** (⓼, 09.09) | 🔴 **⛔ אין ממצא על המסך — הפריט נשאר פתוח.** `B-304` נסגר ב**אימות-מקור** בלבד: `49/49` + `26/26` בדפדפן מקומי, זרוע-הבקרה מתה ב-`4/4` ⇒ הוכח **רדיוס**, ⛔ לא **מראה**. 🆕 **⓼ ✅ נענה 09.09 → `D-076`** — ההודעה **נראתה בעין** ב-`#general` (`SwingEdge Boundary`, ועמה גרסה · פאנל · מסלול) ו-`B-307` נסגר; ⛔ **הוא נשאר ברשימה** כי הוא **גולש** בכל נגיעה ב-`api/alert.js`/`alertChannel.js`. ⚠️ **ו-⓼ ⛔ מקדם את ⓵–⓻ ולו בפריט אחד** — נצפה ה**ערוץ**, ⛔ לא המסך. ⚠️ **והמסר היה ידני (`POST` ל-`/api/alert`), ⛔ פאנל שקרס** ⇒ ⛔ **⛔ נצפתה קריסה אמיתית מקצה-לקצה**, וזו בדיוק ⓵ |
| C-044 | 🆕 **מבחן הירי של הסנטינל על מסך אמיתי — האם `SNTNL1` באמת מפילה פאנל בדפדפן, והאם הסנטינל **מדווח** על כך.** נולד מ-`B-305` (09.09), `PLAN-2026-09-09-sentinel-representative.md`. ⛔ **הגל ביצע `B-305` בלבד** — `B-306` (שאילתת `firstSeen`) ⛔ נגע. 🔴 **מה נמדד ⛔ ומה ⛔:** `probe:firing` (`8/8`) מריץ את **גדר התא** שחולצה מבייטי `51a12d2` ומבייטי עץ העבודה, ומוכיח ש-`SNTNL1` **זורקת** בישן ומרנדרת `—` בחדש ⇒ הוכח ש**הספק מסוגל לייצר את צורת 07.09** ברמת ה**ערך**. ⛔ **ו⛔ הוכח דבר על רינדור** — React · ה-Boundary · דפדפן · Playwright · פרודקשן חיים כאן בלבד; אותו גבול בדיוק של `C-036`/`C-038`/`C-039`/`C-041`/`C-043`. **מה לבדוק:** ⓵ **הספק בפרודקשן** — יצירת `SNTNL` בטופס ואז **שינוי הטיקר** ל-`SNTNL1` דרך מודל העריכה ⇒ השורה נושאת `100` מניות ו-stop (⚠️ **יצירה ישירה ⛔ עובדת** — `sizePosition` מסרב ⇒ `shares=null`; זו זרוע הבקרה) · ⓶ לוח הסיכון **⛔ קורס** בעץ הנוכחי ומציג `—` באחוז · ⓷ 🔴 **הזרקת הצורה הישנה** (או בדיקה מול deploy של `51a12d2`) ⇒ הפאנל **כן** קורס ⇒ ⓸ **הסנטינל מדווח על כך** — ממצא `panel-crashed\|risk` ב-`browser-findings-auth.json`, ⛔ ריצה ירוקה · ⓹ **חמשת הפאנלים בנפרד** — `mentoring` ⛔ נמדד בחשבון ה-QA (⛔ אין `myMentees`) ⇒ הסריקה מדווחת `boundary-unmeasurable`, ⛔ ירוק · ⓺ **הניקוי הושלם** — `0` שורות `SNTNL`/`SNTNL1` נשארו ב-DB אחרי הריצה | **תנאי-גלישה — ⛔ לא תדירות.** גולש ב-**כל נגיעה** ב-`tests-sentinel/sentinel-auth.spec.js` · `scripts/sentinel-firing-probe.mjs` · `RISK_TICKER`/`PANEL_TAB` · או במסלול העריכה שב-`src/components/EditTradeModal.jsx` — **וגם חובה לפני ש-`B-305` ייחשב מאומת-עין** (§15, `T3`) | 🔴 **`2/6` נענו** — ריצה `34399564706` (09.09 20:11Z · `workflow_dispatch` · `SENTINEL_AUTH: 1` · `3 passed`). ✅ **⓹** — `boundary-unmeasurable\|mentoring` צהוב בממצא, ⛔ ירוק; `4/5` הפאנלים האחרים פעילים ו-`0` כרטיסי `data-boundary` · ✅ **⓺** — `sentinel rest cleanup: removed 0 SNTNL/SNTNL1 row(s)`. ⚠️ **⓵ ⓶ חלקיים** — השם זז `SNTNL`→`SNTNL1` (מדוד), ⛔ **נמדדו `100` המניות וה-stop**: `:436-437` סופר **שורות** בלבד וההערה `:434-435` טוענת «shares must have survived» ⛔ ואוכפת ‹R-3›; הלוח ⛔ קרס (⛔ `panel-crashed\|risk`) ⛔ **ו⛔ נקרא ה-`—` באחוז** — ⛔ אסרציה קוראת את התא. 🔴 **⓷ ⓸ ⛔ נענו כלל** — ⛔ הוזרקה הצורה הישנה ו⛔ נבדק deploy של `51a12d2` ⇒ **זרוע הביקורת ⛔ קיימת** ⇒ ⛔ הוכח שהסנטינל **מדווח** על קריסה, רק שהוא **שותק** כשאין. | 🔴 **⛔ אין ממצא על המסך — הפריט נפתח פתוח.** `B-305` נסגר ב**אימות-מקור** בלבד: `probe:firing` `8/8` ברמת הערך, שלוש זרועות בקרה מתות בשני העצים. ⚠️ **ו-`G2` הכריע שזה ⛔ מספיק לסגירה מלאה** — ד.2 (דפדפן) **חסום ומוצהר**, ⛔ «בוצע» |
| C-045 | 🆕 **פתיחת מודל עריכה על מסך אמיתי — האם שדה הטיקר מלא ב**פריים הראשון**.** נולד מ-`B-314` (10.09), `PLAN-2026-09-10-b314-modal-race.md`. הפרוב (`probe-real.mjs`) מודד `L0` — `useLayoutEffect` של אח, **חוזה React** ⛔ הגרלת-מתזמן — על **הבייטים האמיתיים** של `SwingEdge_App.jsx` שחולצו לפי עוגן, ומחזיר `0/8` בלוע קר ו-`0/8` בפתיחה חוזרת מול זרוע-ביקורת **נעוצה `8/8`**. 🔴 **⛔ ואף אחד מהמספרים האלה ⛔ הוא עין** — הפרוב ⛔ מהדר Tailwind · ⛔ טוען את ה-CSS של האפליקציה · ⛔ מריץ RTL · ⛔ קורא-מסך · ⛔ פרודקשן; הוא מודד ש-`input.value` ⛔ ריק בקומיט, ⛔ שמשתמש **רואה** ערך. אותו גבול בדיוק של `C-036`/`C-038`/`C-039`/`C-041`/`C-043`. **מה לבדוק:** ⓵ **פתיחה קרה** — טעינת הג'ורנל, לחיצה על ✏️ בעסקה בעלת טיקר ידוע ⇒ שדה הטיקר מציג את הטיקר **מיד**, ⛔ הבהוב ריק · ⓶ **פתיחה חוזרת** — סגירה, לחיצה על ✏️ ב-עסקה **אחרת** ⇒ הטיקר החדש מוצג, ⛔ הישן · ⓷ **שאר השדות** — כניסה · סטופ · יעד · מניות · הערות: `6/6` מלאים בפריים הראשון, ⛔ הטיקר בלבד · ⓸ **a11y על מסך** — הפוקוס **נכנס** לדיאלוג בפתיחה ו**חוזר** לכפתור ✏️ בסגירה (הפרוב מדד `8/8`+`8/8`, ⛔ בדפדפן אמיתי עם קורא-מסך) · ⓹ **RTL** — הכיוון ⛔ נשבר בפירוק-ומונט-מחדש שה-`key` מכריח · ⓺ **⛔ הבהוב** — ה-`key` **מפרק** את המופע בכל פתיחה; יש לוודא ש⛔ נראית קפיצה או אנימציה כפולה. ⚠️ **⓺ הוא הסיכון ה**חדש** שהתיקון הכניס** — `0/8` מודד ש⛔ ריק, ⛔ שהמעבר חלק | תנאי-גלישה: **כל נגיעה ב-`SwingEdge_App.jsx:8266-8272`** (אתר הרינדור של `EditTradeModal`) · **או ב-`src/components/EditTradeModal.jsx`** — `initForm` · `useState` · ה-`useEffect` על `[trade?.id]` · `useModalA11y` | ✅ **בוצעה 11.09** — ניב, בפרודקשן, `Cmd+Shift+R`, 00:36 | ✅ **`4/4` הנקודות שנבדקו — ⛔ תקלה.** ⓵ **פתיחה קרה** ⇒ שדה הטיקר **מלא בפריים הראשון**, ⛔ ריק · ⓶+⓷ **פתיחה חוזרת בעסקה אחרת** ⇒ **כל השדות התחלפו**, נצפה בשני צילומים: `NVDA` (`225.16`·`215`·`280`·פריצה·עלייה) מול `BE` (`212.41`·`197.52`·`274.86`·בלימה·ירידה) ⇒ ⛔ הבזק · ⛔ ערבוב · ⓺ 🔴 **הסיכון שה-`key` הכניס — ⛔ התממש:** ⛔ הבהוב · ⛔ קפיצה · ⛔ אנימציה כפולה ⇒ **`0/8` מדד ריקנות בלבד, וההחלקה אוששה בעין** ו⛔ בפרוב · ⓸ `Esc` ⇒ נסגר, ו**הפוקוס חזר** לכפתור העריכה. ⇒ `B-314` נסגר → **`D-079`**. ⚠️ **והמכנה הוא `4`, ⛔ `6`** — ⓹ (RTL) ⛔ דווח בנפרד ו⛔ נמדד; הוא נשאר בגוף הבדיקה לגלישה הבאה ו⛔ מסומן ירוק. ⚠️ **וזו עדות עין, ⛔ אסרציה** — הגבול של `C-036`/`C-043` נחצה כאן על ⓵·⓶·⓷·⓺·⓸ בלבד |
