# DECISIONS — החלטות נעולות

> שורה אחת להחלטה: **תאריך · ההחלטה · הנימוק · מה נפסל.**
> קובץ append-only. החלטה שמתבטלת — שורה חדשה שמבטלת את הישנה, לא מחיקה.
> תאריך שלא ניתן לאמת מול קומיט או `docs/INCIDENTS.md` — מסמנים ככזה במפורש.

| תאריך | ההחלטה | הנימוק | מה נפסל |
|-------|---------|---------|----------|
| 2026-07-19 | אין service-role key ב-Vercel — RPC עם security-definer | `is_admin()` נבדק בתוך ה-DB, RLS מגביל לנתוני המשתמש; ה-key לא קיים בשום סביבת ריצה | הטמעת service-role key בפונקציה — חשיפה שלא ניתנת לביקורת |
| 2026-07-25 | Vercel serverless ל-`send-invites`, לא Supabase Edge Function | ה-JWT של הקורא מועבר ישירות ל-RPC; אין צורך ב-service-role key | Edge Function — הייתה מחייבת service-role או קפיצת auth נוספת |
| 2026-07-25 | שליחה מיידית בלחיצה, לא תור מתוזמן | `SEND_GAP_MS=300` → 25 מיילים בתוך `maxDuration` 60s; אין תור לתחזק ואין מצב ביניים | תור מתוזמן — מורכבות תפעולית בלי תמורה |
| 2026-07-25 | תקרה יומית נאכפת ב-DB (`admin_invite_send`, DAILY 120 / BATCH 25) | לקוח לא יכול להרחיב אותה; RLS מאמת admin בתוך ה-RPC ולא בשכבת האפליקציה | אכיפה במשגר — ניתנת לשינוי בקוד, בלי קשירה |
| 2026-07-26 | עסקת QA מזוהה לפי `ticker='SNTNL'` | ticker נקבע ביצירה ואינו משתנה → סחיפה דטרמיניסטית ב-`?ticker=eq.SNTNL` | שדה notes — ניתן לעריכה או מחיקה תוך כדי ריצת הבדיקה |
| 2026-07-26 | חלון gate 45→09 + `concurrency{group:sentinel-auth, cancel-in-progress:false}` | מתזמן GitHub מאחר דרך קבע 5–15 דק'; ה-`concurrency` הוא ההגנה האמיתית מפני מרוץ SNTNL, החלון רק אופטימיזציה | 45–59 — שכבת ה-auth פשוט לא הייתה רצה · 45→14 — חופף לריצת `:20` שמאחרת, auth פעמיים באותה שעה על אותו חשבון |
| 2026-07-26 | monitor לא מסיק בריאות מהיעדר סיגנל שלא נאסף | בדיקה שרצה על gate צר ממחזור הדיווח **חייבת** להצהיר אם רצה (`AUTH_EXPECTED`), והמדווח חייב להתייעץ בזה לפני שהוא מכריז על התאוששות | הסקת התאוששות מהיעדר תקלה — ראה `INCIDENTS.md` #6 |
| 2026-07-26 | שדה `reason` נושא תצפיות בלבד; היפותזות שייכות ל-`fix` | ניחוש שהודפס כעובדה עיגן שלושה אבחונים שגויים ברצף | היפותזה מקודדת בקשיח בתוך הדיווח |
| 2026-07-26 | `~/Swing-edge` לא זז · session אחד בכל רגע · אין worktrees | נתיבים קשיחים ב-CI ובסוכני Cowork; עבודה מקבילה היא מה שהוביל ל-force-push. **תאריך ההחלטה המקורי לא אומת** — הכלל נהג בפועל קודם; העקבה הקרובה היא `5e65fc7` (2026-05-22, untrack worktrees) אך היא אינה מתעדת את הכלל עצמו. התאריך כאן הוא היום שבו נכתב | worktrees / עבודה מקבילה על אותו ריפו |
| 2026-07-26 | force-push אסור מוחלט — נאכף ב-`.githooks/pre-push` | שני מסמכים שסותרים זה את זה בנושא הרסני הם באג בפני עצמו: הקורא הבא עושה את מה שנוח לו. מקור-אמת-אחד גם בחוקים | היתר force-push "עם אישור מפורש" (`CONTEXT.md` #8 בנוסחו הישן) · `--force-with-lease` |
| 2026-07-27 | לא מורידים react-router-dom ל-7.11.0 למרות high | ה-CVE הוא RSC Mode CSRF; האפליקציה משתמשת ב-BrowserRouter דקלרטיבי בלבד (src/main.jsx) — לא ישים | `npm audit fix --force` — downgrade breaking בתלות production על פגיעות שאינה ישימה |
| 2026-07-27 | אישור יציאה מ-Plan Mode ≠ אישור ביצוע | Plan Mode אוסר git write עד אישור, ו-§9 דרש push לפני אישור — הנוהל היה בלתי ניתן לביצוע וייצר חשד שווא ל"קומיט רפאים" | הדבקת התוכנית בצ'אט (באג קבצים ריקים) · עקיפת Plan Mode |
| 2026-07-27 | S2.3 (סינון 404 של financialmodelingprep) אומת בפרודקשן | `AUTH_EXPECTED: 1` בלוג ריצה 30253015899 (2026-07-27T09:13:31Z) — שכבת ה-auth אכן רצה, ההתאוששות אמיתית ולא אינצידנט #6 חוזר | הסתמכות על היעדר תקלה כהוכחה להתאוששות |
| 2026-07-27 | GA4 מגודר בהסכמה, @vercel/analytics לא | GA4 כותב עוגיות `_ga` מתמידות; Vercel Analytics cookieless וללא מזהה אישי — הקו הוא עוגיות, לא "אנליטיקס". הפער מוסבר במפורש ב-/privacy | גידור שניהם — היה מוחק את קו הבסיס · אי-גידור שניהם — GA4 כותב עוגיות |
| 2026-07-27 | ה-gutter 104px/92px ב-`ConsentBanner.css` נאכף באסרשן ב-sentinel, לא בזיכרון | המספר צמוד ל-FAB ב-`SwingEdge_App.jsx:6671`; קבוע CSS שמסתמך על זיכרון אנושי סוחף בשקט עד שהוא מכסה את פעולת הליבה | תגובה על ה-FAB — היה מחייב שינוי ב-`SwingEdge_App.jsx` · הסתמכות על z-index במקום על גאומטריה |
| 2026-07-27 | באנר ההסכמה ב-`z-index: 91` | `95` תפוס בשני overlays של מסך מלא (`ToastProvider.jsx:109` ConfirmProvider, `AdminPanel.jsx:248`) — באנר שמופיע מאוחר יותר ב-DOM היה צף מעל דיאלוג אישור | `95` — היפוך יחסי הכיסוי · מעל `120` — היה מנקב את ה-spotlight של `OnboardingTour` |
| 2026-07-27 | לאמת זהות המשתמש המושפע לפני בניית תוכנית סביבו | תוכנית FIN קבוצה 1 הקצתה ציר "משתמשים 🔴" והודעה יזומה למשתמש שהתברר כניב עצמו — הנחה במקום שאילתה | להסתמך על "יש משתמש מושפע" בלי לשאול את auth.users |
| 2026-07-27 | **אפס artifacts ואפס נתוני משתמש ב-stdout כל עוד הריפו PUBLIC** | `niveven183/Swing-edge` הוא ריפו ציבורי (אומת ב-`gh repo view --json visibility`), ולכן artifacts, לוגי ריצה ו-job summaries ניתנים להורדה בידי כל אדם באינטרנט. סוכן CI שקורא נתוני משתמשים חייב לשלוח פירוט בערוץ פרטי — מייל דרך `MAIL_USERNAME`/`MAIL_PASSWORD` — ולהשאיר ב-stdout מספרים מצטברים בלבד. הכלל חל על כל סוכן עתידי, לא רק על User Analytics | artifact בן 30 יום עם דוח התנהגות פר-משתמש (כפי שהופיע בבריף המקורי) — היה מפרסם פרופילים התנהגותיים של 32 משתמשים · הדפסת הדוח ל-job summary כ"גיבוי" למקרה שהמייל נכשל |
| 2026-07-27 | User Analytics שלב א' — אפס LLM, SQL דטרמיניסטי בלבד | הדוח נקרא כל בוקר ומשמש לקבלת החלטות; מודל שמנסח מחדש בכל ריצה הופך שינוי בניסוח לשינוי-לכאורה בנתון, ומכניס עלות ואי-דטרמיניזם לנתיב שכולו ספירות. בנוסף — שליחת נתוני משתמשים ל-API חיצוני היא ערוץ יציאה נוסף שדורש ביקורת | שכבת סיכום LLM מעל התוצאות (`ANTHROPIC_API_KEY` כבר קיים בריפו, כך שהפיתוי היה זמין ללא עלות הקמה) |
| 2026-07-27 | זיהוי עסקאות מיובאות = OR של שלושה סימנים, לא חלון-זמן | חלון 10 דקות מסודר תפס **0 שורות**: המייבא כותב `createdAt` מה-CSV, כך ש-173 העסקאות של המקרה הידוע פרושות על פני 9 חודשים ולא על 10 דקות. `"createdAt" < auth.users.created_at` תפס 25/25 | "חותמת בשנייה שלמה בלי חלקיק שנייה" — יורה על 24 מתוך 40 עסקאות של המשתמש הידני הבריא ביותר (`92a06c0c`: 100% `stop`, 95% `notes`); נדחה על בסיס מספרי. הכלל הצר `11:30:00.000000` נשמר — 25/25, חופף בדיוק ל-predates-signup |
| 2026-07-28 | Watchdog מנוטר ע"י Fleet Daily, לא ע"י עצמו | סוכן שרץ אינו יכול לדווח שהוא לא רץ; fleet-daily ב-06:00 קודם ל-watchdog ב-08:00 ולכן תמיד בודק ריצה שהסתיימה | הוספת watchdog.yml ל-MAXAGE של עצמו — חסר משמעות · סוכן שלישי — תחזוקה מיותרת |
| 2026-07-28 | אוכלוסייה מפורשת לכל שדה נמדד, בלי ברירת מחדל | ארבעה שדות בלתי תלויים (followedPlan · exitReason · maxFavorable · lessonLearned) החזירו 33% זהה — חתימה של מכנה שגוי, לא ממצא. טבלה בלי ברירת מחדל הופכת את הכשל לבלתי אפשרי במקום ללא-סביר | assert בלבד — מפספס שאילתות אד-הוק ומספרים בפרומפט · ברירת מחדל POP_ALL — שדה חדש היה נשען עליה בשקט |
| 2026-07-28 | test:coach אינו שער רגרסיה למתמטיקת R | הפיקסצ'רים ב-coach-invariance-test.mjs:52 בונים status:"closed" ו-exitPrice; getClosed דורש "CLOSED" ו-exit — כולם מסוננים מהמסלול הסטטיסטי. 110 האסרשנים מגנים על אינווריאנטיות DecisionCoach בלבד | להסתמך על 110 כראיה לכיסוי — הנחה שהוחזקה עד שנבדקה |
| 2026-07-28 | `avgR` וכל נגזרותיו הם `number \| null`, לעולם לא `0` | עסקה בלי `stop` לא עשתה 0R — אין לה מיקום ביחס ל-1R כלל. `0` הוא ערך חוקי (עסקה שיצאה בדיוק בכניסה) ולכן אינו יכול לשמש גם כ"לא נמדד". כל צרכן שקורא R חייב להחליט מפורשות מה קורה כשאין מדידה | `?? 0` בנקודת החישוב — הפך היעדר מדידה לתצפית, ומשך כל ממוצע לכיוון אפס ביחס ישר למספר הסטופים החסרים |
| 2026-07-28 | BE נשאר במכנה של Expectancy, בתרומה 0R, כאוכלוסייה שלישית מפורשת | שלוש אוכלוסיות (win/loss/be) שסכום משקליהן 1, במקום `1 - winRate` שסיווג כל BE כהפסד. עסקה שיצאה בתיק אינה הפסד ואינה זכייה — היא תצפית אמיתית בתרומה אפס | להוציא BE מהמכנה — היה מנפח את Expectancy · להשאיר `1 - winRate` — כל BE נספר כהפסד מלא |
| 2026-07-28 | Expectancy זז ב-ulp אחד (1.1499999999999997 → 1.15) והערך החדש נשמר כבסיס | המשקל החדש סופר את אוכלוסיית ההפסד ישירות (`losses/n` = 0.3 מדויק) במקום `1 - winRate` (0.30000000000000004). התוצאה קרובה יותר לערך המתמטי, לא רחוקה ממנו. במקום להקפיא ליטרל חדש, נוסף אסרשן־זהות: בלי BE — Expectancy הוא avgR בדיוק | סובלנות מספרית (`Math.abs(d) < 1e-9`) — הייתה מסתירה גם סחיפה אמיתית · שמירת הליטרל הישן — הייתה מקבעת את הפחות מדויק מבין השניים |
| 2026-07-29 | מיפוי עמודות לפי ספציפיות, לא לפי סדר בקובץ | סדר עמודות משתנה בין ברוקרים; "מס' נייר / סימול" נושא את הסימבול האמיתי (PLTR) בעוד "שם נייר" נושא שם חברה שאינו ניתן להצלבה מול מחיר חי. אותו מנגנון פותר גם את "שער עלות ממוצע" מול "שער" ואת פסילת טוקן-בודד ב-fallback | הסתמכות על סדר העמודות · דחיית התיקון לשלב ב' — מותיר באג חי בכותרות אנגליות |
| 2026-07-29 | `test:coach` תוקן — מכסה עכשיו גם שה-5 עסקאות ב-`seeded` באמת עוברות דרך `getClosed`/`calcTradeMetrics`, לא רק אינווריאנטיות פרופילים | הפיקסצ'ר החסיר `shares` לגמרי (מעבר ל-`status`/`exit` שכבר אובחנו ב-28.07) — בלעדיו `calcTradeMetrics` היה מחזיר `NaN` בשקט. `shares:20` משחזר בדיוק את ה-`pnl`/`rMultiple` שכבר היו כתובים בפיקסצ'ר כליטרל מת. אסרשן שמירה חדש (#111) מוודא ש-100% מעסקאות היסטוריה לא-ריקה עוברות `getClosed` | ריכוך הציפיות כדי שהאסרשנים יעברו — verdict נשאר זהה בכל 5 התרחישים; שני שינויי confidence (`earnings-window` 62→100, `no-target` 40→64) הם הביטוי הישיר של המנוע שסוף-סוף מקבל את חמש העסקאות, לא תיקון-כדי-לעבור |
| 2026-07-30 | `getClosed` (`status==="CLOSED"` **וגם** `exit != null`) הוא ההגדרה **היחידה** של "עסקה סגורה" בכל האפליקציה | בפרודקשן היום 0/102 עסקאות `CLOSED` בלי `exit` — זהו **אינווריאנט הגנתי, לא תיקון נזק**. עסקה בלי מחיר יציאה אין לה P&L, ולכן כניסתה לאוכלוסייה סטטיסטית מייצרת עסקה ברווח 0 שאינה BE ומדללת כל שיעור. פיקסצ'רה 6 (`closedWithoutExit`) מכריחה את החוזה להיות מוכח בקוד ולא מוסכם בעל-פה | סינון לפי `status` בלבד — עובד עד השורה הראשונה שתישבר · לתקן רק את האתרים ה"חשובים" — משאיר את הסתירה על המסך |
| 2026-07-30 | `statisticalModels` נשאר 0..1 · `tradingStats` מחזיר 0..100 · ההמרה בגבול **אחד ומסומן** | שני הסולמות קיימים מסיבה: המנוע מכפיל שיעורים זה בזה (Kelly, Wilson) ולכן חייב שבר; המסך מציג אחוז. איחוד לסולם אחד היה מחייב `*100` ו-`/100` מפוזרים — בדיוק הסחיפה שקבוצה 2 מוחקת | סולם אחד לכל המערכת — מפזר המרות · המרה בכל צרכן — כל צרכן חדש הוא הזדמנות לשכוח |
| 2026-07-30 | `Infinity` הוא **סנטינל מוצהר** ל-`profitFactor` כשאין הפסדים כלל | `null` נראה בטוח יותר והוא מלכודת: `Number(null)===0` ולכן `isFinite(null)===true`, ההגנה הקיימת ב-`SwingEdge_App.jsx` הייתה עוברת, ואז `null.toFixed(2)` זורק — מסך לבן. `Infinity` נכשל בבדיקה ומגיע ל-`"∞"` כמתוכנן. אגב כך הוקשח `isFinite` → `Number.isFinite` | `null` — כשל שקט שהופך למסך לבן · `0` — טוען שהתיק מפסיד כשהוא רק לא הפסיד מעולם |
| 2026-07-30 | `expectancy`/`kelly` **לא** נוספו לפלט `tradingStats` למרות שהם פרימיטיבים משותפים | החלטה 5 קובעת מאיפה פרימיטיב **נשאב**, לא שחובה לפלוט אותו. שדה שאף מסך אינו צורך הוא חוב: הוא ייבדק, יתועד ויתוחזק בלי שאיש יראה אותו. מנוע האינטליגנציה כבר צורך אותם ישירות מ-`statisticalModels` | הוספה "ליתר ביטחון" — שדה יתום שמייצר עבודה ולא ערך (הכרעת ניב, 30.07) |
| 2026-07-30 | דלי `open` נשאר כפי שהוא; במקום שדה `unmeasurable` נוסף **אסרשן שלמות** בכל פיקסצ'רה | `totalTrades + openTrades + לא-נספרות === trades.length`. שדה פלט חדש היה מחייב כל צרכן להתייחס אליו; אסרשן פנימי תופס עסקה שנעלמת משתי האוכלוסיות בלי להרחיב את חוזה ה-API | שדה `unmeasurable` — מרחיב את פני-השטח לכל צרכן · לא לבדוק כלל — העלמות שקטה (הכרעת ניב, 30.07) |
| 2026-07-30 | סחיפת ה-ULP שנחזתה ב-`profitFactor` **לא התרחשה**, ולכן אין מה לנעול | 123 שדות נוספו לבייסליין · 0 הוסרו · **0 ערכים השתנו**. איחוד שני מעברי `reduce` ל-`grossPnl` יחיד שומר על סדר הסכימה, והיפוך סימן ב-IEEE-754 מדויק — `Math.abs(Σ שליליים)` שווה ביט-בביט ל-`Σ Math.abs(שלילי)`. הכלל של 28.07 (אסרשן־זהות על הערך החדש, לא tolerance) נשאר בתוקף למקרה הבא | להוסיף tolerance "ליתר ביטחון" — היה מסתיר סחיפה אמיתית בעתיד · לדווח "עבר" בלי לפרט את היקף התוספות |
| 2026-08-01 | מחיר חי **לעולם אינו** ממלא שדה שה-OCR נכשל לקרוא | זהו הכשל השקט עצמו: הטריידר אינו יכול להבחין בין מחיר שנקרא מהגרף למחיר שהודבק מהשוק, ולכן שדה שהתמלא אוטומטית אחרי כשל OCR נראה בדיוק כמו הצלחה. השומר הוא ref ממוקד-טיקר — טיקר אחר משחרר אותו אוטומטית, לחיצה מפורשת (`force`) גוברת, ואפקט על ניקוי הבאדג' משחרר בכל אתרי האיפוס. **המחיר החי עצמו נשאר פיצ'ר מבוקש** — הצ'יפ ממשיך להציג `LAST CLOSE`; רק המסלול לשדה נחסם | לבטל את המחיר החי — הורג פיצ'ר תקין בגלל באג באינטגרציה · דגל בוליאני חד-פעמי — לא היה נפתח בטיקר חדש · ניקוי ידני ב-8 אתרי איפוס — כל אתר עתידי הוא הזדמנות לשכוח |
| 2026-08-01 | "אין כלי פוזיציה" מקבל `Math.min(modelConf, 25)`, לא שדה `reason` חדש | ה-spread דרס את `nullResult.confidence` ושלח שלושה `null` עם ביטחון 85. תקרה מתחת ל-`MIN_CONFIDENCE` (40) נקראת ככשל בכל צרכן **קיים** בלי לשנות אף אחד מהם — שדה חדש היה מחייב שלושה באדג'ים ולקוח לדעת עליו, כלומר כשל שקט חדש בכל צרכן ששכח. 25 ולא 0 במכוון: "בטוח שאין כלי" הוא מידע אמיתי ששונה מ"לא הצלחתי לקרוא את התמונה" (0) | `0` — מוחק את ההבחנה בין שני מצבי כשל שונים · שדה `reason` חדש — כל צרכן שלא עודכן ממשיך להציג ירוק · `validate()` על המסלול — אין לו מה לאמת, אין מחירים |
| 2026-08-01 | מצב ה-OCR נגזר ממה **שהתמלא** (`ocrDetection`), לא מה-`confidence` | `confidence` הוא הערכת המודל על עצמו; המשתמש שואל "האם השדות שלי מלאים". ארבעה מצבים — `full`/`partial`/`ticker_only`/`none` — ו-`partial` (entry בלי stop) **לעולם אינו ירוק**, כי עסקה בלי stop אינה ניתנת למדידה ב-R. נקודת החלטה אחת (`ocrBadgeState`) לשלושת הבאדג'ים שהיו שלושה מימושים כפולים; כל אתר שומר את מפת הצבעים הליטרלית שלו כי §13 אוסר אינטרפולציית class | סף `confidence` בלבד — בדיוק הבאג ("OCR ✓ 85%" על טופס ריק) · שלושה מצבים בלי `partial` — ממצא D (stop שלילי) היה נשאר ירוק · פונקציה שמחזירה מחרוזות class — נשברת ב-Tailwind JIT |
| 2026-08-01 | **גיאומטריה נמדדת גוברת על התווית של המודל, ושתיהן גוברות על הטוגל של המשתמש** — היררכיה מוצהרת ב-`sideSource`: `geometry` → `tool` → `user` | הדלתאות שהמודל קורא הן **חסרות סימן**, ולכן `side` לבדו מכריע אם הסטופ נוחת מעל או מתחת לכניסה — כיוון שגוי מייצר עסקה שלמה, סבירה והפוכה. מבחן AFRM החי של 01.08 הוא בדיוק זה: המודל תייג SHORT, הקווים שהוא עצמו קרא היו LONG חד-משמעית, ומה שנשמר היה 79.74/63.43 במקום 68.44/84.75. מחיר מוחלט שנקרא מתקבל כראיית-כיוון **רק** כשמרחקו מהכניסה תואם את הדלתא המהימנה בתוך `GEO_TOL=0.005` — ראיה מאומתת-גודל, לא קריאה בודדת | להאמין לתווית `direction` של המודל — היא בדיוק מה שנכשל · להאמין תמיד לטוגל של המשתמש — מבחן MRVL הוכיח שהוא לפעמים הטועה · לקבל מחיר קרוא בלי אימות גודל — קריאה שגויה אחת הייתה הופכת עסקה תקינה |
| 2026-08-01 | **קונפליקט כיוון מוצג, לעולם אינו מוכרע אוטומטית** — הטוגל לא זז, מוצגת אזהרה כתומה שנוקבת בשני הכיוונים בשם; עדכון שקט של `side` רק כשאין סתירה | היפוך אוטומטי של הטוגל היה הופך את מבחן AFRM לעסקת SHORT **פנימית-עקבית** שעוברת את `validateTradeInputs` בשקט — כלומר הורג את הרשת היחידה שתפסה את הבאג מלכתחילה, ומייצר בדיוק את מחלקת הכשל השקט ש-F1/F2/F3 סגרו באותו בוקר. הניסוח המקורי בפרומפט של ניב היה "הודעה שקטה + היפוך"; **ניב הפריך את עצמו וקיבל את הניסוח המתוקן** | היפוך אוטומטי של הטוגל (הנוסח המקורי) — כשל שקט חדש · חסימת השמירה בקונפליקט — מעניש טריידר על קריאה נכונה של המודל · שתיקה מוחלטת — משאיר לטריידר להסיק כיוון הפוך מבאנר ולידציה אדום |
| 2026-08-01 | `stopPrice`/`targetPrice` מהמודל הם **cross-check בלבד, לעולם לא מקור מחיר** — בדיוק התפקיד ש-`entry` ממלא היום | המחירים המוחלטים על הפאנל נתונים ל-OCR שגוי בספרה בודדת; הנגזרת `entry ± delta` נשענת על שני מספרים שכבר הוצלבו זה מול זה (delta מול percent). לכן הם משמשים לשני דברים בלבד: הכרעת כיוון מאומתת-גודל, ווטו על רמה מחושבת שסוטה מהקריאה ביותר מ-`GEO_TOL` (`stop`/`target` → `null`, לא החלפה) | להשתמש בהם כמחיר — מחליף חישוב מוצלב בקריאה בודדת · להתעלם מהם — מוותר על ראיית הכיוון היחידה שאינה תלויה בתווית המודל |
| 2026-08-01 | `max_tokens` 300→340, **מותנה בכך שתרחיש C הקפוא (חיתוך JSON) מתנהג זהה** | שני שדות חדשים בסכימה מרחיבים את התשובה; 300 היה חותך את ה-JSON באמצע והופך קריאה תקינה ל-`confidence: 0`. C נבדק אחרי ההגדלה והוא ביט-בביט זהה — הוא נחתך על מחרוזת קבועה בסטאב, לא על התקרה, ולכן ההגדלה אינה מחלישה את השער שלו | להשאיר 300 — חיתוך שקט על כל קריאה מלאה · להגדיל בלי לאמת את C — היה מוותר על השער שמוכיח שחיתוך עדיין מדרדר נכון |
| 2026-08-01 | `dupKey` ביבוא הורחב ל-`ticker\|date\|entry\|exit\|shares\|exit-date` — **שינוי מוצהר גם למסלול הגנרי** | המפתח בן שלושת השדות היה אגרסיבי-יתר: קנייה אחת של 100 שנסגרת ב-40 ואז ב-60 מייצרת שתי עסקאות סגורות לגיטימיות שחולקות `ticker\|date\|entry`, השנייה סומנה ככפילות, ו-`importDupes` ברירת מחדל `false` מחק אותה **בלי מילה**. תאריך היציאה נלקח כתאריך ולא כחותמת מלאה: יבוא נוחת ב-20:00 ועסקה שנסגרה ידנית נוחתת בשעה שרירותית, והשוואת חותמות הייתה מונעת זיהוי יבוא-חוזר של עסקה שנסגרה ביד. תרחיש 5 עובר ללא תזוזה באסרשנים — הזרע בו הושלם לעסקה אמיתית, כי אובייקט בלי `shares` ובלי `status` לא יכול היה להתקיים ב-`public.trades` מלכתחילה | להשאיר את המפתח הצר — FIFO היה מוחק מכירות בשקט · לכלול את `closedAt` המלא — היה שובר זיהוי כפילות מול עסקאות שנסגרו ידנית · לוותר על `shares` — שתי סגירות באותו מחיר ובאותו יום היו מתנגשות |
| 2026-08-01 | היפוך סדר שורות ביבוא רק בירידה **חד-משמעית** (ירידה אחת לפחות, אפס עליות); קובץ מעורבב נשאר כפי שהוא ומקבל שורת log | לשני הברוקרים אין עמודת שעה (`brokerProfiles.js:25-53`), ולכן שובר-השוויון היחיד בתוך יום הוא סדר השורות. ברוקר שמייצא מהחדש לישן היה הופך כל זוג באותו יום למכירה יתומה. ניחוש על קובץ מעורבב היה גרוע מהמיון היציב שכבר קיים, ולכן הוא נקרא בשם (`orderNote: "mixed"`) במקום להיות מתוקן | היפוך על כל ירידה שהיא — קובץ מעורבב היה נהרס · אי-היפוך כלל — קובץ הפוך היה מייצר SHORTים יתומים בשקט · מיון לפי חותמת זמן — אין חותמת זמן בקובץ |
| 2026-08-02 | `realizedAt()` / `realizedDayKey()` ב-`src/utils.js` הם **מקור-האמת היחיד** ל"מתי רווח מומש" — `closedAt`, ובנפילה לאחור `date`. שתי פונקציות ולא אחת, במכוון | כל חישוב שמדבר על רצף (עקומת הון, drawdown, רצפים) צריך **רגע מדויק** למיון, וכל חישוב שמדבר על יום (לוח שנה, דוח חודשי, "היום") צריך **יום מקומי** לדליים. פונקציה אחת הייתה מכריחה כל צרכן להמיר בעצמו — בדיוק הסחיפה שקבוצה 2 מחקה. הנפילה לאחור ל-`date` היא הצהרה: עסקה פתוחה **מסודרת כאילו מומשה ביום הכניסה**, ולא נעלמת | שדה `realizedAt` מחושב על העסקה עצמה — היה דורש מיגרציה ו-backfill · פונקציה אחת שמחזירה יום — הורגת את המיון בתוך יום · `?? 0` בנפילה — היה ממקם עסקאות בלי תאריך ב-1970 בראש כל עקומה |
| 2026-08-02 | קבוצה 4 נסגרה ב**אפס מגע בנתונים** — לא כפשרה אלא כמסקנה | ההנחה שהובילה לתוכנית ("14 עסקאות `CLOSED` בלי `closedAt`") נפלה במדידה: 14/14 הן `OPEN` עם `exit IS NULL`, כלומר `closedAt: null` הוא הערך ה**נכון** לפי חוזה `deriveCloseState`. אפס אנומליות בכל הטבלה (0 תאריכים עתידיים · 0 לפני 2000 · 0 `closedAt < date` · 0 `CLOSED` בלי חותמת · 0 `OPEN` עם חותמת). כשאין נזק בנתונים, backfill אינו "תיקון זהיר" — הוא כתיבה מיותרת על נתוני משתמש | backfill של `closedAt` מ-`date` — היה ממציא תאריכי סגירה לעסקאות פתוחות · מיגרציה "רק ל-NULL" — WHERE צר על אוכלוסייה שלא הייתה קיימת |
| 2026-08-02 | פיקסצ'ר שנגזר מעצמו אינו יכול לחסום באג בממד שממנו הוא נגזר — נבדק במפורש לפני שסומכים על baseline | `mk()` ב-`scripts/tradingstats-test.mjs:88` מייצר `closedAt` **מתוך** `date`, ולכן בכל פיקסצ'ר קיים יום-הסגירה שווה ליום-הכניסה. ה-baseline הקפוא **לא זז** בתיקון FIN-019/020 — לא כי הקוד לא השתנה, אלא כי אף תרחיש לא היה מסוגל להבחין בין מיון-לפי-כניסה למיון-לפי-סגירה. תרחיש `6c` (`LONGHOLD` נכנס ראשון, נסגר אחרון — Max DD 14% מול 12.5%) נוסף ואומת שהוא **נכשל** על הקוד הישן | לקרוא ל-baseline שלא זז "הצלחה" — היה מאשר שער עיוור · לעדכן את ה-baseline "ליתר ביטחון" — היה מקבע ערך שאיש לא בדק · לדלג על אימות-כישלון של 6c — טסט שלא נצפה נכשל אינו שער |
| 2026-08-02 | `matched` **הוסר** מ-`matchIdeaToEdge`; נשאר `score` רציף, והסף שייך לצרכן (`EDGE_MATCH_MIN = 1`) | הסף 0.75 לא היה נגיש **בבנייה**: ההתאמה החלקית נבנית משלושה ממדים, ולכן כל הציונים החלקיים האפשריים הם 0/⅓/½/⅔ — הדגל נדלק רק בהתאמה מדויקת, שבה הוא מיותר. דגל בוליאני שלעולם אינו נדלק הוא שקר; ציון רציף אומר אמת. צרכן שדורש בוליאני מגדיר סף בעצמו ומתעד אותו (הכרעת ניב, 02.08) | ריכוך הסף ל-0.66 — היה משנה התנהגות של שני צרכנים בשקט · השארת הדגל "כי הוא לא מזיק" — קוד מת שהאודיט הבא יגלה מחדש · `matchIdeaToAntiEdge` **לא נגעה**: ה-`matched` שלה הוא התאמה-מדויקת ולכן אמיתי |
| 2026-08-02 | `CAUTION` יצא ממחשב הדיוק של `calibrationReport`; דלי לא-נמדד מקבל `accuracy: null`, והמכנה מוצהר (`scoredN` + `accuracyBasis: "GO/SKIP"`) | `CAUTION` אינה תחזית בינארית ולכן אין לה "נכון/לא נכון" — כל תחזית CAUTION נספרה כנכונה וניפחה את הדיוק הכולל. `null` ולא `0`: "לא ניתן לניקוד" אינו "ניקוד אפס". **תנאי ניב:** אחוז בלי מכנה מפורש הוא §2, ולכן הדוח נושא את `scoredN` ואת שם האוכלוסייה עמו. הצרכן היחיד היום (`SwingEdgeAI.js:129`) אינו מרונדר בשום מסך — **אפס מספר גלוי זז** | להשאיר CAUTION במונה — דיוק מנופח שנראה כמו הצלחה · להוציא אותה גם מ-`n` — היה מוחק את העובדה שהמנוע פלט תחזיות · לפלוט `accuracy` בלי `scoredN` — מנה בלי מכנה |
| 2026-08-02 | `LOCK_STALE_WEEKS = 6` — נעילת anti-edge שראייתה בת יותר מ-6 שבועות יורדת מ-`locked` ל-`warnings`, **וכל נעילה חיה נושאת תאריך שחרור גלוי** | ההערה בקוד הבטיחה שחרור אוטומטי אחרי שבוע מרוויח, אבל הנעילה עצמה **חוסמת** את הכניסות שהיו מייצרות את אותו שבוע — תנאי שחרור שדורש פעולה שהנעילה חוסמת הוא מלכודת, לא עיצוב. או שהקוד מקיים את ההבטחה או שההבטחה יורדת; בחרנו לקיים. **תנאי ניב:** נעילה בלי תאריך שחרור גלוי היא כשל שקט בפני עצמה, ולכן ההודעה מסתיימת ב-`החסימה תוסר אוטומטית ב-YYYY-MM-DD` | הורדת ההערה במקום תיקון הקוד — משאיר טריידר נעול לצמיתות על 3 עסקאות · שחרור על ראיה חיובית בלבד — התנאי הבלתי אפשרי המקורי · שחרור שקט בלי תאריך — המשתמש אינו יודע שהוא נעול או מתי זה נגמר |
| 2026-08-02 | `TradeDNA.js:185` **אינו** מאוחד ל-`getClosed` — הוא מונח ב**מפתח cache**, לא באוכלוסייה סטטיסטית | הסטטיסטיקה עצמה כבר עוברת `getClosed` (`:192`). מונח ה-cache הוא OR **בכוונה רחב יותר**: הוא חייב להשתנות בכל שינוי של `status`/`exit`/`exitPrice` כדי שה-cache יתפוגג. איחוד היה גורם לעריכה שמוסיפה `exit` לעסקה `OPEN` **לא** להחליף את המפתח — כלומר DNA מיושן שמוצג כטרי | איחוד "לעקביות" — היה מייצר cache שאינו מתפוגג, כשל שקט חמור יותר מזה שנסגר |
| 2026-08-02 | `EditTradeModal.jsx:37` (`showExitFields`) **אינו** מאוחד ל-`getClosed` — זו שאלת UI, לא שאלת אוכלוסייה | `getClosed` שואל "האם לספור את העסקה בסטטיסטיקה"; השורה הזו שואלת "האם להציג את שדות היציאה". עסקה `CLOSED` בלי `exit` היא בדיוק המקרה שבו **חייבים** להציג את השדה כדי שהמשתמש ימלא אותו — `getClosed` היה מסתיר את השדה היחיד שמתקן את הרשומה | איחוד ל-`getClosed` — היה הופך רשומה פגומה לבלתי-ניתנת לתיקון מהממשק |
| 2026-08-02 | מסלול העריכה קורא את **אותו** `validateTradeInputs` כמו ההזנה החדשה, מגודר ב-`stop != null && stop > 0` — בדיוק כמו `normalizeRow.js:112` | חמישה מימושי גיאומטריה כבר קיימים; מימוש שישי היה מוסיף עוד מקום להתיישן. הגידור על קיום הסטופ אינו ויתור אלא **החלטת הייבוא מ-01.08**: עסקה בלי סטופ חוקית, ולכן חסימת עריכה שלה היא באג — משתמש שאינו יכול לתקן הערה על העסקה שלו הוא כשל מוצרי (הכרעת ניב, 02.08). ⚠️ הוולידציה חלה על **שמירות חדשות בלבד**; רשומות קיימות (0/17 הפוכות בפרודקשן) אינן נחסמות ואינן משתנות | בדיקת גיאומטריה חדשה בתוך המודאל — מימוש שישי · חסימת עסקאות בלי סטופ — סותר את החלטת הייבוא · ולידציה רטרואקטיבית על רשומות קיימות — נועל למשתמש את היומן על נתון שנכתב לפני שהכלל היה קיים |
| 2026-08-02 | שער תשיעי `test:engine` סורק את `src/` **ואת `SwingEdge_App.jsx`** אחר `status === "CLOSED"` מול allowlist בן 6 קבצים מתועדים | האודיט מצא את המסלולים הפרטיים **בדיעבד**, רבעון אחרי שנכתבו. סריקה מבנית ב-`verify` הופכת את המימוש השביעי לכשל build במקום לממצא באודיט הבא. `SwingEdge_App.jsx` יושב בשורש ולא ב-`src/` — הרחבת ההליכה אליו אומתה שהיא **מבחינה** (הסרת הרשומה מה-allowlist מפילה את השער על `:3288`) | סריקת `src/` בלבד — הקובץ הגדול ביותר בריפו היה מחוץ לשער · הסתמכות על אודיט תקופתי — בדיוק המנגנון שהחזיק חמישה מסלולים פרטיים רבעון שלם |
| 2026-08-02 | קהל שליחה הוא **פרמטר** `audience` ב-`email-campaign.yml` (`waitlist_approved` ברירת מחדל · `registered_users`), לא workflow שני | ההנחה ש"רשימת ההמתנה ≈ המשתמשים" נמדדה ונפלה: **21/38 מהרשומים אינם ב-waitlist**, ו-**40/57 מהמאושרים ב-waitlist מעולם לא נרשמו**. אלו שתי אוכלוסיות, לא קירוב — הודעת עדכון מוצר שנשלחת לקהל הישן מפספסת מחצית מהמשתמשים ומגיעה לרוב מי שאין לו חשבון. הערך בורר בין **שני heredocs קבועים** ואינו משורשר ל-SQL; ערך לא מוכר יוצא `exit 1` ולא נופל לקהל ברירת מחדל. שאילתת "נותרו בתור" לדיסקורד עודכנה באותו `case` — דיווח שסופר אוכלוסייה אחרת מזו שנשלחה הוא מנה בלי מכנה | שכפול ה-workflow — שני קבצים שמתחזקים במקביל SMTP, dedup ודיווח; תיקון שיגיע לאחד ולא לשני הוא כשל שקט מובנה · `audience` כמחרוזת חופשית שמשורשרת ל-SQL — הזרקה · fallback שקט לקהל ברירת מחדל בערך לא מוכר — שליחה לרשימה הלא נכונה בלי שאיש ידע |
| 2026-08-02 | קהל `registered_users` מסנן TLD-ים שמורים (`local\|localhost\|test\|invalid\|example`), לא כתובת ספציפית | `auth.users` מכיל חשבון בדיקה ב-`swing-edge.local`; `.local` שמור ב-RFC 6762 ו-SMTP ייכשל עליו בוודאות. הכללתו הייתה מייצרת שורת `failed` מובטחת בכל ריצה — רעש שמלמד להתעלם מכשלים אמיתיים. הסינון על משפחת ה-TLD ולא על הכתובת: חשבון בדיקה **עתידי** ייחסם באותה דרך בלי שאיש יזכור לעדכן. **המכנה לשליחה הוא 37/38** | סינון הכתובת הספציפית — החשבון הבא היה עובר · אי-סינון והשלמה עם כשל אחד — כשל ידוע-מראש שמדווח כתקלה |
| 2026-08-02 | שער ה-grep הגורף שהאודיט ביקש ("אין פורמוט `$` inline מחוץ ל-`utils.js`") **לא יושם**; במקומו — `test:format` §8 עם guard ממוקד על חמשת האתרים שתוקנו | הכלל הגורף נכשל **היום** על עשרות אתרים לגיטימיים שאינם P&L חתום (`capital`, `curEquity`, שדות מחיר בטופס) — כסף לא-חתום אינו זקוק לסימן ואינו יכול לשקר על כיוון. כדי לעבור היינו צריכים allowlist ענקית או השתקה, ו**שער שצריך להשתיק אותו כדי שיעבור הוא לא שער** (הכרעת ניב, 02.08) — הוא מלמד להתעלם ממנו ואז מפספס את המקרה האמיתי. הכיוון הנכון אם זה יחזור: lint rule על מסלולי P&L חתומים בלבד, לא grep על התו `$`. הפער נרשם ב-`STATE.md` ⚠️ | יישום הכלל כלשונו + allowlist — רשימה שגדלה בכל קומיט עד שאיש לא קורא אותה · השתקת השער כדי שיעבור — שער ריק שנותן ביטחון כוזב · ויתור על כל guard — האתרים שתוקנו היו נסחפים חזרה בקומיט הבא |
| 2026-08-02 | **מיפוי מקורות CSP חייב מדידת ריצה, לא רק סטטי** — הפרות שנתפסו רק בזמן ריצה: `grep` סטטי והבאנדל לא הכילו את `s.tradingview.com` ואת `tradingview-widget.com` | `TradingViewWidgets.jsx:3` מצהיר על `s3.tradingview.com` — אבל זה ה-host של ה**סקריפט** בלבד. ה-iframe שהסקריפט מזריק בזמן ריצה מגיע מ-`s.tradingview.com` (בלי 3) ומקנן בתוכו frame שני מ-`www.tradingview-widget.com`; שני ה-hosts אינם מופיעים בשום מחרוזת בקוד או בבאנדל, ולכן **שום סריקה סטטית לא יכולה הייתה למצוא אותם**. ההליכה על המסכים תחת `Report-Only` החזירה 3 הפרות (2 hosts), כולן `frame-src`. אלמלא Report Mode, אכיפה הייתה משתקת את סרט הטיקרים **בשקט**. §2: מקור שלא נמדד בריצה אינו מקור שמופה | מיפוי מ-`grep` על `src/` בלבד — פספס גם את `api.emailjs.com` (מסלול הפידבק) · חילוץ מהבאנדל בלבד — תופס origins ליטרליים אך **לא** hosts שנוצרים בזמן ריצה בידי SDK צד-שלישי · אכיפה ישירה בלי Report Mode — הפיצ'ר נשבר בפרודקשן והמדידה נעשית על גב המשתמשים |
| 2026-08-02 | מטבע הוא תכונה של ה**חשבון** (`settings.accountCurrency`) **וגם** של ה**עסקה** (`trades.currency`), לא של אחד מהם | עסקה מיובאת נושאת את המטבע שנגזר מהקובץ שלה ולכן חייבת שדה משלה; אבל הון, סיכון-לעסקה, גודל פוזיציה ושווי חשבון נגזרים מ`capital` שאין לו עסקה — ולכן חייבים מטבע ברמת החשבון. עסקה שמוקלדת ביד מתומחרת בהגדרה במטבע החשבון, ולכן `newTrade` נושא `currency: accountCurrency` בבנייה. זהו **שלב א' של תמיכה ב-₪**, נבנה ככזה מהיום הראשון ולא כתיקון נקודתי לייבוא | מטבע ברמת העסקה בלבד — משאיר את ההון, הסיכון והפוזיציה בדולרים על חשבון שקלי · מטבע ברמת החשבון בלבד — קובץ ברוקר עם שני מטבעות היה נכפה למטבע אחד · **סירוב `posSize` על אי-התאמת מטבע** (הנוסח בתוכנית) — נפל כמיותר: לעסקה מוקלדת אין מצב שבו המטבע שלה שונה מזה של ההון |
| 2026-08-02 | שני שערים **בלתי תלויים** בייבוא: יחידת המחיר (אריתמטי) וזהות הנייר (`nonSecurityKind`) | הם תופסים שתי חטיבות זרות ואינם ניתנים לאיחוד: השער האריתמטי **מוכח שאינו תופס** שורת מס — כמות 666.94 בשער 100 נותנת יחס 0.01 בדיוק, זהה לציטוט אגורות חוקי לחלוטין. IBI רושם חיוב מס כ**קנייה** של נייר-דמה, ולכן ה-whitelist של סוגי הפעולה (מבחן תחילית) מקבל אותה בצדק — הפועל אמיתי, ה**נייר** הוא הפיקטיבי. איחוד לשער אחד עם שני תנאים היה מייצר שער שעובר בשקט על אחת משתי המחלקות | שער אחד עם שני תנאים — עיוור לאחת המחלקות · הרחבת ה-whitelist של סוגי הפעולה — הפועל לגיטימי, הבעיה אינה שם · ההנחה ש-9 שורות המס הן "ככל הנראה שריד מייבוא ישן" — הופרכה ע"י ניב ונפסלה במפורש |
| 2026-08-02 | סכום חוצה-מטבעות **מוצג ומתויג**, לעולם לא מומר ולא מוסתר | `statsCcy(st)` מחזיר את המטבע היחיד של האוכלוסייה כשיש בדיוק אחד, ונופל ל-`accountCurrency` כשיש יותר — ואז `MixedCurrencyBanner` מצהיר שהסכום אינו סכום אמיתי ומדפיס את הפילוח פר-מטבע (`pnlByCurrency`). המרה לפי שער הייתה ממציאה נתון (איזה שער? של איזה יום?), והסתרה הייתה מוחקת ליומן מעורב את כל שכבת הסטטיסטיקה | המרה לפי שער חליפין — נתון מומצא, ותלוי-תאריך שאיש לא בחר · הסתרת הסכום ביומן מעורב — מוחק את המסך למי שסוחר בשני מטבעות · סכימה שקטה — בדיוק המספר שהתחיל את T9 |
| 2026-08-02 | 41 העסקאות הפגומות של `a0556783290` **אינן מתוקנות ב-`UPDATE`** — התרופה יזומה-משתמש (מחיקה וייבוא מחדש) | חלוקה ב-100 בדיעבד מניחה שכל 41 באמת נקובות באגורות — הנחה שאי אפשר לאמת בלי קובץ המקור, שיושב בסל האשפה של הבעלים. `UPDATE` שקט על נתוני מסחר של משתמש חי הוא בדיוק §2 במקום המסוכן ביותר: הוא נראה כמו תיקון וייתכן שהוא נזק. הייבוא המתוקן גוזר את היחידה מהאריתמטיקה של השורה עצמה — הקובץ יודע, אנחנו לא | `UPDATE ... / 100` — מניח ולא יודע, ובלתי הפיך · השארת הנתונים כפי שהם בלי הודעה — משאיר למשתמש הפסד פיקטיבי של $280,000 על המסך |
| 2026-08-02 | בדיקת הרשאות מול PostgREST נבדקת **בלי** `Prefer: return=representation`, וכל "אפס" מוכח בבקרת-נגד | אותו `INSERT` בדיוק מחזיר 201 בלי ה-header ו-401 עם ה-header: PostgREST מוסיף קריאה-בחזרה של השורה, ול-`anon` אין policy של SELECT — כלומר ה-header הפך **הצלחה** לכישלון מדומה. הבדיקה דיווחה "חור סגור" על חור פתוח לרווחה, וזהו כשל המדידה המסוכן ביותר: הוא מייצר ביטחון כוזב בדיוק במקום שבו נדרשת פעולה. אותו כלל חל על מדידת CSP — "אפס הפרות" התקבל כתקף רק אחרי ש-`example.org` ייצר הפרה באותו listener | לסמוך על ה-probe עם ה-header — היה סוגר את 🔴 ה-`WITH CHECK(true)` כ"לא רלוונטי" · לקרוא "אפס הפרות" בלי בקרת-נגד — בדיוק ה-false-clean שכבר קרה פעמיים בשלב ב' |
| 2026-08-02 | **דלתא למדידה:** העסקאות של `a0556783290` הן **50, לא 41**, והמחיקה המומלצת היא **31/50** — 16 ניירות ת"א באגורות + 9 שורות מס + 6 המרות מט"ח; **19 עסקאות אמריקאיות נשארות** | השורה הקודמת (אותו תאריך) נכתבה לפי המספר שהופיע בתיאור המשימה. מדידה חיה מול פרודקשן החזירה 50, והפילוח מראה שהתרופה אינה "מחק הכול": שני שלישים מהמחיקה הם שורות שאינן עסקאות מלכתחילה, ושליש מהיומן תקין ואסור לגעת בו. "מחק ותייבא הכול" היה מוחק 19 עסקאות תקינות בלי סיבה | לשלוח את המייל עם המספר 41 — מספר שאינו קיים באף מקום, ומזמין את המשתמש לספור ולגלות שאיננו יודעים מה יש לו · "מחק את כל היומן וייבא מחדש" — פשוט יותר לניסוח, ומוחק 19 שורות תקינות |
| 2026-08-03 | **T10 · מחירי מכשיר (`entry`/`stop`/`target`/`exit`) נשארים במטבע המכשיר עם הסימן הנכון. צברים ברמת החשבון (הון, equity, P&L כולל, גודל פוזיציה, סיכון) מומרים למטבע התצוגה.** נאכף ב-`fmtPrice` (`src/utils.js`) שהוא בלתי-חתום במכוון, מול `fmtBalance`/`fmt$` לכסף-שלך | מחיר הוא **הצהרה על השוק**, לא כסף בחשבון. מניה שנסחרת ב-₪73.61 ומוצגת כ-$24.14 היא מספר שאי-אפשר להזין בו הוראה — ההמרה הופכת עובדת שוק לערך שאין לו קיום. צבר הוא כסף שלך, ולכן הוא חייב להיות נקוב במטבע אחד כדי להיות סכום בכלל. **ניב: "המשתמש חייב לראות את ההבחנה"** — ולכן היא כתובה בהגדרות (`accountCurrencyDesc`) ולא רק בקוד | להמיר הכול — מייצר מחירים בלתי-סחירים · לא להמיר כלום — משאיר את `generateEquityCurve` מסכם בין מטבעות, כלומר מספר שגוי · להמיר מחירים "רק בתצוגה מקוצרת" — שתי אמיתות על אותו שדה |
| 2026-08-03 | **T10 · פיצול `capitalCurrency` (המטבע שבו ההון נקוב) מ-`accountCurrency` (המטבע שבו הוא מוצג).** שניהם ב-jsonb של `user_settings`, ללא מיגרציה; ההמרה קורית **בזמן תצוגה בלבד** ואף פעם לא נכתבת חזרה | **שדה אחד עם שתי משמעויות הוא באג ממתין.** ההון נקוב במטבע אחד ומוצג באחר, והפיצול הופך את דיוק ההלוך-חזור ל**מבני** במקום לתלוי בעיגול: מעבר ₪→$→₪ מחזיר את אותם 2,500 בדיוק כי המספר השמור מעולם לא זז. אומת בדפדפן 03.08 — $2,500 → ₪7,644 → $2,500 | לשכתב את ההון בהמרה (`UPDATE`) — הפסד דיוק מצטבר בכל החלפה, ונתון שנכתב מחדש בלי שהמשתמש ביקש · להשאיר שדה אחד — הבאג של T9 בדיוק: הסימן התחלף והערך לא |
| 2026-08-03 | **T10 · מקור השער הוא `api.frankfurter.dev/v1` (ECB), לא `api.frankfurter.app`** — שונה בשלושת הקוראים: `api/fx.js`, `api/health.js`, ו-proxy הפיתוח ב-`vite.config.js`. `frankfurter` נוסף ל-`api/health.js` כבדיקה **לא-פטאלית** | `api.frankfurter.app` עדיין עונה, אבל ב-**301** ל-`.dev/v1` — **שירות שעובד רק משום ש-redirect נעקב הוא תלות שאיש לא רשם**. נמדד חי 03.08. ההוספה ל-health היא לא-פטאלית במכוון: כשל שער אינו שובר את התצוגה אלא מוריד אותה למטבע המקורי עם סמן, ולכן הוא אינו אירוע זמינות | להישאר על ה-`.app` — עובד היום, נשבר בשקט ביום שה-redirect יורד · health פטאלי — היה מכריז על האפליקציה כמושבתת בגלל תלות שהיא מדרג-מטה מוצהר |
| 2026-08-04 | **A5 · שיעור על אוכלוסייה ריקה הוא `null`, ומעבר סקאלה 0..1→0..100 מותר אך ורק דרך `toPct`/`toPctRound`.** `outcomeRates` מחזיר `winRate`/`lossRate`/`beRate` כ-`null` כש-`n === 0`; `n` ממשיך לנסוע בהחזרה, ולכן צרכן שרוצה להבחין בין "0 מתוך 0" ל-"0 מתוך 40" מקבל את הכול. `EMPTY_STATS` (יומן ריק לחלוטין) **נשאר `0` — מחוץ להיקף במכוון** | מנה שמכנה שלה אפס אינה אפס, היא אינה קיימת (§2), והמשתמש קרא "אחוז הצלחה 0%" בשבוע שבו לא סחר — "הכול נכשל" במקום "לא עשיתי דבר". `avgR` מחזיר `null` במצב הזהה מאז `9581b78`; שלושת השיעורים אומרים עכשיו את אותו הדבר באותה הדרך. **החלק השני של ההחלטה חשוב לא פחות מהראשון:** `null * 100 === 0` ו-`Math.round(null) === 0` ו-`null <= 45` → `true` — כלומר `rate * 100` גולמי **מחזיר את הבאג לחיים שורה אחת אחרי שתוקן**, בשקט ובלי חריגה. הפונקציה היא הגבול; בלעדיה החוזה הוא כוונה טובה. `EMPTY_STATS` הוצא כי הוא נוגע ב-`isEmpty: true` שכל ה-onboarding מגודר עליו — מחלקת סיכון אחרת, גל אחר | `{ rate: null, n: 0 }` כאובייקט חדש — שובר כל צרכן קיים כדי לומר מה ש-`n` כבר אומר · `NaN` — נבלע ב-`JSON.stringify` כ-`null` ומשקר בהשוואות · `0` עם דגל `hasData` נפרד — שני שדות שיכולים להיסתר, וכל צרכן שישכח את הדגל מקבל את הבאג המקורי · לתקן את התצוגה בלבד (`formatPct`) — משאיר את הבדיה במנוע, שם היא נכנסת לדלתאות ולהשוואות · `* 100` גולמי עם code review — תלוי בזיכרון אנושי (§2) |
| 2026-08-05 | **M · צבר חוצה-משתמשים אינו מוצג כמספר כל עוד אין לו ממד מטבע.** `Avg P&L` באדמין מרנדר `—` עם `Mixed USD+ILS — no unit`, ובמקומו נכנס כרטיס `% with stop` — מדד חסר-מטבע במהותו, שה-RPC כבר החזיר ואיש לא רינדר | `admin_trades_agg()` מחשב `avg(pnl)` בלי `group by currency` (`admin_rpcs.sql:241-251`), ו-`admin_trades_list` אינו מחזיר `currency` — כלומר **הלקוח אינו יכול לפלח את המספר בשום דרך**. ה-`$` שהיה שם הצהיר יחידה שלמספר אין. פילוח פר-מטבע דורש `create or replace function`, כלומר מיגרציה — שנאסרה בגל הזה. אין דרך שלישית: או שהמספר מפולח או שהוא חסר משמעות, ומספר חסר משמעות לא מוצג. **תנאי ניב:** כרטיס שמראה רק `—` מלמד את האדמין להפסיק להסתכל, ולכן החלל מתמלא במדד חי במקום להישאר ריק | סמן "ניטרלי" על הצבר — מספר בלי מטבע הוא מספר בלי משמעות, נפסל מפורשות · המרה לפי שער — ממציא נתון, ובאיזה יום · הצגת המספר כמו שהוא בלי סמן — אותו שקר בלי הסימן · השארת החלל ריק אחרי הורדת המספר — כרטיס מת שמלמד להתעלם מהלוח |
| 2026-08-05 | **M · `pct_with_stop`/`pct_with_setup` מוגנים ב**לקוח** (`!agg?.total ? "—"`), וה-RPC נשאר שגוי — במודע** | `admin_rpcs.sql:267-268` מחזיר `else 0` על מכנה ריק, ולכן טבלה ריקה מרנדרת **"0% עם סטופ"** — משפט שקורא כ"אף אחד לא שם סטופים" בעוד האמת היא "אין עסקאות בכלל". זו בדיוק מחלקת A5 (04.08), הפעם ב-SQL. תיקון במקור דורש מיגרציה, שנאסרה בגל הזה, אבל **באג ידוע שנשאר בלי הגנה עד שמותר לתקן אותו הוא באג פעיל** — ולכן ההגנה נוחתת עכשיו והמקור נרשם ב-⏭️. ה-`sub` נושא את ה**מכנה** ולא את המונה: ה-RPC מחזיר אחוז מעוגל בלבד, ושחזור המונה ממנו הוא דיוק מזויף (§2) | דחיית ההגנה עד שמותר לגעת ב-RPC — משאיר "0% עם סטופ" על המסך · תיקון ה-RPC בגל הזה — מיגרציה אסורה · שחזור המונה מהאחוז ל-`sub` — דיוק מזויף · `sub` בלי מכנה כלל — מנה בלי מכנה |
| 2026-08-05 | **M · טוקן עברי קצר ב-`FALLBACK_CANDIDATES` הוא מלכודת: מילים כאלה נכנסות ל-`EXACT_ONLY` ומותרות בהתאמה מדויקת בלבד** (`src/import/synonyms.js:60`, `e9de006`) | רצפות האורך (`floor` 3 לטוקן בודד) אינן מספיקות בעברית: `מטבע` עובר אותן, אבל הוא **תת-מחרוזת** של כותרת שמשמעותה אחרת לגמרי — IBI כותב `שווי במטבע` לעמודת **שווי**. עברית בונה צירופים בתחילית **דבוקה, בלי מפריד**, ולכן התאמת תת-מחרוזת אינה יכולה להבחין בין השתיים: `מטבע` חטף את עמודת השווי, המייבא סימן אותה כעמודת המטבע, ו**כל שורות ה-IBI נפסלו**. הצורות הרב-מיליות (`סוג מטבע`, `מטבע עסקה`) אינן מושפעות — הן ממשיכות להתאים במדויק | להסתמך על אורך הטוקן — **נפסל מפורשות**: `מטבע` בן 4 תווים עובר כל רצפה סבירה, והבעיה אינה אורך אלא **דביקות התחילית** · להוריד את `מטבע` מהמילון כליל — היה שובר קבצים שבהם זו באמת כותרת העמודה · לטפל בזה ב-`normKey` — הופך כלל ספציפי-לשפה לכלל גלובלי על כל כותרת |
| 2026-08-05 | **בורר הקבצים של מערכת ההפעלה גובר על תפריט בחירה שנבנה בידיים, ו-`capture` אסור על קלט fallback שנורה תוכניתית** (`SwingEdge_App.jsx:5784`, נעול ב-`test:ocr`) | `capture="environment"` על הקלט הזה כפה **מצלמה** והסתיר את הגלריה, כלומר סקרין שוט — הדבר היחיד שהמשתמש בא להעלות — היה **בלתי-בחיר**. הקלט אינו נלחץ בידי המשתמש: הוא נורה מ-`openChartFallback`, שמגיעים אליו רק מענף `!getDisplayMedia` (`:3172`), כלומר **רק ממובייל**. כך המסלול היחיד שנחשף רק למובייל היה היחיד שכפה מצלמה. הסרת המאפיין מחזירה את בורר המערכת, שהוא **כבר** התפריט "גלריה או מצלמה" — מתורגם, מקומי ומוכר. שלושת הקלטים האחרים (`:4667`, `:6481`, `:6979`) הם `<label>` גלוי של "העלה תמונה" ומעולם לא נשאו `capture`. **ההערה בקוד היא חלק מההחלטה:** האסרציה חוסמת את החזרת המאפיין, אבל העורך הבא צריך לראות את הסיבה במקום שבו הוא ישבור אותה | שני כפתורים מפורשים ("בחר תמונה" / "צלם") — **משכפל בידיים בחירה שמערכת ההפעלה כבר נותנת**, מוסיף הקשה קבועה לכל משתמש מובייל בנתיב הראשי (§3), דורש ענף הסתרה לדסקטופ שבו "צלם" חסר משמעות, ונוגע בשרשרת מחוות הקליק שההערה ב-`:3173` מזהירה עליה במפורש · השארת `capture` עם קלט שני לגלריה — מותיר את ברירת המחדל שבורה · `capture="user"` או `capture` ריק — אותה מחלקה, מצלמה במקום בורר |
| 2026-08-06 | **U · `/api/notify` נשען על `public.is_admin()` הקיים דרך ה-JWT של הקורא, ו**הליגר** `email_campaign_log` הוא התקרה — לא ה-rate limit.** `campaign = "reply:<feedback_id>"` ייחודי לכל שורת פידבק, ולכן פידבק נענה **פעם אחת בדיוק, לתמיד**. קריאת ליגר שנכשלת מחזירה `503` ו**אינה** שולחת | הדרישה הייתה אפס מיגרציות, ושלושת הרכיבים כבר קיימים ומורשים: `is_admin()` כבר `grant execute … to authenticated` (`20260719120000:56`), `admin_log_campaign_send` כבר בודק `is_admin()` **בעצמו** ואידמפוטנטי מול `email_campaign_log_uniq`, ו-`campaign` הוא `text` בלי CHECK כך ש-43 תווים נכנסים. כלומר ההרשאה נבדקת ב**בסיס הנתונים** ולקוח אינו יכול להרחיב אותה, בלי עמודה חדשה ובלי policy חדשה. **ה-rate limit אינו תקרה:** `_lib/rateLimit.js` הוא in-memory פר-instance ומתאפס ב-cold start — הוא אומר זאת על עצמו בשורה 1. להציג אותו כהגנה מפני שליחה כפולה היה ביטחון כוזב; הליגר הוא ההגנה, וה-fail-closed על קריאה כושלת הוא מה שהופך אותו לכזה: סירוב לשלוח הפיך, מייל כפול לאדם אמיתי אינו | service-role key + בדיקת אדמין ב-Node — מעביר את ההרשאה מה-DB לקוד שהלקוח משפיע עליו, ומכניס סוד חדש · עמודה `replied_at` ב-`feedback` — מיגרציה, ו**ליגר שני** שיסתור את הראשון ביום שאחד מהם ייכשל · fail-**open** על קריאת ליגר שנכשלה — הופך תקלת רשת רגעית לשליחה כפולה, כלומר לנזק בלתי הפיך · הסתמכות על ה-rate limit כתקרה — מתאפס ב-cold start, כלומר תקרה שאינה קיימת |
| 2026-08-06 | **U · הנמען נפתר **בשרת** מ-`feedback_id`, והתבנית היא **מפתח ל-allowlist** — לא HTML ולא נתיב מהבקשה. `dry_run` מחזיר כתובת **ממוסכת** (`om***@domain`) לאישור בעין** | `api/feedback.js` חסר שער אימות **במכוון** (משתמש שתקוע לפני login חייב לדווח), ולכן `feedback.user_email` הוא **קלט שנטען על ידי הקורא ואינו ניתן לאימות**. תוקף שמזין שורת פידבק עם כתובת של אדם שלישי גורם ל-SwingEdge לשלוח מייל ממותג למי שמעולם לא נרשם. המיסוך פותר את המתח בין "⛔ אפס כתובות בפלט" (`SKILL.md`) לבין הצורך בוודאות לפני פעולה בלתי הפיכה: 2 תווים + דומיין מספיקים לאדמין לזהות את האדם הנכון ואינם מספיקים לדליפה. קבלת HTML מהבקשה הייתה הופכת את הנתיב ל-relay פתוח עוטה מיתוג; קבלת **נתיב** הייתה path traversal אל כל קובץ בריפו | קבלת נמען מגוף הבקשה — relay פתוח · אימות הכתובת מול `auth.users` — דורש RPC חדש, כלומר מיגרציה שנאסרה · החזרת הכתובת המלאה ב-`dry_run` — מפר `SKILL.md` בדיוק במקום שבו הוא הכי נחוץ · `dry_run` בלי כתובת כלל — האדמין מאשר שליחה בלתי הפיכה בעיוורון · fallback לתבנית גנרית כמו ב-`send-invites` — מייל בעיצוב שגוי למשתמש שכבר אכזבנו, גרוע ממייל שלא נשלח |
| 2026-08-06 | **U · קמפיין המשך מסונן בהרחבת ה-dedup לרשימת קמפיינים (`prior_campaigns`), ולעולם לא בזריעת שורות בליגר** | `email_campaign_log` הוא **הרשומה היחידה** של מה באמת יצא ולמי. שורת `sent` על מייל שלא נשלח היא זיוף לוג השליחה — היא מכווצת קהל היום ומשקרת בכל אודיט מחר. `l.campaign = ANY(string_to_array(:'campaigns', ','))` משיג את אותו קהל (21) ב**אפס** כתיבות, וחוזר על עצמו בכל קמפיין המשך עתידי. הפרדיקט מוחל על שלושת הקהלים ועל שלב הדיווח **באותו נוסח** — קלט ריק מייצר `{campaign}` בודד, כלומר התנהגות זהה-לבית לקודמת | זריעת 5 שורות `sent` מראש — זיוף מקור-האמת · dedup נפרד ל-`stuck_users` בלבד — שלושה מסלולי dedup = סחיפה · הרכבת ה-SQL כמחרוזת עם שמות הקמפיינים — הזרקה; הרשימה עוברת כליטרל **בודד** מאומת-תווים |
| 2026-08-06 | **U · משתמש שעסקאותיו מוקדמות מהרשמתו **מוצא** ממדידת הזמן, ומספרו מדווח בשורה נפרדת** | ל-`public.trades` **אין** `created_at` בצד השרת; `"createdAt"` מגיע מהלקוח ובעסקה מיובאת נושא את תאריך העסקה. נמדד 06.08: **40/56 שורות מוקדמות מהרשמת הבעלים**, על פני 3 משתמשים. חוסר-מדידוּת שמדווח הוא פער ידוע; מדידה מפוברקת היא נזק שמתגלגל לכל דוח | `greatest("createdAt", signed_up)` — היה מייצר "0 ימים" יש-מאין והופך נתון חסר לנתון שקרי · השמטה שקטה של המוצאים — מכנה שאיש אינו יודע שהצטמק |
