# DONE — גלים שנסגרו

> **חוזה חמשת הקבצים — פריט חי בקובץ אחד בלבד.**
>
> | קובץ | מה חי בו |
> |------|-----------|
> | `docs/STATE.md` | 🔴 עכשיו · ⏭️ 3 הבאים · ⏸️ חסום · ⚠️ סיכונים. **תקרת 100 שורות בתוקף.** |
> | `docs/BACKLOG.md` | כל פריט פתוח שאינו פעיל. אין תקרה. |
> | `docs/DONE.md` (כאן) | **הושלם** — מזהה · תאריך · hash · **איך אומת** · **`T`**. |
> | `docs/CHECKS.md` | בדיקות ידניות חוזרות. |
> | `docs/audits/STATE-ARCHIVE-*.md` | **סגור בלבד** — ⛔ פריט פתוח לעולם לא נוחת שם. |
>
> מעבר בין קבצים = **מחיקה משם והוספה כאן**, ⛔ לא העתקה.
> ⛔ **מזהה לא נמחק ולא ממוחזר לעולם.**
> ⚠️ **טבלה, ⛔ לא פרוזה.** הפירוט המלא נשאר ב-`docs/audits/STATE-ARCHIVE-2026-08.md` ומופנה לפי מזהה.
> ⚠️ **כל שורה חייבת hash.** גל בלי hash אינו גל שנסגר — הוא טענה.
> ⚠️ **כל שורה חייבת `T`** — רמת הסיווג (`CLAUDE.md` §15), ⛔ לא "כמה זה היה
> מסובך" אלא התשובה לשלוש השאלות. **`T3` מחייב אימות עין בעמודת "איך אומת"**;
> ⛔ בלעדיו הגל אינו נסגר.
> ⚠️ **29 השורות ההיסטוריות נושאות `(רטרו)`** — הסיווג נגזר **מטקסט השורה עצמה**
> ב-13.08 ו⛔ אינו ידיעה בת-הזמן. ⇒ הן **אינן** כפופות לשער אימות-העין: רטרו-התאמה
> של `D-009`/`D-011` כך שייראו מאומתים הייתה **בדיוק** ההפרה ‹R-3›. הדיוק שלהן
> נבדק ב-`C-024`.
> נאכף מכנית ב-`npm run test:registry` (חוליה 23).

---

## גלים שנסגרו

| מזהה | מה | תאריך | hash | איך אומת | T |
|------|-----|--------|------|-----------|-----|
| D-001 | מנוע ההון — ספי סיווג פר-מטבע · סמל מטבע בסיכום | 09.08 | `13fd7da` | `test:fx` 21/21 · **10 אסרציות נצפו נכשלות על הקוד הישן** (פלט הודבק בדיווח הגל) | T3 (רטרו) |
| D-002 | בסיס עקומת ההון בשער יום-הבסיס | 09.08 | `e2ea15d` | `test:fx` 21/21 ירוק אחרי · הקוד הישן חילק בשער היום ולא בשער יום-הבסיס | T3 (רטרו) |
| D-003 | גידור מטבע ב-`src/intelligence/` + הסרת ברירת מחדל שקטה | 09.08 | `da5b78c` | `test:coach` 111/111 · השרשרת המלאה הודבקה | T3 (רטרו) |
| D-004 | `test:import` §4 — פינים לאינווריאנט במקום לטקסט השומר הזמני | 09.08 | `92d105d` | `test:import` ירוק · RPC מחזיר `null` על מכנה ריק ומונים גולמיים | T1 (רטרו) |
| D-005 | גל הדייג'סט והסורק — פנקס תשובות + השתקה מתועדת | 08.08 | `ac9b38f` | `test:digest` 9/9 · `test:notify` 8/8 | T2 (רטרו) |
| D-006 | גל אנדרואיד — יכולת נצרכת רק אחרי בדיקה | 08.08 | `dbd5fb8` | `test:capability` 11/11 · **7 נצפו נכשלות על הקוד הישן** | T2 (רטרו) |
| D-007 | גל GA4 — מסכים פנימיים נמדדים, כל אירוע נולד מסונן | 08.08 | `c4810f1` | `test:analytics` 14/14 · **11 נצפו נכשלות על הקוד הישן** | T3 (רטרו) |
| D-008 | גל התיוג — הטקסט מכריז על היחס, הצבע נמדד ולא מוערך | 08.08 | `df1077b` | `test:copy` 16/16 · `npm run glossary` באותו קומיט | T2 (רטרו) |
| D-009 | גל אופק העסקה — נגזר מהצהרת המשתמש בלבד | 10.08 | `d7e4a4d` | `test:horizon` 140/140 · **7 נצפו נכשלות** · ⚠️ **לא אומת בעין** — ראה `C-001` | T3 (רטרו) |
| D-010 | גרש-הפוך בהערת SQL הפיל את `user-analytics` — תיקון + שער חוליה 21 | 10.08 | `dea4cc1` | `test:syntax` 4/4 · **2 נצפו נכשלות** · ריצת פרודקשן `#19` ירוקה · `INCIDENTS#15` | T3 (רטרו) |
| D-011 | גל א׳ מטבע — מטבע הנייר נגזר בזמן קריאה, שלושת השערים יכולים להיכשל | 11.08 | `e881248` | `test:instrument` 105 אסרציות · **18 נצפו נכשלות** · ⚠️ **לא אומת בעין ⇒ הבאג נשאר חי בשני מקומות נוספים** — הסיבה ש-`C-001` קיים | T3 (רטרו) |
| D-012 | המרה לפני החלוקה — גודל הפוזיציה חדל להיות גדול פי-שער | 12.08 | `636b490` | `test:instrument` 165/165 · `verify` 22 חוליות + build | T3 (רטרו) |
| D-013 | מחלקת הכתיבה — `tradeWrite` INSERT מאומת ב-`handleSubmit`, שורש המרוץ | 06.08 | `32a9efa`→`af858e2` | `test:write` 25/25 · `INCIDENTS#14` נסגר ב-`014bb15` | T3 (רטרו) |
| D-014 | מחיקה מאומתת + rollback — DELETE שאינו מוחק אינו מדווח הצלחה | 05.08 | `fadd61d` | `test:write` — 7 אסרציות על מסלול המחיקה | T3 (רטרו) |
| D-015 | תבניות המענה — `body_text` + `escapeHtml` + טוקן `reply:<id>:<n>:<h8>` | 04.08 | `d3eb7dd`,`f8616c6`,`e9d89e6` | `test:notify` — **11 מתוך 23 נצפו נכשלות על הקוד הישן** · יבש מאמת מכתב מלא | T2 (רטרו) |
| D-016 | מטבע אחד במסך הנחיתה — Pro 25₪/$8 עם toggle ידני | 03.08 | `22d6623` | `test:landing` — **1 מתוך 7 נצפתה נכשלת** | T3 (רטרו) |
| D-017 | מפתח rate-limit נפרד ל-`dry_run` — שער שחוסם את עצמו אינו שער | 03.08 | `67229a4` | `test:notify` — **8 מתוך 11 נצפו נכשלות** | T2 (רטרו) |
| D-018 | ערוץ entry שלישי ב-OCR — entry מקווי stop/target כשאין אחוזים | 04.08 | `8977fc5` | `test:ocr` סצנות I+J · **נצפו נכשלות על הקוד הישן ב-`61a7cd9` לפני התיקון** | T3 (רטרו) |
| D-019 | ייבוא FIFO — התאמת buy↔sell בקבצי ברוקר | 02.08 | `f375478` | `test:import` T5 | T3 (רטרו) |
| D-020 | ייבוא ו-undo — אימות מונה ודיווח חלקי מדויק | 06.08 | `b2bfa8c` | `test:import` · מונה שורות מאומת מול `row_count` | T3 (רטרו) |
| D-021 | עדכון · סגירה · מחיקה מרובה — אימות מונה שורות | 06.08 | `c553df3` | `test:write` — כל מסלול כתיבה מחזיר מונה ונבדק | T3 (רטרו) |
| D-022 | נוהל אבחון-לפני-ביצוע (`CLAUDE.md` §8.1) | 10.08 | `5b544e3` | תיעוד — אומת בשימוש בגלים `d7e4a4d`, `e881248`, `636b490` | T1 (רטרו) |
| D-023 | גל המרשם — פריט חי בקובץ אחד + `test:registry` אוכף | 12.08 | `075533e` | `test:registry` 13/13 · ⚠️ **נסגר בקומיט הבא, ⛔ לא בשלו** — ראה למטה. סוגר את `B-117` | T2 (רטרו) |
| D-024 | גל ג׳ — סגירת המטבע: 9/10 אתרי סכום-בחשבון + עיגול בתצוגה | 12.08 | `3b1a1cf` | `test:instrument` 216/216 · **11 נצפו נכשלות לפני החיווט** · ✅ **אומת בעין 13.08** (`C-019`): שורת היומן · הכרטיס במובייל · מודל היום · הטוסט — כולם מומרים בשער יום-המימוש; `POS. VALUE` = ₪774.88 בשתי ספרות; שער `2.9895` עקבי בכל האתרים. ⚠️ **ליקוי אחד נמצא בבדיקה ⇒ `B-119`** | T3 (רטרו) |
| D-025 | שורת `BE` — תווית המטבע `ILS`→`USD` על שורה אחת | 13.08 | `3b1a1cf` | ניב הריץ ידנית את `20260812150000_be_row_currency_label.sql` (§12). SELECT אחרי: `be_usd=1 · be_ils=0 · ils_total=0` · 61 שורות · `entry`/`stop`/`exit`/`shares` **זהים בדיוק** לפלט שלפני · הצ'יפ "מטבע לא מאומת" נעלם וסמל `$` חזר (צילום 11:15). `C-020` · סוגר את `B-122` | T3 (רטרו) |
| D-026 | הרחבת `trades.currency` — `ILA`/`EUR`/`GBP`, והסרת `default 'USD'` + `NOT NULL` | 12.08 | `636b490` | ניב הריץ ידנית את `20260812120000_trades_currency_widen.sql` (§12). אימות: `nullable=YES` · `default=null` · ה-CHECK מונה חמישה ערכים · 61 שורות, **אפס נגעו**. `C-004` · סוגר את `B-089` ואת `B-047`. ⚠️ `ILA` בר-**רישום** ⛔ אינו `ILA` בר-**קריאה** — `B-121` נשאר פתוח | T3 (רטרו) |
| D-027 | קמפיין `registered_users` — רץ מול הסוד האמיתי, ⛔ לא dry-run | 02.08 | `5ffc660` | `STATE-ARCHIVE:975`: **`sent=37 · failed=0`**, run `30749079414`, דיסקורד HTTP 204; הקהל 37/38 מ-`auth.users`. ⚠️ **נסגר כסתירה ⛔ ולא כגל** — הראיה שכבה בארכיון מ-02.08 בעוד `:1633` הכריז את ההפך, 11 יום. סוגר את `B-116` | T3 (רטרו) |
| D-028 | שכבת השורשים + מנגנון המצבה — מצב-סיום למזהה `B-` | 13.08 | `38896e4` | `test:registry` **18/18** בקומיט עצמו (13 לפני הגל ⇒ §8 הוסיפה חמש). **`8.4` נצפתה נכשלת על 5 מצבות אמיתיות** ⛔ ולא על הפרה מסונתזת, ו-`8.3` אוכפת את מיפוי השורשים **דו-כיוונית** (6 שורשים · 50 שיוכים). ⚠️ **נסגר בקומיט הבא, ⛔ לא בשלו** — כלל `4.1`. סוגר את `B-126` | T2 (רטרו) |
| D-029 | `NEXT` + `INBOX` + מפת הערוצים — הכרעה במקום רשימה, וקליטה במקום זיכרון | 13.08 | `c7ce628` | `test:registry` §9, **חמש אסרציות שנצפו נכשלות על הפרה זמנית לפני החיווט**. מדוד אחרי: `NEXT` = 32 שורות (תקרה 40) · **3 פריטים בדיוק** · 5/5 הפניות `B-` קיימות ב-`BACKLOG` · 3/3 נושאות מדד סגירה. `docs/AGENT-CHANNELS.md` מודד את השורש: **9/10 הערוצים יוצאים בלבד** ⇒ `B-130` · `B-131` | T2 (רטרו) |
| D-030 | חיסול המרשם השני + `docs/PROJECT-GUIDE.md` + פרוטוקול `T1`/`T2`/`T3` + הכרעת ה-spot | 13.08 | `2382a98` | `npm run verify` — **23 חוליות + build ירוקים**; `test:registry` **26/26** (23 לפני הגל ⇒ §10 ו-§11 הוסיפו שלוש). **שלוש האסרציות החדשות נצפו נכשלות לפני החיווט:** §10.1 — על `SwingEdge-Master-Tasks.md` ירה **סמן הכותרת בלבד** (שורותיו `\| T10 \|` בלי מקף), וקובץ זמני `TEMP-ROGUE.md` בכותרת תמימה `# Notes` ושורת `\| B-999 \|` הוכיח שסמן-השורה יורה **בנפרד** ⇒ שני סמנים עצמאיים, ⛔ לא אחד; §11.1 — על 29 שורות בלי עמודת `T`; §11.2 — בשלושה מצבים, כולל **הכשל-שקט**: `/אומת בעין/` תמים עובר גם על "**לא** אומת בעין", שזה בדיוק מה ש-`D-009`/`D-011` אומרים. **מדוד אחרי המחיקה:** **48 יחידות הכרעה** (17 שורות טבלה · 2 חסומים · 22 חובות · 5 כללי עבודה · מקטע ה-✅ · תמונת ה-📊) מוינו אחת-אחת מול הרפו — **38 מכוסות או מיושנות-בנימוק · 10 חסרות-בית** ⇒ `B-132`…`B-141`; 3 קבצי `.md` עקובים בשורש, אף אחד אינו מרשם. `CLAUDE.md` 267→296 שורות (תקרה 300). ⚠️ **`T2` ולא `T3`: אפס נגיעה בקוד רץ** ⇒ אין מסך לאמת בעין, והאימות הוא פלט הכלים המודבק. ⚠️ **נסגר בקומיט הבא** — כלל `4.1`. 🔴 **ובאותה צפייה-חוזרת נמצא חור ב-§11.2 עצמה:** השורה הזו, שסומנה `T3` זמנית, **עברה בירוק** משום שהיא **מתעדת את הרגקס** ⇒ הסמן החיובי נמצא בתוך ציטוט. הפעם ה**שלישית** שאסרציה נופלת על התיעוד של עצמה (`B-120`) ⇒ הציטוטים מנוכים לפני הבדיקה, והשער נצפה **אדום** אחרי התיקון (`D-030b`). סוגר את `B-131` | T2 |
| D-031 | שלוש שאלות הסיווג → **חמש**, וסמן אימות `אומת-מקור` ל-`T3` בלי מסך | 13.08 | `6ef1984` | `npm run verify` — **23 חוליות + build ירוקים**; `test:registry` **26/26** (⛔ אפס אסרציות חדשות — §11.2 **הורחבה**, לא נוספה). **ארבעה מצבים נצפו, שניים בכל כיוון:** (א) `T3` + `אומת-מקור` מחוץ לציטוט ⇒ **ירוק**; (ב) `T3` + "**לא** אומת-מקור" ⇒ **אדום** — ההכחשה נדחית גם בסמן החדש; (ג) `T3` + `אומת בעין` ⇒ **ירוק**, ⛔ אין רגרסיה בסמן הישן; (ד) `T3` בלי סמן ⇒ **אדום**. ⚠️ **והמצב הראשון נכשל בניסיון הראשון — והכשל היה ב**פיקסצ'ר** ⛔ לא בשער:** הזרקתי את הסמן **בתוך** ``` `` ``` ⇒ `strip` ניכה אותו כמתוכנן. ⛔ **לא ריככתי את `strip` כדי ש"יעבוד"** — זו בדיוק ההתנהגות שנקנתה ב-`D-030b`. **מדוד:** `CLAUDE.md` 296→**299** שורות (תקרה 300) ⇒ ניסוח החמש נדחס, ⛔ לא סעיפים אחרים. ⚠️ **`T2` ולא `T1`:** הסיווג נקבע בתחילת המשימה תחת **השלוש** (שאלה 2 = "כן"), ו⛔ **רמה אינה יורדת תוך משימה** — תחת החמש אותה משימה יוצאת `T1` (קובץ קוד אחד, `docs` אינם נספרים). ⚠️ **נסגר בקומיט הבא** — כלל `4.1`, פעם חמישית | T2 |
| D-032 | `B-119` — P&L חי מומר ב-**spot**, ואין שער ⇒ **סכום חלקי מוצהר** | 13.08 | `c4e811d` | ✅ **אומת בעין** (`C-023`, 4/4 המצבים ב**חיבור** ⛔ לא ב"נראה סביר"): `$/$` → `$10,124.75`=10,000+100+24.75 · `₪/₪` → `₪10,274.25`=10,000+100×2.0+24.75×**3.0**, **Δ=₪49.50** — בדיוק הפער שנמדד ב-13.08 · `$/₪` → `₪20,274.25` · `₪/$` → `$5,124.75` (ללא שינוי, F2). שלושת אתרי הכותרת (סרגל · כרטיס · פוטר) נושאים **אותו** מספר בכל מצב. **אין spot** ⇒ תג עם **מונה** (`אינו כולל P&L של 1 פוזיציות — אין שער`), כותרת `₪10,100.00` — **סכום חלקי** ⛔ לא `—` ערום, ושתי שורות ה-P&L `—` עם נימוק ב-`title`. **המצב החמישי (C1)**: הכותרת ⛔ **לא** גדלה. ה-PDF: `Excludes 2 open position(s) — 1 without a live price — 1 without an FX rate`. 🔴 **מלכודת F1 הופרכה בדפדפן בשערי פרודקשן** — ה-P&L החי חושב ב-spot `2.9810` (73.78) ⛔ ולא בשער יום-הכניסה `3.0009` (74.27). `test:instrument` **252/252** (165 לפני), **A1–A8 + 7 אסרציות חיווט נצפו אדומות לפני התיקון**; `npm run verify` 23 חוליות + build ירוקים. ⚠️ **שארית מוכרת ⛔ אינה רגרסיה:** ההון ה**סגור** נשאר לא-מומר תחת סמל החשבון כשאין שער ⇒ `B-142`; אסרציה `13.9b` ⛔ לא נגעה — היא קו קפוא עליו. סוגר את `B-119` | T3 |
| D-033 | `B-142` — ההון ה**סגור** נקוב ב-`dispCcy`, ומחובר לא-מומר **מוחרג ונספר** | 15.08 | `9525e6b` | ✅ **אומת בעין** (`C-023`, כל מספר ב**חיבור** ⛔ לא ב"נראה סביר"). **3 מצבי-סירוב + 1 שלא היה בתוכנית:** **S5** (אין טבלה) → `₪10,000.00` = בסיס גולמי ₪10,000 + **0** (AAPL 100$, `no_table`) + **0** (MSFT פתוח, אין מחיר), תג `1/1` פתוחות + `1/1` סגורות · **S7** → `₪20,200.00` = 10,000$×2.0 + 100$×2.0, AAPL בעל `currency:"ILS"` שגוי מוחרג `unverified_instrument`, תג `1/2`, תשואה 200/20,000 = **1.0%** · **S8α** → `₪20,200.00`, MSFT שנסגר 2026-04-15 מוחרג `no_rate_for_day` — **מעבר ל-7 ימי ההליכה-אחורה**, תג `1/2`. 🔴 **S8γ — באג שני שהתכנון ⛔ לא צפה, ונמצא **רק** על המסך:** אין spot + **יש** fixing ליום הבסיס ⇒ ההכרעה **מצליחה** ומחזירה ₪, בעוד `fxOk` שקר ⇒ `dispCcy` נופל ל-$ ⇒ **`$20,100.00`** = 20,000 **שקלים** + 100 **דולר** תחת "$". ⚠️ **וההערה שעמדה שם הצהירה שזה בלתי-אפשרי** ("אחרי השער למעלה זה **תמיד** `dispCcy`") — הוחלפה בתיאור מה שנמדד. ⛔ **⛔ אינו קצה:** זו טבלת ה-`catch` של `loadRateTable` — spot ⛔ לעולם אינו נשמר ל-localStorage (מכוון) ⇒ "יש `byDay`, אין `spot`" הוא ה**ברירה** אחרי נפילת רשת. תוקן ל-`equityBase = fxOk ? equityBaseD.value : capital`, **נצפה אדום** (`instrument 1/330`) ואז ירוק, ואומת שוב על המסך: **`$10,100.00`**. ⚠️ **שלוש הסביבות נבנו בכוונה** — הניסיון הראשון ב-S5 **ריפא את עצמו** ל-`$3,260.50` כי `vite.config.js` מנתב `/api/fx` ל-Frankfurter בפיתוח ⇒ ה-`₪10,000.00` הראשון היה **מצב-טעינה חולף**, ⛔ לא `no_table` אמיתי; הוחלף בפרוקסי ליציאה בלתי-נתיבה (500 מאומת ב-`curl`), ו-S8α בחסימה **סלקטיבית** של `start=` בלבד. `test:instrument` **330/330** (252 לפני); `npm run verify` **23 חוליות + build ירוקים**; `test:registry` **26/26**, 202 מזהים. ⚠️ **הבייסליין של `tradingstats` נלכד מחדש תחת שלוש דרישות ניב** — הוכחת-אבחנה (שיבוש→אדום→החזרה→ירוק), אסרציית **ערך** עם מכנה בטסט עצמו ⛔ לא בבייסליין, ובלוק `BASELINE VERSION` באותו קומיט. ⚠️ **הקו הקפוא `13.9b` הוסר במפורש** — ⛔ לא עודכן כדי לעבור. §10: `B-143` · `B-144` · `B-145`. סוגר את `B-142` | T3 |
| D-034 | 🔴 **לקח `R-3` — אסרציה שמדדה **נוכחות טקסט** במקום **צריכה**, ונענשה על התיעוד שהגל נדרש לכתוב** | 15.08 | `9525e6b` | ✅ **אומת-מקור** (`git show origin/main:scripts/instrument-currency-test.mjs` `:1259-1266` — ⛔ לא מהעץ המקומי). **מה קרה:** הניסוח הראשון היה `!/stats\.currentEquity/` על **הקובץ כולו**, והוא נכשל על **3 שורות הערה** שמסבירות למה השדה נעזב — כלומר על ההערה שדרישת P2 של ניב **חייבה** לכתוב. ⚠️ **הפעם הרביעית שאסרציה נופלת על התיעוד של עצמה** (`B-120`: `\|\| 1` · `accountAmount` · §11.2 של `test:registry` — וההיא הייתה בכיוון המסוכן, שער שמאשר בטעות). **התיקון חידד ⛔ ולא ריכך:** ההערות מנוכות לפני הבדיקה (`replace(/\/\*[\s\S]*?\*\//g,"")` + `replace(/^\s*\/\/.*$/gm,"")`) והנושא נשמר מילה במילה — אף מסלול רינדור ⛔ אינו קורא את השדה. **ונוספו שיניים:** אסרציה משלימה שרואה **צריכה אמיתית** (`stats.closedMetrics`) ⇒ אסרציה שלילית שאינה יכולה לעבור סתם מפני שהביטוי נעלם. ⚠️ **נרשם בשורה נפרדת בדרישת ניב:** *"אסרציה שמדדה נוכחות טקסט במקום צריכה היא R-3 קלאסי, וזה לקח שחוזר"* — ⛔ אינו פרט-לוואי של `B-142` אלא השורש שלו. ⚠️ **`T3` ⛔ ולא `T1`:** בוצע **בתוך** הגל ורמה ⛔ אינה יורדת תוך משימה (§15); ⛔ אין לו מסך ⇒ הסמן הוא `אומת-מקור` | T3 |
| D-035 | `B-145` — **`STATE ⏭️` מצטט את `docs/NEXT.md`, ⛔ ואינו מחזיק רשימה משלו**, ושער `9.6` אוכף **זהות דו-כיוונית** | 15.08 | `a95f0d8` | ✅ **אומת-מקור** (`git show origin/main:scripts/registry-test.mjs` + `origin/main:docs/STATE.md` — ⛔ לא מהעץ המקומי). **מה נמדד:** שתי רשימות "שלושת הבאים" חיו במקביל ב-**0/3 חפיפה** — `STATE ⏭️` נשא `B-007`·`B-009`·`B-006` בעוד `NEXT` נשא `B-001`+`B-002`+`B-018` · `B-129` · `B-143`. **ההכרעה:** מקור-אמת יחיד = `docs/NEXT.md`; `STATE ⏭️` נושא **מזהה בלבד**, וההנמקה/דרך היישום/מדד הסגירה חיים ב-`NEXT` ⛔ ולא בשני עותקים (`R-6`). 🔴 **למה `9.1`–`9.5` ⛔ לא יכלו לתפוס זאת:** חמשתן קוראות את `NEXT` **לבדו** ומאמתות שהוא תקין **בפני עצמו** (תקרה · מניין · הפניות קיימות · מדד סגירה) — **שער שמאמת נוכחות ואינו מאמת הסכמה הוא `R-3`**, והוא ירוק בדיוק ביום שבו שני הקבצים אומרים דברים הפוכים. **`9.6`** משווה את מזהי **כותרות** `NEXT` מול מזהי מקטע `⏭️` ב-`STATE`, **בשני הכיוונים**. ⚠️ **מול הכותרות בלבד ⛔ ולא מול גוף הפריט** — הגוף מצטט מזהי **הקשר** (`B-142`·`B-144`), ודרישה ש-`STATE` ישקף גם אותם הייתה הופכת אותו לעותק שני של הפרוזה, בדיוק ה-`R-6` שהשער נועד למנוע. ⚠️ **ודו-כיוונית בכוונה** — "כל מזהה של `NEXT` מופיע ב-`STATE`" לבדה עוברת גם כשב-`STATE` יושבים שלושה פריטים **נוספים**, כלומר בדיוק המצב שנמדד. **איך נצפתה אדומה — פעמיים, ⛔ ואף פעם על סתירה מסונתזת:** (1) `git stash push -- docs/STATE.md` החזיר את `STATE` ל-`a538a46` תוך שמירת השער ⇒ `❌ 9.6 · 6 מזהים`, `ב-NEXT ⛔ ואינו ב-STATE: B-001 · B-002 · B-018` + `ב-STATE ⛔ ואינו ב-NEXT: B-007 · B-009 · B-006 · B-145`; (2) **מבחן השיניים ל-`R-3` עצמו** — `B-006` נוסף ל-`STATE ⏭️` כך ש**כל 5 מזהי `NEXT` נוכחים ועוד אחד עודף**: שער-נוכחות היה עובר, ו-`9.6` ירתה `ב-STATE ⏭️ ⛔ ואינו ב-NEXT: B-006`. ואז ירוקה: `5 מזהים משני הצדדים`. ⚠️ **`B-007`/`B-009`/`B-006` ⛔ לא נמחקו** — נמדדו **3/3 פתוחים** ב-`BACKLOG` לפני הציטוט ⇒ המעבר ⛔ אינו מאבד פריט. `test:registry` **27/27** (26 לפני), **204 מזהים**; `npm run verify` **23 חוליות + build ירוקים**. **אימות פרודקשן באותו גל:** הנכס החי `/assets/index-BvcR2Rtx.js` (1,638,589 בתים, HTTP 200) נושא `fxUnconverted` **3×** בשלושת הצרכנים המיועדים — הסימון ב-`makeConvertingCalc` · הדילוג בעקומת ההון · הקיפול של ה-P&L הסגור עם המונה. ⚠️ **`T2` ⛔ ולא `T3`:** רוחב-קוד קובץ אחד (`scripts/`), ⛔ אפס נגיעה במסלול משתמש · ⛔ אין מסך ⇒ הסמן `אומת-מקור`. סוגר את `B-145` | T2 |
| D-036 | **גל השומרים** — `B-001` + `B-018` + הצלע הנמדדת של `B-002`: השומר מודד **כשל בפועל**, ⛔ לא נוכחות | 15.08 | `8fb8b11` · `4b8f5a7` · `f22800f` · `d46baf1` | ✅ **אומת בקנרית חיה** (`🐤 Canary Drill`, `workflow_dispatch` בלבד — `grep -cE '^\s*schedule:'` = **0** ⇒ ⛔ אפס workflow שמייצר כשל אוטומטית). ✅ **ואומת בעין 15.08 — ניב צילם את ערוץ הדיסקורד** (`C-026`). 🔴 **שתי שכבות, ⛔ לא אחת — ושתיהן נרשמות:** **(1) מכנית** — `discord POST -> HTTP 204` · `channels delivered: 2/2` · נושא המייל `🚨 🐤 Canary Drill — התראת-בדיקה נכשל`. ⚠️ **כל השלוש מודדות את ה**משגר**:** 204 הוא מה ש-Discord **קיבל**, ⛔ לא מה שניב **ראה** — ומדידת-משגר לבדה היא בדיוק ‹R-4›, השורש שהגל הזה נועד לסגור. **(2) ✅ בעין** — **5/5** הודעות בערוץ, כל אחת עם 🐤 + שם ה-workflow + URL. זמנים (מקומי UTC+3): **16:24:59 · 16:29:05 · 16:37:30 · 16:40:32 · 16:51:56**. **T-A — חמש מסירות, ⛔ לא ארבע:** `31887147462`(`8fb8b11`) · `31887326697`(`4b8f5a7`) · `31887705569`(`f22800f`) · `31887840180`(`d46baf1`) · והחוזרת אחרי הבלוק האחרון `31888353149`(`1991f83`), שנורתה מ-canary **`31888345445`**. ⚠️ **הרישום הקודם "4/4" היה מכנה שגוי ⛔ ולא מספר שגוי** — הוא ספר ריצות תחת `Delivery gate`, והראשונה (16:24:59) קדמה ללידתו (`4b8f5a7`, 16:28:46) ⇒ **4/4 עם שער · 5/5 בערוץ**, ושני המספרים נכונים תחת אוכלוסייה מוצהרת (§2). **התג הגיע לנושא המייל עצמו** ⛔ בלי לכופף את מסלול המייל לקנרית. ⚠️ **הקנרית התאימה את עצמה למסלול ⛔ ולא להפך** — ⛔ אפס שינוי במסלול המייל כדי לשאת את `🐤`. **T-B — בקרת-שלילה, ⛔ לא רשות (D2):** קנרית ב-`mode=pass` ⇒ `success`, ושלוש ריצות `failure-alert` (`31888391814`·`31888389459`·`31888376326`) **כולן `skipped` עם `steps: 0`** ⇒ ⛔ אפס מסירה על ירוק. **`B-001` — הצופה שלא היה:** `failure-alert` לא היה ברשימת הצפייה של עצמו ו-`watchdog` דילג עליו **בהגדרה** (`workflow_run` בלי `schedule:`). נסגר במתאם ההתראות ב-`fleet-daily` — **מכנה של כשלים אמיתיים ⛔ לא של זמן**, ולכן ⛔ אינו יכול להתריע-שווא בשבוע שקט. נצפו **ארבעת הענפים** לפני הקומיט: ירוק `2/2` · אדום `0/2` עם שמות ו-URLs (נכפה ב-`WINDOW_MIN=0`) · `bail` (404 אמיתי — `nivhareven` מול `niveven183`) שהוציא **embed אדום ⛔ ולא היעלמות** · ומכנה ריק (נכפה ב-lookback `0 hours ago`). **`B-002` — שלושה מצבי כשל-שקט נסגרו:** `curl -sS` בלי `--fail` יוצא `0` על 4xx/5xx ⇒ הוחלף בהשוואה מפורשת ל-**204**; `continue-on-error` שהשתיק שער; ו-`delivered` שנכתב פעם **אחת** בלבד ל-`$GITHUB_OUTPUT` ⇒ ⛔ אין last-write-wins. הריצה נכשלת **רק** אם ⛔ אף ערוץ לא מסר. **`B-018`:** `Restore Drill` נכנס לרשימת הצפייה (12→**13**), ושתי קריאות ה-Discord שלו הוחלפו בהשוואה מפורשת (`::warning::` ⛔ ולא `exit 1` — שם Discord משני). **תקרות זמן נגזרו ⛔ ולא נוחשו:** `restore-drill` **4/4** ריצות מקסימום `1m22s` ⇒ `timeout-minutes: 20` (ברירת המחדל 360 = **263×** הנצפה); `failure-alert` **9/9** מקסימום `30s` ⇒ `5`. ⚠️ **הערת ה-`S8γ` הישנה הוחלפה** — היא הצהירה ש-`Restore Drill` "כבר מתריע על תנאי הכשל של עצמו", כלומר הכריזה מצב כבלתי-אפשרי בעודו אפשרי; מייל כפול אחד התקבל כמחיר חסום ומוצהר. **כיסוי נמדד:** 18 קבצי workflow — `failure-alert` 13 + `Triage` 3 = **16/18**; השניים שלא: `failure-alert` עצמו (מכוסה במתאם, כי ריצת-התראה שנכשלה ⛔ אינה כיסוי) ו-`health.yml` (`workflow_dispatch` בלבד ⇒ שהות = "אף אחד לא נזקק", ⛔ לא "מת" — **תועד בקובץ עצמו** ב-`1991f83`). 🔴 **בלוק 4 ⛔ לא בוצע — המדידה הפריכה את הכרעה (ב):** ראה `D-037`. ⇒ צלע הספים של `B-002` ⛔ אינה נסגרת כאן ועברה ל-`B-148`. `npm run verify` **23 חוליות + build ירוקים**; `test:registry` **27/27**, 209 מזהים. **`RUNBOOK` § 🐤 תרגיל הקנרית** כולל `### 🔴 מה עושים אם T-A ⛔ לא הגיעה`; `C-026` נולד ו**נסגר בשתי השכבות**. §10: `B-146`·`B-147`·`B-148`·`B-149` | T3 |
| D-037 | 🔴 **הפרכת M1 — בלוק 4 **נעצר**, ⛔ ואפס ספי `Watchdog` שונו** | 15.08 | `1b4d3ad` (`docs/audits/AUDIT-guardians-M0.md`) | ✅ **אומת-מקור** (`git show origin/main:docs/audits/AUDIT-guardians-M0.md` §M1 + `origin/main:.github/workflows/watchdog.yml` — ⛔ לא מהעץ המקומי; `git diff d46baf1 -- .github/workflows/watchdog.yml` = **ריק**). **מה הופרך:** הכרעה (ב) שאושרה כללה `Sentinel: 3h → 26h`. **המדידה:** בודק יומני ב-08:00 יכול לצפות רק **גילאים בדידים** — `(8 − cron_hour) mod 24 + 24k` — ולכן זמן הגילוי נקבע ע"י **הדלי הראשון שמעל הסף** והוא **עולה מונוטונית ב-`MAXAGE`**. ⇒ `Sentinel=3` יורה אחרי **24.17ש'**, ו-`Sentinel=26` היה יורה אחרי **48.17ש'**: השינוי המאושר היה **מוסיף 24ש' עיוורון** לשורה ה**יחידה** שכבר אופטימלית. **האוכלוסייה המלאה:** **8/11** שורות מבזבזות בדיוק 24ש' · `restore-drill` **96ש'** · **2/11** (`user-analytics` 26.47ש' מול דלי 25.5 · `fleet-daily` חלון בטוח 0.59ש') ⛔ אינן ניתנות להידוק כלל. ⛔ **ולא נכתב נימוק ל-26** — זו הייתה בדיוק הרציונליזציה שהכרעה (ג) אסרה ("מספר בלי נימוק הוא חוב" ⇒ נימוק שנתפר למספר שגוי גרוע ממנו). ⛔ **והאסרציה המכנית שהוצעה (`MAXAGE ≥ מחזור×1.1`) נפסלה** — היא הייתה **מחייבת** את הרגרסיה ומקבעת אותה בשער. **הכלל הנכון הוא ההפוך:** **מזעור** `MAXAGE` בכפוף לחריגה מהפער הנצפה, ⛔ לא רצפה הנגזרת ממחזור הבודק. ⚠️ **הרישום הזה ⛔ אינו "משימה שנדחתה" אלא ממצא** — הוא סוגר את חובת §10.1 על החלטה שהתגלתה תוך כדי; **הביצוע** חסום על הכרעת ניב ב-`B-148`, והוא ⏭️ **#1 ב-`docs/NEXT.md`**. ⚠️ **`T3` ⛔ ולא `T1`:** נמדד **בתוך** הגל ורמה ⛔ אינה יורדת תוך משימה (§15); ⛔ אין מסך ⇒ הסמן `אומת-מקור` | T3 |
| D-038 | `B-146` — **עוגן יציב לטבלת היומן**, ואסרציה שעברה בשקט כשהיה עליה ליפול | 15.08 | `1991f83` | ✅ **אומת-מקור** — ⛔ **אין מסך מושפע:** `data-testid` הוא תכונה בלתי-נראית למשתמש, ⇒ האימות הוא **קריאת התוצר החי** (⛔ לא מהעץ המקומי) + **מדידה חוזרת עצמאית**. **מה נמדד:** `table.w-full.text-xs` תואם **3** טבלאות ב-`SwingEdge_App.jsx` ועוד **3** ב-`AdminPanel.jsx` ⇒ strict-mode violation ברגע ששתיים גלויות. 🔴 **והשורה השנייה הייתה גרועה יותר:** `:204` משווה `rows.count()` כ-`n < FIXED_TRADES.length` ⇒ טבלה תואמת נוספת **מנפחת את המונה** והאסרציה **עוברת** במקום להיכשל — **מעבר שקט**, ⛔ לא נפילה רועשת. ⚠️ **לכן שונתה גם `:38` וגם `:204`** — התוכנית המוקדמת שלי הותירה את `:38` ותוקנה אחרי המדידה. **התיקון:** `data-testid="journal-table"` יחיד, והספֶּק מצביע עליו דרך `JOURNAL_TABLE`/`ROWS`. **אימות פרודקשן:** `/assets/index-B9QP4642.js` (HTTP **200**, **1,638,619** בתים) נושא `data-testid":"journal-table"` — **1/1 מופעים**, שזו בדיוק הטענה (עוגן שאינו יחיד אינו עוגן). **וריצת Sentinel חיה מול פרודקשן** (`31888449189` @ `1991f83`): `3 passed (13.4s)` · `sentinel auth findings: 0` · `✓ 1 [chromium] › tests-sentinel/sentinel-auth.spec.js:301:1 › authenticated journey: login → journal → create/delete SNTNL (6.3s)`. ⚠️ **ואומת ש**רץ** ⛔ ולא שדולג** — `minute=54 event=workflow_dispatch auth=1`; שכבת-האימות ב-Sentinel מותנית בדקה (`≥45` או `≤9`) ⇒ "ירוק" בלי השורה הזו היה בדיקה **לא-חד-משמעית** שמתחזה לתוצאה. ⚠️ **המיקום מזוהה ב**עוגן** ⛔ ולא במספר שורה:** ה-INBOX רשם `:4377·:4557·:4884` ובעת התיקון הם היו `:4473·:4653·:4980` — **96 שורות סחיפה ביממה**. **`B-147` הונפק ב-`BACKLOG` לפני התיקון** (דרישה D4) ונשאר במכוון **לא-מתוקן**: הסוכן שממציא סיבה מעל המדידה הוא ‹R-2› והוא גל נפרד | T3 |
| D-039 | `B-149` — **§11.2 קורא שני קבצים: אזכור מזהה-בדיקה ⛔ אינו ביצועה**, ושני ענפי-האישור-בטעות נסגרו | 15.08 | `f7926fd` | ✅ **אומת-מקור** — ⛔ **אין מסך מושפע:** שער CI פנימי. ⇒ האימות הוא **clone נקי מ-`origin`** (⛔ לא מהעץ המקומי) + **מדידה חוזרת עצמאית** מעל אותו עותק. **מה נמדד ב-clone** (`HEAD = f7926fd8dfcb…`): השער רץ שם **29/29** · `C-023`=**5 תאים** · `C-026`=**5 תאים** · **23/26** בדיקות מסווגות כ**בוצעו**, והשלוש שאינן הן `C-021` · `C-024` · `C-016` · **6/6 שורות `T3` לא-רטרו עוברות** (3 על `אומת בעין`, 3 על `אומת-מקור`). 🔴 **ארבע ראיות נצפו אדומות על השורה ה-אמיתית, ⛔ לא על fixture מסונתז:** (1) **שיניים בשני הכיוונים** — `git show cd65365:docs/DONE.md` ⇒ `D-036` **נופלת** «אזכור בלבד — `C-026`=הוכחשה בשורה», ואותה שורה ב-`HEAD` **עוברת** «סמן מפורש: אומת בעין». **נזק צדדי: 1/6 בעבר ⇒ 0/6 בהווה.** (2) **שלוש צורות ההכחשה** (`D1`) — `⛔ לא בוצעה` · `⛔ טרם בוצעה` · `⛔ פתוחה` — כל אחת אדומה **בנפרד**, ולכל אחת נמדדה גם הצורה **בלי** ההכחשה, שעוברת ⇒ הנפילה ⛔ אינה צורנית. (3) **הענף שירה** — נוסח ההמתנה של `D-036` עבר בשער ה**ישן** ונופל בחדש: «⛔ אין סמן אימות כלל». (4) **הכחשה רחוקה** (400+ תווים) ⛔ **אינה** פוסלת סמן תקין. 🔴 **ההפרכה — וההנחה השגויה הייתה שלי:** ההנחה בפרומפט **ובדיווח הקודם שלי** הייתה שענף `C-0\d\d` הוא שאישר את `D-036`. **המדידה הפריכה זאת:** `strip` מנכה גרשיים אחוריים, וכל מזהי הבדיקה במרשם כתובים בגרשיים ⇒ **הענף היה מת ולא ירה מעולם**. הענף ש**ירה בפועל** הוא זה של תמונת-המסך — שם-עצם שמופיע **זהה** בהצהרת קיום ובהצהרת היעדר — והוא התאים למשפט שמצהיר שהראיה **עדיין אצל ניב** ⇒ אסרציה שנופלת על התיעוד של **היעדרה**: ‹R-3› בכיוון ה**מאשר**, פעם **רביעית** (`B-120`). ⇒ הענף **הוסר** מרשימת הסמנים. **שלושה לקחים, כל אחד מדוד:** ⓵ **`⛔` ⛔ אינו מילת הכחשה** — הוא סימן **הדגשה** בכל תא במרשם. אב-טיפוס שכלל אותו באוצר-המילים הפיל **6/6** שורות תקינות ⇒ נזנח ותועד בתוכנית כדי שלא ינוסה שוב. ⓶ **קרבה היא חובה, ⛔ לא עידון** — `בלוק 4 ⛔ לא בוצע` יושב **2,165 תווים** מהסמן של `D-036` ועוסק ב**בלוק עבודה**. כלל שקורא את התא כולו היה מכריח **לרכך את `DONE`** כדי לעבור — בדיוק האסור ⇒ חלון **±90** תווים. ⓷ **«בוצעה» ⛔ אינה `✅`** — רק **3/26** שורות `CHECKS` נושאות `✅`, בעוד `C-006` · `C-011` · `C-017` · `C-018` רצו ומצאו 🔴. בדיקה שרצה ומצאה תקלה **בוצעה** ⇒ ההגדרה היא **תאריך אמיתי + תוצאה שאינה ⏳** + כל מצביע `D-` שבתוצאה **קיים** ב-`DONE`. ⚠️ **ו-`11.3` תפסה באג במסווג של עצמי בריצה הראשונה** (`10/11`): מזהי ה-fixture שבחרתי ⛔ לא התאימו לרג׳קס `/C-0\d\d/` ⇒ **תוקנו ה-fixtures, ⛔ לא הרג׳קס** — ריכוך שם היה מוחק בדיוק את השן שנוספה. **המבנה:** `CHECKS` נורמל ל**מינימום שהכלל מחייב** ⛔ ולא מעבר — `C-026` **6⇒5** תאים (תא תדירות כפול שנקרא מיקומית כתוצאה) · `C-023` **4⇒5** עם **מצביע** ל-`D-032`/`D-033`, ⛔ ולא העתקת תוצאה ‹R-6›. **אסרציות 27⇒29:** `5.2` — כל שורת `CHECKS` נושאת 5 תאים (חריגת מבנה עתידית הופכת בדיקה שרצה ל«שלא רצה» **בשקט**) · `11.3` — self-test **קבוע** מול **11** fixtures **הרמטיים** ש⛔ אינם קוראים `DONE` חי, ⇒ עריכת שורה ⛔ אינה יכולה לשנות את השער שבודק את עצמו. ⛔ **אפס נגיעה ב-`docs/DONE.md` בגל עצמו** ⛔ ואפס ריכוך — השורה הזו נכתבת ב**קומיט שאחריו** (כלל 4.1) | T3 |
| D-040 | `B-129` — **סולם הראיות מעיד על עצמו: `currency_source` · `source` · `import_batch_id` · `inserted_at`** ‹סוגר גם `B-152` (א) · `B-153` (א) · `B-071`› | 16.08 | `60e57c6` | ✅ **אומת בעין** — ניב הריץ את שני מסלולי הכתיבה בפרודקשן וקרא את התוצאה מה-DB. **4/4 שורות הייבוא:** `ZZTEST`+`ZZTESTB` → `broker_arithmetic` (דרגה 1, מסלול הברוקר) · `ZZTESTC`+`ZZTESTD` → `file_cell` (דרגה 2, המסלול הגנרי) · **2 `import_batch_id` נבדלים** (`f899706f…` ×2 · `d2cc0a39…` ×2) ⇒ המזהה מסמן **מנה**, ⛔ לא שורה · `inserted_at` מלאה ב-4/4. **הידנית:** `manual` · `manual_capital` · `ILS`. **⇒ ארבעה ערכי תעודה נבדלים בפרודקשן** — `broker_arithmetic` · `file_cell` · `manual_capital` · `null` ⇒ **`V5` מוכחת על נתון אמיתי: אותה תווית `USD` בדיוק, מקורות שונים, ונבדלות אחרי הכתיבה.** 4 שורות הבדיקה נמחקו ב-`DELETE` ממוקד לפי ארבעת הטיקרים. **שערים:** `verify` 23 חוליות + build · `test:import` 33 תרחישים · `test:instrument` 359/359 · `test:write` 29/29 · `test:registry` 29/29. **נצפה אדום לפני ירוק — 15 אסרציות**, בהן `V5` (`expected true\|false, got false\|false`) ו-`V5⁻¹` ההפוכה; ⚠️ `V5⁻¹` על **דרגה 3** נשארה **ירוקה** במצב הישן, וזו הסיבה שהיא קיימת: **סירוב גורף הוא כישלון, ⛔ לא הצלחה**. **יצחק נמדד ⛔ ולא הונח:** 2 מסלולי ייבוא × פיקסצ'רים קיימים ⇒ **0 שורות הוסרו**, 430=215×2 ו-32=16×2 נוספו, ומנוקות משני השדות החדשים התוצאות **byte-identical** (`diff` יצא 0). 🔴 **הגל מונע הבאות ⛔ ואינו מתקן קיימות** — **67/67** השורות שקדמו לו נושאות `currency_source = null` · `source = null` · `import_batch_id = null` · `inserted_at = null`, והצעד הכפול (`add column` בלי default → `set default now()`) **נמנע במכוון** מלהטביע 67 חותמות מסונתזות ‹§2›. **13 שורות ת"א באגורות ⛔ לא נגעו** — הן `B-005`/`B-121`, חסומות על הודעה מוקדמת למשתמש. ⚠️ **והכי חשוב — הפרכה של הנחה שהייתה שלי:** באימות העין הועלה חשד שהגל **יצר** תיוג `ILS` שגוי חדש. נמדד ב-`git`: `currency: capitalCurrency` נולד ב-**`5977ba5`, 02.08** — **14 יום לפני הגל** — וה-`diff 5c93ab3..60e57c6` מראה **0 שורות שהוסרו** באזור המטבע. ⇒ הגל **הצהיר** על התיוג הקיים כהנחה (`manual_capital`, דרגה 5, `isEvidenceBacked = false`) ו⛔ לא יצר אותו. **החשד נולד מצילום בלי `git log`** — בדיוק מה ש-§8.1 מונע, ו-`0/67 ILS` באבחון מדד **אוכלוסייה** ⛔ ולא התנהגות ‹§2›. ⚠️ **וגם הכיוון שהוצע כתיקון הופרך:** `deriveInstrumentCurrency` מחזיר `USD`/`assumed` לכל טיקר **אלפביתי** — כולל נייר ת"א — ו⛔ **אינו יכול להחזיר `ILS` לעולם** ⇒ גזירה מהנייר הייתה מחליפה הנחה בהנחה. נרשם ב-`B-154`. ⚠️ **מה ⛔ לא נסגר:** `B-042` (העמודה קיימת, **0 צרכנים**) · `B-080` (חוזה ה-Vision חסום על `B-110` ❄️; רק קדם-המילוי הוצהר, `C-027`) ⇒ `B-154` · `B-155` | T3 |
| D-041 | `B-009` — **המוצר מפסיק להמציא מספרים על יומן ריק** ‹סוגר גם `B-064` (`EMPTY_STATS` נשאר `0`) · `B-065` (צורת `lastWeekStats`) · `B-157` (השער והמספרים קראו שתי אוכלוסיות)› | 16.08 | `eb8758b` | ✅ **אומת בעין** — נצפה על `/app` בבנייה של `eb8758b`, **שלושת החצאים כל אחד לפני ואחרי** (הקבצים הוחזרו ל-`19d1dd0` ובחזרה; `git status` נקי בשני הקצוות). **(א) יומן ריק:** `אחוז הצלחה` **`0%` ⇒ `"—"`** · ה-`▲ 0.00%` הירוק על ה-P&L הסגור **נעלם** (`trend != null`, ⛔ לא `!== undefined` — `null >= 0` הוא `true`) · סרגל הסטטיסטיקה ⛔ **אינו מרונדר כלל** · **0 שגיאות קונסולה**. ⚠️ **וה-`$0` ששרד ⛔ הוא ממצא, לא כשל:** `רווח/הפסד נקי +$0.00` על `0 עסקאות סגורות` הוא **סכום מעל קבוצה ריקה** ⇒ `0` הוא **איבר היחידה** ⇒ מבחן 2 של `PLAN-B009.md` §1.1 מכשיר אותו. וכן `▲ 0.0%` על **הון החשבון**, שהופרך כשייך לגל: הוא `curEquityReturnPct` (`:2467`) — `(2500−2500)/2500` — **מדידה** מהון אמיתי ⛔ ולא `EMPTY_STATS`. **(ב) יומן מלא — 4 סגורות שנזרעו לחישוב-ביד:** `+200` · `−200` · `+100` · `−120`. **7/7 ערכי הסרגל חושבו ביד והתאימו:** `4` · `50%` (2/4) · `+$150.00` ((200+100)/2) · `-$160.00` ((200+120)/2) · `0.94` (300/320=0.9375) · `-$220.00` (שיא +200 ⇐ שפל −20) · `2.8d` ((3+3+3+2)/4=2.75). וכן 4 KPI: `-$20.00` · `$2,480.00` · `-0.80%` · `-0.05R` ((2−2+1−1.2)/4). 🔴 **ו-0 מספרים אמיתיים הפכו ל-`"—"`** — 4 המופעים של `—` בעמוד נמדדו **כולם** כמקף בפרוזה ⛔ ולא כמציין-ערך ⇒ **סירוב גורף ⛔ לא קרה**. **(ג) `B-157` — הראיה שהבאג היה על המסך:** פילטר טיקר `ZZZZ` על אותו יומן של **4 סגורות אמיתיות**; **לפני** — `סה״כ סגורות 0` · `אחוז הצלחה 0%` · `רווח ממוצע +$0.00` · `הפסד ממוצע -$0.00` · `PROFIT FACTOR 0.00` · `ירידה מקס' -$0.00` · `זמן החזקה 0.0d`, **שבעה תאים מומצאים ישירות מעל "אין עסקה שתואמת"**; **אחרי** — הסרגל **נעלם**. ⚠️ ⇒ זהו המסלול ה**יחיד** שבו משתמש **פעיל** ראה `EMPTY_STATS`, ולכן הוא ⛔ לא נמצא כל עוד חיפשו "יומן ריק". **הכלל שנקנה — שלושה מבחנים בסדר** (`PLAN-B009.md` §1.1, ⛔ החלטה כללית ולא נקודתית): **טווח** (האם המסלול החי יכול לייצר את הערך? `bestWin: 0` נופל — זכייה היא `pnl>0` **בהגדרה**) → **איבר-יחידה** (האם `0` הוא הזהות של הצבירה מעל הריק? סכומים ומונים כן, **מנות וקיצונים לא**) → **התנגשות** (האם `0` נוצר גם בתנאי אחר **בעל משמעות**? רק `returnPct` נופל שם). ⇒ **מנה או קיצון ⇒ `null` · מונה או סכום ⇒ `0`**. **שערים:** `verify` 23 חוליות + build (`coach` 111 · `instrument` 359/359 · `horizon` 140/140 · `syntax` 4/4 · `registry` 29/29 · `vite build ✓ 7.37s`). **נצפה אדום לפני ירוק:** 3 אסרציות ערך. **והבייסליין הוקפא מחדש (v4⇒v5) עם הוכחת-אפליה דו-כיוונית** ⛔ ולא כהתאמת-ציפייה: `winRate: 0` ⇒ **5 אדומות** כולל `✗ empty — DIVERGED`; **ובנוסף** `planAdherence: 0` — שדה ש**בלוק 9b ⛔ אינו מקבע** ⇒ **2 אדומות** כולל אותה סטייה ⇒ האדום מיוחס ל**בייסליין עצמו** ⛔ ולא לאסרציות הערך. שניהם הוחזרו ⇒ ירוק. **16 ערכים זזו, 2 נוספו, 0 הוסרו — כולם בתרחיש `empty` בלבד**, כפי שנחזה **לפני** ההרצה. ⚠️ **ותיקון לדיווח שלי עצמו, שנמדד 16.08 בבירור נפרד ⛔ ולא הונח:** `isEmpty` **כן** נצרך בקוד המוצר — `SwingEdge_App.jsx:4824`, שער `B-157` (הגרפ שדיווח «0 צרכנים» סרק `src/`, והקובץ יושב ב**שורש**). 🔴 **אבל הטענה ש-`tradingstats-test.mjs:99` «הפסיק לדלג» הופרכה:** `ratesSumTo100` נקרא מ-**5 אתרים · 0 מהם עם אובייקט ריק** ⇒ הענף החדש (`:126`) **⛔ אינו רץ** — ו-`if (s.isEmpty) return` הישן ⛔ לא רץ גם הוא. ⇒ **`0%` שרד כל ריצה ירוקה ⛔ לא בגלל דילוג, אלא משום שבלוק 1 קיבע `winRate: 0` כ*חוזה*** (`19d1dd0:127`) — ‹R-3› במקום אחר מזה שנרשם. ⇒ `B-160`. 🔴 **מה ⛔ לא נסגר, ובמכוון:** `B-156` — `50` ב-`TradeDNA`/`GrowthTracker` (**10 אתרי ייצור · 9 שדות · 6 צרכנים אריתמטיים**) ⇒ `null` שם היה `NaN` על המסך, וה**כיוון ⛔ לא הוכרע** (ייתכן **גדר תצוגה**) ⇒ עלה ל-`NEXT` §1. **וסריקה סביבתית §11 הוציאה שניים חדשים, שנמדדו ⛔ ולא הונחו:** `B-158` — אותו באג דרגה אחת למטה (5 שדות נופלים ל-`0` על תת-קבוצה ריקה; נמדד על יומן **2/2 מרוויחות** ⇒ `isEmpty=false` · `losses=0` ⇒ התא מדפיס **`-$0`**) ⇒ **וזו גם ההוכחה ש-4 הגדרים שנוספו כאן ⛔ אינם קוד מת** · `B-159` — הבייסליין **עיוור** ל-`avgHold`: `holdDays` (`utils.js:48`) דורש `closedAt` והפיקסצ'רים נושאים `exitDate` ⇒ **7/7 התרחישים הלא-ריקים מקבעים `0`** ⇒ **קיפאון של השער ⛔ ולא באג במוצר** ‹R-3› | T3 |
| D-042 | `B-160` — **`isEmpty` הוסר, ⛔ לא שונה-שם: `isEmpty ⟺ total === 0` בדיוק, והשער קורא עכשיו את אותו ביטוי שהתא מתחתיו מדפיס** ‹`openTrades` פוצל ל-`B-161`, ⛔ אינו בגל הזה› | 16.08 | `1fc6170` | ✅ **אומת בעין** — שני מצבי שער `B-157` נצפו **לפני וגם אחרי** השינוי: פילטר שאינו תואם אף עסקה סגורה ⇒ הלוח **נעדר**; יומן אמיתי ⇒ הלוח **נוכח**, והתא הראשון (`total`) חושב ביד ותאם. **הטענה שכתבתי ב-`D-041` הופרכה במדידה נפרדת** (`docs/audits/AUDIT-B160.md`): ⓵ **טאוטולוגיה** — `tradingStats.js:50` הוא `if (closed.length===0) return EMPTY_STATS(capital)` ⇒ `isEmpty === (total===0)` נמדד **4/4** מצבים, כולל 0 סגורות·**5 פתוחות**; הדגל נכתב כ**ליטרל ביד** בשני ענפים (`:276`·`:330`) ⛔ ולא נגזר. ⓶ **הענף מת** — `ratesSumTo100` נקרא מ-**5 אתרים · 0 עם אובייקט ריק** ⇒ `:126` **⛔ מעולם לא ירה**; `0%` שרד לא בגלל דילוג אלא כי בלוק 1 קיבע `winRate:0` **כחוזה**. ⓷ **השם שגוי** — יומן עם 5 פתוחות/0 סגורות קיבל `isEmpty:true`. **הביצוע:** השער ב-`SwingEdge_App.jsx:4824` → `journalStats.total > 0`; `:126` **נמחק** (⛔ לא שומר בשם אחר — `R-6`), האסרציה הייחודית (`wins+losses+be===0`) עברה לבלוק 9b **חי**; `:158`/`:536` נכתבו מחדש על `total`. **תחזית מחייבת נרשמה לפני הריצה** (16 מחיקות/8 הוספות) — הבייסליין נקפא מחדש `v5→v6` וה-`git diff` תאם **בדיוק**. **הוכחת-אפליה דו-כיוונית, שני הפלטים הודבקו בדיווח:** מוטציה ב-`winRate` (מסלול חי) הפילה **גם** אסרציות **וגם** בייסליין; מוטציה ב-`bestStreak:251` (`maxWinStreak → maxWinStreak+1`, נבחר במדידה — `grep -c bestStreak` על קובץ הבדיקה = 0, ⛔ לא ברשימת `SCALARS`) הפילה **רק** את הבייסליין ⇒ לבייסליין כוח אבחנה **עצמאי**. `C-028` ירה שוב (תנאי-הגלישה: כל נגיעה בלוח הסטטיסטיקה) ואומת בעין בשני המצבים. **שערים:** `npm run verify` **23 חוליות + build ירוקים** (פלט מלא הודבק בדיווח) · `test:tradingstats` — כל 8 התרחישים deep-equal לבייסליין הקפוא · `test:registry` 29/29 (אחרי הקומיט הבא). ⚠️ **`openTrades` — חפיפה עם `isEmpty` נמדדה אפס** (`tradingStats.js:50`/`:301` משליכים את מערך העסקאות ⇒ `stats.openTrades=0` גם על יומן עם פתוחות; המוצר מחשב `openTrades` בעצמו ב-`SwingEdge_App.jsx:2206` ואינו קורא את השדה — דפוס `B-154`) ⇒ **מזהה נפרד `B-161`**, ⛔ לא שוכפל כאן (`R-6`) | T3 |

✅ **`D-030` נכנס כאן בקומיט שאחריו — הפעם הרביעית ברציפות.** ⚠️ **הדפוס חוזר כי
הכלל נכון, ⛔ לא כי משהו שכח:** קומיט אינו יכול לשאת את ה-hash של עצמו, ו-`--amend`
אסור (§5). ⛔ אל "תתקן" זאת.

✅ **`D-028` ו-`D-029` נכנסו כאן בקומיט שאחריהם, מאותה סיבה בדיוק.** ⚠️ **זו
הפעם השלישית ברציפות** (`D-023` · `D-024` · וכעת שניהם) — ⛔ לא תקלה חוזרת אלא
צורת העבודה שכלל `4.1` מכתיב: **גל שסוגר את עצמו נרשם תמיד בקומיט הבא.**

✅ **`D-023` נכנס כאן בגל ג׳ (הקומיט הבא שנגע במרשם), ⛔ לא בקומיט של עצמו.**
השער `4.1` תפס זאת בריצתו הראשונה: **קומיט אינו יכול לשאת את ה-hash של עצמו**,
ו-`--amend` אסור (`CLAUDE.md` §5). ⚠️ **הכלל לא רוכך כדי לעבור** — זו בדיוק
ההתנהגות שהשער נבנה בשבילה, והשורה הזו היא ההוכחה שהמנגנון סגר מעגל בפועל.

✅ **גל ג׳ נכנס כ-`D-024` בגל השורשים (13.08)** — שני התנאים שעצרו אותו נסגרו,
כל אחד בנפרד: (1) ה-hash `3b1a1cf` נודע אחרי הקומיט, כלל `4.1`; (2) **הוא אומת
בעין 13.08** מול צילומי מסך, `C-019`. ⚠️ **הבדיקה מצאה ליקוי** — `B-119` — ולכן
היא בדיקה, ⛔ לא טקס: אילו הייתה עוברת חלק, לא היה לה כוח ראייתי.

---

## ⚠️ מה **לא** אומת בעין

- **`D-009` אופק העסקה** — נצפו 140 אסרציות ירוקות. הספים משנים כמה משתמשים רואים את הפיצ'ר; מספר ירוק אינו מסך.
- **`D-011` גל א׳ מטבע** — נצפו 105 אסרציות ירוקות. **הבאג נשאר חי** ב-`posSize` ובתשעה אתרי סכום-בחשבון. 105 ירוקות וגל שלא עבד — `C-001` נולד מכאן.
- ~~**גל ג׳ מטבע**~~ — ✅ **אומת בעין 13.08 ונרשם כ-`D-024`.** ⚠️ **הוא נשאר ברשימה הזו כתקדים, ⛔ לא כחוב:** הוא היה 216/216 ירוק במשך יממה שלמה מעל מסך שאיש לא ראה, וה-⛔ שחסם אותו הוא מה שהחזיק אותו פתוח עד שנראה — ואז נמצא בו `B-119`.

⚠️ **שלושת גלי המטבע הם שלושה פריטים נפרדים, ⛔ לא אחד:** `D-011` (גזירת מטבע
הנייר בזמן קריאה) · `D-012` (המרה לפני החלוקה ב-`sizePosition`) · **גל ג׳**
(9/10 אתרי סכום-בחשבון + עיגול בתצוגה). ⛔ אל תאחד אותם בדיווח — `D-011` הוכרז
סגור בעוד הבאג חי, ואיחוד היה מסתיר בדיוק את הפער הזה.

⛔ **אסרציה ירוקה אינה אימות בעין.** כל גל שנוגע במספר פונה-משתמש חייב פריט ב-`docs/CHECKS.md` לפני שהוא נסגר.
