# DONE — גלים שנסגרו

> **חוזה חמשת הקבצים — פריט חי בקובץ אחד בלבד.**
>
> | קובץ | מה חי בו |
> |------|-----------|
> | `docs/STATE.md` | 🔴 עכשיו · ⏭️ 3 הבאים · ⏸️ חסום · ⚠️ סיכונים. **תקרת 100 שורות בתוקף.** |
> | `docs/BACKLOG.md` | כל פריט פתוח שאינו פעיל. אין תקרה. |
> | `docs/DONE.md` (כאן) | **הושלם** — מזהה · תאריך · hash · **איך אומת** · **`T`**. |
> | `docs/CHECKS.md` | בדיקות ידניות חוזרות. |
> | `docs/audits/STATE-ARCHIVE-*.md` | **סגור בלבד** — ⛔ פריט פתוח לעולם לא נוחת שם. |
>
> מעבר בין קבצים = **מחיקה משם והוספה כאן**, ⛔ לא העתקה.
> ⛔ **מזהה לא נמחק ולא ממוחזר לעולם.**
> ⚠️ **טבלה, ⛔ לא פרוזה.** הפירוט המלא נשאר ב-`docs/audits/STATE-ARCHIVE-2026-08.md` ומופנה לפי מזהה.
> ⚠️ **כל שורה חייבת hash.** גל בלי hash אינו גל שנסגר — הוא טענה.
> ⚠️ **כל שורה חייבת `T`** — רמת הסיווג (`CLAUDE.md` §15), ⛔ לא "כמה זה היה
> מסובך" אלא התשובה לשלוש השאלות. **`T3` מחייב אימות עין בעמודת "איך אומת"**;
> ⛔ בלעדיו הגל אינו נסגר.
> ⚠️ **29 השורות ההיסטוריות נושאות `(רטרו)`** — הסיווג נגזר **מטקסט השורה עצמה**
> ב-13.08 ו⛔ אינו ידיעה בת-הזמן. ⇒ הן **אינן** כפופות לשער אימות-העין: רטרו-התאמה
> של `D-009`/`D-011` כך שייראו מאומתים הייתה **בדיוק** ההפרה ‹R-3›. הדיוק שלהן
> נבדק ב-`C-024`.
> נאכף מכנית ב-`npm run test:registry` (חוליה 23).

---

## גלים שנסגרו

| מזהה | מה | תאריך | hash | איך אומת | T |
|------|-----|--------|------|-----------|-----|
| D-001 | מנוע ההון — ספי סיווג פר-מטבע · סמל מטבע בסיכום | 09.08 | `13fd7da` | `test:fx` 21/21 · **10 אסרציות נצפו נכשלות על הקוד הישן** (פלט הודבק בדיווח הגל) | T3 (רטרו) |
| D-002 | בסיס עקומת ההון בשער יום-הבסיס | 09.08 | `e2ea15d` | `test:fx` 21/21 ירוק אחרי · הקוד הישן חילק בשער היום ולא בשער יום-הבסיס | T3 (רטרו) |
| D-003 | גידור מטבע ב-`src/intelligence/` + הסרת ברירת מחדל שקטה | 09.08 | `da5b78c` | `test:coach` 111/111 · השרשרת המלאה הודבקה | T3 (רטרו) |
| D-004 | `test:import` §4 — פינים לאינווריאנט במקום לטקסט השומר הזמני | 09.08 | `92d105d` | `test:import` ירוק · RPC מחזיר `null` על מכנה ריק ומונים גולמיים | T1 (רטרו) |
| D-005 | גל הדייג'סט והסורק — פנקס תשובות + השתקה מתועדת | 08.08 | `ac9b38f` | `test:digest` 9/9 · `test:notify` 8/8 | T2 (רטרו) |
| D-006 | גל אנדרואיד — יכולת נצרכת רק אחרי בדיקה | 08.08 | `dbd5fb8` | `test:capability` 11/11 · **7 נצפו נכשלות על הקוד הישן** | T2 (רטרו) |
| D-007 | גל GA4 — מסכים פנימיים נמדדים, כל אירוע נולד מסונן | 08.08 | `c4810f1` | `test:analytics` 14/14 · **11 נצפו נכשלות על הקוד הישן** | T3 (רטרו) |
| D-008 | גל התיוג — הטקסט מכריז על היחס, הצבע נמדד ולא מוערך | 08.08 | `df1077b` | `test:copy` 16/16 · `npm run glossary` באותו קומיט | T2 (רטרו) |
| D-009 | גל אופק העסקה — נגזר מהצהרת המשתמש בלבד | 10.08 | `d7e4a4d` | `test:horizon` 140/140 · **7 נצפו נכשלות** · ⚠️ **לא אומת בעין** — ראה `C-001` | T3 (רטרו) |
| D-010 | גרש-הפוך בהערת SQL הפיל את `user-analytics` — תיקון + שער חוליה 21 | 10.08 | `dea4cc1` | `test:syntax` 4/4 · **2 נצפו נכשלות** · ריצת פרודקשן `#19` ירוקה · `INCIDENTS#15` | T3 (רטרו) |
| D-011 | גל א׳ מטבע — מטבע הנייר נגזר בזמן קריאה, שלושת השערים יכולים להיכשל | 11.08 | `e881248` | `test:instrument` 105 אסרציות · **18 נצפו נכשלות** · ⚠️ **לא אומת בעין ⇒ הבאג נשאר חי בשני מקומות נוספים** — הסיבה ש-`C-001` קיים | T3 (רטרו) |
| D-012 | המרה לפני החלוקה — גודל הפוזיציה חדל להיות גדול פי-שער | 12.08 | `636b490` | `test:instrument` 165/165 · `verify` 22 חוליות + build | T3 (רטרו) |
| D-013 | מחלקת הכתיבה — `tradeWrite` INSERT מאומת ב-`handleSubmit`, שורש המרוץ | 06.08 | `32a9efa`→`af858e2` | `test:write` 25/25 · `INCIDENTS#14` נסגר ב-`014bb15` | T3 (רטרו) |
| D-014 | מחיקה מאומתת + rollback — DELETE שאינו מוחק אינו מדווח הצלחה | 05.08 | `fadd61d` | `test:write` — 7 אסרציות על מסלול המחיקה | T3 (רטרו) |
| D-015 | תבניות המענה — `body_text` + `escapeHtml` + טוקן `reply:<id>:<n>:<h8>` | 04.08 | `d3eb7dd`,`f8616c6`,`e9d89e6` | `test:notify` — **11 מתוך 23 נצפו נכשלות על הקוד הישן** · יבש מאמת מכתב מלא | T2 (רטרו) |
| D-016 | מטבע אחד במסך הנחיתה — Pro 25₪/$8 עם toggle ידני | 03.08 | `22d6623` | `test:landing` — **1 מתוך 7 נצפתה נכשלת** | T3 (רטרו) |
| D-017 | מפתח rate-limit נפרד ל-`dry_run` — שער שחוסם את עצמו אינו שער | 03.08 | `67229a4` | `test:notify` — **8 מתוך 11 נצפו נכשלות** | T2 (רטרו) |
| D-018 | ערוץ entry שלישי ב-OCR — entry מקווי stop/target כשאין אחוזים | 04.08 | `8977fc5` | `test:ocr` סצנות I+J · **נצפו נכשלות על הקוד הישן ב-`61a7cd9` לפני התיקון** | T3 (רטרו) |
| D-019 | ייבוא FIFO — התאמת buy↔sell בקבצי ברוקר | 02.08 | `f375478` | `test:import` T5 | T3 (רטרו) |
| D-020 | ייבוא ו-undo — אימות מונה ודיווח חלקי מדויק | 06.08 | `b2bfa8c` | `test:import` · מונה שורות מאומת מול `row_count` | T3 (רטרו) |
| D-021 | עדכון · סגירה · מחיקה מרובה — אימות מונה שורות | 06.08 | `c553df3` | `test:write` — כל מסלול כתיבה מחזיר מונה ונבדק | T3 (רטרו) |
| D-022 | נוהל אבחון-לפני-ביצוע (`CLAUDE.md` §8.1) | 10.08 | `5b544e3` | תיעוד — אומת בשימוש בגלים `d7e4a4d`, `e881248`, `636b490` | T1 (רטרו) |
| D-023 | גל המרשם — פריט חי בקובץ אחד + `test:registry` אוכף | 12.08 | `075533e` | `test:registry` 13/13 · ⚠️ **נסגר בקומיט הבא, ⛔ לא בשלו** — ראה למטה. סוגר את `B-117` | T2 (רטרו) |
| D-024 | גל ג׳ — סגירת המטבע: 9/10 אתרי סכום-בחשבון + עיגול בתצוגה | 12.08 | `3b1a1cf` | `test:instrument` 216/216 · **11 נצפו נכשלות לפני החיווט** · ✅ **אומת בעין 13.08** (`C-019`): שורת היומן · הכרטיס במובייל · מודל היום · הטוסט — כולם מומרים בשער יום-המימוש; `POS. VALUE` = ₪774.88 בשתי ספרות; שער `2.9895` עקבי בכל האתרים. ⚠️ **ליקוי אחד נמצא בבדיקה ⇒ `B-119`** | T3 (רטרו) |
| D-025 | שורת `BE` — תווית המטבע `ILS`→`USD` על שורה אחת | 13.08 | `3b1a1cf` | ניב הריץ ידנית את `20260812150000_be_row_currency_label.sql` (§12). SELECT אחרי: `be_usd=1 · be_ils=0 · ils_total=0` · 61 שורות · `entry`/`stop`/`exit`/`shares` **זהים בדיוק** לפלט שלפני · הצ'יפ "מטבע לא מאומת" נעלם וסמל `$` חזר (צילום 11:15). `C-020` · סוגר את `B-122` | T3 (רטרו) |
| D-026 | הרחבת `trades.currency` — `ILA`/`EUR`/`GBP`, והסרת `default 'USD'` + `NOT NULL` | 12.08 | `636b490` | ניב הריץ ידנית את `20260812120000_trades_currency_widen.sql` (§12). אימות: `nullable=YES` · `default=null` · ה-CHECK מונה חמישה ערכים · 61 שורות, **אפס נגעו**. `C-004` · סוגר את `B-089` ואת `B-047`. ⚠️ `ILA` בר-**רישום** ⛔ אינו `ILA` בר-**קריאה** — `B-121` נשאר פתוח | T3 (רטרו) |
| D-027 | קמפיין `registered_users` — רץ מול הסוד האמיתי, ⛔ לא dry-run | 02.08 | `5ffc660` | `STATE-ARCHIVE:975`: **`sent=37 · failed=0`**, run `30749079414`, דיסקורד HTTP 204; הקהל 37/38 מ-`auth.users`. ⚠️ **נסגר כסתירה ⛔ ולא כגל** — הראיה שכבה בארכיון מ-02.08 בעוד `:1633` הכריז את ההפך, 11 יום. סוגר את `B-116` | T3 (רטרו) |
| D-028 | שכבת השורשים + מנגנון המצבה — מצב-סיום למזהה `B-` | 13.08 | `38896e4` | `test:registry` **18/18** בקומיט עצמו (13 לפני הגל ⇒ §8 הוסיפה חמש). **`8.4` נצפתה נכשלת על 5 מצבות אמיתיות** ⛔ ולא על הפרה מסונתזת, ו-`8.3` אוכפת את מיפוי השורשים **דו-כיוונית** (6 שורשים · 50 שיוכים). ⚠️ **נסגר בקומיט הבא, ⛔ לא בשלו** — כלל `4.1`. סוגר את `B-126` | T2 (רטרו) |
| D-029 | `NEXT` + `INBOX` + מפת הערוצים — הכרעה במקום רשימה, וקליטה במקום זיכרון | 13.08 | `c7ce628` | `test:registry` §9, **חמש אסרציות שנצפו נכשלות על הפרה זמנית לפני החיווט**. מדוד אחרי: `NEXT` = 32 שורות (תקרה 40) · **3 פריטים בדיוק** · 5/5 הפניות `B-` קיימות ב-`BACKLOG` · 3/3 נושאות מדד סגירה. `docs/AGENT-CHANNELS.md` מודד את השורש: **9/10 הערוצים יוצאים בלבד** ⇒ `B-130` · `B-131` | T2 (רטרו) |
| D-030 | חיסול המרשם השני + `docs/PROJECT-GUIDE.md` + פרוטוקול `T1`/`T2`/`T3` + הכרעת ה-spot | 13.08 | `2382a98` | `npm run verify` — **23 חוליות + build ירוקים**; `test:registry` **26/26** (23 לפני הגל ⇒ §10 ו-§11 הוסיפו שלוש). **שלוש האסרציות החדשות נצפו נכשלות לפני החיווט:** §10.1 — על `SwingEdge-Master-Tasks.md` ירה **סמן הכותרת בלבד** (שורותיו `\| T10 \|` בלי מקף), וקובץ זמני `TEMP-ROGUE.md` בכותרת תמימה `# Notes` ושורת `\| B-999 \|` הוכיח שסמן-השורה יורה **בנפרד** ⇒ שני סמנים עצמאיים, ⛔ לא אחד; §11.1 — על 29 שורות בלי עמודת `T`; §11.2 — בשלושה מצבים, כולל **הכשל-שקט**: `/אומת בעין/` תמים עובר גם על "**לא** אומת בעין", שזה בדיוק מה ש-`D-009`/`D-011` אומרים. **מדוד אחרי המחיקה:** **48 יחידות הכרעה** (17 שורות טבלה · 2 חסומים · 22 חובות · 5 כללי עבודה · מקטע ה-✅ · תמונת ה-📊) מוינו אחת-אחת מול הרפו — **38 מכוסות או מיושנות-בנימוק · 10 חסרות-בית** ⇒ `B-132`…`B-141`; 3 קבצי `.md` עקובים בשורש, אף אחד אינו מרשם. `CLAUDE.md` 267→296 שורות (תקרה 300). ⚠️ **`T2` ולא `T3`: אפס נגיעה בקוד רץ** ⇒ אין מסך לאמת בעין, והאימות הוא פלט הכלים המודבק. ⚠️ **נסגר בקומיט הבא** — כלל `4.1`. 🔴 **ובאותה צפייה-חוזרת נמצא חור ב-§11.2 עצמה:** השורה הזו, שסומנה `T3` זמנית, **עברה בירוק** משום שהיא **מתעדת את הרגקס** ⇒ הסמן החיובי נמצא בתוך ציטוט. הפעם ה**שלישית** שאסרציה נופלת על התיעוד של עצמה (`B-120`) ⇒ הציטוטים מנוכים לפני הבדיקה, והשער נצפה **אדום** אחרי התיקון (`D-030b`). סוגר את `B-131` | T2 |
| D-031 | שלוש שאלות הסיווג → **חמש**, וסמן אימות `אומת-מקור` ל-`T3` בלי מסך | 13.08 | `6ef1984` | `npm run verify` — **23 חוליות + build ירוקים**; `test:registry` **26/26** (⛔ אפס אסרציות חדשות — §11.2 **הורחבה**, לא נוספה). **ארבעה מצבים נצפו, שניים בכל כיוון:** (א) `T3` + `אומת-מקור` מחוץ לציטוט ⇒ **ירוק**; (ב) `T3` + "**לא** אומת-מקור" ⇒ **אדום** — ההכחשה נדחית גם בסמן החדש; (ג) `T3` + `אומת בעין` ⇒ **ירוק**, ⛔ אין רגרסיה בסמן הישן; (ד) `T3` בלי סמן ⇒ **אדום**. ⚠️ **והמצב הראשון נכשל בניסיון הראשון — והכשל היה ב**פיקסצ'ר** ⛔ לא בשער:** הזרקתי את הסמן **בתוך** ``` `` ``` ⇒ `strip` ניכה אותו כמתוכנן. ⛔ **לא ריככתי את `strip` כדי ש"יעבוד"** — זו בדיוק ההתנהגות שנקנתה ב-`D-030b`. **מדוד:** `CLAUDE.md` 296→**299** שורות (תקרה 300) ⇒ ניסוח החמש נדחס, ⛔ לא סעיפים אחרים. ⚠️ **`T2` ולא `T1`:** הסיווג נקבע בתחילת המשימה תחת **השלוש** (שאלה 2 = "כן"), ו⛔ **רמה אינה יורדת תוך משימה** — תחת החמש אותה משימה יוצאת `T1` (קובץ קוד אחד, `docs` אינם נספרים). ⚠️ **נסגר בקומיט הבא** — כלל `4.1`, פעם חמישית | T2 |
| D-032 | `B-119` — P&L חי מומר ב-**spot**, ואין שער ⇒ **סכום חלקי מוצהר** | 13.08 | `c4e811d` | ✅ **אומת בעין** (`C-023`, 4/4 המצבים ב**חיבור** ⛔ לא ב"נראה סביר"): `$/$` → `$10,124.75`=10,000+100+24.75 · `₪/₪` → `₪10,274.25`=10,000+100×2.0+24.75×**3.0**, **Δ=₪49.50** — בדיוק הפער שנמדד ב-13.08 · `$/₪` → `₪20,274.25` · `₪/$` → `$5,124.75` (ללא שינוי, F2). שלושת אתרי הכותרת (סרגל · כרטיס · פוטר) נושאים **אותו** מספר בכל מצב. **אין spot** ⇒ תג עם **מונה** (`אינו כולל P&L של 1 פוזיציות — אין שער`), כותרת `₪10,100.00` — **סכום חלקי** ⛔ לא `—` ערום, ושתי שורות ה-P&L `—` עם נימוק ב-`title`. **המצב החמישי (C1)**: הכותרת ⛔ **לא** גדלה. ה-PDF: `Excludes 2 open position(s) — 1 without a live price — 1 without an FX rate`. 🔴 **מלכודת F1 הופרכה בדפדפן בשערי פרודקשן** — ה-P&L החי חושב ב-spot `2.9810` (73.78) ⛔ ולא בשער יום-הכניסה `3.0009` (74.27). `test:instrument` **252/252** (165 לפני), **A1–A8 + 7 אסרציות חיווט נצפו אדומות לפני התיקון**; `npm run verify` 23 חוליות + build ירוקים. ⚠️ **שארית מוכרת ⛔ אינה רגרסיה:** ההון ה**סגור** נשאר לא-מומר תחת סמל החשבון כשאין שער ⇒ `B-142`; אסרציה `13.9b` ⛔ לא נגעה — היא קו קפוא עליו. סוגר את `B-119` | T3 |
| D-033 | `B-142` — ההון ה**סגור** נקוב ב-`dispCcy`, ומחובר לא-מומר **מוחרג ונספר** | 15.08 | `9525e6b` | ✅ **אומת בעין** (`C-023`, כל מספר ב**חיבור** ⛔ לא ב"נראה סביר"). **3 מצבי-סירוב + 1 שלא היה בתוכנית:** **S5** (אין טבלה) → `₪10,000.00` = בסיס גולמי ₪10,000 + **0** (AAPL 100$, `no_table`) + **0** (MSFT פתוח, אין מחיר), תג `1/1` פתוחות + `1/1` סגורות · **S7** → `₪20,200.00` = 10,000$×2.0 + 100$×2.0, AAPL בעל `currency:"ILS"` שגוי מוחרג `unverified_instrument`, תג `1/2`, תשואה 200/20,000 = **1.0%** · **S8α** → `₪20,200.00`, MSFT שנסגר 2026-04-15 מוחרג `no_rate_for_day` — **מעבר ל-7 ימי ההליכה-אחורה**, תג `1/2`. 🔴 **S8γ — באג שני שהתכנון ⛔ לא צפה, ונמצא **רק** על המסך:** אין spot + **יש** fixing ליום הבסיס ⇒ ההכרעה **מצליחה** ומחזירה ₪, בעוד `fxOk` שקר ⇒ `dispCcy` נופל ל-$ ⇒ **`$20,100.00`** = 20,000 **שקלים** + 100 **דולר** תחת "$". ⚠️ **וההערה שעמדה שם הצהירה שזה בלתי-אפשרי** ("אחרי השער למעלה זה **תמיד** `dispCcy`") — הוחלפה בתיאור מה שנמדד. ⛔ **⛔ אינו קצה:** זו טבלת ה-`catch` של `loadRateTable` — spot ⛔ לעולם אינו נשמר ל-localStorage (מכוון) ⇒ "יש `byDay`, אין `spot`" הוא ה**ברירה** אחרי נפילת רשת. תוקן ל-`equityBase = fxOk ? equityBaseD.value : capital`, **נצפה אדום** (`instrument 1/330`) ואז ירוק, ואומת שוב על המסך: **`$10,100.00`**. ⚠️ **שלוש הסביבות נבנו בכוונה** — הניסיון הראשון ב-S5 **ריפא את עצמו** ל-`$3,260.50` כי `vite.config.js` מנתב `/api/fx` ל-Frankfurter בפיתוח ⇒ ה-`₪10,000.00` הראשון היה **מצב-טעינה חולף**, ⛔ לא `no_table` אמיתי; הוחלף בפרוקסי ליציאה בלתי-נתיבה (500 מאומת ב-`curl`), ו-S8α בחסימה **סלקטיבית** של `start=` בלבד. `test:instrument` **330/330** (252 לפני); `npm run verify` **23 חוליות + build ירוקים**; `test:registry` **26/26**, 202 מזהים. ⚠️ **הבייסליין של `tradingstats` נלכד מחדש תחת שלוש דרישות ניב** — הוכחת-אבחנה (שיבוש→אדום→החזרה→ירוק), אסרציית **ערך** עם מכנה בטסט עצמו ⛔ לא בבייסליין, ובלוק `BASELINE VERSION` באותו קומיט. ⚠️ **הקו הקפוא `13.9b` הוסר במפורש** — ⛔ לא עודכן כדי לעבור. §10: `B-143` · `B-144` · `B-145`. סוגר את `B-142` | T3 |
| D-034 | 🔴 **לקח `R-3` — אסרציה שמדדה **נוכחות טקסט** במקום **צריכה**, ונענשה על התיעוד שהגל נדרש לכתוב** | 15.08 | `9525e6b` | ✅ **אומת-מקור** (`git show origin/main:scripts/instrument-currency-test.mjs` `:1259-1266` — ⛔ לא מהעץ המקומי). **מה קרה:** הניסוח הראשון היה `!/stats\.currentEquity/` על **הקובץ כולו**, והוא נכשל על **3 שורות הערה** שמסבירות למה השדה נעזב — כלומר על ההערה שדרישת P2 של ניב **חייבה** לכתוב. ⚠️ **הפעם הרביעית שאסרציה נופלת על התיעוד של עצמה** (`B-120`: `\|\| 1` · `accountAmount` · §11.2 של `test:registry` — וההיא הייתה בכיוון המסוכן, שער שמאשר בטעות). **התיקון חידד ⛔ ולא ריכך:** ההערות מנוכות לפני הבדיקה (`replace(/\/\*[\s\S]*?\*\//g,"")` + `replace(/^\s*\/\/.*$/gm,"")`) והנושא נשמר מילה במילה — אף מסלול רינדור ⛔ אינו קורא את השדה. **ונוספו שיניים:** אסרציה משלימה שרואה **צריכה אמיתית** (`stats.closedMetrics`) ⇒ אסרציה שלילית שאינה יכולה לעבור סתם מפני שהביטוי נעלם. ⚠️ **נרשם בשורה נפרדת בדרישת ניב:** *"אסרציה שמדדה נוכחות טקסט במקום צריכה היא R-3 קלאסי, וזה לקח שחוזר"* — ⛔ אינו פרט-לוואי של `B-142` אלא השורש שלו. ⚠️ **`T3` ⛔ ולא `T1`:** בוצע **בתוך** הגל ורמה ⛔ אינה יורדת תוך משימה (§15); ⛔ אין לו מסך ⇒ הסמן הוא `אומת-מקור` | T3 |
| D-035 | `B-145` — **`STATE ⏭️` מצטט את `docs/NEXT.md`, ⛔ ואינו מחזיק רשימה משלו**, ושער `9.6` אוכף **זהות דו-כיוונית** | 15.08 | `a95f0d8` | ✅ **אומת-מקור** (`git show origin/main:scripts/registry-test.mjs` + `origin/main:docs/STATE.md` — ⛔ לא מהעץ המקומי). **מה נמדד:** שתי רשימות "שלושת הבאים" חיו במקביל ב-**0/3 חפיפה** — `STATE ⏭️` נשא `B-007`·`B-009`·`B-006` בעוד `NEXT` נשא `B-001`+`B-002`+`B-018` · `B-129` · `B-143`. **ההכרעה:** מקור-אמת יחיד = `docs/NEXT.md`; `STATE ⏭️` נושא **מזהה בלבד**, וההנמקה/דרך היישום/מדד הסגירה חיים ב-`NEXT` ⛔ ולא בשני עותקים (`R-6`). 🔴 **למה `9.1`–`9.5` ⛔ לא יכלו לתפוס זאת:** חמשתן קוראות את `NEXT` **לבדו** ומאמתות שהוא תקין **בפני עצמו** (תקרה · מניין · הפניות קיימות · מדד סגירה) — **שער שמאמת נוכחות ואינו מאמת הסכמה הוא `R-3`**, והוא ירוק בדיוק ביום שבו שני הקבצים אומרים דברים הפוכים. **`9.6`** משווה את מזהי **כותרות** `NEXT` מול מזהי מקטע `⏭️` ב-`STATE`, **בשני הכיוונים**. ⚠️ **מול הכותרות בלבד ⛔ ולא מול גוף הפריט** — הגוף מצטט מזהי **הקשר** (`B-142`·`B-144`), ודרישה ש-`STATE` ישקף גם אותם הייתה הופכת אותו לעותק שני של הפרוזה, בדיוק ה-`R-6` שהשער נועד למנוע. ⚠️ **ודו-כיוונית בכוונה** — "כל מזהה של `NEXT` מופיע ב-`STATE`" לבדה עוברת גם כשב-`STATE` יושבים שלושה פריטים **נוספים**, כלומר בדיוק המצב שנמדד. **איך נצפתה אדומה — פעמיים, ⛔ ואף פעם על סתירה מסונתזת:** (1) `git stash push -- docs/STATE.md` החזיר את `STATE` ל-`a538a46` תוך שמירת השער ⇒ `❌ 9.6 · 6 מזהים`, `ב-NEXT ⛔ ואינו ב-STATE: B-001 · B-002 · B-018` + `ב-STATE ⛔ ואינו ב-NEXT: B-007 · B-009 · B-006 · B-145`; (2) **מבחן השיניים ל-`R-3` עצמו** — `B-006` נוסף ל-`STATE ⏭️` כך ש**כל 5 מזהי `NEXT` נוכחים ועוד אחד עודף**: שער-נוכחות היה עובר, ו-`9.6` ירתה `ב-STATE ⏭️ ⛔ ואינו ב-NEXT: B-006`. ואז ירוקה: `5 מזהים משני הצדדים`. ⚠️ **`B-007`/`B-009`/`B-006` ⛔ לא נמחקו** — נמדדו **3/3 פתוחים** ב-`BACKLOG` לפני הציטוט ⇒ המעבר ⛔ אינו מאבד פריט. `test:registry` **27/27** (26 לפני), **204 מזהים**; `npm run verify` **23 חוליות + build ירוקים**. **אימות פרודקשן באותו גל:** הנכס החי `/assets/index-BvcR2Rtx.js` (1,638,589 בתים, HTTP 200) נושא `fxUnconverted` **3×** בשלושת הצרכנים המיועדים — הסימון ב-`makeConvertingCalc` · הדילוג בעקומת ההון · הקיפול של ה-P&L הסגור עם המונה. ⚠️ **`T2` ⛔ ולא `T3`:** רוחב-קוד קובץ אחד (`scripts/`), ⛔ אפס נגיעה במסלול משתמש · ⛔ אין מסך ⇒ הסמן `אומת-מקור`. סוגר את `B-145` | T2 |
| D-036 | **גל השומרים** — `B-001` + `B-018` + הצלע הנמדדת של `B-002`: השומר מודד **כשל בפועל**, ⛔ לא נוכחות | 15.08 | `8fb8b11` · `4b8f5a7` · `f22800f` · `d46baf1` | ✅ **אומת בקנרית חיה** (`🐤 Canary Drill`, `workflow_dispatch` בלבד — `grep -cE '^\s*schedule:'` = **0** ⇒ ⛔ אפס workflow שמייצר כשל אוטומטית). ✅ **ואומת בעין 15.08 — ניב צילם את ערוץ הדיסקורד** (`C-026`). 🔴 **שתי שכבות, ⛔ לא אחת — ושתיהן נרשמות:** **(1) מכנית** — `discord POST -> HTTP 204` · `channels delivered: 2/2` · נושא המייל `🚨 🐤 Canary Drill — התראת-בדיקה נכשל`. ⚠️ **כל השלוש מודדות את ה**משגר**:** 204 הוא מה ש-Discord **קיבל**, ⛔ לא מה שניב **ראה** — ומדידת-משגר לבדה היא בדיוק ‹R-4›, השורש שהגל הזה נועד לסגור. **(2) ✅ בעין** — **5/5** הודעות בערוץ, כל אחת עם 🐤 + שם ה-workflow + URL. זמנים (מקומי UTC+3): **16:24:59 · 16:29:05 · 16:37:30 · 16:40:32 · 16:51:56**. **T-A — חמש מסירות, ⛔ לא ארבע:** `31887147462`(`8fb8b11`) · `31887326697`(`4b8f5a7`) · `31887705569`(`f22800f`) · `31887840180`(`d46baf1`) · והחוזרת אחרי הבלוק האחרון `31888353149`(`1991f83`), שנורתה מ-canary **`31888345445`**. ⚠️ **הרישום הקודם "4/4" היה מכנה שגוי ⛔ ולא מספר שגוי** — הוא ספר ריצות תחת `Delivery gate`, והראשונה (16:24:59) קדמה ללידתו (`4b8f5a7`, 16:28:46) ⇒ **4/4 עם שער · 5/5 בערוץ**, ושני המספרים נכונים תחת אוכלוסייה מוצהרת (§2). **התג הגיע לנושא המייל עצמו** ⛔ בלי לכופף את מסלול המייל לקנרית. ⚠️ **הקנרית התאימה את עצמה למסלול ⛔ ולא להפך** — ⛔ אפס שינוי במסלול המייל כדי לשאת את `🐤`. **T-B — בקרת-שלילה, ⛔ לא רשות (D2):** קנרית ב-`mode=pass` ⇒ `success`, ושלוש ריצות `failure-alert` (`31888391814`·`31888389459`·`31888376326`) **כולן `skipped` עם `steps: 0`** ⇒ ⛔ אפס מסירה על ירוק. **`B-001` — הצופה שלא היה:** `failure-alert` לא היה ברשימת הצפייה של עצמו ו-`watchdog` דילג עליו **בהגדרה** (`workflow_run` בלי `schedule:`). נסגר במתאם ההתראות ב-`fleet-daily` — **מכנה של כשלים אמיתיים ⛔ לא של זמן**, ולכן ⛔ אינו יכול להתריע-שווא בשבוע שקט. נצפו **ארבעת הענפים** לפני הקומיט: ירוק `2/2` · אדום `0/2` עם שמות ו-URLs (נכפה ב-`WINDOW_MIN=0`) · `bail` (404 אמיתי — `nivhareven` מול `niveven183`) שהוציא **embed אדום ⛔ ולא היעלמות** · ומכנה ריק (נכפה ב-lookback `0 hours ago`). **`B-002` — שלושה מצבי כשל-שקט נסגרו:** `curl -sS` בלי `--fail` יוצא `0` על 4xx/5xx ⇒ הוחלף בהשוואה מפורשת ל-**204**; `continue-on-error` שהשתיק שער; ו-`delivered` שנכתב פעם **אחת** בלבד ל-`$GITHUB_OUTPUT` ⇒ ⛔ אין last-write-wins. הריצה נכשלת **רק** אם ⛔ אף ערוץ לא מסר. **`B-018`:** `Restore Drill` נכנס לרשימת הצפייה (12→**13**), ושתי קריאות ה-Discord שלו הוחלפו בהשוואה מפורשת (`::warning::` ⛔ ולא `exit 1` — שם Discord משני). **תקרות זמן נגזרו ⛔ ולא נוחשו:** `restore-drill` **4/4** ריצות מקסימום `1m22s` ⇒ `timeout-minutes: 20` (ברירת המחדל 360 = **263×** הנצפה); `failure-alert` **9/9** מקסימום `30s` ⇒ `5`. ⚠️ **הערת ה-`S8γ` הישנה הוחלפה** — היא הצהירה ש-`Restore Drill` "כבר מתריע על תנאי הכשל של עצמו", כלומר הכריזה מצב כבלתי-אפשרי בעודו אפשרי; מייל כפול אחד התקבל כמחיר חסום ומוצהר. **כיסוי נמדד:** 18 קבצי workflow — `failure-alert` 13 + `Triage` 3 = **16/18**; השניים שלא: `failure-alert` עצמו (מכוסה במתאם, כי ריצת-התראה שנכשלה ⛔ אינה כיסוי) ו-`health.yml` (`workflow_dispatch` בלבד ⇒ שהות = "אף אחד לא נזקק", ⛔ לא "מת" — **תועד בקובץ עצמו** ב-`1991f83`). 🔴 **בלוק 4 ⛔ לא בוצע — המדידה הפריכה את הכרעה (ב):** ראה `D-037`. ⇒ צלע הספים של `B-002` ⛔ אינה נסגרת כאן ועברה ל-`B-148`. `npm run verify` **23 חוליות + build ירוקים**; `test:registry` **27/27**, 209 מזהים. **`RUNBOOK` § 🐤 תרגיל הקנרית** כולל `### 🔴 מה עושים אם T-A ⛔ לא הגיעה`; `C-026` נולד ו**נסגר בשתי השכבות**. §10: `B-146`·`B-147`·`B-148`·`B-149` | T3 |
| D-037 | 🔴 **הפרכת M1 — בלוק 4 **נעצר**, ⛔ ואפס ספי `Watchdog` שונו** | 15.08 | `1b4d3ad` (`docs/audits/AUDIT-guardians-M0.md`) | ✅ **אומת-מקור** (`git show origin/main:docs/audits/AUDIT-guardians-M0.md` §M1 + `origin/main:.github/workflows/watchdog.yml` — ⛔ לא מהעץ המקומי; `git diff d46baf1 -- .github/workflows/watchdog.yml` = **ריק**). **מה הופרך:** הכרעה (ב) שאושרה כללה `Sentinel: 3h → 26h`. **המדידה:** בודק יומני ב-08:00 יכול לצפות רק **גילאים בדידים** — `(8 − cron_hour) mod 24 + 24k` — ולכן זמן הגילוי נקבע ע"י **הדלי הראשון שמעל הסף** והוא **עולה מונוטונית ב-`MAXAGE`**. ⇒ `Sentinel=3` יורה אחרי **24.17ש'**, ו-`Sentinel=26` היה יורה אחרי **48.17ש'**: השינוי המאושר היה **מוסיף 24ש' עיוורון** לשורה ה**יחידה** שכבר אופטימלית. **האוכלוסייה המלאה:** **8/11** שורות מבזבזות בדיוק 24ש' · `restore-drill` **96ש'** · **2/11** (`user-analytics` 26.47ש' מול דלי 25.5 · `fleet-daily` חלון בטוח 0.59ש') ⛔ אינן ניתנות להידוק כלל. ⛔ **ולא נכתב נימוק ל-26** — זו הייתה בדיוק הרציונליזציה שהכרעה (ג) אסרה ("מספר בלי נימוק הוא חוב" ⇒ נימוק שנתפר למספר שגוי גרוע ממנו). ⛔ **והאסרציה המכנית שהוצעה (`MAXAGE ≥ מחזור×1.1`) נפסלה** — היא הייתה **מחייבת** את הרגרסיה ומקבעת אותה בשער. **הכלל הנכון הוא ההפוך:** **מזעור** `MAXAGE` בכפוף לחריגה מהפער הנצפה, ⛔ לא רצפה הנגזרת ממחזור הבודק. ⚠️ **הרישום הזה ⛔ אינו "משימה שנדחתה" אלא ממצא** — הוא סוגר את חובת §10.1 על החלטה שהתגלתה תוך כדי; **הביצוע** חסום על הכרעת ניב ב-`B-148`, והוא ⏭️ **#1 ב-`docs/NEXT.md`**. ⚠️ **`T3` ⛔ ולא `T1`:** נמדד **בתוך** הגל ורמה ⛔ אינה יורדת תוך משימה (§15); ⛔ אין מסך ⇒ הסמן `אומת-מקור` | T3 |
| D-038 | `B-146` — **עוגן יציב לטבלת היומן**, ואסרציה שעברה בשקט כשהיה עליה ליפול | 15.08 | `1991f83` | ✅ **אומת-מקור** — ⛔ **אין מסך מושפע:** `data-testid` הוא תכונה בלתי-נראית למשתמש, ⇒ האימות הוא **קריאת התוצר החי** (⛔ לא מהעץ המקומי) + **מדידה חוזרת עצמאית**. **מה נמדד:** `table.w-full.text-xs` תואם **3** טבלאות ב-`SwingEdge_App.jsx` ועוד **3** ב-`AdminPanel.jsx` ⇒ strict-mode violation ברגע ששתיים גלויות. 🔴 **והשורה השנייה הייתה גרועה יותר:** `:204` משווה `rows.count()` כ-`n < FIXED_TRADES.length` ⇒ טבלה תואמת נוספת **מנפחת את המונה** והאסרציה **עוברת** במקום להיכשל — **מעבר שקט**, ⛔ לא נפילה רועשת. ⚠️ **לכן שונתה גם `:38` וגם `:204`** — התוכנית המוקדמת שלי הותירה את `:38` ותוקנה אחרי המדידה. **התיקון:** `data-testid="journal-table"` יחיד, והספֶּק מצביע עליו דרך `JOURNAL_TABLE`/`ROWS`. **אימות פרודקשן:** `/assets/index-B9QP4642.js` (HTTP **200**, **1,638,619** בתים) נושא `data-testid":"journal-table"` — **1/1 מופעים**, שזו בדיוק הטענה (עוגן שאינו יחיד אינו עוגן). **וריצת Sentinel חיה מול פרודקשן** (`31888449189` @ `1991f83`): `3 passed (13.4s)` · `sentinel auth findings: 0` · `✓ 1 [chromium] › tests-sentinel/sentinel-auth.spec.js:301:1 › authenticated journey: login → journal → create/delete SNTNL (6.3s)`. ⚠️ **ואומת ש**רץ** ⛔ ולא שדולג** — `minute=54 event=workflow_dispatch auth=1`; שכבת-האימות ב-Sentinel מותנית בדקה (`≥45` או `≤9`) ⇒ "ירוק" בלי השורה הזו היה בדיקה **לא-חד-משמעית** שמתחזה לתוצאה. ⚠️ **המיקום מזוהה ב**עוגן** ⛔ ולא במספר שורה:** ה-INBOX רשם `:4377·:4557·:4884` ובעת התיקון הם היו `:4473·:4653·:4980` — **96 שורות סחיפה ביממה**. **`B-147` הונפק ב-`BACKLOG` לפני התיקון** (דרישה D4) ונשאר במכוון **לא-מתוקן**: הסוכן שממציא סיבה מעל המדידה הוא ‹R-2› והוא גל נפרד | T3 |
| D-039 | `B-149` — **§11.2 קורא שני קבצים: אזכור מזהה-בדיקה ⛔ אינו ביצועה**, ושני ענפי-האישור-בטעות נסגרו | 15.08 | `f7926fd` | ✅ **אומת-מקור** — ⛔ **אין מסך מושפע:** שער CI פנימי. ⇒ האימות הוא **clone נקי מ-`origin`** (⛔ לא מהעץ המקומי) + **מדידה חוזרת עצמאית** מעל אותו עותק. **מה נמדד ב-clone** (`HEAD = f7926fd8dfcb…`): השער רץ שם **29/29** · `C-023`=**5 תאים** · `C-026`=**5 תאים** · **23/26** בדיקות מסווגות כ**בוצעו**, והשלוש שאינן הן `C-021` · `C-024` · `C-016` · **6/6 שורות `T3` לא-רטרו עוברות** (3 על `אומת בעין`, 3 על `אומת-מקור`). 🔴 **ארבע ראיות נצפו אדומות על השורה ה-אמיתית, ⛔ לא על fixture מסונתז:** (1) **שיניים בשני הכיוונים** — `git show cd65365:docs/DONE.md` ⇒ `D-036` **נופלת** «אזכור בלבד — `C-026`=הוכחשה בשורה», ואותה שורה ב-`HEAD` **עוברת** «סמן מפורש: אומת בעין». **נזק צדדי: 1/6 בעבר ⇒ 0/6 בהווה.** (2) **שלוש צורות ההכחשה** (`D1`) — `⛔ לא בוצעה` · `⛔ טרם בוצעה` · `⛔ פתוחה` — כל אחת אדומה **בנפרד**, ולכל אחת נמדדה גם הצורה **בלי** ההכחשה, שעוברת ⇒ הנפילה ⛔ אינה צורנית. (3) **הענף שירה** — נוסח ההמתנה של `D-036` עבר בשער ה**ישן** ונופל בחדש: «⛔ אין סמן אימות כלל». (4) **הכחשה רחוקה** (400+ תווים) ⛔ **אינה** פוסלת סמן תקין. 🔴 **ההפרכה — וההנחה השגויה הייתה שלי:** ההנחה בפרומפט **ובדיווח הקודם שלי** הייתה שענף `C-0\d\d` הוא שאישר את `D-036`. **המדידה הפריכה זאת:** `strip` מנכה גרשיים אחוריים, וכל מזהי הבדיקה במרשם כתובים בגרשיים ⇒ **הענף היה מת ולא ירה מעולם**. הענף ש**ירה בפועל** הוא זה של תמונת-המסך — שם-עצם שמופיע **זהה** בהצהרת קיום ובהצהרת היעדר — והוא התאים למשפט שמצהיר שהראיה **עדיין אצל ניב** ⇒ אסרציה שנופלת על התיעוד של **היעדרה**: ‹R-3› בכיוון ה**מאשר**, פעם **רביעית** (`B-120`). ⇒ הענף **הוסר** מרשימת הסמנים. **שלושה לקחים, כל אחד מדוד:** ⓵ **`⛔` ⛔ אינו מילת הכחשה** — הוא סימן **הדגשה** בכל תא במרשם. אב-טיפוס שכלל אותו באוצר-המילים הפיל **6/6** שורות תקינות ⇒ נזנח ותועד בתוכנית כדי שלא ינוסה שוב. ⓶ **קרבה היא חובה, ⛔ לא עידון** — `בלוק 4 ⛔ לא בוצע` יושב **2,165 תווים** מהסמן של `D-036` ועוסק ב**בלוק עבודה**. כלל שקורא את התא כולו היה מכריח **לרכך את `DONE`** כדי לעבור — בדיוק האסור ⇒ חלון **±90** תווים. ⓷ **«בוצעה» ⛔ אינה `✅`** — רק **3/26** שורות `CHECKS` נושאות `✅`, בעוד `C-006` · `C-011` · `C-017` · `C-018` רצו ומצאו 🔴. בדיקה שרצה ומצאה תקלה **בוצעה** ⇒ ההגדרה היא **תאריך אמיתי + תוצאה שאינה ⏳** + כל מצביע `D-` שבתוצאה **קיים** ב-`DONE`. ⚠️ **ו-`11.3` תפסה באג במסווג של עצמי בריצה הראשונה** (`10/11`): מזהי ה-fixture שבחרתי ⛔ לא התאימו לרג׳קס `/C-0\d\d/` ⇒ **תוקנו ה-fixtures, ⛔ לא הרג׳קס** — ריכוך שם היה מוחק בדיוק את השן שנוספה. **המבנה:** `CHECKS` נורמל ל**מינימום שהכלל מחייב** ⛔ ולא מעבר — `C-026` **6⇒5** תאים (תא תדירות כפול שנקרא מיקומית כתוצאה) · `C-023` **4⇒5** עם **מצביע** ל-`D-032`/`D-033`, ⛔ ולא העתקת תוצאה ‹R-6›. **אסרציות 27⇒29:** `5.2` — כל שורת `CHECKS` נושאת 5 תאים (חריגת מבנה עתידית הופכת בדיקה שרצה ל«שלא רצה» **בשקט**) · `11.3` — self-test **קבוע** מול **11** fixtures **הרמטיים** ש⛔ אינם קוראים `DONE` חי, ⇒ עריכת שורה ⛔ אינה יכולה לשנות את השער שבודק את עצמו. ⛔ **אפס נגיעה ב-`docs/DONE.md` בגל עצמו** ⛔ ואפס ריכוך — השורה הזו נכתבת ב**קומיט שאחריו** (כלל 4.1) | T3 |
| D-040 | `B-129` — **סולם הראיות מעיד על עצמו: `currency_source` · `source` · `import_batch_id` · `inserted_at`** ‹סוגר גם `B-152` (א) · `B-153` (א) · `B-071`› | 16.08 | `60e57c6` | ✅ **אומת בעין** — ניב הריץ את שני מסלולי הכתיבה בפרודקשן וקרא את התוצאה מה-DB. **4/4 שורות הייבוא:** `ZZTEST`+`ZZTESTB` → `broker_arithmetic` (דרגה 1, מסלול הברוקר) · `ZZTESTC`+`ZZTESTD` → `file_cell` (דרגה 2, המסלול הגנרי) · **2 `import_batch_id` נבדלים** (`f899706f…` ×2 · `d2cc0a39…` ×2) ⇒ המזהה מסמן **מנה**, ⛔ לא שורה · `inserted_at` מלאה ב-4/4. **הידנית:** `manual` · `manual_capital` · `ILS`. **⇒ ארבעה ערכי תעודה נבדלים בפרודקשן** — `broker_arithmetic` · `file_cell` · `manual_capital` · `null` ⇒ **`V5` מוכחת על נתון אמיתי: אותה תווית `USD` בדיוק, מקורות שונים, ונבדלות אחרי הכתיבה.** 4 שורות הבדיקה נמחקו ב-`DELETE` ממוקד לפי ארבעת הטיקרים. **שערים:** `verify` 23 חוליות + build · `test:import` 33 תרחישים · `test:instrument` 359/359 · `test:write` 29/29 · `test:registry` 29/29. **נצפה אדום לפני ירוק — 15 אסרציות**, בהן `V5` (`expected true\|false, got false\|false`) ו-`V5⁻¹` ההפוכה; ⚠️ `V5⁻¹` על **דרגה 3** נשארה **ירוקה** במצב הישן, וזו הסיבה שהיא קיימת: **סירוב גורף הוא כישלון, ⛔ לא הצלחה**. **יצחק נמדד ⛔ ולא הונח:** 2 מסלולי ייבוא × פיקסצ'רים קיימים ⇒ **0 שורות הוסרו**, 430=215×2 ו-32=16×2 נוספו, ומנוקות משני השדות החדשים התוצאות **byte-identical** (`diff` יצא 0). 🔴 **הגל מונע הבאות ⛔ ואינו מתקן קיימות** — **67/67** השורות שקדמו לו נושאות `currency_source = null` · `source = null` · `import_batch_id = null` · `inserted_at = null`, והצעד הכפול (`add column` בלי default → `set default now()`) **נמנע במכוון** מלהטביע 67 חותמות מסונתזות ‹§2›. **13 שורות ת"א באגורות ⛔ לא נגעו** — הן `B-005`/`B-121`, חסומות על הודעה מוקדמת למשתמש. ⚠️ **והכי חשוב — הפרכה של הנחה שהייתה שלי:** באימות העין הועלה חשד שהגל **יצר** תיוג `ILS` שגוי חדש. נמדד ב-`git`: `currency: capitalCurrency` נולד ב-**`5977ba5`, 02.08** — **14 יום לפני הגל** — וה-`diff 5c93ab3..60e57c6` מראה **0 שורות שהוסרו** באזור המטבע. ⇒ הגל **הצהיר** על התיוג הקיים כהנחה (`manual_capital`, דרגה 5, `isEvidenceBacked = false`) ו⛔ לא יצר אותו. **החשד נולד מצילום בלי `git log`** — בדיוק מה ש-§8.1 מונע, ו-`0/67 ILS` באבחון מדד **אוכלוסייה** ⛔ ולא התנהגות ‹§2›. ⚠️ **וגם הכיוון שהוצע כתיקון הופרך:** `deriveInstrumentCurrency` מחזיר `USD`/`assumed` לכל טיקר **אלפביתי** — כולל נייר ת"א — ו⛔ **אינו יכול להחזיר `ILS` לעולם** ⇒ גזירה מהנייר הייתה מחליפה הנחה בהנחה. נרשם ב-`B-154`. ⚠️ **מה ⛔ לא נסגר:** `B-042` (העמודה קיימת, **0 צרכנים**) · `B-080` (חוזה ה-Vision חסום על `B-110` ❄️; רק קדם-המילוי הוצהר, `C-027`) ⇒ `B-154` · `B-155` | T3 |
| D-041 | `B-009` — **המוצר מפסיק להמציא מספרים על יומן ריק** ‹סוגר גם `B-064` (`EMPTY_STATS` נשאר `0`) · `B-065` (צורת `lastWeekStats`) · `B-157` (השער והמספרים קראו שתי אוכלוסיות)› | 16.08 | `eb8758b` | ✅ **אומת בעין** — נצפה על `/app` בבנייה של `eb8758b`, **שלושת החצאים כל אחד לפני ואחרי** (הקבצים הוחזרו ל-`19d1dd0` ובחזרה; `git status` נקי בשני הקצוות). **(א) יומן ריק:** `אחוז הצלחה` **`0%` ⇒ `"—"`** · ה-`▲ 0.00%` הירוק על ה-P&L הסגור **נעלם** (`trend != null`, ⛔ לא `!== undefined` — `null >= 0` הוא `true`) · סרגל הסטטיסטיקה ⛔ **אינו מרונדר כלל** · **0 שגיאות קונסולה**. ⚠️ **וה-`$0` ששרד ⛔ הוא ממצא, לא כשל:** `רווח/הפסד נקי +$0.00` על `0 עסקאות סגורות` הוא **סכום מעל קבוצה ריקה** ⇒ `0` הוא **איבר היחידה** ⇒ מבחן 2 של `PLAN-B009.md` §1.1 מכשיר אותו. וכן `▲ 0.0%` על **הון החשבון**, שהופרך כשייך לגל: הוא `curEquityReturnPct` (`:2467`) — `(2500−2500)/2500` — **מדידה** מהון אמיתי ⛔ ולא `EMPTY_STATS`. **(ב) יומן מלא — 4 סגורות שנזרעו לחישוב-ביד:** `+200` · `−200` · `+100` · `−120`. **7/7 ערכי הסרגל חושבו ביד והתאימו:** `4` · `50%` (2/4) · `+$150.00` ((200+100)/2) · `-$160.00` ((200+120)/2) · `0.94` (300/320=0.9375) · `-$220.00` (שיא +200 ⇐ שפל −20) · `2.8d` ((3+3+3+2)/4=2.75). וכן 4 KPI: `-$20.00` · `$2,480.00` · `-0.80%` · `-0.05R` ((2−2+1−1.2)/4). 🔴 **ו-0 מספרים אמיתיים הפכו ל-`"—"`** — 4 המופעים של `—` בעמוד נמדדו **כולם** כמקף בפרוזה ⛔ ולא כמציין-ערך ⇒ **סירוב גורף ⛔ לא קרה**. **(ג) `B-157` — הראיה שהבאג היה על המסך:** פילטר טיקר `ZZZZ` על אותו יומן של **4 סגורות אמיתיות**; **לפני** — `סה״כ סגורות 0` · `אחוז הצלחה 0%` · `רווח ממוצע +$0.00` · `הפסד ממוצע -$0.00` · `PROFIT FACTOR 0.00` · `ירידה מקס' -$0.00` · `זמן החזקה 0.0d`, **שבעה תאים מומצאים ישירות מעל "אין עסקה שתואמת"**; **אחרי** — הסרגל **נעלם**. ⚠️ ⇒ זהו המסלול ה**יחיד** שבו משתמש **פעיל** ראה `EMPTY_STATS`, ולכן הוא ⛔ לא נמצא כל עוד חיפשו "יומן ריק". **הכלל שנקנה — שלושה מבחנים בסדר** (`PLAN-B009.md` §1.1, ⛔ החלטה כללית ולא נקודתית): **טווח** (האם המסלול החי יכול לייצר את הערך? `bestWin: 0` נופל — זכייה היא `pnl>0` **בהגדרה**) → **איבר-יחידה** (האם `0` הוא הזהות של הצבירה מעל הריק? סכומים ומונים כן, **מנות וקיצונים לא**) → **התנגשות** (האם `0` נוצר גם בתנאי אחר **בעל משמעות**? רק `returnPct` נופל שם). ⇒ **מנה או קיצון ⇒ `null` · מונה או סכום ⇒ `0`**. **שערים:** `verify` 23 חוליות + build (`coach` 111 · `instrument` 359/359 · `horizon` 140/140 · `syntax` 4/4 · `registry` 29/29 · `vite build ✓ 7.37s`). **נצפה אדום לפני ירוק:** 3 אסרציות ערך. **והבייסליין הוקפא מחדש (v4⇒v5) עם הוכחת-אפליה דו-כיוונית** ⛔ ולא כהתאמת-ציפייה: `winRate: 0` ⇒ **5 אדומות** כולל `✗ empty — DIVERGED`; **ובנוסף** `planAdherence: 0` — שדה ש**בלוק 9b ⛔ אינו מקבע** ⇒ **2 אדומות** כולל אותה סטייה ⇒ האדום מיוחס ל**בייסליין עצמו** ⛔ ולא לאסרציות הערך. שניהם הוחזרו ⇒ ירוק. **16 ערכים זזו, 2 נוספו, 0 הוסרו — כולם בתרחיש `empty` בלבד**, כפי שנחזה **לפני** ההרצה. ⚠️ **ותיקון לדיווח שלי עצמו, שנמדד 16.08 בבירור נפרד ⛔ ולא הונח:** `isEmpty` **כן** נצרך בקוד המוצר — `SwingEdge_App.jsx:4824`, שער `B-157` (הגרפ שדיווח «0 צרכנים» סרק `src/`, והקובץ יושב ב**שורש**). 🔴 **אבל הטענה ש-`tradingstats-test.mjs:99` «הפסיק לדלג» הופרכה:** `ratesSumTo100` נקרא מ-**5 אתרים · 0 מהם עם אובייקט ריק** ⇒ הענף החדש (`:126`) **⛔ אינו רץ** — ו-`if (s.isEmpty) return` הישן ⛔ לא רץ גם הוא. ⇒ **`0%` שרד כל ריצה ירוקה ⛔ לא בגלל דילוג, אלא משום שבלוק 1 קיבע `winRate: 0` כ*חוזה*** (`19d1dd0:127`) — ‹R-3› במקום אחר מזה שנרשם. ⇒ `B-160`. 🔴 **מה ⛔ לא נסגר, ובמכוון:** `B-156` — `50` ב-`TradeDNA`/`GrowthTracker` (**10 אתרי ייצור · 9 שדות · 6 צרכנים אריתמטיים**) ⇒ `null` שם היה `NaN` על המסך, וה**כיוון ⛔ לא הוכרע** (ייתכן **גדר תצוגה**) ⇒ עלה ל-`NEXT` §1. **וסריקה סביבתית §11 הוציאה שניים חדשים, שנמדדו ⛔ ולא הונחו:** `B-158` — אותו באג דרגה אחת למטה (5 שדות נופלים ל-`0` על תת-קבוצה ריקה; נמדד על יומן **2/2 מרוויחות** ⇒ `isEmpty=false` · `losses=0` ⇒ התא מדפיס **`-$0`**) ⇒ **וזו גם ההוכחה ש-4 הגדרים שנוספו כאן ⛔ אינם קוד מת** · `B-159` — הבייסליין **עיוור** ל-`avgHold`: `holdDays` (`utils.js:48`) דורש `closedAt` והפיקסצ'רים נושאים `exitDate` ⇒ **7/7 התרחישים הלא-ריקים מקבעים `0`** ⇒ **קיפאון של השער ⛔ ולא באג במוצר** ‹R-3› | T3 |
| D-042 | `B-160` — **`isEmpty` הוסר, ⛔ לא שונה-שם: `isEmpty ⟺ total === 0` בדיוק, והשער קורא עכשיו את אותו ביטוי שהתא מתחתיו מדפיס** ‹`openTrades` פוצל ל-`B-161`, ⛔ אינו בגל הזה› | 16.08 | `1fc6170` | ✅ **אומת בעין** — שני מצבי שער `B-157` נצפו **לפני וגם אחרי** השינוי: פילטר שאינו תואם אף עסקה סגורה ⇒ הלוח **נעדר**; יומן אמיתי ⇒ הלוח **נוכח**, והתא הראשון (`total`) חושב ביד ותאם. **הטענה שכתבתי ב-`D-041` הופרכה במדידה נפרדת** (`docs/audits/AUDIT-B160.md`): ⓵ **טאוטולוגיה** — `tradingStats.js:50` הוא `if (closed.length===0) return EMPTY_STATS(capital)` ⇒ `isEmpty === (total===0)` נמדד **4/4** מצבים, כולל 0 סגורות·**5 פתוחות**; הדגל נכתב כ**ליטרל ביד** בשני ענפים (`:276`·`:330`) ⛔ ולא נגזר. ⓶ **הענף מת** — `ratesSumTo100` נקרא מ-**5 אתרים · 0 עם אובייקט ריק** ⇒ `:126` **⛔ מעולם לא ירה**; `0%` שרד לא בגלל דילוג אלא כי בלוק 1 קיבע `winRate:0` **כחוזה**. ⓷ **השם שגוי** — יומן עם 5 פתוחות/0 סגורות קיבל `isEmpty:true`. **הביצוע:** השער ב-`SwingEdge_App.jsx:4824` → `journalStats.total > 0`; `:126` **נמחק** (⛔ לא שומר בשם אחר — `R-6`), האסרציה הייחודית (`wins+losses+be===0`) עברה לבלוק 9b **חי**; `:158`/`:536` נכתבו מחדש על `total`. **תחזית מחייבת נרשמה לפני הריצה** (16 מחיקות/8 הוספות) — הבייסליין נקפא מחדש `v5→v6` וה-`git diff` תאם **בדיוק**. **הוכחת-אפליה דו-כיוונית, שני הפלטים הודבקו בדיווח:** מוטציה ב-`winRate` (מסלול חי) הפילה **גם** אסרציות **וגם** בייסליין; מוטציה ב-`bestStreak:251` (`maxWinStreak → maxWinStreak+1`, נבחר במדידה — `grep -c bestStreak` על קובץ הבדיקה = 0, ⛔ לא ברשימת `SCALARS`) הפילה **רק** את הבייסליין ⇒ לבייסליין כוח אבחנה **עצמאי**. `C-028` ירה שוב (תנאי-הגלישה: כל נגיעה בלוח הסטטיסטיקה) ואומת בעין בשני המצבים. **שערים:** `npm run verify` **23 חוליות + build ירוקים** (פלט מלא הודבק בדיווח) · `test:tradingstats` — כל 8 התרחישים deep-equal לבייסליין הקפוא · `test:registry` 29/29 (אחרי הקומיט הבא). ⚠️ **`openTrades` — חפיפה עם `isEmpty` נמדדה אפס** (`tradingStats.js:50`/`:301` משליכים את מערך העסקאות ⇒ `stats.openTrades=0` גם על יומן עם פתוחות; המוצר מחשב `openTrades` בעצמו ב-`SwingEdge_App.jsx:2206` ואינו קורא את השדה — דפוס `B-154`) ⇒ **מזהה נפרד `B-161`**, ⛔ לא שוכפל כאן (`R-6`) | T3 |
| D-043 | `B-143` — **הסכום, ⛔ לא הסמל: ההפרדה נעשית לפי **סוג המטריקה** ו⛔ לא לפי השדה** ‹`B-144` **יצא** מההיקף במדידה, ⛔ לא בשיקול› | 17.08 | `e5afdc7` | ✅ **אומת בעין ב-PDF, כולל ה**ציר** — `C-029`.** לפני/אחרי מאותו `exportMonthlyPDF` בדיוק (המצב הישן שוחזר ע"י עטיפת ה-`calc` כך שתפשיט `fxUnconverted`, ⛔ לא ע"י כתיבת קוד ישן מחדש). **לפני:** `₪9,900.00` · `−₪100.00` · צניחת-V עמוקה · ⛔ **אפס הצהרה**. **אחרי:** `₪10,100.00` · `+₪100.00` · ⚠️ *«Excludes 2 trade(s) from the totals and the equity curve — 2 closed without an FX rate to ILS»*. 🔴 **ו-`CLOSED TRADES 4` + `WIN RATE 50%` ⛔ לא זזו** — פיצול סוגי-המטריקה נראה בפריים אחד. 🔴 **והציר היה מעוות ⛔ ולא רק המספר:** שני מסלולי ה-SVG חושבו **ביד** והתאימו תו-בתו — *אחרי* 10,000→10,200→10,100, `max=10,200` `min=10,000`, `toY=150−(eq−10000)·0.75` ⇒ `M0.0,150.0 L300.0,0.0 L600.0,75.0`; *לפני* 10,000→9,700→9,800→10,000→9,900, `min=`**`9,700`**, `toY=150−(eq−9700)·0.5` ⇒ `M0.0,0.0 L150.0,150.0 L300.0,100.0 L450.0,0.0 L600.0,50.0` ⇒ **הרצפה הישנה הייתה ארטיפקט** (דולרים שהוחסרו מיתרה שקלית), ⛔ לא ירידת הון. ⚠️ **ומה נותר בלתי-נצפה, מוצהר ⛔ ולא מוחלק:** **מסך האפליקציה החי ⛔ נותר ללא פתיחה בדפדפן** — הוא נשען על **מקור + אסרציות** בלבד; ⇒ `C-023` נותרה על מצבה מ-15.08. **ההצהרה המלאה יושבת בסעיף הייעודי בתחתית הקובץ, שורת `D-043`** — ⛔ לא בתא הזה, כי תא ראיה שנושא גם טענה וגם הכחשתה ⛔ אינו קריא לשער ‹ראה `B-162`›. 🔴 **המדידה הפכה את הפריט:** הכותרת ⛔ **אינה** «28 אתרי `statsCcy`» אלא **7 שדות כסף** שנבנו כ-`reduce` **חוצה-יחידות** מתחת לסמל ⇒ יישור-הסמל לבדו היה משאיר את **כל** הפגם ומוחק את הרמז היחיד אליו (`DECISIONS` Q1). **ההכרעה: הפרדה לפי סוג המטריקה** — **ספירות · שיעורים · `R` שומרים על האוכלוסייה המלאה** (סימנו של P&L ⛔ אינו תלוי-מטבע ⇒ `winRate` על תת-קבוצה היה **מקלקל מספר תקין**), ו**סכומי כסף בלבד** צרים לתת-הקבוצה הניתנת להמרה — **כל אחד עם המכנה שלו** (`fxUnconvertedCount`), כי מונה מסונן מול מכנה לא-מסונן הוא `avgWin` **נמוך מדי בשקט**, שגרוע מהמצב שתוקן. **C8 היה מחיקה — נמדד ⛔ ולא הונח:** `menteeStats` מחושב באותו `calc` ממוקד-`dispCcy` ⇒ 28 האתרים הוסבו ו**ההגדרה של `statsCcy` נמחקה**, ⛔ לא נשמרה בשם אחר (`R-6`). **נצפה אדום לפני ירוק** — כולל מקרה ה**סירוב החלקי** (`fxOk === true` + `no_rate_for_day`) ש**הפריך את האבחון**. 🔴 **והאסרציה ההפוכה היא החצי השני, ⛔ לא נספח:** יומן חד-מטבעי ⇒ ⛔ **אף שדה לא הפך ל-«—» ואף ממוצע לא ירד** — **סירוב גורף הוא כישלון, ⛔ לא זהירות**; הצורה המכנית שלו היא **84/84** שדות `fxUnconvertedCount` שנולדו וקוראים `0`. **בייסליין `v6 → v7` תחת שלוש הדרישות:** התחזית נכתבה **לפני** הלכידה (תוספות בלבד · 0 ערכים שזזו), והנמדד היה **132 `+` מול 40 `-`** ⇒ **סתירה לכאורה**. ⛔ **לא הונח שזה פסיק — הוכח:** `comm -23` על הקבוצות הממוינות והמנורמלות יצא **ריק** ⇒ כל שורה שהוסרה חוזרת **זהה** ⇒ **0 ערכים זזו**, נטו **+92 מפתחות** (84 `fxUnconvertedCount` + 8 `fxAggregatableCount`) ⇒ התחזית **אוששה** ⛔ ולא רוככה; ועם הוכחת-אבחנה דו-כיוונית. ⚠️ **ונתפס בקריאה ⛔ ולא בכשל — כמעט שוחזר `B-065`:** `summarize()` גדל למפתח **חמישי** בעוד `lastWeekStats`/`lastMonthStats` ב-`EMPTY_STATS` עדיין פלטו ארבעה ⇒ **סחיפת צורה**; תוקנו שני האובייקטים **וההערה שמנתה אותם**. ⚠️ **ואסרציה שעברה מהסיבה הלא-נכונה, `15.1`:** היא הזינה `makeConvertingCalc(null,"USD","identity")` בעוד **התווית שלה** תיארה את תרחיש ה-₪ ⇒ ⛔ מעולם לא בדקה את מה שהצהירה. נכתבה מחדש על **אותו** `calc` שמזין את `foldClosed`, + אסרציית **שוויון מכנה**. **עותק פונה-משתמש:** `mixedCurrencyBody` הצהיר «Totals below add different currencies together» — נכון לפני הגל ו**שקר אחריו** ⇒ נכתב מחדש ב-**5 שפות**, עם אסרציה שתופסת את הטענה הישנה **בכל שפה בנפרד** **וגם** שהגוף ⛔ לא רוקן (≥40 תווים; בדיקת-היעדר לבדה עוברת על מחרוזת ריקה). ⛔ **`mixedCurrency` עצמו ⛔ לא נגע.** ⚠️ **וההערה ב-`tradingStats.js:64` שהודתה בפגם — תוקנה באותו שינוי:** הערה שמודה בפגם **שתוקן** היא שקר חדש. ⚠️ **`stats.currentEquity` — נקי, ⛔ אך עדיין ללא בעלים:** נמדד שהכותרת מרכיבה `equityBase + closed + live` **בעצמה** ⇒ השדה ⛔ אינו נצרך שם; מוצהר ככזה בהערה וב-`DECISIONS`. **מחיקה נשקלה ונדחתה** — מחוץ להיתר («שדות הכסף וה-`fxUnconverted` בלבד»). ⚠️ **עקומת המסך (`generateEquityCurve`) ⛔ לא נגעה — בכוונה, ובהערה:** היא כבר מדירה מ-`B-142`, והייתה ה**יעד** שאליו יושרו השתיים האחרות. 🔴 **ומה ⛔ לא נסגר, ובמכוון — `B-144`:** תנאי ניב (⟪אם המדידה מראה שזה מכפיל את הגל — עצור ושאל⟫) **נתפס בפועל, לפני שורת קוד**. §2.4 שלי מדד **ספירות** והכליל ללולאה (`tradingStats.js:81-85`) שנושאת **גם כסף**: «השורה האחת» הופכת את `pnlByCurrency` ל-`{USD:300}` = 200 ₪ + 100 $ מודפס `$300` — **`B-142` מילה במילה, בשדה אחר** — ו⛔ **אינה תלוית-סירוב** (יומן מומר-כולו ⇒ `{USD:400}` שקלים תחת `$`), ובנוסף מכבה את `mixedCurrency` בדיוק ביומן שצריך אותו. **השורש: לולאה אחת עונה על שתי שאלות** ⇒ הכיוון הוא **פיצול הגזירה**, גודל **`M`** ⛔ ולא `S` ⇒ `NEXT` §1. ⚠️ **המדידה הזו הצילה מופע חדש של הדפוס שהגל בא לחסל.** 🆕 **סריקה סביבתית §11 — פגם שתוקן בו-במקום:** שורת `C-023` ב-`docs/CHECKS.md` הייתה **שבורה** — הראיה שלה (S5·S7·S8α·S8γ) ישבה בשורה **יתומה בלי `\|` פותח** ⇒ רונדרה מחוץ לטבלה ופיצלה אותה לשתיים. אוחדה חזרה לתא התוצאה, **אפס טקסט אבד**. **שערים:** `npm run verify` — **23 חוליות + `build`, exit 0** (`coach` 111 · `import` 33 · `write` 29/29 · `rcontract` · `fx` · `tradingstats` deep-equal לבייסליין · `ocr` 10/10 · `analytics` 14/14 · `copy` 16/16 · `horizon` 140/140 · `syntax` 4/4 · **`instrument` 370/370** · `registry` 29/29 · `vite build ✓ 7.20s`). ⚠️ **`8.4` נצפתה אדומה בקומיט `e5afdc7` ו⛔ לא רוככה** — קומיט אינו יכול לשאת את ה-hash של עצמו ⇒ השורה נשארה `פתוח`, והמצבה נכתבה **בקומיט הזה** | T3 |
| D-044 | `B-162` — **מיקום הווטו הוא התיקון, ⛔ לא היעדרו: `DENIED` ירד דרגה אחת — אחרי לולאת `EYE`, לפני ענף `C-0xx` (וריאנט D)** ‹`B-165` נסגר באותו גל כ-`D-045`, ⛔ לא נבלע› | 19.08 | `707e6fd` | ✅ **אומת-מקור** — clone נקי מ-`origin` (`git clone --depth 3`, ⛔ לא העץ המקומי) ומדידה עצמאית חוזרת: הווטו נמדד ב-`:518`, **אחרי** לולאת `EYE` (`:507-509`) ו**לפני** `const refs` ⇒ **30/30 אסרציות · 13/13 fixtures · 11/11 שורות `T3`**. **ההכרעה (ניב, 19.08):** תא שמכחיש אימות **ומצטט בדיקה שבוצעה** — **נופל**; ⟪הודאה מפורשת גוברת על כל ראיה עקיפה⟫. ⇒ הווטו ⛔ לא נמחק ו⛔ לא רוכך — **ירד דרגה**. ⛔ **אף רגקס ⛔ לא נגע** (`EYE`·`DENIED`·`DENY_NEAR`·`NEAR_W`·`strip`·`denied`). **מה המיקום הישן עשה:** מעל לולאת `EYE` הוא פסל את **כל התא** והותיר את ענף הקרבה (±90 תווים) **בלתי-נגיש** ⇒ סמן תקין נפל בגלל הכחשה של משהו **אחר** 400 תווים משם. 🔴 **ומה שאִפשר לפגם לחיות מתחת לשער ירוק הוא **שם** של fixture, ⛔ לא היעדר fixture — ‹R-3› בצורתו הטהורה:** fixture 11 נקרא «הכחשה רחוקה ⛔ אינה פוסלת» וטען לשמור על ענף הקרבה, בעוד אוצר-המילים שבתא (`⛔ לא בוצע`) יושב ב-`DENY_NEAR` **בלבד** ו-`DENIED` ⛔ אינו מתאים לו כלל ⇒ הוא ⛔ **מעולם לא נגע בווטו**, ו-`11/11` יצאו זהים איתו ובלעדיו. **השם היה הראיה היחידה שאיש לא קרא** — אימות שבודק את מה **שנכתב** ⛔ ולא את מה **שנמדד**. ⇒ **שמו תוקן, תאו ⛔ לא** (`הכחשה רחוקה ב-DENY_NEAR בלבד`), ⛔ והוא ⛔ לא נמחק: הוא הכיסוי **היחיד** לאוצר-המילים הזה. **שני fixtures נוספו, ⛔ ולא אחד:** `DENIED ⊄ DENY_NEAR` **פורמלית** (המפרידים `"* *"`/`"** **"` מותאמים ב-`DENIED` בלבד) ⇒ fixture אחד ⛔ אינו יכול לשמור על שניהם. **12** = הכחשה רחוקה ב-`DENIED` ⇒ **עובר**; **13** = הכרעת ניב, קפואה ⇒ **נופל**. 🔴 **שני הכיוונים נצפו אדומים לפני התיקון — ⛔ לא הונחו, והפלט הודבק:** **12 על `HEAD` כפי שהוא** ⇒ ⟪ציפייה: עובר · בפועל: נופל · why: הכחשה מפורשת של אימות-עין/מקור⟫ ⇒ `:485` הוא מה שחוסם · **13 על `HEAD` פחות הווטו** (= וריאנט B, המחיקה הנאיבית) ⇒ ⟪ציפייה: נופל · בפועל: עובר · why: אזכור בדיקה שבוצעה: C-091⟫ ⇒ הווטו **נושא משקל**. ⚠️ **הרתמה חיה ב-`/tmp` ו⛔ אינה נקומטת** — חילוץ טקסטואלי של `markerVerdict` + שש תלויותיו מגרסת ה-build הרלוונטית, עם **הוכחת-נאמנות** שכל השבעה נוכחים לפני הייבוא. ✅ **`11/11` שורות `T3` החיות — אפס שינוי מצב:** `ok`+`why` נמדדו לכל אחת **לפני ואחרי** דרך אותו מסלול קוד (עותק מכשיר ב-`/tmp` שקורא את ה-`DONE` החי), ו-`diff` יצא **ריק** — `D-032`·`D-033`·`D-036`·`D-040`·`D-041`·`D-042`·`D-043` ⟪סמן מפורש: אומת בעין⟫ · `D-034`·`D-037`·`D-038`·`D-039` ⟪סמן מפורש: אומת-מקור⟫. ⛔ **אפס שורת `DONE` נגעה** — לא לתיקון, לא «כדי שתעבור». ⚠️ **ושתי וריאנטות נפסלו במדידה ⛔ ולא בטעם:** **B** (מחיקת הווטו) פותחת בדיוק את החור של fixture 13 · **C** (העברה לסוף) ⛔ אינה סוגרת אותו — הווטו נוחת **אחרי** ענף `C-0xx` וה-N2 בורח דרך `:495`. **שערים:** `npm run verify` — **23 חוליות + `build`, exit 0** (`coach` 111 · `import` 33 · `rcontract` · `fx` · `tradingstats` · `ocr` 10/10 · `landing` 7/7 · `digest` 9/9 · `capability` 11/11 · `arch` 8/8 · `copy` 16/16 · `horizon` 140/140 · **`syntax` 4/4 · 46 קבצים מתקמפלים** · `instrument` 370/370 · **`registry` 30/30** · `vite build ✓ 7.45s`). ⛔ **`CHECKS` — ⛔ אין פריט `C-` חדש, ונאמר במפורש כדי ש«לא נוסף» ⛔ לא ייקרא כ«נשכח»:** הגל ⛔ אינו נוגע במסך פונה-משתמש. ⚠️ **המצבה נכתבה בקומיט שאחרי `707e6fd`** — קומיט אינו יכול לשאת את ה-hash של עצמו (כלל 4.1), ו-`--amend` אסור (§5) | T3 |
| D-045 | `B-165` — **סעיף ההצהרות בתחתית `DONE` נכנס למשטח הקריאה של השער: אסרציה `4.3`, `29 → 30`** ‹נולד באבחון `B-162` (`843a0b8`), ⛔ ונסגר כפריט משלו› | 19.08 | `707e6fd` | ✅ **אומת-מקור** — מאותו clone נקי מ-`origin`: ⟪✅ 4.3  4/4 שורות הצהרה · כולן מצביעות ל-`D-` קיים⟫. **הפגם:** `ROW` (`:56`) דורש `\|` פותח ⇒ הסעיף `## ⚠️ מה **לא** אומת בעין` — ובו **ההודאה הכנה על `D-043`** שהמסך החי ⛔ לא נפתח — ישב מחוץ למשטח הקריאה של **כל** תשע האסרציות. **‹R-4› על השער שנועד לאכוף אמת:** הוא מדד **נוכחות** של שורות טבלה, ו⛔ לא ידע שקיים מקום שבו כתובה האמת ההפוכה. **המינימום, ובכוונה ⛔ לא צעד מעבר:** האסרציה מאתרת את הכותרת, קוראת עד ה-`## ` הבא (או EOF), סופרת שורות `- `, ומאמתת שכל אחת נוקבת ב-`D-\d{3}` **קיים ב-`DONE`**. ⛔ **אינה מסווגת הצהרות · ⛔ אינה מחייבת ניסוח · ⛔ אינה מכריעה מה מותר להצהיר · ⛔ אינה מרחיבה את `ROW`.** 🔴 **נצפתה אדומה בשני כיוונים, על עותק-סקרץ' ב-`/tmp` בלבד:** ⓵ מצביע שהוחלף ל-`D-999` ⇒ ⟪❌ 3/4 … ⛔ מצביע ל-`D-` שאינו קיים ב-`DONE`⟫ · ⓶ הסעיף שנמחק כליל ⇒ ⟪❌ ⛔ סעיף ההצהרות ⛔ אינו קיים … הצהרה שנמחקת בשקט היא בדיוק ‹R-4›⟫. ⚠️ **הכיוון השני ⛔ אינו קישוט** — אסרציה שקוראת סעיף בלי לאמת שהוא **קיים** עוברת ירוקה על קובץ שממנו הסעיף נמחק, וזה בדיוק אותו כשל דרגה אחת למעלה. ⛔ **אפס נגיעה ב-`docs/DONE.md`, גם ⛔ לא זמנית** — `git status docs/DONE.md` נמדד **ריק** אחרי שתי התצפיות. ⚠️ **והמדידה בפועל: `4/4` שורות ההצהרה כבר נשאו מצביע תקין** ⇒ האסרציה ⛔ אינה מתקנת חוב קיים אלא **חוסמת את הבא** — וזה נאמר ⛔ ולא מוצג כתיקון | T3 |
| D-046 | `B-166` Phase 2 — **הסרת `--with-deps` מ-`npx playwright install chromium` ב-`sentinel.yml`** ‹כיוון (ד), אושר במפורש ע"י ניב 19.08 — ⛔ לא (א) העלאת timeout, ⛔ לא (ג) retry, ⛔ לא (ב) caching› | 19.08 | `a0ee59e` | ✅ **אומת-מקור** — 3 ריצות `workflow_dispatch` **רצופות**, נקראו מ-`gh run view` (⛔ לא מהעץ המקומי) על הקוד המתוקן, אף אחת `cancelled`: `32280995030`·`32281133594`·`32281250164`, כולן `browser` job = `success`, וצעד ה-Install Chromium נמדד **1 שנייה** בשלושתן (17:20:24→25 · 17:21:53→54 · 17:23:08→09 UTC). **לפני:** 2 ריצות שנקטעו תחת הקוד הישן נמדדו לצד זה — `32279513418` (17:04:37→17:13:39, **9 דק' 2 ש'** עד ה-cancel, נתקע בהורדת פונטים ב-25-45KB/s) ו-`32278086581` מ-Phase 1 (נתקע ב-`apt-get update` לפני כל התקנה). ⚠️ **ריצה מוצלחת בודדת ⛔ אינה ראיה** — Phase 1 מדד שהצלחה תחת הקוד **הישן** גם היא 22-33ש"נ (התנהגות בי-מודלית) — לכן נדרשו דווקא 3 רצופות, ונבדקו ברמת ה-**job הבודד** (`browser`), ⛔ לא רק ה-run הכולל (ראה `B-168`). **מדוד לפני נגיעה (תנאי ניב):** 3 לוגים נבדקו — הצלחה אחת (471 שורות: `0 upgraded, 9 newly installed` — כולן פונטים CJK/תאי) ושתי תקיעות — ואישרו ש-`ubuntu-latest` כבר נושא כל ספריית runtime שכרום דורש (`libnspr4`/`libnss3`/`libgbm1`/`xvfb`/... כולן `already the newest version`) לפני שהקוד שונה. **שערים:** `npm run verify` — 23 חוליות + build ירוקים (`instrument` 370/370 · `registry` 30/30 · `vite build ✓ 7.97s`). `docs/NEXT.md` §3 עודכן ל-`B-168` (הוציא את `B-148`, קריטריון-רוחב, ⛔ לא שיפוט-חשיבות) ו-`docs/STATE.md` ⏭️ בהתאמה, באותו קומיט של רישום זה. ⛔ **`B-169` ⛔ לא הוכרע במכוון** — שני כיוונים נרשמו ב-`BACKLOG` בלי בחירה, לפי הנחיית ניב  🆕 **שכבת אימות שנייה, נוספה 20.08 (⛔ לא שכתוב של השורה — תוספת מתוארכת):** האימות המקורי נשען על **3 ריצות `workflow_dispatch`** שאני יזמתי. ⚠️ **ריצות יזומות ⛔ אינן מודדות את המסלול שנכשל** — הכשל התרחש ב-`schedule`, ותנאי העומס שונים. ⇒ נמדד מ-`gh run list` (⛔ לא מהעץ המקומי): **25/25 ריצות `sentinel.yml` מאז `a0ee59e` הסתיימו `success` · 0 `cancelled`** — **22 `schedule`** + 3 `workflow_dispatch`, מ-19.08T17:20:04Z עד 20.08T08:58:08Z (**15ש׳ 38ד׳ רצופות**). ✅ **ובריצת ה-`schedule` `32290127793` (19.08T18:55:39Z) נקראו שני ערכי-הסיכום מהלוג בפועל** (⛔ לא מהד-הפקודה): ⟪דפדפן: `נטען · 0 ממצאים`⟫ **וגם** ⟪דפדפן מחובר: `נטען · 0 ממצאים`⟫, `browser` job = `success` ב-34 שניות (18:55:43→18:56:17Z). ⇒ **שתי הרגליים של Layer A רצו עד הסוף במסלול המתוזמן** — זה מה ש-3 הריצות היזומות ⛔ לא יכלו להראות | T3 |
| D-047 | `B-144` — **הגזירה סופרת יחידת-תצוגה, ⛔ לא תווית-נייר; `mixedCurrency` הופך סמנטיקה** ‹`B-170` נסגר באותו גל, ⛔ לא נבלע› | 20.08 | `1fab5b9` | 🔴 **`אומת-מקור` — ⛔ ולא «אומת בעין», והפער מוצהר ⛔ ולא מרוכך.** ⛔ **מסך האפליקציה החי ⛔ לא נפתח** — `MixedCurrencyBanner` (`SwingEdge_App.jsx:888`) ⛔ אינו מיוצא ו⛔ אינו ניתן לטעינה לבד, והמסך דורש סשן מאומת ⇒ שתי הדרכים אליו הן הזנת אישורים (אסור) או זריעה ליומן הפרודקשן של ניב בלי אישור (⛔ לא חד-צדדית). ⇒ **`C-030` חצי (א) נשאר פתוח**, ההוראות מוכנות, והצילום פתוח על ניב — תקדים `D-043`/fixture 11.2. **מה כן אומת, ⛔ ולא מהעץ המקומי:** `git show origin/main:src/lib/instrumentCurrency.js` ⇒ `isMixedCurrency` = `codes.size > 1` בלבד · `git show origin/main:src/lib/tradingStats.js` ⇒ `const unit = !m.fxUnconverted && pnlCurrency ? pnlCurrency : paper`. 🔴 **הפרכת דרך היישום שאושרה — נרשמת כאן ⛔ ולא רק בדלתא של התוכנית (הנחיית ניב, 20.08):** התוכנית קבעה `dispCcy` כפרמטר הרביעי ואת הנוסחה `pnlCurrency` עם נפילה ל-`"USD"`. **שתיהן נמדדו שגויות.** `MUTANT-1` שיחזר את נוסחת התוכנית תו-בתו והפיל 4 אסרציות, ובהן ⟪12.T · `pnlByCurrency` → got `{"USD":300}`⟫ (במקום `{"USD":200,"ILS":100}`) ובאנר `+$300.00 (1)` — **שקלים בדלי הדולר**. ⚠️ **⇒ הגל שבא לחסל את הדפוס היה מייצר אותו** — `B-142` שלישי. **השורש:** קיים מסלול **רביעי** שהתוכנית §2.2 החמיצה — `WeeklyReviewTab` מקבל `calc` **גולמי** שאינו ממיר ואינו מסמן דגל ⇒ `m.pnl` נשאר ביחידת ה**נייר**. ⇒ הפרמטר מתאר את **יחידת הפלט של ה-calc** (`pnlCurrency`), ו-`null` הוא ערכו החוקי ל-calc גולמי. ⛔ **זו הפעם השנייה בפריט הזה שהנחה לא-נמדדה נתפסה במדידה** (הראשונה: תיקון «שורה אחת», `PLAN-B143` §10). ✅ **D1 — §6.5 נצפתה ירוקה ו⛔ הוכח שיכלה ליפול. שלושה מוטנטים, כל אחד נצפה אדום והוחזר:** ⓵ נוסחת התוכנית ⇒ 4 כשלים (לעיל) · ⓶ `const mixedCurrency = false` — **מצב הכשל ששמו ניב** ⇒ **בדיוק 2 כשלים, שניהם בתוך 12.T** ⇒ 12.T היא האסרציה ה**יחידה** שמבדילה «מוסתר נכון» מ«לעולם לא יורה» · ⓷ `isMixedCurrency → return false` ⇒ 2 כשלים, אחד מהם אסרציית-השיניים החדשה. ✅ **D2 — 4 אסרציות התהפכו (⛔ לא 2), כל אחת עם ציטוט ההכרעה ו**תאריכה** (`20.08`), ⛔ לא «עודכן ב-B-144».** הנלווית-חובה נכתבה כ**ערך** ⛔ ולא כטקסט: `13/15` — הלא-מאומתים ⛔ **לא נעלמו**, הם עברו ל**מכנה**. ונוספה אסרציית-שיניים שמוכיחה שההיפוך ⛔ אינו «תמיד שקר». ✅ **D3 — `B-170`:** הרג'קס רג'קס אלטרנציה על שני שמות-השדות מול טקסט המקור (`.test(app)`) נמחק **ומוצהר**; מחליפו בודק **ערך** על מחרוזת הבאנר: `+₪50.00 (1)  ·  +$50.00 (1)`. ✅ **D4 — A/D קפואים, נצפו לפני **וגם** אחרי:** בייסליין `tradingstats` יצא **זהה בייט-לבייט**. ✅ **מדידת דפדפן על המודולים הנשלחים** (vite dev, `preview_eval` עם ייבוא דינמי של `/src/lib/…`) — שחזור הצורה שנרשמה ב-`NEXT.md`: ⟪לפני `mixedCurrency:true`, באנר `+$200.00 (1)` · אחרי `false`, `(מוסתר)` · `fxUnconverted 1/2`⟫. 🔴 **והבאנר הישן היה סותר-עצמו:** הכריז «מטבע מעורב» ומנה דלי **יחיד** שמחזיק 1 מתוך 2. **שערים:** `npm run verify` — **23 חוליות + `build`, exit 0** (`coach` 111 · `horizon` 140/140 · `syntax` 4/4 · `instrument` **376/376** · `registry` 30/30 · `tradingstats` בייסליין קפוא זהה · `vite build ✓ 7.17s`). ⚠️ **הפגם היה רדום ⛔ ולא תקלה חיה: 0/46 משתמשים** היו במצב שמדליק אותו (`AUDIT-B144` §D7 · 3/46 שקליים · 2/3 מציגים ב-USD · 0/3 עם עסקאות). ⇒ ⛔ אל תקרא את הגל כתיקון-חירום. 🔴 **וממצא שנמדד בסגירה ו**סותר את מדד-הסגירה החצי השני**, ⛔ ולכן נאמר בקול:** `MEASURED` נגזר מ-`trade.instrumentCurrency` בלבד, שדה ש⛔ **אף מסלול קוד בפרודקשן ⛔ אינו כותב** ול-`trades` ⛔ אין עמודה כזו ⇒ **324/324 צירופים מחזירים `{"USD"}` בלבד** ⇒ `codes.size > 1` **בלתי-נגיש** ⇒ `mixedCurrency === false` לכל משתמש, כל יומן, תמיד. ⚠️ **⇒ «הבאנר נעדר» ⛔ אינו אימות של הגזירה היום** — הוא נעדר כי אין לו איך להידלק. ⇒ `B-172`, ו-`C-030` חצי (ב) חסום עליו. ⛔ **הסמנטיקה ⛔ לא רוככה בגלל זה** — הישנה ירתה על **כל** יומן עם נייר לא-מאומת, וזה היה הבאג | T3 |
| D-048 | `B-172` — **`currency_source` מחובר ל-`deriveInstrumentCurrency` — שלב 2, (ב)** ‹`PLAN-2026-08-20-B172.md` (`a9a729e`), אישור מלא בצ'אט 20.08· `B-154` ⛔ **לא** נסגר עמו, ראה שם› | 20.08 | `5f698b9` | 🔴 **`אומת-מקור` — ⛔ ולא «אומת בעין», והנימוק: 0/78 שורות פרודקשן evidence-backed היום ⇒ ⛔ אין מסך חי שיכול להראות שינוי ואין מה לצלם — המדידה **היא** האימות (`origin` נקרא + `SELECT` חוזר עצמאי), ⛔ לא צילום מסך שלא היה יכול להיות שונה (`B-162`).** **המנגנון:** ענף `currency_source_evidence` יושב בין `provider_code` (`:181-184`) לבדיקת הטיקר (`:186`) — עדות בלתי-תלויה-בצורת-הטיקר, כמו `provider_code`, ⛔ ולא רק בתוך בלוק ה-`ALPHA_TICKER`. קורא את `isEvidenceBacked`/`normalizeProviderCurrency` הקיימים, ⛔ אינו משכפל את `EVIDENCE_SOURCES`. ✅ **D1 — 6 אסרציות ערך נצפו אדומות ואז ירוקות, כולל שני שערי-שלילה:** ⓵ `{TEVA,ILS,broker_arithmetic}` → `MEASURED/ILS/currency_source_evidence` (⛔ לא `CONTRADICTED`) · ⓶ `{AAPL,USD,file_cell}` → `MEASURED/USD/currency_source_evidence` · ⓷ `{AAPL,USD,manual_capital}` → ⛔ **לא** דרך הענף החדש, `ASSUMED/USD/no_evidence_against` כרגיל (הנחה נשארת הנחה) · ⓸ `{AAPL,USD,currency_source:null}` → ⛔ **לא** דרך הענף החדש, זהה להתנהגות טרום-הגל · ⓹ רגרסיה: `provider_code` עדיין גובר על `currency_source_evidence` (`{TEVA,instrumentCurrency:ILA,USD,broker_arithmetic}` → `MEASURED/ILS/provider_code`) · ⓺ רגרסיה: `pair_quote`/`numeric`/`alpha` בלי `currency_source` — זהים בייט-לבייט לפלט טרום-הגל (3 פיקסצ'רים קיימים, מוצלבים ללא שינוי). **שני מוטנטים נצפו אדומים והוחזרו:** סדר-הענפים (הענף מוזז אחרי `:203`) הפיל אסרציה ⓵ · שער-השלילה (הסרת בדיקת `isEvidenceBacked`) הפיל ⓷+⓸. `test:instrument` **395/395**. ✅ **D2 — קו קפוא, נצפה לפני וגם אחרי:** `SELECT currency_source, currency, count(*) FROM trades GROUP BY 1,2` (מדידת `AUDIT-B172.md` D2) → `{null,USD:67}, {manual_capital,USD:10}, {manual_capital,ILS:1}` — **אף אחד מהשלושה אינו evidence-backed** ⇒ **0/78 שורות זזו** דרך הענף החדש; `test:tradingstats` בייסליין קפוא יצא **זהה בייט-לבייט** לפני ואחרי. ⇒ **הענף מחובר ורדום, ⛔ לא פעיל היום** — 0 תועלת מיידית **בכוונה** (`PLAN-2026-08-20-B172.md` §"למה (ב)"): זה הרגע הבטוח ביותר לחבר צנרת, כי אין שורה שיכולה לזוז בטעות. 🔴 **D3 — `B-154` ⛔ לא נסגר עמו, נאמר ולא נכפה:** `isEvidenceBacked` עלה **0→1** קריאה בקוד המוצר (`deriveInstrumentCurrency` עצמו, דרך `currency_source_evidence`) — אבל הצרכן שהפריט ביקש במקור, **מצג-מסך** לעסקה שתוויתה בניחוש, עדיין לא קיים; הקריאה החדשה פנימית-למנוע בלבד, ⛔ לא תצוגה. נשאר פתוח ב-`BACKLOG`, עולה `NEXT` **#3**. **שערים:** `npm run verify` — **23 חוליות + `build`, exit 0** (`instrument` **395/395** · `registry` 30/30 · `tradingstats` בייסליין קפוא זהה · `vite build ✓ 7.18s`). `docs/STATE.md`/`docs/CHECKS.md`/`docs/BACKLOG.md`/`docs/NEXT.md` עודכנו **באותו קומיט** של הקוד (`5f698b9`); שורת `DONE` זו נכנסת **בקומיט הבא** — קומיט אינו יכול לשאת את ה-hash של עצמו (כלל `4.1`) | T3 |
| D-049 | `B-156` — **`TradeDNA`/`GrowthTracker` מפסיקים להמציא `50` על מדגם שלא נמדד — גדר-תצוגה, ⛔ לא `null` במנוע** ‹`PLAN-B156.md`, אישור מלא בצ'אט 20.08 · סוגר גם `B-009` §"מה ⛔ לא נסגר" (`D-041`)› | 20.08 | `35a7b18` | ✅ **אומת בעין** — שלושת אתרי הרינדור נצפו דרך רמה חד-פעמית זמנית (`EyeVerifyHarness.jsx` + נתיב זמני ב-`src/main.jsx`, נמחקו במלואם לפני קומיט; `git status` נקי לפני ואחרי) ⛔ **לא** "אומת-מקור" — יש מסך חי מושפע ולכן הצפייה בו חובה, ⛔ לא רק קריאת קוד. **המנגנון:** `computeScores` (`TradeDNA.js`) ו-`calculateGrowthScore`/`dnaEvolutionSeries` (`GrowthTracker.js`) מחזירים `null` על כל ציון שמדגמו מתחת ל-`MIN_SAMPLE_R`, ⛔ לא ברירת-מחדל אריתמטית — 6 צרכנים אריתמטיים חישבו על ציון שקרי לפני התיקון. `GrowthTracker.total` **מנורמל-מחדש** על משקלי השדות **הנמדדים בלבד** בלבד, עם `measuredCount`/`totalCount` חשוף כשבר-שקיפות (Q1, `DECISIONS` 20.08). 🔴 **ואגב התיקון נחשף כפל-נוסחה שלא היה בתוכנית:** שני המנועים חישבו `consistency` בנוסחאות/ספי `stddev` **שונים** — אוחדו על `stddev`/`MIN_SAMPLE_R` המשותפים מ-`statisticalModels.js`, **הבייסליין הקפוא זז 53→46, מוצהר ולא מרוכך** (Q4, `test:dna` תרחיש 7 מוכיח את האיחוד ישירות). **שלושה אתרי רינדור:** `DNACard` **לעולם לא נעלם** על יומן ריק — 4 מקפים + שורת הסבר, כי עוגן `data-tour="trading-dna"` על משתמש **חדש** בלי עסקה אחת (Q3, override מפורש של ניב על התוכנית שאושרה, שהצהירה שהקומפוננטה **תיעלם**) · `GrowthChart` null-safe + תג שבר `measuredCount/totalCount`, הגרף עדיין `null` על evolution ריקה · `menteeDNA` עודכן באותו דפוס. ⚠️ **חריגה מוצהרת של 2 שורות מהתוכנית שאושרה** — `SwingEdge_App.jsx` נגע בנקודת הקריאה ל-`GrowthChart` כדי להעביר את שני שדות השבר; התוכנית הצהירה "0 שורות" (§6). ✅ **27/27 `test:dna` + מוטציה מפלה** (fixture 3: `risk` על יומן בלי סטופ ⇒ `null` ⛔ לא `100`; fixture 5: `total` מנורמל על 2/5 שדות נמדדים ⛔ לא ממוצע-שקרי על 5) · **`test:coach` 111/111 לפני *וגם* אחרי** · `test:instrument`/`test:rcontract` תוקנו אגב — שני הפיקסצ'רים מדדו **אפס-אוכלוסייה** במקום נתון אמיתי, ⛔ לא סחפו את הבדיקה עצמה. **אומת בעין — שני מצבים, לפני ואחרי:** יומן **ריק** ⇒ `DNACard` 4 מקפים + הסבר (⛔ לא נעלם), `GrowthChart` ⛔ לא מרונדר כלל · יומן **עסקה בודדת** ⇒ `DNACard` `discipline`/`riskMgmt`/`growth`=`100` (נמדדו, לא הומצאו — 1 עסקה מספיקה ל-`MIN_SAMPLE_R` שלהם) ו-`consistency`=`null` (דורש 2 ערכי-R) · `GrowthChart` נקודה אמיתית אחת + תג `"3/5 נמדד"`. **שערים:** `npm run verify` — **23 חוליות + `build`, exit 0** (`coach` 111 · `dna` 27/27 · `instrument` 395/395 · `registry` 30/30 · `vite build ✓ 7.32s`). `docs/STATE.md`/`docs/CHECKS.md`/`docs/DECISIONS.md`/`docs/NEXT.md` עודכנו **באותו קומיט** של הקוד (`35a7b18`); שורת `DONE` זו וה**מצבה** ב-`BACKLOG` נכנסות **בקומיט הבא יחד** — `8.4` דורשת שה-`D-` יהיה קיים לפני שהמצבה יכולה להצביע אליו, וקומיט אינו יכול לשאת את ה-hash של עצמו (כלל `4.1`, תקדים `D-044`/`D-045`) | T3 |
| D-051 | `B-164` — **rewrite גורף ב-`vercel.json` הוגבל ל-`/assets/*` + retry חד-פעמי ב-`index.html`** ‹`docs/audits/AUDIT-B164.md` (`e0fdfcf`) · `docs/plans/PLAN-2026-08-23-b164-asset-rewrite-fix.md` (`f1fad93`), אישור מלא בצ'אט 23.08› | 23.08 | `19b898a` (retry, deploy #1) + `b7ce874` (rewrite, deploy #2) | ✅ **אומת בעין + אומת-מקור** — שילוב: שלושת הטאבים (לפני/בזמן/אחרי כל deploy) נצפו ב-screenshot+console (עין), ובדיקות D4 נמדדו ישירות מ-production ב-`curl` (מקור), ⛔ לא מהעץ המקומי. 🔴 **תיקון מסגור, נדרש במפורש בתוכנית שאושרה:** `B-164` ⛔ **אינו** מקרה של מנגנון skew/atomic-deploy חסר. הממצא המדוד: `vercel.json` הגדיר rewrite **גורף** (`/((?!api/).*)`) שלא החריג נתיבי נכסים ⇒ **כל** נתיב לא-`api/` שאינו קיים כקובץ מוגש כ-`200 text/html` — **בכל זמן**, ⛔ לא רק בחלון deploy; חלון ה-deploy הוא רק המקרה השכיח ביותר שבו מתבקש נכס לא-קיים (שם-hash ישן שהוחלף). **D8 (`b7ce874`):** ה-lookahead הורחב ל-`/((?!api/\|assets/).*)` — שורה אחת. **D10 (`19b898a`):** תג `id="se-main-script"` על ה-`<script type="module">` + מאזין `error` על **האלמנט** (⛔ לא `window.onerror`) עם דגל חד-פעמי ב-`sessionStorage` (⛔ לא `localStorage`) — כשל ראשון ⇒ `location.reload()` יחיד; כשל שני ⇒ מחליף את ה-spinner בהודעת שגיאה גלויה **דו-לשונית** (עברית+אנגלית) + כפתור רענון, ⛔ לא ספינר-נצחי. ✅ **D1 — סדר ביצוע כפוי כפי שנדרש, אומת מהיסטוריית Vercel:** `19b898a` (retry-בלבד) עלה ל-`READY` והוצמד ל-production **לפני** ש-`b7ce874` (rewrite-בלבד) אפילו נכתב — שני deploys נפרדים לגמרי, ⛔ לא אותו deploy. ✅ **D4 — אומת בפרודקשן, שני הכיוונים, אחרי deploy #2:** `curl` ל-`/assets/<שם-בדוי-אקראי>` ⇒ `404` עם `x-vercel-error: NOT_FOUND` (⛔ לא `200 text/html`) · `curl` ל-`/`, `/app`, `/terms`, `/privacy`, `/xyz-random-b164-check` (נתיב-שרירותי לא-קיים) ⇒ **כולם** `200 html` — ⛔ אפס רגרסיית ניתוב SPA. ⚠️ **D9 — נמדד עקיפין, ⛔ לא ישירות, ונאמר בקול כפי שנדרש (D3: "אם אי-אפשר לבצע — עצור ואמור, ⛔ אל תסמן בוצע"):** שלושה טאבים נפתחו/נצפו (לפני deploy #1 · לפני deploy #2 · **בזמן** ה-`BUILDING` של deploy #2) ונבדקו נקיים ב-console+screenshot אחרי כל deploy. **הממצא המכריע:** Vercel atomic-deploy נמדד חי — טאב חדש שנפתח **בזמן** ה-`BUILDING` של deploy #2 קיבל את ה-deployment **הקודם** (עדיין READY) נקי לגמרי; ⛔ **אין מצב-ביניים חשוף למבקר כלל** — ה-alias מצביע על deployment שלם עד רגע ה-flip הבודד. אחרי ה-flip, ה-hash שהיה חי רגע קודם כ-entry chunk של deploy #1 (`index-CzWjKyoE.js`) חזר `404` אמיתי — הוכחה חיה שהתיקון (D8) פותר בדיוק את התרחיש שה-retry (D10) נועד לתפוס. ⛔ **לא בוצע ואינו מסומן כבוצע:** חלון פרטי של מבקר אנונימי-לגמרי בדיוק ברגע ה-alias-flip הבודד — לא ניתן לתזמן בוודאות כזו; המדידה שכן בוצעה (atomic-deploy model + hash-churn 404) מהווה אימות מנגנוני חזק, ⛔ ולא תחליף מזויף לתצפית המדויקת שהתבקשה. **שערים:** `npm run verify` — **24 חוליות + `build`, exit 0**, `test:registry` 30/30. ⛔ **אין `docs/INCIDENTS.md`** — שני ה-deploys עלו נקי, ללא התנהגות פרודקשן חריגה. `docs/STATE.md`/`docs/BACKLOG.md` עודכנו **באותו קומיט** של תיקון ה-`vercel.json` (`b7ce874`); שורת `DONE` זו נכנסת **בקומיט הבא** — קומיט אינו יכול לשאת את ה-hash של עצמו (כלל `4.1`) | T3 |
| D-052 | `B-171` — **TwelveData מכסת קרדיטים: `2,389→440` (−82%) אחרי `2d95218`, אומת בעין מצילום ניב 25.08 16:25 IL** ‹`AUDIT-B171.md`/`AUDIT-B171b.md`/`AUDIT-B171c.md` · `C-032` M1-M3 הושגו, D3+M4 נשארים פתוחים › | 25.08 | `9a87c14` | ✅ **אומת בעין** — צילום דשבורד TwelveData שסיפק ניב (25.08 16:25 IL, 13.5 שעות אחרי איפוס-UTC 03:00 IL, ⛔ **לא** 24 שעות): קרדיטים `2,389→440` (**−82%**), avg/min `6/8→2/8` (**−67%**), max/min `40/8→17/8` (**−57%**), מכסה-מול-תקרה `300%→55%` ✅ — עומד בסף `C-032`/M3 (<800). ⚠️ **440 הוא קריאת-ביניים, ⛔ לעולם לא "440/יום":** 13.5 שעות בלבד ⇒ אומדן-יממה-מלאה **~550-650**. **`C-032` נשאר פתוח בחצי השני:** D3 (בדיקת-תת-קבוצה — האם `days=30` מכיל `days=7`/`days=1` בדיוק כתת-קבוצה) **⛔ לא אומת חי** — מכסת TwelveData הייתה נעולה לאורך כל גל 24.08 ומנעה A/B אמיתי; M4 גם טרם נמדד/זוהה. 🔴 **שתי הסתייגויות מוצהרות בסגירה, ⛔ לא נבלעות לתוכה:** ⓵ `17/8` עדיין חוצה את תקרת ה-2×/דקה ברגעים בודדים ⇒ ⛔ אין "אפס חריגות" — מזהה נפרד `B-183` (חריגות שיא נקודתיות, טרם פורק לפי צרכן). ⓶ המרווח **אינו יציב לצמיחה**: `440` נמדד על **~50 משתמשים**; הכפלה ל-~100 משתמשים צפויה לחצות שוב את ה-800 — נרשם כסיכון פתוח ב-`STATE.md` §⚠️, ⛔ לא רק כאן. 🔴 **לקח `B171c`, נרשם כדי שלא יישכח:** המדידה הראשונה (`2,389`, 24.08) נלקחה **לפני** ש-`2d95218` בכלל הגיע לפרודקשן (עלה 17:35:02Z/20:35 IL) — הכרזה על "לא עבד" מהמדידה ההיא הייתה הטעות שכבר נתפסה אז (`AUDIT-B171c.md` D19); התיקון-העצמי הוא מה שהציל את הגל מ-false negative, ⛔ ולא מזל. **באותו קומיט (`9a87c14`) תוקן גם `B-176`:** "2,622 מופעים" (Vercel `get_runtime_errors`, `AUDIT-B176.md`) הוברר כמצטבר על 7 שבועות ⛔ ולא יומי — נורמל ל-**≈53-57/יום** (`AUDIT-B176.md` §D8), כדי שלא יישאר מספר מצטבר שנקרא כיומי — בדיוק לקח `B171c` שני על אותו קומיט. ⛔ **T3 (§15, חמש שאלות):** הפיכות — כן, הגל מדווח על תיקון מכסה שכבר בפרודקשן ולא ניתן "לבטל" צריכת קרדיטים שכבר קרתה; אמון-משתמש — כן, מכסת נתוני-שוק חיה מזינה `RegimeIndicator`/`DecisionCoach`; אבטחה/גישה — לא; רוחב-קוד — המשימה הזו עצמה registry-בלבד, ⛔ אך הגל הבסיסי (`B-176`/`B-171`) נגע ב-`api/health.js` ובמסלולי poller מרובים; ודאות — המספר החדש (13.5ש') נמדד השבוע. ⇒ **T3**, אימות-עין בוצע כנדרש. **שערים:** `npm run verify` — **23 חוליות + `build`, exit 0**, `test:registry` **30/30**. `docs/STATE.md`/`docs/CHECKS.md`/`docs/BACKLOG.md`/`docs/audits/AUDIT-B176.md` עודכנו **באותו קומיט** (`9a87c14`); שורת `DONE` זו והמצבה הסופית ב-`BACKLOG` (`✅ בוצע → \`D-052\``) נכנסות **בקומיט הזה** — `9a87c14` כבר קיים ב-`origin`, ⛔ אין הפרת כלל `4.1` | T3 |
| D-053 | **DEEP-AUDIT — 8 פאזות read-only · 113 ממצאים · `P0=9 · P1=41 · P2=53 · P3=10` · ⛔ 0 קבצי קוד שונו** ‹`docs/audits/DEEP-{A..H}.md` + `DEEP-SUMMARY.md`› ⇒ **60 פריטי `B-184`…`B-243`** ורישום בלבד, ⛔ אפס תיקון | 26.08 | `d87b68c` | ✅ **אומת-מקור** (⛔ אין מסך מושפע — הגל ⛔ לא נגע בקוד). **קריאה מ-`origin` ⛔ לא מהעץ:** `git show origin/main:docs/BACKLOG.md` → **243** שורות `\| B-nnn \|`, מתוכן **60** בטווח `B-184`…`B-243`. **מדידה חוזרת עצמאית:** `243 − 183 = 60` ✅ — שני מסלולים בלתי-תלויים מסכימים. ⚠️ **הצלב הזה ⛔ אינו טקסי:** המנייה הראשונה החזירה **64** מפני שהתבנית `'^\\\| B-(1[89][0-9]\|2[0-4][0-9]) \\\|'` בלעה גם את `B-180`…`B-183` ⇒ המספר תוקן ל-`'^\\\| B-(18[4-9]\|19[0-9]\|2[0-3][0-9]\|24[0-3]) \\\|'`; **מלכודת התבנית נרשמה ב-`DEEP-SUMMARY.md`** כדי שלא תחזור. 🔴 **מה ה-`DONE` הזה ⛔ אינו אומר: ⛔ אף ממצא ⛔ לא תוקן.** 9 `P0` — כסף/אבטחה/משפט שמשתמש רואה **היום** — פתוחים ורשומים ב-`STATE` §⚠️. הגל סגר **מדידה**, ⛔ לא סיכון. ⚠️ **מוחרג במפורש מכל שמונה הפאזות:** `B-176`/`B-178` (היו מוקפאים בזמן הסריקה) ⇒ הכיסוי ⛔ אינו מלא, וזה מוצהר. **שיוכי שורש:** 27 חדשים (R-1 +3 · R-2 +6 · R-3 +3 · R-4 +4 · R-5 +3 · R-6 +8), נמדדו בפיצול על `\|` **לא-מוברח** כמו `registry-test.mjs:96` — ⚠️ פיצול נאיבי החזיר מספרים שגויים ומזהים קטועים (`B-22`/`B-16`), כי תא `B-187` נושא `` `\\\|\\\| 1` ``. **סיווג (§15, חמש תשובות `לא/לא/לא/לא/כן`):** הפיכות — לא, ⛔ 0 קבצי קוד · אמון — לא, ⛔ אין פלט למשתמש · אבטחה — לא, read-only בלבד · רוחב-קוד — לא, `docs` ⛔ אינם נספרים (§15/4) · ודאות — **כן**, הגל **קיים** כדי למדוד הנחות שלא נמדדו ⇒ **T2**. **אסרציית ערך שנצפתה אדומה:** `test:registry` §8.3 ירתה `❌ B-050 נושא ‹R-6› ואינו ברשימת החברים שלו` על עריכה שלי, והוירקה רק אחרי רישום הדדי. **שערים:** `verify` 23 חוליות + `build` exit 0 · `test:registry` **30/30** | T2 |
| D-054 | 🔴 **תקרת `docs/STATE.md` עברה מ-100 שורות ל-16,000 בתים, והקובץ נגזם `75,177 → 6,925b` (43%) · `100 → 72` שורות** ‹`DECISIONS` 26.08 · `test:registry` §3 · פרובננס מלא `docs/audits/STATE-PRUNE-2026-08-26.md`› | 26.08 | `90d69da` | ✅ **אומת-מקור** (⛔ אין מסך מושפע). **קריאה מ-`origin`:** `git show origin/main:docs/STATE.md \| wc -c` → **6,949** · `git show origin/main:scripts/registry-test.mjs` → `STATE_BUDGET = 16000` ב-`171` · קובץ הפרובננס קיים ב-`origin` ב-**75,029b**. **מדידה חוזרת עצמאית:** `wc -c docs/STATE.md` → **6,949** — ⚠️ **בכוונה `wc -c` ו⛔ לא הטסט**, כי זו בדיוק הסיבה שנבחרו בתים: `wc -c` = `Buffer.byteLength` בדיוק ⇒ ניב מאמת את השער בפקודה אחת. 🔴 **אסרציית הערך נצפתה אדומה לפני שהוירקה:** `❌ 3.1 docs/STATE.md = 75,177 bytes מתוך 16,000 (470%)` על הקובץ הלא-גזום. **מה הפריך את התקרה הישנה:** `100→100` שורות ב-11.08 בזמן `8,162→49,950` בתים (×6.12), ואז `68→99` שורות ב-26.08 בזמן שהתוכן הגיע ל-`75,177` (×6.91) — **×9.2 מאז החקיקה, והמספר שהשער שמר ⛔ לא זז.** ‹R-4›+‹R-1›. ⛔ **ו⛔ אינו תיאורטי:** `Read` על הקובץ **נכשל** — `36,375 tokens > 25,000`. ⚠️ **בדיקת אובדן לפני שנגעתי:** ~200 אסימונים מבחינים (`\d+/\d+` · אחוזים · `קובץ.js:שורה`) מ-28 פריטי מקטע הסיכונים הושוו ל-`BACKLOG`/`DONE`/`DECISIONS`/`INCIDENTS`/`CHECKS`/כל `audits` — **7 בלבד** ⛔ לא נמצאו במקום אחר. **שניים מהם משנים פריט חי ולכן נכנסו לתוכו, ⛔ ולא לארכיון:** `B-050` (`analyst.mjs:408` — **11/25 מפתחות חסרים, 44%** ⇒ **הסחיפה שה-`TODO` נועד למנוע כבר קרתה**) · `B-146` (`sentinel-auth.spec.js:204` — **7** התאמות מול «3+3=6» שברשומה; ⛔ **הסתירה סומנה ו⛔ לא הוכרעה בבחירת המספר הנוח**). שניהם אומתו ב-`origin`: `grep` החזיר `1/1` לכל אחד. חמשת הנותרים נשמרו כפרובננס; **יתר ~193 אומתו כקיימים** ⇒ הגיזום ⛔ אינו מוחק מדידה. 🔴 **למה ⛔ לא הרחבתי את התקרה:** סיווג מכני של 28 הפריטים נתן **20 שנושאים `B-` שקיים ב-`BACKLOG` (37,083b) · 7 סגורים ב-`DONE` (9,374b) · 1 יתום (519b) = 27/28 (98.8%) כבר קיימים במקום אחר** ⇒ `STATE` ⛔ **אינו קטן מדי — הוא היה עותק שני של `BACKLOG`** ‹R-6›. ⚠️ **ו-§1.3 («STATE מצטט, ⛔ אינו מגדיר») עבר ✅ כל הזמן**, כי הוא בודק **שורות טבלה** ו⛔ לא כפילות בפרוזה — **שומר שלישי שמדד צורה במקום אמת.** **מה נפסל ולמה — ב-`DECISIONS` 26.08:** הרחבת התקרה · JSON/YAML/SQLite («קובץ שמכיל יותר מילים» — ל-`.md` ⛔ אין תקרת מילים; המשאב הוא **תקציב הקורא**, ופורמט מובנה **הורג את קריאות ה-diff**) · טוקנים (תלויי-מודל) · תווים (`wc -c` ⛔ אינו מסכים בעברית — **1.46** בתים לתו) · גיזום ל-`ARCHIVE` (אסור §14 — זה מה שאיבד 33 פריטים ב-12.08). **סיווג (§15):** הפיכות — **כן** (68K בתים יצאו מקובץ; git שומר, ⛔ אך התקדים של 12.08 מוכיח שגיזום **כן** מאבד פריטים בפועל ⇒ «ספק ⇒ כן») ⇒ **טריגר-יחיד, `T3`**; יתר ארבע: לא/לא/לא/לא. **שערים:** `verify` 23 חוליות + `build` exit 0 · `test:registry` **30/30** · `test:syntax` **4/4** (נדרש — `scripts/registry-test.mjs` שונה, §7) | T3 |
| D-055 | 🆕 **`docs/METRICS.md` — קובץ שמיני בחוזה, ושער `test:registry` §12 (5 אסרציות) שהופך את כלל 08.08 ממכתב למנגנון.** ⚠️ **ובאותו גל: `docs/INBOX.md` רוקן** — 3 שורות (20.08·21.08·23.08) ⇒ `B-244` · כבר-מוכרע · `M-004`/`M-005` ‹`DECISIONS` 26.08 · `CLAUDE.md` §14› | 26.08 | `70123a0` | ✅ **אומת-מקור** (⛔ אין מסך מושפע). **קריאה מ-`origin` ⛔ לא מהעץ:** `git show origin/main:docs/METRICS.md` → **10** שורות `M-` (7 פנקס + 3 שושלת) · `git show origin/main:scripts/registry-test.mjs` → **5** קריאות `check("12.` · `git show origin/main:docs/INBOX.md` → **0** שורות ממתינות · `git show origin/main:CLAUDE.md` → **0** מופעי «תקרת 100 שורות» ו-**1** מופע `docs/METRICS.md`. 🔴 **מה שהכריח, ⛔ ולא רעיון מופשט:** בטריאז' 26.08 השורה «0 עסקאות ב-70.4 שעות» ⛔ **לא התאימה לאף אחד משלושת המצבים** של §10.2 — ⛔ אינה משימה, ⛔ לא טופלה, ו⛔ אין נימוק לדחותה: **היא מדידה.** ⚠️ ו-`STATE` **ציטט אותה מ-`INBOX`**, קובץ שהחוזה שלו מצהיר «⛔ פריט לא נשאר כאן מעבר לסשן» ⇒ **ציטוט שמצביע לקובץ שמוחק את עצמו** ‹R-5›. ⇒ החוזה בן שבעת הקבצים ⛔ **לא היה שלם**. **מה §12 מודד, ומה ⛔ אינו יכול:** משווה `נמדד + תוקף` ל**היום** ומחייב `⏳ פג`. ⛔ **⛔ אינו יודע אם המספר נכון — רק אם הוא טרי.** 🔴 **אסרציית ערך נצפתה אדומה בשלושה כיווני כשל בלתי-תלויים לפני שהוירקה** (⛔ לא הרצה אחת): ⓵ `❌ 12.4 M-001/M-002 — נמדד לפני 15 יום, תוקף 14, וסטטוסו "✅ בתוקף"` · ⓶ `❌ 12.4 M-004/M-005 — נמדד בעתיד (127 יום קדימה) ⇒ שעון שבור שיעבור לנצח` · ⓷ `❌ 12.3 M-007 — תוקף="לתמיד" ⛔ אינו מספר ימים`. **ובנוסף §12.2 ירתה על הקובץ שאני עצמי כתבתי** — `M-006` נשא «−82%» בלי מכנה; **כתיבת המכנה חשפה ש-`440` הוא 13.5 שעות ו-`2,389` הוא יממה מלאה ⇒ ⛔ אינם אותה יחידה**, וההשוואה מטה **לטובת** התיקון. ⚠️ זה בדיוק מה ש-§2 מבקש («האם המכנה יכול בכלל להכיל את מה שנספר?») — ונתפס **רק** מפני שהשער אילץ לכתוב אותו. **מדידה חוזרת עצמאית בתוך הגל:** `M-007` נסחף מ-`109/243` ל-`111/244` באותו סשן (הוספת `B-244`); המנייה החוזרת החזירה **130** תגי `‹R-n›` מול **121** ב-§8.1 — הפער `130−121=9` הוא **ציטוט חוזר** ב-8 פריטים (`B-042`·`B-071`·`B-080`·`B-152`·`B-153`·`B-160`·`B-165`·`B-171`), ⛔ לא שיוך. **⛔ לא בחרתי את המספר הנוח** — הוגדר «שיוך = זוג `(פריט,שורש)` ייחודי», שני המסלולים הוסכמו על `121`, וההפרש נכתב בשורה. **הערך הישן ירד לשושלת ⛔ ולא נדרס.** ⚠️ **שתי סחיפות שנתפסו בדרך:** `CLAUDE.md` §14 עדיין נשא «תקרת 100 שורות» **אחרי** ש-`D-054` שינה אותה — **החוזה עצמו סחף**; ו-§7 תיאר את `test:registry` כ-**26** assertions בעוד בפועל **35**. **סיווג (§15, חמש תשובות `לא/לא/לא/לא/כן`):** הפיכות — לא, `docs` + שער · אמון — לא, ⛔ אין פלט למשתמש · אבטחה — לא · רוחב-קוד — **לא**: `scripts/registry-test.mjs` הוא קובץ **קוד יחיד**, ו-`docs` ⛔ אינם נספרים (§15/4) · ודאות — **כן**, כל שבעת המדדים היו הנחות עד שנמדדו ⇒ **T2**. **שערים:** `verify` 23 חוליות + `build` **exit 0** · `test:registry` **35/35** · `test:syntax` **4/4** | T2 |
| D-056 | 🎨 **אימות read-only של אבחון עיצוב שנעשה בצ'אט אחר — 8 טענות · 14 מדידות · `0` אוששו במלואן · `3/8` חלקית · `5/8` הופרכו · ⛔ 0 קבצי קוד שונו** ‹`docs/audits/AUDIT-design-system.md`› ⇒ **`B-245`…`B-250`**, רישום בלבד | 26.08 | `9dd60cc` | ✅ **אומת-מקור** (⛔ אין מסך מושפע — הגל ⛔ לא נגע בקוד; **⛔ ואין אימות עין, וזו מגבלה מוצהרת בדוח §4**). 🔴 **מה הופרך, ⛔ בלי ריכוך:** ההשערה המרכזית — «הבעיה אינה עיצוב מכוער אלא **היעדר** מערכת טוקנים» — ⛔ **לא חלקית שגויה; הפוכה.** `src/design/tokens.css` = **149** משתני CSS · **1,167** צריכות `var(--` · והדאשבורד (`SwingEdge_App.jsx:4302-4819`) מטוקן ב-**89%** — **88** קריאות טוקן מול **11** ליטרלי הקס, **3 צבעים נבדלים**, ⛔ לא 92. **T1 שגויה פי שניים** — גם המספר (`92` מול **141** בעץ · **118** בקוד חי) וגם המשטח. **T5** הופרכה **מהערת קוד שהמדידה המקורית ⛔ לא קראה** (`--v3-info` נושא «equity/live-data channel»). **T6** הופרכה בדאשבורד — **0** glass ו-**צל 1**, ו-`index.css:204-206` כבר מרכך כל צל שיטתית. **T8** הופרכה בבסיסה — `General Sans` **נטען וקודם ל-Inter בכל מחסנית**, ועברית ⇒ `Heebo` ⇒ **Inter ⛔ אינו מרונדר כלל**. **T7** הופרכה כי היא **מקטינה** את הבעיה פי ~4 וממקמת אותה במשטח הלא-נכון (**17** ערכי `borderRadius` inline מול 9 טוקנים). ⚠️ **והזנב ⛔ אינו בטסטים:** `scripts`/`tests`/`docs` = **0 מוחלט**; אבל **283/972 (29%)** מהמספר הגלובלי מגיע מ-`emails/`, שאינו קוד דפדפן ⇒ המספר המקורי נופח ממשטח לא רלוונטי. 🔴 **הממצא שאף אחד מ-14 המדדים ⛔ לא שאל עליו:** כולם מודדים **מלאי** (כמה צבעים · כמה רדיוסים · איזה פונט), ⛔ **אף אחד ⛔ אינו מודד דירוג.** `SwingEdge_App.jsx:972` מרנדר את **שישה** ה-KPI ב-`text-lg md:text-2xl font-bold font-mono` — גודל, משקל, פונט וריווח **זהים** ב-`lg:grid-cols-6` ⇒ **הון החשבון ⛔ אינו נבדל ממונה הרצף**, והגוון — ההיררכיה היחידה — **סמנטי ב-3 כרטיסים ודקורטיבי ב-3** ⇒ זה ההסבר ל-«זה נראה אותו דבר» אחרי 4 ניסיונות (`B-246`). 🔴 **והממצא החמור ביותר יושב במצב ש⛔ לא נבדק:** **5/8** צמדי הדאשבורד נופלים מ-AA ב**בהיר** (**0/7** כהה · **0/8** ב-v3), וה-P&L ב-**`2.54:1`** — נכשל גם ב-AA-large (`B-245`). **אימות עצמאי של המספרים בדרך — שלוש מדידות שלי נצפו אדומות ותוקנו, ⛔ ולא נמסרו:** ⓵ `comm` החזיר **0** חפיפה בין הקס חי לערכי הטוקן — בלתי-אפשרי, כי `#00c076` **מוגדר** ב-`tokens.css`; הסיבה `cut -c9-` על פלט `uniq -c` מרופד-רוחב ⇒ תוקן ל-`awk '{print $2}'` והתוצאה **41 חופפים / 77 מפוזרים**. ⓶ ספירת וריאנטי `dark:` החזירה **0** בזמן ש-`StatCard` שקראתי נושא `dark:bg-[#0d1424]` ⇒ תוקן ל-**538**. ⓷ `\brounded\b` החזיר **644** כי מקף הוא גבול-מילה ⇒ עם lookahead שלילי: **0** שימושים חשופים. ⚠️ **ו-§8.3 ⛔ לא ירתה בגל הזה** (הרישום ההדדי קדם לה) — אבל **הפרוזה בשכבת השורשים נסחפה `119` מול `124` שהשער הדפיס**, והשער היה **ירוק**: §8.1 **מדפיס ו⛔ אינו משווה** ⇒ נרשם `B-250` ‹R-4›, **חזרה רביעית של אותה תבנית באותה שורה**. **סיווג (§15, חמש תשובות `לא/לא/לא/לא/כן`):** הפיכות — לא, ⛔ 0 קבצי קוד · אמון — לא, ⛔ אין פלט למשתמש · אבטחה — לא, read-only · רוחב-קוד — לא, `docs` ⛔ אינם נספרים (§15/4) · ודאות — **כן**, הגל **קיים** כדי למדוד 8 טענות שלא נמדדו ⇒ **T2**. **שערים:** `verify` 23 חוליות + `build` exit 0 · `test:registry` **35/35** | T2 |
| D-057 | 🔒 **`test:registry` §8.6 — מספר בפרוזה הפך לערך נמדד; `35→36` אסרציות.** סוגר את `B-250`. §8.1 **הדפיס** את מספר השיוכים ו⛔ לא השווה אותו לכלום ⇒ הפרוזה בראש שכבת השורשים ב-`BACKLOG` נסחפה **ארבע פעמים באותה שורה** («126» 13.08 · «167·81·88·86» 19.08 · «170·82·89·88» 20.08 · «243·109·119·134» 26.08), והשער היה **ירוק** בכל אחת. ⛔ **התיקון ⛔ אינו עדכון המספר** — זה בדיוק מה שנעשה ארבע פעמים ‹R-4›. §8.6 קוראת ארבעה מספרים (סה"כ · עם-שורש · שיוכים · בלי-שורש) **כולל המכנה של `X/Y`** ומשווה למדוד ב-§8.1. ⚠️ **ותבנית שאינה נתפסת בדיוק פעם אחת = כשל, ⛔ לא דילוג** — ניסוח מחדש היה הופך את השער לעיוור ומחזיר את `R-4` בתחפושת חדשה | 27.08 | `09db219` | **נצפתה אדומה בארבעה מוטנטים** לפני ירוק: שיוכים `125→124` (הסחיפה ההיסטורית המדויקת) · עם-שורש `115→109` · **מכנה `250→243` כשהמונה נכון** · ניסוח-מחדש ⇒ `נתפסה 0 פעמים`. `BACKLOG` שוחזר byte-identical (`git diff --stat` ריק) בין המוטנטים. 🔴 **ואז ירתה אדום בשימוש האמיתי הראשון** — `B-251`…`B-254` הזיזו את ארבעת המספרים בו-זמנית והשער תפס את כולם (`250→254 · 115→119 · 125→129` + מכנה), **הפעם הראשונה שהסחיפה נתפסה על ידי מכונה ⛔ ולא ביד**. `verify` 23 חוליות + build exit 0 · registry 36/36 | T2 |
| D-058 | 🔬 **`C1` — `B-245` נמדד בדפדפן חי ו⛔ לא שרד כפי שנרשם · ⛔ 0 קבצי קוד-מוצר שונו.** `docs/audits/AUDIT-C1-contrast-live-2026-08-27.md`. **שרד:** `2.54:1` על `#10B981`/לבן **אושש בדיוק** · `M-01` `.dark`-scoped אושש. 🔴 **הופרך:** ההיקף **27/37** ⛔ ולא «5/8» · הגרוע **`amber` `1.67:1`** ⛔ ולא ה-P&L · **5/5** נכשלים ב-`390×844` מול `2/5` ב-`1440×900` (`text-lg md:text-2xl` ⇒ `18px/700` **מתחת** ל-`18.66` ⇒ סף `4.5` ⛔ ולא `3.0`) · כהה ⛔ **אינו נקי** (`3/37` ב-`4.01:1`, כי השכבה כתובה על `.text-slate-500` ו⛔ אינה רואה את `.dark\:text-slate-500`). 🔴 **והשורש שנרשם מסביר `7/27` בלבד:** **18/27** הם **טוקן שהוא עצמו מתחת ל-AA** (`--text-muted: #9CA89A` = `2.48:1`) ⇒ תיקון שנבנה על ההסבר המקורי היה **נכשל בשקט**. `B-245` תוקן במקום; נרשם `B-251`…`B-254` · `M-008`; `M-007` נמדד מחדש עם שושלת | 27.08 | `09db219` | **אומת-מקור** — ⛔ אין מסך מושפע שאני יכול לפתוח: פרודקשן `/app` מאחורי אימות ו⛔ אין לי רשות להזין אישורים (§12). ⇒ המדידה רצה על **גיליון הסגנון הבנוי** `dist/assets/index-DL1LuIfv.css`, שאומת **זהה-בתים** ל-`HEAD` (`md5 c61335756fc9088423ee1f36f85d104b` לפני ואחרי `npm run build`), עם מחרוזות class ש**חולצו מכנית** מהמקור (חילוץ שנכשל **זורק**, ⛔ לא מנחש), תגי גופן שהועתקו מ-`index.html`, וקינון אמיתי. מדידה: `getComputedStyle` + הרכבת source-over של **כל** שרשרת האבות + CDP `CSS.getPlatformFontsForNode`. **שני viewports הורצו בפועל** ⛔ ולא נגזרו זה מזה. ⚠️ **תיקון-דרך שנתפס תוך כדי:** גרסה ראשונה של המסווג ספרה 7 צמתים לפי **נוכחות** `dark:` ב-class ⛔ במקום לפי הכלל שניצח — תוקן, `18+7+1+1=27` נסגר על המכנה | T2 |
| D-059 | 🔴 **`W1a` · `B-184` — טוקן הסשן ⛔ אינו עוזב יותר את הדפדפן אל Vercel.** `inject()` נקרא **חשוף**, והבייטים ש-Vercel מגישה בונים `{ o: location.href }` **כולל fragment**; `supabaseClient.js:13` רץ ב-implicit flow ⇒ `#access_token` נשלח לצד שלישי. **החלון: 54 יום** (`ee2c00d`, 04.07) ⛔ **ולא 10** — Vercel קדם ל-GA4 ב-**23 יום** ⇒ הוא היה הספק ה**ראשון** שקיבל טוקנים, ו-`INCIDENTS#12` סגר את הערוץ השני והשאיר את זה פתוח **עוד 21 יום**. **התיקון:** `beforeSend` **בונה מחדש** `origin + analyticsPath(pathname)`; `analyticsPath()` מיוצא מ-`consent.js` ⇒ מקור-אמת-אחד לשני הערוצים (§13). ⛔ **רשימה-לבנה ⛔ ולא מסנן.** ⚠️ **שלושה מלכודים שכל אחד מהם היה מייצר תשובה שגויה:** ⓵ הראיה המקורית של `B-184` הייתה **הדפסת קונסולה ב-dev** (`script.debug.js`) — המסקנה שרדה, הראיה הוחלפה · ⓶ החבילה ב-`node_modules` ⛔ **אינה שולחת דבר** ⇒ אודיט שנעצר בה קורא קוד נקי · ⓷ הסקריפט **מתאבד** על `navigator.webdriver` ⇒ **Playwright headless היה סוגר את הכרטיס ירוק**. **`R4` — נמדד ⛔ ולא נערך:** ארבע הטענות ב-`LegalPages.jsx:162` נכונות **אחרי** ההצמדה ו«ספירת עמודים בלבד» ⛔ לא הייתה נכונה לפניה ⇒ ⛔ **אין פריט משפטי לפתוח**, ו⛔ **אף תו ב-`LegalPages.jsx` ⛔ לא שונה.** `INCIDENTS#18` · `DECISIONS` 27.08 · §7 של `docs/plans/PLAN-2026-08-27-b184-vercel-analytics.md`. **§11 — נרשם ⛔ ולא תוקן:** `B-255` · `B-256` · `B-257` · `B-258` (העותק השני של מספרי שכבת השורשים ב-`M-007`, שנתפס **בקומיט-ההמשך** כשה-§8.6 נשארה ירוקה בזמן ש-`METRICS` הצהיר `254` מול `257` מדוד; `M-007` נמדד מחדש עם שושלת, **⛔ והמדידה החוזרת ⛔ אינה התיקון**) | 27.08 | `5255fa8` | 🔴 **אומת בעין** — בדפדפן אמיתי, מארז מבודד, מול `dist/` ה**בנוי** + הבייטים של פרודקשן** (2,495 בתים, sha256 `79bf638d…` — זהה למדוד ב-`AUDIT` §2). ⛔ **לא Playwright · ⛔ לא פרודקשן** (`fetch`/`sendBeacon` מוחלפים ברושם מקומי ⇒ ⛔ שום דבר ⛔ לא עזב את המכונה). כתובת נושאת query **וגם** fragment **וגם** פרמטר בשם `session_key` — שם ש⛔ שום מסנן ⛔ לא היה מכיר: **`o` 146 → 22** (`o_equals_expected: true`, מחושב בזמן ריצה ⛔ ולא קבוע) · `o_has_hash` **true → false** · `o_has_query` **true → false** · **ביקונים 1 → 1** (הספירה ⛔ לא נפגעה) · `pushState` לנתיב לא-מוכר ⇒ **`/other`** · מטען = `{o, sv, sdkn, sdkv, ts}` · `document.cookie` = **0 בתים** · ⛔ **אין שדה `r`** גם עם `document.referrer` לא-ריק הנושא query. **`R2` נצפתה אדומה לפני שהוירקה:** `15/17` על העץ הלא-מתוקן (`❌ 15` · `❌ 16`) ⇒ `17/17` אחרי; `17` — ירוקה מלידה — נבדקה **במוטנט** (`import { track } from "@vercel/analytics"` ב-`consent.js` ⇒ ❌, החזרה ⇒ ✅). **ובנוסף אומת-מקור** — `git show origin/main` מאשר את ההצמדה, את `analyticsPath` המיוצא, **17** קריאות `ok(` ו-`(17 assertions)` ב-`CLAUDE.md`. `verify` **23/23 + build, EXIT=0** | T3 |
| D-050 | `B-173` — **`smoke.yml`: הסרת `--with-deps` (אותו באג-תלייה שתוקן ב-`sentinel.yml`, `D-046`) + הוספת `timeout-minutes: 10`** ‹מזהה נפרד מ-`B-168`, לפי הנחיית ניב 21.08 — ⛔ לא נבלע› | 21.08 | `e1c90b1` (הסרת `--with-deps`) + `7628912` (`timeout-minutes: 10`) | ✅ **אומת-מקור** — כל הריצות נקראו מ-`gh run view`/`gh run list`, ⛔ לא מהעץ המקומי. **המנגנון, ⛔ לא הנחה:** `npx playwright install --with-deps chromium` מפעיל `apt-get` מול `azure.archive.ubuntu.com`; `npx playwright install chromium` (בלי הדגל) מוריד **רק** את הבינארי מ-`cdn.playwright.dev` — אומת ישירות בלוג הצעד (`Downloading Chrome for Testing 149.0.7827.55 ... from https://cdn.playwright.dev/builds/cft/...`), ⛔ אין קריאת `apt` בכלל בקוד המתוקן. **לפני (מ-`AUDIT-B168.md` D6, 4/29 תליות בחלון 30 יום, כולן 19.08, כולן `conclusion:"cancelled"` על צעד ה-install):** `32216081147` (3ש'47ד') · `32233540507` (4ש'52ד') · `32295545808` (6ש' — פגע בתקרת ברירת-המחדל 360 דק' של GitHub בדיוק, כי לא היה `timeout-minutes` בכלל) · `32231957189` (19ד'50ש'). **אחרי — 6 ריצות `workflow_dispatch` רצופות (3 לפני הוספת ה-timeout, 3 אחריו), 6/6 `success`, 0 `cancelled`:** צעד ה-install נמדד **10-11 שנייה** בכל השש (⛔ לא בי-מודלי — טווח יציב, כי `apt`/`azure.archive.ubuntu.com` הוסרו מהמסלול ולא רק נחסמו בזמן), ומשך ה-job הכולל 35-45 שנייה. ⚠️ **ריצה בודדת ⛔ אינה ראיה (הנחיית ניב, בי-מודליות אומרת שהצלחה מהירה קיימת גם תחת קוד באגי) — לכן 6, ⛔ לא 1.** ✅ **ספריות ה-runtime אומתו נוכחות בלי `apt`/`--with-deps`:** ⛔ לא נבדק לוג `apt` (אין עוד לוג `apt` לבדוק — הצעד לא מריץ אותו) אלא הופעלה כרום בפועל והרצו הבדיקות — **8/8 בדיקות Playwright עברו** בכל אחת מהריצות, מה שאינו אפשרי אם ספריית runtime חסרה (Chromium קורס בהפעלה). **גזירת `timeout-minutes: 10`, ⛔ לא הועתק מ-`sentinel.yml`'s 12 (הנחיית ניב במפורש):** סכימת מקסימום נצפה-בפועל על 60 ריצות מוצלחות היסטוריות לכל שאר הצעדים (`checkout`/`setup-node`/`npm ci` = 8ש'/8ש'/11ש' מקסימום) + צעד "Wait for this commit's deploy" שכבר אוכף `TIMEOUT_S=180` פנימי משלו ויוצא `exit 1` ⛔ ולא נתקע (מקסימום נצפה בפועל: 12ש', 180ש' מעולם לא נפגע) + צעד ה-install המתוקן (מדוד 10-11ש' × 3 בנפרד) ⇒ **סכום מקסימום מעשי ≈110 שניות, × מרווח ~5 ליום-רשת גרוע** ⛔ בלי להשאיר חלון-עיוורון של שעות כמו זה שנסגר. **חלוקת האחריות מ-`smoke.yml` (⛔ לא הוכפלה מ-`sentinel.yml`):** ⚠️ נבדק במפורש שהצעד המתוקן זהה במנגנון (`cdn.playwright.dev` בלבד, לא `apt`) — ⛔ לא הונח שזהה. **שערים:** `npm run verify` — `test:registry` **30/30**, 23 חוליות + `build` ירוקים (הקובץ שנגע הוא `.github/workflows/*.yml` בלבד, ⛔ לא קוד אפליקציה — §12/§15 T1). ⛔ **T1 (§15, חמש שאלות, כולן «לא»):** הפיכות — כן, `git revert`; אמון-משתמש — לא נוגע במסך/כסף/סטטיסטיקה; אבטחה — לא נוגע `auth`/RLS/secrets; רוחב-קוד — קובץ workflow יחיד, ⛔ לא קוד אפליקציה; ודאות — כל טענה נמדדה ישירות (לוג הצעד, `gh run view`, 6 ריצות). `docs/STATE.md`/`docs/BACKLOG.md`/`docs/DONE.md` עודכנו יחד באותו קומיט; שורת `DONE` זו והמצבה ב-`BACKLOG` נכנסות **בקומיט שאחרי `7628912`** — קומיט אינו יכול לשאת את ה-hash של עצמו (כלל `4.1`) | T1 |

✅ **`D-030` נכנס כאן בקומיט שאחריו — הפעם הרביעית ברציפות.** ⚠️ **הדפוס חוזר כי
הכלל נכון, ⛔ לא כי משהו שכח:** קומיט אינו יכול לשאת את ה-hash של עצמו, ו-`--amend`
אסור (§5). ⛔ אל "תתקן" זאת.

✅ **`D-028` ו-`D-029` נכנסו כאן בקומיט שאחריהם, מאותה סיבה בדיוק.** ⚠️ **זו
הפעם השלישית ברציפות** (`D-023` · `D-024` · וכעת שניהם) — ⛔ לא תקלה חוזרת אלא
צורת העבודה שכלל `4.1` מכתיב: **גל שסוגר את עצמו נרשם תמיד בקומיט הבא.**

✅ **`D-023` נכנס כאן בגל ג׳ (הקומיט הבא שנגע במרשם), ⛔ לא בקומיט של עצמו.**
השער `4.1` תפס זאת בריצתו הראשונה: **קומיט אינו יכול לשאת את ה-hash של עצמו**,
ו-`--amend` אסור (`CLAUDE.md` §5). ⚠️ **הכלל לא רוכך כדי לעבור** — זו בדיוק
ההתנהגות שהשער נבנה בשבילה, והשורה הזו היא ההוכחה שהמנגנון סגר מעגל בפועל.

✅ **גל ג׳ נכנס כ-`D-024` בגל השורשים (13.08)** — שני התנאים שעצרו אותו נסגרו,
כל אחד בנפרד: (1) ה-hash `3b1a1cf` נודע אחרי הקומיט, כלל `4.1`; (2) **הוא אומת
בעין 13.08** מול צילומי מסך, `C-019`. ⚠️ **הבדיקה מצאה ליקוי** — `B-119` — ולכן
היא בדיקה, ⛔ לא טקס: אילו הייתה עוברת חלק, לא היה לה כוח ראייתי.

---

## ⚠️ מה **לא** אומת בעין

- **`D-009` אופק העסקה** — נצפו 140 אסרציות ירוקות. הספים משנים כמה משתמשים רואים את הפיצ'ר; מספר ירוק אינו מסך.
- **`D-011` גל א׳ מטבע** — נצפו 105 אסרציות ירוקות. **הבאג נשאר חי** ב-`posSize` ובתשעה אתרי סכום-בחשבון. 105 ירוקות וגל שלא עבד — `C-001` נולד מכאן.
- ~~**גל ג׳ מטבע**~~ — ✅ **אומת בעין 13.08 ונרשם כ-`D-024`.** ⚠️ **הוא נשאר ברשימה הזו כתקדים, ⛔ לא כחוב:** הוא היה 216/216 ירוק במשך יממה שלמה מעל מסך שאיש לא ראה, וה-⛔ שחסם אותו הוא מה שהחזיק אותו פתוח עד שנראה — ואז נמצא בו `B-119`.
- 🔴 **`D-043` `B-143` — אימות **חלקי**, והחלק החסר הוא המסך.** ✅ **ה-PDF וה**ציר** אומתו בעין** לפני/אחרי מאותו `exportMonthlyPDF`, ושני מסלולי ה-SVG חושבו **ביד** והתאימו תו-בתו (`C-029`). ⛔ **אבל מסך האפליקציה החי ⛔ לא נפתח בדפדפן בגל הזה** — 28 אתרי ה-`dispCcy`, מחיקת `statsCcy` ו-`menteeStats` נשענים על **מקור + אסרציות**, ⛔ לא על עין. ⚠️ **הסיבה מבנית ⛔ ולא עצלות:** פתיחתו דורשת יומן **מעורב-מטבע** של משתמש **מאומת** בפרודקשן, ו⛔ אין בידי Code ליצור אותו. ⇒ `C-023` נותרה על מצבה מ-15.08, וה**חוב הזה חי** — בדיוק כמו «גל ג׳» למעלה, שהיה 216/216 ירוק מעל מסך שאיש לא ראה ואז נמצא בו `B-119`.
- 🔴 **`D-047` `B-144` — המסך החי ⛔ לא נפתח, וזו הפעם השנייה ברציפות שהחסם **מבני** ⛔ ולא עצלות.** `MixedCurrencyBanner` ⛔ אינו מיוצא ⇒ ⛔ אינו ניתן לטעינה לבד, והמסך דורש סשן מאומת ⇒ שתי הדרכים היחידות אליו הן אישורים (אסור) או זריעה חד-צדדית ליומן פרודקשן (⛔ לא). ✅ **מה כן:** `origin/main` נקרא ישירות (⛔ לא העץ המקומי) · 3 מוטנטים נצפו אדומים והוחזרו · מדידת דפדפן על המודולים ה**נשלחים** ⇒ לפני `+$200.00 (1)` / אחרי `(מוסתר)` · 23 חוליות + build. ⏸️ **`C-030` חצי (א) פתוח** — ההוראות מוכנות, הצילום על ניב, וה**צילום הוא «אחרי» בלבד** (⛔ אין זוג: ה«לפני» כבר נמדד ו⛔ לא ייטען כצילום). 🔴 **וחצי (ב) ⛔ אינו «טרם בוצע» אלא ⛔ בלתי-נגיש** — 324/324 צירופים ⇒ `{"USD"}` ⇒ `mixedCurrency` ⛔ אינו יכול לירות לאף משתמש ⇒ ⛔ **«הבאנר נעדר» על המסך ⛔ אינו מעיד שהגזירה נכונה.** ⇒ `B-172`

⚠️ **שלושת גלי המטבע הם שלושה פריטים נפרדים, ⛔ לא אחד:** `D-011` (גזירת מטבע
הנייר בזמן קריאה) · `D-012` (המרה לפני החלוקה ב-`sizePosition`) · **גל ג׳**
(9/10 אתרי סכום-בחשבון + עיגול בתצוגה). ⛔ אל תאחד אותם בדיווח — `D-011` הוכרז
סגור בעוד הבאג חי, ואיחוד היה מסתיר בדיוק את הפער הזה.

⛔ **אסרציה ירוקה אינה אימות בעין.** כל גל שנוגע במספר פונה-משתמש חייב פריט ב-`docs/CHECKS.md` לפני שהוא נסגר.
