# INBOX — נקודת קליטה אחת

> ⚠️ **קליטה בלבד — ⛔ פריט לא נשאר כאן מעבר לסשן אחד.**
> **בתחילת כל סשן: triage.** כל שורה הופכת ל-`B-` ב-`docs/BACKLOG.md` עם תג
> `‹R-n›` ועם מקור, **או** נדחית עם נימוק ב-`docs/DECISIONS.md`.
> ⛔ **אין החלטות כאן, ואין הנמקות.** מי · מה · מאיפה · מתי, ותו לא.
>
> **למה הקובץ הזה קיים:** ממצא שנולד בדיסקורד, במייל או בשיחה חי בראש של ניב
> עד שהוא נשכח. ⚠️ **זה `R-5` בדיוק** — מצב קריטי באחסון נדיף.
>
> **זרימה מהטלפון:** ניב אומר "תוסיף ל-INBOX: X" ⇒ שורה + push. ⛔ שום דבר מעבר.
> ⚠️ **סשן אחד על הריפו** — ראה `docs/AGENT-CHANNELS.md`.
>
> כל שורה נושאת **תאריך** ו**מקור**. נאכף מכנית ב-`test:registry` §9.

---

| תאריך | מה | מקור |
|-------|-----|-------|
| 15.08 | **Sentinel S1 · סלקטור עמום.** `locator('table.w-full.text-xs')` תופס **3 טבלאות ב-`SwingEdge_App.jsx`** (`:4377` · `:4557` · `:4884`) ועוד **3 ב-`AdminPanel.jsx`** ⇒ strict mode violation כשיותר מאחת נראית. ⛔ **אינה תקלת פרודקשן.** נמדד ב-HEAD `be41b78` — קוד `B-142` טרם נדחף ⇒ ⛔ אינו קשור לגל. תיקון מוצע: `data-testid` ייעודי לטבלת היומן | Sentinel S1 · 15.08 |
| 15.08 | **Sentinel · אבחון מומצא.** ההתראה כתבה "היומן לא רונדר — ייתכן שגיאת JS" בעוד השגיאה עצמה (`resolved to 2 elements`) **מוכיחה שהיומן כן רונדר**. בנוסף המליץ **rollback על פרודקשן בריא** וסימן "סיכון נמוך". הדרישה: כשהכשל הוא strict mode / timeout / selector — הסוכן **מדווח מה נמדד** ו⛔ אינו מנחש סיבה; rollback ⛔ אינו מוצע כשה-HEAD לא זז | Sentinel · 15.08 |

⚠️ **2 שורות ממתינות ל-triage בסשן הבא** — רישום בלבד, ⛔ אפס תיקון בפרומפט
הזה. שתיהן מאותה ריצת Sentinel, ⛔ אך ⛔ אינן אותו פריט: הראשונה היא **פגם
בכלי המדידה**, השנייה היא **הסוכן ממציא סיבה** — ‹R-2› מובהק, ובדיוק המחלקה
שבה ברירת מחדל מומצאת בלתי-ניתנת להבחנה ממדידה אמיתית.
