# INBOX — נקודת קליטה אחת

> ⚠️ **קליטה בלבד — ⛔ פריט לא נשאר כאן מעבר לסשן אחד.**
> **בתחילת כל סשן: triage.** כל שורה הופכת ל-`B-` ב-`docs/BACKLOG.md` עם תג
> `‹R-n›` ועם מקור, **או** נדחית עם נימוק ב-`docs/DECISIONS.md`.
> ⛔ **אין החלטות כאן, ואין הנמקות.** מי · מה · מאיפה · מתי, ותו לא.
>
> **למה הקובץ הזה קיים:** ממצא שנולד בדיסקורד, במייל או בשיחה חי בראש של ניב
> עד שהוא נשכח. ⚠️ **זה `R-5` בדיוק** — מצב קריטי באחסון נדיף.
>
> **זרימה מהטלפון:** ניב אומר "תוסיף ל-INBOX: X" ⇒ שורה + push. ⛔ שום דבר מעבר.
> ⚠️ **סשן אחד על הריפו** — ראה `docs/AGENT-CHANNELS.md`.
>
> כל שורה נושאת **תאריך** ו**מקור**. נאכף מכנית ב-`test:registry` §9.

---

| תאריך | מה | מקור |
|-------|-----|-------|
| 20.08 | ⟪🟠 שדות מתחת ל-40% חציוני⟫ זז **1 מתוך 7** (17.08) ⇒ **4 מתוך 12** (20.08). ⛔ **אילו 4 השדות, ומה זז ב**מכנה** (7⇒12 שדות שנמדדו)?** ⚠️ **שאלת מוצר, ⛔ לא באג** — `user-analytics.mjs:728-731` סופר רק שדות עם `median !== null`, ולכן **גם המונה וגם המכנה** יכולים לזוז משתי סיבות שונות: שדה חדש שנכנס למדידה, או איכות מילוי שירדה. ⛔ **⛔ לא נמדד — ⛔ אין כאן הכרעה** | `scripts/user-analytics.mjs:728-731` · דוח 17.08 מול 20.08 · ניב |
| 21.08 | `smoke.yml` נושא את אותו באג-תלייה של `npx playwright install --with-deps chromium` שתוקן ב-`sentinel.yml` (`D-046`) — **אך בלי `timeout-minutes` בכלל**, כך שתלייה יכולה לרוץ עד ברירת-המחדל של GitHub (360 דק') במקום 12. נמדדו **4 תליות** בחלון 30 יום (כולן 19.08), כולן `conclusion:"cancelled"` ⇒ בלתי-נראות לכל שכבת התראה כולל `watchdog.yml` (לא-יעיל בפועל בגלל תדירות-push גבוהה של Smoke Tests). ⛔ לא תוקן — טרם הוכרע כיוון | `docs/audits/AUDIT-B168.md` D6 · מדידת `gh api` על 30 יום · Code |
| 23.08 | **עובדת-מוצר, ⛔ לא תקלה:** 0 עסקאות חדשות ב-**70.4 שעות** (`max("createdAt")=2026-08-20T17:24:42Z`) · 0 הרשמות ב-**77.1 שעות** (`max(created_at)=2026-08-20T10:42:41Z`, `auth.users`). נמדד ישירות מ-Supabase 23.08T15:48Z, ⛔ ללא פרשנות | Supabase read-only · אבחון `docs/audits/AUDIT-scan-2308.md` M3 · ניב |

⏳ **שלוש שורות ממתינות לטריאז'** — ⛔ אינן שורדות את הסשן הבא (§14).

**טריאז' 16.08:** שתי השורות שנקלטו 15.08 (404 של `JetBrains Mono` · מזהה בדיקה
בפרוזה) **עברו** ל-`docs/BACKLOG.md` כ-`B-150` ‹R-4› ו-`B-151` ‹R-3› —
מחיקה והוספה, ⛔ לא העתקה (§14). הן חיות שם, ⛔ ובשום מקום אחר.
⚠️ **שתיהן נרשמו עם כיוון ⛔ שלא הוכרע** — הצעה שנכתבת כהכרעה היא בדיוק `R-2`.

**קדם לכן, טריאז' 15.08:** שתי השורות שנקלטו באותו יום (סלקטור עמום · אבחון
מומצא) עברו באותה דרך ל-`B-146` ו-`B-147`.
