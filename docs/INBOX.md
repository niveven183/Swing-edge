# INBOX — נקודת קליטה אחת

> ⚠️ **קליטה בלבד — ⛔ פריט לא נשאר כאן מעבר לסשן אחד.**
> **בתחילת כל סשן: triage.** כל שורה הופכת ל-`B-` ב-`docs/BACKLOG.md` עם תג
> `‹R-n›` ועם מקור, **או** נדחית עם נימוק ב-`docs/DECISIONS.md`.
> ⛔ **אין החלטות כאן, ואין הנמקות.** מי · מה · מאיפה · מתי, ותו לא.
>
> **למה הקובץ הזה קיים:** ממצא שנולד בדיסקורד, במייל או בשיחה חי בראש של ניב
> עד שהוא נשכח. ⚠️ **זה `R-5` בדיוק** — מצב קריטי באחסון נדיף.
>
> **זרימה מהטלפון:** ניב אומר "תוסיף ל-INBOX: X" ⇒ שורה + push. ⛔ שום דבר מעבר.
> ⚠️ **סשן אחד על הריפו** — ראה `docs/AGENT-CHANNELS.md`.
>
> כל שורה נושאת **תאריך** ו**מקור**. נאכף מכנית ב-`test:registry` §9.

---

| תאריך | מה | מקור |
|-------|-----|-------|

⛔ **הפנקס ריק — אפס שורות ממתינות.**

**טריאז' 26.08 — שלוש השורות (20.08 · 21.08 · 23.08) הוכרעו, ⛔ כל אחת אחרת:**

- **20.08** ⟪🟠 שדות מתחת ל-40% חציוני: `1/7` (17.08) ⇒ `4/12` (20.08)⟫ ⇒ **`B-244`**
  ‹R-1›. ⚠️ **גם המונה וגם המכנה זזו**, ולכן «1⇒4» ⛔ אינו מודד הידרדרות:
  `user-analytics.mjs:728-731` סופר רק שדות עם `median !== null`, ולכן שדה חדש
  שנכנס למדידה מגדיל את שניהם. ⛔ **לא הוכרע כיוון — נרשם כשאלה, ⛔ לא כבאג.**
- **21.08** ⟪`smoke.yml` — `--with-deps` בלי `timeout-minutes`⟫ ⇒ **כבר עבר טריאז'
  ב-21.08 ופשוט ⛔ לא נמחק מכאן.** ⚠️ **נמדד ⛔ ולא הונח:** `smoke.yml:30` נושא
  `timeout-minutes: 10` ו-`:38` הוא `npx playwright install chromium` **בלי**
  `--with-deps` ⇒ החצי הראשון **סגור** ב-`B-173` → **`D-050`** (`e1c90b1`).
  **החצי השני — ש-`cancelled` בלתי-נראה לשכבות ההתראה — פתוח ב-`B-168`**, ⛔ לא אבד.
- **23.08** ⟪0 עסקאות ב-70.4ש׳ · 0 הרשמות ב-77.1ש׳⟫ ⇒ **`M-004`/`M-005`
  ב-`docs/METRICS.md`.** 🔴 **⛔ אינה משימה ו⛔ אינה נדחית — היא מדידה**, והחוזה
  של שבעת הקבצים ⛔ לא היה לה בית. ⚠️ **וזה ⛔ לא היה תיאורטי:** `STATE` **ציטט
  אותה מ-`INBOX`** — קובץ שהחוזה שלו מצהיר «⛔ פריט לא נשאר כאן מעבר לסשן אחד»
  ⇒ ציטוט שמצביע לקובץ שמוחק את עצמו, `R-5`. ראה `DECISIONS` 26.08.

**טריאז' 16.08:** שתי השורות שנקלטו 15.08 (404 של `JetBrains Mono` · מזהה בדיקה
בפרוזה) **עברו** ל-`docs/BACKLOG.md` כ-`B-150` ‹R-4› ו-`B-151` ‹R-3› —
מחיקה והוספה, ⛔ לא העתקה (§14). הן חיות שם, ⛔ ובשום מקום אחר.
⚠️ **שתיהן נרשמו עם כיוון ⛔ שלא הוכרע** — הצעה שנכתבת כהכרעה היא בדיוק `R-2`.

**קדם לכן, טריאז' 15.08:** שתי השורות שנקלטו באותו יום (סלקטור עמום · אבחון
מומצא) עברו באותה דרך ל-`B-146` ו-`B-147`.
