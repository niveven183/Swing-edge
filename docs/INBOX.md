# INBOX — נקודת קליטה אחת

> ⚠️ **קליטה בלבד — ⛔ פריט לא נשאר כאן מעבר לסשן אחד.**
> **בתחילת כל סשן: triage.** כל שורה הופכת ל-`B-` ב-`docs/BACKLOG.md` עם תג
> `‹R-n›` ועם מקור, **או** נדחית עם נימוק ב-`docs/DECISIONS.md`.
> ⛔ **אין החלטות כאן, ואין הנמקות.** מי · מה · מאיפה · מתי, ותו לא.
>
> **למה הקובץ הזה קיים:** ממצא שנולד בדיסקורד, במייל או בשיחה חי בראש של ניב
> עד שהוא נשכח. ⚠️ **זה `R-5` בדיוק** — מצב קריטי באחסון נדיף.
>
> **זרימה מהטלפון:** ניב אומר "תוסיף ל-INBOX: X" ⇒ שורה + push. ⛔ שום דבר מעבר.
> ⚠️ **סשן אחד על הריפו** — ראה `docs/AGENT-CHANNELS.md`.
>
> כל שורה נושאת **תאריך** ו**מקור**. נאכף מכנית ב-`test:registry` §9.

---

| תאריך | מה | מקור |
|-------|-----|-------|
| 18.09 | ⟪בדיקה ידנית «עברה» ואז נמדדה כ⛔-הופעלה — **מופע רביעי בסבב**: `C-047` · `C-049` · `C-054`⓶ · `C-055`. ב-`C-055` שלוש הצהרות נשלחו כאימות וממפות ל-**⓸ בלבד**, כלומר נספרו **הצהרות** במקום **סעיפים** (`1/5`, ⛔ `3/5`)⟫ | סריקה סביבתית §11 בגל סגירת החסמים · 18.09 |
| 20.09 | 🔴 ⟪**סיסמת ה-QA נחשפה בצילום מסך שנשלח בצ'אט.** ההמלצה: ניב מחליף אותה בחשבון ה-QA **ומעדכן את `SENTINEL_QA_PASSWORD` ב-secrets** — שניהם באותו חלון, אחרת הסנטינל המאומת נופל בהתחברות. ⛔ **Code ⛔ נגע בה ו⛔ ייגע** — `CLAUDE.md` §12: `gh secret set` הוא ניב בלבד. ⚠️ **והסיסמה ⛔ מצוטטת כאן** — קובץ מרשם בריפו **ציבורי**⟫ | ניב, בצ'אט, בגל שער ההידרציה · 20.09 |
| 20.09 | ⟪**`sentinel.yml` ⛔ ירה `8` סלוטים רצופים** — cron הוא `'20 * * * *'` + `'50 * * * *'` (`:19-20`), הריצה האחרונה היא `35495938336` ב-`2026-09-20T07:05:30Z`, והשעה בעת המדידה `11:00:03Z` ⇒ `07:20 · 07:50 · 08:20 · 08:50 · 09:20 · 09:50 · 10:20 · 10:50` ⛔ נפתחו. ⚠️ **ו⛔ «Actions מושבת»** — `Analyst` המתוזמן **כן** רץ ב-`10:38:40Z` באותו ריפו ⇒ ההשמטה ייחודית ל-`sentinel.yml`. ⚠️ **ומה שזה חוסם הוא מדד הסגירה של `B-355`**, שהוא ריצה מתוזמנת של סלוט `:50`⟫ | מדידה ב-`gh run list` בעת ניסיון לקיים את דרישה ג׳ · 20.09 |
| 21.09 | ⟪**בתור: `npm run test:auth`** — דפדפן headed שמכין תנאים לכל פריט `C-` ועוצר **לפני** אימות-עין. התוכנית **תוכננה בשיחה ו⛔ נשלחה ל-Code** עדיין. מטרה: לפתוח `7` פריטים חסומים — `C-046`·`C-049`·`C-050`·`C-052`·`C-053`·`C-056`·`C-057`. ⛔ **`B-` ⛔ נפתח** — זו **משימת תכנון בתור**, ⛔ ממצא⟫ | ניב, בצ'אט · 21.09 |
| 21.09 | ⟪**`triage.yml` ⛔ מאזין ל-`Sentinel`** — `workflow_run.workflows` = `Smoke Tests`·`Build`·`Supabase Backup` בלבד ⇒ אדום של הסנטינל ⛔ נחקר אוטומטית. הצעה: הוספה — **אבחון בלבד, ⛔ PR**. תנאים: ⓵ **רק במעבר ירוק⇒אדום** (אחרי ⓕ ~`30` ריצות/יום ⇒ ללא סינון `30` קריאות API + `30` מיילים ביום בתקלה ממושכת ⇒ ערוץ מושתק) · ⓶ סיבה = ציטוט מהלוג, ⛔ ניסוח (מחלקת `B-334`/`C-051`) · ⓷ **אחרי ⓕ**, ⛔ לפני. Antigravity (Google) נשקל ⇒ ⛔ נדרש: הפונקציה קיימת, וסוכן כותב שני מפר session-יחיד⟫ | ניב, בצ'אט · 21.09 |
| 21.09 | ⟪**מעקב `B-318` — לבדוק כשמצטברות `50` ריצות `schedule`.** נמדד 21.09: `12/12` ריצות (20.09 12:38Z→21.09 11:08Z) — `0/5` סלוטי `:20` עם `auth=1`, `7/7` סלוטי `:50` עם `auth=1`. ⛔ נסגר על `N=12` — ניב ביקש להמתין ל-`N=50` כמו מדידת 11.09 המקורית (שגילתה `0/50` פגיעות כפולות, אך עם שיעור נשירה גבוה שהסתיר את הבאג). **פעולה בצ'אט הבא:** ספור ריצות `schedule` חדשות מ-21.09 11:08Z; אם `N≥50` — מדוד `auth=1` על `:20` ו-`:50` בנפרד, דווח לניב, ואז הסר שורה זו. ⛔ B-275 לפני שזה נסגר (החלטת ניב 11.09, ראה B-318)⟫ | ניב, בצ'אט · 21.09 |
| 22.09 | ⟪**`probe:eye` בחניה** — `PLAN-2026-09-21-test-auth-IMPL.md` אושר עקרונית ע"י ניב. חסום: ההרנס מתחבר לחשבון ה-QA, והעותק המקומי של הסיסמה מחזיר `invalid_credentials` (CI עובר). ניב החליט ⛔ לאפס כרגע. + `C-050` (א)/(ג) — ניב: למזג בקומיט המימוש⟫ | ניב, בצ'אט · 22.09 |

**טריאז' 13.09 — השורה מ-11.09 הוכרעה:** ⟪סיווג §15 דווח **אחרי** תחילת העבודה
ולא לפניה — גל `B-315` (`2bdfe0e`) נקב «`T2`» רק בדיווח הסיום⟫ ⇒ **נדחית ל-`B-`
ונרשמת כתבנית ב-`DECISIONS` 13.09** — מחיקה מכאן, ⛔ העתקה (§14). ⚠️ **המנייה
היא `2`, ⛔ `1`** — הגל הזה עצמו (`B-320`) הוא המופע השני. ⛔ **⛔ נפתח `B-`
בכוונה:** הכלל כבר מנוסח **מודגש** ב-`CLAUDE.md` §15 ⇒ `B-` הוא עותק שני ‹R-6›,
ו⛔ קיים מדד סגירה שאינו הצהרה ⇒ ‹R-4›. **הנימוק המלא והנפסלים — `DECISIONS`.**

**טריאז' 09.09 — השורה מ-04.09 הוכרעה:** ⟪`ec8c832` משאיר `verify` אדום
בהיסטוריה; `bisect` עתידי ייתקל בשרשרת אדומה שאינה הבאג שהוא מחפש⟫ ⇒ **`B-311`**
‹R-4› — **מחיקה מכאן והוספה ל-`BACKLOG`, ⛔ לא העתקה** (§14). ⚠️ **השורש נרשם
בכיוון ההפוך במפורש** — `R-4` הוא שומר ירוק על דבר מת, וזה שומר **אדום** על דבר
חי; אותה מחלקה, כיוון הפוך, ⛔ ולא הולבש בשקט. ⛔ **⛔ לא תוקנה ו⛔ לא נעשה
rebase** — §5 אוסר כתיבה מחדש של היסטוריה שנדחפה.

**טריאז' 26.08 — שלוש השורות (20.08 · 21.08 · 23.08) הוכרעו, ⛔ כל אחת אחרת:**

- **20.08** ⟪🟠 שדות מתחת ל-40% חציוני: `1/7` (17.08) ⇒ `4/12` (20.08)⟫ ⇒ **`B-244`**
  ‹R-1›. ⚠️ **גם המונה וגם המכנה זזו**, ולכן «1⇒4» ⛔ אינו מודד הידרדרות:
  `user-analytics.mjs:728-731` סופר רק שדות עם `median !== null`, ולכן שדה חדש
  שנכנס למדידה מגדיל את שניהם. ⛔ **לא הוכרע כיוון — נרשם כשאלה, ⛔ לא כבאג.**
- **21.08** ⟪`smoke.yml` — `--with-deps` בלי `timeout-minutes`⟫ ⇒ **כבר עבר טריאז'
  ב-21.08 ופשוט ⛔ לא נמחק מכאן.** ⚠️ **נמדד ⛔ ולא הונח:** `smoke.yml:30` נושא
  `timeout-minutes: 10` ו-`:38` הוא `npx playwright install chromium` **בלי**
  `--with-deps` ⇒ החצי הראשון **סגור** ב-`B-173` → **`D-050`** (`e1c90b1`).
  **החצי השני — ש-`cancelled` בלתי-נראה לשכבות ההתראה — פתוח ב-`B-168`**, ⛔ לא אבד.
- **23.08** ⟪0 עסקאות ב-70.4ש׳ · 0 הרשמות ב-77.1ש׳⟫ ⇒ **`M-004`/`M-005`
  ב-`docs/METRICS.md`.** 🔴 **⛔ אינה משימה ו⛔ אינה נדחית — היא מדידה**, והחוזה
  של שבעת הקבצים ⛔ לא היה לה בית. ⚠️ **וזה ⛔ לא היה תיאורטי:** `STATE` **ציטט
  אותה מ-`INBOX`** — קובץ שהחוזה שלו מצהיר «⛔ פריט לא נשאר כאן מעבר לסשן אחד»
  ⇒ ציטוט שמצביע לקובץ שמוחק את עצמו, `R-5`. ראה `DECISIONS` 26.08.

**טריאז' 16.08:** שתי השורות שנקלטו 15.08 (404 של `JetBrains Mono` · מזהה בדיקה
בפרוזה) **עברו** ל-`docs/BACKLOG.md` כ-`B-150` ‹R-4› ו-`B-151` ‹R-3› —
מחיקה והוספה, ⛔ לא העתקה (§14). הן חיות שם, ⛔ ובשום מקום אחר.
⚠️ **שתיהן נרשמו עם כיוון ⛔ שלא הוכרע** — הצעה שנכתבת כהכרעה היא בדיוק `R-2`.

**קדם לכן, טריאז' 15.08:** שתי השורות שנקלטו באותו יום (סלקטור עמום · אבחון
מומצא) עברו באותה דרך ל-`B-146` ו-`B-147`.
