# INBOX — נקודת קליטה אחת

> ⚠️ **קליטה בלבד — ⛔ פריט לא נשאר כאן מעבר לסשן אחד.**
> **בתחילת כל סשן: triage.** כל שורה הופכת ל-`B-` ב-`docs/BACKLOG.md` עם תג
> `‹R-n›` ועם מקור, **או** נדחית עם נימוק ב-`docs/DECISIONS.md`.
> ⛔ **אין החלטות כאן, ואין הנמקות.** מי · מה · מאיפה · מתי, ותו לא.
>
> **למה הקובץ הזה קיים:** ממצא שנולד בדיסקורד, במייל או בשיחה חי בראש של ניב
> עד שהוא נשכח. ⚠️ **זה `R-5` בדיוק** — מצב קריטי באחסון נדיף.
>
> **זרימה מהטלפון:** ניב אומר "תוסיף ל-INBOX: X" ⇒ שורה + push. ⛔ שום דבר מעבר.
> ⚠️ **סשן אחד על הריפו** — ראה `docs/AGENT-CHANNELS.md`.
>
> כל שורה נושאת **תאריך** ו**מקור**. נאכף מכנית ב-`test:registry` §9.

---

| תאריך | מה | מקור |
|-------|-----|-------|
| 15.08 | **404 על JetBrains Mono.** נצפה: `https://fonts.gstatic.com/s/jetbrainsmono/v24/...woff2` → **404**. `index.html:55` טוען את הפונט מ-Google Fonts; gstatic **מסובב hash** בקובצי `woff2` ⇒ גיליון סגנון ממוטמח מצביע ל-URL מת. **ההשלכה:** JetBrains Mono הוא תקן העיצוב לכל ספרה באפליקציה ⇒ **כל המספרים נופלים לפונט חלופי** — שינוי ויזואלי שקט, ⛔ לא שגיאה חוסמת. **חלף מעצמו ב-17:03 UTC** ⇒ ריצוד, ⛔ לא מצב יציב. ⚠️ קשור ללקח קיים: `document.fonts.check()` לבדו ⛔ אינו אימות פונט — נדרשת מדידת **רוחב גליף** בפועל. כיוונים לגל ייעודי, ⛔ **לא הוכרע:** self-host · fallback מוצהר · אימות רוחב-גליף ב-Sentinel | Sentinel · 15.08 16:00 UTC |
| 15.08 | **מזהה בדיקה שיושב בפרוזה ⛔ אינו נראה לשער.** נמדד בגל `B-149`: `C-024` מופיע ב-`docs/DONE.md:23` בשורת פרוזה (`> נבדק ב-C-024`) ⛔ ולא בתא של שום שורת טבלה. §11.2 קורא **תאים** ⇒ האזכור הזה ⛔ אינו נראה לו כלל, לא לטובה ולא לרעה. **ההשלכה:** ראיה שנכתבה בפרוזה נקראת ע"י אדם כאילו היא מתועדת, ובעוד השער ⛔ אינו יכול לאמת אותה — פער שקט בין מה שנקרא למה שנאכף. ⛔ **לא תוקן בגל `B-149`** — תיקון היה מחייב עריכת `docs/DONE.md`, שנאסרה במפורש. כיוונים, ⛔ **לא הוכרע:** העברת האזכור לתא · אסרציה שסופרת מזהי `C-` בפרוזה · השארה מודעת עם הצהרה | `B-149` · 15.08 |

⚠️ **2 שורות ממתינות ל-triage** — ⛔ טרם קיבלו `B-`, ⛔ וטרם תוקנו. **טריאז' בסשן הבא.**

שתי השורות שנקלטו 15.08 (סלקטור עמום · אבחון מומצא) עברו triage בגל השומרים
ו**נמחקו מכאן** — מחיקה והוספה, ⛔ לא העתקה (§14). הן חיות כעת כ-`B-146`
ו-`B-147` ב-`docs/BACKLOG.md`, ⛔ ובשום מקום אחר.
