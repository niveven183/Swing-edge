# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 16.08 · אחרי סגירת `B-129`+`B-152`+`B-153` (`D-040`)

---

## 1 · `B-143` — משפחת סמל-המצרף

- **למה עכשיו:** `B-129` ירד מהרשימה ⇒ זהו הפריט **הבר-ביצוע היחיד** מבין השלושה — `B-148` חסום על הכרעת ניב, ו-`B-154` דורש בחירת צרכן. ⚠️ `B-142` השאיר את `stats.currentEquity` **מזוהם ובלי צרכן**, ואותו מספר עדיין מודפס תחת סמל אחר ב-PDF.
- **דרך יישום:** אותה הפרדה שנקנתה ב-`B-142` — כל מחובר שאינו ניתן להמרה מוחרג ו**נספר**, ⛔ לא מוסתר.
- **מה חוסם:** אין. ⚠️ `B-144` יושב באותו קובץ — להכריע אם נכנס לאותו גל.
- **מדד סגירה:** `C-023` ירוק **גם ב-PDF וגם בכותרת הסטטיסטיקה**.

## 2 · `B-154` — לתעודת המקור אין צרכן

- **למה עכשיו:** `B-129` קנה את ההבחנה בין תווית שנמדדה לתווית שנוחשה ו⛔ לא חיווט אותה — **0/22 אתרי `isEvidenceBacked` בקוד המוצר**. ⚠️ **שדה שנכתב ואיש אינו קורא מתיישן בשקט** — בדיוק דפוס `B-068`.
- **דרך יישום:** צרכן **אחד** ראשון, בשפה של `ccyUnverified` (`C-027`) — ⛔ לא חוסם, ⛔ לא מספר חדש.
- **מה חוסם:** 🔴 **הכרעת ניב — איזה צרכן.** ⚠️ וכל צרכן חייב לטפל ב-`null` כמצב **שלישי** (67/67 השורות הישנות).
- **מדד סגירה:** אתר קריאה אחד בקוד המוצר + אסרציה שנצפית אדומה על מצב ה-`null`.

## 3 · `B-148` — ספי `Watchdog` נכתבים מחדש

- **למה עכשיו:** גל השומרים **נעצר** בבלוק 4 כשהמדידה הפריכה את ההנחה שעליה נשען הסף ⇒ **אפס ספים שונו**. ⚠️ **⛔ לא ירד בחשיבות** — הוא עדיין 🔴 חוסם את `B-002` — אלא ⛔ אינו בר-התחלה ע"י Code.
- **דרך יישום:** הכלל ההפוך — **מזעור** `MAXAGE` בכפוף לחריגה מהפער הנצפה, ⛔ לא רצפה שנגזרת ממחזור הבודק.
- **מה חוסם:** 🔴 **הכרעת ניב** — הכלל, ערך `Sentinel`, והאסרציה המכנית. ראה `docs/audits/AUDIT-guardians-M0.md`.
- **מדד סגירה:** כל שורה נושאת נימוק מדוד; אסרציה נצפית אדומה לפני שהיא ירוקה.

---

🔴 **הסתייגות מנומקת:** «נתונים שבורים **חדשים** כל יום» ⛔ **אינו נתמך** —
`STATE` רושם **60 עסקאות** ו**אפס סגירות מאז 29.07** (נמדד 12.08). **הסדר
מאושש; הקצב ⛔ לא** — הנימוק התקף הוא **דלת פתוחה + בר-ביצוע**, ⛔ לא תדירות.
