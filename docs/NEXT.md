# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: **04.09 (סגירת `B-186` — `D-066`, `אומת-מקור` על bundle הפרודקשן)**. ⚠️ **`B-186` ירד מפני שנסגר**; `B-185` ו-`B-275` עלו מקום אחד, ומקום 3 הוא `B-274` — **המלצת Code ⛔ ולא הכרעה**, להחלפה ע"י ניב. ⛔ **`C-038` ⛔ אינו נכנס לכאן** — הוא בדיקה ידנית חסומה על אוכלוסייה (⛔ הערך ב-`M-018` ⛔ ולא כאן), ⛔ לא משימה.

🔴 **`B-184` ירד — נסגר** (`W1a`, 27.08 · `INCIDENTS#18`). ⛔ **המקום שהתפנה ⛔ לא הלך לעיצוב**: הכרעת ניב 27.08 קובעת ש-`B-246` ⛔ אינו נכנס על חשבון P0 של כסף/אבטחה. הסדר `W1b`·`B-186` → `W1c`·`B-185` הוא הכרעת ניב מאותו יום (החיתוך ∅ ⇒ ⛔ אין נימוק לאיחוד). ⚠️ **מקום 3 הוא המלצת Code ⛔ ולא הכרעה** — להחלפה ע"י ניב.

---

## 1 · `B-185` — מחירים קשיחים מרונדרים כחיים מתחת לתג `🟢 LIVE`

- **למה עכשיו:** המקום **היחיד** באודיט שבו משתמש יכול **לפעול** על מספר מומצא. `R-2` בשכבת הכסף. **`W1c`** — אחרון מבין השלושה מפני שהוא היחיד שנוגע ב-10 אתרים ובנתון **שמור**.
- **דרך יישום:** ⛔ לא הוכרע. ⚠️ **⛔ לא יתחיל בלי תוכנית נפרדת לנתון השמור** — **זיהום DB, ⛔ לא באג תצוגה**. ⚠️ ומצטלב עם `B-245` על אותו פיקסל (`#10b981`).
- **מה חוסם:** 🔴 **הכרעת ניב** — מה מוצג כשהספק נופל: ערך אחרון עם חותמת, או `—`.
- **מדד סגירה:** ניתוק הספק ⇒ רשימת המעקב ⛔ אינה מציגה מספר תחת `🟢 LIVE`; אימות עין.

## 2 · `B-275` — הסנטינל מבוצע הרבה מתחת לתזמון, ⛔ ואינו יכול לראות זאת

- **למה עכשיו:** ⚠️ **המלצת Code, ⛔ לא הכרעה** — מחליף את `B-187`, שחוזר ל-`BACKLOG` **ללא שינוי**. אובחן read-only 29.08 (`docs/audits/AUDIT-2026-08-29-sentinel-cron-drop.md`): ⛔ הכשל אינו כשל אלא **אי-הרצה** — ⛔ **המניות ב-`M-010` ⛔ ולא כאן** (העותק ה**שלישי** שעמד כאן נגזם 05.09 ‹R-6›; הוא כבר הופרך). כל עוד זה פתוח, «נחכה שהסנטינל יעבור לירוק» הוא מדד מפגר של שעות.
- **דרך יישום:** ⛔ לא הוכרע — Q2 באבחון מתמחר ארבע חלופות בתחזוקה ⛔ ואינו ממליץ. 🔴 **והאבחון הפריך את ההנמקה שעמדה כאן**: ההידרדרות היא **ברמת הריפו** (`M-012`) ⛔ ולא מדיניות-תדר ⇒ ⛔ **אין ודאות שהורדת תדר עוזרת** — ⛔ אין בריפו cron בתדר ביניים שאפשר למדוד עליו.
- **מה חוסם:** ⛔ כלום למדידה; 🔴 **הכרעת ניב** על החלופה. ⚠️ **`B-276` ⛔ אינו נגזר מכאן** — עומד בנפרד ב-`BACKLOG`. ✅ **`B-277` נסגר 02.09** (`D-065`) ⛔ **ו⛔ לא שינה סף** ⇒ המדידה כאן ⛔ אינה מושפעת.
- **מדד סגירה:** מדידה חוזרת של `M-010` אחרי 7 ימים ⇒ מבוצע/מתוזמן ≥ `80%`. ⚠️ **וזהו מדד על משתנה ש⛔ אינו בשליטתנו** — ⛔ אסור לרכך אותו כדי לעבור (`R-4`). ⚠️ **המספרים גזומו מהכותרת 04.09 ‹`R-6`› — הם ב-`M-010` בלבד**, ש**הפריך** את `5.6%` שעמד כאן.


## 3 · `B-274` — מניית חוליות ה-`verify` ב-`CLAUDE.md` §7 מתוחזקת ביד

- **למה עכשיו:** ⚠️ **המלצת Code, ⛔ לא הכרעה.** גל `B-186` (04.09) היה ה**מופע השלישי**: המספר עודכן `25→26` ביד באותו קומיט שהוסיף חוליה — בדיוק ההזזה ש-`B-274` מתאר. הפרוזה כבר נסחפה פעם אחת מול `package.json` (`B-257`), ו⛔ **אף שער ⛔ אינו יכול לראות זאת** — `test:registry` בודק את קבצי המרשם, ⛔ לא את `CLAUDE.md`.
- **דרך יישום:** ⛔ לא הוכרע. הכיוון: אסרציה שגוזרת את המנייה מ-`package.json` (`verify` מפוצל ב-`&& npm run `) ומשווה לשני הביטויים ב-§7 — הבלוק והפרוזה. ⛔ **הזזת המספר ⛔ אינה תיקון** — זה מה שנעשה שלוש פעמים.
- **מה חוסם:** ⛔ כלום.
- **מדד סגירה:** השער **נצפה אדום** על העץ הנוכחי אחרי הזזת ספרה אחת בפרוזה, ואז ירוק — ⛔ לא «נוסף ועבר».

---

🔴 **מה ירד, ולמה:** `B-184` ירד **מפני שנסגר** (⛔ היחיד כאן שירד מסיבה זו). `B-168`/`B-154`/`B-158` ירדו קודם — ⛔ **לא נסגרו ולא הודחו על איכות.** שלושתם חסומים על **הכרעת ניב** (כך היה גם ב-20.08), והאודיט העלה שלושה פריטים שאחד מהם ⛔ אינו חסום כלל (`B-184`) ושניים נוגעים בכסף שהמשתמש **רואה עכשיו**. שלושתם חיים ב-`BACKLOG` ללא שינוי וחוזרים ברגע שההכרעות ניתנות.
⚠️ **60 פריטי DEEP ⛔ אינם 60 משימות** — שכבת השורשים (`BACKLOG` §🧬) מראה שהכרעה אחת סוגרת 9–11 ממצאים.
