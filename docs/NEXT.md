# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 15.08 · אחרי גל השומרים (`B-001`+`B-002`+`B-018`)

---

## 1 · `B-148` — ספי `Watchdog` נכתבים מחדש

- **למה עכשיו:** גל השומרים **נעצר** בבלוק 4. המדידה הפריכה את ההנחה שעליה נשען הסף: `Sentinel=3` יורה אחרי **24.17ש'**, ו-`Sentinel=26` היה יורה אחרי **48.17ש'** ⇒ ההכרעה שאושרה הייתה **מוסיפה 24ש' עיוורון** לשורה היחידה שכבר אופטימלית. ⛔ **אפס ספים שונו.** 8/11 שורות מבזבזות בדיוק 24ש', `restore-drill` מבזבזת 96ש', ו-2 שורות ⛔ אינן ניתנות להידוק כלל.
- **דרך יישום:** הכלל ההפוך — **מזעור** `MAXAGE` בכפוף לחריגה מהפער הנצפה, ⛔ לא רצפה שנגזרת ממחזור הבודק.
- **מה חוסם:** 🔴 **הכרעת ניב** — הכלל, ערך `Sentinel`, והאסרציה המכנית. ראה `docs/audits/AUDIT-guardians-M0.md`.
- **מדד סגירה:** כל שורה נושאת נימוק מדוד; אסרציה נצפית אדומה לפני שהיא ירוקה.

## 2 · `B-129` — שלושת מסלולי הכתיבה

- **למה עכשיו:** **OCR כותב אפס אזכורי `currency`** ⇒ כל תיקון נתונים יירקב תוך שבוע, כי הדלת שמכניסה שורות בלי תווית נשארת פתוחה. ⚠️ **נשאר #2 על טיעון חדש, ⛔ לא על הישן:** ראיות הסגירה שלו נשענות על רשת התראות — והרשת נמדדה ואומתה בגל השומרים (`2/2` ערוצים, T-A חוזרת), ולכן החסם הזה **הוסר**.
- **דרך יישום:** יישור שלושת המסלולים לסולם הראיות המדורג של הייבוא.
- **מה חוסם:** אין.
- **מדד סגירה:** `C-022` ירוק.

## 3 · `B-143` — משפחת סמל-המצרף

- **למה עכשיו:** `B-142` ניקה את הכותרת והשאיר את `stats.currentEquity` **מזוהם ובלי צרכן** — בעלות בהערה בלבד. ⚠️ אותו מספר עדיין מודפס תחת סמל אחר ב-PDF.
- **דרך יישום:** אותה הפרדה שנקנתה ב-`B-142` — כל מחובר שאינו ניתן להמרה מוחרג ו**נספר**, ⛔ לא מוסתר.
- **מה חוסם:** אין. ⚠️ `B-144` יושב באותו קובץ — להכריע אם נכנס לאותו גל.
- **מדד סגירה:** `C-023` ירוק **גם ב-PDF וגם בכותרת הסטטיסטיקה**.
