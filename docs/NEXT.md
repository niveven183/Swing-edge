# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 13.08 · הכרעת ניב+Claude

---

## 1 · `B-142` — ההון ה**סגור** לא-מומר תחת סמל החשבון

- **למה עכשיו:** `B-119` תיקן את המחובר ה**חי** והשאיר את ה**סגור** באותה כותרת — נמדד בדפדפן 13.08. אותו מספר פונה-משתמש, והפגם היחיד שנשאר בו.
- **דרך יישום:** יישור `statsCcy` ל-`dispCcy`, כפי שנעשה ל-`openPnL`.
- **מה חוסם:** אין. ⚠️ אסרציה `13.9b` היא **קו קפוא** — מסירים אותה במפורש, ⛔ לא מעדכנים ציפייה כדי לעבור.
- **מדד סגירה:** `C-023` ירוק **גם כשאין שער** — ⛔ אפס סכום לא-מומר תחת סמל החשבון.

## 2 · `B-001` + `B-002` + `B-018` — גל השומרים

- **למה עכשיו:** **11/17 workflows בלי התרעת-כישלון**, ו-`Watchdog` מודד התיישנות ⇒ **מדלג תמיד על הכשל הראשון**. הבאג הבא יהיה שקט. הערכה: ~שעה.
- **דרך יישום:** `failure-alert` נכנס לרשימת ה-workflows של עצמו · מדידת **כישלון** ⛔ לא התיישנות.
- **מה חוסם:** אין.
- **מדד סגירה:** כשל **מכוון** ב-workflow אחד מייצר התרעה תוך <1ש'.

## 3 · `B-129` — שלושת מסלולי הכתיבה

- **למה עכשיו:** **OCR כותב אפס אזכורי `currency`** ⇒ כל תיקון נתונים יירקב תוך שבוע, כי הדלת שמכניסה שורות בלי תווית נשארת פתוחה.
- **דרך יישום:** יישור שלושת המסלולים לסולם הראיות המדורג של הייבוא.
- **מה חוסם:** אין.
- **מדד סגירה:** `C-022` ירוק.
