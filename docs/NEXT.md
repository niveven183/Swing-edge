# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: **07.09 (אבחון read-only «פער ההתראה» — `audits/AUDIT-2026-09-07-alerting-gap.md`. ⛔ 0 קבצי קוד · 0 workflows · 0 חוקי Sentry. 🔴 הכרעת ניב: `B-275` מ-«המלצת Code» ל-`P0`)**. ⛔ **השלושה ⛔ לא זזו בסדר** — `P0` היא הכרעה על **חומרה**, ⛔ לא על סדר, וסדר הוא הכרעת ניב (§14). 🆕 **נפתחו `B-305`…`B-310`** — ⛔ אף אחד ⛔ אינו נכנס לכאן (⛔ 3 בדיוק). ⚠️ **שני מועמדים למקום 3 במקום `B-274`: `B-298`** (כסף שהמשתמש רואה עכשיו) ו-**`B-300`** — **הכרעת ניב, ⛔ לא Code**. ⛔ **`C-036`/`C-038`/`C-039`/`C-041`/`C-042` ⛔ אינם נכנסים** — בדיקות ידניות, ⛔ לא משימות.

🔴 **`B-184` ירד — נסגר** (`W1a`, 27.08 · `INCIDENTS#18`). ⛔ **המקום שהתפנה ⛔ לא הלך לעיצוב**: הכרעת ניב 27.08 קובעת ש-`B-246` ⛔ אינו נכנס על חשבון P0 של כסף/אבטחה. הסדר `W1b`·`B-186` → `W1c`·`B-185` הוא הכרעת ניב מאותו יום (החיתוך ∅ ⇒ ⛔ אין נימוק לאיחוד). ⚠️ **מקום 3 הוא המלצת Code ⛔ ולא הכרעה** — להחלפה ע"י ניב.

---

## 1 · `B-185` — מחירים קשיחים מרונדרים כחיים מתחת לתג `🟢 LIVE`

- **למה עכשיו:** המקום **היחיד** באודיט שבו משתמש יכול **לפעול** על מספר מומצא. `R-2` בשכבת הכסף. **`W1c`** — אחרון מבין השלושה מפני שהוא היחיד שנוגע ב-10 אתרים ובנתון **שמור**.
- **דרך יישום:** ✅ **(ב)+(ג)+(ד) בוצעו 05.09** (`PLAN-2026-09-05-b185-fallback.md`, `test:watchlist` 27/27). ⛔ **(א) — תג חיוּת אמיתי — ⛔ לא בוצע**, נרשם `B-290`. ⛔ **`priceService.js:136` שורד** (`?? 0` בשכבת הספק), נרשם `B-291`.
- **מה חוסם:** ⛔ **לא עוד הכרעת ניב על מה מוצג** (הוכרע — `—`, ‹ב2›, `DECISIONS` 05.09). 🔴 **חוסם על פריסה + `C-039` (אימות עין)** — `T3` ⛔ אינו נסגר בלעדיו (§15).
- **מדד סגירה:** `C-039` — ניתוק הספק ⇒ רשימת המעקב ⛔ אינה מציגה מספר תחת `🟢 LIVE`, ושורת האחוז נעדרת (⛔ לא `+0.00%`); אימות עין בדפדפן אמיתי על ה-bundle החי.

## 2 · `B-275` — הסנטינל מבוצע הרבה מתחת לתזמון, ⛔ ואינו יכול לראות זאת

- **למה עכשיו:** 🔴 **`P0` — הכרעת ניב 07.09, ⛔ לא עוד המלצת Code.** ב-29.08 נבחרה חלופה `D` («ללא שינוי») כשהמחיר היה **תיאורטי**; ב-07.09 הוא נמדד: **18:34:40** בין שיגור `70816e1` להתראה **היחידה** בחלון (מייל Sentry), ⛔ בלי שאף שכבה משלנו ידעה (`INCIDENTS#20` addendum ב׳). ⛔ **המניות ב-`M-010` ⛔ ולא כאן.**
- **דרך יישום:** ⛔ לא הוכרע — **שש** חלופות תומחרו באבחון 07.09 §10 (מה תופסת · זמן · עלות · כשל שקט · §3) ⛔ **ללא המלצה**. 🔴 **והאבחון הפריך «העלאת תדר = תיקון»:** `sentinel.yml:378-395` שואל `firstSeen:-35m` ⇒ **`6/6` הריצות הדפיסו «אין issues חדשים»**, וגם `48/48` היו מדפיסות (`B-306`); והפיקסצ'ר ⛔ אינו מייצר את המקרה כלל (`B-305`).
- **מה חוסם:** ⛔ כלום למדידה; 🔴 **הכרעת ניב** על החלופה. ⚠️ **`B-305`·`B-306` ⛔ אינם נגזרים מכאן** — הם חסימות **בלתי-תלויות** שעומדות ב-`BACKLOG` בנפרד, ותיקון אחת בלבד שייקרא כתיקון הוא `R-4`. ⚠️ **`B-276` ⛔ אינו נגזר מכאן** גם הוא.
- **מדד סגירה:** מדידה חוזרת של `M-010` אחרי 7 ימים ⇒ מבוצע/מתוזמן ≥ `80%`. ⚠️ **וזהו מדד על משתנה ש⛔ אינו בשליטתנו** — ⛔ אסור לרכך אותו כדי לעבור (`R-4`). ⚠️ **⛔ ו⛔ אינו מספיק לבדו** — `80%` ביצוע עם שאילתת `firstSeen` נותן `100%` שקט; הסגירה דורשת גם `B-306`.


## 3 · `B-274` — מניית חוליות ה-`verify` ב-`CLAUDE.md` §7 מתוחזקת ביד

- **למה עכשיו:** ⚠️ **המלצת Code, ⛔ לא הכרעה.** גל `B-186` (04.09) היה ה**מופע השלישי**: המספר עודכן `25→26` ביד באותו קומיט שהוסיף חוליה — בדיוק ההזזה ש-`B-274` מתאר. הפרוזה כבר נסחפה פעם אחת מול `package.json` (`B-257`), ו⛔ **אף שער ⛔ אינו יכול לראות זאת** — `test:registry` בודק את קבצי המרשם, ⛔ לא את `CLAUDE.md`.
- **דרך יישום:** ⛔ לא הוכרע. הכיוון: אסרציה שגוזרת את המנייה מ-`package.json` (`verify` מפוצל ב-`&& npm run `) ומשווה לשני הביטויים ב-§7 — הבלוק והפרוזה. ⛔ **הזזת המספר ⛔ אינה תיקון** — זה מה שנעשה שלוש פעמים.
- **מה חוסם:** ⛔ כלום.
- **מדד סגירה:** השער **נצפה אדום** על העץ הנוכחי אחרי הזזת ספרה אחת בפרוזה, ואז ירוק — ⛔ לא «נוסף ועבר».

---

🔴 **מה ירד, ולמה:** `B-184` ירד **מפני שנסגר** (⛔ היחיד כאן שירד מסיבה זו). `B-168`/`B-154`/`B-158` ירדו קודם — ⛔ **לא נסגרו ולא הודחו על איכות.** שלושתם חסומים על **הכרעת ניב** (כך היה גם ב-20.08), והאודיט העלה שלושה פריטים שאחד מהם ⛔ אינו חסום כלל (`B-184`) ושניים נוגעים בכסף שהמשתמש **רואה עכשיו**. שלושתם חיים ב-`BACKLOG` ללא שינוי וחוזרים ברגע שההכרעות ניתנות.
⚠️ **60 פריטי DEEP ⛔ אינם 60 משימות** — שכבת השורשים (`BACKLOG` §🧬) מראה שהכרעה אחת סוגרת 9–11 ממצאים.
