# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 20.08 · `B-172` **נסגר** (`D-048`, שלב 2) ⇒ יצא — ⛔ לא כי הודח: ענף `currency_source_evidence` חובר ל-`deriveInstrumentCurrency`, 6 אסרציות ירוקות, קו קפוא `tradingstats` byte-identical, 0/78 שורות פרודקשן זזו. `B-156`/`B-168` חוזרים לסדר שקדם ל-20.08 (#1/#2). `B-154` נכנס **#3** — ⛔ לא פריט חדש, אלא צד-הקריאה של אותה תעודה שצד-הכתיבה שלה (`B-172`) נסגר עכשיו (`B-172`≡`B-154`, `AUDIT-B172.md`). ⚠️ **סדר #1–#3 לא אושר בפירוש ע"י ניב בסבב הזה** — המשך מנגני של הסדר שקדם ל-20.08, ⛔ לא בחירה חדשה; טעון אישור.

---

## 1 · `B-156` — `TradeDNA`/`GrowthTracker` מחזירים `50`

- **למה עכשיו:** `B-009` סגר את `EMPTY_STATS` ⇒ **5 מתוך 6 המספרים המומצאים שמשתמש חדש רואה נשארו ⛔ בחוץ**, והם ה**גרועים** מבין השישה: `50` נראה כציון אמיתי בעוד `0` נראה כהיעדר. מוצג תחת `data-tour="trading-dna"` — מסלול ההדרכה של מי שאין לו עסקה.
- **דרך יישום:** ⚠️ ⛔ **לא** `null` במנוע — **6 צרכנים אריתמטיים** יהפכו ל-`NaN`. הכיוון הסביר הוא **גדר תצוגה**: המנוע ממשיך ב-prior, המסך מסרב להציג ציון בלי מדגם.
- **מה חוסם:** 🔴 **הכרעת ניב על הכיוון.** `test:coach` (111) לפני **וגם** אחרי (§7).
- **מדד סגירה:** אימות עין על יומן ריק — ⛔ אף `50` על המסך; ו-111/111 ירוקות בשני הקצוות.

## 2 · `B-168` — 3/4 שכבות ההתראה עיוורות ל-`cancelled`

- **למה עכשיו:** מרחיב את `B-163` — אותו שורש (מנגנון-ההתראה עיוור למחלקת-כשל זו), מופע שני: נמדד ב-`AUDIT-B166.md` §D3 ש-`failure-alert.yml:27`/`triage.yml:32`/"Alert correlation" (`fleet-daily.yml:318`) מסננים `conclusion=='failure'`/`status=failure` בלבד ⇒ ב-6/6 מקרים נמדדים `failure-alert.yml` **לא רץ כלל**. ⛔ לא ייחודי ל-Sentinel — כל workflow שנחתך בטיימאאוט חולק את הפער.
- **דרך יישום:** הוספת `cancelled` לתנאי הסינון בשלוש השכבות (או מעבר לבדיקת ה-run הכולל, ⛔ לא רק ה-job) — כיוון מועמד, ⛔ לא הוכרע.
- **מה חוסם:** 🔴 **הכרעת ניב על המנגנון המדויק.**
- **מדד סגירה:** run שנחתך ב-`cancelled` (⛔ לא רק `failure`) מייצר התראה ב-3/3 השכבות שנבדקו.

## 3 · `B-154` — תעודת `currency_source` נכתבת, כמעט איש אינו קורא אותה

- **למה עכשיו:** צד-הכתיבה של אותה תעודה (`B-172`) נסגר 20.08 — 2/2 מסלולי כתיבה תקינים, ו-`deriveInstrumentCurrency` עצמו עלה `isEvidenceBacked` מ-0/22 ל-1/22 קריאה בקוד המוצר. אבל הצרכן שהפריט ביקש במקור — **מצג-מסך** לעסקה שתוויתה בניחוש (דרגה 3–4) — עדיין לא קיים; הקריאה החדשה פנימית-למנוע בלבד.
- **דרך יישום:** תג הצהרה על עסקה עם `currency_source` לא-evidence-backed, באותה שפה של `ccyUnverified` (`C-027`) — ⛔ לא גזירת מטבע חדשה מהנייר (נפסלה במדידה 16.08: `assumed/USD` על כל טיקר אלפביתי, אינו יכול להחזיר `ILS`).
- **מה חוסם:** 🔴 **הכרעת ניב על בחירת הצרכן** — איזה מסך, איזו נוסחה בדיוק.
- **מדד סגירה:** צרכן ראשון פונה-מסך קורא `currency_source`/`isEvidenceBacked` ומציג הצהרה — ⛔ לא רק קריאה פנימית במנוע.

---

🔴 **מה ירד, ולמה:** `B-172` ירד כי **נסגר** (`D-048`, 20.08, שלב 2) — ⛔ לא כי הודח: ענף `currency_source_evidence` חובר, 6 אסרציות ירוקות, קו קפוא `tradingstats` byte-identical. ⚠️ **ו⛔ אינו סגור על המסך:** 0/78 שורות פרודקשן evidence-backed היום ⇒ הענף **מחובר ורדום** — `C-030`(ב) עדיין ⛔ בלתי-נגיש, מתועד ב-`CHECKS.md`.
`B-144` נשאר סגור (`D-047`, 20.08).
`B-148` נשאר ב-`BACKLOG` (19.08, ⛔ לא נסגר) — עדיין מוחלף ב-`B-168` על קריטריון-**רוחב**.
