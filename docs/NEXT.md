# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 19.08 · `B-162`+`B-165` נסגרו ו**השלושה ⛔ לא זזו** — ⛔ לא כי לא נבדקו, אלא כי **אף אחד מהם ⛔ לא ישב כאן** ⇒ ⛔ אין מקום שהתפנה. `B-144` נשאר #1 מאותו קריטריון מדוד: היחיד שאינו חסום על הכרעת ניב

---

## 1 · `B-144` — הגזירה סופרת יחידת-תצוגה, ⛔ לא תווית-נייר

- **למה עכשיו:** הוא ה**בר-ביצוע** היחיד מהשלושה, בדיוק כמו `B-143` לפניו. ⚠️ **והוא כבר נמדד בדפדפן** — באנר `+$100.00 (1)` על יומן של **2 ניירות USD**, ו-`mixedCurrency` יורה על יומן **חד-מטבעי**; זו הראיה, ⛔ לא הערכה.
- **דרך יישום:** **פיצול הגזירה לשתיים** — תווית-נייר ויחידת-תצוגה ⛔ אינן יכולות לצאת מאותו `derivations`. ⚠️ **תיקון "שורה אחת" נמדד ונפסל** ב-`PLAN-B143` §10: הוא שובר את `pnlByCurrency`.
- **מה חוסם:** אין. ⚠️ `computeTradingStats` נגע זה עתה ב-`B-143` ⇒ **בייסליין** לפני נגיעה.
- **מדד סגירה:** יומן חד-מטבעי ⇒ הבאנר **נעדר**; יומן דו-מטבעי ⇒ כל סל נושא סמל שתואם למה שסוכם בתוכו, מחושב ביד.

## 2 · `B-156` — `TradeDNA`/`GrowthTracker` מחזירים `50`

- **למה עכשיו:** `B-009` סגר את `EMPTY_STATS` ⇒ **5 מתוך 6 המספרים המומצאים שמשתמש חדש רואה נשארו ⛔ בחוץ**, והם ה**גרועים** מבין השישה: `50` נראה כציון אמיתי בעוד `0` נראה כהיעדר. מוצג תחת `data-tour="trading-dna"` — מסלול ההדרכה של מי שאין לו עסקה.
- **דרך יישום:** ⚠️ ⛔ **לא** `null` במנוע — **6 צרכנים אריתמטיים** יהפכו ל-`NaN`. הכיוון הסביר הוא **גדר תצוגה**: המנוע ממשיך ב-prior, המסך מסרב להציג ציון בלי מדגם.
- **מה חוסם:** 🔴 **הכרעת ניב על הכיוון.** `test:coach` (111) לפני **וגם** אחרי (§7).
- **מדד סגירה:** אימות עין על יומן ריק — ⛔ אף `50` על המסך; ו-111/111 ירוקות בשני הקצוות.

## 3 · `B-148` — ספי `MAXAGE` ב-`watchdog.yml`

- **למה עכשיו:** הוא **חוסם את `B-002`** — צלע הספים ⛔ לא נסגרה ב-`D-036`, ו-`watchdog.yml` עצמו ⛔ לא נגע. ⚠️ **"⛔ אינו בר-התחלה ע"י Code" הוא נימוק ל*תזמון*, ⛔ לא לדירוג** — הורדתו מהשלושה בגל `B-009` הייתה שגיאת דירוג, והוחזרה בהכרעת ניב 16.08.
- **דרך יישום:** ההכרעה שאושרה **הופרכה במדידה** ⇒ הספים נגזרים מהגילאים הבדידים שבודק יומני יכול לצפות, ⛔ לא ממספר שנראה סביר.
- **מה חוסם:** 🔴 **הכרעת ניב על 11 השורות.**
- **מדד סגירה:** `B-002` ניתן לסימון «בוצע» — כלומר `watchdog.yml` עצמו נגע, ⛔ לא רק מתאם ההתראות סביבו.

---

🔴 **מה ירד, ולמה:** `B-143` ירד כי **נסגר** (`D-043`, 17.08) — ⛔ לא כי הודח.
`B-154` ירד מהשלושה ל-`BACKLOG` — חסום על **בחירת צרכן**,
ואותה שאלה בדיוק נפתחת בבירור `isEmpty` (16.08). ⛔ **לא ירד בחשיבות:** שדה
שנכתב ואיש אינו קורא מתיישן בשקט. ⚠️ **ניב יכול להפוך זאת** — הוא חי ב-`BACKLOG`.
