# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 26.08 · **DEEP-AUDIT** (8 פאזות · 113 ממצאים · `P0=9 · P1=41 · P2=53 · P3=10`) נרשם כ-`B-184`…`B-243`. שלושת הפריטים כאן נגזרים **ישירות** מ-5 שורות "מה הייתי מתקן ראשון" ב-`docs/audits/DEEP-SUMMARY.md` — ⛔ לא מבחירה חדשה. ⚠️ **9 P0 ⛔ אינם 9 מקומות ב-NEXT** — הסדר כאן הוא חומרה **כפול** עלות-תיקון, וההנמקה להדחת `B-191` (החמור ביותר משפטית) כתובה בשורה 4 של אותן חמש.

---

## 1 · `B-184` — טוקן סשן חי עוזב את הדפדפן אל צד שלישי

- **למה עכשיו:** הממצא **היחיד** מבין 113 שבו **אישור גישה** חוצה את גבול המערכת, ⛔ לא נתון. קורה בכל התחברות ב-magic-link, בלי שער הסכמה.
- **דרך יישום:** התיקון **כבר כתוב בריפו** — `index.html:140-144` ביצע בדיוק את ההצמדה הזו לערוץ האנליטיקה הראשון אחרי `INCIDENTS#12`; להחיל על השני, שנולד לפניו ונשאר פתוח. ⛔ לא מנגנון חדש.
- **מה חוסם:** ⛔ כלום. הזול והחמור בו-זמנית ⇒ ראשון.
- **מדד סגירה:** ניווט יזום עם `#access_token` ב-URL ⇒ **0** בקשות יוצאות הנושאות אותו, נמדד ב-Network של דפדפן חי (⛔ לא בקריאת קוד).

## 2 · `B-185` — מחירים קשיחים מרונדרים כחיים מתחת לתג `🟢 LIVE`

- **למה עכשיו:** המקום **היחיד** באודיט שבו משתמש יכול **לפעול** על מספר מומצא — מחיר שגוי שנראה חי הוא ההפרש בין החלטת מסחר נכונה לשגויה. `R-2` בשכבת הכסף.
- **דרך יישום:** ⛔ לא הוכרע — התג חייב להפוך ממדידת **שעון** למדידת **חיוּת**, ומסלול ה-fallback חייב להצהיר במקום להשלים. שתי הכרעות נפרדות.
- **מה חוסם:** 🔴 **הכרעת ניב** — מה מוצג כשהספק נופל: ערך אחרון עם חותמת, או `—`.
- **מדד סגירה:** ניתוק הספק ⇒ רשימת המעקב ⛔ אינה מציגה מספר תחת `🟢 LIVE`; אימות עין.

## 3 · `B-186` — אחוז P&L חי ב-SHORT מחלק במחיר הנוכחי

- **למה עכשיו:** הזול מבין תשעת ה-P0 — שורה אחת, ⛔ אפס תלויות, ומספר כסף על המסך. המכנה זז עם השוק ⇒ האחוז שגוי **תמיד**, ⛔ לא בקצה.
- **דרך יישום:** המכנה הוא מחיר הכניסה. ⛔ אסור `\|\| 1` בשער (CLAUDE.md §7).
- **מה חוסם:** ⛔ כלום.
- **מדד סגירה:** עסקת SHORT פתוחה — האחוז מחושב ידנית ומושווה למסך (⛔ לא "נראה סביר"); בייסליין קפוא נצפה אדום לפני התיקון.

---

🔴 **מה ירד, ולמה:** `B-168`/`B-154`/`B-158` ירדו — ⛔ **לא נסגרו ולא הודחו על איכות.** שלושתם חסומים על **הכרעת ניב** (כך היה גם ב-20.08), והאודיט העלה שלושה פריטים שאחד מהם ⛔ אינו חסום כלל (`B-184`) ושניים נוגעים בכסף שהמשתמש **רואה עכשיו**. שלושתם חיים ב-`BACKLOG` ללא שינוי וחוזרים ברגע שההכרעות ניתנות.
⚠️ **60 פריטי DEEP ⛔ אינם 60 משימות** — שכבת השורשים (`BACKLOG` §🧬) מראה שהכרעה אחת סוגרת 9–11 ממצאים.
