# NEXT — ההמלצה

> **NEXT = ההמלצה · `docs/BACKLOG.md` = המצאי.** פריט כאן **חייב** להתקיים שם.
> ⛔ **3 בדיוק — הכרעה, לא רשימה.** מתעדכן **בסוף כל גל**.
> ⛔ **אפס פרטים חדשים כאן** — מזהה והנמקה בלבד. הפירוט חי ב-`BACKLOG` ⇒ פרט
> שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף מכנית ב-`test:registry` §9.

עודכן: 20.08 · `B-156` **נסגר** (`D-049`) ⇒ יצא — ⛔ לא כי הודח: `TradeDNA`/`GrowthTracker` מפסיקים להמציא `50` על מדגם שלא נמדד (גדר-תצוגה, ⛔ לא `null` במנוע — 6 צרכנים אריתמטיים), `total` מנורמל-מחדש על שדות נמדדים עם שבר `measuredCount/totalCount` גלוי, וסף/נוסחת `consistency` אוחדו בין שני המנועים (קו קפוא זז 53→46, מוצהר). 27/27 `test:dna` + מוטציה מפלה, `test:coach` 111/111 לפני **וגם** אחרי, אימות עין על שלושת אתרי הרינדור. `B-168`/`B-154` חוזרים ל-**#1/#2** (הסדר שקדם ל-20.08). `B-158` נכנס **#3** — ⛔ לא פריט חדש: אותו דפוס-שורש (`R-2`, ברירת-מחדל ממציאה) שנולד מאותו גל (`B-009`, 16.08) ועודנו פתוח וללא כיוון.

---

## 1 · `B-168` — 3/4 שכבות ההתראה עיוורות ל-`cancelled`

- **למה עכשיו:** מרחיב את `B-163` — אותו שורש (מנגנון-ההתראה עיוור למחלקת-כשל זו), מופע שני: נמדד ב-`AUDIT-B166.md` §D3 ש-`failure-alert.yml:27`/`triage.yml:32`/"Alert correlation" (`fleet-daily.yml:318`) מסננים `conclusion=='failure'`/`status=failure` בלבד ⇒ ב-6/6 מקרים נמדדים `failure-alert.yml` **לא רץ כלל**. ⛔ לא ייחודי ל-Sentinel — כל workflow שנחתך בטיימאאוט חולק את הפער.
- **דרך יישום:** הוספת `cancelled` לתנאי הסינון בשלוש השכבות (או מעבר לבדיקת ה-run הכולל, ⛔ לא רק ה-job) — כיוון מועמד, ⛔ לא הוכרע.
- **מה חוסם:** 🔴 **הכרעת ניב על המנגנון המדויק.**
- **מדד סגירה:** run שנחתך ב-`cancelled` (⛔ לא רק `failure`) מייצר התראה ב-3/3 השכבות שנבדקו.

## 2 · `B-154` — תעודת `currency_source` נכתבת, כמעט איש אינו קורא אותה

- **למה עכשיו:** צד-הכתיבה של אותה תעודה (`B-172`) נסגר 20.08 — 2/2 מסלולי כתיבה תקינים, ו-`deriveInstrumentCurrency` עצמו עלה `isEvidenceBacked` מ-0/22 ל-1/22 קריאה בקוד המוצר. אבל הצרכן שהפריט ביקש במקור — **מצג-מסך** לעסקה שתוויתה בניחוש (דרגה 3–4) — עדיין לא קיים; הקריאה החדשה פנימית-למנוע בלבד.
- **דרך יישום:** תג הצהרה על עסקה עם `currency_source` לא-evidence-backed, באותה שפה של `ccyUnverified` (`C-027`) — ⛔ לא גזירת מטבע חדשה מהנייר (נפסלה במדידה 16.08: `assumed/USD` על כל טיקר אלפביתי, אינו יכול להחזיר `ILS`).
- **מה חוסם:** 🔴 **הכרעת ניב על בחירת הצרכן** — איזה מסך, איזו נוסחה בדיוק.
- **מדד סגירה:** צרכן ראשון פונה-מסך קורא `currency_source`/`isEvidenceBacked` ומציג הצהרה — ⛔ לא רק קריאה פנימית במנוע.

## 3 · `B-158` — `avgWin`/`avgLoss`/`bestWin`/`worstLoss`/`avgHold` נופלים ל-`0` על תת-אוכלוסייה ריקה

- **למה עכשיו:** אותו שורש כמו `B-156` שנסגר עכשיו (`R-2`), דרגה אחת מתחת: `B-009` תיקן את היומן **הריק**; נמדד 16.08 שיומן **לא-ריק** עם תת-קבוצה ריקה (למשל 2/2 עסקאות מרוויחות) מדפיס `avgLoss=0`/`worstLoss=0` ⇒ `"-$0"` תחת "הפסד ממוצע" — ערך שהמסלול החי אינו יכול לייצר (הפסד הוא `pnl<0` בהגדרה).
- **דרך יישום:** ⛔ לא הוכרע. תיקון מזיז את הבייסליין הקפוא ב-**7 תרחישים** (⛔ לא רק `empty`) ⇒ גל עם recapture + הוכחת-אפליה, לא ריפקטור נקי.
- **מה חוסם:** 🔴 **הכרעת ניב על הכיוון** — התבנית (גדר-תצוגה מול `null` במנוע) קיימת עכשיו מ-`B-156`, אך `B-158` נוגע ב-`tradingStats.js` ⛔ לא במנוע ה-DNA, וצרכניו שונים.
- **מדד סגירה:** יומן 2/2 מרוויחות ⇒ "הפסד ממוצע" מציג `"—"` ⛔ לא `"-$0"`; 7/7 תרחישי הבייסליין נצפים ואז מוכחים כמפלים (recapture + הוכחת-אפליה).

---

🔴 **מה ירד, ולמה:** `B-156` ירד כי **נסגר** (`D-049`) — ⛔ לא כי הודח: גדר-תצוגה על `TradeDNA`/`GrowthTracker`, איחוד סף/נוסחת `consistency` (53→46 מוצהר), `total` מנורמל עם שבר גלוי, 27/27 `test:dna` + מוטציה מפלה, `test:coach` 111/111 לפני/אחרי, אימות עין על `DNACard` (ריק+בודד) ו-`GrowthChart` (ריק+בודד+שבר).
`B-172`/`B-144` נשארים סגורים (`D-048`/`D-047`, 20.08).
`B-148` נשאר ב-`BACKLOG` (19.08, ⛔ לא נסגר) — עדיין מוחלף ב-`B-168` על קריטריון-**רוחב**.
