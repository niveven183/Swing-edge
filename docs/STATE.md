# STATE — מצב חי

> **חוזה שבעת הקבצים: `CLAUDE.md` §14.** ⛔ אינו משוכפל כאן — עותק שני נסחף (`R-6`).
> **מה חי כאן:** 🔴 עכשיו · ⏭️ שלושת הבאים (**מצטט** את `NEXT`) · ⏸️ חסום · ⚠️ סיכונים.
> ⛔ **הקובץ הזה מצטט, ⛔ אינו מגדיר.** פירוט = `BACKLOG`. הנמקה = `NEXT`. אימות = `DONE`.
>
> 🔴 **תקציב: 16,000 בתים** (נחקק 26.08 — `DECISIONS` 26.08 · `test:registry` §3). ⚠️ **התקרה הקודמת ספרה
> שורות ואיבדה את הדבר** — `100→99` שורות בזמן שהתוכן גדל `8,162→75,177` בתים (×9.2),
> פעמיים, והמחזור הראשון הסתיים ב-12.08 באובדן **33 פריטים פתוחים**.
> **אימות בפקודה אחת: `wc -c docs/STATE.md`.** ⛔ חריגה = גיזום ל-`BACKLOG`/`DONE`,
> ⛔ לעולם לא ל-`ARCHIVE` אם הפריט פתוח, ⛔ ולעולם לא הרחבת התקציב.
> **כלל HEAD:** כל קומיט שנוגע כאן מעדכן את שורת HEAD **באותו קומיט**.

עודכן: 2026-08-28 · HEAD: `e0e2300`

**מדדים — ⛔ אינם חיים כאן.** 🆕 **`docs/METRICS.md`** (26.08) · `test:registry` §12 מחשב התיישנות מהתאריך בשורה ⛔ ולא מזיכרון. **המצב כרגע: 1/9 מדדים `⏳ פג`** — `M-003` בלבד (**שימור שבוע 1 = 0/6**) ⇒ ⛔ **אינו עובדה עד מדידה חוזרת.** 🆕 **`M-001`/`M-002` נמדדו מחדש 28.08 אחרי 3 שבועות כ-`⏳ פג`** — `46→51` נרשמים · `39/51 = 76%` נושאים `user_settings`. ⛔ **מדידה חוזרת ⛔ אינה הארכת תוקף.** בתוקף גם: `M-004`/`M-005` · `M-006` · `M-007` (נמדד מחדש 28.08) · `M-008` · 🆕 **`M-009`** (חשיפה ל-`DEFAULT_CAPITAL`). ⚠️ **המכנה `7` שעמד כאן ⛔ לא יכול היה להכיל את `M-008`** (נוסף ב-`C1`) — ‹R-4›, נספר תחת `B-258`.

---

## 🔴 עכשיו

✅ **`W1a` · `B-184` נסגר — `D-059` (`5255fa8`).** טוקן הסשן ⛔ אינו עוזב יותר את הדפדפן אל Vercel. ⚠️ **הפירוט המלא — החלון בן 54 הימים, המדידה בדפדפן אמיתי מול הבייטים של פרודקשן, אסרציות `15`–`17`, ופסק `R4` על `/privacy` — חי ב-`DONE.md` `D-059` ו⛔ אינו מועתק לכאן** (§14 · `R-6`). ⛔ **`W1b`·`B-186` ו-`W1c`·`B-185` ⛔ אינם בגל הזה ו⛔ טרם התחילו.**

🧹 **גל `F1` — הבלוק היתום ב-`BACKLOG` יצא מ«❄️ מוכרע-לא-לעשות», ו-`B-125` הופרך והפך למצבה (`D-060`).** `test:registry` §13 (`36→38`) **מייחס שורת `B-` לכותרת שמעליה** — ייחוס ש⛔ **לא היה קיים**: השער פירסר כותרות בארבעה קבצים ו⛔ מעולם לא ב-`BACKLOG` ⇒ 6 פריטים בסטטוס `פתוח` ישבו תחת «לא לעשות» 15 יום ‹R-4›. `docs/audits/AUDIT-backlog-hygiene.md` §F1 · `docs/plans/PLAN-2026-08-27-f1-section-gate.md`. ✅ **נסגר ב-`D-061`** (`0bfadd1`) — נרשם בקומיט **הבא**, כי קומיט ⛔ אינו יכול לשאת את ה-hash של עצמו (§5). §11 הוסיף `B-266`·`B-267`, ⛔ נרשמו ולא תוקנו.

🔴 **`B-268` — הון משתמש נדרס בברירת מחדל. אובדן נתונים בפרודקשן, נצפה ⛔ לא היפותטי.** אבחון read-only 28.08 (§8.1 שלב 1, **⛔ אפס תיקון · 0 קבצי קוד · 0 כתיבות DB**) — `docs/audits/AUDIT-2026-08-28-sentinel-capital-clobber.md`. **הסנטינל צדק:** הון חשבון ה-QA השתנה `10,000 → 2,500` בחלון `26.08 09:59:41Z…16:05:27Z`, מאומת מארטיפקטי `browser-findings-auth.json` (**ירוק ב-14 ריצות כשירות**, ואז `hydration-default`). ⇒ **הבאג במוצר ⛔ לא בשער.** ⚠️ **ו-`$2,500` הוא `DEFAULT_CAPITAL` ש⛔ נכתב ל-DB** — `:1734` מסמן `hydratedRef = true` **ללא תנאי** גם כש-`loadSettings` נפלה, ו-`:1814` עושה upsert לברירת המחדל מעל הערך האמיתי ‹R-4›‹R-2›. 🆕 **חשיפה נמדדה מחדש 28.08 וה⛔מדידה מפריכה את האבחון: `2/4` דריסות ודאיות · `2/4` הוצאו · מתוך `39/39` שורות** (`M-009`). האבחון הכריז «⛔ אי-אפשר לחדד — אין `created_at`»; ⛔ **`created_at` אכן חסר, אבל `updated_at` קיים ותוחם** ⇒ ההצהרה ההיא נשאה ‹R-1› משלה. קצב הדימום **3 כתיבות/24ש'** ⇒ ⛔ **לא שעתי.** ✅ **התיקון נחת 28.08** (`W-CAP`, `docs/plans/PLAN-2026-08-28-wcap-capital-clobber.md`): `1a` `migrate` עוצר על `error` · `1b` `loadSettings` תלת-מצבי `ok`/`empty`/`failed` · `:1724` (ענף הסמכות הכוזבת) נמחק · `failed` ⛔ אינו מסמן hydrated · שני באנרים. 🆕 **`P7` — מסלול דריסה שלישי, ⛔ לא היה באבחון ו⛔ לא בתוכנית:** `flushSettings:145` עשה upsert **ללא תנאי**, כך שסגירת טאב אחרי קריאה כושלת כתבה את המראה על השורה האמיתית. נמצא **תוך כתיבת אסרציה 7**, ⛔ לא בתכנון. `test:settings` **9/9** — 4 בלוקים נצפו **אדומים תחילה**. `INCIDENTS#18` נכתב באותו קומיט (§10). 🔴 **הגל ⛔ אינו סגור: אימות עין (P5 שלב 2) הוא פעולת ניב** — חסימת הבקשה ל-`user_settings` ב-DevTools ואימות ש⛔ אף `upsert` ⛔ אינו עוזב את הטאב. ⛔ **אין UPDATE ו⛔ אין שחזור אוטומטי** — `onboarding` הוא ערך ההרשמה ⛔ ולא ההווה (`M-009`).

⛔ **אין גל אחר בביצוע.** שישה גלים נסגרו, ו**פירוטם חי ב-`DONE.md` בלבד** — ⛔ אין עותק שני כאן (§14): **`D-058`** `C1`/`B-245` · **`D-057`** `C2`/`B-250` · **`D-056`** אימות אבחון העיצוב · **`D-055`** `docs/METRICS.md` · **`D-054`** גיזום `STATE` · **`D-053`** `DEEP-AUDIT`. ⚠️ **9 `P0` פתוחים** — מקטע ⚠️ למטה.

**מצב חי ש⛔ אינו נגזר מ-`DONE`:**

- 🎨 **מסלול העיצוב — הוכרע, ⛔ טרם התחיל, ו⛔ אינו ב-`NEXT`.** `DECISIONS` 27.08. **הסדר: `B-254` → `B-246` → `B-245`** (⛔ אינו ציטוט של `NEXT` — הכרעה, ולכן חיה כאן ⛔ ולא ב-⏭️). ⚠️ **הסדר והנימוק שניהם השתנו:** גופנים הודחו בנימוק «שינוי גופן שובר RTL», ו-`C1` מדד ש-**RTL כבר שבור בפועל** ⇒ ⛔ אין מה לשבור ⇒ `B-254` הוא **תיקון באג** ⛔ ולא שינוי עיצובי. ⛔ **`B-246` ⛔ אינו נכנס ל-`NEXT`** — שלושת המקומות תפוסים ב-`P0` של כסף/אבטחה ו⛔ אינם מוחלפים (הכרעת ניב, §14). ⛔ **`B-245` ⛔ אינו מתוקן ע"י קס→טוקן** (`7/27`; השורש הוא `--text-muted`). 🔴 **הצלבה הדדית `B-185`↔`B-245` נרשמה 27.08 (⛔ פריט חדש ⛔ לא נפתח):** ה-`#10b981` שנמדד `2.54:1` הוא **צבע תג `LIVE`** (`src/priceService.js:445`, 4 אתרי רינדור) — **הסמן שמצהיר «המחיר אמיתי» הוא עצמו בלתי-קריא, מעל מחירים ש-`B-185` מוכיח שהם קשיחים-בקוד. שני באגים על אותו פיקסל** ⇒ ⛔ אין לתקן את הצבע לבדו: זה משאיר תג **שקרי** — רק **קריא יותר**.
- 📐 **`Q1` — שלושת ה-P0 ניתנים לתיקון בלי לגעת בשכבת התצוגה. ⛔ 0 קבצי קוד שונו** — `docs/audits/AUDIT-Q1-p0-display-overlap-2026-08-27.md`. **חיתוך שורות `B-184`∪`B-185`∪`B-186` עם `{:954-984, 3926, 3946, 4339-4347, index.css:67, tokens.css:29,71}` = ∅.** `B-184` ⛔ אינו פותח את `SwingEdge_App.jsx` כלל (2 שורות ב-`main.jsx`). ⚠️ **הצימוד היחיד שאינו אפס: סחיפת מספרי שורה** — עריכות `:954-984`/`:3946` יושבות **מעל** `:4076`/`:4503`/`:6672` ⇒ **הגל השני מאתר יעדים לפי תוכן ⛔ ולא לפי המספרים הרשומים.** ⛔ **האיחוד ⛔ לא הוכרע — הכרעת ניב.** ⚠️ **ותוקן דיווח קודם:** 86% מהקובץ (`:1159-8189`) הוא `export default function SwingEdge()` אחת ⇒ **בעלוּת-קומפוננטה ⛔ חסרת כוח-הפלייה כאן**; המבחן הוא **שורות**.

---

## ⏭️ הבא בתור — ⛔ **מצטט** את `docs/NEXT.md`, ⛔ אינו רשימה משלו

> 🔴 **מקור-אמת יחיד = `docs/NEXT.md`.** כאן **מזהה בלבד**; ההנמקה, דרך היישום ומדד הסגירה חיים שם ⛔ ולא כאן.

1. ⏭️ אחוז P&L חי ב-SHORT מחלק במחיר הנוכחי במקום במחיר הכניסה — **`B-186`** (`W1b`)
2. ⏭️ מחירים קשיחים-בקוד מרונדרים כחיים מתחת לתג `🟢 LIVE` — **`B-185`** (`W1c`)
3. ⏭️ סכומים לא-ממירים תחת סמל מטבע החשבון — **`B-187`** ⚠️ **המלצת Code, ⛔ לא הכרעה**

⛔ **אין להמליץ סדר בתוך קטגוריה** — הבחירה של ניב. שאר הפתוחים ב-`docs/BACKLOG.md`.

---

## ⏸️ חסום / ממתין לניב

- 🔴 **`C-030`** — באנר הערבוב: הצילום עצמו בוטל, ⛔ לא חסימה על פריט אחר.
- 🔴 **גל ב׳ — 13 שורות ת"א באגורות.** המיגרציה הורצה; חסום על **הודעה מוקדמת למשתמש `e403e391`**.
- **ארבע הכרעות מוצריות** (⛔ אף אחת אינה משימת קוד): פנייה ל-12 הרדומים · ועוד שלוש.
- **`why_stopped_r2`** (`prior_campaigns=why_stopped` · `audience=stuck_users`) — ⛔ לא הורץ. **`B-088`**
- **מיגרציה שנכתבה ולא הורצה** (§12): `b69b295` — מכנה מסונן ב-`admin_rpcs`.
- **אימות ידני ממתין:** אונבורדינג ב-₪ בחלון אנונימי · §1-§4 של M · 2 קבצי Sheets של `a0556783290`.
- **חסום על גישה:** Stripe Test Mode · Search Console · Workspace · QA מובייל · ערכת OCR · עו"ד על `/privacy` · מלאי סודות.
- ⏳ **ממתין לאות, ⛔ לא למשימה:** תשובת עומרי על `fix_mobile_upload` — אימות אנדרואיד.
- ✅ **`INBOX` ריק — שלוש השורות עברו טריאז' 26.08:** `B-244` (20.08) · כבר-מוכרע `B-173`→`D-050` + `B-168` פתוח (21.08) · `M-004`/`M-005` (23.08).

---

## ⚠️ סיכונים פתוחים

> ⛔ **שורה אחת לסיכון, ומזהה.** הפירוט חי ב-`BACKLOG`/`audits` ⛔ ולא כאן — זה מה שניפח את הקובץ פי 9.

- 🔴 **9 ממצאי `P0` פתוחים מה-DEEP-AUDIT** — כסף/אבטחה/משפט שמשתמש רואה **היום**. `B-184`…`B-192` + `B-191` (הבטחת מחיקת חשבון שהקוד ⛔ אינו יודע לבצע).
- 🔴 **ניגודיות AA — `27/37` צמתים מתחת ל-`4.5:1` בבהיר, `3/37` בכהה, הגרוע `1.67:1`.** ✅ **נמדד בדפדפן חי 27.08** (`C1`) ⇒ ⛔ **אינו עוד «מחושב».** 🔴 **`18/27` הם טוקן שנכשל בעצמו** ⛔ ולא הקס חורג. **`B-245`** · **`M-008`**
- ⚠️ **גוון ה-accent של `StatCard` ⛔ אינו מרונדר** — שתי הצהרות רקע על אלמנט אחד; `5/5` הכרטיסים לבנים. **`B-251`**
- ⚠️ **חמישה שמות accent ⇒ שלושה צבעים** (`cyan`≡`purple`) · **מחלקת גבול צובעת טקסט ב-`!important`**. **`B-252`·`B-253`**
- 🔴 **`Heebo` ⛔ אינו מרונדר ב-`/app` — `style` inline ב-`:3946` מבטל את `--font-hebrew`; העברית ב-fallback של מערכת ההפעלה.** ⚠️ **מפריך את `M8`/`T8` של `AUDIT-design-system`** — Inter **כן** מרונדר. **`B-254`**
- 🔴 **ה-CSP הוא Report-Only בלי אספן** (`vercel.json:18`) — כותרת שנראית כמו הגנה. **`B-003`**
- 🔴 **כל מדד GA4 מלפני 05.08 בלתי-שמיש** (`INCIDENTS#13`). **`B-105`**
- 🔴 **17.08 — רשת ההתראות שתקה 47מ׳ 44ש׳ ו⛔ איש לא ידע** (`INCIDENTS#17`). **`B-163`·`B-164`**
- **`react-router` GHSA-qwww-vcr4-c8h2 (high) — סיכון מקובל, ⛔ לא באג.** **`B-104`**
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — מגיע מהלקוח; 40/59 מוקדמות מההרשמה. **`B-035`·`B-036`**
- ⚠️ **42 `catch {}` ריקים** ב-`SwingEdge_App.jsx`+`AdminPanel.jsx` — ⛔ אף אחד לא נבדק. ה-DEEP מדד **192 catch · 72 ריקים · 0 שמדווחים**. **`B-016`·`B-195`**
- ⚠️ **ארבעה ממצאי §11 מגל `636b490` שנמדדו ו⛔ לא תוקנו.** **`B-019`·`B-022`·`B-024`·`B-025`·`B-050`·`B-068`·`B-070`**
- ⚠️ **`B-171`/`D-052` — המרווח שנסגר בו (25.08) אינו יציב לצמיחה.** 440 קרדיטים/יום.
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות** — ⛔ לא נטש בשקט. **`B-034`**
- 📏 **שתי סריקות דיסקורד, ⛔ אפס תיקונים:** 19.08 (3 מדידות) · 21-23.08 (5 מדידות) — `docs/audits/AUDIT-scan-2308.md`. **`B-147`·`B-150`·`B-166`…`B-175`**
- 📌 **הכלל שנחקק 08.08: מספר שנרשם פעם ולא נמדד שוב אינו עובדה.** **`B-028`**
- ✅ **סיכון הגיזום נסגר מכנית 26.08** — `test:registry` §6.1 חוסם היעלמות מזהה בין קומיטים, ו-§3 מודד בתים. ⚠️ **זה מה שהיה חסר ב-12.08** כשנעלמו `B-051`…`B-083`.

- 🧾 **`W1a` השאיר ארבעה פריטים ⛔ ולא אפס** — **`B-258`** (ארבעת מספרי שכבת השורשים חיים **גם** ב-`M-007`, ו-§8.6 מודדת את העותק שב-`BACKLOG` בלבד ⇒ `M-007` הצהיר `254` בזמן שהמדוד `257`, **והשער היה ירוק**) · **`B-255`** (תקרת הנפגעים של `INCIDENTS#18` ⛔ לא נמדדה; ⛔ אין לשאול «8/41» מ-`#12`) · **`B-256`** (`Referer` נושא query — ⛔ מחוץ ל-`beforeSend`; ה-fragment ⛔ לעולם ⛔ אינו נשלח שם ⇒ הטוקן ⛔ אינו נחשף) · **`B-257`** (מניות אסרציות ב-`CLAUDE.md` §7 נסחפו: `test:write` 25→29 · `test:instrument` 252→395 ⇒ כלל «אותו קומיט» הופר פעמיים בשקט).
