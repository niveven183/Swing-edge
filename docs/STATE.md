# STATE — מצב חי

> מתעדכן ע"י Claude Code **בסיום כל משימה**, לפי סיום משימה ולא לפי לוח שנה.
> קובץ מצב, לא ארכיון — מה שנסגר נגזם, לא נצבר.

עודכן: 2026-08-02 · HEAD: 0aff647
<!-- HEAD = הקומיט שהמצב הזה מתאר. פיגור של קומיט אחד הוא מובנה: הקובץ נכתב לפני שה-hash קיים. -->


---

## 🔴 עכשיו

🆕 **T9 — מטבע בייבוא. הקוד נסגר ✅ 02.08; ⛔ הפיצ'ר אינו חי עד שניב מריץ את המיגרציה.**
`docs/plans/PLAN-2026-08-02-import-currency.md`.

**מה נשבר אצל המשתמש.** `a0556783290` (יצחק גלובינסקי) דיווח על הפסד של ~$280,000
על חשבון של ~50,000₪. נייר בבורסה בתל אביב **נקוב באגורות**, `normalizeRow` הפשיט
את סימני המטבע ולכן המספר נכנס חסר-הקשר, ולעסקה **לא היה שדה מטבע בכלל** — כלומר
המערכת לא יכלה לדעת ולא יכלה לומר. פי 100 בכל מחיר, וסימן `$` קשיח על התוצאה.

**שני שערים, בלתי תלויים במכוון — לא אחד עם שני תנאים:**
· **שער היחידה** (`§ג.3`) — אריתמטי. יחס בין שווי העסקה לשער × כמות מכריע אם
  השורה נקובה בשקלים או באגורות. אין ניחוש: יחס שאינו 1 ואינו 0.01 → `bad_unit`,
  השורה נדחית עם קוד סיבה ויורדת ב-CSV הדחיות.
· **שער הנייר** (`nonSecurityKind`, `§ג.4`) — זהותי. IBI רושם חיוב מס כ**קנייה**
  של נייר-דמה בשער 100, ולכן הפועל לגיטימי ומה שאינו נייר הוא ה**נייר**. השער
  האריתמטי **מוכח שאינו תופס** את השורות האלה: כמות 666.94 × שער 100 מייצרת יחס
  0.01 בדיוק, זהה לציטוט אגורות חוקי. איחוד השניים היה מייצר שער עיוור.

**המטבע הוא תכונה של החשבון, לא רק של העסקה** — `settings.accountCurrency`
(USD/ILS, בורר בהגדרות). זהו **שלב א' של תמיכה ב-₪**, נבנה ככזה מהיום הראשון.
היקף T9 נשאר **ייבוא + תצוגה נכונה** ותו לא: אין המרת שער, אין נרמול בין-מטבעי,
אין סכימה חוצת-מטבעות שמתיימרת להיות סכום.

**סכום מעורב אינו מוסתר, הוא מוצהר.** `statsCcy(st)` מחזיר את המטבע היחיד של
היומן כשיש בדיוק אחד, ונופל ל-`accountCurrency` כשיש יותר; `MixedCurrencyBanner`
אומר במפורש שהסכום אינו סכום אמיתי ומדפיס את הפילוח פר-מטבע. §2 — המספר מתויג,
לא נעלם.

**אפס פגיעה — נעול.** אין `UPDATE` שקט על העסקאות הפגומות (**31/50** — נמדד
02.08). **המספר 41 שהופיע בתיאור המשימה מדד אוכלוסייה אחרת:** 41 הוא מספר
העסקאות ה**פתוחות בכל המערכת** (41/90), לא העסקאות הפגומות של יצחק. §2 —
מספר בלי שם אוכלוסייה נדד בין שתי אוכלוסיות בלי שאיש שם לב. סטופ שנכתב בדיעבד
או מחיר שחולק ב-100 בדיעבד הם נתון מומצא. התרופה יזומה-משתמש: מחיקה וייבוא מחדש,
עם מייל שני שמפרט **אילו עסקאות למחוק ואילו להשאיר**.

**מה שנתפס רק בדפדפן, אחרי ש"הקוד היה גמור" — 16 מוקדי `$` קשיחים.** קריאת DOM
חיה הציגה עסקה שקלית כ-`$120 / $74 / … / ₪130 / +₪1,000.00`: המחיר והרווח בשקלים,
הכניסה והסטופ בדולרים. אימות קוד לבדו לא היה תופס את זה.

**תיקוני שלמות מספרית לפי AUDIT-2026-07-27 — קבוצות 1 ו-3 נסגרו ✅.**
קבוצה 3 (חוזה ה-`null` ב-R) סגרה את FIN-007/008/009/010/011/012/032/034: `rMultiple`
הוא `number | null`, `avgR` וכל נגזרותיו לעולם אינם `0` פיקטיבי, וכל מדד R מדווח
`rSampleSize` לצד הערך — גם במסך, גם ב-PDF. `+0.00R` לעסקה בלי `stop` אינו קיים
עוד באף מסך או ייצוא. שער חדש: `npm run test:rcontract` (11 בלוקים) בתוך `verify`,
כולל שני שערי הקבלה — תיק בלי אף סטופ מחזיר `avgR: null` ולא 0, ו-BE יושב
באוכלוסייה שלישית שמשקלה סוגר ל-1.
**תיקון הייבוא — שלב א' נסגר ✅ (29.07).** המילון קיבל את משפחת "שער" ואת "סוג פעולה",
ו-`fieldForHeader` הוחלף ב-`matchHeader` שמדרג לפי ספציפיות במקום לפי סדר הכרזת השדות.
IBI ואלטשולר נקלטים אוטומטית מקצה לקצה. אגב כך כובה באג חי: `"l/s"` התנרמל ל-`"l s"`,
ולכן `"stop loss price"` ו-`"total shares sold"` מופו ל-`side`. `test:import` עלה
מ-6 ל-10 תרחישים, כולל שער baseline חוסם על 6 הפיקסצ'רים המקוריים.
✅ **"הכול פתוח" של IBI נפתר (01.08)** — `src/import/fifoMatch.js` משדך קנייה↔מכירה
לפי טיקר, כולל סגירות חלקיות. רץ **רק** כשזוהה פרופיל ברוקר; המסלול הגנרי לא נגע.
`test:import` 28 תרחישים, 17 הקודמים ללא תזוזה. ⏭️ נותר להודיע ל-`a0556783290`.
**קבוצה 2 (מקור אמת אחד) נסגרה ✅ 30.07 — ראה למטה. שלבים א'+ב'+ג' לייבוא נסגרו ✅.**
~~⚠️ לא לאמת אף אחת מהן מול 36 עסקאות הדמו~~ — **האזהרה התיישנה ונסגרה 02.08.**
עסקאות הדמו אינן קיימות עוד (`count(*) filter (where is_demo)` = **0/40**); ניב
אישר שהמחיקה הייתה מכוונת ושלו. אין יותר אוכלוסיית דמו לאמת מולה — ולכן אין
יותר אזהרה. המדידה הנוכחית: **90 עסקאות · 11 משתמשים · 49 סגורות · 41 פתוחות ·
0 דמו** (02.08, אחרי הייבוא של יצחק; קודם לכן 40 · 10 · 26 · 14).

✅ **קבוצה 4 (תאריכים) נסגרה — T6, 02.08.** כל 8 הממצאים תוקנו **בקוד בלבד**:
אפס מגע בנתונים, אפס backfill, אפס מיגרציה. ההנחה שהובילה לתוכנית ("14 עסקאות
`CLOSED` בלי `closedAt`") הופרכה במדידה — כל 14 הן `OPEN` עם `exit IS NULL`,
כלומר `closedAt: null` הוא הערך ה**נכון** שלהן לפי חוזה `deriveCloseState`.
מקור-אמת חדש: `realizedAt()` / `realizedDayKey()` ב-`src/utils.js` — עוגן הזמן
של כל חישוב שמדבר על מתי רווח **מומש** (`closedAt`, ובנפילה לאחור `date`).
עקומת ההון, drawdown, רצפים, חלונות שבוע/חודש, לוח השנה, הדוח החודשי ו-Weekly
Review כולם עברו מ**כניסה** ל**סגירה**. בנוסף: `AntiEdgeLock` קיבל חישוב שבוע
ISO-8601 אמיתי, ושלושה `toISOString()` במסלולי **קריאה** הוחלפו ב-`localDayKey`.

✅ **T8 — קבוצה 7 (עקביות תצוגה) — נסגר 02.08. האודיט סגור: 7/7 קבוצות.**
`docs/plans/PLAN-2026-08-02-display-group7.md`. **שער עשירי חדש: `npm run test:format`**
(`scripts/format-test.mjs`, 47 אסרשנים) בתוך `verify`, אחרי `test:rcontract`.
מתוך 14 ממצאי הקבוצה: **2 כבר היו סגורים** לפני התחלת המשימה (FIN-026 — Profit
Factor אינסופי, נחסם ב-`Number.isFinite` ב-`SwingEdge_App.jsx:3912`; והחצי של
FIN-046 שנוגע ל-`avgR`), **8 תוקנו כאן**, ו-**4 יצאו מההיקף כי הם מנוע ולא תצוגה**
(רשומים ב-⚠️ למטה, כל אחד עם שורת תזמון).
- **שני הממצאים שהבריף ביקש לאמת — שניהם כבר היו מתוקנים ב-`9581b78`.** `+0.00R`
  בייצוא PDF לעסקה בלי סטופ אינו קיים (`SwingEdge_App.jsx:446` קורא `m.rMultiple`
  גולמי ומרנדר `fmtR`), והצבע על `—` כבר ניטרלי (`#94a3b8`/`#64748b` ב-`:456`,`:532`).
  **לא נגעתי בהם.** האודיט תיאר מצב שקדם ל-T3
- **שורש משותף לכל הקבוצה — שתי מלכודות JS.** `-0 >= 0` הוא `true`, ולכן הפסד
  קטן מדי מכדי לשרוד עיגול נבדק כרווח והוצג **ירוק עם `+`**; `NaN >= 0` הוא
  `false`, ולכן מדד שהתפוצץ נפל לענף השלילי והוצג `-$NaN`. שניהם נסגרו במקום
  אחד: `isNegativeValue()` ב-`src/utils.js:114` הוא עכשיו **מבחן הסימן היחיד**,
  והצבע והטקסט קוראים ממנו — הם כבר לא יכולים לסתור זה את זה
- **FIN-028/029** — `fmt$` נבנה מחדש מעל ליבה משותפת `money(n, digits)`:
  לא-סופי → `—` · `-0` → `-$0.00` · `maximumFractionDigits: 2` (היום `fmt$(1234.567)`
  החזיר `+$1,234.567`). **FIN-030** — `formatPct`/`formatReturnPct` איבדו את
  `Number(v) || 0` שהפך "לא נמדד" ל-`0%` בטוח. **מצב ריק ממשיך להציג `0%`** —
  `EMPTY_STATS` (`src/lib/tradingStats.js:242`) מזרים `0` מספרי אמיתי
- **`fmt$0` — פורמטר חדש, דולרים שלמים.** קידום של `money()` המקומי מ-
  `WeeklyReviewTab.jsx` לרמת `utils.js` (§13 מקור-אמת-אחד), עם הגנת `NaN`/`-0`
  שלא הייתה שם. משרת תאי לוח שנה וצירי גרפים, שבהם אין מקום לאגורות
- **FIN-042/043/045/046b** — הסימן חזר לטקסט: תא יום מפסיד בלוח השנה (היה `$450`
  בלי מינוס, הצבע לבדו נשא את המידע) · `SwingEdge_App.jsx:4906` · כותרת הדוח
  החודשי (היה `$-1,234`) · ציר ה-Y וה-tooltip בגרף החודשי
- **FIN-031** — `GrowthPredictor` הציג **`+-0%` בירוק**: לוגיקת `"+"` הופעלה על
  מחרוזת שכבר נשאה מינוס מהעיגול. `pct` פוצל ל-`pctNum` (מספר) ו-`pct` (מחרוזת
  מלאה כולל סימן), ההשוואה רצה על המספר **לפני** העיגול, ואפס קיבל צבע **ניטרלי**
  (slate) — אפס אינו איתות
- **FIN-044 — התיקון החשוב בקבוצה: התיעוד לימד את ההפך מהאמת.** הנוסחה
  `R = (Exit − Entry) ÷ Initial Risk` נכונה ל-LONG בלבד; ב-SHORT מנצחת היא נותנת
  R **שלילי**. `src/utils.js:44-46` חישב נכון לפי `side` מאז ומעולם — רק הטקסט
  פיגר. תוקנו **10 מחרוזות** (`avgR` + `rMultiple` × en/he/es/pt/ar), והמשפט
  שנוסף ב-T3 על עסקה בלי סטופ נשמר כלשונו בכולן. `docs/SwingEdge-Terms-Glossary.md`
  ו-`src/data/glossary.json` **נגזרו מחדש באותו קומיט** (`npm run glossary`) —
  אחרת הפיצול בין המקור לתיעוד חוזר מיד
- **10 ממצאים חדשים מציד §11, מאותה משפחה, נכנסו להיקף.** שלושה מהגרפ הראשוני:
  `DayTradesModal.jsx:221`
  (`(pnl || 0)` — אפס שקרי, **וגם** `$-12.34` עם הסימן במקום הלא נכון) ·
  `TradeCalendar.jsx:132` (כותרת סיכום החודש) · **`fmtR` עצמו** — `-0 >= 0` הפך
  הפסד זעיר ל-**`+0.00R`**, בדיוק FIN-028 במטבע אחר, ולא נרשם באודיט.
  `fmtR(0) === "+0.00R"` — הערך הקפוא ב-`rContract-test.mjs:174` — **לא זז**
  (`Object.is(0, -0)` הוא `false`)
- **7 ממצאים נוספים נתפסו רק בדפדפן, אחרי שהקוד כבר "היה גמור".** האודיט ייחס את
  ציר ה-`$${v}` ל-`MonthlyReportTab` בלבד, אבל מסך "ניתוח ביצועים" הראה `$-900`
  בפועל: **5 צירי P&L ב-`SwingEdge_App.jsx`** (`:4916` רווח/הפסד לפי עסקה ·
  `:4968` לפי יום בשבוע · `:5256` לפי חודש · `:5283` לפי רגש · `:5432` פיזור),
  ועוד **2 כרטיסי Edges/Anti-Edges** (`:3463`,`:3479`) שהדפיסו `$-1380` דרך
  `` `${edge.totalPnL.toFixed(0)}` `` עם `$` ליטרלי. כולם → `fmt$0`.
  **הלקח: הגרפ מצא את מה שחיפשתי, המסך מצא את מה שלא ידעתי לחפש.** שער
  `test:format` הורחב באסרשן שמכסה גם את `SwingEdge_App.jsx` (סה"כ 47)
- **RTL — נבדק ונמצא תקין, לא באג.** בהקשר `dir="rtl"` המינוס מופיע ויזואלית
  בקצה **הימני** (`$1,380.40-`). זו ההתנהגות התקנית של bidi ולא סחיפה: הסימן
  יושב בתחילה הלוגית, שהיא בעברית הצד הימני — בדיוק היכן שהקורא מתחיל.
  אומת מול `Intl.NumberFormat("he-IL")`, שמפיק פריסה זהה. **אין מה לתקן**

✅ **T7 — קבוצות 5+6 + 5 המסלולים הפרטיים + ולידציית עריכה — נסגר 02.08.**
`docs/plans/PLAN-2026-08-02-engine-group56.md`. **שער תשיעי חדש: `npm run test:engine`**
(`scripts/engine-test.mjs`, 6 בלוקים) בתוך `verify`. הבייסליין הוכח לפני התיקון —
הסוויטה רצה על הקוד המקורי והפילה **25 אסרשנים**, בעוד אסרשני-הבקרה (`GO accuracy
→ 0.5`, `התאמה מדויקת עדיין 1`, `ראיה טרייה עדיין נועלת`) עברו. שער שנכשל בכל דבר
אינו מבחין; זה כן.
- **FIN-036** — `matched` **הוסר לחלוטין** מ-`matchIdeaToEdge`. סף 0.75 לא היה נגיש
  בבנייה (כל הציונים החלקיים האפשריים הם 0/⅓/½/⅔). הסף עבר לצרכן: `EDGE_MATCH_MIN = 1`
  ב-`DecisionCoach.js:447` וב-`GrowthTracker.js:61`. `matchIdeaToAntiEdge` **לא נגעה** —
  ה-`matched` שלה הוא התאמה-מדויקת ולכן אמיתי
- **FIN-037** — CAUTION יצא מחישוב הדיוק. `SCORABLE = {GO, SKIP}` · דלי לא-נמדד מקבל
  `accuracy: null` ולא ניקוד מלאכותי · **המכנה מוצהר**: `scoredN` + `accuracyBasis: "GO/SKIP"`
  (§2 — אין מנה בלי מכנה). הצרכן היחיד הוא `SwingEdgeAI.js:129 getCalibration`, שאין לו
  צרכן UI — **אפס מספר גלוי זז**
- **FIN-038** — `LOCK_STALE_WEEKS = 6`. נעילה שהראיה האחרונה שלה בת יותר מ-6 שבועות
  יורדת מ-`locked` ל-`warnings`. **התנאי של ניב מומש**: כל הודעת נעילה חיה נושאת
  `החסימה תוסר אוטומטית ב-YYYY-MM-DD`, מרונדר ב-`IntelligenceUI.jsx:563`
- **FIN-013** — ה-trend בכרטיס ההון נגזר מ-`curEquity` (אותו בסיס כמו הערך), לא מ-P&L
  סגור בלבד. **FIN-015** — פורמטר אחד (`pct`) לכרטיס ול-tooltip. **FIN-017** — Best/Worst
  setup ממוין ב-`edgeScore` (Wilson × R) במקום win-rate גולמי; "100% על 3" כבר אינו מנצח
  edge מוכח. **FIN-014 — ✅ התיישן, נסגר בלי פעולה** (`:3390` כבר קורא `winLossBeSub`)
- **חמשת המסלולים הפרטיים — 3 אוחדו, 2 נשארו במכוון:** `MonthlyReport.js:158,372` →
  `getClosed` + `outcomeRates` (שלושה דליים, `breakEven` נוסף לפלט); `GrowthPredictor.jsx:204`
  → `getClosed`. `TradeDNA.js:185` ו-`EditTradeModal.jsx:37` **לא אוחדו** — ראה `DECISIONS.md`.
  `normalizeRow.js:107` **נמחק כרישום שהתיישן** (הוא לא מסנן "סגור" כלל)
- **ולידציית עריכה (שלב ד')** — `EditTradeModal.validate()` קורא עכשיו את **אותו**
  `validateTradeInputs` שמגן על הזנה חדשה. **אפס מימוש שישי.** ⚠️ חל על **שמירות חדשות
  בלבד** — אפס מגע ברשומות קיימות
- **באג נלווה שנסגר באותו שינוי:** עסקה בלי `stop` לא הייתה ניתנת לעריכה כלל
  (הוולידציה דרשה `stop > 0` בניגוד להחלטת הייבוא "עסקה בלי סטופ חוקית"), ו-`handleSave`
  היה כותב `Number("") === 0` — כלומר מסלול השחתה שהיה נפתח ברגע שהנעילה תוסר. שניהם תוקנו
- **מדידת פרודקשן (read-only, Supabase MCP, לפני הביצוע):** גיאומטריה הפוכה
  **0 long + 0 short / 17 עם סטופ = 0%** → שלב ד' לא חסום · `CLOSED` בלי `exit`
  **0/40** · BE **0/26 סגורות** · בלי סטופ **23/40 = 57.5%**. כלומר איחוד "סגור"
  ו-BE הם **אכיפת חוזה, לא שינוי מספר** — היום הם בוחרים אוכלוסייה זהה

**`test:coach` (111) תוקן ✅ (29.07)** — ראה `docs/plans/PLAN-2026-07-29-coach-fixtures.md`.
הפיקסצ'רים ב-`coach-invariance-test.mjs` קיבלו `status:"CLOSED"`, `exit` (במקום
`exitPrice`) ו-`shares:20` (חסר לגמרי קודם — בלעדיו `calcTradeMetrics` היה מחזיר
`NaN` בשקט). עכשיו הבדיקה מכסה **גם** שחמש עסקאות ה-`seeded` באמת עוברות דרך
`getClosed`/`calcTradeMetrics` (אסרשן שמירה חדש #111), לא רק אינווריאנטיות
פרופילים כמו קודם. Characterization לפני/אחרי: `verdict` זהה בכל 5 התרחישים;
שני שינויי `confidence` (`earnings-window` 62→100, `no-target` 40→64) הם הביטוי
הישיר של המנוע שסוף-סוף מקבל את 5 העסקאות — לא רוככה אף ציפייה.
⚠️ **נותר לא-מכוסה:** פיקסצ'רי `mkClosed` חסרים `createdAt`/`date`, ולכן
`dayOfWeek()` הוא תמיד `null` עבורם — אף קומבינציית-ממדים ב-`EdgeFinder` שכוללת
את ממד ה-"יום" לא נבדקת אי-פעם ע"י `test:coach`. לא תוקן במכוון (מחוץ לגבול
המצומצם של המשימה — הוספת שדות רק כפי שנדרש ל-`calcTradeMetrics`).

במקביל, שבוע הבנת משתמשים — GA4 הותקן ✅. נותר: **צעד ידני חובה בקונסולת GA4**
(ראה למטה) · retention day-7 · מייל ל-5 פעילים.

**T2 — חילוץ `useTradingStats.js` לפונקציה טהורה נסגר ✅ (30.07).** `src/lib/tradingStats.js`
חדש מכיל `computeTradingStats(trades, capital, calcTradeMetrics)` — אותה לוגיקה
בדיוק, מאומת ב-`diff -w` מול הגרסה הישנה (רק הבדלי wrapper/whitespace, אפס
הבדל לוגי). `useTradingStats.js` הוא עכשיו `useMemo` דק שעוטף אותה. אפס שינוי
ערך/סקאלה/באג במכוון — זו הכנה ל-T3, לא תיקון. שער חדש: `test:tradingstats`
(`scripts/tradingstats-test.mjs`, 7 בלוקים כולל baseline קפוא ב-
`scripts/fixtures/tradingstats-baseline.json`) בתוך `verify`. `test:coach`
נשאר 111=111 לפני ואחרי (התלות הטרנזיטיבית המשותפת — `statisticalModels.js` —
לא נגעה). ראה `docs/plans/PLAN-2026-07-29-extract-trading-stats.md`.
**תוצר מרכזי: טבלת הבדלים מדויקת בין שני היקומים הסטטיסטיים, שמזינה את T3
(קבוצה 2 בפריט הבא בתור #3):**

| ציר | `useTradingStats.js` (הישן/tradingStats.js החדש) | `statisticalModels.js` |
|---|---|---|
| סקאלת win-rate | 0–100 תמיד | 0–1 (המרה ל-100 רק בגבול מנוע האינטליגנציה) |
| אוכלוסיית "סגור" | `status==="CLOSED"` בלבד | `status==="CLOSED" && exit!=null` (`getClosed`) |
| סיווג BE (`pnl===0`) | נופל דרך implicit — לא נספר לא כ-win ולא כ-loss, אבל כן בתוך `closed.length` (`winRate+lossRate<100` כשיש BE) | דלי שלישי מפורש `rPopulations().be`, בשימוש ישיר ב-`expectancyWeights`/`expectedValueR`/`kellyFraction` — אין מקבילה ל-EV/Kelly ב-tradingStats.js בכלל |
| null handling ל-R בלתי-מדיד | מיושר — `rMultiple: null`, לא 0 (חוזה קבוצה 3) | מיושר — זהה |
| `maxDrawdown` | שדה `%` (מספר כמו 12.5 = 12.5%) | פונקציה גנרית שמחזירה שבר 0–1 גולמי על סדרת equity | **התנגשות סקאלה/סמנטיקה של אותו שם שדה — מוקש ל-T3** |
| `profitFactor` | `Infinity` אפשרי כשאין הפסדים; זהה ב-`groupAndAnalyze()` (`bySetup[]`/`byEmotion[]` וכו') | (טרם הושווה ישירות — לא בסקופ T2) |

**ממצאי ציד T2 (§11 — תיעוד בלבד, לא תוקן, מוזנים ל-T3):**
- **5 מועמדי חישוב-inline כפול ב-`SwingEdge_App.jsx`** שמשכפלים מדד שכבר קיים
  באחד משני המנועים: `:3091` (סינון win/loss ידני ל-badge), `:485` (best-setup
  win-rate בתוך `generateSmartLessons`), `:535-536` (avgWin/avgLoss ידני
  באותה פונקציה), `:4720`+`:4862` (emotion win-rate ידני בייצוא PDF),
  `:4868-4879` (לולאת streak-history ידנית לייצוא PDF). כולם מועמדים ל-T3
  (קריאה ממנוע יחיד במקום חישוב מקומי) — לא תוקנו.
- **מפת צרכנים:** `useTradingStats` נצרך ב-`SwingEdge_App.jsx` (המסך הראשי) וב-
  `WeeklyReviewTab.jsx:57` (`wStats` — `totalPnL`/`currentStreak`/`total`/`winRate`/`bySetup`).
  לא נמצאה כפילות תצוגה (אותו ערך מוצג פעמיים ממקורות שונים).
- **חקירת FIN-030 (`profitFactor: ∞`):** רק `journalStats.profitFactor`
  (`SwingEdge_App.jsx:3599`) מוצג אי-פעם על מסך, וכבר מוגן (`isFinite(...) ? ... : "∞"`).
  ⚠️ **סיכון Infinity רדום ולא-מוצג נמצא**: `bySetup[]`/`byEmotion[]`/וכו'
  ב-`groupAndAnalyze()` (`src/lib/tradingStats.js:244`) מחשבים `profitFactor`
  באותה נוסחה, ללא הגנה — כרגע לא נצרך ע"י שום מסך, אך יתפוצץ בשקט אם ייפתח
  צרכן חדש (למשל טבלת פירוט-לפי-סטאפ). מוזן ל-T3 כדרישת הגנה.
- ~~**שלוש `.claude/worktrees/` ישנות נמצאו**~~ ✅ **נסגר 31.07** — הוסרו אחרי אימות
  ש-`git status --short` ו-`git log main..HEAD` ריקים בשלושתן. ראה §✅.

**T3 (קבוצה 2) — מקור אמת סטטיסטי אחד — בוצע ונסגר ✅ (30.07).**
`docs/plans/PLAN-2026-07-30-fin-group2-unify.md`. FIN-001..006 סגורים.
`getClosed` (`status==="CLOSED" && exit != null`) הוא **ההגדרה היחידה** של "עסקה
סגורה" בכל האפליקציה · BE הוא **דלי שלישי מפורש** (`winRate+lossRate+beRate = 100`)
בכל רמה — כללי, `bySetup`, `byEmotion`, `byMarket`, `byEntryQuality`, `byDayOfWeek`,
`topEdges`, `antiEdges`, `lastWeek/lastMonth` · `statisticalModels` נשאר 0..1 ו-
`tradingStats` מחזיר 0..100 עם **גבול המרה יחיד ומסומן** · `maxDrawdown` →
`maxDrawdownFraction` · שלוש מימושי `profitFactor` → מסלול אריתמטי אחד
(`grossPnl` → `profitFactorFromPnls`), עם `Infinity` כ**סנטינל מוצהר** (לא `null`:
`isFinite(null)===true` היה עובר את ההגנה הקיימת ואז `null.toFixed(2)` = מסך לבן).
**דיף מול baseline: 123 שדות נוספו · 0 הוסרו · 0 ערכים השתנו** — כל מספר קיים
שרד ביט-בביט, כולל `profitFactor` (סחיפת ULP שנחזתה **לא התרחשה**: היפוך סימן
וסכימה באותו סדר מדויקים ב-IEEE-754, ולכן אין מה "לנעול"). שער חדש בכל פיקסצ'רה:
`totalTrades + openTrades + לא-נספרות === trades.length`. פיקסצ'רה 6
(`closedWithoutExit`) מכריחה את החוזה להיות מוכח — עסקה `CLOSED` בלי `exit` **אינה**
BE, ונעלמת **בהצהרה** ולא בשקט.
שלב 0 (אבחון read-only) העלה **ארבעה תיקוני הנחה** שנשמרים כאן לתיעוד:
- **מדידה חיה 30.07** (שאילתה אחת, read-only): 116 עסקאות · `CLOSED` 102/116 ·
  **`CLOSED` ו-`exit IS NULL` = 0/102** · **עסקאות BE = 0/102**. כלומר שתי
  ההחלטות הנעולות הראשיות (`getClosed`, דלי BE) אינן משנות אף מספר לאף משתמש
  היום — הן אינווריאנטים הגנתיים, ובהתאם ציר המשתמשים ב-§8 הוא אפס
- **14 העסקאות אינן "CLOSED בלי exit"** — הן `CLOSED` עם `closedAt IS NULL`
  ו**יש** להן `exit`, ולכן `getClosed` משאיר אותן באוכלוסייה. אפס קשר להחלטה 1
- **אף אחת מ-5 הפיקסצ'רות אינה מכילה `CLOSED` בלי `exit`** → החלטה 1 אינה
  ניתנת להוכחה בסוויטה הקיימת; התוכנית מוסיפה פיקסצ'רה 6
- **FIN-006 כבר תוקן** (`MonthlyReport.js:59,206` קוראים את הבוליאני המועשר
  מ-`isFollowedPlan`) — נותר רק אסרשן נעילה

**ממצאי ציד T3 — מה נסגר בביצוע:**
- ✅ **קונבנציית BE שלישית** (`App:469-470`, `generateSmartLessons`) — **מתה
  באיחוד**: `setupMap` הוחלף ב-`stats.bySetup`. לא היה אתר שישי
- ✅ **teaser המנטור** (`App:2238`) — חובר ל-`getClosed` + `outcomeRates`. היה
  אתר `winRate` inline שמיני שלא הופיע במפת T2. **המספר לא זז** (המכנה כלל BE
  גם קודם), אבל האוכלוסייה והסיווג כעת משותפים
- ✅ **`App:4853` = `"Neutral"` לרגש חסר** — מת באיחוד. `stats.byEmotion` שם
  עסקה בלי רגש ב-**`"Unknown"`**. רגש חסר אינו רגש רגוע
- ✅ **`TradeCalendar.jsx:77` + `DayTradesModal.jsx:62`** (ציד של ניב) — שני מסכים
  שחישבו `wins/n` inline. **חלון הזמן נשאר מקומי** (המנוע מקבץ לפי יום-בשבוע
  וחלונות מתגלגלים — אין לו מקבילה ל"חודש קלנדרי"), אבל **הסיווג עבר** ל-
  `outcomeRates`. האוכלוסייה תוקנה במקור: `App:3692` → `getClosed(filteredTrades)`,
  ו-`DayTradesModal` יורש דרך `tradesByDate`
- ✅ **`App:5430` `calcSetupSuccess`** — אתר inline **שביעי** שלא הופיע במפת T2.
  חובר ל-`stats.bySetup.find(...)`. כרטיס Playbook והמטריצה ב-Analytics כבר
  אינם יכולים להראות שני מספרים לסטאפ אחד
- `statisticalModels.maxDrawdown` (`:135`) ו-`isSignificant` (`:159`) — **אפס
  מייבאים**. שונה שם ל-`maxDrawdownFraction` (החלטה 4); `isSignificant` לא נגעו

**שבעה מסלולי `status === "CLOSED"` — הכרעה פרטנית (בקשת ניב):**

| אתר | הכרעה |
|-----|--------|
| `App:148` `generateEquityCurve` | חובר ל-`getClosed` |
| `App:231` `allClosed` (PDF חודשי) | חובר ל-`getClosed` |
| `App:1570` `closedTrades` | חובר ל-`getClosed` |
| `App:1575` `closedCountAll` | חובר ל-`getClosed` |
| `App:2238` teaser מנטור | חובר ל-`getClosed` (+`outcomeRates`) |
| `App:3692` prop ל-`TradeCalendar` | חובר ל-`getClosed` — **לא היה ברשימה**, נמצא רק כי הוא כתוב במרכאות בודדות |
| `App:5430` `calcSetupSuccess` | מת — הוחלף ב-`stats.bySetup` |
| `App:2981` `const closed = tr.status === "CLOSED"` | **נשאר במכוון** — תצוגה בלבד (צביעת תא), ואתר השימוש כבר קורא `closed && tr.exit != null` |

**שינויי תצוגה שנובעים מהאיחוד (מה שהמשתמש יראה אחרת):**
- תג ה-Win Rate בדשבורד ובכרטיס החניך: `12W / 8L` → `12W / 8L / 2BE` **רק כאשר
  `be > 0`**. במקרה הנפוץ (0 BE בפרודקשן היום) אין שינוי גלוי
- גרף הרגשות ב-Analytics: עסקה בלי רגש עוברת מ-`"Neutral"` ל-`"Unknown"`,
  וסדר העמודות הוא כעת P&L יורד (היה סדר הופעה שרירותי)
- כרטיס "הרגש החזק ביותר": שובר-שוויון מפורש `winRate → count` (היה סדר הרשימה
  הלבנה), זהה לשובר-השוויון של "הסטאפ החזק ביותר" שמעליו
- "הסטאפ החזק ביותר" בתובנות היומן: עסקה בלי `setup` מתויגת `"Unknown"` (היה
  `"Undefined"`)

## ⏭️ הבא בתור

00. **🆕 אין מסלול לשליחת מייל לנמען יחיד — כל תשובה למשתמש בודד נשלחת ידנית.
    לשקול endpoint פשוט בגל התקשורת.** (ניסוח ניב, 02.08.) `email-campaign.yml`
    בורר בין **שני קהלים קבועים** ואין לו פרמטר נמען; שאילתה עם `limit 1` בוחרת
    נמען שרירותי ולכן אינה תחליף. שני המיילים ליצחק (T9) נשלחו ידנית מהג'ימייל
    של ניב.

0b. **🆕 T9 · OCR **מחוץ להיקף** — `api/ocr.js` לא נגעו בו.** המודל קורא מחירים
    מגרף TradingView ואינו יודע דבר על אגורות מול שקלים; גרף של נייר בת"א יזרים
    מספרים גדולים פי 100 לטופס בדיוק כמו הייבוא לפני התיקון. **הפער ידוע ואינו
    מתוקן.** דורש פרומפט משלו — נגיעה ב-`api/ocr.js` מחייבת `test:ocr` לפני ואחרי.

0. **🆕🔴 גל הסטופים — עסקאות נכנסות בלי `stop`, וזו בעיית מוצר.**

   **המדידה (02.08, על 40 השורות החיות):** `stop IS NULL` ב-**23/40** מכלל
   העסקאות. מבין ה**סגורות** בלבד — **23/26 = 88.5%** בלי סטופ. **17/40** עסקאות
   כן נושאות סטופ. גיאומטריה הפוכה (סטופ מעל הכניסה ב-LONG או מתחת ב-SHORT):
   **0** — כלומר מי שכן ממלא, ממלא נכון. אין כאן באג הזנה; יש שדה שרוב הזמן
   נשאר ריק.
   ⚠️ §2 — המכנה כאן קטן ומרוכז: 22 מתוך 23 החסרים שייכים למשתמש בודד
   (`ad1a0494`), והחציון פר-משתמש הוא **0.0%**. **אין להציג "57.5%" כהתנהגות
   של ציבור המשתמשים** — הוא מודד את הרכב האוכלוסייה.

   **המשמעות — בעיית מוצר, לא בעיית חוזה.** חוזה ה-R תקין מאז T3 (`9581b78`):
   עסקה בלי סטופ מחזירה `rMultiple: null`, מוחרגת מהמונה **ומהמכנה**, וכל מדד R
   מדווח `rSampleSize`. שום מספר אינו משקר. אבל **התוצאה של חוזה נכון על נתון
   חסר היא מסך ריק**: המשתמש רואה `—` במקום R, וכל שכבת ה-R של המוצר — Coach,
   DNA, Growth, הדוח החודשי — אינה יכולה לומר לו כלום. **המנוע החזק ביותר
   באפליקציה מושבת ע"י שדה ריק אחד.**

   **התזמון:** גל עצמאי, **אחרי מקבץ האבטחה ולפני B1**. לא "בדרך" לתוך משימה
   אחרת — הוא נוגע בטופס ההזנה, בייבוא, ובקופי, ומגיע לו פרומפט משלו.

   **שלוש גישות — טרם הוכרעו, החלטת ניב:**
   · **א' — חובה רכה.** הסטופ נשאר שדה חופשי, אבל שמירה בלעדיו דורשת בחירה
     מפורשת ("אין לי סטופ") במקום השארה שקטה של ריק. יתרון: מבחין בין "לא הזנתי"
     ל"אין". חיסרון: חיכוך בכל שמירה.
   · **ב' — ברירת מחדל מחושבת.** הצעת סטופ מ-1% הסיכון לעסקה ומגודל החשבון,
     כערך **מוצע וניתן לדריסה**. יתרון: אפס חיכוך. ⚠️ חיסרון חמור: ערך מוצע
     שנשמר בלי מחשבה הוא **נתון מומצא** שנכנס למנוע כאילו היה החלטת מסחר —
     בדיוק סוג הכשל השקט של §2.
   · **ג' — התרעת Coach.** בלי לגעת בטופס: ה-Coach אומר למשתמש כמה עסקאות שלו
     אינן מדידות ומה זה עולה לו. יתרון: אפס סיכון לנתונים. חיסרון: המלצה שאפשר
     להתעלם ממנה לנצח.

   ⚠️ **אזהרת אפס-פגיעה — נעולה מראש:** **אין `backfill` ל-23 העסקאות הקיימות.**
   סטופ שמוזן בדיעבד אינו החלטת מסחר, וכל R שייגזר ממנו הוא בדיה. הן נשארות
   `null` לנצח. הגל מטפל ב**עסקאות חדשות בלבד**.

   **מדידת הצלחה:** אחוז ה**עסקאות החדשות** (נוצרו אחרי התיקון) שנושאות `stop`,
   נמדד **שבוע** אחרי הפריסה — עם המונה, המכנה, ופילוח פר-משתמש. לא "האחוז
   הכולל ירד", שיזוז גם בלי ששום התנהגות תשתנה.

1. **תיקון ייבוא — שלבים ב'+ג'.** שלב א' (מילון + דירוג התאמה) נסגר 29.07 ✅ —
   ראה `docs/plans/PLAN-2026-07-29-import-phase-a.md`.
   ⚠️ משתמש `a0556783290` ממתין ליומן שמיש. ✅ שלב ג' נסגר 01.08 — עסקאות IBI
   נסגרות עכשיו ב-FIFO. ⏭️ **נותר להודיע לו** (§10.2 — פידבק בלי אחד משלושת המצבים).
   **02.08 — הוא בקהל של קמפיין `import_and_ocr_live`** (חשבון פעיל ומאומת, 1 מ-37).
   ⚠️ **הקמפיין לבדו אינו סוגר את §10.2:** הודעה גנרית בקול החברה אינה מענה לפידבק
   שלו ("הכול נכנס כפתיחת עיסקה") ואינה נושאת את hash התיקון (`f375478`).
   ⏭️ החלטת ניב: מענה אישי בנוסף לקמפיין, או הכרעה מנומקת שהקמפיין מספיק
   ⚠️ שובר-השוויון של `ticker` (`SYMBOL_RE` ב-`detectColumns.js`) הוא היוריסטיקה
   אמריקאית: נייר ישראלי מספרי (`1159250`) אינו נתפס כסימבול. **נמדד 01.08 על
   הקבצים האמיתיים — הבעיה עמוקה יותר: אין סימול בקובץ כלל.** ראה ⚠️ ייעודי
   ⚠️ **🆕 התקן אינו מוצג לפני ההעלאה (02.08).** `imp_step1_body` אומר "קובץ CSV
   או Excel מהברוקר או מיומן אחר" בלבד; שלוש עמודות החובה (`ticker`+`entry`+
   `shares`, `synonyms.js:24`) מתגלות רק אחרי שהקובץ כבר נותח, באזהרת שלב 2.
   המשתמש מגלה שהקובץ שלו פסול רק בדיעבד. **עלה בניסוח קמפיין `import_and_ocr_live`
   — המייל מפרסם את התקן, האפליקציה לא.** ⏭️ שורת תקן ב-`imp_step1_body`
   ⚠️ **🆕 T9 (02.08) — הוא חזר עם באג חמור יותר: מטבע.** מניות ת"א באגורות
   נכנסות פי 100 ומסומנות `$`. אומת במספרים: חשיפה ממוצעת **2,845% מההון** ב-16
   העסקאות הישראליות מול **7%** ב-19 האמריקאיות **באותו קובץ**; ÷100 → 28%.
   ה-"280,000$" שדיווח הוא **−281,140 בסגירות 31.03** = ≈2,811 ₪ אמיתיים.
   בנוסף **15 מ-50 עסקאותיו אינן ניירות** (מס · המרות מט"ח שנרשמות ב-IBI כקנייה
   בשער 100). **41/50 עסקאותיו פגומות; 19 האמריקאיות תקינות.**
   **מכנה הנזק: 1 מ-11 משתמשים (9%)** — אף אחד אחר לא ייבא קובץ ישראלי.
   ✅ **חלופה C אושרה 02.08** — מטבע כתכונה של **החשבון**, לא רק של העסקה, וזהו
   **שלב א' של תמיכת ₪**. תוכנית מלאה: `docs/plans/PLAN-2026-08-02-import-currency.md`,
   ממתינה לביקורת (§9 שלב 2). גבול ההיקף מפורש שם — 8 פריטים בפנים, 4 בחוץ.
   ⏸️ **מייל אישי ליצחק — נוסח, לא נשלח.** אין בריפו מסלול לשליחה לנמען יחיד
   (נספח ח' בתוכנית). **ניב שולח ידנית ל-`a0556783290@gmail.com`** — וזה מה
   שסוגר §10.2 מול הפידבק שלו, לא הקמפיין. ⏭️ מייל סגירה שני אחרי שהתיקון יעלה.

   **1b. ייבוא שלב ב' — ✅ הושלם (01.08). קבצי IBI/אלטשולר נקראים ומוצגים;
   נותר T5 FIFO לסגירת עסקאות.** התוכנית — `docs/plans/PLAN-2026-08-01-import-phase-b.md`.
   זיהוי שורת כותרת דינמי (ב-IBI הכותרות בשורה **11** — אומת) · טביעת אצבע על
   מערך העמודות המלא (8/11 ב-IBI, 9/13 באלטשולר) · whitelist של סוגי פעולה:
   **10 מתוך 12 סוגי השורות ב-IBI אינם עסקאות** ו-**5 מתוך 11 באלטשולר**
   (המספרים המשוערים היו 6 מתוך 8) · גזירת `side` מסימן הכמות ב-IBI (עקבי
   243/243) ומטקסט באלטשולר (**הכמות שם חיובית תמיד, גם במכירות**) ·
   `shares = Math.abs(shares)` · מונה דילוגים גלוי + i18n he/en.
   ⚠️ **אין "פורמט ישראלי".** פרופיל נפרד לכל ברוקר — לשני הקבצים אפס מוסכמות משותפות.
   ✅ **מנה 1 בוצעה (01.08)** — משימות 1–5 + פיקסצ'רים + תרחישים 11–15:
   `src/import/brokerProfiles.js` חדש · `matrixToTable(matrix, headerIdx)` ·
   `opts.sideResolver` + `opts.tickerResolver` · `test:import` 15 תרחישים,
   **10 המקוריים ללא שינוי**. מדוד על הפיקסצ'רים: IBI 4 עסקאות / 5 דילוגים
   מתוכם מכירה = SHORT עם `shares` חיובי; אלטשולר 6 עסקאות / 5 דילוגים.
   ✅ **מנה 2 בוצעה (01.08)** — משימות 6–10, שכבת התצוגה נסגרה:
   `counts.skipped` + `skippedByKind` ב-`buildImport` (ו-`counts.total` הפך
   למכנה אמיתי: `valid+rejected+duplicates+skipped`) · ת1 המונה הגלוי בשלב 3,
   **מוצג גם באפס** · 8 מפתחות i18n he+en (4 מהתוכנית + `imp_unresolved_symbols`
   ל-❓4-ג + `imp_no_header_found` + `imp_profile_unknown` + באדג' הפרופיל) ·
   ת2 שני ה-`catch` הבולעים נסגרו (`console.error` עם שם הקובץ/גודל/גיליון
   + `e.message` כשורת משנה למשתמש) · ת3 · `"מכר"` → SHORT.
   `detectProfile`/`applyProfile` חוברו ל-`ImportJournalModal`.
   `test:import` 17 תרחישים, **15 הקודמים ללא תזוזה**.
   **אומת בדפדפן** על `ibi-full.xlsx` דרך הקומפוננטה האמיתית: "זוהה פורמט: IBI",
   כותרות בשורה 11, מונה `יובאו 4 · דולגו 5` עם פירוט 5 הסוגים,
   `3 עם שם נייר בלבד`, והעסקאות שיצאו — מכירה qty −10 → SHORT עם `shares=10`.
   כשל רועש אומת על קובץ פגום: `Unsupported ZIP encryption` הגיע גם לקונסול
   וגם למסך.

   **1b′. ⏭️ השלמת סימול — לא לביצוע עכשיו (הוחלט 01.08).** ההכרעה של ❓4
   מכניסה עסקאות תחת שם הנייר; שני צעדים משלימים אותה מאוחר יותר:
   (א) **רזולוציה אוטומטית שם-נייר → סימול דרך `api/symbol-search.js` הקיים** —
   על **שמות ייחודיים בלבד** (לא שורה-שורה), עם cache. ⏸️ **מותנה בהוספת
   rate-limit ל-`symbol-search`** — כבר בתור, Gate 2.2 (§סעיף 8).
   (ב) **עריכת `ticker` ידנית בטבלת התצוגה המקדימה** לפני אישור הייבוא.

   **1c. ייבוא שלב ג' — מנוע FIFO (T5). ✅ הושלם (01.08).**
   `docs/plans/PLAN-2026-08-01-import-fifo.md` — כל 4 שלבי §9 הושלמו, שלוש
   ההכרעות של ניב (א' dupKey · ב' היפוך סדר · ג' פלט מלא) בוצעו כלשונן.
   `src/import/fifoMatch.js` חדש — פונקציה טהורה, אפס תלות ב-UI:
   זיווג קנייה↔מכירה לפי טיקר · **סגירות חלקיות הן הנורמה** (קנייה 100 שנסגרת
   ב-40+60 → שתי עסקאות סגורות; מכירה 50 מול קניות 20+30 → שתי סגורות) ·
   מכירה בלי קנייה קודמת = SHORT פתוח, לא שגיאה · **P&L לא נשמר** —
   `calcTradeMetrics` גוזר אותו · `stop` נשאר `null` מפורש (חוזה ה-R).
   **שער:** `fifo: !!applied` ב-`ImportJournalModal` — המנוע רץ **רק** כשזוהה
   פרופיל ברוקר; המסלול הגנרי לא נגע.
   `test:import` 28 תרחישים, **17 הקודמים ללא תזוזה**; פיקסצ'רה חדשה
   `ibi-fifo.xlsx`. שני מכנים מוצהרים ונאכפים באסרשן:
   שורות קובץ `rejected+skipped+accepted = total` · עסקאות
   `valid+duplicates = closed+open`.
   **אומת בדפדפן** בקומפוננטה האמיתית על `ibi-fifo.xlsx`: `6 תקינות / 0 דחויות /
   0 כפולות` · `4 עסקאות סגורות (שודכו קנייה↔מכירה) · 2 פתוחות` · 6 העסקאות
   שיצאו זהות למספרים שחושבו ביד (סכום P&L 2300).
   🔴 **באג שנתפס בעין ולא ע"י אף בדיקה:** המסך הציג `יובאו 6 עסקאות` ומתחתיו
   `7 יובאו עם שם נייר בלבד` — `unresolvedSymbols` נספר לפי **שורות מקור**
   בעוד המוצג הוא 6 עסקאות ה-FIFO. מנה בלי מכנה תואם (§2). תוקן: הספירה
   מחושבת מחדש על תוצרי ה-FIFO (5), + 3 אסרשנים בתרחיש 28.
   ⚠️ **זה השלב שפותר את "הכול נכנס כפתוח" של `a0556783290`.** ⏭️ נותר להודיע לו.
2. **שבוע הבנת משתמשים** — ⚠️ **Enhanced Measurement → "Page changes based on browser
   history events" חייב להיות ON** (SwingEdge הוא SPA; בלעדיו אף מסלול פנימי לא נספר
   ו-`/app` ייראה מת) · אימות חי של `gcs=G100`/`G101` · retention day-7 · מייל ל-5 פעילים
3. **שלמות מספרית — `docs/audits/AUDIT-2026-07-27-financial-integrity.md`** (46 ממצאים:
   🔴 27 · 🟠 14 · 🟡 4 · ⚪ 1). ⚠️ **הפריט הזה הוא קבוצות 2 ו-4 בלבד** — קבוצה 1
   בוצעה ✅, וקבוצה 3 קודמה לראש התור (פריט 1). שלושת התסמינים שדווחו (סתירת כרטיס
   ההון · +0.00R בלי stop · PROFIT FACTOR ∞) הם תסמינים, לא שורשים: לאפליקציה **שני
   יקומים סטטיסטיים מקבילים** (`useTradingStats.js` מול `statisticalModels.js`) החלוקים
   על הסקאלה, על סיווג ה-BE ועל אוכלוסיית "סגור", ועליהם חישובים inline
   ב-`SwingEdge_App.jsx`. הסדר שנותר: **קבוצה 2** (מקור אמת אחד) → **קבוצה 4** (תאריכים).
   **T2 נסגר ✅ (30.07)** — `useTradingStats.js` הוא עכשיו פונקציה טהורה
   (`src/lib/tradingStats.js`), טבלת ההבדלים המדויקת מוכנה (ראה 🔴 עכשיו למעלה),
   ו-5 מועמדי inline-duplication ב-`SwingEdge_App.jsx` מזוהים. **T3 (קבוצה 2
   בפועל — האיחוד עצמו) עדיין לא התחיל.**
   ✅ **שיעורי ה-`stop` נמדדו מחדש 28.07** — 23/78 עסקאות אמיתיות = **29.5%**
   (מצרפי כולל דמו: 20.2%), **חציון פר-משתמש 0.0%**. המספרים נכנסו לראש §6 באודיט.
   FIN-007 נשאר 🔴 כבאג, אך הדילול הוא ~30% ולא ~44% ומרוכז ב-1-2 משתמשים — לא
   התנהגות אוכלוסייה. **"מחיקת 173 העסקאות" הושלמה ע"י המשתמש**
4. **חובות מוצר בקופי** — תג CLOSED בקריפטו · waitlist בלנדינג · ניסוח דוח Growth ·
   **ריבוי עברי ב-`riskUnmeasured`** — "ל-1 עסקאות פתוחות אין סטופ" עבור `n=1`.
   דורש pluralization ב-i18n, לא תיקון נקודתי במחרוזת
5. **מיפוי קבצים במחשב** — read-only, דוח מקומי, לא לריפו · ואחריו **סידור בפועל**
   (מחשב + ריפו)
6. **Dispatcher → Actions** (IMAP)
7. **Trader Persona → Actions**
8. **Gate 2.2** — RLS audit מלא + בדיקת חדירה + rate-limit ל-symbol-search + נגישות
9. **₪ + Stripe + entitlement** — אין ₪ בקוד כלל
10. **B1 Multi-Account**
11. **Track A — לנדינג V2**
12. **אודיט 4 קבצי הידע** מול הקאנון (Minervini / O'Neil / Weinstein / Tharp)
13. **איכות OCR** — נבדק 27.07: עובד, לא מספיק טוב על גרפים עמוסים. `api/ocr.js`
    בנוי נכון (confidence + `validate()` + `nullResult` "לעולם לא לנחש") אך **מעולם
    לא נמדד ולא כויל**. אין Tesseract בריפו — Claude Vision בלבד
    (`MODEL = claude-sonnet-4-6`). **חסם: סט בדיקה של 20-30 גרפים + ground truth**
    (עם ניב, ראה ⏸️). אחרי המדידה: MODEL→Opus · חידוד prompt · כיול סף ה-confidence ·
    מסך אישור לפני שמירה עם הדגשת שדות בביטחון נמוך.
    🎯 **היעד הוא אפס טעויות שקטות — לא קריאה מושלמת.** 100% על קלט תמונה חופשי אינו
    בר-השגה ואסור לתכנן סביבו.
    ✅ **F1+F2+F3 נסגרו 01.08** — ראה `docs/plans/PLAN-2026-08-01-ocr-f123.md`
    ו-✅ נסגר השבוע. **החוסם היחיד שנותר לפריט הזה הוא סט ה-30** (ראה ⏸️):
    MODEL→Opus · חידוד prompt · כיול הסף — כולם דורשים מדידה, לא קוד
    ✅ **F4 (קוהרנטיות כיוון) נסגר 01.08** — ראה `docs/plans/PLAN-2026-08-01-ocr-f4-side.md`.
    ⚠️ **אמינות קריאת הכיוון עדיין לא נמדדה**: מבחן אחד מתוך שני מבחני QA חיים
    (01.08) החזיר כיוון הפוך. **1/2 הוא גודל מדגם, לא שיעור** — סט ה-30 הוא
    היחיד שיכול להפוך אותו למספר
14. **Lightbox לתמונת הגרף ב-Analyzer** (קטן, UX) — לחיצה על התמונה שהועלתה מציגה
    אותה מוגדלת. בקשת משתמש 31.07: "יש לי מלא סקרינשוטים של מסחר בפלאפון" — אימות
    שהתמונה הנכונה נבחרה, במיוחד כשה-OCR לא מילא
15. **העלאת כמה סקרינשוטים בבת אחת** — בקשת משתמש 31.07. **מותנה באיכות OCR
    (פריט 13)** — הכפלת קלט לפני שהקריאה מכוילת מכפילה גם את הטעויות השקטות
16. **🆕 `toFixed(2)` בצד הלקוח הורג מחירי קריפטו** (נמצא 01.08, סעיף ציד של
    `PLAN-2026-08-01-ocr-f123`). `api/ocr.js` שומר 12 ספרות מובהקות (`tidy`,
    `:104`) — **הלקוח מוחק אותן**: `String((0.00004).toFixed(2))` = `"0.00"`.
    חמישה אתרים: `SwingEdge_App.jsx:2419-2421` · `:2461-2463` · `:2565-2566` ·
    `:2573-2575`, וגם המחיר החי `:1894`/`:1923`. **כניסה 0.00 = חלוקה באפס
    ב-R.** דורש החלטת עיצוב (דיוק דינמי לפי גודל המחיר) — אינו "קטן ובטוח"
    ולכן לא נכלל ב-F1-F3
17. **🆕 `confidence: 90` על `stopDelta` שלילי** (נמצא 01.08 ב-baseline של
    `scripts/ocr-contract-test.mjs`, תרחיש D). `stopDelta = -4.39` בלונג:
    `computeEntry` דוחה את רגל ה-stop, נופל על ה-target בלבד, `validate` מאפס
    את ה-stop (✅) — אבל `rrOk` מחזיר `true` בוואקום כי `stopDelta > 0` נכשל,
    ו-Vision מסכים על ה-entry → `+5`. וריאנט של F4. הוקפא ב-`FROZEN.D` כדי שלא
    יזוז בשקט.
    🟠 **התסמין הנראה נסגר ב-F3 (01.08)** — `ocrDetection` מחזיר `partial`
    (entry מלא, stop חסר), והבאדג' כתום עם "זוהו חלק מהמחירים — השלם את השאר
    ידנית". **הסיבה עצמה פתוחה:** `api/ocr.js` עדיין מחזיר `confidence: 90` על
    קלט סותר, ומספר גבוה על קריאה חלקית הוא עדיין מספר לא-נכון בכל צרכן עתידי
    שיסתמך עליו ישירות ולא על `detection`
    ⚠️ **F4 (01.08) לא סגר את זה** — הוא טיפל בכיוון, לא בביטחון על קלט סותר.
    תרחיש D עדיין `confidence: 90` ו-`FROZEN.D` עדיין חוסם דריפט. **פתוח**
18. **🆕 סריקת `side` מול גיאומטריית `entry/stop` ב-`public.trades`** — read-only,
    ממצאים לניב לפני כל מגע (עקרון אפס-פגיעה). **הכרעת ניב 01.08: מחוץ להיקף F4,
    לא לבצע עכשיו.** ההקשר: עד F4 הכיוון שהמודל קרא דרס את בחירת המשתמש בשקט,
    ולכן ייתכנו עסקאות שנשמרו עם `side` שאינו מתיישב עם הרמות שלהן. **כמה — לא
    ידוע ולא נמדד**; אין בסכימה סימון מקור עסקה (ראה ⚠️ F6), ולכן אי-אפשר
    להפריד עסקאות-OCR מידניות בדיעבד, והסריקה תמדוד גיאומטריה בלבד
19. **🆕 השלמת QA מובייל** (נדחה לשבוע 03-09.08 בהוראת ניב, התחיל 01.08).
    **בוצע:** 2 מבחני OCR חיים (AFRM+MRVL) → חשפו את F4 ואימתו את F1 (מסך מלא
    → "לא זוהה" נקי). **נותר, מהטלפון, כמשתמש:**
    1. **OCR מלא אחרי F4** — שני הגרפים מחדש: קונפליקט מוצג? הודעה כתומה עם שני
       הכיוונים? ולידציה לא נדרכת במסלול תקין?
    2. **כרטיס האישור (Analyzer)** — קריא ב-406px, כפתורים באצבע, שום שדה לא
       נכנס עד "החל"
    3. **מסנן R ביומן** — "X עסקאות ללא R מוסתרות" מופיע ונעלם
    4. **כרטיס Profit Factor** — "אין הפסדים" במקום ∞
    5. **באדג' CLOSED על קריפטו** — נעלם אחרי התיקון?
    6. **ציד חופשי:** הרשמה→סיור→עסקה→סגירה→DNA→Settings→שפה EN/HE
    7. **Add to Home Screen** — התנהגות כאפליקציה
    **פורמט דיווח:** מסך · מה ראיתי · מה ציפיתי · צילום.
    ⚠️ **מסכים 1, 3 ו-4 חסומי-auth ל-Claude Code — עין אנושית היא האימות היחיד
    שלהם.** אין להצהיר עליהם "אומת" בלי דיווח של ניב
20. **~~T6 — קבוצה 4~~ ✅ בוצע ונסגר 02.08.** `docs/plans/PLAN-2026-08-02-dates-group4.md`.
    8/8 הממצאים (FIN-016/018/019/020/021/022/023/024) תוקנו בקוד; אפס מגע בנתונים.
    שתי הנחות הבריף נפלו במדידה ותועדו למעלה. **מה שהתגלה תוך כדי הביצוע:**
    · **ה-baseline הקפוא לא זז — וזו הייתה בעיה, לא הצלחה.** `mk()` ב-
      `scripts/tradingstats-test.mjs:88` מייצר `closedAt` **מתוך** `date`, ולכן
      בכל פיקסצ'ר קיים יום-הסגירה שווה ליום-הכניסה, ו**אף תרחיש — כולל ה-baseline
      הקפוא — לא היה מסוגל להבחין בין שני הסדרים.** השער היה עיוור בדיוק לבאג
      שהוא אמור לשמור עליו. נוסף תרחיש `6c` שבו `LONGHOLD` נכנס ראשון ונסגר אחרון
      (Max DD 14% לפי סגירה מול 12.5% לפי כניסה), ואומת שהוא **נכשל** על הקוד הישן.
      ⚠️ **הכללה לכל שער עתידי:** פיקסצ'ר שנגזר מעצמו אינו יכול לחסום באג בממד
      שממנו הוא נגזר. לפני שסומכים על baseline — לשאול מה הוא **לא** יכול לבטא.
    · **מופע תשיעי של FIN-016 שלא היה ברשימת ה-8** — `lastWeek`/`lastMonth` ב-
      `src/lib/tradingStats.js:146-147` סיננו על `m.date`. תוקן באותו מקבץ.
    · **שלושה `toISOString()` נוספים במסלולי קריאה** (`psychologyPatterns.js:61`,
      `MonthlyReport.js:223,305`) הוחלפו ב-`localDayKey`. ב-`psychologyPatterns`
      ההערה מעל השורה כבר אמרה "local time" בעוד הקוד עשה UTC.

**🆕 S1 — הקשחת אבטחה. תוכנית ממתינה לאישור:
`docs/plans/PLAN-2026-08-02-security-s1.md`.** אבחון read-only הושלם 02.08.
ממצאים שאינם בהיקף התוכנית או שממתינים לשלב נפרד:

- ✅ **`/api/health` — rate-limit 30/דקה לכל IP** (שלב א'). היה וקטור העלות
  היחיד שנשאר פתוח: כל קריאה לא-מטמונת שורפת credit של TwelveData + קריאת
  Finnhub, והמטמון (20 שנ') הוא per-instance ואינו מגן על מופעים קרים מקבילים.
  **השיא הלגיטימי הנמדד הוא 0.2/דקה לכל IP** — רצפת UptimeRobot היא בדיקה אחת
  ל-5 דקות, והקוראים המתוזמנים היחידים הם `sentinel.yml` (פעמיים בשעה) ו-
  `fleet-weekly.yml` (שבועי), כל אחד מ-IP חדש של runner. ⚠️ **`health.yml`
  מושבת** (`workflow_dispatch` בלבד, ללא cron) ואינו נספר — הדיווח המוקדם שאמר
  "3 workflows" שגה. 30/דקה = פי ~150 מהשיא. ה-CORS נשאר `*` במכוון (ניטור חיצוני)
- ✅ **`api/feedback.js` — דלף הודעת Postgres ללקוח נסגר** (שלב א'). `detail:
  dbError.message` הוחלף בלוג צד-שרת; הלקוח מקבל `insert_failed` בלבד
- ✅ **CSP ב-Report-Only + HSTS** נדחפו ל-`vercel.json` (שלב ב'). `vercel.json`
  אינו תומך בהערות, ולכן **מקור כל source מתועד כאן**. המיפוי נעשה **מהבאנדל
  החי, לא מ-grep על `src/`** — וזה מה שתפס את `api.emailjs.com`, ש-grep סטטי
  פספס לגמרי והוא יושב על מסלול הפידבק. מכאן והלאה: מיפוי מקורות = חילוץ מבאנדל.
  | source | מאיפה נמדד |
  |---|---|
  | `zicstkfkwhzvmdkzpidm.supabase.co` | הבאנדל החי + `list_projects` |
  | `o4511416120049664.ingest.us.sentry.io` | DSN בתוך `assets/sentry-*.js` (אזור US) |
  | `api.emailjs.com` | `FeedbackTab.jsx:189` דרך הבאנדל — **פספוס ב-grep הסטטי** |
  | `challenges.cloudflare.com` | Turnstile — script + iframe |
  | `s3.tradingview.com` · `www.tradingview.com` | `TradingViewWidgets.jsx:3` + iframe שהוידג'ט מזריק |
  | `s.tradingview.com` · `www.tradingview-widget.com` | **מדידת ריצה בלבד** — ראה ההפרה שנתפסה למטה |
  | `va.vercel-scripts.com` · `vitals.vercel-insights.com` | Vercel Analytics — script + beacon |
  | `www.googletagmanager.com` · `*.google-analytics.com` | GA4, `index.html` |
  | `fonts.googleapis.com`/`gstatic` · `api.fontshare.com`/`cdn.fontshare.com` | `index.html` + ה-CSS ש-fontshare מחזיר |
  | `financialmodelingprep.com` | `TickerLogo.jsx:18` |
  | `data:` + `blob:` | תצוגה מקדימה של OCR (`readAsDataURL`/`toDataURL`) והורדות CSV |

  ⚠️ **אין `report-uri`** — הפרות נראות בקונסולת הדפדפן בלבד. הוספת endpoint
  לדיווח היא משטח תקיפה חדש; ההליכה הידנית על המסכים היא מנגנון האיסוף
- ✅ **הפרת `frame-src` שנתפסה ב-Report Mode ותוקנה (02.08).** ההליכה על המסכים
  החזירה 3 הפרות (2 hosts), כולן `frame-src`, כולן בלוח הבקרה: `s.tradingview.com`
  (×2) ו-`www.tradingview-widget.com`. הסיבה: `s3.tradingview.com` הוא ה-host של
  ה**סקריפט**; ה-iframe שהוא מזריק מגיע מ-`s.tradingview.com` (בלי 3) ומקנן בתוכו
  frame שני מ-`tradingview-widget.com`. **שני ה-hosts אינם קיימים בקוד המקור ולא
  בבאנדל** — grep סטטי מחזיר רק `s3.` ו-`www.` — ורק מדידת ריצה תפסה אותם.
  אלמלא Report Mode, אכיפה הייתה משתקת את סרט הטיקרים בשקט. תוקן ב-`vercel.json`
- ⚠️ **כיסוי CSP ל-`api.emailjs.com` ולמסלול ה-OCR אומת ב-probe, לא בשליחה
  אמיתית.** בהליכת שלב ב' לא נשלח פידבק אמיתי (מייל + שורת פרודקשן) ולא הורץ OCR
  אמיתי (עלות קריאה); הכיסוי הוכח דרך ה-origin בבאנדל + `fetch(...,{mode:'no-cors'})`
  ו-probes ל-`data:`/`blob:`. **לאמת בשליחה אחת בכוונה אחרי אכיפת ה-CSP** — probe
  מוכיח שה-host מותר, לא שהזרימה המלאה עוברת
- ⏭️ **CSP אוכף** — פרומפט נפרד, רק אחרי שדוח ה-Report-Only ריק
- ✅ **בדיקת חדירה חיה ל-RLS (שלב ד', 02.08) — 109 ניסיונות, אפס דליפה חוצה-משתמש.**
  שני משתמשי בדיקה זמניים (`rls-probe-*@swing-edge.local`), 11 טבלאות, 20 RPC-ים,
  SELECT/INSERT/UPDATE/DELETE, גם כ-`anon` וגם כמשתמש מחובר לא-אדמין.
  **אפס מגע ב-38 המשתמשים האמיתיים** — כל ניסיון הרסני כוון לשורות של משתמשי
  הבדיקה בלבד, וכל RPC מסוכן (`admin_invite_send`, `admin_waitlist_approve`,
  `admin_delete_feedback`) נקרא עם UUID שאינו תואם **שום** שורה, כך שגם כשל
  הרשאה מוחלט היה נוגע באפס שורות ושולח אפס מיילים. **12/12 טבלאות חזרו
  לספירת הבסיס המדויקת** בסיום (38 · 90 · 8 · 57 · 26 · 0 · 2 · 0 · 1 · 0 · 1 · 94)
  - כל 11 הטבלאות עם RLS פעיל; `admins` עם **0 policies** = deny מוחלט
  - `anon` רואה 0 שורות בכל 11 הטבלאות; משתמש A רואה **רק את שורותיו**
  - A מול B: SELECT/UPDATE/DELETE → 0 שורות; INSERT עם `user_id` מזויף → 403
  - 16 RPC-ים של אדמין: לא-אדמין → 403 `not authorized`; `anon` → 401
    (ה-GRANT הוא ל-`authenticated`/`service_role` בלבד, `anon` נעצר לפני `is_admin()`)
- 🔴 **שתי חורי `WITH CHECK(true)` אושרו בפועל — `anon` כותב ל-`feedback`
  ול-`waitlist` (HTTP 201).** ⚠️ **הכתיבה עיוורת:** אין policy של SELECT ל-`anon`,
  ולכן התוקף כותב ואינו יכול לקרוא — הסיכון הוא **זיהום/הצפה, לא גניבת מידע**.
  `feedback.user_id` הוא `text` ללא FK ⇒ **ייחוס ניתן לזיוף**. זה הפריט האחרון
  של S1: מיגרציה עם 2–3 חלופות, ⛔ ניב מריץ, לא Claude
- ⚠️ **מלכודת מדידה שכמעט ייצרה דיווח הפוך:** probe עם `Prefer: return=representation`
  מחזיר 401 על אותו INSERT שמצליח בלעדיו — PostgREST קורא את השורה בחזרה, ול-`anon`
  אין SELECT. כלומר הכלי דיווח "החור סגור" בזמן שהוא פתוח. **בדיקת הרשאות חייבת
  להיבדק בלי `return=representation`, ואפס חייב להיות מוכח בבקרת-נגד**
- ⚠️ **שני מקורות אמת ל"מי אדמין".** `feedback` ו-`waitlist` מגדירות אדמין
  כ-`auth.jwt()->>'email' = 'niveven183@gmail.com'` (מחרוזת קשיחה בתוך ה-policy),
  בעוד כל השאר משתמשות ב-`is_admin()` מול טבלת `admins`. החלפת כתובת או הוספת
  אדמין שני תעבוד בחצי מהמערכת ותיכשל בשקט בחצי השני. לאחד ל-`is_admin()`
  **באותה מיגרציה** של החורים למעלה.
  **✅ 02.08 — נכנס למיגרציה, ובדיקת נעילה-החוצה עברה לפני הכתיבה (שתי הוכחות
  בלתי-תלויות):** (1) `admins` מכיל `92a06c0c-c407-42f0-8bf7-476d58f31c9d` =
  `auth.users.id` של `niveven183@gmail.com` (`match_ok = true`), ו-`is_admin()`
  הוא `exists(select 1 from admins where user_id = auth.uid())` ⇒ מחזיר true לניב;
  (2) **פאנל האדמין כלל אינו עובר במדיניות הזו** — `AdminPanel.jsx:335` קורא
  `rpc("admin_feedback_list")` ו-`:336` קורא `rpc("admin_waitlist_list")`, ושתיהן
  `SECURITY DEFINER` ולכן עוקפות RLS. 16/16 מ-RPC-ים של `admin_*` כבר משתמשים
  ב-`is_admin()`, 0/16 מקודדים אימייל ⇒ שלוש המדיניות הן **המקום היחיד בסכימה**
  שמקודד את הכתובת, ואינן על מסלול הגישה של ניב לפידבק
- ⚠️ **`redeem_mentor_invite` ללא throttle על ניסיונות.** קוד בן 8 תווים
  מאלפבית של 30 (≈6.6·10¹¹), אך זהו **מסלול הקריאה החוצה-משתמש היחיד ל-`trades`**.
  כרגע 0 שותפויות פעילות → סיכון נמוך היום, גדל עם אימוץ. דורש מיגרציה.
  **תזמון: S2 — הפרומפט הבא אחרי S1, או מיד עם השותפות הפעילה הראשונה,
  המוקדם מביניהם.** הטריגר מדיד: `SELECT count(*) FROM mentorships` > 0
  (נמדד 02.08: 0 שורות).
  **🆕 02.08 — נבחן להכנסה למיגרציה של S1 ו*נדחה מסיבה מבנית*, לא מתזמון:**
  ניסיון כושל עושה `raise exception` ⇒ הטרנזקציה מתגלגלת אחורה ⇒ **שום מונה
  ב-SQL אינו יכול לספור ניסיונות כושלים** בלי להחליף `raise` ב-`return`, מה
  ששובר את חוזה השגיאות שהקליינט מציג (`invalid code` / `code already used` /
  `code expired`). ⇒ throttle אמיתי = ניתוב דרך endpoint עם `rateLimit()`
  כמו `/api/feedback` = **שינוי קוד, לא מיגרציה**. ⏭️ S2
- ⏭️ **S2 · ייחוס `feedback.user_id` — FK אינו הפתרון.** FK ל-`auth.users` הופך
  את השדה ל*תקין* ולא ל*אמיתי*: הערך מגיע מהקליינט (`FeedbackTab.jsx:160`)
  ועובר דרך `api/feedback.js` עם ה-anon key **בלי להעביר את ה-JWT** ⇒ `auth.uid()`
  שם הוא תמיד NULL, ולכן אכיפת `user_id = auth.uid()` הייתה מאפסת את השיוך של
  כל 38 המשתמשים. הפתרון: העברת ה-JWT ל-`/api/feedback` וגזירת `user_id` בשרת.
  המיגרציה של 02.08 סוגרת רק את **הצורה** (uuid תחבירי או NULL)
- ⏭️ **S2 · חסם קצב ל-`waitlist`.** ה-INSERT יוצא ישירות מהדפדפן
  (`LandingPage.jsx:537`) עם ה-anon key, בלי proxy ובלי rate limit — בניגוד
  ל-`feedback` שכבר עובר ב-`/api/feedback` (3/דקה/IP). ה-DB **אינו רואה IP**
  (PostgREST אינו מעביר אותו), ומונה גלובלי היה מאפשר לתוקף אחד לחסום נרשמים
  אמיתיים. ⇒ הפתרון הוא `/api/waitlist` עם `rateLimit()`, שינוי קוד. המיגרציה
  של 02.08 סוגרת רק את צורת השורה (פורמט אימייל, אורכי `source`/`campaign`,
  `approved_at IS NULL`)
- ⚠️ **`'unsafe-inline'` יישאר ב-`script-src`** — `index.html` מכיל שני בלוקי
  `<script>` inline (פותר ערכת-נושא L83-96, GA Consent Mode L106-136) שסדרם
  load-bearing. nonce/hash דורש שינוי build, לא רק header. חוב מוכר
- ⚠️ **הסף של `quote`/`symbol-search` לא ישונה — במכוון.** השיא הלגיטימי הוא
  ~4.2 קריאות/דקה למשתמש (רענון 15 שנ' בשוק פתוח + Overview), מול סף 60/דקה =
  פי ~14. המרווח מכסה טאבים מרובים ו-NAT משותף. **הורדת הסף היא הסיכון**

## ⏸️ חסום / ממתין לניב

- **🆕🔴 T9 · מיגרציית `currency` — ⛔ טרם הורצה. §12: ניב בלבד.**
  `supabase/migrations/20260802140000_trades_currency.sql`
  (`alter table public.trades add column currency text not null default 'USD'`).
  **סדר הפריסה קריטי ואינו הפיך בשקט:** `currency` **לא** נוסף ל-`TRADE_COLUMNS`
  ב-`src/supabaseClient.js`, כי מפתח שקיים ברשימה ואינו קיים בפוסטגרס מפיל את
  **כל** ה-INSERT — כלומר כל שמירת עסקה באפליקציה. עד ההרצה: המטבע חי בייבוא,
  בתצוגה וב-`localStorage`, ו**אינו נשמר לשרת**.
  **אחרי שניב מריץ:** קומיט המשך בשורה אחת שמוסיף `currency` ל-`TRADE_COLUMNS`.
  הפריט נסגר רק אחרי אותו קומיט, לא אחרי המיגרציה.
- **🆕🔴 T9 · המייל השני ליצחק — נוסח מלא, ⛔ לא נשלח.**
  `docs/comms/2026-08-02-globinski-followup.md`. המייל אומר למשתמש **למחוק 31
  שורות ולייבא מחדש**, ולכן שליחתו לפני שהתיקון חי היא פגיעה: הוא ימחק נתונים
  ויקבל את אותו באג בחזרה. **שלושה תנאי שליחה, כולם חייבים:** (1) המיגרציה
  הורצה · (2) `currency` נוסף ל-`TRADE_COLUMNS` · (3) Vercel פרס ואומת בדפדפן
  שייבוא חוזר מייצר מחירים בשקלים ודוחה את 15 שורות-הדמה. עד אז המייל הראשון
  עומד בתוקף: "אל תמחק כלום".
  **המדידה החיה (50/50, לא 41):** 16 ניירות ת"א באגורות · 9 שורות מס ·
  6 המרות מט"ח · **19 עסקאות אמריקאיות תקינות שנשארות**. 9 שורות המס אושרו
  שוב בפרודקשן — השער השני אינו תיאורטי.
  §10.2: הפידבק של יצחק נשאר ⏸️ **חסום**, לא `Reviewed` ולא "טופל", עד השליחה.
- Google Workspace
- רשם הדומיין — איש קשר + Auto-Renew
- מלאי סודות
- החלטת ריפו פרטי מול דקות Actions
- **סעיף ה-`/privacy` ממתין לאישור עורך דין** — נוסח ונדחף 28.07, טרם אושר משפטית
- **סט בדיקת ה-OCR** — 20-30 גרפים + הערכים הנכונים. בלעדיו פריט 13 אינו מדיד
- **🆕 שדה טווח עסקה (סווינג/יומי/ארוך)** — בקשת משתמש 31.07. נוגע בסכימה + מסננים;
  לתזמן מול B1 (פריט 10). **החלטת ניב**
- **🆕 האם להודיע למשתמשים על שלושת המספרים הגלויים שזזו ב-T7 (02.08).**
  עקרון 27.07 — שינוי במספר גלוי דורש הודעה **ידועה מראש**. שלושה מספרים
  יכולים לזוז בפועל: (1) אחוז ה-trend בכרטיס ההון — נגזר עכשיו מ-`curEquity`
  ולכן זז **רק כשיש פוזיציה פתוחה**; (2) שם ה-setup המוביל/הגרוע ב-Weekly
  Review — מיון `edgeScore` במקום win-rate גולמי, יכול לנקוב בשם אחר; (3)
  הודעות נעילת anti-edge — נושאות עכשיו תאריך שחרור, ונעילה בת יותר מ-6 שבועות
  יורדת לאזהרה. **החלטת ניב: להודיע או לא**
- **🆕 עבודה לא-מקומטת ב-`tests-sentinel/sentinel-public.spec.js`** (נמצא 01.08
  ב-safety gate). מוסיף מעקב 5xx מצד-שלישי לסנטינל — עבודה קוהרנטית, לא זבל.
  האודיט לא נגע בו וקומט קבצים בשמות מפורשים. **החלטת ניב: להשלים או לגנוז**
- **מייל ל-`saridel`** — 22 עסקאות, **100% ללא stop, כולן סגורות**. הדפוס אינו ייבוא
  (ייבוא מייצר עסקאות פתוחות). המשתמש היחיד עם היסטוריה נקייה שיש ממנה מה ללמוד
- **🆕 S1 · הגנת סיסמאות דלופות מושבתת ב-Supabase Auth** (אבחון 02.08). בדיקת
  HaveIBeenPwned כבויה — משתמש יכול להירשם עם סיסמה שכבר דלפה. **תזמון: היום,
  02.08 — הגדרת קונסולה, לא קוד, ~30 שניות.** §12: ניב בלבד. הדרך: קונסולת
  Supabase → פרויקט `zicstkfkwhzvmdkzpidm` → **Authentication** → הגדרות
  הסיסמאות → הפעל **"Prevent use of leaked passwords"** → Save. תיעוד רשמי:
  https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection
  אין השפעה על משתמשים קיימים — הבדיקה חלה על הרשמה ועל שינוי סיסמה בלבד.
  אימות שבוצע: `get_advisors(security)` מחזיר `auth_leaked_password_protection`
  ברמת WARN. **הפריט נסגר כשה-lint הזה נעלם מהפלט**
- **🆕 S1 · שתי מדיניות RLS מסוג `WITH CHECK(true)`** — `feedback` INSERT ל-`public`
  (שם המדיניות: `Anyone can insert feedback`) ו-`waitlist` INSERT ל-`anon`
  (שם המדיניות: `waitlist_anon_insert`). מפתח ה-anon בבאנדל, ולכן תוקף כותב ישירות
  ל-`/rest/v1/` ו**עוקף את ה-rate-limit של `/api/feedback` לגמרי**. וקטור מכסה
  (500MB). **דורש מיגרציה → §12: ניב מריץ.** ראה `docs/plans/PLAN-2026-08-02-security-s1.md` §0.2א
  **תזמון: שתיהן במיגרציה אחת בסוף S1 (שלב ה'), מוצגת לניב ולא מורצת.**
  `waitlist` נכנס לאותה מיגרציה ולא לפרומפט נפרד — אותו וקטור בדיוק, ופיצול
  היה משאיר חצי דלת פתוחה.
  **🆕 02.08 — המיגרציה נכתבה ונדחפה: `supabase/migrations/20260802120000_s1_rls_hardening.sql`.
  ⛔ טרם הורצה. הפריט נסגר רק כשניב מריץ אותה ב-SQL Editor ו-`get_advisors(security)`
  מפסיק להחזיר `rls_policy_always_true` (היום: 2 מופעים — `feedback`, `waitlist`).**
  המיגרציה כוללת גם את איחוד `is_admin()` (הפריט שמתחת) ושני CHECK ברמת טבלה

## ✅ נסגר השבוע

### 🏁 גל T — הושלם 02.08. שמונה משימות, `AUDIT-2026-07-27` סגור

⚠️ **קראו לפי שם הקבוצה, לא לפי המספר.** קיימות **שתי מספורים** בריפו: הקומיט
`133e2f4` מסמן את עצמו `(T3 group 2)`, בעוד הבריף של T8 קרא ל**חוזה ה-R** T3.
שם הקבוצה חד-משמעי, המספר לא. הטבלה מסודרת **כרונולוגית**:

| # | מה נסגר | hash |
|---|----------|------|
| 1 | **קבוצה 1 — שרשרת הנתונים.** מסלול הכתיבה הפסיק לפגום בשורות | `80b5858` |
| 2 | **קבוצה 3 — חוזה ה-R.** `null` אינו `0`; כל מדד R נושא את המכנה שלו | `9581b78` |
| 3 | **חילוץ `useTradingStats` לפונקציה טהורה** — `src/lib/tradingStats.js` | `1426eef` |
| 4 | **קבוצה 2 — מקור אמת סטטיסטי אחד** (FIN-001…006) | `133e2f4` |
| 5 | **ייבוא — שידוך FIFO קנייה↔מכירה** לקבצי ברוקר | `f375478` |
| 6 | **קבוצה 4 — עקביות תאריכים.** עקומת ההון עוקבת אחרי החשבון, לא אחרי טופס הכניסה | `0c2e476` |
| 7 | **קבוצות 5+6 — מנוע.** אין דגלים מתים, אין דיוק מומצא, אין נעילות נצחיות | `603fb45` |
| 8 | **קבוצה 7 — עקביות תצוגה.** הסימן חזר לטקסט; `—` הוא לא `0` | `c904878` |

**מה שהגל השאיר אחריו:** `verify` עלה מ-**6 ל-10 שערים** — נוספו `test:rcontract`
(11 בלוקים), `test:tradingstats`, `test:engine` (6 בלוקים) ו-`test:format`
(47 אסרשנים). **מה שהגל לא סגר:** 4 ממצאי מנוע מקבוצה 7 (⚠️ למעלה, עם תזמון),
ו**גל הסטופים** (⏭️ פריט 0) — שהוא לא ממצא באודיט אלא מה שהאודיט חשף.

- **🆕 קמפיין `import_and_ocr_live` נשלח (02.08). `sent=37 · failed=0`**, run
  `30749079414`, דיסקורד HTTP 204. הקהל הוא `registered_users` — פרמטר חדש
  ב-`email-campaign.yml`: **37/38** משתמשי `auth.users` (חשבון `.local` מסונן,
  RFC 6762). האבחון הראה שהקהל הקודם אינו קירוב אלא אוכלוסייה אחרת —
  **21/38** רשומים אינם ב-waitlist ו-**40/57** מאושרי waitlist מעולם לא נרשמו.
  `limit=50` בפועל (ברירת המחדל 10 הייתה מותירה 27/37 בלי מייל). תוכנית:
  `docs/plans/PLAN-2026-08-02-user-update-campaign.md`. שלוש טענות בקופי תוקנו
  מול `TRUTH.md` לפני השליחה, ושמות הברוקרים הוחלפו בתקן הייבוא עצמו
  (`180d097`) — **אינו סוגר את §10.2 של `a0556783290`**, ראה ⚠️ ייעודי

- **🆕 מקבץ תיקונים קטנים — 7 סגירות (01.08).** תוכנית:
  `docs/plans/PLAN-2026-08-01-small-fixes-batch.md`.
  · **1א — החלטת המוצר במסנן ה-R התקבלה ומומשה:**
    **"עסקה בלתי-מדידה: גלויה תמיד, מוחרגת ממסנן R פעיל עם ספירה גלויה."**
    השער הוא `Number.isFinite` (לא `!= null`) — זהה להגדרת האוכלוסייה ב-`rValues()`
    ופוסל גם `NaN`. המסנן כבוי → הכל גלוי, ה-R מוצג `—`. המסנן פעיל → העסקאות
    מוחרגות **ונספרות**, והמספר מוצג ליד המסנן (`rFilterHidden`). הפריט ירד מ-⏸️
  · **1ב — `avgMaeMfe`:** `numOrNull` (שומר `v == null` **לפני** `Number()`, כי
    `Number(null)===0` ו-`isFinite(0)` הוא `true`), ומכנה נפרד לכל מדד — `maeN`/`mfeN`
    = מספר הערכים שקיימים באמת. אין מה למדוד → `null`, לא `0`
  · **2 — ריבוי בעברית:** `plural(t, key, n)` ב-`src/i18n.js` — מפתח בסיס + `_one`
    (+ `_two` לערבית), נפילה חיננית למפתח הבסיס. `riskUnmeasured` ו-`rFilterHidden`
    בכל 5 השפות
  · **3א — קריפטו:** `parseChartResult` הטביע תג `CLOSED` על BTC נע, כי
    `getMarketState()` הוא שעון בורסת NYSE. קריפטו מזוהה דרך `instrumentType` עם
    נפילה לסיומת `-USD`, ומסומן `OPEN` תמיד
  · **3ב — קופי רשימת ההמתנה (he+en):** מוביל בפעולה ונשען רק על שורות 🟢
    ב-`docs/TRUTH.md`, בניסוח המאושר שלהן
  · **3ג — "רשימת המתנה: N" → "סך שורות waitlist: N"** בדוח Growth ובמילון
    `user-analytics.mjs`. המספר הוא ספירת שורות הטבלה (כולל מי שכבר הוזמן והומר),
    לא אורך תור
  · **4 — `api/send-invites.js:134`:** `return` שקט על `SENTINEL_DISCORD_WEBHOOK`
    חסר → `console.error` שנוקב בשם המשתנה. **4ב (`MAIL_*`) לא בוצע בהחלטת ניב**
  · **5 — FIN-030:** `profitFactor: Infinity` הוצג כ-`∞`. **המנוע לא נגעו בו** —
    `Infinity` הוא סנטינל מוצהר (`statisticalModels.js:159-164`), ו-`null` היה עובר
    את `isFinite` ומתפוצץ על `.toFixed`. התיקון בשכבת התצוגה בלבד: `t.pfNoLosses`
  · **6-B — מסך אישור OCR באנלייזר:** תשובת ה-OCR נעצרת בכרטיס `OcrReviewCard`
    (module scope + `memo` + draft מקומי) ולא נכנסת לטופס עד "החל". שדה שה-OCR
    **לא** קרא מסומן "לא זוהה" במפורש. `analyzerOcrTickerRef` נדרך ב-Apply מהערכים
    **הסופיים** — תיקן המשתמש את הטיקר, החסימה נדרכת מחדש; הזין entry ידנית,
    היא משתחררת. `status: "review"` נופל דרך `ocrBadgeState` בדיוק כמו `"ok"`,
    ולכן ארבעת מצבי הבאדג' לא נגעו. `api/ocr.js` לא נגע
  · **7 — `CLAUDE.md §10.2` "אין פידבק יתום"** נכתב: שלושה מצבי סיום, `Reviewed`
    מוגדר במפורש כמצב ביניים. בנוסף **§7 היה מיושן** — תיאר שרשרת של 3 מבחנים
    כשבפועל יש 7 + build, ו-`test:coach` (111, לא 110)
- **🆕 קוהרנטיות כיוון ב-OCR — F4 (01.08).** תוכנית:
  `docs/plans/PLAN-2026-08-01-ocr-f4-side.md`. **מקור: שני מבחני QA חיים של ניב.**
  הדלתות שה-OCR קורא הן מגניטודות חסרות-סימן, ולכן `side` **לבדו** מכריע אם הסטופ
  נוחת מעל או מתחת לכניסה. עד F4 הכיוון היחיד היה `parsed.direction` — סיווג של
  המודל — והוא דרס את בחירת המשתמש בשקט.
  - **מבחן 1 (AFRM, כלי LONG):** המודל סיווג SHORT → הרמות חושבו הפוך
    (`79.74 = 74.09+5.65` במקום `68.44`). הרשת היחידה שתפסה הייתה
    `validateTradeInputs`, שחסמה שמירה.
  - **מבחן 2 (MRVL, כלי LONG):** הרמות חזרו **מדויקות**, אבל הטופס נשאר על SHORT
    כי הלקוח זרק את ה-`side` שחזר — וולידציה חסמה חילוץ מושלם.
  - **F4a (`api/ocr.js`) — היררכיית כיוון מפורשת, נמדדת.** הפרומפט מבקש שני שדות
    חדשים: `stopPrice`/`targetPrice` (מחירים אבסולוטיים, **cross-check בלבד,
    לעולם לא מקור מחיר** — אותו תפקיד כמו `entry` היום). `dirFromPrice` מאמת שה-
    **מרחק** של המחיר הנקרא מהכניסה תואם את הדלתא שכבר נסמכנו עליה, ורק אז מקבל
    את ה**סימן** כראיה. שלוש רמות, מדווחות ב-`sideSource`:
    `geometry` (נמדד) → `tool` (סיווג המודל) → `user` (הבורר). `sideConflict`
    מדווח שהתשובה שונה מהבורר. `max_tokens` 300→340 (שני שדות נוספים).
  - **F4b (`SwingEdge_App.jsx`) — קונפליקט מוצג, לא מוכרע.** זו **סטייה מכוונת
    מהפרומפט המקורי**, שביקש עדכון שקט של הטוגל. הפרכה שאושרה ע"י ניב: על מבחן 1,
    היפוך אוטומטי היה הופך את העסקה לעקבית פנימית, **הוולידציה הייתה עוברת**,
    ועסקת SHORT הייתה נשמרת בשקט על גרף שבו צויר LONG — כלומר הפיכת הכשל הרועש
    היחיד שתפס את הבאג לכשל שקט (§2). עכשיו: אין סתירה → סנכרון שקט; **יש סתירה
    → הטוגל לא זז**, ופס כתום מנקב את **שני** הכיוונים בשם. הפס משווה מול הטוגל
    **החי**, ולכן נעלם ברגע שניב בוחר — לשני הכיוונים.
  - **F4c — הבאדג' מול הוולידציה.** `ocrBadgeState` קיבל `contradicted`: קריאה
    `full`/`partial` שהטופס שמתחתיה גיאומטרית בלתי-אפשרי **אינה ירוקה**. לעולם לא
    `OCR ✓ 92%` ירוק לצד "קלט לא תקין" אדום.
  - **`posCalc` לא נגע במכוון** — אין לו שדה `side`, והסיזינג נגזר מ-`|entry−stop|`,
    זהה בשני הכיוונים. תועד בהערה בקוד.
  - **חוזה:** `scripts/ocr-contract-test.mjs` עלה מ-4 ל-8 תרחישים. **E** (גיאומטריה
    גוברת על הבורר) · **F** (אין כלי → הבורר עומד) · **G** (מבחן 1 של ניב — עכשיו
    מחזיר 68.44/84.75, מספרי הגרף עצמו) · **H** (מבחן 2 — `sideConflict: true`
    מדווח במקום להשאיר לניב להסיק מפס אדום). **A–D: אף ערך קיים לא זז** — קיבלו
    שני שדות חדשים בלבד, בקומיט נפרד עם נימוק.
  - **i18n:** `ocrSideConflict` · `ocrSideConflictReview` · `ocrContradicted`
    ב-5 השפות.
- **🆕 שרשרת הכשל השקט ב-OCR — F1+F2+F3 (01.08).** תוכנית:
  `docs/plans/PLAN-2026-08-01-ocr-f123.md`. אבחון: `AUDIT-2026-08-01-ocr-diagnosis.md`.
  **קדם לתיקון baseline קפוא** — `scripts/ocr-contract-test.mjs`, 4 תרחישים מול
  ה-handler האמיתי (רק Anthropic ו-GoTrue מזויפים). תרחיש A הוא חוזה הרגרסיה
  ונשאר **זהה-בייט** לפני ואחרי; רק B זז, וזו כל מטרת F2.
  - **F2 (`api/ocr.js`) — השורש.** בענף "אין כלי פוזיציה" ה-spread דרס את
    `nullResult.confidence`, ולכן שלושה `null` יצאו עם ביטחון המודל המלא (85).
    עכשיו `Math.min(modelConf, 25)` — מתחת ל-`MIN_CONFIDENCE` (40), כלומר כל
    צרכן קורא כשל. לא אפס במכוון: "בטוח שאין כלי" ≠ "לא הצלחתי לקרוא את התמונה".
  - **F3 (`SwingEdge_App.jsx`) — הבאדג'.** `ocrDetection()` גוזר את המצב ממה
    שהתמלא בפועל: `full` / `partial` / `ticker_only` / `none`, ו-`ocrBadgeState()`
    היא נקודת ההחלטה היחידה לשלושת הבאדג'ים (יומן · אנלייזר · גרף) שהיו שלוש
    מימושים כפולים. **`partial` לעולם אינו ירוק** — עסקה בלי stop מסומנת 🟠.
    כל אתר שומר את מפת הצבעים הליטרלית שלו (§13 Tailwind JIT).
  - **F1 (`SwingEdge_App.jsx`) — הרגרסיה שעומרי ראה.** `formOcrTickerRef` /
    `analyzerOcrTickerRef`: אחרי OCR שלא הפיק `entry`, אפקט הציטוט החי חסום
    מלכתוב לשדה ה-Entry עבור **אותו טיקר**. טיקר אחר משחרר אוטומטית, `force`
    (לחיצה מפורשת) גובר, ו-`useEffect` על ניקוי הבאדג' משחרר בכל 8 אתרי האיפוס
    בלי לפזר ניקויים ידניים. **המחיר החי עצמו לא נגע** — הצ'יפ עדיין מציג
    `LAST CLOSE`; רק המסלול לשדה נחסם.
  - **תצפיתיות.** `trackEvent()` חדש ב-`src/lib/consent.js` (הבעלים היחיד של
    `gtag`) שולח `ocr_result` עם `detected` + `confidence_bucket` — enum
    נמוך-קרדינליות בלבד, אף פעם לא טיקר או מחיר.
  - **i18n.** `ocrTickerOnly` + `ocrPartial` ב-5 השפות, ושלושת הבאדג'ים עברו
    ל-`t.*` — es/pt/ar היו מקודדים-קשיח he/en וקיבלו תרגום אגב כך.
  - **אומת בדפדפן** ב-406px (רוחב המכשיר של עומרי) על שרת dev בלי Supabase:
    (1) קריאה מלאה → שדות מלאים + `OCR ✓ 95%` ירוק, זהה ל-baseline A;
    (2) בלי כלי → Entry **ריק**, צ'יפ `LAST CLOSE $61.42` קיים, באדג' כתום,
    GA `detected: ticker_only`; (3) טיקר ידני בלי OCR → מילוי אוטומטי עובד
    כרגיל ואפס אירועי GA מזויפים.
- **שלוש `.claude/worktrees/` ישנות הוסרו (31.07)** — `cool-shaw-bf882b`,
  `naughty-haibt-efc05f`, `nervous-curie-c217a7`. שלושתן `git status --short` ריק
  ו-`git log main..HEAD` ריק, כלומר אפס קומיטים שאינם כבר ב-`main` — לא אבדה עבודה.
  היחיד שנמחק מעבר ל-checkout: `dist/` ו-`.claude/*.json` מקומיים. CLAUDE.md §5.3
  שוב נאכף.
- **Sentinel — עיוורון URL בדיווח 5xx (31.07).** האבחון הפריך חלק מההנחה:
  `page.on('response')` עם `status>=500` **כבר היה קיים** בשלושת ה-specs, וכל בדיקות
  ה-`curl` ב-`sentinel.yml` כבר נושאות את ה-URL בשדה `checked`. שני חורים אמיתיים
  נסגרו: (1) `msg.location()` בשתי ה-specs — ב-`sentinel-auth` הוא חושב רק כדי לסנן
  לפי `IGNORE_SOURCE` ואז **נזרק**, וב-`sentinel-public` לא נקרא כלל, כך שממצא
  `console_error` נשא רק את מה שהאפליקציה בחרה להדפיס; עכשיו הוא מצורף כ-`@ <url>`.
  (2) 5xx מהוסט צד-שלישי נפל בין כל הדליים ("by design") — עכשיו דלי `thirdparty_5xx`
  בחומרה 🟡 (`sentinel.yml:533` — נספר, לעולם לא מתריע מיידית), 4xx צד-שלישי נשאר
  מסונן כפי שהיה. **הסינון לא הורחב באף כיוון** — לכידה בלבד, כדי לא לייצר שקט חדש
- **מדד מילוי השדות — מכנה לפי רלוונטיות, ומכנה מוצג בכל שיעור.**
  ⚠️ **ההסבר המבני שהונח הופרך מול הקוד:** `followedPlan`, `maxFavorable` ו-`maxAdverse`
  **כן** נמצאים בטופס הסגירה (`SwingEdge_App.jsx:6483` MFE · `6488` MAE · `6495`
  followedPlan) **וכן** נשמרים (`1992-1995`). מה ששבר את המדידה: לולאת `FIELDS` נמדדה
  מול ה-CTE `clean` **בלי סינון `status='CLOSED'`** — שדות שנקבעים ברגע הסגירה נמדדו
  גם על עסקאות פתוחות, שמעולם לא ראו את הטופס. חמישה שדות סגירה
  (`followedPlan`, `exitReason`, `maxFavorable`, `maxAdverse`, `lessonLearned`) עברו
  ל-`POP_CLOSED`; שבעת שדות הכניסה נשארו ב-`POP_ALL`. אין ברירת מחדל — הוספת שדה
  מחייבת בחירת אוכלוסייה.
  מדידה חוזרת 28.07 מול ה-CTE של הסקריפט עצמו, חציון פר-משתמש:

  | שדה | מול כל העסקאות (מה שהדוח הציג) | מול סגורות בלבד |
  |------|--------------------------------|------------------|
  | שיעור העסקאות הסגורות | — | **33%** |
  | `followedPlan` | 33% | **100%** |
  | `exitReason` | 33% | **100%** |
  | `maxFavorable` | 33% | **60%** |
  | `maxAdverse` | 0% | **60%** |
  | `lessonLearned` | 33% | **60%** |

  **ה-33% הוא בדיוק שיעור העסקאות הסגורות** — ארבעה שדות שונים החזירו את אותו מספר
  כי כולם מדדו את אותו דבר: כמה מהעסקאות סגורות. אין פער משמעת, יש מכנה שגוי.
  ⚠️ **תיקון למספרים שדווחו קודם באותו יום:** MFE/MAE דווחו כ-80%/30% מול סגורות.
  הנכון הוא **60%/60%** (24-25 מתוך 40 אצל המשתמש היחיד בקוהורט). הפער נבע מקוהורט
  שונה — הספירה הישנה שמרה על "≥3 עסקאות" ולא על "≥3 **סגורות**". הכלל של הפריט הזה
  חל גם עליו: מספר בלי מכנה מפורש הוא מספר שלא נבדק.
  ⚠️ **והמסקנה שהמכנה חושף:** אחרי הסקופינג הנכון, הקוהורט לשדות סגירה הוא **משתמש
  אחד** (רק ל-`92a06c0c` יש ≥3 עסקאות סגורות). הדוח מדפיס "קוהורט 1-2 משתמשים",
  ולכן "100% followedPlan" אינו ממצא מוצרי ואינו ניתן להצגה ככזה. **זו בדיוק
  התועלת בהצגת המכנה** — הוא פוסל את המסקנה בשורה עצמה, בלי שאיש יצטרך לזכור.
  ➕ `assertRatiosCarryDenominator()` — שומר קבוע: כל שורה שמכילה `%` חייבת לשאת
  `מונה/מכנה` ושם אוכלוסייה, אחרת **זריקה** (`process.exit(1)`, הדוח לא נשלח).
  חל על גוש המילוי ועל המשפך (שהדפיס עד עכשיו יחסים חשופים). מוחרג במפורש ובהערה:
  שורת "שינוי מאתמול" — זו דלתא, והבסיס `was → cur` כבר מודפס באותה שורה.
  ➕ `CLAUDE.md` §2 — "אפס מנה בלי מכנה". **הסקריפט לבדו לא היה מספיק:** שניים משלושת
  המקרים ("61% בלי stop", "47% אובדן דמו") נולדו בשאילתות אד-הוק ובפרומפט, לא בסקריפט.
  ה-assert מונע נסיגה במקום שבו הפורמט בשליטתנו; הכלל ב-`CLAUDE.md` מכסה את השאר

- **שמירה הדדית — Fleet Daily שומר על ה-Watchdog.** הפער: `watchdog.yml` דילג על עצמו
  בבדיקת העקביות, וסוכן שרץ אינו יכול לדווח שהוא לא רץ — אם ה-cron שלו נכבה, השקט
  נראה זהה לבריאות. צעד `Watchdog liveness` ב-`fleet-daily.yml` שואל את GitHub API מתי
  `watchdog.yml` הצליח לאחרונה (אותו דפוס `runs?status=success&per_page=1`), סף 30ש',
  ומדווח 🔴 בארבעה מצבים: מאחר · מעולם לא הצליח · שגיאת API · timestamp לא תקין.
  fleet-daily ב-06:00 קודם ל-watchdog ב-08:00 ולכן תמיד קורא ריצה שהסתיימה, לעולם לא
  אחת באוויר. `actions: read` נוסף ל-permissions; אפס `${{ }}` בתוך `run` — הכול דרך `env`

- **סוכן התנהגות משתמשים — שלב א' חי** (`scripts/user-analytics.mjs` +
  `user-analytics.yml`, קרון 06:30 UTC). נבנה כולו על נתונים ש-Supabase כבר שומר:
  אפס אינסטרומנטציה, אפס כתיבה, אפס שינוי סכימה, אפס נגיעה ב-`SwingEdge_App.jsx`.
  ארבעה קווים אדומים אכופים בקוד (guards שזורקים ולא מדרדרים): תוכן יומן לעולם לא
  נקרא — רק נספר ונמדד באורך · מזהים מקוצרים ל-8 תווים בלבד · התנהגות ומבנה בלבד ·
  k-anonymity — פלח מתחת ל-3 משתמשים מדווח "1-2". הפירוט במייל בלבד, דיסקורד מקבל
  סיכום מצטבר, stdout מספרים מצטברים בלבד, **אפס artifact ואפס job summary**.
  שתי תקלות CI בדרך, שתיהן מתועדות ב-`INCIDENTS.md` #7 ו-#8: `-q` חסר ב-psql
  (תג `SET` הזיז כל מדד בשורה, והריצה דיווחה ירוק עם `NaN`) — נתפס בדיוק בגלל
  ש-dry run קדם לקרון; ודליפת מזהים מקוצרים ללוג ציבורי, כי **Actions מדפיס את
  קלטי ה-`with:` של action** — הדוח עובר עכשיו כ-`body: file://`.
  `MAXAGE=30` ב-watchdog ו-`"User Analytics"` ב-`failure-alert` נרשמו באותו קומיט
  שבו הקרון קם

- **`docs/TRUTH.md` — גשר האמת המוצרית לקופי.** מקור-אמת יחיד ל-`swingedge-creatives`,
  כל שורה אומתה מול הקוד כולל מעקב import עד שורש האפליקציה. **חמש שורות מהבריף סתרו
  את הקוד:** דוח ה-DNA החודשי הוא **חי** ולא "בקרוב" (`MonthlyReport.js:125-131`, נגיש
  מ-Tools); OCR חי **וחינם** — **אין מנגנון תשלום בריפו כלל**, ולכן "Pro" הוא 🔴; נעילת
  ה-30 דק' **אינה נועלת** (`cooldownRemainingMs` נקרא לתצוגה בלבד, ולצדו כפתור ביטול);
  es/pt/ar מכוסות **~63%** בלבד (382–392 מפתחות מול 607) ונופלות לאנגלית בשקט; **אין
  service worker** ולכן אין offline. נוסף סעיף **🔴 אסור לטעון** עם חמש הטענות השגויות.
  `CLAUDE.md` §10.4 מחייב עדכון הקובץ באותו commit של שינוי יכולת פונה-משתמש.
  ב-`fleet-weekly.yml` נוסף צעד (בתוך `Weekly Vitals` הקיים, אפס צעדים חדשים) שמושך את
  ה-hash דרך `gh api` — **בלי `checkout`, שאין בג'וב** — ומשווה מול `.fleet-state` של
  השבוע הקודם. השוואה חוצת-ריפו נפסלה: `swingedge-creatives` **פרטי** ו-`github.token`
  אינו יכול לקרוא אותו, כלומר היא דרשה PAT חדש (§12). כשל קריאת API מדווח 🟠 ולא שותק.

- **FIN קבוצה 1 — שלמות שרשרת הנתונים** (FIN-039 · FIN-040 · FIN-041). שלושתם
  **מניעתיים במלואם**: מדידה חוזרת מול פרודקשן הראתה `status='CLOSED' AND exit IS NULL`
  = **0** ו-36 שורות דמו שעדיין `is_demo=true` — אפס backfill, אפס `UPDATE`, אפס DDL.
  `tradeFromSupabase` נולד כפונקציה אחות ל-`tradeForSupabase` באותו קובץ (האסימטריה
  הייתה הבאג); `isDemo: undefined` **משמיט** את העמודה במקום לכתוב `false`, כך ששמירה
  לא יכולה לדרוס דגל שהיא לא מכירה. `cleanTrades` הפסיקה להמציא `false` — היא זו
  שמפשיטה `SIM-`/`Hive-`, כלומר ההיוריסטיקה מחקה בעצמה את הקלט שלה ועבדה פעם אחת
  בלבד. `exitDate` (שדה שמעולם לא היה לו עמודה) חובר ל-`closedAt` דרך `deriveCloseState`
  — `status` **נגזר** מנוכחות `exit`, ולכן `CLOSED` בלי `exit` אינו בלתי-רצוי אלא
  בלתי-ניתן-לביטוי. `tradeForSupabase` צועק `console.error` על עמודה לא-מוכרת (המנגנון
  ששתק ובלע את `exitDate`), ו-`test:datachain` (22 אסרשנים) נכנס ל-`verify` לפני `build`.
  חילוץ ללוגיקה טהורה בלבד: `src/lib/cleanTrades.js` · `src/lib/tradeCloseState.js` ·
  `src/data/tradeEnums.js` — אף קומפוננטה לא פורקה, אף מחרוזת `value` לא שונתה
- **GA4 + Consent Mode v2 + באנר הסכמה + /privacy** (`G-VC8PKL4NL1`): בלוק ב-`index.html`
  עם `consent default` **denied** כ-`dataLayer[0]` ללא תנאי + שחזור אישור שמור ב-head
  (כדי שמשתמש חוזר לא יאבד את ה-pageview הראשון). `src/lib/consent.js` — מקור-אמת-אחד
  ל-`swingEdgeConsent` (`{v,analytics,ts}`), `revokeAnalytics` מוחק גם עוגיות `_ga*`.
  באנר עברי לא-מודאלי ב-`z-index:91`, מעוגן ל-inline-start ב-lane יורש-כיוון
  (`pointer-events:none`) — אי-חפיפה עם ה-FAB נכונה **מעצם המבנה**, לא בזכות z-index.
  שני הכפתורים חולקים מחלקה אחת (דרישה רגולטורית). `/privacy`: כפתור ביטול פעיל
  (`ConsentControl`) + הסבר מפורש למה `@vercel/analytics` אינו מגודר.
  **הגנת זיהום דאטה:** route-abort ל-googletagmanager בשלושת ה-specs + קליק
  `consent-decline` ב-sentinel-auth, אחרת ~100 pageviews סינתטיים/יום מול 29 משתמשים.
- **רוטציית Discord webhook** — בוצעה ידנית 27.07, אומתה חי (☀️ ב-13:39)
- **S2 — מחזור מלא ראשון בפרודקשן** (26.07 23:16, ריצה ידנית, `411d1dc`): לוגין →
  hydration → יומן → יצירת SNTNL → טאבים → מחיקה → ניקוי REST. ✅ התאוששות דווחה.
  שפיות ביומן: 3 עסקאות בדיוק (AAPL / NVDA / BTC-USD), אפס SNTNL
- **S2.3 — סינון 404 של לוגו הסימבול**: `financialmodelingprep.com` נוסף ל-`IGNORE_SOURCE`
  (חסימה לפי מקור בלבד, לעולם לא לפי הטקסט "404"). מסלול ה-network נבדק ולא שונה —
  host צד-שלישי אינו נכנס ל-`failedReq` ולא ל-`supabaseFailedReq`. **אומת חי**: ריצת
  Sentinel `30253015899` (2026-07-27T09:13:31Z) מכילה `AUTH_EXPECTED: 1` — שכבת ה-auth
  אכן רצה, ההתאוששות שדווחה אמיתית (ראה `DECISIONS.md` 2026-07-27).
- **Watchdog + ספי Growth Pulse + פידבק ל-Discord**: `watchdog.yml` חדש — 4 קטגוריות
  (טרי/מאחר/מעולם לא הצליח/הבדיקה עצמה נכשלה) + בדיקת עקביות עצמית מול טבלת ה-max-age,
  דיווח יומי תמיד (fallback 🔴 אם צעד הבדיקה קרס). `fleet-daily.yml` Growth Pulse: 3 ספי
  🟠 (0 עסקאות ב-48ש' / 0 נרשמים ב-72ש' / activation מתחת ל-25%), אפס state חדש. פידבק
  משתמש לא-פתור מוצג עכשיו גם בדיסקורד (`daily-digest.yml`, כתום + snippet מסונן).
  `restore-drill.yml`: רבעוני → חודשי. `failure-alert.yml`: Watchdog נוסף לרשימת המעקב.
- **ניקוי מאוחד** (`665c3a7` תוכנית, ביצוע בקומיט הבא): `npm audit fix` תיקן
  `brace-expansion`; `react-router` high נותר במכוון (CVE לא ישים — ראה `DECISIONS.md`
  2026-07-27) · try/catch fail-open ל-`api/_lib/rateLimit.js` · timeout 8s
  ל-`api/verify-turnstile.js` (קריאה חיצונית אחת בלבד, לא שתיים כפי שהונח) · דיווח
  דיסקורד נוסף (לצד מייל קיים) ל-8 סוכנים: analyst, arch-auditor, daily-digest,
  data-guardian, restore-drill (×2), triage, failure-alert, Supabase Backup ·
  `actions/upload-artifact`/`download-artifact` שודרגו ל-`@v7` ב-3 קבצים (backup,
  smoke, sentinel) · smoke.yml הושאר בלי דיווח דיסקורד במכוון (גם מופעל על כל push ל-main)
- **Sentinel #6** — התאוששות שקרית + לכידת ראיות (`54713d3`, `411d1dc`)
- **סיבוב `BACKUP_PASSPHRASE` + Restore Drill** — 222 שורות ב-11 טבלאות אומתו
- **כל 7 תקלות Sentry סומנו Resolved** — הלוח נקי
- שער אישור waitlist + שולח קמפיין
- הגירת Fleet ל-Actions 24/7
- קריסת Journal (60/137 עסקאות בלי `stop`)

## ⚠️ סיכונים פתוחים

- **🆕 T9 · שלושה מוקדי `$` שנשארו קשיחים **במכוון** (02.08).** לא פספוס — הכרעה:
  * `AdminPanel.jsx:929` (`avgPnl`) — **פער אמיתי, לא תוקן.** זהו ממוצע P&L
    **חוצה-משתמשים**, ולכן אין לו מטבע יחיד: משתמש שקלי ומשתמש דולרי נסכמים
    לאותו מספר. התיקון הנכון הוא פילוח פר-מטבע כמו `pnlByCurrency`, לא החלפת
    סימן. מסך אדמין בלבד, לא פונה-משתמש. **⏭️ להכרעת ניב.**
  * `SwingEdge_App.jsx` — ציטוטי שוק חיים (`q.price`, market movers, חיפוש טיקר).
    ה-API משרת סימבולים אמריקאיים בלבד; אלו **אינם כספו של המשתמש** אלא נתון שוק
    דולרי. סימן `$` שם נכון ואינו אמור לזוז.
- **🆕 T9 · `settings.accountCurrency` נשמר ב-jsonb חופשי, בלי אכיפה בשרת (02.08).**
  `user_settings.settings` הוא blob; ערך זר יעבור. ההגנה היחידה היא בקריאה
  (`CURRENCY_SYMBOL[s] ? s : "USD"`), כלומר נופל ל-USD בשקט. מספיק להיום — שני
  ערכים ובורר סגור — אבל מטבע שלישי בעתיד ידרוש בדיקה אמיתית ולא מפתח-קיים.
- **🆕 `+$0` על תווית האפס של צירי הגרפים — נראה ולא שונה (02.08, T8).**
  `fmt$0(0)` מחזיר `"+$0"`, ולכן קו האפס בצירים החדשים נקרא `+$0`. בתא לוח שנה
  זה נכון — יום ששקלל לאפס הוא מדידה. בציר, לעומת זאת, האפס הוא **סימון סקאלה
  ולא ערך**, וסימן פלוס עליו הוא רעש. **לא שיניתי** כי זו הכרעת טעם ולא באג,
  ו-§11 דורש ודאות של 100%; ומכיוון שכל שינוי כאן מזיז את `fmt$0(0)` שהוקפא
  זה עתה ב-`format-test.mjs`. **הכיוון הנכון אם יוחלט:** פרמטר `signed:false`
  ל-`money()`, לא פורמטר שלישי. **⏭️ להכרעת ניב — לא חוסם.**
- **🆕 ארבעת ממצאי קבוצה 7 שיצאו מהיקף T8 — הם מנוע, לא תצוגה (02.08).**
  T8 היה **תצוגה בלבד**, ותיקון מנוע "בדרך" הוא בדיוק §11. כל הארבעה אושרו
  ליציאה ע"י ניב בתנאי שיישאו תזמון מפורש:
  **⏭️ לתזמון בגל הסטופים או במקבץ מנוע נפרד** — חל על כל ארבעת הפריטים הבאים.
  * **FIN-025** — `src/utils.js:43`: `if (!trade.exit)` הוא בדיקת falsy, ולכן
    **יציאה במחיר 0 נחשבת "אין יציאה"**. חדל-פירעון שנמחק מהמסחר הוא המקרה
    האמיתי היחיד, ולכן הסיכון נמוך — אבל הבדיקה שגויה כפי שהיא.
  * **FIN-027** — `statisticalModels.js:192`: `peak > 0 ? … : 0` מחזיר drawdown
    **0** כשה-peak אינו חיובי. חשבון שמעולם לא היה מעל נקודת הפתיחה מקבל
    "אפס ירידה" — אפס שקרי בשכבת המנוע, בדיוק המשפחה של FIN-030.
  * **FIN-033** — `calcTradeMetrics` אינו מוודא `shares > 0` לפני החלוקה.
    `shares: 0` מייצר `Infinity`/`NaN` שזורם החוצה לכל צרכן.
  * **FIN-035** — `statisticalModels.js:257` ו-`MonthlyReport.js:220`: עסקת
    break-even **אינה שוברת רצף**. הרצף המוצג ארוך מהרצף האמיתי.
- **🆕 שער ה-grep הגורף שהאודיט ביקש — לא יושם, במכוון (02.08).**
  האודיט (`AUDIT-2026-07-27…md:272-276`) ביקש assertion ש"אין פורמוט `$` inline
  מחוץ ל-`utils.js`". הוא **נדחה** — ראה `DECISIONS.md 2026-08-02` לנימוק המלא.
  **מה שנשאר פתוח כתוצאה:** `test:format` §8 שומר על **חמישה** אתרים ספציפיים
  שתוקנו, ולא על הכלל. אתר `$` inline **חדש** שייכתב מחר בקומפוננטה אחרת לא
  ייתפס ע"י שום שער. ⏭️ אם זה יחזור — הכיוון הוא lint rule על מסלולי P&L
  חתומים בלבד, לא grep על התו `$`.

- **~~36 עסקאות הדמו אינן קיימות עוד~~ ✅ הוסבר 02.08 — ניב אישר מחיקה מכוונת שלו.**
  סה"כ הטבלה 114 → 40. `select count(*) filter (where is_demo) from public.trades`
  = **0/40**. זה **אינו** אירוע — אין `INCIDENTS.md`, אין חקירה. שתי השלכות נסגרו
  כאן: (א) האזהרה "לא לאמת מול עסקאות הדמו" בראש §עכשיו התיישנה והוסרה;
  (ב) **כל מספר בקובץ הזה שנמדד על בסיס של 114 שורות מיושן.** המדידה דאז
  (02.08, לפני הייבוא של `a0556783290`): **40 עסקאות · 10 משתמשים · 26 סגורות ·
  14 פתוחות · 0 דמו.**
  **מדידה מעודכנת (02.08, אחרי הייבוא): 90 עסקאות · 11 משתמשים · 49 סגורות ·
  41 פתוחות · 0 דמו.** 49+41 = 90/90. הפער כולו הוא 50 העסקאות של יצחק
  (40+50 = 90 · 10+1 = 11), ולכן אינו אירוע. **⚠️ כל שיעור בקובץ הזה שנמדד על
  מכנה 40 מיושן כעת** — כולל שיעור ה-`stop` החסר בשורה הבאה.
- **🆕 שיעור ה-`stop` החסר "עלה" 20.2% → 57.5% בלי שאף משתמש שינה התנהגות (02.08).**
  `stop IS NULL` = **23/40 = 57.5%** מכלל השורות היום, מול 20.2% שנמדד ב-28.07 על
  114 שורות. **המונה כמעט לא זז — המכנה התכווץ.** 22 מתוך 23 החסרים שייכים
  למשתמש בודד (`ad1a0494`, 22/22 = 100%); ה**חציון פר-משתמש הוא 0.0%** (8 מתוך
  10 משתמשים ב-0%). זו הדגמה חיה של `CLAUDE.md` §2: על אוכלוסייה של 10 משתמשים
  "שיעור מצרפי" מודד את גודל האוכלוסייה, לא התנהגות. **אין להשוות את שני המספרים
  זה לזה** ואין להסיק מהם רגרסיה מוצרית.
- **🆕 `date` בהזנה ידנית נכתב ב-UTC (`SwingEdge_App.jsx:2168`) — נמצא 02.08, לא תוקן.**
  `new Date().toISOString().slice(0,10)`. טריידר בישראל שמזין עסקה אחרי 02:00
  מקומי מקבל את **תאריך המחר**. זו אותה משפחת באג של FIN-022/023 אבל במסלול
  **כתיבה**, ולכן היא **מחוץ** ל-T6 (שמתקן קריאה בלבד) — תיקון "בדרך" הוא §11.
  ⚠️ שים לב לסדר: תיקון כתיבה **אינו** מרפא שורות קיימות, ולכן צריך להיות
  החלטה נפרדת עם התייחסות לנתונים שכבר נכתבו.

- **🆕🔴 ייבוא: שורות שאינן עסקאות נקלטות כעסקאות אמיתיות (01.08).** נמדד תחילה
  על פיקסצ'ר סינתטי, ו**נמדד מחדש (01.08, מאוחר יותר) על שני קבצי הברוקר
  האמיתיים של `a0556783290`** — המספרים המדויקים מחליפים את ההערכה הראשונה:
  * **אלטשולר:** מתוך 93 השורות שעברו את `normalizeRow`, **38 (41%) אינן עסקאות**
    — 21 `משיכה` ו-17 `הפקדה`, כלומר תנועות מזומן שנכנסות כפוזיציות `LONG`
    במניה. 55/55 העסקאות האמיתיות כן נקלטו.
  * **IBI (עם שורת הכותרת הנכונה):** **87 מתוך 243 העסקאות האמיתיות (36%) נדחות**
    כ-`no_qty` — כל המכירות, שכמותן שלילית — ובמקומן נכנסות **17 שורות
    "המרת מט״ח"** כעסקאות `LONG`.
  כשל שקט מסוג §2 בצורתו הגרועה — לא "נכשל בשקט" אלא **"הצליח בשקט עם נתון
  שקרי"**: אינו נספר באף מונה שגיאות, והמשתמש רואה מסך ירוק. ✅ **נסגר בשלב ב'
  (01.08)** — whitelist של סוגי פעולה + `abs(shares)` + מונה דילוגים גלוי
  שמפרט כל סוג שדולג. **הפער שנותר: המספרים לעיל נמדדו על הקבצים האמיתיים,
  והאימות נעשה על פיקסצ'רים סינתטיים בעלי אותו מבנה** — הקבצים האמיתיים
  נמצאים באשפת ה-Drive של הבעלים (⚠️ ייעודי למטה). מדידה חוזרת עליהם כשיתקבלו.
- **🆕🔴 ייבוא IBI: 0 עסקאות מתוך 317 שורות — המצב בפועל היום (01.08).** נמדד על
  הקובץ האמיתי דרך הצינור הקיים ללא שינוי: שורת הכותרת של IBI היא **שורה 11**,
  ולכן `matrixToTable` בוחר את שורת הפתיח `["תנועות היסטוריות","",…]` ככותרת.
  `mapping = {"date":7}`, ו-`counts = {total:317, valid:0, rejected:317}` —
  **317/317 = 100% נדחו, כולן `no_ticker`.** זו חוויית המשתמש בפועל, ולא תרחיש
  תיאורטי. ✅ **נסגר בשלב ב' (01.08)** — `findHeaderRow` דינמי; אומת בדפדפן
  על `ibi-full.xlsx` שהכותרות בשורה 11 נבחרות ו-11 העמודות נשמרות.
- **🆕 ייבוא: `width` נגזר משורת הלוגו והורס את הטבלה (01.08).** `parseFile.js:19-21`
  לוקח את השורה הראשונה שאינה ריקה ככותרת, וגוזר ממנה `width` שמקבע את **כל**
  השורות. שורת לוגו בלי פסיקים היא באורך תא אחד → הטבלה כולה נחתכת לעמודה אחת
  (`headers = ["דוח תנועות בחשבון"] width = 1`). לכן זיהוי כותרת *אחרי*
  `matrixToTable` אינו יכול לעבוד — הפרופיל חייב לרוץ על המטריצה הגולמית.
  ✅ **נסגר בשלב ב' (01.08)** — `parseFile` מחזיר גם `matrix` גולמי,
  ו-`applyProfile` רץ עליו.
- **🆕 ייבוא: `catch` שמבליע את השגיאה האמיתית (01.08).** `ImportJournalModal.jsx:84`
  תופס `(e)` ומציג מחרוזת גנרית — `e` עצמו לא נרשם ולא מגיע ל-Sentry. בנוסף,
  במצב IBI הקובץ בכלל **נפתח בהצלחה** והמשתמש נחסם בשלב 2 עם "חסר שדה חובה"
  שמתאר תסמין ולא סיבה — אין שום רמז שהכותרות אינן בשורה הראשונה. ✅ **ניב
  הכריע (01.08): שניהם נסגרים בתוך שלב ב'** כמשימות ת2 (רישום השגיאה האמיתית)
  ו-ת3 (הודעת כשל שאומרת מה נמצא בפועל). ✅ **תוקן במנה 2 (01.08)** — שני
  ה-`catch` (`handleFile` ו-`pickSheet`) עוברים דרך `reportFailure`; אומת
  בדפדפן על קובץ פגום. Sentry **לא** נוסף: אין דפוס `captureException` באף
  קומפוננטה בריפו (רק `main.jsx`, `priceService.js`, `useSupabaseSession.js`),
  והדפוס הקיים בקומפוננטות הוא `console.error("[tag] …", error)`.
- **🆕🔴 ייבוא: אף אחד משני קבצי הברוקר אינו נושא סימול נסחר (01.08).** נמדד על
  הקבצים האמיתיים: ב-IBI **0 מתוך 243** שורות העסקה נושאות סימול בצורת
  `^[A-Z][A-Z0-9.\-]{0,5}$` — `שם נייר` מחזיק שם חברה (`<COMPANY NAME>`, וגם
  עברית) ו-`מספר נייר` מחזיק מזהה ישראלי מספרי. באלטשולר **11 מתוך 55** בלבד,
  בצורת `"SYM US"` בתוך `שם נייר`, בעוד `מס' נייר / סימבול` **ריק** באותן שורות.
  **המשמעות: פרופיל ברוקר לבדו אינו יכול לייצר `ticker` שניתן לתמחר או לגרוף.**
  ✅ **ניב הכריע (01.08):** סימול כשקיים → נכנס; אין סימול → **שם הנייר נכנס
  ל-`ticker` כמות שהוא** ומסומן בדגל `unresolved_symbol` **בזיכרון הייבוא
  בלבד — לא בסכימת DB, אין מיגרציה**. נימוק: *עסקה סגורה אינה זקוקה לסימול
  נסחר — P&L/R/סטטיסטיקה נגזרים ממחירי הקובץ; מחיר חי נדרש רק לפתוחות.*
  מומש במנה 1 (`resolveTicker` ב-`brokerProfiles.js` + `opts.tickerResolver`).
  **החסימה על משימות 3–4 הוסרה.**
- **🆕 ייבוא: טבלת המיפוי מציגה `סימול → מספר נייר` בעוד ה-resolver גובר (01.08).**
  נצפה בדפדפן במנה 2 על `ibi-full.xlsx`: `detectColumns` בוחר את עמודת המזהה
  המספרי (`מספר נייר`) כ-`ticker`, וה-select בשלב 2 מציג זאת. הערך שנכנס בפועל
  מגיע מ-`tickerResolver` (שם הנייר) — כלומר **מה שמוצג אינו מה שיקרה**.
  התוצאה נכונה, התצוגה מטעה. ⏭️ נסגר יחד עם 1b′(ב) — עריכת `ticker` ידנית
  בטבלת התצוגה המקדימה, שם ממילא צריך להציג את הערך האפקטיבי ולא את המיפוי.
- **🆕 קבצי הברוקר של `a0556783290` נמצאים באשפה של Google Drive של הבעלים (01.08).**
  Google הציג "הקובץ נמצא באשפה של הבעלים" בשניהם. אשפת Drive נמחקת אוטומטית
  אחרי 30 יום מרגע ההשלכה. **המבנה המלא חולץ ותועד בתוכנית** (11 עמודות IBI,
  13 אלטשולר, 12+11 סוגי פעולה), ולכן אין תלות בקבצים להמשך — אבל **לא ניתן
  לחזור אליהם לאימות נוסף.** ⏭️ אם יידרש אימות חוזר: לבקש מהמשתמש עותק חדש
- **🆕 פידבק משתמש שלא טופל: "אפשרות נורמלית להעלות לפה קבצים" (§10.2).**
  באותה הודעת פידבק של `a0556783290` שממנה נלקחו שני הקבצים, הופיע גם:
  *"מומלץ שתתנו אפשרות נורמלית להעלות לפה קבצים"* — כלומר המשתמש נאלץ להעלות
  ל-Drive ולשלוח קישור במקום לצרף קובץ. **אינו מטופל בשלב ב'** ואינו אותו דבר
  כמו מודאל הייבוא (זה נוגע לערוץ הפידבק עצמו). נרשם כאן כדי שלא יישאר יתום —
  ⏭️ החלטת ניב: משימה, או דחייה עם נימוק כתוב
- **🆕 ייבוא כושל אינו מותיר עקבה — חוב מדידה (01.08).** אי-אפשר לענות "כמה
  משתמשים ניסו לייבא": האות היחיד (`user-analytics.mjs:158-196`) הוא היוריסטיקת
  "שאריות ייבוא" על **עסקאות שנוצרו**, וייבוא כושל אינו יוצר אף עסקה. `trackEvent`
  (`src/lib/consent.js:110`) אינו נקרא מ**אף מקום** ב-`src/` (0 קוראים). **המכנה
  הזמין אינו מסוגל להכיל ייבוא כושל** — כל שיעור שיחושב עליו ימדוד ייבוא מוצלח
  ויציג 0 כושלים, בדיוק מלכודת §2 סיפא. ⏭️ החלטת ניב: אינסטרומנטציה או ויתור מודע
- **🆕 אזהרת Node.js 20 מיושן ב-Actions (01.08).** התגלתה בריצת Restore Drill #4.
  `restore-drill.yml` אינו מריץ `actions/checkout` כלל — שני ה-`uses:` היחידים שלו
  הם `dawidd6/action-send-mail@v3`, וזהו מקור האזהרה. **הבעיה אינה מקומית לקובץ
  אחד:** `dawidd6/action-send-mail@v3` מופיע ב-**8 workflows** (9 שימושים), ובנוסף
  יש **30 שימושים ב-actions רשמיים `@v4`** (`checkout` / `setup-node`) שכולם רצים
  על node20. **לא דחוף — עדיין רץ, רק מזהיר.** השדרוג (`@v5` לרשמיים, גרסה חדשה
  ל-send-mail) הוא משימה נפרדת שנוגעת בכל ה-workflows בבת אחת ולכן לא נעשה "בדרך"
  (§11). ⏭️ מועמד למשימה ייעודית
- **🆕 `avgMaeMfe` הוא export מת (01.08).** `grep -rn 'avgMaeMfe'` → **1/1 מופעים =
  ההגדרה עצמה**. תוקן במקבץ 01.08 (המכנה השגוי פורק), אבל **הפונקציה עדיין אינה
  נקראת מאף מקום.** או שיחווטו אותה, או שתימחק — export שאיש אינו קורא הוא קוד
  שמתיישן בלי שאיש ישים לב, ובדיוק כך נולד המכנה השגוי מלכתחילה. ⏭️ החלטת ניב
- **🆕 `Number(...) || 0` בארבעה שדות טופס ב-`DecisionCoach.js` (01.08).** `:18-20`
  (`entry`/`stop`/`target`) ו-`:542-544` (`shares`/`capital`). **נבדק ולא תוקן במכוון:**
  אלה קלטי טופס גולמיים בזמן הקלדה, שבהם `"" → 0` הוא ההתנהגות הנכונה, ולא נתונים
  שנשמרו. משפחה דומה מבחינה תחבירית, בעיה שונה מבחינה סמנטית. נרשם כדי שהחיפוש
  הבא לא יגלה אותם מחדש כ"ממצא"
- **🆕 `api/ocr.js` — אפס תצפיתיות (F5).** הצהרת `console` אחת בכל הקובץ
  (`:310`, `[rate_limited]`). אין רישום של `hasPositionTool`, `confidence`,
  `computed.source` או התוצאה. **גם כשה-OCR נקרא, לא נשאר עקבות למה החזיר —
  ולכן למדידת ה-30 לא יהיה מקור אמת בצד השרת להצליב מולו.** חוסם את פריט 13
- **🆕 `entry` מ-Vision במסלול שאינו ניתן ל-cross-check (F4).** `api/ocr.js:436`
  נופל ל-`visionEntry` כשהאחוזים לא נקראו. השער ב-`:458-460` דורש
  `stopChecked`/`targetChecked`, שדורשים `stopPercent !== null` — אבל
  `source === "none"` נובע *מכך* שהאחוזים חסרים. **התנאים סותרים: המקרה היחיד
  שמשתמש ב-entry של Vision הוא בדיוק המקרה שבו השער לא יכול לירות.** מסלול שני,
  עצמאי, שמייצר "מחיר נוכחי כ-entry". ⏭️ אחרי מדידת ה-30
- **~~🆕 מסלול עריכת עסקה קיימת אינו עובר ולידציית גיאומטריה (ציד F4, 01.08)~~
  ✅ נסגר ב-T7 (02.08).** `EditTradeModal.validate()` קורא את **אותו**
  `validateTradeInputs`, לא מימוש שישי. אומת בדפדפן: סטופ 105 מעל כניסה 100
  ב-LONG → המודאל נשאר פתוח, הערך המאוחסן לא זז, וההודעה שהוצגה היא המחרוזת
  המדויקת של `validateTradeInputs` (`"ב-LONG הסטופ חייב להיות מתחת לכניסה."`) —
  כלומר הוולידטור המשותף הוא זה שירה. ⚠️ חל על **שמירות חדשות בלבד**; רשומות
  קיימות עם גיאומטריה הפוכה (0/17 בפרודקשן היום) אינן נחסמות ואינן משתנות
- **🆕 הוולידציה בייבוא היא טאוטולוגיה כשה-`side` נגזר (ציד F4, 01.08).**
  `normalizeRow.js:95` — `if (!side) side = inferSide(entry, stop, target)`,
  ומיד אחריו `:99` מריץ `validateTradeInputs(entry, stop, target, side)`.
  כשהעמודה `side` חסרה בקובץ, `inferSide` גוזר את הכיוון **מאותה גיאומטריה
  בדיוק** שהוולידציה אז בודקת מולו — בדיקה שלעולם אינה יכולה להיכשל.
  זו בדיוק הסכנה שההערה ב-`utils.js:63` מזהירה ממנה ("Uses the explicit `side`
  rather than inferSide, which masks reversed input"), מיושמת הפוך. **שורה
  הפוכה בקובץ ייבוא בלי עמודת side תיקלט כתקינה.** ⏭️ החלטת ניב
- **🆕 ארבע מימושים עצמאיים לאותה בדיקת גיאומטריה (ציד F4, 01.08).**
  `inferSide` (`src/utils.js:55-59`) · `validateTradeInputs` (`src/utils.js:65+`) ·
  `directionCheck` (`src/intelligence/core/DecisionCoach.js:120-131`) ·
  `directionOk` (`src/localAI.js:35-36`). כולם עונים על "באיזה צד של הכניסה
  יושב הסטופ", כל אחד בקוד משלו — למרות ש-`utils.js:62` מכריז על עצמו
  "Single source of truth for input validity". F4 הוסיף חמישי, `dirFromPrice`
  (`api/ocr.js:168`), **במכוון**: הוא שונה במהות (מאמת גודל מול דלתא מהימנה)
  ורץ ב-serverless שאינו מייבא מ-`src/`. איחוד ארבעת הראשונים הוא משימה
  נפרדת — לא נעשה "בדרך" (§11). ⏭️ **עדיין פתוח אחרי T7.** T7 **לא הוסיף** מימוש
  שישי — מסלול העריכה חובר ל-`validateTradeInputs` הקיים — אבל גם לא איחד את
  ארבעת הקיימים. ⏭️ מועמד למשימה ייעודית (מקור אמת אחד)
- **🆕 אין סימון מקור עסקה בסכימה (F6) — OCR אינו מדיד בדיעבד.** 25 העמודות
  ב-`public.trades` אינן מתעדות OCR מול ידני, ותמונת הגרף אינה נשמרת.
  **שיעור שדות ריקים בעסקאות-OCR מול ידניות אינו ניתן לחישוב — הנתון לא קיים.**
  מדידת ה-30 חייבת להיות פרוספקטיבית; אין בסיס היסטורי.
  **הרחבה (01.08, ציד T5):** אותו חוסר חל על יבוא — `is_demo` הוא הדגל היחיד,
  ו-`normalizeRow` קובע `isDemo:false`. אין `source`, אין `import_batch_id`.
  **השלכה חדשה:** אם מנוע ה-FIFO (T5) ישדך שגוי, אין דרך לבודד את העסקאות
  שהוא יצר ולהתאושש — לא בשאילתה ולא בממשק. עמודה חדשה = מיגרציה (§12), לא
  בהיקף T5. ⏭️ החלטת ניב
- **✅ `dupKey` ביבוא הורחב (01.08) — הוסר מהחסימות.** `ticker|date|entry` היה
  אגרסיבי-יתר ומחק מפצל fill בשקט. המפתח כולל עכשיו גם `exit`, `shares` ותאריך
  היציאה. **תאריך היציאה נלקח כתאריך בלבד ולא כחותמת `closedAt` מלאה** — חידוד
  מכוון של הנוסח שאושר: יבוא נוחת ב-20:00 ועסקה שנסגרה ידנית נוחתת בשעה
  שרירותית, והשוואת חותמות הייתה מונעת זיהוי יבוא-חוזר של עסקה שנסגרה ביד.
  תרחיש 5 עובר **ללא תזוזה באסרשנים** (`valid=1`, `duplicates=2`); הזרע שלו
  הושלם מ-stub בן 3 שדות לעסקה אמיתית, כי אובייקט בלי `shares` ובלי `status`
  לא יכול היה להתקיים ב-`public.trades` מלכתחילה. תרחיש 27 חדש מוכיח את
  שלושת המקרים. ראה `docs/DECISIONS.md` 2026-08-01.
- **✅ סדר שורות יורד בקובץ ברוקר (01.08) — הוסר מהחסימות.** `fileOrderNote`
  מסווג `ascending`/`reversed`/`mixed`/`flat`; היפוך רק בירידה **חד-משמעית**
  (ירידה אחת לפחות, אפס עליות), קובץ מעורבב נשאר כפי שהוא והמיון היציב עושה
  את שלו. ההיפוך נרשם ב-`log.reversedFileOrder` ומוצג למשתמש
  (`imp_fifo_reversed`, he+en). ראה `docs/DECISIONS.md` 2026-08-01.
- **🆕 ⚠️ קנייה אחרי SHORT יתום אינה מכסה אותו — שתיהן נשארות פתוחות (01.08).**
  `src/import/fifoMatch.js`. הכללים שננעלו מראש מגדירים מכירה-בלי-קנייה כ-SHORT
  פתוח, ולא הגדירו מה סוגר אותו. ספר הפוזיציות מחזיק לוטים של קניות בלבד, ולכן
  טריידר שמוכר בחסר ואז מכסה יקבל שתי עסקאות פתוחות במקום אחת סגורה.
  **לא מומש במכוון** — מימוש בלי בקשה הוא חריגה מ"כללים נעולים מראש".
  ⏭️ החלטת ניב: להרחיב את הספר לדו-כיווני, או להשאיר כמגבלה מוצהרת
- **🆕 ⚠️ קובץ באותו יום שהמכירה בו רשומה ראשונה אינו ניתן לזיהוי כהפוך (01.08).**
  אין עמודת שעה באף אחד משני הברוקרים (`brokerProfiles.js:25-53`), ולכן כשכל
  התאריכים שווים `fileOrderNote` מחזיר `flat` — אין ירידה להיאחז בה. תרחיש 25
  (חצי שני) מקבע את ההתנהגות: המכירה נכנסת כ-SHORT יתום. **זו מגבלת נתונים,
  לא באג** — הקובץ אינו מכיל את המידע. ⏭️ ייפתר רק אם ברוקר יוסיף שעה
- **🆕 `/api/ocr` — 0 קריאות בחלון הלוגים הנשמר (01.08).** מכנה: 24h ו-7d
  מחזירים כמעט אותם מספרים (152 מול 151 `/api/quote`) → **השימור הוא ~24 שעות,
  לא 7 ימים.** הטענה המדויקת: 0 קריאות ב-~24 השעות שנשמרו. ניסוי ביקורת אישר
  שהלוגים כן קולטים את הנתיב (2 בדיקות שלי הופיעו מיד). **לא ניתן לאשר שהבקשה
  של עומרי הגיעה לשרת כלל** — ייתכן שאף משתמש אינו מגיע לנקודה בפועל.
  דורש אימות חי לפני מסקנה מוצרית
- **🆕 `routeChartOcr` יעד `position` דורס שדה שמולא ביד (01.08).**
  `SwingEdge_App.jsx:2565-2566` כותב `result.entry != null ? … : f.entry` **בלי**
  שומר `f.entry ||`, בניגוד לשלושת מסלולי ה-OCR האחרים (`:2419`, `:2461`, `:2573`)
  שכולם מכבדים ערך ידני. חוסר-עקביות ודאי; התיקון הוא הוספת `f.entry ||`.
  **ממתין להכרעת ניב** ב-`PLAN-2026-08-01-ocr-f123` §9 שאלה 3
- **🆕 מילוי המחיר החי ב-`posCalc` מוגן מ-OCR במקרה בלבד (01.08).**
  `SwingEdge_App.jsx:4377-4385` יורה מתוך `onChange` של קלט הטיקר, ו-OCR מגיע
  ל-`posCalc` דרך `setPosCalc` ב-`routeChartOcr:2560` — נתיב אחר, ולכן ה-`onChange`
  אינו נורה. **אין כאן שומר, רק צירוף מקרים.** חיווט עתידי של השדה ל-`useEffect`
  (כמו שני הטפסים האחרים) יפתח מחדש את באג F1 בדיוק.
  🟡 **מותן ב-01.08 להערה בקוד** (`routeChartOcr`, ענף `position`) שאומרת בדיוק
  את זה. הערה אינה שער — הסיכון נשאר פתוח
- **🆕 `ocr_result` ב-GA4 נספר רק על מסכימים (01.08).** `trackEvent` יוצא מוקדם
  בלי `analytics: granted`, ולכן **המכנה של כל שיעור שייגזר מהאירוע הזה הוא
  אוכלוסיית המסכימים, לא כלל המשתמשים** (§2 — אין מנה בלי מכנה). כל דיווח חייב
  לומר "N קריאות OCR **בקרב מסכימים**". אין בריפו מדד לשיעור ההסכמה עצמו, ולכן
  **גודל ההטיה אינו ידוע** — לא ניתן להסיק ממנו על שיעור הכשלים האמיתי
- **🆕 גבול מול סשן מקבץ-התיקונים (01.08).** פריט 6 שלו צומצם למסך האישור בלבד;
  `SwingEdge_App.jsx` (טפסי OCR) ו-`api/ocr.js` היו בבעלות בלעדית של המשימה הזו
  עד ה-push. **אחרי ה-push הבעלות משוחררת** — הסשן ההוא חייב `git pull` לפני
  שהוא נוגע באותם קבצים
- ~~**🆕 `CLAUDE.md` — אין `§10.2` (01.08)**~~ ✅ **נסגר 01.08.** הכלל נכתב
  (`CLAUDE.md §10.2 — אין פידבק יתום`): שלושה מצבי סיום בלבד — משימה ב-`STATE.md`
  עם ציטוט · טופל עם hash · נדחה עם נימוק כתוב. `Reviewed` מוגדר במפורש כמצב
  ביניים ולא כסיום
- **~~🆕 חמישה מסלולי "סגור"/win-rate מחוץ ל-`SwingEdge_App.jsx` ו-`src/components/`
  שלא אוחדו ב-T3~~ ✅ הוכרע ב-T7 (02.08) — 2 אוחדו · 2 נשארו בהצהרה · 1 היה רישום שגוי.**
  * `MonthlyReport.js:158,372` → `getClosed` + `outcomeRates`. **✅ אוחד**
  * `GrowthPredictor.jsx:204` → `getClosed`. **✅ אוחד**
  * `TradeDNA.js:185` → **לא אוחד במכוון.** הוא מסנן `status==="CLOSED" && exit`
    לצורך ניתוח דפוסים פסיכולוגיים, לא לצורך שיעור. `DECISIONS.md` 2026-08-02
  * `EditTradeModal.jsx:37` → **לא אוחד במכוון.** `showExitFields` הוא **שאלת UI**
    ("האם להציג את שדות היציאה"), לא שאלת אוכלוסייה סטטיסטית. עסקה `CLOSED` בלי
    `exit` היא בדיוק המקרה שבו **חייבים** להציג את השדה כדי שהמשתמש ימלא אותו;
    `getClosed` היה מסתיר אותו. `DECISIONS.md` 2026-08-02
  * `normalizeRow.js:107` → **הרישום היה שגוי מלכתחילה, נמחק.** אין שם סינון
    "סגור" כלל — זהו נתיב הוולידציה ביבוא
  שער `test:engine` בלוק 5 סורק את `src/` אחר `status === "CLOSED"` מול allowlist
  בן 5 קבצים, כך שמסלול פרטי חדש נופל ב-`verify` ולא מתגלה באודיט הבא
- **קובץ תוכנית נדחף ריק 29.07** — `head -n -8` אינו נתמך ב-BSD/macOS, ההפניה יצרה
  קובץ 0 בייט, והקומיט+push רצו בשורה נפרדת ולכן לא נעצרו. **לאמת גודל/תוכן אחרי
  כתיבה, לא רק שהקומיט עבר.** תוקן בקומיט נפרד (`e38877d`), לא ב-`--amend`.
  **הכללה: `head` של BSD אינו תומך ב-`-n` שלילי כלל** — כל סקריפט שנכתב מול הרגלי
  GNU עלול להיכשל כך בשקט על מק, ולייצר קובץ ריק במקום שגיאה חוסמת
- **~~14 עסקאות `CLOSED` עם `closedAt IS NULL`~~ ❌ הרישום היה שגוי — הופרך 02.08.**
  הרישום נשאר ומתוקן כאן ולא נמחק (§2 — הוכחה, לא זיכרון). המדידה בפועל:
  `select status, count(*) from trades where "closedAt" is null group by status`
  מחזיר **`OPEN` = 14, `CLOSED` = 0**. כל 14 השורות הן `OPEN` עם `exit IS NULL`,
  ולכן `closedAt: null` הוא הערך ה**נכון** שלהן לפי חוזה `deriveCloseState`
  (`src/lib/tradeCloseState.js`) — אין כאן נתון חסר ואין מה למלא. **התוכנית
  שנבנתה סביב הרישום הזה (backfill/מיגרציה) בוטלה כולה ב-T6.** הלקח: רישום
  סיכון שמצטט מספר חייב לצטט גם את השאילתה שהפיקה אותו, אחרת מדידה חלקית
  ("14 שורות עם `closedAt IS NULL`") נשמרת כאבחנה ("14 עסקאות סגורות פגומות")
- **`entryQuality` הוא `text` ב-DB בעוד הקוד כותב לשם מספר** (`entryQuality: 3`).
  אומת מול `information_schema`, לא הוסק. סחיפת מחרוזת-מול-מספר → **קבוצה 2/7**
- **`status` מוגדר `default 'open'::text` בלוויאנית**, בעוד כל השוואה בקוד היא
  `"OPEN"`. אפס שורות כאלה היום (כולן נכתבות עם `status` מפורש), אבל כל `insert`
  עתידי בלי `status` ייצור שורה שאף מסנן בקוד לא יתפוס. **מלכודת רדומה**
- **`createdAt` של עסקאות מיובאות מקדים את יצירת החשבון.** 25 עסקאות עם
  `"createdAt" < auth.users.created_at` — המייבא כותב את התאריך מה-CSV ולא את זמן
  הכתיבה בפועל. שלוש תוצאות: (א) כל מדד "מה קרה ב-24 השעות האחרונות" מפספס אותן;
  (ב) כל funnel לפי `createdAt` גולמי מראה פעילות *לפני* ההרשמה; (ג) חלון-זמן מסודר
  אינו מזהה ייבוא כלל — 173 העסקאות של המקרה הידוע פרושות על פני 9 חודשים.
  **לא מתוקן במכוון** — כתיבה מחדש של `createdAt` היסטורי היא שינוי נתונים, לא דוח.
  `scripts/user-analytics.mjs` מנטרל זאת ב-`GREATEST("createdAt", acct_created)`
  ומזהה ייבוא בשלושה סימנים ב-OR. ראה `docs/plans/PLAN-2026-07-27-user-analytics.md`
- **אל תסמוך על `rows` מ-Supabase MCP `list_tables` כמדד ספירה.** הערך הוא
  `pg_class.reltuples` — הערכת planner, לא ספירה, ומתעדכן רק ב-autovacuum.
  **כל מספר שנכנס לדוח חייב לבוא מ-`count(*)`.** הכלל עומד בפני עצמו ואינו הסבר
  לשום הפרש ספציפי שנצפה ב-27.07 — ראה השורה הבאה
- **27.07 22:30 — משתמש `a0556783290` מחק את כל 173 עסקאותיו** (תוצר ייבוא IBI
  כושל). `trades`: 285→114, משתמשים-עם-עסקאות: 10→9. הפרשי ספירה שנצפו במהלך הערב
  נבעו **משינוי אמיתי בנתונים, לא מכלי המדידה** — שני נתיבי הקריאה (psql ו-MCP)
  היו נכונים בכל רגע, ואומתו כזהים בריצת dispatch (114 בשלוש ספירות רצופות).
  **לקח: הפרש בין שתי מדידות — לבדוק קודם מה השתנה בנתונים, ורק אחר כך לחשוד בכלי**
- **מחיקה שקופה לחלוטין בדיעבד.** אין `deleted_at`, אין `updatedAt` — שום שאילתת
  חלון-זמן לא יכולה לראות ש-173 שורות נעלמו. איש לא ידע במשך ~3 שעות. זו הסיבה
  ש-`user-analytics.yml` שומר ספירות מצטברות ב-Actions cache, ומאז 27.07 גם מתריע
  🟠 על שינוי נפח חד (>20% מול אתמול) — מחיקה של 60% מהטבלה חייבת לצעוק
- **✅ מדידה 28.07 אחרי מחיקת 173 השורות: 20.2% מכלל העסקאות בלי `stop`**
  (29.5% בנטרול 36 הדמו), אך **החציון בין משתמשים הוא 0.0%** — 22 מתוך 23 החריגות
  אצל משתמש בודד. FIN-007 תקף כבאג מתמטי אך אינו התנהגות רוחבית. ⚠️ הסדר נגזר מכך:
  קבוצה 3 (חוזה ה-null) חייבת לקדום לתיקון הייבוא — כל עסקה מיובאת נכנסת בלי `stop`,
  ולכן הייבוא יחזיר את ההטיה לסדר גודל מלא על נתונים שהמשתמש סומך עליהם.
  **הלקח הכללי: "שיעור מצרפי" על אוכלוסייה של 9 משתמשים אינו מדד התנהגות — הוא נשלט
  ע"י המשתמש עם הכי הרבה שורות.** כל סף מוצרי עתידי נמדד בחציון פר-משתמש, לא בשיעור
  מצרפי. המספרים בראש §6 באודיט
- **~~🆕 אותה משפחה, טרם טופל — שני אתרי `?? 0` על R שנשארו מחוץ לקבוצה 3~~ ✅ נסגר 01.08.**
  שני האתרים תוקנו במקבץ התיקונים הקטנים. **הרישום המקורי כלל שתי טענות שגויות,
  שאומתו מול הקוד ומתוקנות כאן ולא נמחקות** (§2 — הוכחה, לא זיכרון):
  · **`src/intelligence/utils/psychologyPatterns.js:117` — לא היה `?? 0` ולא היה על R.**
    הקוד בפועל היה `Math.abs(Number(t.maxAdverse) || 0)` — כלומר `|| 0` על **MAE/MFE**,
    לא `?? 0` על R. אותה משפחת באג (ברירת מחדל שמחליפה נתון אמיתי), ערך אחר לגמרי
  · **"לא נצפה במסך ישירות, אבל מזין לקחים שכן נצפים" — שגוי.** `avgMaeMfe` אינה
    מזינה דבר: `grep -rn 'avgMaeMfe'` מחזיר **1/1 מופעים = ההגדרה עצמה**, אפס import
    ואפס קריאה. התיקון אינו תיקון להתנהגות שמשתמש רואה היום — הוא **פירוק מוקש לפני
    שמישהו יחווט אותו** ויירש את המכנה השגוי בשקט
  · **`SwingEdge_App.jsx:1642-1645`** — התיאור כאן היה מדויק, ותוקן: המסנן היה כופה
    `null` להשוואה מספרית, ולכן "R מעל 1" הסתיר עסקה חסרת-סטופ בעוד "R מעל 1-" השאיר
    אותה, בלי שום סימון על המסך
- **🆕 דפוס — מנה עם מכנה שגוי.** 27-28.07: שלוש פעמים נמדדה מנה עם מכנה שגוי
  (61% בלי stop · 47% אובדן דמו · 33% שדות סגירה). בכל המקרים המונה היה נכון והמכנה
  כלל אוכלוסייה לא רלוונטית. כל מנה בדוח או בפרומפט חייבת לציין מכנה.
  **נאכף מאז 28.07 בשתי רמות:** `CLAUDE.md` §2 "אפס מנה בלי מכנה" (חל על דוחות,
  שאילתות אד-הוק ופרומפטים — שם נולדו שניים מהשלושה), ו-`assertRatiosCarryDenominator()`
  ב-`scripts/user-analytics.mjs` (זורק, לא מדרדר — הריצה יוצאת 1 והדוח לא נשלח).
  הכתיבה המלאה, כולל התיקון העצמי 80/30→60/60 כהדגמה של הכלל על עצמו: `INCIDENTS.md` #9
- **~~36 עסקאות הדמו אינן בסיס בדיקה תקף לקבוצות 2–3~~ — התיישן 02.08, אין דמו בטבלה.**
  הרישום המקורי: מילוי 100% בכל שדה אופציונלי (`lessonLearned` 36/36 · `stop` 36/36 ·
  `notes` 36/36) מול ~60% `lessonLearned` בעסקאות האמיתיות — חתימה של סדרה מוזרעת.
  **הכלל עצמו לא התיישן, רק האוכלוסייה:** כל אוכלוסיית-בדיקה מוזרעת עתידית תיראה
  ירוקה על התפלגות שאינה קיימת בפועל. `lessonLearned` היום: **1/40 שורות מלאות.**
- **ה-pageview הראשון של משתמש שמאשר הוא חסר-עוגייה** — האישור מגיע אחרי ה-`g/collect`
  הראשון ו-GA4 לא שולח מחדש. landing → הרשמה ממודלת ולא מיוחסת בעמוד הראשון.
  **מקבלים במודע** — `page_view` ידני היה יוצר ספירה כפולה
- **`OnboardingTour` (`fixed inset-0 z-[120]`) מכסה את הבאנר** בדיוק למשתמש החדש.
  הבאנר נשאר שם אחרי הסיור. **אין להעלות את z-index של הבאנר מעל 120**
- **CSP עתידי ישבור את שני ה-inline scripts ב-head** (theme resolver + GA). יידרשו
  nonce/hash + `script-src` ל-googletagmanager + `connect-src` ל-`*.google-analytics.com`
- שכבת auth רצה פעם בשעה בעוד הדיווח כל 20 דק' → אזור עיוור מובנה
- `core.hooksPath` הוא config מקומי — clone חדש נשאר בלי הגנת hooks, בשקט
- `smoke.yml` לא מקבל צעד דיסקורד (במכוון — גם מופעל על כל push ל-main, לא רק
  cron). ריצה ירוקה שקטה לגמרי; רק כשל מדווח (דרך `triage.yml`'s workflow_run watch)
- `actions/download-artifact@v7` מול `upload-artifact@v7` ב-`sentinel.yml` (שורות 93,
  117) טרם אומת בריצה אמיתית ב-Actions — v4 עלה ל-v7 בשני הצדדים יחד, אך אין הרצה
  שאישרה שההורדה עדיין תופסת את ה-artifact
- `api/send-invites.js:133` (`reportDiscord`) קורא `SENTINEL_DISCORD_WEBHOOK` שאינו קיים
  ב-Vercel env — `if (!webhook) return;` יוצא בשקט. הדיווח 📧 מעולם לא ירה משם; מה שנצפה
  ב-25.07 הגיע מ-`email-campaign.yml`
- **🆕 אין מנגנון ביטול הרשמה למיילים יוצאים.** `grep` על
  `unsubscribe|List-Unsubscribe|הסרה` מחזיר **0 מופעים** בנתיב השליחה
  (`email-campaign.yml`, `api/send-invites.js`, `emails/*`). הקיים הוא **טקסט
  בלבד** בפוטר התבנית ("השב הסר") — כלומר ההסרה תלויה בכך שניב יקרא מייל חוזר
  ויימחק ידנית: בדיוק "תלות בזיכרון אנושי" של §2, ובדיוק סוג הפריט ש-§3 מודד
  כשלילי (צעד ידני קבוע). לא חוסם היום ב-37 נמענים; הופך לחוב אמיתי בקנה מידה,
  ו-`List-Unsubscribe` הוא גם אות דירוג אצל ספקי דואר. ⏭️ החלטת ניב: משימה
  ייעודית (טבלת `email_opt_out` + כותרת `List-Unsubscribe` + סינון בשאילתת הקהל),
  או דחייה עם נימוק כתוב
- **🆕 מדיניות הפרטיות מתארת בסיס איסוף צר מהשימוש בפועל.** `LegalPages.jsx:156`
  מנסח את איסוף המייל כ"כתובת מייל שנמסרה בהרשמה **לרשימת המתנה** — לצורך עדכון
  על **פתיחת הגישה למוצר בלבד**". קמפיין `import_and_ocr_live` פונה ל**בעלי
  חשבון** על יכולות במוצר — בסיס שונה (יחסי לקוח) ומוצדק יותר, אבל **המדיניות
  אינה מתארת אותו**. הפער אינו בשליחה אלא בטקסט. ⏭️ החלטת ניב: הרחבת הסעיף
  למיילי שירות/מוצר לבעלי חשבון, או דחייה עם נימוק כתוב
- **🆕 `email-campaign.yml` — קהל `registered_users` טרם רץ מול הסוד האמיתי.**
  השאילתה קוראת מ-`auth.users`, בעוד כל הריצות עד היום קראו מ-`public`. אם
  התפקיד שב-`SUPABASE_DB_URL` אינו רשאי לקרוא את סכימת `auth`, ה-fetch ייכשל.
  ⚠️ **הכשל מדורג נכון** (`::error` + `exit 1`, לא נפילה שקטה לאפס נמענים), אבל
  ה-dry-run הוא ההוכחה היחידה — אין דרך לאמת את ההרשאה מקומית
- **🆕🔴 המטבע אינו קיים בשום שכבה של המוצר (02.08, T9).** לא בסכימה
  (`public.trades` — 25 עמודות, אף אחת אינה מטבע), לא בהגדרות
  (`user_settings.settings` של יצחק: `capital=40000`, `currency` **לא קיים**),
  ולא בתצוגה (`utils.js:118–130` מדביק `$` ללא תנאי; `$` קשיח ב-JSX ב-
  `SwingEdge_App.jsx:561, 3393, 3708, 4735, 6027` ו-`OnboardingScreen.jsx:81`).
  🔴 **ההון של יצחק שבור גם בלי הייבוא:** הונו בשקלים משמש מכנה ל-P&L דולרי
  וגם ל-`posSize` (`SwingEdge_App.jsx:2040`) שמחלק הון בשקלים במרווח בדולרים.
  ✅ **הוכרע 02.08 — חלופה C: זהו שלב א' של תמיכת ₪, לא טלאי ייבוא.** ההון
  נכנס לחלופה באופן טבעי: `settings.accountCurrency` + `fmtMoney(n, currency)`,
  ו-`posSize` **מסרב לחשב** על אי-התאמת מטבע במקום להחזיר מספר שקרי.
  מתוזמן לקומיט 3 בתוכנית: `docs/plans/PLAN-2026-08-02-import-currency.md`
- **🆕 ה-OCR באותה מלכודת מטבע, ולא נמדד (02.08, T9). ⏭️ מחוץ להיקף T9.**
  הסכימה שחוזרת מהמודל
  (`api/ocr.js:257`) היא מספרים חשופים — `"entry": <number|null>` — ללא שדה
  מטבע ובלי אזכור יחידה בפרומפט. גרף ישראלי ב-TradingView מוצג **באגורות**,
  ולכן מסלול ה-OCR מייצר בדיוק את אותו `3470` שהייבוא מייצר, גם אחרי שהייבוא
  יתוקן. ⏭️ **נסגר בגל המטבע, לא ב-T9. לא נוגעים ב-`api/ocr.js`** —
  החלטת ניב 02.08, כדי ש-T9 לא ייפרס לתשתית מטבע מלאה. אחרת נסגר חור אחד
  ונשאר שני
- **🆕 מטא-דאטה של מטבע קיימת בקוד ואינה נצרכת (02.08, T9).**
  `priceService.js:385` קורא `currency_code` מתוצאת TradingView ומשליך אותו;
  `api/quote.js:125,293` מקודדים `currency: "USD"` ואף צרכן אינו קורא. ⏭️ אם
  תיבנה תמיכת מטבע — אלה נקודות החיבור הקיימות, לא צריך להמציא אותן
- **🆕🔴 15 פסאודו-ניירות ביומן של יצחק — באג חי. ההשערה שזו "שארית" הופרכה
  (02.08, T9).** ⛔ **תיקון להערכה שלי מאותו יום** — 9 תנועות מס
  (`מס ששולם` ×3 · `זיכוי מס` ×3 · `מס תקבולים 26` ×3, כולן `entry=100` בדיוק)
  ו-6 המרות מט"ח (`B/S USD/ILS <שער>`, `entry` = השער ×100).
  **ההוכחה שהן נכנסו במנוע של היום ולא לפניו:** ל-23 מעסקאותיו יש `exit`
  ו-`closedAt`, ולפני 01.08 **כל** מכירות IBI נדחו כ-`no_qty` (כמות שלילית,
  `STATE.md:995–997`) ותלונתו שלו הייתה *"הכול נכנס כפתיחת עיסקה"* — כלומר
  עסקה סגורה **לא יכולה** להיות מלפני 01.08. בנוסף חתימות FIFO מובהקות:
  `NFLX shares=0.0799999999999983` (שארית float מפיצול לוט) ו-`מזרחי טפחות`
  שנסגרת ב-3 רגליים 40/60/10 באותו תאריך ובאותו `exit`. FIFO עלה ב-`f375478`
  (01.08). **מסקנה: 9 שורות המס עברו את ה-whitelist החי.**
  **למה:** `isTradeAction` הוא מבחן **תחילית** — `startsWith("קנייה"|"קניה"|"מכירה")`.
  IBI רושם חיוב מס כ**קנייה** של נייר-דמה בשם `מס ששולם` בשער נקוב 100. פועל
  הפעולה **תקין**; מה שאינו נייר ערך הוא ה**נייר**. ה-whitelist אינו יכול
  לראות זאת כי המבחין אינו בעמודת הפעולה אלא בעמודת הנייר — **בדיוק הנימוק
  של ניב לשער שני.** ⚠️ **מה שעדיין לא ידוע:** המחרוזת המילולית המדויקת של
  הפעולה. הקבצים אינם נשמרים (⚠️ אשפת ה-Drive, למטה), ולכן ידוע רק האילוץ
  שהיא מקיימת — התחילית — וזו ודאות מהקוד, לא הסקה. ✅ **השער השני נבנה ב-T9**
- **🆕 אין מסלול לשליחת מייל שירות לנמען יחיד (02.08).** `email-campaign.yml`
  מקבל `audience` כ-choice של שתי קבוצות רחבות בלבד — `limit=1` בוחר נמען
  שרירותי, כלומר סיכון לשלוח פרטי חשבון של משתמש אחד לאדם אחר;
  `api/send-invites.js` פועל על **מזהי waitlist** עם תבנית קבועה ודורש JWT
  אדמין. **תוצאה מעשית: כל מענה אישי לפידבק (§10.2) נשלח ידנית ע"י ניב.**
  ⏭️ החלטה — לבנות מסלול נמען-יחיד, או לקבע שמענה אישי הוא ידני
