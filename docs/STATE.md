# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.**
> חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט**
> (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-06 · HEAD: abf4d9a — ⚠️ מפגר בקומיט אחד **מעצם הגדרתו** (אין hash לפני commit, ו-amend אסור). `git log -1` הוא האמת
⚠️ **סחיפת תאריכים — הוכרע 06.08: `git log` הוא מקור-האמת, שמות מסמכים נגזרים ממנו.** קיימת ב-`INCIDENTS#12` וב-**3/3 מ-`docs/plans/PLAN-2026-08-06-*`** (`cleanup`·`user-visibility`·`workflows`) שנוצרו כולם **05.08 +0300**. ⛔ **לא לשנות שמות קיימים** — שובר את ההפניות. זו רשומה, לא תיקון

**מדדים חיים (06.08, נמדד):** 41 נרשמו · 56 עסקאות · 12 עם עסקאות · 26 סגורות · 0 שאינן USD ·
0 mentorships · 5 פידבקים פתוחים · 31 תקועים (22 `joined == last_seen`) · **activation 12/41 = 29%** · power users 2.

🔴 **שימור שבוע 1 = 0/6** (`scripts/retention.sql`, 06.08). מתוך 9 בני-מדידה, 6 פעילים בשבוע 0
ו-**אפס** חזרו בשבוע 1; 5/9 רשמו עסקה תוך שעה. **הבעיה אינה האונבורדינג — היא היום השני.** ⏭️ הגל הבא

## 🔴 עכשיו

**U — אנליטיקת משתמש: GA4 SPA + retention + `/api/notify`.** ראשון ברצף הנעול.

🔴 **`navigator.clipboard` בלי בדיקת קיום — כשל שקט במסלול המנטורינג (05.08).**
`SwingEdge_App.jsx:2608` — `undefined` בהקשר לא-מאובטח וב-WebView אנדרואיד: הקריאה זורקת,
ה-`catch` בולע, **הקוד לא הועתק והמשתמש לא יודע** (§2). **0 mentorships** — זו הסיבה, או שאיש לא ניסה? ⛔ אין דרך להבחין בלי טלמטריה

🔴 **`github-script@v9` — 0/3, טרם הורץ על runner** (הוצמד ב-`03c343e`). ריצה ראשונה צפויה
`data-guardian` **07.08 05:00 UTC**, אחריה `arch-auditor` **09.08 05:00** ו-`analyst` **09.08 06:00**. שלושתם נתיבי **דיווח** — אם v9 שבר אותם הכשל שקט מהגדרתו. ⛔ לא להריץ ידנית: הרצה מחוץ להקשר אינה מוכיחה את המסלול

---

## ⏭️ הבא בתור

**הרצף נעול:** U → R (ניקוי + docs) → W (workflows + מרוץ ה-smoke) → S2 (CSP + JWT + fontshare) → עצירה.

**S2 אחרון בכוונה** — אכיפת CSP לפני ש-U סיים = הפרה על גב המשתמשים. **הקשחה אחת לשלושה נתיבים**: `/api/notify` (נולד 06.08 לא-מוקשח), `api/feedback.js`, `waitlist` — אותו פער CSP+JWT; שלוש הקשחות נפרדות = סחיפה.

**W — project מובייל (Pixel 5): מכסה 1–2 מתוך 6, לא שש** — בעיקר #5 `parseUA`. אמולציה משנה UA/viewport ו**אינה מסירה יכולות**: #2/#3 → השורה ב-⏭️ למטה; #4 דורש WebKit/מכשיר (⏸️), Chromium לא מאמלץ iOS.

**פידבק פתוח שאינו מכוסה בשום מקום אחר (§10.2, מוין 06.08):**
- 🔴 **ה-OCR קרא את המחיר הנוכחי כמחיר הכניסה ואת ה-SL השאיר ריק** (`omrikapara1`,
  31.07, אנדרואיד). זה **הבאג שהוא דיווח** ו-**הוא לא תוקן** — `capture` היה בורר
  התמונה בלבד. `emails/fix_mobile_upload.html` אומר זאת במפורש ⛔ לא "טופל"
- הגדלת תמונת הסטאפ בלחיצה · כמה צילומים ברצף (אחרי ה-OCR) · העלאת קובץ בטופס הפידבק עצמו

**קמפיין ל-12 שרשמו עסקה ראשונה ונטשו** — "עשית עסקה ולא חזרת. מה היה חסר?" שאלה אחרת מ-`why_stopped`: 0/6 שימור שבוע 0→1 אומר שהתשובה אצלם, לא אצל התקועים. ⛔ רישום בלבד, אין תבנית

**`dawidd6/action-send-mail` v3→v18 — גל נפרד משלו.** 15 גרסאות ראשיות × 9 אתרי קריאה,
אחד מהם התרעת הכשל של `restore-drill`. ⛔ הוצא מ-W במפורש: "גל אחד, שינוי אחד"

נוספים, מחוץ לרצף:
- ⏭️ בדיקות **הסרת** יכולת ל-#2/#3 (`addInitScript` + `delete`) — אמולציה לא מגיעה לשם
- RPC פר-מטבע `avg_pnl_by_ccy` · ספי `generateProfile` מכוילים לדולר
- `admin_rpcs.sql:267-268` — `else 0` על מכנה ריק (§2) · שמות CSV ב-AdminPanel ב-UTC ולא מקומי

---

## ⏸️ חסום / ממתין לניב

- **שליחת 2 המענים** — `fix_mobile_upload` + `files_received` בריפו ו-`/api/notify` בנוי. ⛔ לא נשלח: הפרומפט הבא
- **הרצת `why_stopped_r2`** — `campaign=why_stopped_r2` · `prior_campaigns=why_stopped` · `audience=stuck_users`. ⛔ לא הורץ
- אימות ידני: אונבורדינג ב-₪ בחלון אנונימי · יתר §1-§4 של M (מטבע בייבוא · זבל→דחייה · round-trip · AdminPanel)
- 2 קבצי Sheets של `a0556783290` (IBI + אלטשולר) — חומר הגלם לתיקון הייבוא, טרם נוצל · **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה. עם השדרוג
- PR #44 Dependabot · דוח Architecture Auditor (14 ממצאים, טרם מוינו) · מלאי סודות · **`docs/TASKS-REGISTRY.md` טרם הורד לעץ** (הפיל את שלב 5 של R; ⛔ Claude לא יוצר אותו)
- Stripe Test Mode · Google Search Console · Google Workspace · ריפו פרטי מול Actions · QA מובייל 7 סעיפים · ערכת OCR (30 גרפים) · 390 תמונות · עו"ד על `/privacy` · רשם הדומיין (Auto-Renew)
- שדה טווח עסקה (בקשת 31.07, נוגע בסכימה+מסננים) · הודעה על 3 המספרים שזזו ב-T7 · מייל ל-`saridel` (22/22 ללא stop)
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05, הכשיל סשן 04.08). ⚠️ יושב שם `Backup-codes-niveven183.txt` — 2FA גלוי, **לא בריפו (אומת)**. הסיכון: `git add -A` משם

---

## ✅ נסגר השבוע

- **M** פערי מטבע §1-§6 · **S1** RLS · **R** ניקוי · **#12** טוקני GA4 (`08ca710`) · `why_stopped_r2` מוכן ⛔ לא נשלח — הפירוט נגזם ל-`docs/audits/STATE-ARCHIVE-2026-08.md`
- **W · `blockAnalytics` תוקן בשלושת הקבצים (05.08)** — גלוב→רג'קס. הוכחה: ישן חסם 0/דלף 2, חדש חסם 2/דלף 0, פרודקשן חסם 1/דלף 0. ממצאי sentinel 2→0, **אומת בפרודקשן** (`31049305536`: `findings: 0`). `INCIDENTS.md#13`
- **W · 33 pins מ-node20** — checkout v7 · setup-node v7 · cache v6 · github-script v9 (13 קבצים, 33/33, אפס שורה שאינה `uses:`). ארבעת התגים אומתו קיימים ב-API לפני ההצמדה; 17/17 workflows עוברים YAML parse · **`node-version` 20→22** (9/9). ⚠️ `backup.yml` לא נגע בשניהם — אין בו setup-node ואין הצמדת node20. ⚠️ **parse אינו הרצה: הופעלו על runner 9/33 מופעים, 3/4 סוגים** — checkout·setup-node·`cache*` (smoke+build `deb3c34`, sentinel `31049305536`). **`github-script@v9` 0/3 → 🔴 למעלה**
- **W · project `mobile-android` (Pixel 5)** — 2 בדיקות ב-`grep`, 8/8 עוברות. ⏭️ `download-artifact@v7→v8` (1 שורה) לא בוצע — לא היה ב-4 שאושרו

---

## ⚠️ סיכונים פתוחים

- **`react-router` GHSA-qwww-vcr4-c8h2 (high) נשאר פתוח — סיכון מקובל, לא באג.** התיקון היחיד הוא downgrade ל-7.11.0. הפגיעות היא CSRF ב-**RSC Mode** ולאפליקציה אין RSC. נאכף כהערה ב-`package.json` ליד `dependencies`. ⛔ **מתבטל ברגע שנאמץ RSC/server actions.** `DECISIONS.md` 27.07 + 06.08
- **הסתעפויות יכולת-דפדפן ללא כיסוי (05.08).** אימות ברוחב מוקטן אינו נוגע בהן **בהגדרה** — אותו מנוע,
  אותו משטח API. #1 `capture` תוקן · #2 → 🔴 למעלה · **#3** `ThemeContext.jsx:42` `matchMedia` לא מוגן
  (`:31` כן) → WebView ישן זורק ב-`useEffect` והעץ נופל · **#4** `IOSInstallBanner.jsx:9,13,20` iOS-only, מעולם לא הורצה · **#5** `FeedbackTab.jsx:49` `parseUA()` (מכוסה) · **#6** `:1304` חשוף מול `OnboardingTour:37`
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.** **לא נטש בשקט** — דיווח, צירף Sheets, הציע שיפור. **מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) בלי fallback ובלי עותק מקומי (500 ב-05.08). ⏭️ S2
- **מרוץ ה-smoke — נמדד ונאכף (W · 4ב) ✅.** ⚠️ **ההנחה הופרכה: `/app` החזיר 200, לא 500** — הכישלון היה
  500 מ-`api.fontshare.com` דרך ערוץ ה-console הלא-מסונן (תוקן, #13). המרוץ היה אמיתי אך רדום: המרווח
  deploy→בדיקות 5–21ש ב-4 ריצות הוא תוצר לוואי של `npm ci`, **לא כלל**; הסיכון **✅ כוזב, לא ❌**.
  `smoke.yml` ממתין ל-Deployment API (`success` ל-`github.sha`, 180ש, timeout=❌). ⚠️ **מוכיח ready, לא התפשטות alias** — אין סמן SHA באפליקציה ⏭️
- **אין בדיקה שחוסמת נתון דמוי-טוקן באנליטיקס יוצאת** (#12 בקריאה בלבד) ⏭️ S2 · ⛔ ואין אסרציה ש**ריצת CI שולחת אפס בקשות אנליטיקס** — השומר של #13 מוכח חד-פעמית, לא חוזרת · **שורות `waitlist` ירדו ב-2 תוך 24ש** (60, ▼2) — מחיקה ידנית או ספירה שגויה ב-Growth. **לא נבדק**
- 🔴 **כל מדד GA4 מלפני 05.08 בלתי-שמיש** (#13): ≈906 page_view סינתטיים (smoke 108×5 · sentinel
  122×3) מול 41 נרשמו / 12 פעילים, **בלי סמן שמפריד אותם בדיעבד**. ⚠️ **תאריך התיקון = קו הבסיס, אין להשוות לרוחבו.** ⏸️ סימון/ניקוי ב-GA4: על ניב
- ⚠️ **`Failure Alert` הוא `workflow_run` ואין לו צופה** — נעדר מטבלת ה-max-age של Watchdog **ומרשימת 11 ה-workflows של עצמו**. אם ייפול, **11 workflows מאבדים התרעה בשקט**. ⛔ לא תוקן ב-W
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — אין `created_at` בצד השרת; הערך מגיע מהלקוח
  ובעסקה מיובאת נושא את תאריך העסקה. **40/56 שורות מוקדמות מהרשמת הבעלים** (3 משתמשים) ולכן מוצאים מכל מדידת זמן. `retention.sql` מדווח בנפרד ⛔ לא ממציא 0
- **אין ליגר שליחה לאף מסלול egress מלבד מייל** (ציד 06.08): `api/ocr.js` מפעיל קריאה **בתשלום** לכל
  משתמש מאומת בלי רשומה של מי/כמה — העלות הכי לא-נראית · `api/health.js:160` היחיד עם `Allow-Origin: *`, עוקף את `_lib/cors.js` · 8 workflows מייל + 11 דיסקורד, אפס ליגר
- **`tradeImage` (data-URL) ב-localStorage בלבד** (`supabaseClient.js:65`, **בכוונה**) — לא עובר מכשיר, והוא
  צרכן המכסה ש-`:1665` בולע ב-`catch {}` ריק: המכסה מתמלאת, **כל** העסקאות מפסיקות להישמר מקומית, ואין סימן. ⛔ החלטת ניב 06.08
- `SwingEdge_App.jsx` (7,275) ו-`src/i18n.js` (2,891) גדולים **בכוונה** — ייפוצלו בעילה. **החלטה, לא חוב**
