# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.**
> חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט**
> (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-05 · HEAD: 918f9c3

**מדדים חיים (05.08, נמדד):** 41 נרשמו · 56 עסקאות · 12 משתמשים עם עסקאות ·
26 עסקאות סגורות · 0 שאינן USD · 0 mentorships · 5 פידבקים פתוחים · 31 תקועים
(22 מהם `joined == last_seen`) · חציון ימים לעסקה ראשונה 0.4 ·
**activation 12/41 = 29%** · power users 2.

## 🔴 עכשיו

**U — אנליטיקת משתמש: GA4 SPA + retention + `/api/notify`.** ראשון ברצף הנעול.

🔴 **`navigator.clipboard` בלי בדיקת קיום — כשל שקט על מסלול המנטורינג (05.08).**
`SwingEdge_App.jsx:2606` — `clipboard` הוא `undefined` בהקשר לא-מאובטח וב-WebView
אנדרואיד. הקריאה זורקת, ה-`catch` בולע, **הקוד לא הועתק והמשתמש לא יודע** (§2).
**שאלה פתוחה:** **0 mentorships** בפרודקשן — האם זו הסיבה, או שאיש לא ניסה?
⛔ לא תוקן במכוון; אין דרך להבחין בלי טלמטריה או משתמש שיבדוק.

---

## ⏭️ הבא בתור

**הרצף נעול:** U → R (ניקוי + docs) → W (workflows + מרוץ ה-smoke) → S2 (CSP + JWT + fontshare) → עצירה.

**S2 אחרון בכוונה** — U מוסיף `/api/notify` שצריך את אותה הקשחה; אכיפת CSP
לפני ש-U סיים = הפרה שתתגלה על גב המשתמשים.

**W — project מובייל ל-Playwright.** `playwright.config.js:26` מגדיר project **יחיד**,
`Desktop Chrome`, וכל בדיקות `verify` הן Node בלי DOM. זה התיקון האמיתי למחלקה
כולה, לא לבאג הבודד. **בלי project מובייל, כל שש הסתעפויות היכולת נשארות עיוורות.**

נוספים, מחוץ לרצף:
- RPC פר-מטבע `avg_pnl_by_ccy` · ספי `generateProfile` מכוילים לדולר
- `admin_rpcs.sql:267-268` — `else 0` על מכנה ריק (§2: שיעור בלי מכנה)
- שמות קבצי CSV ב-AdminPanel ב-UTC ולא בזמן מקומי

---

## ⏸️ חסום / ממתין לניב

- **מענה ל-5 הפידבקים** (`omrikapara1` ×4, `a0556783290` ×1) — הנוסחים אצל ניב.
  ✅ **חסימת המובייל ירדה** — "תוקן, נסה שוב" מדויק כעת לגבי בחירת סקרין שוט
- בדיקה ידנית: אונבורדינג ב-₪ בחלון אנונימי (בורר → אייקון → Settings=ILS)
- אימות ידני יתר §1-§4 של M (מטבע בייבוא · זבל→דחייה · round-trip · AdminPanel)
- שני קבצי Google Sheets של `a0556783290` — IBI + אלטשולר, לתיקון הייבוא
- **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה. עם השדרוג
- PR #44 Dependabot · דוח Architecture Auditor (14 ממצאים, טרם מוינו)
- Stripe Test Mode · Google Search Console · Google Workspace · ריפו פרטי מול Actions
- QA מובייל 7 סעיפים · ערכת בדיקה ל-OCR (30 גרפים) · תיקיית 390 תמונות
- נדנוד לעו"ד על `/privacy` · רשם הדומיין (איש קשר + Auto-Renew) · מלאי סודות
- שדה טווח עסקה (סווינג/יומי/ארוך) — בקשת משתמש 31.07, נוגע בסכימה + מסננים
- האם להודיע למשתמשים על שלושת המספרים שזזו ב-T7 (02.08)
- מייל ל-`saridel` — 22 עסקאות, 22/22 ללא stop, כולן סגורות
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05, הכשיל סשן ב-04.08).
  ⚠️ יושב שם `Backup-codes-niveven183.txt` — 2FA בטקסט גלוי, **לא בריפו (אומת)**.
  ניב החליט להשאיר. הסיכון הנותר: `git add -A` שירוץ מהתיקייה ההיא

---

## ✅ נסגר השבוע

- **M · פערי המטבע** §1-§6 (05.08) — `2491c86` `b5d99dd` `e9de006` `e09833e`
- **מובייל · `capture` הוסר** (05.08) — בורר המערכת מציע גלריה **וגם** מצלמה.
  אסרציה ב-`test:ocr` נועלת. **חסימת מענה הפידבק ירדה**
- **S1 · מיגרציית ה-RLS הורצה** (`ee72492`) — `rls_policy_always_true` **נעלם**. החסימה ירדה
- **גל הסטופים — מת, ולא בוצע.** ה-57.5% היה artifact של ייבוא **שנמחק מאז**
- **קמפיין `why_stopped`** נשלח 04.08 ל-5 מתוך 26 סגורות. ⚠️ **מנה הבאה חייבת
  שם campaign חדש** — ה-dedup יחסום את אותו שם בשקט
- **A5** `null` על אוכלוסייה ריקה (04.08) · **T10** מטבע (03.08) · **T9** (03.08) ·
  **גיזום `STATE.md`** 1,669 → ≤100, הארכיון מוכח זהה בית-בבית ל-`e09833e`

---

## ⚠️ סיכונים פתוחים

- **הסתעפויות יכולת-דפדפן ללא כיסוי (05.08).** אימות ברוחב מוקטן אינו נוגע בהן
  **בהגדרה** — אותו מנוע, אותו משטח API. #1 `capture` תוקן, #2 → 🔴. נותרו:
  **#3** `ThemeContext.jsx:42` `matchMedia` **לא מוגן** (`:31` כן) → WebView ישן
  זורק ב-`useEffect` והעץ נופל · **#4** `IOSInstallBanner.jsx:9,13,20` iOS-only,
  מעולם לא הורצה · **#5** `FeedbackTab.jsx:49` `parseUA()`, תיוג OS שגוי מסווה
  באגי פלטפורמה · **#6** `SwingEdge_App.jsx:1304` חשוף בעוד `OnboardingTour:37` מגן
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.**
  **לא נטש בשקט** — דיווח באג, צירף 2 קישורי Sheets, הציע שיפור. **מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) בלי fallback ובלי עותק
  מקומי. Sentinel תפס HTTP 500 ב-05.08. לא חוסם, אבל הטיפוגרפיה משתנה. ⏭️ S2
- **Smoke נכשל על `e09833e` ב-15:05** — `/app` 500, גם ה-retry. ב-18:07 יציב 200
  (×3). מרוץ deploy. ⚠️ retry אחד לא הספיק: או שאינו ממתין ל-deploy, או ש-cold
  start ארוך ומשתמש אמיתי יפגוש 500. ⏭️ ל-W
- **שורות `waitlist` ירדו ב-2 תוך 24ש** (60, ▼2). מחיקה ידנית או ספירה שגויה בדוח Growth. **לא נבדק**
- `agents/` ו-`core/` — תיקיות Python בריפו React, אפס מייבאים. שאריות Trading Room. ⏭️ R
- `HIVE_ARCHITECTURE.md` מתעד מערכת שאינה בריפו · `docs/` מערבב 8 דוחות
  מתוארכים עם מסמכי-קבע (`docs/audits/` כבר קיים). ⏭️ R
- `SwingEdge_App.jsx` (7,275) ו-`src/i18n.js` (2,891) נשארים גדולים **בכוונה** —
  ייפוצלו בעילה, לא כדי לסדר. **החלטה, לא חוב**
