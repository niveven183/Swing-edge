# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.**
> חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט**
> (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-05 · HEAD: e09833e

**מדדים חיים (05.08, נמדד):** 41 נרשמו · 56 עסקאות · 12 משתמשים עם עסקאות ·
26 עסקאות סגורות · 0 שאינן USD · 0 mentorships · 5 פידבקים פתוחים · 31 תקועים
(22 מהם `joined == last_seen`) · חציון ימים לעסקה ראשונה 0.4 ·
**activation 12/41 = 29%** · power users 2.

## 🔴 עכשיו

**U — אנליטיקת משתמש: GA4 SPA + retention + `/api/notify`.** ראשון ברצף הנעול.

---

## ⏭️ הבא בתור

**הרצף נעול:** U → R (ניקוי + docs) → W (workflows + מרוץ ה-smoke) → S2 (CSP + JWT + fontshare) → עצירה.

**S2 אחרון בכוונה** — U מוסיף `/api/notify` שצריך את אותה הקשחה; אכיפת CSP
לפני ש-U סיים = הפרה שתתגלה על גב המשתמשים.

נוספים, מחוץ לרצף:
- RPC פר-מטבע `avg_pnl_by_ccy` · ספי `generateProfile` מכוילים לדולר
- `admin_rpcs.sql:267-268` — `else 0` על מכנה ריק (§2: שיעור בלי מכנה)
- שמות קבצי CSV ב-AdminPanel ב-UTC ולא בזמן מקומי

---

## ⏸️ חסום / ממתין לניב

- **מענה ל-5 הפידבקים** (`omrikapara1` ×4, `a0556783290` ×1) — הנוסחים מוכנים אצל ניב.
  ⛔ **קרא את ⚠️ המובייל לפני שליחה** — "תוקן, נסה שוב" אינו מדויק היום
- בדיקה ידנית: אונבורדינג ב-₪ בחלון אנונימי (בורר → אייקון → Settings=ILS)
- אימות ידני יתר §1-§4 של M (ייבוא עם עמודת מטבע · ערך זבל → דחייה ·
  round-trip ייצוא→ייבוא · כרטיסי AdminPanel)
- שני קבצי Google Sheets של `a0556783290` — IBI + אלטשולר, לתיקון הייבוא
- **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה של
  30 שניות כפי שנרשם קודם. ייכנס עם השדרוג
- PR #44 Dependabot · דוח Architecture Auditor (14 ממצאים, טרם מוינו)
- Stripe Test Mode · Google Search Console · Google Workspace
- QA מובייל 7 סעיפים · ערכת בדיקה ל-OCR (30 גרפים) · תיקיית 390 תמונות
- נדנוד לעו"ד על `/privacy` · רשם הדומיין (איש קשר + Auto-Renew) · מלאי סודות
- החלטת ריפו פרטי מול דקות Actions
- שדה טווח עסקה (סווינג/יומי/ארוך) — בקשת משתמש 31.07, נוגע בסכימה + מסננים
- האם להודיע למשתמשים על שלושת המספרים שזזו ב-T7 (02.08)
- מייל ל-`saridel` — 22 עסקאות, 22/22 ללא stop, כולן סגורות
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05). עלה פעמיים ב-04.08:
  סשן על הריפו השגוי, ותבנית שנלקחה ממנו. ⚠️ יושב שם `Backup-codes-niveven183.txt`
  — קודי 2FA בטקסט גלוי, **לא בריפו (אומת)**. ניב החליט להשאיר.
  הסיכון הנותר: `git add -A` שירוץ מהתיקייה ההיא

---

## ✅ נסגר השבוע

- **M · פערי המטבע** §1-§6 (05.08) — `2491c86` `b5d99dd` `e9de006` `e09833e`
- **S1 · מיגרציית ה-RLS הורצה** (`ee72492`) — `rls_policy_always_true` **נעלם**
  מ-`get_advisors`. הרשומה הקודמת ("⛔ טרם הורצה") הייתה שגויה; החסימה ירדה
- **גל הסטופים — מת, ולא בוצע.** אצל מי שמזין ידנית: stop/target/setup/emotion
  = 100%. ה-57.5% היה artifact של ייבוא **שנמחק מאז**. הוסר מהתור
- **קמפיין `why_stopped`** נשלח 04.08 ל-5 מתוך 26 סגורות.
  ⚠️ **מנה הבאה חייבת שם campaign חדש** — ה-dedup יחסום את אותו שם בשקט
- **A5** שיעור על אוכלוסייה ריקה = `null` (04.08) · **T10** מטבע (03.08) · **T9** (03.08)
- **גיזום `STATE.md`** 1,669 → ≤100. הארכיון מוכח זהה בית-בבית ל-`e09833e`

---

## ⚠️ סיכונים פתוחים

- 🔴 **OCR במובייל — מסלול שונה, מעולם לא נבדק (ממצא 05.08).**
  `SwingEdge_App.jsx:5784` — הקלט של מסך **כלים** נושא `capture="environment"`,
  והוא **הקלט היחיד בריפו** שנושא אותו (`:4667`, `:6481`, `:6979` — בלי).
  `:3172` — באנדרואיד אין `getDisplayMedia`, ולכן המסלול **תמיד** נופל לקלט הזה,
  והדפדפן פותח **מצלמה במקום גלריה**. משתמש שמנסה להעלות **סקרין שוט** לא יכול
  לבחור אותו. זהו בדיוק מסך הדיווח של `omrikapara1` (×4, אנדרואיד 406px).
  האימות "ב-406px" (ארכיון) נעשה בדפדפן **שולחני**, שבו `getDisplayMedia` קיים —
  ולכן הענף הזה לא נבדק שם מעולם. **F1-F4 אינם נוגעים בו.** חוסם את מענה הפידבק
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.**
  ⚠️ תיקון לרשומה קודמת: הוא **לא נטש בשקט** — דיווח באג, צירף שני קישורי
  Google Sheets (IBI + אלטשולר), והציע שיפור. **משתמש מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) בלי fallback ובלי עותק
  מקומי. Sentinel תפס HTTP 500 ב-05.08. לא חוסם — הדפדפן נופל לפונט מערכת — אבל
  הטיפוגרפיה משתנה. ⏭️ לאחד עם S2 (fontshare כבר ב-CSP)
- **Smoke Tests נכשל על `e09833e` ב-15:05** — `/app` החזיר 500, וה-retry נכשל גם.
  ב-18:07 `/app` מחזיר 200 יציב (נבדק ×3). מרוץ deploy, לא רגרסיה. ⚠️ אבל retry
  אחד לא הספיק: או שה-smoke אינו ממתין לסיום ה-deploy, או שחלון ה-cold start ארוך
  ומשתמש אמיתי עלול לפגוש 500. ⏭️ ל-W
- **שורות `waitlist` ירדו ב-2 תוך 24ש** (60, ▼2). שורות לא אמורות לרדת — מחיקה
  ידנית או ספירה שגויה בדוח Growth. **לא נבדק**
- `agents/` ו-`core/` — תיקיות Python בריפו React, אפס מייבאים. שאריות Trading Room. ⏭️ R
- `HIVE_ARCHITECTURE.md` בשורש מתעד מערכת שאינה בריפו. ⏭️ R
- `docs/` מערבב 8 דוחות מתוארכים עם מסמכי-קבע. `docs/audits/` כבר קיים. ⏭️ R
- `SwingEdge_App.jsx` (7,275) ו-`src/i18n.js` (2,891) נשארים גדולים **בכוונה** —
  ייפוצלו בעילה, לא כדי לסדר. **החלטה, לא חוב**
