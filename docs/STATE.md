# STATE — מצב חי

> **חוזה שבעת הקבצים — פריט חי בקובץ אחד בלבד.** (`NEXT` ו-`INBOX` ⛔ אינם קבצי מזהים — ראה `CLAUDE.md` §14.)
>
> | קובץ | מה חי בו |
> |------|-----------|
> | `docs/STATE.md` (כאן) | 🔴 **עכשיו** · ⏭️ **שלושת הבאים** · ⏸️ חסום · ⚠️ סיכונים. **תקרה קשיחה: 100 שורות.** |
> | `docs/BACKLOG.md` | כל פריט פתוח שאינו פעיל (`B-001`…). ⛔ אין תקרה. |
> | `docs/DONE.md` | הושלם — מזהה · תאריך · **hash** · **איך אומת** · **`T`** (רמת הסיווג, `CLAUDE.md` §15) (`D-001`…). |
> | `docs/CHECKS.md` | בדיקות ידניות חוזרות — תדירות **או** תנאי-גלישה (`C-001`…). |
> | `docs/audits/STATE-ARCHIVE-*.md` | **סגור בלבד.** ⛔ פריט **פתוח** לעולם לא נוחת שם — הוא הולך ל-`BACKLOG`. |
>
> מעבר בין קבצים = **מחיקה משם והוספה כאן**, ⛔ לא העתקה. ⛔ **מזהה לא נמחק ולא ממוחזר לעולם.** ⛔ **קובץ-מרשם מחוץ ל-`docs/` אסור — נאכף.**
> ⚠️ מי שגוזם את הקובץ הזה מעביר ל-`BACKLOG` או ל-`DONE` — ⛔ **לעולם לא ל-`ARCHIVE` אם הפריט פתוח.** זה בדיוק מה שאיבד 33 פריטים (`B-051`…`B-083`).
> נאכף מכנית ב-`npm run test:registry` (חוליה 23).
>
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט** (נחקק 05.08). ⚠️ שורת HEAD מפגרת בקומיט אחד **מעצם הגדרתה** — `git log -1` הוא האמת.

🧭 **כיוון: ראה `docs/NEXT.md`** — 3 פריטים בדיוק, ההכרעה של ניב+Claude. הקובץ הזה מתאר **מצב**; `NEXT` מתאר **מה עושים**. 📥 קליטה חדשה ⇒ `docs/INBOX.md`, ו-triage בתחילת הסשן הבא.

עודכן: 2026-08-15 · HEAD: d46baf1

**מדדים חיים (נמדדו 11.08–12.08):** **46 ב-`auth.users`** · `user_settings` **34/46** — **12/46 בלי שורה כלל** (⚠️ שארית: 12/12 לא נכנסו מאז 20.07 08:42) · **60 עסקאות** (34 פתוחות · 26 סגורות · 0 דמו) · 🔴 **14/60 תווית מטבע שגויה** (1 ILS על `BE`/NYSE + 13 ת"א באגורות מתויגות **USD** — ⚠️ **⛔ לא ILS**; ההנחה ההפוכה הופרכה 12.08, ראה `DECISIONS`) · **0/60 `horizon`** · **13/46** עם עסקאות · 0 mentorships · **5/8 פידבקים לא-סגורים** (3 ללא מענה · 2 נענו וטרם יושמו) · 31 תקועים · `waitlist` **61** · **activation 13/46 = 28%**. ⚠️ **7/7 נרשמי 03.08+10.08 — 0/7 הפעילו.**

🔴 **שימור שבוע 1 = 0/6** (`scripts/retention.sql`, 06.08). 6 פעילים בשבוע 0, **אפס** חזרו. **הבעיה אינה האונבורדינג — היא היום השני.** ⛔ **התוצאה האמיתית של `why_stopped` היא 5→0→0, לא 5→1→0** — `de66dc99` מעולם לא היה נמען, וה"הצלחה" היחידה הייתה **שיוך שגוי** (`AUDIT-2026-08-11-retention-cohort.md` §0). ⚠️ **הקיפאון האמיתי: אף אחד לא סגר עסקה מאז 29.07.**

---

## 🔴 עכשיו

✅ **ההון ה**סגור** מומר — בוצע ואומת בעין 15.08 (`B-142` → `D-033`).** הכותרת נקובה ב-`dispCcy`, וכל מחובר שאינו ניתן להמרה **מוחרג ונספר** בתג **אחד** מאוחד (חי+סגור). `C-023` ירוק ב-3+1 מצבי-סירוב בחיבור: `₪10,000.00` (אין טבלה) · `₪20,200.00` (`unverified_instrument`) · `₪20,200.00` (`no_rate_for_day`, מעבר ל-7 ימי הליכה-אחורה). 🔴 **ואימות-העין מצא באג שני שהתכנון ⛔ לא צפה:** אין spot + **יש** fixing ליום הבסיס ⇒ הכותרת הראתה **`$20,100.00`** = 20,000 **שקלים** + 100 **דולר** תחת "$" — וההערה בקוד הצהירה שזה בלתי-אפשרי. ⛔ **⛔ אינו קצה:** זו טבלת ה-`catch` של `loadRateTable`, כי spot ⛔ לעולם אינו נשמר ל-localStorage. תוקן, נמדד **אדום** (1/330) ואז ירוק (**330/330**), ואומת שוב על המסך: `$10,100.00`. **`B-142` · `C-023` · `B-143` · `B-144`**

✅ **P&L חי מומר ב-spot — בוצע ואומת בעין 13.08.** `C-023` ירוק ב-4/4 המצבים בחיבור (`₪/₪` → `₪10,274.25`, **Δ=₪49.50** — בדיוק הפער שנמדד); אין spot ⇒ **סכום חלקי** + תג עם **מונה** ו-`—` עם נימוק, ⛔ לא 0 ולא מספר לא-מומר; ה-PDF מצהיר על המצרף החלקי. 🔴 **מלכודת ה-`realizedDayKey` הופרכה בדפדפן:** ה-P&L החי חושב ב-spot `2.9810` ⛔ ולא בשער יום-הכניסה `3.0009`. ⚠️ **שארית מוכרת ⛔ אינה רגרסיה:** ההון ה**סגור** עדיין לא-מומר תחת סמל החשבון כשאין שער ⇒ **`B-142`** (אסרציה `13.9b` היא קו קפוא עליו). ‹R-1› **`B-119` · `C-023` · `B-142`**

✅ **גל השומרים — רשת ההתראות נמדדה, ⛔ לא הונחה (15.08).** לפני הגל: `Failure Alert` בלי צופה, `curl -sS` בלי `--fail` שיוצא 0 על 401 ⇒ webhook מבוטל הדפיס «HTTP 401» ונשאר ירוק, ו-`Watchdog` מודד התיישנות בלבד. **מה נעשה:** 🐤 `Canary Drill` (`workflow_dispatch` בלבד) מייצר כשל מבוקר במסלול האמיתי · `Delivery gate` חדש מפיל את הריצה כשההתראה ⛔ לא נמסרה **באף** ערוץ · סטטוס ה-HTTP **מושווה** ל-204 ⛔ ולא מודפס · `Restore Drill` נכנס לצפייה (11→13 שמות) · `timeout-minutes` נגזרות ממדידה · ו-🔗 **מתאם התראות** ב-`fleet-daily` מודד **כיסוי** (מכנה = כשלים אמיתיים, ⛔ לא זמן). **אומת:** T-A ירוקה **3/3** פעמים עם `channels delivered: 2/2`, T-B שקטה. **כיסוי: 16/18 workflows** — 2 מוחרגים במפורש (`Health Monitor`, dispatch בלבד · `Failure Alert` עצמו, שנצפה ע"י מתאם ההתראות). ⇒ `B-001` · `B-002` · `B-018` נסגרים ב-`DONE` בקומיט הבא. 🔴 **בלוק הספים ⛔ לא בוצע** — המדידה הפריכה את ההכרעה שאושרה ⇒ **`B-148`, חסום על הכרעת ניב**. **`C-026`**

⏸️ **גל ב׳ — 13 שורות ת"א באגורות. ⛔ חסום על הודעה מוקדמת למשתמש `e403e391`.** ✅ **המיגרציה הורצה ואומתה 12.08** (`D-026`): `ILA`/`EUR`/`GBP` פתוחים, `default 'USD'`+`NOT NULL` הוסרו, 61 שורות **אפס נגעו**. ⚠️ **`ILA` בר-רישום ⛔ אינו `ILA` בר-קריאה** — מסלול הקריאה טרם יודע לחלק ב-100 ⇒ `B-121`. **`B-005` · `B-121`**

---

## ⏭️ הבא בתור — ⛔ **מצטט** את `docs/NEXT.md`, ⛔ אינו רשימה משלו

> 🔴 **מקור-אמת יחיד = `docs/NEXT.md`.** כאן **מזהה בלבד**; ההנמקה, דרך היישום ומדד הסגירה חיים שם ⛔ ולא כאן — פרט שנכתב כאן הוא עותק שני שייסחף (`R-6`). נאכף ב-`test:registry` §9.6: **זהות דו-כיוונית**, ⛔ לא נוכחות.

1. ⏭️ ספי `Watchdog` נכתבים מחדש — **`B-148`**
2. ⏭️ שלושת מסלולי הכתיבה — **`B-129`**
3. ⏭️ משפחת סמל-המצרף — **`B-143`**

⛔ **אין להמליץ סדר בתוך קטגוריה** — הבחירה של ניב. שאר הפריטים הפתוחים ב-`docs/BACKLOG.md`.

---

## ⏸️ חסום / ממתין לניב

- 🆕 **ארבע הכרעות מוצריות (⛔ אף אחת אינה משימת קוד):** (1) פנייה ל-**12 הרדומים** · (2) **מסירת** מימוש `omrikapara1` — §10.2: "טופל" דורש מסירה, לא רק hash · (3) **3 פידבקים ללא מענה** · (4) **PR #44 + ענף Dependabot** יחד. **`B-084`…`B-087`**
- **הרצת `why_stopped_r2`** (`prior_campaigns=why_stopped` · `audience=stuck_users`) — ⛔ לא הורץ. **`B-088`**
- **מיגרציות שנכתבו ולא הורצו** (§12): `b69b295` מכנה מסונן ב-`admin_rpcs`. ✅ שתי מיגרציות המטבע הורצו 12–13.08 (`D-026` · `D-025`). **`C-004`**
- אימות ידני: אונבורדינג ב-₪ בחלון אנונימי · §1-§4 של M · 2 קבצי Sheets של `a0556783290` · **הגנת סיסמאות דלופות** (Supabase Pro בלבד). **`B-090`…`B-092`**
- Stripe Test Mode · Search Console · Workspace · QA מובייל · ערכת OCR · עו"ד על `/privacy` · **מלאי סודות** · **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05; ⚠️ להעביר חומרי שיווק תחילה — הסיכון הוא `git add -A` משם). **`B-093`…`B-097`**
- ⏳ **ממתינים לאות, ⛔ לא למשימה:** תשובת עומרי על `fix_mobile_upload` — **אימות אנדרואיד הוא הדבר היחיד שיסגור את באג ה-OCR** (שני חצאיו תוקנו 01.08+08.08, אף אחד לא אומת מולו) · משפך GA4 אחרי 16.08 · הריצה **המתוזמנת** של `user-analytics`. **`B-098`…`B-103` · `C-012` · `C-016`**

---

## ⚠️ סיכונים פתוחים

- **`react-router` GHSA-qwww-vcr4-c8h2 (high) — סיכון מקובל, ⛔ לא באג.** התיקון היחיד הוא downgrade ל-7.11.0; הפגיעות היא CSRF ב-**RSC Mode** ולאפליקציה אין RSC. ⛔ **מתבטל ברגע שנאמץ RSC/server actions.** `DECISIONS` 27.07 + 06.08 · **`B-104`**
- 🔴 **ה-CSP הוא Report-Only בלי אספן** (`vercel.json:18`, `report-uri|report-to` = **0**) — כותרת שנראית כמו הגנה ואינה מייצרת אות. ⛔ אין לאכוף לפני אספן + חלון תנועה נקי, ⛔ ובתנאי שההפרות **לא** ינחתו ב-artifacts: הריפו ציבורי. **`B-003`**
- 🔴 **כל מדד GA4 מלפני 05.08 בלתי-שמיש** (`INCIDENTS#13`): ≈906 page_view סינתטיים מול 41 נרשמו / 12 פעילים, **בלי סמן שמפריד בדיעבד**. ⚠️ **תאריך התיקון = קו הבסיס, אין להשוות לרוחבו.** **`B-105`**
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — הערך מגיע מהלקוח; **40/59 מוקדמות מהרשמת הבעלים** ⇒ מוצאים מכל מדידת זמן. 🆕 **ו-`last_sign_in_at` מדווח בחסר — הוכח:** `92a06c0c` כתב עסקה ב-11.08 בעוד השדה קורא 05.08 ⇒ **מודד re-auth, לא פעילות.** **`B-035` · `B-036`**
- ⚠️ **42 `catch {}` ריקים** ב-`SwingEdge_App.jsx` + `AdminPanel.jsx:50` — ⛔ אף אחד לא נבדק; **הספירה אינה הערכת חומרה.** **`B-016`**
- ⚠️ **ארבעה ממצאי §11 מגל 636b490 שנמדדו ו⛔ לא תוקנו:** `fmtPrice(null,…)` → `"$0"` · `!form.ticker` ⛔ אינו תופס `"   "` · `DayTradesModal.metricsOf` `catch {}` שמחזיר `{pnl:0}` · `src/localAI.js` **קוד מת**. **`B-019`…`B-022`**
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות.** **לא נטש בשקט** — דיווח, צירף Sheets, הציע שיפור. **מעורב שאבד.** **`B-034`**
- 📌 **הכלל שנחקק 08.08: מספר או מיקום שנרשם פעם ולא נמדד שוב אינו עובדה.** ⚠️ **וההוכחה בקובץ הזה עצמו:** `arch-auditor.mjs:134,141,153` מפנים ל-`docs/STATE.md:98` — שורה שנדדה 99→100→98 בתוך יומיים, וגזימת הקובץ הזה הזיזה אותה שוב. `test:arch#5,#7` עוברות כי הן קוראות **תוכן**; ההפניה האנושית שולחת לשורה אחרת **בשקט**. **`B-028`**
- ⚠️ **סירוב שקט ⛔ אינו סירוב — ונמדד שהוא כמעט נכתב פעמיים.** שתי אסרציות "⛔ אין עותק שני" נפלו על **התיעוד של עצמן** (המחרוזת `|| 1` ו-`accountAmount` מצוטטות בהערות). ⇒ כל אסרציית `!/x/.test(src)` חייבת לנכות שורות הערה/ייבוא. 🔴 **וקרה שוב 13.08, פעם שלישית ובכיוון ההפוך:** §11.2 של `test:registry` **עברה בירוק** על שורת `DONE` שסומנה `T3` — משום שהשורה **מתעדת את הרגקס** ⇒ הסמן החיובי ישב בתוך ציטוט. ⚠️ **הפעם זה שער שמאשר בטעות, ⛔ לא שער שפוסל בטעות** — מסוכן יותר. תוקן: ציטוטים ותאי-קוד מנוכים לפני הבדיקה, והשער נצפה אדום אחרי. **`B-120`**
- ⚠️ **הסיכון שהמרשם הזה נועד לסגור:** גיזום שמעביר פריט **פתוח** לארכיון ממית אותו. 33 פריטים (`B-051`…`B-083`) שוחזרו כך 12.08 — חלקם המתינו מ-01.08. ⛔ **בלי `test:registry` זה יקרה שוב.**
- ✅ **סתירת המרשם הוכרעה 13.08 (`DECISIONS`, אפשרות א'): שורת מצבה `✅ בוצע → D-xxx` היא מצב-הסיום של מזהה `B-`** — הפירוט עובר ל-`DONE` תחת `D-` חדש, המזהה נשאר בר-ציטוט, ואין עותק שני. נוספה `8.4`: **מצבה חייבת להצביע על `D-` שקיים** — ⚠️ **נצפתה נכשלת על 5 מצבות אמיתיות** ⛔ ולא על הפרה מסונתזת. ⛔ **השער לא רוכך.** ✅ **`B-126` נסגר ב-`D-028`** — ו-`8.4` **נצפתה נכשלת עליו בפועל** (מצבה שהצביעה על `D-` שטרם נכתב) לפני שהשורה נוספה ל-`DONE` ⇒ המנגנון סגר מעגל שלישי ברציפות תחת כלל `4.1`. **`B-126` · `D-028` · `D-029`**
- 🆕 **שלושה פריטים נולדו בגל השורשים (13.08) ויושבים ב-`BACKLOG`, ⛔ לא כאן:** `B-129` שלושת מסלולי הכתיבה ⛔ אינם מיושרים על מטבע (ידני · ייבוא · OCR — ל-OCR **אפס** אזכורי `currency`) ‹R-1› · `B-127` שני המתגים מייצרים 4 מצבים ו-`$861` מול `$2,524.75` ⛔ אינו מוסבר ‹R-6› · `B-128` הבהוב חשוד במעבר מטבע — **⛔ נרשם בלי שורש**, כי המנגנון שלו ⛔ **לא נמדד**. **`B-127`…`B-129` · `C-022`**
- 🆕 **שני ממצאים מגל ה-NEXT (13.08), מוגדרים ב-`BACKLOG` ⛔ לא כאן:** `B-130` — **9/10 הערוצים יוצאים בלבד** (14/17 workflows לדיסקורד · 9/17 למייל · ⛔ אפס קליטה חזרה; Cowork #7 Dispatcher ⛔ אינו רץ) ⇒ `docs/INBOX.md` הוא הקליטה הידנית ‹R-5› · ✅ **`B-131` הוכרע ובוצע 13.08** — `SwingEdge-Master-Tasks.md` (03.08) מוזג ונמחק: **48 יחידות הכרעה** (17 שורות טבלה · 2 חסומים · 22 חובות · 5 כללי עבודה · מקטע ה-✅ · תמונת ה-📊) מוינו אחת-אחת מול הרפו**, 38 נמצאו מכוסות או מיושנות-בנימוק, ו-**10 לא היה להם בית בשום מקום** ⇒ `B-132`…`B-141`. `test:registry` §10 חוסם היווצרות מרשם-שורש חדש. ✅ **מצבה: `B-131` → `D-030`.** מפה מלאה: `docs/AGENT-CHANNELS.md`. **`B-130` · `B-131` · `D-030` · `C-025`**
- 🆕 **עומק האבחון נקבע מכנית 13.08 — `CLAUDE.md` §15 (`T1`/`T2`/`T3`), ⚠️ **חמש** שאלות בינאריות ⛔ לא תחושה** (השלוש הוחלפו באותו יום: הן נגזרו מכשלי גל ג׳ ⇒ `api/` יצא `T1` ומשימת `docs` רב-קבצית יצאה `T3`; החמש מסווגות לפי **סוג נזק**, ו-`הפיכות`/`אבטחה` הן טריגר-יחיד). ⚠️ **`T3` בלי מסך מושפע ⇒ סמן `אומת-מקור`** — קריאת התוצר מ-`origin`, ⛔ לא מהעץ המקומי. `docs/DONE.md` נושא עמודת `T`; `test:registry` §11 חוסם שורה בלי רמה, ו-`T3` בלי אימות-עין. ⚠️ **29 השורות ההיסטוריות `(רטרו)`** — סיווגן שוחזר מהתיאור, ⛔ לא מהזמן-אמת, ולכן הן **מחוץ** לשער אימות-העין: רטרו-התאמת `D-009`/`D-011` כך שייראו מאומתים הייתה בדיוק ההפרה ‹R-3›. הכיול הדו-שבועי: **`C-024`**
- 🧭 **`docs/PROJECT-GUIDE.md` נולד 13.08** — 10 פרקים, כל מספר עם פקודת המדידה שלו. `docs/ARCHITECTURE.md` הוא שכבת ההתמצאות בלבד ומפנה אליו. ⚠️ **שתי טענות נוספות בו נמצאו מיושנות מעבר למספרים:** `health.yml` **deprecated** מ-25.07 (`workflow_dispatch` בלבד, ⛔ לא "כל 30 דק'") · **`tradeEnums.js`** הוא מקור-האמת ל-Setup/Emotion, ו-`tradeOptions.jsx` **מייצא-מחדש** ממנו.
- ✅ **`B-145` הוכרע 15.08 (ניב): `STATE ⏭️` **מצטט** את `NEXT`, ⛔ ואינו מחזיק רשימה משלו.** לפני ההכרעה שתי הרשימות חיו במקביל ב-**0/3 חפיפה** — `B-007` · `B-009` · `B-006` מול שלושת פריטי `NEXT`. ⚠️ **שלושתם ⛔ לא נמחקו** — נמדד 15.08: 3/3 קיימים ב-`BACKLOG` כ**פתוח**. ⛔ **§9.1–9.5 לא יכלו לראות את הסתירה** — כולן קוראות את `NEXT` **לבדו** ⇒ שער שמאמת **נוכחות** ואינו מאמת **הסכמה** הוא ‹R-3›. נוספה `9.6`, **נצפתה אדומה** על הסתירה האמיתית **ועל מזהה-עודף** (מבחן השיניים ל-`R-3` עצמו), ואז ירוקה. ✅ **נסגר `D-035`** (`a95f0d8`)
- 🆕 **שלושה ממצאים מאבחון השומרים (15.08) שטרם קיבלו בית — `AUDIT-guardians.md`:** (1) **`B-018` תויג «אינו תקלה — הכרעה» ⛔ ואינו מדויק:** ההערה `failure-alert.yml:6-7` מצהירה ש-`Restore Drill` «self-alarms on its own failure condition» — נמדד שהשערים `always() && status != 'pass'` (`:177`,`:218`) **כן** תופסים כשל-צעד, ⛔ **אך לא job שמעולם לא התחיל** (YAML שבור · cron מושבת · service container · timeout; ⛔ ואין `timeout-minutes` ל-job) ⇒ השארית היא `Watchdog` ב-**840ש'** = גילוי אחרי **5.2 ימים**. **`S8γ` — הערה שמצהירה שמצב בלתי-אפשרי, והמצב אפשרי.** (2)+(3) **שתי שורות `INBOX` אוששו בקוד ו⛔ טרם קיבלו מזהה** (הפרומפט אסר נגיעה ב-`INBOX` בשלב האבחון ⇒ **§14 מופר עד שהגל ירשום אותן**): סלקטור עמום ב-**`tests-sentinel/sentinel-auth.spec.js:204`** — **7 התאמות** (3 ב-`SwingEdge_App.jsx` `4473`/`4653`/`4980` + **4** ב-`AdminPanel.jsx`), ⚠️ ומספרי השורות ב-`INBOX` כבר **מיושנים ב-~96** (‹R-3›, מוצע `B-146`·S) · ואבחון מומצא ב-**`:382-387`** — `reason`+`risk` הם **מחרוזות קבועות** בעוד `got` נושא את המדידה שסותרת אותן, **ב-22 אתרי `add()`** ⇒ דפוס ולא שורה (‹R-2›, מוצע `B-147`·M). ⚠️ **`B-146` יסתיר את `B-147` אם יתוקן לבדו.**
- 🧬 **שכבת השורשים נוספה לראש `docs/BACKLOG.md`: שישה מנגנונים · 48 שיוכים · שורש-על.** ⚠️ **126 פריטי המרשם ⛔ אינם 126 באגים בלתי-תלויים.** המיפוי נאכף **דו-כיוונית** ב-`test:registry` §8.3 — **מיפוי שנסחף גרוע ממיפוי שאינו קיים**, כי הוא נקרא כאילו הוא נכון.
