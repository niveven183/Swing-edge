# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.**
> חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט**
> (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-06 · HEAD: d9e54e1 — ⚠️ מפגר בקומיט אחד **מעצם הגדרתו** (אין hash לפני commit, ו-amend אסור). `git log -1` הוא האמת

**מדדים חיים (06.08, נמדד):** 41 נרשמו · 56 עסקאות · 12 עם עסקאות · 26 סגורות ·
0 שאינן USD · 0 mentorships · 5 פידבקים פתוחים · 31 תקועים (22 `joined == last_seen`) ·
**activation 12/41 = 29%** · power users 2.

🔴 **שימור שבוע 1 = 0/6** (`scripts/retention.sql`, 06.08). מתוך 9 בני-מדידה, 6 היו
פעילים בשבוע 0 ו-**אפס** חזרו בשבוע 1. 5/9 רשמו עסקה תוך שעה מההרשמה:
**הבעיה אינה האונבורדינג — היא היום השני.** ⏭️ זו השאלה שהגל הבא עונה עליה.

## 🔴 עכשיו

**U — אנליטיקת משתמש: GA4 SPA + retention + `/api/notify`.** ראשון ברצף הנעול.

🔴 **`navigator.clipboard` בלי בדיקת קיום — כשל שקט על מסלול המנטורינג (05.08).**
`SwingEdge_App.jsx:2608` — `undefined` בהקשר לא-מאובטח וב-WebView אנדרואיד: הקריאה
זורקת, ה-`catch` בולע, **הקוד לא הועתק והמשתמש לא יודע** (§2). **0 mentorships**
בפרודקשן — זו הסיבה, או שאיש לא ניסה? ⛔ לא תוקן: אין דרך להבחין בלי טלמטריה.

---

## ⏭️ הבא בתור

**הרצף נעול:** U → R (ניקוי + docs) → W (workflows + מרוץ ה-smoke) → S2 (CSP + JWT + fontshare) → עצירה.

**S2 אחרון בכוונה** — אכיפת CSP לפני ש-U סיים = הפרה על גב המשתמשים. **הקשחה אחת
לשלושה נתיבים**: `/api/notify` (נולד 06.08 לא-מוקשח), `api/feedback.js`, `waitlist` —
אותו פער CSP+JWT בשלושתם; שלוש הקשחות נפרדות = סחיפה.

**W — project מובייל ל-Playwright.** `playwright.config.js:26` = project **יחיד** `Desktop
Chrome`, ו-`verify` כולו Node בלי DOM. **בלעדיו שש הסתעפויות היכולת נשארות עיוורות.**

נוספים, מחוץ לרצף:
- RPC פר-מטבע `avg_pnl_by_ccy` · ספי `generateProfile` מכוילים לדולר
- `admin_rpcs.sql:267-268` — `else 0` על מכנה ריק (§2) · שמות CSV ב-AdminPanel ב-UTC ולא מקומי

---

## ⏸️ חסום / ממתין לניב

- **מענה ל-5 הפידבקים** (`omrikapara1` ×4, `a0556783290` ×1) — התבניות בריפו, השליחה בפרומפט נפרד
- אימות ידני: אונבורדינג ב-₪ בחלון אנונימי · יתר §1-§4 של M (מטבע בייבוא · זבל→דחייה · round-trip · AdminPanel)
- שני קבצי Google Sheets של `a0556783290` — IBI + אלטשולר, לתיקון הייבוא
- **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה. עם השדרוג
- PR #44 Dependabot · דוח Architecture Auditor (14 ממצאים, טרם מוינו) · מלאי סודות
- Stripe Test Mode · Google Search Console · Google Workspace · ריפו פרטי מול Actions
- QA מובייל 7 סעיפים · ערכת OCR (30 גרפים) · 390 תמונות · עו"ד על `/privacy` · רשם הדומיין (Auto-Renew)
- שדה טווח עסקה (סווינג/יומי/ארוך) — בקשת משתמש 31.07, נוגע בסכימה + מסננים
- הודעה למשתמשים על שלושת המספרים שזזו ב-T7 (02.08) · מייל ל-`saridel` (22/22 עסקאות ללא stop)
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05, הכשיל סשן ב-04.08).
  ⚠️ יושב שם `Backup-codes-niveven183.txt` — 2FA בטקסט גלוי, **לא בריפו (אומת)**.
  ניב החליט להשאיר. הסיכון הנותר: `git add -A` שירוץ מהתיקייה ההיא

---

## ✅ נסגר השבוע

- **M · פערי המטבע** §1-§6 (05.08) `2491c86` `b5d99dd` `e9de006` `e09833e` · **S1 · RLS** (`ee72492`) · **גל הסטופים בוטל** (artifact ייבוא)
- **מובייל · `capture` הוסר** (05.08) — הבורר מציע גלריה **וגם** מצלמה, אסרציה ב-`test:ocr` נועלת
- **קמפיין `why_stopped`** נשלח 04.08 ל-5 מתוך 26 סגורות. ⚠️ **מנה הבאה חייבת
  שם campaign חדש** — ה-dedup יחסום את אותו שם בשקט
- 🔴 **טוקני Supabase ל-GA4 — נסגר (06.08)** `08ca710`. `page_location` ננעל ל-`origin+pathname`.
  ≤8/41 בחלון של 10 ימים; המספר האמיתי לא ניתן למדידה (הסכמה ב-localStorage). `INCIDENTS.md#12`

---

## ⚠️ סיכונים פתוחים

- **הסתעפויות יכולת-דפדפן ללא כיסוי (05.08).** אימות ברוחב מוקטן אינו נוגע בהן
  **בהגדרה** — אותו מנוע, אותו משטח API. #1 `capture` תוקן, #2 → 🔴. נותרו:
  **#3** `ThemeContext.jsx:42` `matchMedia` **לא מוגן** (`:31` כן) → WebView ישן
  זורק ב-`useEffect` והעץ נופל · **#4** `IOSInstallBanner.jsx:9,13,20` iOS-only,
  מעולם לא הורצה · **#5** `FeedbackTab.jsx:49` `parseUA()`, תיוג OS שגוי מסווה
  באגי פלטפורמה · **#6** `SwingEdge_App.jsx:1304` חשוף בעוד `OnboardingTour:37` מגן
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.**
  **לא נטש בשקט** — דיווח באג, צירף 2 קישורי Sheets, הציע שיפור. **מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) בלי fallback ובלי עותק מקומי (500 ב-05.08). ⏭️ S2
- **Smoke נכשל על `e09833e`** — `/app` 500 כולל ה-retry, יציב 200 כעבור 3ש. ⚠️ retry אחד
  לא הספיק: או שאינו ממתין ל-deploy או שמשתמש אמיתי יפגוש 500 ב-cold start. ⏭️ W
- **שורות `waitlist` ירדו ב-2 תוך 24ש** (60, ▼2). מחיקה ידנית או ספירה שגויה בדוח Growth. **לא נבדק**
- **אין בדיקה שחוסמת נתון דמוי-טוקן באנליטיקס יוצאת** (#12 נתפס בקריאה בלבד). ⏭️ S2
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — אין `created_at` בצד השרת, הערך מגיע
  מהלקוח ובעסקה מיובאת נושא את תאריך העסקה. **40/56 שורות מוקדמות מהרשמת הבעלים** (3 משתמשים),
  ולכן הם מוצאים מכל מדידת זמן. `retention.sql` מדווח אותם בנפרד ⛔ לא ממציא להם 0
- **אין ליגר שליחה לאף מסלול egress מלבד מייל** (ציד 06.08): `api/ocr.js` מפעיל קריאה
  **בתשלום** לכל משתמש מאומת בלי רשומה של מי/כמה — העלות הכי לא-נראית · `api/health.js:160`
  היחיד עם `Allow-Origin: *`, עוקף את `_lib/cors.js` · 8 workflows מייל + 11 דיסקורד, אפס ליגר
- **`tradeImage` (data-URL) ב-localStorage בלבד** (`supabaseClient.js:65`, **בכוונה**) — לא עובר
  מכשיר, והוא הצרכן הגדול של המכסה ש-`SwingEdge_App.jsx:1665` בולע ב-`catch {}` ריק: המכסה
  מתמלאת, **כל** העסקאות מפסיקות להישמר מקומית, ואין שום סימן. ⛔ לא תוקן — החלטת ניב 06.08
- `agents/` ו-`core/` — תיקיות Python בריפו React, אפס מייבאים. שאריות Trading Room. ⏭️ R
- `HIVE_ARCHITECTURE.md` מתעד מערכת שאינה בריפו · `docs/` מערבב 8 דוחות מתוארכים עם מסמכי-קבע. ⏭️ R
- `SwingEdge_App.jsx` (7,275) ו-`src/i18n.js` (2,891) גדולים **בכוונה** — ייפוצלו בעילה. **החלטה, לא חוב**
