# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.** חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט** (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-10 · HEAD: 05b4db0 — ⚠️ מפגר בקומיט אחד **מעצם הגדרתו** (אין hash לפני commit, ו-amend אסור). `git log -1` הוא האמת
⚠️ **סחיפת תאריכים — הוכרע 06.08: `git log` הוא מקור-האמת, שמות מסמכים נגזרים ממנו.** קיימת ב-`INCIDENTS#12` וב-**3/3 מ-`docs/plans/PLAN-2026-08-06-*`** (`cleanup`·`user-visibility`·`workflows`) שנוצרו כולם **05.08 +0300**. ⛔ **לא לשנות שמות קיימים** — שובר את ההפניות. זו רשומה, לא תיקון

**מדדים חיים (נמדד 09.08):** **44 ב-`auth.users`** (לא 41) · `user_settings` **32/44** — **12/44 בלי שורת הגדרות כלל** · **59 עסקאות** (26 סגורות · 0 דמו · **0/59 שאינן USD** · 0/59 `_capitalAtEntry` ריק) · 12 עם עסקאות · 0 mentorships · **5/8 פידבקים לא-סגורים — מהם 3 ללא מענה ו-2 נענו וטרם יושמו** · 31 תקועים (22 `joined == last_seen`) · **activation 12/41 = 29%** · power users 2.

🔴 **שימור שבוע 1 = 0/6** (`scripts/retention.sql`, 06.08). מתוך 9 בני-מדידה, 6 פעילים בשבוע 0 ו-**אפס** חזרו בשבוע 1; 5/9 רשמו עסקה תוך שעה. **הבעיה אינה האונבורדינג — היא היום השני.** ⏭️ הגל הבא

🎉 **`de66dc99` — אחד מחמשת נמעני `why_stopped` — חזר אחרי 9 ימים והזין 3 עסקאות** (ZIM/PATH/ONDS, 07.08 13:45-13:49 UTC). **ההחזרה הראשונה של הקמפיין. 1/5 נמענים חזרו** (המכנה: 5 שקיבלו `why_stopped`). ⚠️ **המדד: קמפיין נמדד בכניסות חוזרות, לא בתשובות במייל. 0 תשובות ≠ 0 אפקט** — לולא נמדדו העסקאות, הקמפיין היה נרשם ככישלון

## 🔴 עכשיו

🔴 **`navigator.clipboard` בלי בדיקת קיום — כשל שקט במסלול המנטורינג (05.08).** `SwingEdge_App.jsx:2608` — `undefined` בהקשר לא-מאובטח וב-WebView אנדרואיד: הקריאה זורקת, ה-`catch` בולע, **הקוד לא הועתק והמשתמש לא יודע** (§2). **0 mentorships** — זו הסיבה, או שאיש לא ניסה? ⛔ אין דרך להבחין בלי טלמטריה

🔴 **`github-script@v9` — 0/3, טרם הורץ על runner** (הוצמד ב-`03c343e`). ריצה ראשונה צפויה `data-guardian` **07.08 05:00 UTC**, אחריה `arch-auditor` **09.08 05:00** ו-`analyst` **09.08 06:00**. שלושתם נתיבי **דיווח** — אם v9 שבר אותם הכשל שקט מהגדרתו. ⛔ לא להריץ ידנית: הרצה מחוץ להקשר אינה מוכיחה את המסלול

---

## ⏭️ הבא בתור

**הרצף הושלם:** U → R → W → S2 → ידית `/api/notify` → מחיקה מאומתת → מחלקת הכתיבה 7/7 → **מנוע ההון ✅**.

**סדר הגלים מכאן — עודכן 09.08 אחרי גל GA4:** 1. ✅ **ההון** · 2. ✅ **דיסקורד** · 3. ✅ **תמונה+clipboard+matchMedia** · 4. ✅ **GA4 — מדידת מסכים פנימיים** (זה) · 5. **קמפיינים — משוחררים** (12 שהתחילו ונטשו + `why_stopped_r2` ל-21). ⚠️ **הבלימה הוסרה אך המדידה טרם התמלאה:** GA4 עמד מעל הקמפיינים כי אי-אפשר היה למדוד מה קרה למי שכבר בא. המכשיר קיים מ-09.08 — ⛔ **הנתונים מתחילים להצטבר מהיום, אין דלתא מול העבר** (`INCIDENTS#13`). אחריהם: `send-mail` v18 · אספן CSP · 42 `catch` · fontshare

✅ **גל הדיסקורד — שני מנגנוני הדיווח שמשקרים, נסגרו 09.08.** (א) **ה-Digest מכיר את הליגר:** `classifyFeedback` מסווג דרך `parseCampaign` **המיובא** מ-`api/_lib/replyLedger.js` (פרסר שלישי היה בדיוק הסחיפה שהמודול נבנה למנוע), שני דליים מאותה אוכלוסייה — **3 ממתינים למענה · 2 נענו וטרם יושמו (מתוך 5 לא-סגורים)**; ההתראה נדלקת על הראשון בלבד. ⛔ **הסיווג אינו מושך `message`** — חשיפת ה-LLM זהה לקודם שדה-בשדה, הסניפט בשאילתה ממוקדת אחת. **9/9 `test:digest`**, נצפה נכשל · (ב) **הסורק:** 5 בדיקות תוקנו + מנגנון `ACKNOWLEDGED` (תאריך + הפניה + נימוק, **נספר בקול**, ⛔ הבדיקה אינה נמחקת). **16 → 12 ממצאים + 3 מושתקים** — **נמדד, לא הוכרז** (התוכנית ניבאה 13; ההפרש הוא בדיקת select/limit שנפתחה מחדש והייתה מחוץ ל-16). **8/8 `test:arch`**, נצפה נכשל. ⚠️ **הדפוס שאותר: תנאי-אגרגט "הכול-או-כלום" שמשתיק את עצמו** — `:241` דרש ש**כל** קובץ ב-`api/` ייכשל (12/13 התאימו ⇒ מתה, ו-`send-invites.js` לא נאמר מעולם) · `\bpolicy\b` השתיק את בדיקת ה-RLS על הערה `// Failure policy` ב-`api/fx.js:30` · `.limit(1)` בודד ביטל 21 selects. **בדיקה מתה חמורה מבדיקה שמשקרת — השקר מכריז על עצמו**
⏭️ **מיון 16 ממצאי הסורק (09.08): 3 false positives נסגרו בקוד הסורק · 3 מושתקים מתועדים · 4 אמיתיים, כאן:** `src/components/AdminPanel.jsx` (1,805) — ⚠️ **אינו** מכוסה ב-`STATE:98` שנוקב **בשניים** בשמם, ולכן נשאר **ממצא חי**; השתקה שלישית הייתה הרחבת החלטה שלא התקבלה · `nanoid` **GHSA-2v37-7h3g-55p8** (ה-1 high) — ⚠️ **אינו** ה-pin המתועד של `react-router-dom`, ולכן אינו בר-השתקה · `dist/assets/index-*.js` **1,581KB** + `recharts` **540KB** → code-splitting · `src/components/TickerLogo.jsx:17` ללא `alt`. ⚠️ **מנופחים ומועמדים, רישום בלבד:** "51 יצואים מתים" — `arch-auditor.mjs:39` `CODE_EXT` עיוור ל-`.mjs` ולכן **22 סקריפטים אינם בקורפוס** (`parseCampaign` מדווח "מת" למרות `scripts/notify-handle-test.mjs`); ⛔ **לא הורחב — לא מרחיבים קורפוס בגל ניקוי**, גל נפרד אחרי דוח נקי ראשון · "216 כפתורים ללא `aria-label`" — `:353` מסמן כל `<button>` כולל בעלי טקסט נראה, **false positive מבני** · 19 מלכודות RTL · 7 עמודות ללא אינדקס (דורש סכימה, לא grep) · 516 inline ב-JSX (`CLAUDE.md` §13 כבר מכסה)

✅ **מנוע ההון והשער — אובחן ותוקן 09.08** (`docs/audits/AUDIT-2026-08-09-capital-fx.md`, מול 11 צילומי המסך של משתמש הבדיקה ₪10,000): **5/7 תצוגה בלבד · 1 באג אמיתי · 1 מופרך · 0/7 כסף שגוי · 30/30 התמדת `riskPct`**. `equityYDomain` (`:366-377`, `pad=max(50,|hi|×0.02)`) משחזר את צירי הצילום ספרה בספרה ואינו נוגע בערך · 08.08.2026 הוא **שבת** ו-ECB מפרסם בימי עסקים בלבד — המערכת **אומרת** "משער 07.08" (`:823`) · `maxRiskPct` **נגזר** (`:1243` — נמדד 10.08, `:1232` היה שגוי — `min(5,max(3,riskPct×2))`) ולא קשיח → ⚠️ `ONBOARDING-COACH-AUDIT-2026-07-13.md:48` ("`MAX_RISK_PCT=3` hardcoded") **מיושן ומטעה**
✅ **שלושת הפצעים תוקנו — קוד בלבד, אפס נגיעה בערכי משתמשים:** (1) **ספי `generateProfile`** → `src/lib/riskProfile.js`, טבלה פר-מטבע; **הנזק היה מומש בפרודקשן** — ₪10,000 beginner נחת ב-`medium` וקיבל **1.0% במקום 0.5%, פי 2 מהמגיע** (1/2 האונבורדינגים השקליים) · (2) **בסיס עקומת ההון** → `src/lib/equityBase.js`, שער יום-הבסיס במקום spot; **הממצא החמור ביותר ולא היה ברשימת ה-7**, סתר את `src/lib/fx.js:16-17` מילה במילה. ⚠️ היה **רדום** — 0/3 משתמשי ILS עם עסקאות · (3) **`|| DEFAULT_CAPITAL`** בשני המנועים → "לא נמדד" + גידור מטבע אופציונלי. `DECISIONS` ×4 · **21/21 `test:fx`** (10 אסרציות נצפו נכשלות על הקוד הישן) · **111/111 `test:coach` לפני ואחרי**
✅ **גל התיוג — נסגר 10.08. הטקסט שהסביר את המנוע התיישר עם המנוע.** `tooltips.js:121` (**נמדד — `:123` בפרומפט ובשורה הזו היה שגוי**) קידד "(1%)…(3%)…בערך 3 עסקאות" כמספרי דוגמה, **שגוי ל-12/32** (0.5→8 · 1.5→3 · 2→1). ⛔ **פרמטריזציה נפסלה במדידה:** `TermTooltip({term,lang,label,children})` הוא חיפוש מחרוזת סטטי — השחלת `riskPct` נוגעת ב-`TermTooltip`+`InfoTooltip`+40 אתרי קריאה = **גל תשתית**. הפתרון: ניסוח גנרי שמכריז על ה**יחס** ועל **קבועי הנוסחה** (3% רצפה · 5% תקרה) ולעולם לא על דוגמה פר-משתמש, 5 שפות · **רצפת ה-3 נראית עכשיו בשני אתרים** — לוח הסיכונים (`:4247`) **וגם ההגדרות** (`:6677`), שם בוחרים `riskPct`, מותנה `riskPct×2 < 3`, **המספר נגזר מ-`maxRiskPct` ולא קשיח** · ⛔ **אפס נגיעה בנוסחה `:1243`** · ⚠️ **תלות שהתגלתה: `glossary.json` הוא עותק מיוצר של כל tooltip ו-`npm run glossary` אינו ב-`build` ואינו ב-`verify`** — עריכת `tooltips.js` לבדה משאירה את המשפט המת חי בגלוסרי בשקט. הורץ באותו קומיט (`72/72`), ואסרציה 5 שומרת עליו מעכשיו. **16/16 `test:copy` (חוליה 19), 10 נצפו נכשלות**
⚠️ **חוב שהגל הזה יצר או השאיר במודע:** `menteeStats` מקבל את בסיס ההון של **המנטור** — ימי המנטי אינם ב-`fxDayKeys`, ובסיס נגזר-מנטי היה נופל לנתיב "אין fixing" ומחזיר הון גולמי · `AdminPanel.jsx` תלוי במפתחות RPC (`with_stop` · `real_count`) ש**קיימים רק אחרי שניב מריץ** את `20260809120000_admin_rpcs_denominators.sql` — עד אז `?? total` ו-`—` מחזיקים את שתי הצורות · `profile.defaults.commission` **מספר מת** (אינו נקרא בשום מקום) · `:412` placeholder · `:533` `2:1` קשיח · ⚠️ **קו בסיס שזז ב-`test:import` §4** — שתי אסרציות פינו את **הטקסט הליטרלי** של השומר הזמני (`!agg?.total ? "—"`), והשומר ההוא נשא TODO משלו ("החזר null על מכנה ריק, ותן ספירות גולמיות") ש**הגל הזה ביצע**. הפינים הוחלפו ל-**אינווריאנט** וחזקים ממנו: השומר על ה**שיעור** ולא על `total`, ואסרציה חדשה אוסרת על הצורה הישנה לחזור. ⛔ **לא הותאמה ציפייה לקוד** — הצורה הישנה נכשלה על טבלת דמו-בלבד (`total>0`, שיעור `null`) והייתה מרנדרת `"null%"`

✅ **גל GA4 — נסגר 09.08. ⚠️ ארבע הנחות הופרכו במדידה לפני שנכתבה שורת קוד:** (א) `blockAnalytics` **אינו** מסנן חשבונות בפרודקשן — הוא `route.abort` של Playwright; השער היחיד הוא ההסכמה (`consent.js:111`), ולכן **המכנה של כל שיעור הוא אוכלוסיית המסכימים** · (ב) 122 ה-views הם **סנטינל, לא בני אדם** (`INCIDENTS#13`) — וה**ממצא האמיתי חזק יותר**: הטאבים הם `useState` ולא routes, ולכן צפיות פנימיות **מעולם לא נספרו** · (ג) **2 מ-4 האירועים כבר היו קיימים** (`first_trade_saved`·`ocr_result`) — נטו 3 + `screen_view` · (ד) **שני פגמי אמת:** `ocr_result` ירה רק בהצלחה (מונה בלי מכנה), ו-`first_trade_saved` ירה **לפני** אימות הכתיבה — ומכיוון שהסמן הוא `localStorage`, עסקה שגולגלה לאחור נספרה **לנצח**. תוקן בשתי הדלתות (`handleSubmit` **וגם** `handleImportTrades`). `screen_view` מ-**נקודה אחת** (`useEffect` על `[tab, showOnboarding]`) · 12 שמות מסך מדודים + `"other"`; `analyzer`/`position` **מוחרגים** — ערכי `tab` לטיק אחד לפני שכתוב ל-`tools` · **`ALLOWED_PARAMS` סגורה בת 6**, כולם enum/boolean. **14/14 `test:analytics`, 11 נצפו נכשלות** (חוליה 18)
⏭️ **מה שנשאר פתוח מהגל:** **אחרי שבוע נתונים — שאילתת המשפך הראשונה**, ⛔ ובמפורש עם מונה ומכנה ושם אוכלוסייה: *כמה מהסשנים **המסכימים** הגיעו ל-`first_trade_saved`* — ⚠️ לא "כמה משתמשים", ההסכמה היא המכנה · ⏭️ **`mentoring` — חי או קוד מת?** נכלל ברשימה הסגורה **כמדידה, לא כהצהרה**: `0 mentorships` ו-`navigator.clipboard` השבור באותו מסלול (🔴 למעלה) אינם ניתנים להפרדה בלי נתון. **`screen_view` הוא בדיוק מה שיכריע**, ומחיקה לפניו הייתה משמידה את הראיה · ⏭️ **בדיקת הסרת-יכולת עדיין חסרה כאן גם כן:** 8-14 הן אסרציות **סטטיות** ומוכיחות שהקריאה **נכתבה**, לא שהיא **רצה** (1-7 כן מריצות את `consent.js` מול `window`/`localStorage` מדומים)

⚠️ **שני מסלולי העלאת תמונה שולחים את הקובץ הגולמי — בלי הקטנה ובלי בדיקת גודל.** `handleImageUpload` (`SwingEdge_App.jsx:2860`) ו-`handleChartFileFallback` (`:3196`) קוראים `readAsDataURL(file)` ישירות, בעוד `grabChartFrame` (`:3078-3088`) **כן** מקטין ל-≤2000px ומקודד מחדש מול תקרת 6MB. צילום מסך מאנדרואיד מודרני עובר את התקרה בקלות → `400 image_too_large` מהספק. **נסרק בגל ה-OCR של 08.08 ולא תוקן בו במכוון** — נגיעה ב-`SwingEdge_App.jsx` הייתה מחוץ להיקף המשימה (§12). ⏭️ הפתרון הוא לחלץ את ההקטנה של `grabChartFrame` לפונקציה אחת ולקרוא לה משלושת האתרים
⚠️ **הפרומפט של Vision סותר את עצמו סביב מקור ה-entry, ו**במכוון** לא תוקן.** `api/ocr.js:226-230` אוסר על המודל לקרוא את ה-entry מסקאלת המחיר, בעוד `:236-237` **מחייב** לקרוא כך את ה-stop/target. ההתרה נבחנה 08.08 ונפסלה: היא הייתה מזרימה קריאת-סקאלה בלתי-מסומנת ל-`visionEntry` — ערוץ שכבר גובר על הערוץ הדטרמיניסטי החדש — ולכן דורשת גם דירוג פנימי בתוך `visionEntry`, כלומר שדה חדש בסכימה ותרחישים קפואים נוספים. ⛔ לא בגל אחד עם הערוץ השלישי
⏭️ **Stripe — המחירים נעולים ומוצגים כבר: `$8`/חודש · `25₪`/חודש קבוע** (הוכרע 08.08, `DECISIONS`; הלנדינג ב-`22d6623`). ⛔ `$5` ו-`$7` **בטלים** — `$7` נפסל מפורשות. ⚠️ **הסתייגות "צד אחד" חוזרת לתוקף ברגע שהסליקה עולה**: היום אין קוד Stripe בריפו כלל (9 אזכורים, כולם `.md`; `package.json` = `NONE`), ולכן אין צד שני לסתור — משעולה חשבון גובה, כל שינוי מחיר חייב לנחות בשני המקומות באותו גל

**פידבק פתוח שאינו מכוסה בשום מקום אחר (§10.2, מוין 06.08):**
- 🟡 **ה-OCR קרא את המחיר הנוכחי כמחיר הכניסה ואת ה-SL השאיר ריק** (`omrikapara1`, 31.07, אנדרואיד). **שני חצאים, ושניהם טופלו — אך ללא אימות מהמדווח:** (א) **התסמין** "המחיר החי כמחיר כניסה" נסגר 01.08 ב-F1–F4 (`AUDIT-2026-08-01-ocr-diagnosis.md`, `DECISIONS` ×3) — הוא היה **צד-לקוח**, אפקט ציטוט חי, ולא `api/ocr.js`. (ב) **איכות הקריאה** נסגרה 08.08: `computeEntry` היה נעול על אחוזים גלויים (`api/ocr.js:139-147`), ולכן כלי פוזיציה מסומן כהלכה שתוויתו בלי אחוזים ותווית ה-entry שלו אינה קריאה החזיר `entry: null` — ו-`computeLevels` איפס אחריו **גם את ה-SL**. "ה-SL לא נקרא" היה **נגזרת, לא כשל נפרד.** נוסף ערוץ שלישי ואחרון בהיררכיה (`entryFromLines`), יורה רק כששני הקודמים `null` ורק כששתי קריאות מתכנסות ב-`GEO_TOL`; אי-התכנסות = `null`. תרחישים I+J, 10/10 קפואים, 8/8 הישנים ביט-בביט. `DECISIONS` ×2, 08.08 ⏳ **מה שנשאר פתוח הוא האימות, לא התיקון:** **איזה** משלושת מסלולי ה-`null` עומרי נחת בו **אינו ניתן להכרעה** — הצהרת `console` אחת וחלון לוג ~24ש, ולכן 31.07 אינו בר-שחזור. ⛔ אין לרשום "נסגר" עד תשובת אנדרואיד ממנו · ⛔ **ולא נשלח אליו מייל שני** — "תיקנו, תבדוק" הוא תזכורת בתחפושת. **האות שממתינים לו הוא תשובתו** (`DECISIONS` 08.08) · 🆕 **09.08 — פער שלישי, צד-לקוח, נסגר:** שלושת מסלולי העלאת התמונה שלחו `File` **גולמי** ל-`/api/ocr` (תקרה 6MB) בעוד `grabChartFrame` הקטין ועבד. צילום אנדרואיד מודרני חוצה את התקרה ⇒ 400 `image_too_large` ⇒ `status:"error"` גנרי. ⚠️ **ייתכן שהתמונה שלו מעולם לא הגיעה ל-Vision**, ו-(א)+(ב) טיפלו בחצי אחר. `src/lib/imageResize.js` · `test:capability`. ⛔ **המייל היזום עדיין לא נשלח**
- הגדלת תמונת הסטאפ בלחיצה · כמה צילומים ברצף (אחרי ה-OCR) · העלאת קובץ בטופס הפידבק עצמו

**קמפיין ל-12 שרשמו עסקה ראשונה ונטשו** — "עשית עסקה ולא חזרת. מה היה חסר?" שאלה אחרת מ-`why_stopped`: 0/6 שימור שבוע 0→1 אומר שהתשובה אצלם, לא אצל התקועים. ⛔ רישום בלבד, אין תבנית · **`dawidd6/action-send-mail` v3→v18 — גל נפרד משלו** (15 גרסאות ראשיות × 9 אתרי קריאה, אחד מהם התרעת הכשל של `restore-drill`; ⛔ הוצא מ-W: "גל אחד, שינוי אחד")

נוספים, מחוץ לרצף: **W — project מובייל (Pixel 5) מכסה 1–2 מתוך 6, לא שש** (בעיקר #5 `parseUA`; אמולציה משנה UA/viewport ו**אינה מסירה יכולות**, #4 דורש WebKit/מכשיר ⏸️) · ✅ **#2 (clipboard) · #3 (`ThemeContext:42`) · #6 (`SwingEdge_App:1307`) נסגרו בקוד 09.08** — בדיקת קיום + כשל נראה (`toast`), `test:capability` 11/11 (**7/11 נצפו נכשלות לפני**). ⚠️ **בדיקת-מקור מוכיחה שההגנה נכתבה, לא שהיא עובדת** ⇒ ⏭️ בדיקות **הסרת** יכולת ב-Playwright (`addInitScript` + `delete`) **נשארות פתוחות** · ⏭️ `mq.addListener` (WebView שיש לו רק את ה-API הישן) — **לא יושם, הרחבת סקופ** · ⏭️ `handlePlaybookImageUpload:6211` דוחף data-URL **גולמי** ל-`localStorage` דרך `catch {}` ריק (`savePlaybook`) — מכסת ~5MB, צילום אנדרואיד ממלא אותה, **הסטאפ נעלם ברענון והמשתמש לא יודע**. ⛔ הוכרע 09.08: **מסלול אחר, לא בגל** · ⏭️ `grabChartFrame` מקבע **רוחב בלבד** — לכידה בפורטרט מעל 2000px גובה אינה מוקטנת (פער סמוי, ההתנהגות המקורית נשמרה במכוון) · שמות CSV ב-AdminPanel ב-UTC ולא מקומי

**⏭️ נפתח או תוקן-ייחוס באבחון 09.08 (`AUDIT-2026-08-09-capital-fx.md`):**
- ⏭️ **`avg_pnl_by_ccy` — לא נבנה, אבל מה שמוצג במקומו הגון.** `admin_rpcs.sql:242-252` עושה `avg(pnl)` **בלי `group by currency`**, ⚠️ **אך הסקלר הזה אינו נקרא בלקוח כלל** — `AdminPanel.jsx:993` מקודד `value="—"` עם `sub="Mixed USD+ILS — no unit"`. סירוב להציג מספר חסר-יחידה, לא כשל שקט. ל-AdminPanel אין באנר ערבוב כי `admin_trades_list` לא מחזיר `currency` (`:287-297`)
- ⏸️ **`admin_rpcs` — המכנה תוקן בקובץ, ⛔ טרם הורץ.** שלושה מופעים, אותה מחלקה: `else 0` באוכלוסייה ריקה (`:267-268`) + **מופע שלישי שלא היה ברשימה, `:118-120` `avg_trades_user`** — ⚠️ והקובץ **סתר את עצמו 18 שורות קודם** (`:249` עשה נכון עם `nullif`). ובנוסף: המכנה **הכיל עסקאות דמו** בעוד המונה נספר עליהן — השיעור מדד את ה-seed. `supabase/migrations/20260809120000_admin_rpcs_denominators.sql`, **ניב מריץ** (§12). ⛔ **תיקון ייחוס שנסגר:** `test:settings` **אינו** מדפיס את הערת ערבוב המטבעות — היא ב-`scripts/import-test.mjs:924`
- ⚠️ **`toDisp` (`:1942-1946`) הוא ממיר חי שנראה סטטי** — `useCallback` תלוי-`fxTable`, נקרא בזמן רינדור. צורכים: `:4105` · `:4120` · `:4150` · `:4796`

**--- הוכרע (ניב, 09.08): עסקה פתוחה ותיקה היא מקור תובנה, לא בעיה --- ⏭️ גל מוצר, המועמד הראשון אחרי גל התיוג**
- ⏭️ **תובנות ביניים (לא-ממומשות)** מעסקה פתוחה ותיקה — **מסומנות ככאלה ומתוארכות**; הסגירה **מחליפה** אותן בממומשות. ⛔ **אפס ערבוב — עסקה אחת = תובנה חיה אחת.** Coach ו-DNA נשארים ממומש-בלבד כברירת מחדל
- ⏭️ **תזכורת סגירה נגזרת מ-`strategy` שכבר נשמר באונבורדינג** (נמדד 09.08: **אפס שאלות חדשות נדרשות**): day→3 ימים · swing→21 · combined/searching→45. **נקודה כתומה ביומן + tooltip.** ⛔ לא התראה, לא מייל
- ⏭️ **`horizon` אופציונלי פר-עסקה גובר על הנגזר** — **זו בקשת `omrikapara1` מ-31.07 ("שדה טווח עסקה") עצמה, אותו פיצ'ר, נסגרת איתו** (§10.2) · ⏭️ **`saridel` נבלע:** 22/22 ללא stop נשפטות מול הפרופיל **המוצהר** שלו ולא מול נורמה כללית. ⛔ אין לכתוב לו לפני שהמנגנון קיים — המייל היה מניח את התשובה

---

## ⏸️ חסום / ממתין לניב

- ⏳ **ממתינים לאות, לא למשימה:** תשובת עומרי על `fix_mobile_upload` — **אימות אנדרואיד הוא הדבר היחיד שיסגור את באג ה-OCR** — שני חצאיו תוקנו (01.08 + 08.08), ואף אחד מהם לא אומת מולו (ראה 🟡 בפידבק למעלה) · המשך פעילות `de66dc99` — **⏳ ביקור, טרם שימור: 3 עסקאות ביום אחד ואפס מאז, ולכן המדד נשאר 0/6.** ⚠️ שימור הוא חזרה **ביום אחר**, וזה בדיוק מה ש-0/6 שבוע 0→1 קיים כדי לתפוס. ⛔ אין לרשום 1/5 כשימור. ⚠️ **סתירת תאריך שלא הוכרעה:** הפרומפט של 08.08 אמר "3 עסקאות ב-06.08", והשורה הנמדדת למעלה אומרת **07.08 13:45-13:49 UTC**. ⛔ לא נמדד מחדש בגל הזה — לא לצטט אף אחד מהם כעובדה עד מדידה
- **הרצת `why_stopped_r2`** — `campaign=why_stopped_r2` · `prior_campaigns=why_stopped` · `audience=stuck_users`. ⛔ לא הורץ
- אימות ידני: אונבורדינג ב-₪ בחלון אנונימי · יתר §1-§4 של M (מטבע בייבוא · זבל→דחייה · round-trip · AdminPanel)
- 2 קבצי Sheets של `a0556783290` (IBI + אלטשולר) — חומר הגלם לתיקון הייבוא, טרם נוצל · **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה. עם השדרוג
- PR #44 Dependabot **+ ענף Dependabot נוסף שנפתח מאז** — שניהם ממתינים להכרעה יחד · ✅ **Architecture Auditor — מוין ותוקן 09.08, ראה ⏭️ למעלה.** ⚠️ **תיקון ייחוס:** השורה הזו טענה ש"אין rate-limit" יורה על דפוס מיושן — **שגוי בשני חלקיו**: הבדיקה ההיא (`:241`) **מתה** ולא ירתה מעולם; מה שנצפה בפועל הוא בדיקה **אחרת** (`:408`, "endpoint ציבורי ללא אימות") שסימנה את `api/fx.js` ו-`api/symbol-search.js` — **שניהם קוראים `rateLimit()`** (`:136` · `:32`). `dangerouslySetInnerHTML` (0 מופעים) וסודות (0 התאמות) היו שקטים **בצדק**; RLS ו-`select`/`limit` היו שקטים **מהסיבה הלא-נכונה** · **`docs/TASKS-REGISTRY.md` טרם הורד לעץ** (הפיל את שלב 5 של R; ⛔ Claude לא יוצר אותו)
- Stripe Test Mode · Google Search Console · Google Workspace · QA מובייל 7 סעיפים · ערכת OCR (30 גרפים) · 390 תמונות · עו"ד על `/privacy` · **מלאי סודות** — לא בוער, אין תסמין; ⚠️ הערך מופיע **ביום שמשהו נשבר** ואין רשימה של מה מזין את מה
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05, הכשיל סשן 04.08). ⚠️ **לפני מחיקה — להעביר חומרי שיווק שאינם ב-`~/swingedge-creatives`.** `Backup-codes-niveven183.txt` נשאר (2FA, **לא בריפו — אומת**). הסיכון: `git add -A` משם

---

## ✅ נסגר השבוע

- **M · S1 · R · #12 · W · S2** — כולם סגורים, הפירוט המלא ב-`docs/audits/STATE-ARCHIVE-2026-08.md`. נותר חי מהם: `github-script@v9` **0/3** → 🔴 למעלה · ⏭️ `download-artifact@v7→v8` · ⚠️ שורות פידבק **קיימות** נשארו caller-asserted (S2) — המיסוך ואישור האדמין בעין הם ההגנה
- **ידית `/api/notify` · §3 הפאנל הציג מצב שאינו קיים (06.08)** — הפירוט נגזם לארכיון · **✅ תבניות המענה (7 סוגים) · `dry_run` שחלק מפתח rate-limit עם השליחה · בדיקת GA4 ל-7 ימים — שלושתם נסגרו 08.08, הפירוט נגזם לארכיון.** מה-GA4 נשאר חי מספר אחד: **15 משתמשים פעילים ב-7 ימים** (13 ישראל, 2 ארה"ב), 2–7 ביום — תואם ל-41 הנרשמים, כלומר תיקון `blockAnalytics` (05.08) עבד ו**אין זיהום שיורי**
- **✅ מחלקת הכתיבה השקטה נסגרה 7/7 + 3/3 אתרי ה-RPC בפאנל (08.08)** — הפירוט נגזם לארכיון ול-`INCIDENTS#14`. **מה שנשאר חי הוא הכלל: PostgREST מחזיר `error: null` על כתיבה שהתאימה אפס שורות**, ולכן רק ספירת שורות מפרידה כתיבה שקרתה מ-no-op — `src/lib/tradeWrite.js`, `classifyRows` אחת טהורה (מנרמלת גם **מונה סקלרי** מ-RPC). ⚠️ **rollback השמירה הפוך מזה של המחיקה** · **חלקי הוא מצב שלישי** שהקוד הישן לא יכול היה לייצג. **קווי בסיס אדומים בני-שחזור בכל רגע** — `7/20`→`20/20` ו-`9/25`→`25/25` דרך `TRADEWRITE_IMPL=... node scripts/tradeWrite-test.mjs` מול `scripts/fixtures/tradeWrite-legacy.mjs`, לא מספר ביומן. `test:write` בשרשרת החוסמת

---

## ⚠️ סיכונים פתוחים

- **`react-router` GHSA-qwww-vcr4-c8h2 (high) נשאר פתוח — סיכון מקובל, לא באג.** התיקון היחיד הוא downgrade ל-7.11.0. הפגיעות היא CSRF ב-**RSC Mode** ולאפליקציה אין RSC. נאכף כהערה ב-`package.json` ליד `dependencies`. ⛔ **מתבטל ברגע שנאמץ RSC/server actions.** `DECISIONS.md` 27.07 + 06.08
- **הסתעפויות יכולת-דפדפן ללא כיסוי (05.08).** אימות ברוחב מוקטן אינו נוגע בהן **בהגדרה** — אותו מנוע, אותו משטח API. #1 `capture` תוקן · #2 → 🔴 למעלה · **#3** `ThemeContext.jsx:42` `matchMedia` לא מוגן (`:31` כן) → WebView ישן זורק ב-`useEffect` והעץ נופל · **#4** `IOSInstallBanner.jsx:9,13,20` iOS-only, מעולם לא הורצה · **#5** `FeedbackTab.jsx:49` `parseUA()` (מכוסה) · **#6** `:1304` חשוף מול `OnboardingTour:37`
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.** **לא נטש בשקט** — דיווח, צירף Sheets, הציע שיפור. **מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) — ⚠️ **"בלי fallback" הופרך:** שרשרת `'General Sans','Inter','system-ui'` קיימת ב-`tailwind.config.js:31`, `tokens.css:68-69`, `index.html:155`, ו-Inter נטען ממקור אחר. ה-500 נחת על Inter — **קוסמטי, לא שובר.** הנזק האמיתי: `<link>` **חוסם-רינדור**, נמדד 06.08 **1.46ש מול 0.25ש** ל-Google Fonts — **חמור מה-500 שהוביל לכאן** ⏭️ גל נפרד. ⏸️ **חסום על ניב** — אירוח עצמי *וגם* הצמדת CDN מותנים בתנאי רישיון ש**לא ניתן היה לקרוא** (עמוד הרישיון JS-rendered, ה-CSS בלי כותרת רישיון). ⛔ לא שונה דבר. חלופה ללא רישיון: לוותר על General Sans לטובת Inter (מסיר 2 מארחים מה-CSP) — **החלטת עיצוב, לא שלי**
- 🔴 **ה-CSP הוא Report-Only בלי אספן — כותרת שנראית כמו הגנה ואינה מייצרת אות** (`vercel.json:18`, נמדד 06.08: `report-uri|report-to` = **0**). אף דוח לא נאסף **מעולם**, ולכן "אמת שהדוח נקי" אינו ניתן למענה — לא לפני U ולא אחריו. ⛔ אין לאכוף לפני אספן + חלון תנועה נקי, ⏭️ **גל נפרד** — ⛔ ובתנאי שההפרות **לא** ינחתו ב-artifacts של Actions: הריפו ציבורי והן מכילות URLים של משתמשים. ⚠️ **`api/notify.js` אינו זקוק לרשומת CSP** — serverless, קריאותיו server-to-server, מחוץ לתחום השיפוט של CSP (טעות קטגוריה שנסגרה)
- ⚠️ **נתון דמוי-טוקן באנליטיקס — נחסם חלקית 09.08, והפער שנשאר מדויק:** `test:analytics#13` אוכפת **רשימת פרמטרים סגורה** בת 6 מפתחות (כולם enum/boolean) על כל ליטרל `trackEvent`, ו-`trackPageView` בונה `page_location` מ-origin + נתיב מורשה ולעולם לא מ-`href` (#12). ⛔ **מה שאינו מכוסה: ערך שאינו ליטרל** — spread או משתנה יחמקו מהרג'קס · ⛔ ואין אסרציה ש**ריצת CI שולחת אפס בקשות אנליטיקס** — השומר של #13 מוכח חד-פעמית, לא חוזרת · **`waitlist` — ▼2 תוך 24ש (60) ואז ▼1 נוסף (61)** — מחיקה ידנית או ספירה שגויה ב-Growth. ⛔ **לא נבדק, אף פעם.** ⚠️ Turnstile ל-`waitlist` **הוכרע ⛔ לא מיישמים** (08.08, `DECISIONS`) — התנאי: ייבנה אם וכאשר `waitlist` הופך יעד ספאם **בפועל**
- 🔴 **כל מדד GA4 מלפני 05.08 בלתי-שמיש** (#13): ≈906 page_view סינתטיים (smoke 108×5 · sentinel 122×3) מול 41 נרשמו / 12 פעילים, **בלי סמן שמפריד אותם בדיעבד**. ⚠️ **תאריך התיקון = קו הבסיס, אין להשוות לרוחבו.** ⏸️ סימון/ניקוי ב-GA4: על ניב
- ⚠️ **`Failure Alert` הוא `workflow_run` ואין לו צופה** — נעדר מטבלת ה-max-age של Watchdog **ומרשימת 11 ה-workflows של עצמו**. אם ייפול, **11 workflows מאבדים התרעה בשקט**. ⛔ לא תוקן ב-W
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — אין `created_at` בצד השרת; הערך מגיע מהלקוח ובעסקה מיובאת נושא את תאריך העסקה. **40/56 שורות מוקדמות מהרשמת הבעלים** (3 משתמשים) ולכן מוצאים מכל מדידת זמן. `retention.sql` מדווח בנפרד ⛔ לא ממציא 0
- **ציד ה-egress — מוין 06.08, שלוש מהטענות תוקנו במדידה:** `api/ocr.js` **יש** לו JWT (`:323`) ותקרה 10/דק+60/שע (`:330`); חסרה **מדידת עלות** (⏭️ דורשת טבלה, כלומר מיגרציה), והחור האמיתי הוא ש-`rateLimit` הוא **in-memory per-Lambda** — התקרה **רכה תחת concurrency**, וזה שונה מ"אין rate limit" ⏭️ · `api/health.js:160` `Allow-Origin: *` **מכוון ומתועד** (`:156-159`, UptimeRobot) והגוף מחזיר latency ms + שמות שירות בלבד → **סיכון מקובל** · **"8 מייל + 11 דיסקורד, אפס ליגר" שגוי**: נמדד **9/9** שולחי מייל עם `to:` קשיח → `niveven183@gmail.com`, **14** דיסקורד, והשולח היחיד למשתמשים אמיתיים (`email-campaign.yml`) **יש לו ליגר** → **סיכון מקובל** · `scripts/analyst|arch-auditor|data-guardian` שולחים אגרגטים/`file:line`/UUIDs — **לא PII** → מקובל, ⚠️ הסיכון הוא **סחיפה**: הריפו ציבורי ולוגי Actions גלויים
- **`tradeImage` (data-URL) ב-localStorage בלבד** (`supabaseClient.js:65`, **בכוונה**) — לא עובר מכשיר, והוא צרכן המכסה. ✅ **הכשל השקט ב-`:1665`/`:1670` נסגר (S2)** — `console.error` שאומר שכל השמירה המקומית מתה. ⚠️ **רישום בלבד: המשתמש עדיין לא רואה כלום ב-UI** ⏭️
- ⚠️ **ההנחות שהופרכו במדידה (08.08) — רשומה, לא משימה. 📌 הכלל: מספר או מיקום שנרשם פעם ולא נמדד שוב אינו עובדה** (`DECISIONS` 08.08): (א) `AdminPanel:1236` נרשם כזורק מונה — **בנוי נכון**, האתרים האמיתיים `:960 :1261 :1525` · (ב) הנחת ה-chunking בייבוא — **0 מקורות בכל הריפו**, נולדה בפרומפט · (ג) `tradeImagePreview` הוא **data-URL, לא objectURL** · (ד) זיהום Sentinel ב-`trades` — `SNTNL` = **0 שורות עומדות** (`restCleanup` ב-`test.afterAll` רץ גם בקריסה); 3 השורות שיוחסו לו היו `ZIM/PATH/ONDS` של `de66dc99`, **משתמש אמיתי שחזר** — ייחוס בלי מדידה כמעט מחק החזרה אמיתית מהספירה
- ⚠️ **דפוס: עסקאות שלא נסגרות — 33 מתוך 59 פתוחות.** `de66dc99`: **4/4 פתוחות**, כולל `TTWO` מ-28.07 (11 ימים). ⚠️ **הערך של היומן — R, win rate, Coach, Trading DNA — נולד רק בסגירה**, ולכן משתמש שרושם כניסות ולא סוגר אינו מקבל שום תובנה ואז נעלם. זו השערה למנגנון של 0/6 שימור, לא רק תסמין. ⏸️ שאלה מוצרית פתוחה לניב (ראה ⏸️)
- ⚠️ **מהשלושה נשאר אחד: 42 `catch {}` ריקים** ב-`SwingEdge_App.jsx` + אחד ב-`AdminPanel.jsx:50` (`writeJSON`) — ⛔ אף אחד לא נבדק, **הספירה אינה הערכת חומרה**. ✅ **השניים האחרים נסגרו 10.08 בגל התיוג:** הניגודיות **חושבה ולא הוערכה בעין** — **ארבע המשפחות נכשלו במצב בהיר, לא רק הירוקה** (`:root` ב-`tokens.css:9` הוא **בהיר**, `--bg-elevated: #FFFFFF`): amber 1.54 · cyan 1.65 · emerald 1.75 · violet 2.41 מול רצפת 4.5. `-600` נבחן ונפסל **במספרים** (2.95–3.43), `-700` עבר (4.65–6.29) והכהה **מעולם לא היה השבור** (6.13–9.48) ולכן נשמר · **מתג השפה** — `AuthScreen.jsx:46` ו-`ToastProvider.jsx:58` קוראים מהמסמך ורק `SwingEdge_App.jsx:1810` (**נמדד — `:1781-1782` היה שגוי**) כתב; `LandingPage` כותב עכשיו `lang`+`dir` ב-`useEffect` על `[lang, dir]` — ⛔ **לא בתוך `chooseLang`**, אחרת הטעינה הראשונה נשארת שבורה
- `SwingEdge_App.jsx` (**7,504**, נמדד 09.08) ו-`src/i18n.js` (**2,894**) גדולים **בכוונה** — ייפוצלו בעילה. **החלטה, לא חוב.** ⚠️ **שניים בשמם, לא שלושה** — זו ההפניה של `ACKNOWLEDGED` ב-`arch-auditor.mjs`; הוספת קובץ כאן משתיקה ממצא
