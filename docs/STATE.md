# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.** חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט** (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-07 · HEAD: fadd61d — ⚠️ מפגר בקומיט אחד **מעצם הגדרתו** (אין hash לפני commit, ו-amend אסור). `git log -1` הוא האמת
⚠️ **סחיפת תאריכים — הוכרע 06.08: `git log` הוא מקור-האמת, שמות מסמכים נגזרים ממנו.** קיימת ב-`INCIDENTS#12` וב-**3/3 מ-`docs/plans/PLAN-2026-08-06-*`** (`cleanup`·`user-visibility`·`workflows`) שנוצרו כולם **05.08 +0300**. ⛔ **לא לשנות שמות קיימים** — שובר את ההפניות. זו רשומה, לא תיקון

**מדדים חיים (07.08, נמדד):** 41 נרשמו · **59 עסקאות** (▲3 ב-6ש — כולן של `de66dc99`) · 12 עם עסקאות · 26 סגורות · 0 שאינן USD · 0 mentorships · 5 פידבקים פתוחים · 31 תקועים (22 `joined == last_seen`) · **activation 12/41 = 29%** · power users 2.

🔴 **`dry_run` צורך את תקציב השליחה.** `/api/notify` מוגבל 3/10min, והיבש הוא POST **שחולק מפתח עם השליחה** — שלוש הרצות יבשות נועלות את השליחה ב-429. **קרה בפועל 07.08 15:08:** המייל לעומרי נחסם וניב המתין לחלון. התיקון: **מפתח נפרד `notify_dry` עם מכסה נדיבה.** ⚠️ **שער חובה שחוסם את עצמו אינו שער.** ⛔ לא בגל הזה — המסלול חי

🔴 **שימור שבוע 1 = 0/6** (`scripts/retention.sql`, 06.08). מתוך 9 בני-מדידה, 6 פעילים בשבוע 0 ו-**אפס** חזרו בשבוע 1; 5/9 רשמו עסקה תוך שעה. **הבעיה אינה האונבורדינג — היא היום השני.** ⏭️ הגל הבא

🎉 **`de66dc99` — אחד מחמשת נמעני `why_stopped` — חזר אחרי 9 ימים והזין 3 עסקאות** (ZIM/PATH/ONDS, 07.08 13:45-13:49 UTC). **ההחזרה הראשונה של הקמפיין. 1/5 נמענים חזרו** (המכנה: 5 שקיבלו `why_stopped`). ⚠️ **המדד: קמפיין נמדד בכניסות חוזרות, לא בתשובות במייל. 0 תשובות ≠ 0 אפקט** — לולא נמדדו העסקאות, הקמפיין היה נרשם ככישלון

## 🔴 עכשיו

🔴 **`navigator.clipboard` בלי בדיקת קיום — כשל שקט במסלול המנטורינג (05.08).**
`SwingEdge_App.jsx:2608` — `undefined` בהקשר לא-מאובטח וב-WebView אנדרואיד: הקריאה זורקת, ה-`catch` בולע, **הקוד לא הועתק והמשתמש לא יודע** (§2). **0 mentorships** — זו הסיבה, או שאיש לא ניסה? ⛔ אין דרך להבחין בלי טלמטריה

🔴 **שני מטבעות בסקשן תמחור אחד** — `LandingPage.jsx:69` `freePrice: "0₪"` ליד `:73` `proPrice: "$5"` (נמדד 06.08). אותה מחלקת כשל של T9/T10, אבל **זה הדבר הראשון שמבקר רואה**. ⛔ לא בגל הזה — התיקון תלוי בהכרעת המחיר ב-₪ (⏸️)

🔴 **`github-script@v9` — 0/3, טרם הורץ על runner** (הוצמד ב-`03c343e`). ריצה ראשונה צפויה `data-guardian` **07.08 05:00 UTC**, אחריה `arch-auditor` **09.08 05:00** ו-`analyst` **09.08 06:00**. שלושתם נתיבי **דיווח** — אם v9 שבר אותם הכשל שקט מהגדרתו. ⛔ לא להריץ ידנית: הרצה מחוץ להקשר אינה מוכיחה את המסלול

---

## ⏭️ הבא בתור

**הרצף הושלם:** U → R → W → **S2 ✅** → **ידית `/api/notify` ✅** → **מחיקה מאומתת ✅** → עצירה.

**סדר העדיפויות מכאן — נקבע 07.08, לפי הסדר:** 1. **`notify_dry`** (משחרר את מסלול המענה, 🔴 למעלה) · 2. **הלנדינג ₪0 מול $5** (הדבר הראשון שמבקר רואה, 🔴 למעלה) · 3. **תבניות המענה — 7 סוגים** (למטה) · 4. **קמפיין ל-12 שהתחילו ונטשו** + `why_stopped_r2` ל-21 · 5. **`send-mail` v18 · אספן CSP · 42 `catch`**

⏭️ **המחלקה שנשארה מ-#14 — 6 אתרים, `SwingEdge_App.jsx`.** אותה צורה: אופטימי → שגיאה ללוג בלבד → `toast.success` ללא תנאי. **⚠️ ראשון בתור: `handleSubmit` (`:2416`) — ה-INSERT ה-fire-and-forget הוא *השורש של המרוץ*, לא עוד אתר ברשימה. תיקון המחיקה בלי תיקון השמירה משאיר את המחלקה חיה** (`pendingWrites` עוקף את המרוץ, לא מבטל אותו). אחריו: `handleBulkDelete` (`:2489`) · `handleEditSubmit` (`:2525`) · סגירה (`:2444`) · `handleImportTrades` (`:2557`) · `handleUndoImport` (`:2573`)
⏭️ **`AdminPanel.jsx:1236` זורק את ערך ההחזרה של `admin_delete_feedback`** — שהוא **מונה השורות**. הצורה שם נכונה (server-first + `toast.error` נראה), אבל מחיקת אפס שורות עדיין תדווח כהצלחה. ⛔ אפס נגיעה בגל הזה
⏭️ **עיצוב מחדש של תבניות המענה — 7 סוגים.** שתי תבניות קפואות לשבעה סוגי אירועים, **ולכן כל מכתב מעורפל ונשמע תבנתי**. העיצוב: תבנית = **שלד** (פתיחה · מה מצאנו · מה עשינו · הבקשה · חתימה), האדמין ממלא את הליבה. הסוגים: באג תוקן · באג בטיפול · פיצ'ר התקבל · פיצ'ר נדחה · צריך מידע · חומר התקבל · תודה.
**⚠️ שלוש תלויות שהעיצוב חייב לפתור, אחרת הוא נשבר בשקט:** `reply:<id>` נועל תשובה אחת לנצח → **`reply:<id>:<n>`** · היבש מאמת bytes מול קובץ קפוא → **עם תוכן חופשי חייב להציג גוף מלא** · תוכן אדמין נכנס ל-HTML → **`escapeHtml` חובה**
⏭️ **אספן CSP — גל נפרד.** בלי `report-uri`/`report-to` אין מה לאכוף ואין מה לאמת (ראה ⚠️). ⛔ תנאי: ההפרות **לא** ינחתו ב-artifacts של Actions — הריפו ציבורי והן מכילות URLים של משתמשים.
⏭️ **Turnstile ל-`waitlist`** — הטופס הציבורי היחיד שנותר חשוף לבוטים (insert ישיר מהדפדפן, `LandingPage.jsx:538`). התבנית כבר בריפו (`api/verify-turnstile.js`). ⛔ **לא** לפידבק: משתמש מתוסכל לא יעבור CAPTCHA
⏭️ **מדידת עלות ל-`api/ocr.js`** — ראה ⚠️; דורש טבלה, כלומר מיגרציה
⏭️ **Pro $5 → $7 — אושר.** מבוצע בגל Stripe, לא לפניו: המחיר חי בקוד הנחיתה **ובחשבון הסליקה**, ושינוי צד אחד בלבד הוא הסתירה שכבר יש לנו

**W — project מובייל (Pixel 5): מכסה 1–2 מתוך 6, לא שש** — בעיקר #5 `parseUA`. אמולציה משנה UA/viewport ו**אינה מסירה יכולות**: #2/#3 → השורה ב-⏭️ למטה; #4 דורש WebKit/מכשיר (⏸️), Chromium לא מאמלץ iOS.

**פידבק פתוח שאינו מכוסה בשום מקום אחר (§10.2, מוין 06.08):**
- 🔴 **ה-OCR קרא את המחיר הנוכחי כמחיר הכניסה ואת ה-SL השאיר ריק** (`omrikapara1`, 31.07, אנדרואיד). זה **הבאג שהוא דיווח** ו-**הוא לא תוקן** — `capture` היה בורר התמונה בלבד. `emails/fix_mobile_upload.html` אומר זאת במפורש ⛔ לא "טופל"
- הגדלת תמונת הסטאפ בלחיצה · כמה צילומים ברצף (אחרי ה-OCR) · העלאת קובץ בטופס הפידבק עצמו

**קמפיין ל-12 שרשמו עסקה ראשונה ונטשו** — "עשית עסקה ולא חזרת. מה היה חסר?" שאלה אחרת מ-`why_stopped`: 0/6 שימור שבוע 0→1 אומר שהתשובה אצלם, לא אצל התקועים. ⛔ רישום בלבד, אין תבנית

**`dawidd6/action-send-mail` v3→v18 — גל נפרד משלו.** 15 גרסאות ראשיות × 9 אתרי קריאה, אחד מהם התרעת הכשל של `restore-drill`. ⛔ הוצא מ-W במפורש: "גל אחד, שינוי אחד"

נוספים, מחוץ לרצף: ⏭️ בדיקות **הסרת** יכולת ל-#2/#3 (`addInitScript` + `delete`) — אמולציה לא מגיעה לשם · RPC פר-מטבע `avg_pnl_by_ccy` · ספי `generateProfile` מכוילים לדולר · `admin_rpcs.sql:267-268` `else 0` על מכנה ריק (§2) · שמות CSV ב-AdminPanel ב-UTC ולא מקומי

---

## ⏸️ חסום / ממתין לניב

- ✅ **הידית עבדה בפרודקשן (07.08).** `reply:eebd6bb1` נשלח **12:06 UTC** ל-`a0556783290` ("הקבצים שלך התקבלו") — **השליחה הראשונה דרך הפאנל**, לא דרך DevTools
- ⏳ **המייל לעומרי** (`fix_mobile_upload`, פידבק 12:22) — ממתין לחלון rate-limit (ראה 🔴 `notify_dry`). **על ניב: יבש פעם אחת → שלח.** ⛔ ניב מריץ יבש, ניב לוחץ שלח
- **הרצת `why_stopped_r2`** — `campaign=why_stopped_r2` · `prior_campaigns=why_stopped` · `audience=stuck_users`. ⛔ לא הורץ
- אימות ידני: אונבורדינג ב-₪ בחלון אנונימי · יתר §1-§4 של M (מטבע בייבוא · זבל→דחייה · round-trip · AdminPanel)
- 2 קבצי Sheets של `a0556783290` (IBI + אלטשולר) — חומר הגלם לתיקון הייבוא, טרם נוצל · **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה. עם השדרוג
- PR #44 Dependabot **+ ענף Dependabot נוסף שנפתח מאז** — שניהם ממתינים להכרעה יחד · דוח Architecture Auditor (14 ממצאים, טרם מוינו) · **`docs/TASKS-REGISTRY.md` טרם הורד לעץ** (הפיל את שלב 5 של R; ⛔ Claude לא יוצר אותו)
- **מלאי סודות** — לא בוער, אין תסמין. ⚠️ הערך מופיע **ביום שמשהו נשבר** ואין רשימה של מה מזין את מה · **רשם הדומיין — Auto-Renew *וגם* פרטי קשר.** פתוח מ-`INCIDENTS` #4/#5, ~5 דקות. ⛔ פג תוקף = האתר יורד, לא מתדרדר
- **בדיקת GA4 — 7 הימים האחרונים.** קו הבסיס נפתח בתיקון `blockAnalytics` (05.08); מספרים **גבוהים מדי** יאמרו שזיהום ה-Sentinel היה אמיתי ולא נסגר. ⛔ לא להשוות לרוחב התאריך · **מחיר ₪ קבוע — טרם הוכרע.** ⛔ **אין לרשום כאן מספר.** מספר שלא הוכרע ונרשם נקרא כהחלטה בעוד חודש
- Stripe Test Mode · Google Search Console · Google Workspace · QA מובייל 7 סעיפים · ערכת OCR (30 גרפים) · 390 תמונות · עו"ד על `/privacy`
- ⚠️ **שדה טווח עסקה** — בקשת `omrikapara1` 31.07, נוגע בסכימה+מסננים. **המייל שאנחנו שולחים לו אומר "נכנס לתוכנית"** — אם הוא יענה ויבדוק, זה מה שהוא יחפש
- ⚠️ **`saridel` (22/22 ללא stop) — שאלת מוצר, לא משימה.** האם זו התנהגות לתקן או סגנון מסחר לגיטימי? ⛔ להכריע **לפני** שכותבים לו, אחרת המייל מניח תשובה
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05, הכשיל סשן 04.08). ⚠️ **לפני מחיקה — להעביר חומרי שיווק שאינם ב-`~/swingedge-creatives`.** `Backup-codes-niveven183.txt` נשאר (2FA, **לא בריפו — אומת**). הסיכון: `git add -A` משם

---

## ✅ נסגר השבוע

- **M · S1 · R · #12 · W · S2** — כולם סגורים, הפירוט המלא ב-`docs/audits/STATE-ARCHIVE-2026-08.md`. נותר חי מהם: `github-script@v9` **0/3** → 🔴 למעלה · ⏭️ `download-artifact@v7→v8` · ⚠️ שורות פידבק **קיימות** נשארו caller-asserted (S2) — המיסוך ואישור האדמין בעין הם ההגנה
- **ידית `/api/notify` · §3 הפאנל הציג מצב שאינו קיים (06.08)** — הפירוט נגזם לארכיון. נותר חי: ⚠️ מפתח limiter נפרד ל-GET (ו-⛔ **`dry_run` עדיין חולק מפתח עם השליחה** → 🔴 למעלה)
- **🔴 #14 — מחיקה שדיווחה הצלחה והשאירה את השורה ב-DB (07.08).** לא RLS, לא רשת, לא hydration: **מרוץ** — ה-INSERT ב-`handleSubmit` נורה בלי `await`, ולכן DELETE יצא על `id` שטרם נכתב, התאים **אפס שורות**, וה-INSERT נחת אחריו. ⚠️ **מה שהפך אותו לשקט: PostgREST מחזיר `error: null` על DELETE שהתאים אפס שורות** — ולכן **שום** בדיקת שגיאה לא יכולה לתפוס את זה, רק ספירת שורות. **שלילת ה-RLS: `restCleanup` מוחק את אותן שורות עם אותו משתמש/מפתח/מדיניות ומצליח — כלומר עצם קיום הממצא מוכיח ש-RLS תקין.** תוקן ב-`src/lib/tradeDelete.js` (טהור, בר-אסרציה): `.select("id")` + `ok` רק על שורה אחת · `pendingWrites` שמשחרר על **settle ולא resolve** · rollback לאינדקס המקורי + שגיאה כלשונה. **הוכחה: `test:delete` נצפה נכשל 0/7 ואז 7/7**, והלוגיקה הישנה שוחזרה מול `{data:null,error:null}` והראתה "העסקה נמחקה" עם אפס לוג. `INCIDENTS#14`

---

## ⚠️ סיכונים פתוחים

- **`react-router` GHSA-qwww-vcr4-c8h2 (high) נשאר פתוח — סיכון מקובל, לא באג.** התיקון היחיד הוא downgrade ל-7.11.0. הפגיעות היא CSRF ב-**RSC Mode** ולאפליקציה אין RSC. נאכף כהערה ב-`package.json` ליד `dependencies`. ⛔ **מתבטל ברגע שנאמץ RSC/server actions.** `DECISIONS.md` 27.07 + 06.08
- **הסתעפויות יכולת-דפדפן ללא כיסוי (05.08).** אימות ברוחב מוקטן אינו נוגע בהן **בהגדרה** — אותו מנוע, אותו משטח API. #1 `capture` תוקן · #2 → 🔴 למעלה · **#3** `ThemeContext.jsx:42` `matchMedia` לא מוגן (`:31` כן) → WebView ישן זורק ב-`useEffect` והעץ נופל · **#4** `IOSInstallBanner.jsx:9,13,20` iOS-only, מעולם לא הורצה · **#5** `FeedbackTab.jsx:49` `parseUA()` (מכוסה) · **#6** `:1304` חשוף מול `OnboardingTour:37`
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.** **לא נטש בשקט** — דיווח, צירף Sheets, הציע שיפור. **מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) — ⚠️ **"בלי fallback" הופרך:** שרשרת `'General Sans','Inter','system-ui'` קיימת ב-`tailwind.config.js:31`, `tokens.css:68-69`, `index.html:155`, ו-Inter נטען ממקור אחר. ה-500 נחת על Inter — **קוסמטי, לא שובר.** הנזק האמיתי: `<link>` **חוסם-רינדור**, נמדד 06.08 **1.46ש מול 0.25ש** ל-Google Fonts — **חמור מה-500 שהוביל לכאן** ⏭️ גל נפרד. ⏸️ **חסום על ניב** — אירוח עצמי *וגם* הצמדת CDN מותנים בתנאי רישיון ש**לא ניתן היה לקרוא** (עמוד הרישיון JS-rendered, ה-CSS בלי כותרת רישיון). ⛔ לא שונה דבר. חלופה ללא רישיון: לוותר על General Sans לטובת Inter (מסיר 2 מארחים מה-CSP) — **החלטת עיצוב, לא שלי**
- **מרוץ ה-smoke — נמדד ונאכף (W · 4ב) ✅.** ⚠️ **ההנחה הופרכה: `/app` החזיר 200, לא 500** — הכישלון היה 500 מ-`api.fontshare.com` דרך ערוץ ה-console הלא-מסונן (תוקן, #13). המרוץ היה אמיתי אך רדום: המרווח deploy→בדיקות 5–21ש ב-4 ריצות הוא תוצר לוואי של `npm ci`, **לא כלל**; הסיכון **✅ כוזב, לא ❌**. `smoke.yml` ממתין ל-Deployment API (`success` ל-`github.sha`, 180ש, timeout=❌). ⚠️ **מוכיח ready, לא התפשטות alias** — אין סמן SHA באפליקציה ⏭️
- 🔴 **ה-CSP הוא Report-Only בלי אספן — כותרת שנראית כמו הגנה ואינה מייצרת אות** (`vercel.json:18`, נמדד 06.08: `report-uri|report-to` = **0**). אף דוח לא נאסף **מעולם**, ולכן "אמת שהדוח נקי" אינו ניתן למענה — לא לפני U ולא אחריו. ⛔ אין לאכוף לפני אספן + חלון תנועה נקי. ⚠️ **`api/notify.js` אינו זקוק לרשומת CSP** — serverless, קריאותיו server-to-server, מחוץ לתחום השיפוט של CSP (טעות קטגוריה שנסגרה)
- **אין בדיקה שחוסמת נתון דמוי-טוקן באנליטיקס יוצאת** (#12 בקריאה בלבד) ⏭️ · ⛔ ואין אסרציה ש**ריצת CI שולחת אפס בקשות אנליטיקס** — השומר של #13 מוכח חד-פעמית, לא חוזרת · **שורות `waitlist` ירדו ב-2 תוך 24ש** (60, ▼2) — מחיקה ידנית או ספירה שגויה ב-Growth. **לא נבדק**
- 🔴 **כל מדד GA4 מלפני 05.08 בלתי-שמיש** (#13): ≈906 page_view סינתטיים (smoke 108×5 · sentinel 122×3) מול 41 נרשמו / 12 פעילים, **בלי סמן שמפריד אותם בדיעבד**. ⚠️ **תאריך התיקון = קו הבסיס, אין להשוות לרוחבו.** ⏸️ סימון/ניקוי ב-GA4: על ניב
- ⚠️ **`Failure Alert` הוא `workflow_run` ואין לו צופה** — נעדר מטבלת ה-max-age של Watchdog **ומרשימת 11 ה-workflows של עצמו**. אם ייפול, **11 workflows מאבדים התרעה בשקט**. ⛔ לא תוקן ב-W
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — אין `created_at` בצד השרת; הערך מגיע מהלקוח ובעסקה מיובאת נושא את תאריך העסקה. **40/56 שורות מוקדמות מהרשמת הבעלים** (3 משתמשים) ולכן מוצאים מכל מדידת זמן. `retention.sql` מדווח בנפרד ⛔ לא ממציא 0
- **ציד ה-egress — מוין 06.08, שלוש מהטענות תוקנו במדידה:** `api/ocr.js` **יש** לו JWT (`:323`) ותקרה 10/דק+60/שע (`:330`); חסרה **מדידת עלות**, והחור האמיתי הוא ש-`rateLimit` הוא **in-memory per-Lambda** — התקרה **רכה תחת concurrency**, וזה שונה מ"אין rate limit" ⏭️ · `api/health.js:160` `Allow-Origin: *` **מכוון ומתועד** (`:156-159`, UptimeRobot) והגוף מחזיר latency ms + שמות שירות בלבד → **סיכון מקובל** · **"8 מייל + 11 דיסקורד, אפס ליגר" שגוי**: נמדד **9/9** שולחי מייל עם `to:` קשיח → `niveven183@gmail.com`, **14** דיסקורד, והשולח היחיד למשתמשים אמיתיים (`email-campaign.yml`) **יש לו ליגר** → **סיכון מקובל** · `scripts/analyst|arch-auditor|data-guardian` שולחים אגרגטים/`file:line`/UUIDs — **לא PII** → מקובל, ⚠️ הסיכון הוא **סחיפה**: הריפו ציבורי ולוגי Actions גלויים
- **`tradeImage` (data-URL) ב-localStorage בלבד** (`supabaseClient.js:65`, **בכוונה**) — לא עובר מכשיר, והוא צרכן המכסה. ✅ **הכשל השקט ב-`:1665`/`:1670` נסגר (S2)** — `console.error` שאומר שכל השמירה המקומית מתה. ⚠️ **רישום בלבד: המשתמש עדיין לא רואה כלום ב-UI** ⏭️
- ⚠️ **זיהום Sentinel ב-`public.trades` — ההשערה נבדקה ו*לא* אוששה (07.08).** החשד: 48 ריצות/יום (`cron '20,50 * * * *'`), כל אחת יוצרת עסקת `SNTNL`. **הנתונים אומרים אחרת:** 3 השורות שנוספו ב-6 שעות הן `ZIM/PATH/ONDS` של `de66dc99` — **משתמש אמיתי שחזר, לא סנטינל**. `restCleanup` רץ ב-`test.afterAll` (`sentinel-auth.spec.js:525`) כלומר **גם כשהמסע קרס**, ולכן הזיהום חסום ב-~שורה יתומה אחת בין ריצות. ⛔ **אין לרשום את סנטינל כמקור זיהום בלי מדידה.** אימות חד-פעמי שנותר לניב — **הממצא הצפוי הוא 0 שורות `SNTNL` עומדות; אם יימצאו שורות, זה הממצא**:
  `select count(*) from public.trades where ticker = 'SNTNL';`
- ⚠️ **`CLAUDE.md` §7 מתאר שרשרת `verify` שאינה מעודכנת** (נמצא בסריקה 07.08) — בפועל `package.json` כולל גם `test:feedback`, `test:notify` ו-`test:delete`. **התיעוד מפגר אחרי הקוד**, וזה הקובץ שגובר בסתירה. ⏭️ תיקון טקסט
- ⚠️ **42 `catch {}` ריקים ב-`SwingEdge_App.jsx` + אחד ב-`AdminPanel.jsx:50` (`writeJSON`)** — קובץ אחר, אותה מחלקה, נמצא בסריקה 06.08. S2 נגע ב-2 בלבד לפי גבול מפורש. ⛔ אף אחד לא נבדק — הספירה אינה הערכת חומרה ⏭️ גל נפרד
- `SwingEdge_App.jsx` (7,340) ו-`src/i18n.js` (2,893) גדולים **בכוונה** — ייפוצלו בעילה. **החלטה, לא חוב**
