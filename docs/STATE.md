# STATE — מצב חי

> מתעדכן ע"י Claude Code **בסיום כל משימה**, לפי סיום משימה ולא לפי לוח שנה.
> קובץ מצב, לא ארכיון — מה שנסגר נגזם, לא נצבר.

עודכן: 2026-08-01 · HEAD: 8f51143
<!-- HEAD = הקומיט שהמצב הזה מתאר. פיגור של קומיט אחד הוא מובנה: הקובץ נכתב לפני שה-hash קיים. -->


---

## 🔴 עכשיו

**תיקוני שלמות מספרית לפי AUDIT-2026-07-27 — קבוצות 1 ו-3 נסגרו ✅.**
קבוצה 3 (חוזה ה-`null` ב-R) סגרה את FIN-007/008/009/010/011/012/032/034: `rMultiple`
הוא `number | null`, `avgR` וכל נגזרותיו לעולם אינם `0` פיקטיבי, וכל מדד R מדווח
`rSampleSize` לצד הערך — גם במסך, גם ב-PDF. `+0.00R` לעסקה בלי `stop` אינו קיים
עוד באף מסך או ייצוא. שער חדש: `npm run test:rcontract` (11 בלוקים) בתוך `verify`,
כולל שני שערי הקבלה — תיק בלי אף סטופ מחזיר `avgR: null` ולא 0, ו-BE יושב
באוכלוסייה שלישית שמשקלה סוגר ל-1.
**תיקון הייבוא — שלב א' נסגר ✅ (29.07).** המילון קיבל את משפחת "שער" ואת "סוג פעולה",
ו-`fieldForHeader` הוחלף ב-`matchHeader` שמדרג לפי ספציפיות במקום לפי סדר הכרזת השדות.
IBI ואלטשולר נקלטים אוטומטית מקצה לקצה. אגב כך כובה באג חי: `"l/s"` התנרמל ל-`"l s"`,
ולכן `"stop loss price"` ו-`"total shares sold"` מופו ל-`side`. `test:import` עלה
מ-6 ל-10 תרחישים, כולל שער baseline חוסם על 6 הפיקסצ'רים המקוריים.
⚠️ **"הכול פתוח" של IBI לא נפתר** — קובץ IBI הוא יומן תנועות בלי עמודת יציאה; הזיווג
הוא FIFO, שלב ג'. אין לדווח למשתמש שהייבוא הושלם.
**קבוצה 2 (מקור אמת אחד) נסגרה ✅ 30.07 — ראה למטה. הבא: שלבים ב'+ג' לייבוא.**
⚠️ לא לאמת אף אחת מהן מול 36 עסקאות הדמו — ראה §סיכונים פתוחים.
**`test:coach` (111) תוקן ✅ (29.07)** — ראה `docs/plans/PLAN-2026-07-29-coach-fixtures.md`.
הפיקסצ'רים ב-`coach-invariance-test.mjs` קיבלו `status:"CLOSED"`, `exit` (במקום
`exitPrice`) ו-`shares:20` (חסר לגמרי קודם — בלעדיו `calcTradeMetrics` היה מחזיר
`NaN` בשקט). עכשיו הבדיקה מכסה **גם** שחמש עסקאות ה-`seeded` באמת עוברות דרך
`getClosed`/`calcTradeMetrics` (אסרשן שמירה חדש #111), לא רק אינווריאנטיות
פרופילים כמו קודם. Characterization לפני/אחרי: `verdict` זהה בכל 5 התרחישים;
שני שינויי `confidence` (`earnings-window` 62→100, `no-target` 40→64) הם הביטוי
הישיר של המנוע שסוף-סוף מקבל את 5 העסקאות — לא רוככה אף ציפייה.
⚠️ **נותר לא-מכוסה:** פיקסצ'רי `mkClosed` חסרים `createdAt`/`date`, ולכן
`dayOfWeek()` הוא תמיד `null` עבורם — אף קומבינציית-ממדים ב-`EdgeFinder` שכוללת
את ממד ה-"יום" לא נבדקת אי-פעם ע"י `test:coach`. לא תוקן במכוון (מחוץ לגבול
המצומצם של המשימה — הוספת שדות רק כפי שנדרש ל-`calcTradeMetrics`).

במקביל, שבוע הבנת משתמשים — GA4 הותקן ✅. נותר: **צעד ידני חובה בקונסולת GA4**
(ראה למטה) · retention day-7 · מייל ל-5 פעילים.

**T2 — חילוץ `useTradingStats.js` לפונקציה טהורה נסגר ✅ (30.07).** `src/lib/tradingStats.js`
חדש מכיל `computeTradingStats(trades, capital, calcTradeMetrics)` — אותה לוגיקה
בדיוק, מאומת ב-`diff -w` מול הגרסה הישנה (רק הבדלי wrapper/whitespace, אפס
הבדל לוגי). `useTradingStats.js` הוא עכשיו `useMemo` דק שעוטף אותה. אפס שינוי
ערך/סקאלה/באג במכוון — זו הכנה ל-T3, לא תיקון. שער חדש: `test:tradingstats`
(`scripts/tradingstats-test.mjs`, 7 בלוקים כולל baseline קפוא ב-
`scripts/fixtures/tradingstats-baseline.json`) בתוך `verify`. `test:coach`
נשאר 111=111 לפני ואחרי (התלות הטרנזיטיבית המשותפת — `statisticalModels.js` —
לא נגעה). ראה `docs/plans/PLAN-2026-07-29-extract-trading-stats.md`.
**תוצר מרכזי: טבלת הבדלים מדויקת בין שני היקומים הסטטיסטיים, שמזינה את T3
(קבוצה 2 בפריט הבא בתור #3):**

| ציר | `useTradingStats.js` (הישן/tradingStats.js החדש) | `statisticalModels.js` |
|---|---|---|
| סקאלת win-rate | 0–100 תמיד | 0–1 (המרה ל-100 רק בגבול מנוע האינטליגנציה) |
| אוכלוסיית "סגור" | `status==="CLOSED"` בלבד | `status==="CLOSED" && exit!=null` (`getClosed`) |
| סיווג BE (`pnl===0`) | נופל דרך implicit — לא נספר לא כ-win ולא כ-loss, אבל כן בתוך `closed.length` (`winRate+lossRate<100` כשיש BE) | דלי שלישי מפורש `rPopulations().be`, בשימוש ישיר ב-`expectancyWeights`/`expectedValueR`/`kellyFraction` — אין מקבילה ל-EV/Kelly ב-tradingStats.js בכלל |
| null handling ל-R בלתי-מדיד | מיושר — `rMultiple: null`, לא 0 (חוזה קבוצה 3) | מיושר — זהה |
| `maxDrawdown` | שדה `%` (מספר כמו 12.5 = 12.5%) | פונקציה גנרית שמחזירה שבר 0–1 גולמי על סדרת equity | **התנגשות סקאלה/סמנטיקה של אותו שם שדה — מוקש ל-T3** |
| `profitFactor` | `Infinity` אפשרי כשאין הפסדים; זהה ב-`groupAndAnalyze()` (`bySetup[]`/`byEmotion[]` וכו') | (טרם הושווה ישירות — לא בסקופ T2) |

**ממצאי ציד T2 (§11 — תיעוד בלבד, לא תוקן, מוזנים ל-T3):**
- **5 מועמדי חישוב-inline כפול ב-`SwingEdge_App.jsx`** שמשכפלים מדד שכבר קיים
  באחד משני המנועים: `:3091` (סינון win/loss ידני ל-badge), `:485` (best-setup
  win-rate בתוך `generateSmartLessons`), `:535-536` (avgWin/avgLoss ידני
  באותה פונקציה), `:4720`+`:4862` (emotion win-rate ידני בייצוא PDF),
  `:4868-4879` (לולאת streak-history ידנית לייצוא PDF). כולם מועמדים ל-T3
  (קריאה ממנוע יחיד במקום חישוב מקומי) — לא תוקנו.
- **מפת צרכנים:** `useTradingStats` נצרך ב-`SwingEdge_App.jsx` (המסך הראשי) וב-
  `WeeklyReviewTab.jsx:57` (`wStats` — `totalPnL`/`currentStreak`/`total`/`winRate`/`bySetup`).
  לא נמצאה כפילות תצוגה (אותו ערך מוצג פעמיים ממקורות שונים).
- **חקירת FIN-030 (`profitFactor: ∞`):** רק `journalStats.profitFactor`
  (`SwingEdge_App.jsx:3599`) מוצג אי-פעם על מסך, וכבר מוגן (`isFinite(...) ? ... : "∞"`).
  ⚠️ **סיכון Infinity רדום ולא-מוצג נמצא**: `bySetup[]`/`byEmotion[]`/וכו'
  ב-`groupAndAnalyze()` (`src/lib/tradingStats.js:244`) מחשבים `profitFactor`
  באותה נוסחה, ללא הגנה — כרגע לא נצרך ע"י שום מסך, אך יתפוצץ בשקט אם ייפתח
  צרכן חדש (למשל טבלת פירוט-לפי-סטאפ). מוזן ל-T3 כדרישת הגנה.
- ~~**שלוש `.claude/worktrees/` ישנות נמצאו**~~ ✅ **נסגר 31.07** — הוסרו אחרי אימות
  ש-`git status --short` ו-`git log main..HEAD` ריקים בשלושתן. ראה §✅.

**T3 (קבוצה 2) — מקור אמת סטטיסטי אחד — בוצע ונסגר ✅ (30.07).**
`docs/plans/PLAN-2026-07-30-fin-group2-unify.md`. FIN-001..006 סגורים.
`getClosed` (`status==="CLOSED" && exit != null`) הוא **ההגדרה היחידה** של "עסקה
סגורה" בכל האפליקציה · BE הוא **דלי שלישי מפורש** (`winRate+lossRate+beRate = 100`)
בכל רמה — כללי, `bySetup`, `byEmotion`, `byMarket`, `byEntryQuality`, `byDayOfWeek`,
`topEdges`, `antiEdges`, `lastWeek/lastMonth` · `statisticalModels` נשאר 0..1 ו-
`tradingStats` מחזיר 0..100 עם **גבול המרה יחיד ומסומן** · `maxDrawdown` →
`maxDrawdownFraction` · שלוש מימושי `profitFactor` → מסלול אריתמטי אחד
(`grossPnl` → `profitFactorFromPnls`), עם `Infinity` כ**סנטינל מוצהר** (לא `null`:
`isFinite(null)===true` היה עובר את ההגנה הקיימת ואז `null.toFixed(2)` = מסך לבן).
**דיף מול baseline: 123 שדות נוספו · 0 הוסרו · 0 ערכים השתנו** — כל מספר קיים
שרד ביט-בביט, כולל `profitFactor` (סחיפת ULP שנחזתה **לא התרחשה**: היפוך סימן
וסכימה באותו סדר מדויקים ב-IEEE-754, ולכן אין מה "לנעול"). שער חדש בכל פיקסצ'רה:
`totalTrades + openTrades + לא-נספרות === trades.length`. פיקסצ'רה 6
(`closedWithoutExit`) מכריחה את החוזה להיות מוכח — עסקה `CLOSED` בלי `exit` **אינה**
BE, ונעלמת **בהצהרה** ולא בשקט.
שלב 0 (אבחון read-only) העלה **ארבעה תיקוני הנחה** שנשמרים כאן לתיעוד:
- **מדידה חיה 30.07** (שאילתה אחת, read-only): 116 עסקאות · `CLOSED` 102/116 ·
  **`CLOSED` ו-`exit IS NULL` = 0/102** · **עסקאות BE = 0/102**. כלומר שתי
  ההחלטות הנעולות הראשיות (`getClosed`, דלי BE) אינן משנות אף מספר לאף משתמש
  היום — הן אינווריאנטים הגנתיים, ובהתאם ציר המשתמשים ב-§8 הוא אפס
- **14 העסקאות אינן "CLOSED בלי exit"** — הן `CLOSED` עם `closedAt IS NULL`
  ו**יש** להן `exit`, ולכן `getClosed` משאיר אותן באוכלוסייה. אפס קשר להחלטה 1
- **אף אחת מ-5 הפיקסצ'רות אינה מכילה `CLOSED` בלי `exit`** → החלטה 1 אינה
  ניתנת להוכחה בסוויטה הקיימת; התוכנית מוסיפה פיקסצ'רה 6
- **FIN-006 כבר תוקן** (`MonthlyReport.js:59,206` קוראים את הבוליאני המועשר
  מ-`isFollowedPlan`) — נותר רק אסרשן נעילה

**ממצאי ציד T3 — מה נסגר בביצוע:**
- ✅ **קונבנציית BE שלישית** (`App:469-470`, `generateSmartLessons`) — **מתה
  באיחוד**: `setupMap` הוחלף ב-`stats.bySetup`. לא היה אתר שישי
- ✅ **teaser המנטור** (`App:2238`) — חובר ל-`getClosed` + `outcomeRates`. היה
  אתר `winRate` inline שמיני שלא הופיע במפת T2. **המספר לא זז** (המכנה כלל BE
  גם קודם), אבל האוכלוסייה והסיווג כעת משותפים
- ✅ **`App:4853` = `"Neutral"` לרגש חסר** — מת באיחוד. `stats.byEmotion` שם
  עסקה בלי רגש ב-**`"Unknown"`**. רגש חסר אינו רגש רגוע
- ✅ **`TradeCalendar.jsx:77` + `DayTradesModal.jsx:62`** (ציד של ניב) — שני מסכים
  שחישבו `wins/n` inline. **חלון הזמן נשאר מקומי** (המנוע מקבץ לפי יום-בשבוע
  וחלונות מתגלגלים — אין לו מקבילה ל"חודש קלנדרי"), אבל **הסיווג עבר** ל-
  `outcomeRates`. האוכלוסייה תוקנה במקור: `App:3692` → `getClosed(filteredTrades)`,
  ו-`DayTradesModal` יורש דרך `tradesByDate`
- ✅ **`App:5430` `calcSetupSuccess`** — אתר inline **שביעי** שלא הופיע במפת T2.
  חובר ל-`stats.bySetup.find(...)`. כרטיס Playbook והמטריצה ב-Analytics כבר
  אינם יכולים להראות שני מספרים לסטאפ אחד
- `statisticalModels.maxDrawdown` (`:135`) ו-`isSignificant` (`:159`) — **אפס
  מייבאים**. שונה שם ל-`maxDrawdownFraction` (החלטה 4); `isSignificant` לא נגעו

**שבעה מסלולי `status === "CLOSED"` — הכרעה פרטנית (בקשת ניב):**

| אתר | הכרעה |
|-----|--------|
| `App:148` `generateEquityCurve` | חובר ל-`getClosed` |
| `App:231` `allClosed` (PDF חודשי) | חובר ל-`getClosed` |
| `App:1570` `closedTrades` | חובר ל-`getClosed` |
| `App:1575` `closedCountAll` | חובר ל-`getClosed` |
| `App:2238` teaser מנטור | חובר ל-`getClosed` (+`outcomeRates`) |
| `App:3692` prop ל-`TradeCalendar` | חובר ל-`getClosed` — **לא היה ברשימה**, נמצא רק כי הוא כתוב במרכאות בודדות |
| `App:5430` `calcSetupSuccess` | מת — הוחלף ב-`stats.bySetup` |
| `App:2981` `const closed = tr.status === "CLOSED"` | **נשאר במכוון** — תצוגה בלבד (צביעת תא), ואתר השימוש כבר קורא `closed && tr.exit != null` |

**שינויי תצוגה שנובעים מהאיחוד (מה שהמשתמש יראה אחרת):**
- תג ה-Win Rate בדשבורד ובכרטיס החניך: `12W / 8L` → `12W / 8L / 2BE` **רק כאשר
  `be > 0`**. במקרה הנפוץ (0 BE בפרודקשן היום) אין שינוי גלוי
- גרף הרגשות ב-Analytics: עסקה בלי רגש עוברת מ-`"Neutral"` ל-`"Unknown"`,
  וסדר העמודות הוא כעת P&L יורד (היה סדר הופעה שרירותי)
- כרטיס "הרגש החזק ביותר": שובר-שוויון מפורש `winRate → count` (היה סדר הרשימה
  הלבנה), זהה לשובר-השוויון של "הסטאפ החזק ביותר" שמעליו
- "הסטאפ החזק ביותר" בתובנות היומן: עסקה בלי `setup` מתויגת `"Unknown"` (היה
  `"Undefined"`)

## ⏭️ הבא בתור

1. **תיקון ייבוא — שלבים ב'+ג'.** שלב א' (מילון + דירוג התאמה) נסגר 29.07 ✅ —
   ראה `docs/plans/PLAN-2026-07-29-import-phase-a.md`.
   ⚠️ משתמש `a0556783290` ממתין ליומן שמיש. שלב א' לבדו נותן לו קליטה של אלטשולר,
   אבל עסקאות IBI עדיין ייכנסו כפתוחות. **לא לדווח לו "תוקן" לפני שלב ג'.**
   ⚠️ שובר-השוויון של `ticker` (`SYMBOL_RE` ב-`detectColumns.js`) הוא היוריסטיקה
   אמריקאית: נייר ישראלי מספרי (`1159250`) אינו נתפס כסימבול. מטופל בפרופילי ברוקר

   **1b. ייבוא שלב ב' — פרופילי ברוקר.**
   זיהוי שורת כותרת דינמי (ב-IBI הכותרות בשורה 11, לא בראשונה — ניב אבחן זאת בעצמו) ·
   סינון סוגי פעולה: **6 מתוך 8 סוגי השורות בקובץ IBI אינם עסקאות** (דיבידנד, המרת
   מט"ח, מתנה, מבצע, חיוב ריבית, ציטוט זמן-אמת) · גזירת `side` מסימן הכמות ב-IBI
   (`-19` / `19`) או מטקסט באלטשולר (`"קניה"` / `"מכירה"`).
   ⚠️ **אין "פורמט ישראלי".** פרופיל נפרד לכל ברוקר — לשני הקבצים אפס מוסכמות משותפות.

   **1c. ייבוא שלב ג' — מנוע FIFO.**
   זיווג קנייה↔מכירה · **סגירות חלקיות הן הנורמה, לא הקצה** · חישוב P&L ·
   `stop` נשאר `null` מפורש (חוזה ה-R — לא 0).
   ⚠️ **זה השלב שפותר את "הכול נכנס כפתוח" של `a0556783290`.** לא לדווח לו שתוקן לפניו.
2. **שבוע הבנת משתמשים** — ⚠️ **Enhanced Measurement → "Page changes based on browser
   history events" חייב להיות ON** (SwingEdge הוא SPA; בלעדיו אף מסלול פנימי לא נספר
   ו-`/app` ייראה מת) · אימות חי של `gcs=G100`/`G101` · retention day-7 · מייל ל-5 פעילים
3. **שלמות מספרית — `docs/audits/AUDIT-2026-07-27-financial-integrity.md`** (46 ממצאים:
   🔴 27 · 🟠 14 · 🟡 4 · ⚪ 1). ⚠️ **הפריט הזה הוא קבוצות 2 ו-4 בלבד** — קבוצה 1
   בוצעה ✅, וקבוצה 3 קודמה לראש התור (פריט 1). שלושת התסמינים שדווחו (סתירת כרטיס
   ההון · +0.00R בלי stop · PROFIT FACTOR ∞) הם תסמינים, לא שורשים: לאפליקציה **שני
   יקומים סטטיסטיים מקבילים** (`useTradingStats.js` מול `statisticalModels.js`) החלוקים
   על הסקאלה, על סיווג ה-BE ועל אוכלוסיית "סגור", ועליהם חישובים inline
   ב-`SwingEdge_App.jsx`. הסדר שנותר: **קבוצה 2** (מקור אמת אחד) → **קבוצה 4** (תאריכים).
   **T2 נסגר ✅ (30.07)** — `useTradingStats.js` הוא עכשיו פונקציה טהורה
   (`src/lib/tradingStats.js`), טבלת ההבדלים המדויקת מוכנה (ראה 🔴 עכשיו למעלה),
   ו-5 מועמדי inline-duplication ב-`SwingEdge_App.jsx` מזוהים. **T3 (קבוצה 2
   בפועל — האיחוד עצמו) עדיין לא התחיל.**
   ✅ **שיעורי ה-`stop` נמדדו מחדש 28.07** — 23/78 עסקאות אמיתיות = **29.5%**
   (מצרפי כולל דמו: 20.2%), **חציון פר-משתמש 0.0%**. המספרים נכנסו לראש §6 באודיט.
   FIN-007 נשאר 🔴 כבאג, אך הדילול הוא ~30% ולא ~44% ומרוכז ב-1-2 משתמשים — לא
   התנהגות אוכלוסייה. **"מחיקת 173 העסקאות" הושלמה ע"י המשתמש**
4. **חובות מוצר בקופי** — תג CLOSED בקריפטו · waitlist בלנדינג · ניסוח דוח Growth ·
   **ריבוי עברי ב-`riskUnmeasured`** — "ל-1 עסקאות פתוחות אין סטופ" עבור `n=1`.
   דורש pluralization ב-i18n, לא תיקון נקודתי במחרוזת
5. **מיפוי קבצים במחשב** — read-only, דוח מקומי, לא לריפו · ואחריו **סידור בפועל**
   (מחשב + ריפו)
6. **Dispatcher → Actions** (IMAP)
7. **Trader Persona → Actions**
8. **Gate 2.2** — RLS audit מלא + בדיקת חדירה + rate-limit ל-symbol-search + נגישות
9. **₪ + Stripe + entitlement** — אין ₪ בקוד כלל
10. **B1 Multi-Account**
11. **Track A — לנדינג V2**
12. **אודיט 4 קבצי הידע** מול הקאנון (Minervini / O'Neil / Weinstein / Tharp)
13. **איכות OCR** — נבדק 27.07: עובד, לא מספיק טוב על גרפים עמוסים. `api/ocr.js`
    בנוי נכון (confidence + `validate()` + `nullResult` "לעולם לא לנחש") אך **מעולם
    לא נמדד ולא כויל**. אין Tesseract בריפו — Claude Vision בלבד
    (`MODEL = claude-sonnet-4-6`). **חסם: סט בדיקה של 20-30 גרפים + ground truth**
    (עם ניב, ראה ⏸️). אחרי המדידה: MODEL→Opus · חידוד prompt · כיול סף ה-confidence ·
    מסך אישור לפני שמירה עם הדגשת שדות בביטחון נמוך.
    🎯 **היעד הוא אפס טעויות שקטות — לא קריאה מושלמת.** 100% על קלט תמונה חופשי אינו
    בר-השגה ואסור לתכנן סביבו.
    ✅ **F1+F2+F3 נסגרו 01.08** — ראה `docs/plans/PLAN-2026-08-01-ocr-f123.md`
    ו-✅ נסגר השבוע. **החוסם היחיד שנותר לפריט הזה הוא סט ה-30** (ראה ⏸️):
    MODEL→Opus · חידוד prompt · כיול הסף — כולם דורשים מדידה, לא קוד
14. **Lightbox לתמונת הגרף ב-Analyzer** (קטן, UX) — לחיצה על התמונה שהועלתה מציגה
    אותה מוגדלת. בקשת משתמש 31.07: "יש לי מלא סקרינשוטים של מסחר בפלאפון" — אימות
    שהתמונה הנכונה נבחרה, במיוחד כשה-OCR לא מילא
15. **העלאת כמה סקרינשוטים בבת אחת** — בקשת משתמש 31.07. **מותנה באיכות OCR
    (פריט 13)** — הכפלת קלט לפני שהקריאה מכוילת מכפילה גם את הטעויות השקטות
16. **🆕 `toFixed(2)` בצד הלקוח הורג מחירי קריפטו** (נמצא 01.08, סעיף ציד של
    `PLAN-2026-08-01-ocr-f123`). `api/ocr.js` שומר 12 ספרות מובהקות (`tidy`,
    `:104`) — **הלקוח מוחק אותן**: `String((0.00004).toFixed(2))` = `"0.00"`.
    חמישה אתרים: `SwingEdge_App.jsx:2419-2421` · `:2461-2463` · `:2565-2566` ·
    `:2573-2575`, וגם המחיר החי `:1894`/`:1923`. **כניסה 0.00 = חלוקה באפס
    ב-R.** דורש החלטת עיצוב (דיוק דינמי לפי גודל המחיר) — אינו "קטן ובטוח"
    ולכן לא נכלל ב-F1-F3
17. **🆕 `confidence: 90` על `stopDelta` שלילי** (נמצא 01.08 ב-baseline של
    `scripts/ocr-contract-test.mjs`, תרחיש D). `stopDelta = -4.39` בלונג:
    `computeEntry` דוחה את רגל ה-stop, נופל על ה-target בלבד, `validate` מאפס
    את ה-stop (✅) — אבל `rrOk` מחזיר `true` בוואקום כי `stopDelta > 0` נכשל,
    ו-Vision מסכים על ה-entry → `+5`. וריאנט של F4. הוקפא ב-`FROZEN.D` כדי שלא
    יזוז בשקט.
    🟠 **התסמין הנראה נסגר ב-F3 (01.08)** — `ocrDetection` מחזיר `partial`
    (entry מלא, stop חסר), והבאדג' כתום עם "זוהו חלק מהמחירים — השלם את השאר
    ידנית". **הסיבה עצמה פתוחה:** `api/ocr.js` עדיין מחזיר `confidence: 90` על
    קלט סותר, ומספר גבוה על קריאה חלקית הוא עדיין מספר לא-נכון בכל צרכן עתידי
    שיסתמך עליו ישירות ולא על `detection`

## ⏸️ חסום / ממתין לניב

- Google Workspace
- רשם הדומיין — איש קשר + Auto-Renew
- מלאי סודות
- החלטת ריפו פרטי מול דקות Actions
- **סעיף ה-`/privacy` ממתין לאישור עורך דין** — נוסח ונדחף 28.07, טרם אושר משפטית
- **סט בדיקת ה-OCR** — 20-30 גרפים + הערכים הנכונים. בלעדיו פריט 13 אינו מדיד
- **🆕 שדה טווח עסקה (סווינג/יומי/ארוך)** — בקשת משתמש 31.07. נוגע בסכימה + מסננים;
  לתזמן מול B1 (פריט 10). **החלטת ניב**
- **🆕 עבודה לא-מקומטת ב-`tests-sentinel/sentinel-public.spec.js`** (נמצא 01.08
  ב-safety gate). מוסיף מעקב 5xx מצד-שלישי לסנטינל — עבודה קוהרנטית, לא זבל.
  האודיט לא נגע בו וקומט קבצים בשמות מפורשים. **החלטת ניב: להשלים או לגנוז**
- **מייל ל-`saridel`** — 22 עסקאות, **100% ללא stop, כולן סגורות**. הדפוס אינו ייבוא
  (ייבוא מייצר עסקאות פתוחות). המשתמש היחיד עם היסטוריה נקייה שיש ממנה מה ללמוד

## ✅ נסגר השבוע

- **🆕 מקבץ תיקונים קטנים — 7 סגירות (01.08).** תוכנית:
  `docs/plans/PLAN-2026-08-01-small-fixes-batch.md`.
  · **1א — החלטת המוצר במסנן ה-R התקבלה ומומשה:**
    **"עסקה בלתי-מדידה: גלויה תמיד, מוחרגת ממסנן R פעיל עם ספירה גלויה."**
    השער הוא `Number.isFinite` (לא `!= null`) — זהה להגדרת האוכלוסייה ב-`rValues()`
    ופוסל גם `NaN`. המסנן כבוי → הכל גלוי, ה-R מוצג `—`. המסנן פעיל → העסקאות
    מוחרגות **ונספרות**, והמספר מוצג ליד המסנן (`rFilterHidden`). הפריט ירד מ-⏸️
  · **1ב — `avgMaeMfe`:** `numOrNull` (שומר `v == null` **לפני** `Number()`, כי
    `Number(null)===0` ו-`isFinite(0)` הוא `true`), ומכנה נפרד לכל מדד — `maeN`/`mfeN`
    = מספר הערכים שקיימים באמת. אין מה למדוד → `null`, לא `0`
  · **2 — ריבוי בעברית:** `plural(t, key, n)` ב-`src/i18n.js` — מפתח בסיס + `_one`
    (+ `_two` לערבית), נפילה חיננית למפתח הבסיס. `riskUnmeasured` ו-`rFilterHidden`
    בכל 5 השפות
  · **3א — קריפטו:** `parseChartResult` הטביע תג `CLOSED` על BTC נע, כי
    `getMarketState()` הוא שעון בורסת NYSE. קריפטו מזוהה דרך `instrumentType` עם
    נפילה לסיומת `-USD`, ומסומן `OPEN` תמיד
  · **3ב — קופי רשימת ההמתנה (he+en):** מוביל בפעולה ונשען רק על שורות 🟢
    ב-`docs/TRUTH.md`, בניסוח המאושר שלהן
  · **3ג — "רשימת המתנה: N" → "סך שורות waitlist: N"** בדוח Growth ובמילון
    `user-analytics.mjs`. המספר הוא ספירת שורות הטבלה (כולל מי שכבר הוזמן והומר),
    לא אורך תור
  · **4 — `api/send-invites.js:134`:** `return` שקט על `SENTINEL_DISCORD_WEBHOOK`
    חסר → `console.error` שנוקב בשם המשתנה. **4ב (`MAIL_*`) לא בוצע בהחלטת ניב**
  · **5 — FIN-030:** `profitFactor: Infinity` הוצג כ-`∞`. **המנוע לא נגעו בו** —
    `Infinity` הוא סנטינל מוצהר (`statisticalModels.js:159-164`), ו-`null` היה עובר
    את `isFinite` ומתפוצץ על `.toFixed`. התיקון בשכבת התצוגה בלבד: `t.pfNoLosses`
  · **6-B — מסך אישור OCR באנלייזר:** תשובת ה-OCR נעצרת בכרטיס `OcrReviewCard`
    (module scope + `memo` + draft מקומי) ולא נכנסת לטופס עד "החל". שדה שה-OCR
    **לא** קרא מסומן "לא זוהה" במפורש. `analyzerOcrTickerRef` נדרך ב-Apply מהערכים
    **הסופיים** — תיקן המשתמש את הטיקר, החסימה נדרכת מחדש; הזין entry ידנית,
    היא משתחררת. `status: "review"` נופל דרך `ocrBadgeState` בדיוק כמו `"ok"`,
    ולכן ארבעת מצבי הבאדג' לא נגעו. `api/ocr.js` לא נגע
  · **7 — `CLAUDE.md §10.2` "אין פידבק יתום"** נכתב: שלושה מצבי סיום, `Reviewed`
    מוגדר במפורש כמצב ביניים. בנוסף **§7 היה מיושן** — תיאר שרשרת של 3 מבחנים
    כשבפועל יש 7 + build, ו-`test:coach` (111, לא 110)
- **🆕 שרשרת הכשל השקט ב-OCR — F1+F2+F3 (01.08).** תוכנית:
  `docs/plans/PLAN-2026-08-01-ocr-f123.md`. אבחון: `AUDIT-2026-08-01-ocr-diagnosis.md`.
  **קדם לתיקון baseline קפוא** — `scripts/ocr-contract-test.mjs`, 4 תרחישים מול
  ה-handler האמיתי (רק Anthropic ו-GoTrue מזויפים). תרחיש A הוא חוזה הרגרסיה
  ונשאר **זהה-בייט** לפני ואחרי; רק B זז, וזו כל מטרת F2.
  - **F2 (`api/ocr.js`) — השורש.** בענף "אין כלי פוזיציה" ה-spread דרס את
    `nullResult.confidence`, ולכן שלושה `null` יצאו עם ביטחון המודל המלא (85).
    עכשיו `Math.min(modelConf, 25)` — מתחת ל-`MIN_CONFIDENCE` (40), כלומר כל
    צרכן קורא כשל. לא אפס במכוון: "בטוח שאין כלי" ≠ "לא הצלחתי לקרוא את התמונה".
  - **F3 (`SwingEdge_App.jsx`) — הבאדג'.** `ocrDetection()` גוזר את המצב ממה
    שהתמלא בפועל: `full` / `partial` / `ticker_only` / `none`, ו-`ocrBadgeState()`
    היא נקודת ההחלטה היחידה לשלושת הבאדג'ים (יומן · אנלייזר · גרף) שהיו שלוש
    מימושים כפולים. **`partial` לעולם אינו ירוק** — עסקה בלי stop מסומנת 🟠.
    כל אתר שומר את מפת הצבעים הליטרלית שלו (§13 Tailwind JIT).
  - **F1 (`SwingEdge_App.jsx`) — הרגרסיה שעומרי ראה.** `formOcrTickerRef` /
    `analyzerOcrTickerRef`: אחרי OCR שלא הפיק `entry`, אפקט הציטוט החי חסום
    מלכתוב לשדה ה-Entry עבור **אותו טיקר**. טיקר אחר משחרר אוטומטית, `force`
    (לחיצה מפורשת) גובר, ו-`useEffect` על ניקוי הבאדג' משחרר בכל 8 אתרי האיפוס
    בלי לפזר ניקויים ידניים. **המחיר החי עצמו לא נגע** — הצ'יפ עדיין מציג
    `LAST CLOSE`; רק המסלול לשדה נחסם.
  - **תצפיתיות.** `trackEvent()` חדש ב-`src/lib/consent.js` (הבעלים היחיד של
    `gtag`) שולח `ocr_result` עם `detected` + `confidence_bucket` — enum
    נמוך-קרדינליות בלבד, אף פעם לא טיקר או מחיר.
  - **i18n.** `ocrTickerOnly` + `ocrPartial` ב-5 השפות, ושלושת הבאדג'ים עברו
    ל-`t.*` — es/pt/ar היו מקודדים-קשיח he/en וקיבלו תרגום אגב כך.
  - **אומת בדפדפן** ב-406px (רוחב המכשיר של עומרי) על שרת dev בלי Supabase:
    (1) קריאה מלאה → שדות מלאים + `OCR ✓ 95%` ירוק, זהה ל-baseline A;
    (2) בלי כלי → Entry **ריק**, צ'יפ `LAST CLOSE $61.42` קיים, באדג' כתום,
    GA `detected: ticker_only`; (3) טיקר ידני בלי OCR → מילוי אוטומטי עובד
    כרגיל ואפס אירועי GA מזויפים.
- **שלוש `.claude/worktrees/` ישנות הוסרו (31.07)** — `cool-shaw-bf882b`,
  `naughty-haibt-efc05f`, `nervous-curie-c217a7`. שלושתן `git status --short` ריק
  ו-`git log main..HEAD` ריק, כלומר אפס קומיטים שאינם כבר ב-`main` — לא אבדה עבודה.
  היחיד שנמחק מעבר ל-checkout: `dist/` ו-`.claude/*.json` מקומיים. CLAUDE.md §5.3
  שוב נאכף.
- **Sentinel — עיוורון URL בדיווח 5xx (31.07).** האבחון הפריך חלק מההנחה:
  `page.on('response')` עם `status>=500` **כבר היה קיים** בשלושת ה-specs, וכל בדיקות
  ה-`curl` ב-`sentinel.yml` כבר נושאות את ה-URL בשדה `checked`. שני חורים אמיתיים
  נסגרו: (1) `msg.location()` בשתי ה-specs — ב-`sentinel-auth` הוא חושב רק כדי לסנן
  לפי `IGNORE_SOURCE` ואז **נזרק**, וב-`sentinel-public` לא נקרא כלל, כך שממצא
  `console_error` נשא רק את מה שהאפליקציה בחרה להדפיס; עכשיו הוא מצורף כ-`@ <url>`.
  (2) 5xx מהוסט צד-שלישי נפל בין כל הדליים ("by design") — עכשיו דלי `thirdparty_5xx`
  בחומרה 🟡 (`sentinel.yml:533` — נספר, לעולם לא מתריע מיידית), 4xx צד-שלישי נשאר
  מסונן כפי שהיה. **הסינון לא הורחב באף כיוון** — לכידה בלבד, כדי לא לייצר שקט חדש
- **מדד מילוי השדות — מכנה לפי רלוונטיות, ומכנה מוצג בכל שיעור.**
  ⚠️ **ההסבר המבני שהונח הופרך מול הקוד:** `followedPlan`, `maxFavorable` ו-`maxAdverse`
  **כן** נמצאים בטופס הסגירה (`SwingEdge_App.jsx:6483` MFE · `6488` MAE · `6495`
  followedPlan) **וכן** נשמרים (`1992-1995`). מה ששבר את המדידה: לולאת `FIELDS` נמדדה
  מול ה-CTE `clean` **בלי סינון `status='CLOSED'`** — שדות שנקבעים ברגע הסגירה נמדדו
  גם על עסקאות פתוחות, שמעולם לא ראו את הטופס. חמישה שדות סגירה
  (`followedPlan`, `exitReason`, `maxFavorable`, `maxAdverse`, `lessonLearned`) עברו
  ל-`POP_CLOSED`; שבעת שדות הכניסה נשארו ב-`POP_ALL`. אין ברירת מחדל — הוספת שדה
  מחייבת בחירת אוכלוסייה.
  מדידה חוזרת 28.07 מול ה-CTE של הסקריפט עצמו, חציון פר-משתמש:

  | שדה | מול כל העסקאות (מה שהדוח הציג) | מול סגורות בלבד |
  |------|--------------------------------|------------------|
  | שיעור העסקאות הסגורות | — | **33%** |
  | `followedPlan` | 33% | **100%** |
  | `exitReason` | 33% | **100%** |
  | `maxFavorable` | 33% | **60%** |
  | `maxAdverse` | 0% | **60%** |
  | `lessonLearned` | 33% | **60%** |

  **ה-33% הוא בדיוק שיעור העסקאות הסגורות** — ארבעה שדות שונים החזירו את אותו מספר
  כי כולם מדדו את אותו דבר: כמה מהעסקאות סגורות. אין פער משמעת, יש מכנה שגוי.
  ⚠️ **תיקון למספרים שדווחו קודם באותו יום:** MFE/MAE דווחו כ-80%/30% מול סגורות.
  הנכון הוא **60%/60%** (24-25 מתוך 40 אצל המשתמש היחיד בקוהורט). הפער נבע מקוהורט
  שונה — הספירה הישנה שמרה על "≥3 עסקאות" ולא על "≥3 **סגורות**". הכלל של הפריט הזה
  חל גם עליו: מספר בלי מכנה מפורש הוא מספר שלא נבדק.
  ⚠️ **והמסקנה שהמכנה חושף:** אחרי הסקופינג הנכון, הקוהורט לשדות סגירה הוא **משתמש
  אחד** (רק ל-`92a06c0c` יש ≥3 עסקאות סגורות). הדוח מדפיס "קוהורט 1-2 משתמשים",
  ולכן "100% followedPlan" אינו ממצא מוצרי ואינו ניתן להצגה ככזה. **זו בדיוק
  התועלת בהצגת המכנה** — הוא פוסל את המסקנה בשורה עצמה, בלי שאיש יצטרך לזכור.
  ➕ `assertRatiosCarryDenominator()` — שומר קבוע: כל שורה שמכילה `%` חייבת לשאת
  `מונה/מכנה` ושם אוכלוסייה, אחרת **זריקה** (`process.exit(1)`, הדוח לא נשלח).
  חל על גוש המילוי ועל המשפך (שהדפיס עד עכשיו יחסים חשופים). מוחרג במפורש ובהערה:
  שורת "שינוי מאתמול" — זו דלתא, והבסיס `was → cur` כבר מודפס באותה שורה.
  ➕ `CLAUDE.md` §2 — "אפס מנה בלי מכנה". **הסקריפט לבדו לא היה מספיק:** שניים משלושת
  המקרים ("61% בלי stop", "47% אובדן דמו") נולדו בשאילתות אד-הוק ובפרומפט, לא בסקריפט.
  ה-assert מונע נסיגה במקום שבו הפורמט בשליטתנו; הכלל ב-`CLAUDE.md` מכסה את השאר

- **שמירה הדדית — Fleet Daily שומר על ה-Watchdog.** הפער: `watchdog.yml` דילג על עצמו
  בבדיקת העקביות, וסוכן שרץ אינו יכול לדווח שהוא לא רץ — אם ה-cron שלו נכבה, השקט
  נראה זהה לבריאות. צעד `Watchdog liveness` ב-`fleet-daily.yml` שואל את GitHub API מתי
  `watchdog.yml` הצליח לאחרונה (אותו דפוס `runs?status=success&per_page=1`), סף 30ש',
  ומדווח 🔴 בארבעה מצבים: מאחר · מעולם לא הצליח · שגיאת API · timestamp לא תקין.
  fleet-daily ב-06:00 קודם ל-watchdog ב-08:00 ולכן תמיד קורא ריצה שהסתיימה, לעולם לא
  אחת באוויר. `actions: read` נוסף ל-permissions; אפס `${{ }}` בתוך `run` — הכול דרך `env`

- **סוכן התנהגות משתמשים — שלב א' חי** (`scripts/user-analytics.mjs` +
  `user-analytics.yml`, קרון 06:30 UTC). נבנה כולו על נתונים ש-Supabase כבר שומר:
  אפס אינסטרומנטציה, אפס כתיבה, אפס שינוי סכימה, אפס נגיעה ב-`SwingEdge_App.jsx`.
  ארבעה קווים אדומים אכופים בקוד (guards שזורקים ולא מדרדרים): תוכן יומן לעולם לא
  נקרא — רק נספר ונמדד באורך · מזהים מקוצרים ל-8 תווים בלבד · התנהגות ומבנה בלבד ·
  k-anonymity — פלח מתחת ל-3 משתמשים מדווח "1-2". הפירוט במייל בלבד, דיסקורד מקבל
  סיכום מצטבר, stdout מספרים מצטברים בלבד, **אפס artifact ואפס job summary**.
  שתי תקלות CI בדרך, שתיהן מתועדות ב-`INCIDENTS.md` #7 ו-#8: `-q` חסר ב-psql
  (תג `SET` הזיז כל מדד בשורה, והריצה דיווחה ירוק עם `NaN`) — נתפס בדיוק בגלל
  ש-dry run קדם לקרון; ודליפת מזהים מקוצרים ללוג ציבורי, כי **Actions מדפיס את
  קלטי ה-`with:` של action** — הדוח עובר עכשיו כ-`body: file://`.
  `MAXAGE=30` ב-watchdog ו-`"User Analytics"` ב-`failure-alert` נרשמו באותו קומיט
  שבו הקרון קם

- **`docs/TRUTH.md` — גשר האמת המוצרית לקופי.** מקור-אמת יחיד ל-`swingedge-creatives`,
  כל שורה אומתה מול הקוד כולל מעקב import עד שורש האפליקציה. **חמש שורות מהבריף סתרו
  את הקוד:** דוח ה-DNA החודשי הוא **חי** ולא "בקרוב" (`MonthlyReport.js:125-131`, נגיש
  מ-Tools); OCR חי **וחינם** — **אין מנגנון תשלום בריפו כלל**, ולכן "Pro" הוא 🔴; נעילת
  ה-30 דק' **אינה נועלת** (`cooldownRemainingMs` נקרא לתצוגה בלבד, ולצדו כפתור ביטול);
  es/pt/ar מכוסות **~63%** בלבד (382–392 מפתחות מול 607) ונופלות לאנגלית בשקט; **אין
  service worker** ולכן אין offline. נוסף סעיף **🔴 אסור לטעון** עם חמש הטענות השגויות.
  `CLAUDE.md` §10.4 מחייב עדכון הקובץ באותו commit של שינוי יכולת פונה-משתמש.
  ב-`fleet-weekly.yml` נוסף צעד (בתוך `Weekly Vitals` הקיים, אפס צעדים חדשים) שמושך את
  ה-hash דרך `gh api` — **בלי `checkout`, שאין בג'וב** — ומשווה מול `.fleet-state` של
  השבוע הקודם. השוואה חוצת-ריפו נפסלה: `swingedge-creatives` **פרטי** ו-`github.token`
  אינו יכול לקרוא אותו, כלומר היא דרשה PAT חדש (§12). כשל קריאת API מדווח 🟠 ולא שותק.

- **FIN קבוצה 1 — שלמות שרשרת הנתונים** (FIN-039 · FIN-040 · FIN-041). שלושתם
  **מניעתיים במלואם**: מדידה חוזרת מול פרודקשן הראתה `status='CLOSED' AND exit IS NULL`
  = **0** ו-36 שורות דמו שעדיין `is_demo=true` — אפס backfill, אפס `UPDATE`, אפס DDL.
  `tradeFromSupabase` נולד כפונקציה אחות ל-`tradeForSupabase` באותו קובץ (האסימטריה
  הייתה הבאג); `isDemo: undefined` **משמיט** את העמודה במקום לכתוב `false`, כך ששמירה
  לא יכולה לדרוס דגל שהיא לא מכירה. `cleanTrades` הפסיקה להמציא `false` — היא זו
  שמפשיטה `SIM-`/`Hive-`, כלומר ההיוריסטיקה מחקה בעצמה את הקלט שלה ועבדה פעם אחת
  בלבד. `exitDate` (שדה שמעולם לא היה לו עמודה) חובר ל-`closedAt` דרך `deriveCloseState`
  — `status` **נגזר** מנוכחות `exit`, ולכן `CLOSED` בלי `exit` אינו בלתי-רצוי אלא
  בלתי-ניתן-לביטוי. `tradeForSupabase` צועק `console.error` על עמודה לא-מוכרת (המנגנון
  ששתק ובלע את `exitDate`), ו-`test:datachain` (22 אסרשנים) נכנס ל-`verify` לפני `build`.
  חילוץ ללוגיקה טהורה בלבד: `src/lib/cleanTrades.js` · `src/lib/tradeCloseState.js` ·
  `src/data/tradeEnums.js` — אף קומפוננטה לא פורקה, אף מחרוזת `value` לא שונתה
- **GA4 + Consent Mode v2 + באנר הסכמה + /privacy** (`G-VC8PKL4NL1`): בלוק ב-`index.html`
  עם `consent default` **denied** כ-`dataLayer[0]` ללא תנאי + שחזור אישור שמור ב-head
  (כדי שמשתמש חוזר לא יאבד את ה-pageview הראשון). `src/lib/consent.js` — מקור-אמת-אחד
  ל-`swingEdgeConsent` (`{v,analytics,ts}`), `revokeAnalytics` מוחק גם עוגיות `_ga*`.
  באנר עברי לא-מודאלי ב-`z-index:91`, מעוגן ל-inline-start ב-lane יורש-כיוון
  (`pointer-events:none`) — אי-חפיפה עם ה-FAB נכונה **מעצם המבנה**, לא בזכות z-index.
  שני הכפתורים חולקים מחלקה אחת (דרישה רגולטורית). `/privacy`: כפתור ביטול פעיל
  (`ConsentControl`) + הסבר מפורש למה `@vercel/analytics` אינו מגודר.
  **הגנת זיהום דאטה:** route-abort ל-googletagmanager בשלושת ה-specs + קליק
  `consent-decline` ב-sentinel-auth, אחרת ~100 pageviews סינתטיים/יום מול 29 משתמשים.
- **רוטציית Discord webhook** — בוצעה ידנית 27.07, אומתה חי (☀️ ב-13:39)
- **S2 — מחזור מלא ראשון בפרודקשן** (26.07 23:16, ריצה ידנית, `411d1dc`): לוגין →
  hydration → יומן → יצירת SNTNL → טאבים → מחיקה → ניקוי REST. ✅ התאוששות דווחה.
  שפיות ביומן: 3 עסקאות בדיוק (AAPL / NVDA / BTC-USD), אפס SNTNL
- **S2.3 — סינון 404 של לוגו הסימבול**: `financialmodelingprep.com` נוסף ל-`IGNORE_SOURCE`
  (חסימה לפי מקור בלבד, לעולם לא לפי הטקסט "404"). מסלול ה-network נבדק ולא שונה —
  host צד-שלישי אינו נכנס ל-`failedReq` ולא ל-`supabaseFailedReq`. **אומת חי**: ריצת
  Sentinel `30253015899` (2026-07-27T09:13:31Z) מכילה `AUTH_EXPECTED: 1` — שכבת ה-auth
  אכן רצה, ההתאוששות שדווחה אמיתית (ראה `DECISIONS.md` 2026-07-27).
- **Watchdog + ספי Growth Pulse + פידבק ל-Discord**: `watchdog.yml` חדש — 4 קטגוריות
  (טרי/מאחר/מעולם לא הצליח/הבדיקה עצמה נכשלה) + בדיקת עקביות עצמית מול טבלת ה-max-age,
  דיווח יומי תמיד (fallback 🔴 אם צעד הבדיקה קרס). `fleet-daily.yml` Growth Pulse: 3 ספי
  🟠 (0 עסקאות ב-48ש' / 0 נרשמים ב-72ש' / activation מתחת ל-25%), אפס state חדש. פידבק
  משתמש לא-פתור מוצג עכשיו גם בדיסקורד (`daily-digest.yml`, כתום + snippet מסונן).
  `restore-drill.yml`: רבעוני → חודשי. `failure-alert.yml`: Watchdog נוסף לרשימת המעקב.
- **ניקוי מאוחד** (`665c3a7` תוכנית, ביצוע בקומיט הבא): `npm audit fix` תיקן
  `brace-expansion`; `react-router` high נותר במכוון (CVE לא ישים — ראה `DECISIONS.md`
  2026-07-27) · try/catch fail-open ל-`api/_lib/rateLimit.js` · timeout 8s
  ל-`api/verify-turnstile.js` (קריאה חיצונית אחת בלבד, לא שתיים כפי שהונח) · דיווח
  דיסקורד נוסף (לצד מייל קיים) ל-8 סוכנים: analyst, arch-auditor, daily-digest,
  data-guardian, restore-drill (×2), triage, failure-alert, Supabase Backup ·
  `actions/upload-artifact`/`download-artifact` שודרגו ל-`@v7` ב-3 קבצים (backup,
  smoke, sentinel) · smoke.yml הושאר בלי דיווח דיסקורד במכוון (גם מופעל על כל push ל-main)
- **Sentinel #6** — התאוששות שקרית + לכידת ראיות (`54713d3`, `411d1dc`)
- **סיבוב `BACKUP_PASSPHRASE` + Restore Drill** — 222 שורות ב-11 טבלאות אומתו
- **כל 7 תקלות Sentry סומנו Resolved** — הלוח נקי
- שער אישור waitlist + שולח קמפיין
- הגירת Fleet ל-Actions 24/7
- קריסת Journal (60/137 עסקאות בלי `stop`)

## ⚠️ סיכונים פתוחים

- **🆕 אזהרת Node.js 20 מיושן ב-Actions (01.08).** התגלתה בריצת Restore Drill #4.
  `restore-drill.yml` אינו מריץ `actions/checkout` כלל — שני ה-`uses:` היחידים שלו
  הם `dawidd6/action-send-mail@v3`, וזהו מקור האזהרה. **הבעיה אינה מקומית לקובץ
  אחד:** `dawidd6/action-send-mail@v3` מופיע ב-**8 workflows** (9 שימושים), ובנוסף
  יש **30 שימושים ב-actions רשמיים `@v4`** (`checkout` / `setup-node`) שכולם רצים
  על node20. **לא דחוף — עדיין רץ, רק מזהיר.** השדרוג (`@v5` לרשמיים, גרסה חדשה
  ל-send-mail) הוא משימה נפרדת שנוגעת בכל ה-workflows בבת אחת ולכן לא נעשה "בדרך"
  (§11). ⏭️ מועמד למשימה ייעודית
- **🆕 `avgMaeMfe` הוא export מת (01.08).** `grep -rn 'avgMaeMfe'` → **1/1 מופעים =
  ההגדרה עצמה**. תוקן במקבץ 01.08 (המכנה השגוי פורק), אבל **הפונקציה עדיין אינה
  נקראת מאף מקום.** או שיחווטו אותה, או שתימחק — export שאיש אינו קורא הוא קוד
  שמתיישן בלי שאיש ישים לב, ובדיוק כך נולד המכנה השגוי מלכתחילה. ⏭️ החלטת ניב
- **🆕 `Number(...) || 0` בארבעה שדות טופס ב-`DecisionCoach.js` (01.08).** `:18-20`
  (`entry`/`stop`/`target`) ו-`:542-544` (`shares`/`capital`). **נבדק ולא תוקן במכוון:**
  אלה קלטי טופס גולמיים בזמן הקלדה, שבהם `"" → 0` הוא ההתנהגות הנכונה, ולא נתונים
  שנשמרו. משפחה דומה מבחינה תחבירית, בעיה שונה מבחינה סמנטית. נרשם כדי שהחיפוש
  הבא לא יגלה אותם מחדש כ"ממצא"
- **🆕 `api/ocr.js` — אפס תצפיתיות (F5).** הצהרת `console` אחת בכל הקובץ
  (`:310`, `[rate_limited]`). אין רישום של `hasPositionTool`, `confidence`,
  `computed.source` או התוצאה. **גם כשה-OCR נקרא, לא נשאר עקבות למה החזיר —
  ולכן למדידת ה-30 לא יהיה מקור אמת בצד השרת להצליב מולו.** חוסם את פריט 13
- **🆕 `entry` מ-Vision במסלול שאינו ניתן ל-cross-check (F4).** `api/ocr.js:436`
  נופל ל-`visionEntry` כשהאחוזים לא נקראו. השער ב-`:458-460` דורש
  `stopChecked`/`targetChecked`, שדורשים `stopPercent !== null` — אבל
  `source === "none"` נובע *מכך* שהאחוזים חסרים. **התנאים סותרים: המקרה היחיד
  שמשתמש ב-entry של Vision הוא בדיוק המקרה שבו השער לא יכול לירות.** מסלול שני,
  עצמאי, שמייצר "מחיר נוכחי כ-entry". ⏭️ אחרי מדידת ה-30
- **🆕 אין סימון מקור עסקה בסכימה (F6) — OCR אינו מדיד בדיעבד.** 25 העמודות
  ב-`public.trades` אינן מתעדות OCR מול ידני, ותמונת הגרף אינה נשמרת.
  **שיעור שדות ריקים בעסקאות-OCR מול ידניות אינו ניתן לחישוב — הנתון לא קיים.**
  מדידת ה-30 חייבת להיות פרוספקטיבית; אין בסיס היסטורי
- **🆕 `/api/ocr` — 0 קריאות בחלון הלוגים הנשמר (01.08).** מכנה: 24h ו-7d
  מחזירים כמעט אותם מספרים (152 מול 151 `/api/quote`) → **השימור הוא ~24 שעות,
  לא 7 ימים.** הטענה המדויקת: 0 קריאות ב-~24 השעות שנשמרו. ניסוי ביקורת אישר
  שהלוגים כן קולטים את הנתיב (2 בדיקות שלי הופיעו מיד). **לא ניתן לאשר שהבקשה
  של עומרי הגיעה לשרת כלל** — ייתכן שאף משתמש אינו מגיע לנקודה בפועל.
  דורש אימות חי לפני מסקנה מוצרית
- **🆕 `routeChartOcr` יעד `position` דורס שדה שמולא ביד (01.08).**
  `SwingEdge_App.jsx:2565-2566` כותב `result.entry != null ? … : f.entry` **בלי**
  שומר `f.entry ||`, בניגוד לשלושת מסלולי ה-OCR האחרים (`:2419`, `:2461`, `:2573`)
  שכולם מכבדים ערך ידני. חוסר-עקביות ודאי; התיקון הוא הוספת `f.entry ||`.
  **ממתין להכרעת ניב** ב-`PLAN-2026-08-01-ocr-f123` §9 שאלה 3
- **🆕 מילוי המחיר החי ב-`posCalc` מוגן מ-OCR במקרה בלבד (01.08).**
  `SwingEdge_App.jsx:4377-4385` יורה מתוך `onChange` של קלט הטיקר, ו-OCR מגיע
  ל-`posCalc` דרך `setPosCalc` ב-`routeChartOcr:2560` — נתיב אחר, ולכן ה-`onChange`
  אינו נורה. **אין כאן שומר, רק צירוף מקרים.** חיווט עתידי של השדה ל-`useEffect`
  (כמו שני הטפסים האחרים) יפתח מחדש את באג F1 בדיוק.
  🟡 **מותן ב-01.08 להערה בקוד** (`routeChartOcr`, ענף `position`) שאומרת בדיוק
  את זה. הערה אינה שער — הסיכון נשאר פתוח
- **🆕 `ocr_result` ב-GA4 נספר רק על מסכימים (01.08).** `trackEvent` יוצא מוקדם
  בלי `analytics: granted`, ולכן **המכנה של כל שיעור שייגזר מהאירוע הזה הוא
  אוכלוסיית המסכימים, לא כלל המשתמשים** (§2 — אין מנה בלי מכנה). כל דיווח חייב
  לומר "N קריאות OCR **בקרב מסכימים**". אין בריפו מדד לשיעור ההסכמה עצמו, ולכן
  **גודל ההטיה אינו ידוע** — לא ניתן להסיק ממנו על שיעור הכשלים האמיתי
- **🆕 גבול מול סשן מקבץ-התיקונים (01.08).** פריט 6 שלו צומצם למסך האישור בלבד;
  `SwingEdge_App.jsx` (טפסי OCR) ו-`api/ocr.js` היו בבעלות בלעדית של המשימה הזו
  עד ה-push. **אחרי ה-push הבעלות משוחררת** — הסשן ההוא חייב `git pull` לפני
  שהוא נוגע באותם קבצים
- ~~**🆕 `CLAUDE.md` — אין `§10.2` (01.08)**~~ ✅ **נסגר 01.08.** הכלל נכתב
  (`CLAUDE.md §10.2 — אין פידבק יתום`): שלושה מצבי סיום בלבד — משימה ב-`STATE.md`
  עם ציטוט · טופל עם hash · נדחה עם נימוק כתוב. `Reviewed` מוגדר במפורש כמצב
  ביניים ולא כסיום
- **🆕 חמישה מסלולי "סגור"/win-rate **מחוץ** ל-`SwingEdge_App.jsx` ו-`src/components/`
  שלא אוחדו ב-T3** (מחוץ להיקף המוצהר — קבוצה 2 כיסתה את מסלול התצוגה, לא את
  מנועי המשנה):
  `src/intelligence/core/TradeDNA.js:185` · `src/intelligence/core/MonthlyReport.js:158,372` ·
  `src/components/GrowthPredictor.jsx:204` · `src/import/normalizeRow.js:107` ·
  `src/components/EditTradeModal.jsx:37`.
  שלושת הראשונים מחשבים win-rate בקונבנציה משלהם; שני האחרונים מסנני `status`
  בלבד. **אין סתירה חיה היום** (0 עסקאות `CLOSED` בלי `exit` ו-0 BE בפרודקשן),
  אבל החוזה אינו נאכף שם. ⏭️ מועמד לקבוצה 4
- **קובץ תוכנית נדחף ריק 29.07** — `head -n -8` אינו נתמך ב-BSD/macOS, ההפניה יצרה
  קובץ 0 בייט, והקומיט+push רצו בשורה נפרדת ולכן לא נעצרו. **לאמת גודל/תוכן אחרי
  כתיבה, לא רק שהקומיט עבר.** תוקן בקומיט נפרד (`e38877d`), לא ב-`--amend`.
  **הכללה: `head` של BSD אינו תומך ב-`-n` שלילי כלל** — כל סקריפט שנכתב מול הרגלי
  GNU עלול להיכשל כך בשקט על מק, ולייצר קובץ ריק במקום שגיאה חוסמת
- **14 עסקאות `CLOSED` עם `closedAt IS NULL`** (כולן `is_demo=false`). עוברות את
  אינווריאנט FIN-041 — יש להן `exit` — אך שקופות לכל מסלול מבוסס-`closedAt`
  (`holdDays`, `TradeCalendar`). לא תוקן: החלפת קוראי התאריך היא **קבוצה 4**
- **`entryQuality` הוא `text` ב-DB בעוד הקוד כותב לשם מספר** (`entryQuality: 3`).
  אומת מול `information_schema`, לא הוסק. סחיפת מחרוזת-מול-מספר → **קבוצה 2/7**
- **`status` מוגדר `default 'open'::text` בלוויאנית**, בעוד כל השוואה בקוד היא
  `"OPEN"`. אפס שורות כאלה היום (כולן נכתבות עם `status` מפורש), אבל כל `insert`
  עתידי בלי `status` ייצור שורה שאף מסנן בקוד לא יתפוס. **מלכודת רדומה**
- **`createdAt` של עסקאות מיובאות מקדים את יצירת החשבון.** 25 עסקאות עם
  `"createdAt" < auth.users.created_at` — המייבא כותב את התאריך מה-CSV ולא את זמן
  הכתיבה בפועל. שלוש תוצאות: (א) כל מדד "מה קרה ב-24 השעות האחרונות" מפספס אותן;
  (ב) כל funnel לפי `createdAt` גולמי מראה פעילות *לפני* ההרשמה; (ג) חלון-זמן מסודר
  אינו מזהה ייבוא כלל — 173 העסקאות של המקרה הידוע פרושות על פני 9 חודשים.
  **לא מתוקן במכוון** — כתיבה מחדש של `createdAt` היסטורי היא שינוי נתונים, לא דוח.
  `scripts/user-analytics.mjs` מנטרל זאת ב-`GREATEST("createdAt", acct_created)`
  ומזהה ייבוא בשלושה סימנים ב-OR. ראה `docs/plans/PLAN-2026-07-27-user-analytics.md`
- **אל תסמוך על `rows` מ-Supabase MCP `list_tables` כמדד ספירה.** הערך הוא
  `pg_class.reltuples` — הערכת planner, לא ספירה, ומתעדכן רק ב-autovacuum.
  **כל מספר שנכנס לדוח חייב לבוא מ-`count(*)`.** הכלל עומד בפני עצמו ואינו הסבר
  לשום הפרש ספציפי שנצפה ב-27.07 — ראה השורה הבאה
- **27.07 22:30 — משתמש `a0556783290` מחק את כל 173 עסקאותיו** (תוצר ייבוא IBI
  כושל). `trades`: 285→114, משתמשים-עם-עסקאות: 10→9. הפרשי ספירה שנצפו במהלך הערב
  נבעו **משינוי אמיתי בנתונים, לא מכלי המדידה** — שני נתיבי הקריאה (psql ו-MCP)
  היו נכונים בכל רגע, ואומתו כזהים בריצת dispatch (114 בשלוש ספירות רצופות).
  **לקח: הפרש בין שתי מדידות — לבדוק קודם מה השתנה בנתונים, ורק אחר כך לחשוד בכלי**
- **מחיקה שקופה לחלוטין בדיעבד.** אין `deleted_at`, אין `updatedAt` — שום שאילתת
  חלון-זמן לא יכולה לראות ש-173 שורות נעלמו. איש לא ידע במשך ~3 שעות. זו הסיבה
  ש-`user-analytics.yml` שומר ספירות מצטברות ב-Actions cache, ומאז 27.07 גם מתריע
  🟠 על שינוי נפח חד (>20% מול אתמול) — מחיקה של 60% מהטבלה חייבת לצעוק
- **✅ מדידה 28.07 אחרי מחיקת 173 השורות: 20.2% מכלל העסקאות בלי `stop`**
  (29.5% בנטרול 36 הדמו), אך **החציון בין משתמשים הוא 0.0%** — 22 מתוך 23 החריגות
  אצל משתמש בודד. FIN-007 תקף כבאג מתמטי אך אינו התנהגות רוחבית. ⚠️ הסדר נגזר מכך:
  קבוצה 3 (חוזה ה-null) חייבת לקדום לתיקון הייבוא — כל עסקה מיובאת נכנסת בלי `stop`,
  ולכן הייבוא יחזיר את ההטיה לסדר גודל מלא על נתונים שהמשתמש סומך עליהם.
  **הלקח הכללי: "שיעור מצרפי" על אוכלוסייה של 9 משתמשים אינו מדד התנהגות — הוא נשלט
  ע"י המשתמש עם הכי הרבה שורות.** כל סף מוצרי עתידי נמדד בחציון פר-משתמש, לא בשיעור
  מצרפי. המספרים בראש §6 באודיט
- **~~🆕 אותה משפחה, טרם טופל — שני אתרי `?? 0` על R שנשארו מחוץ לקבוצה 3~~ ✅ נסגר 01.08.**
  שני האתרים תוקנו במקבץ התיקונים הקטנים. **הרישום המקורי כלל שתי טענות שגויות,
  שאומתו מול הקוד ומתוקנות כאן ולא נמחקות** (§2 — הוכחה, לא זיכרון):
  · **`src/intelligence/utils/psychologyPatterns.js:117` — לא היה `?? 0` ולא היה על R.**
    הקוד בפועל היה `Math.abs(Number(t.maxAdverse) || 0)` — כלומר `|| 0` על **MAE/MFE**,
    לא `?? 0` על R. אותה משפחת באג (ברירת מחדל שמחליפה נתון אמיתי), ערך אחר לגמרי
  · **"לא נצפה במסך ישירות, אבל מזין לקחים שכן נצפים" — שגוי.** `avgMaeMfe` אינה
    מזינה דבר: `grep -rn 'avgMaeMfe'` מחזיר **1/1 מופעים = ההגדרה עצמה**, אפס import
    ואפס קריאה. התיקון אינו תיקון להתנהגות שמשתמש רואה היום — הוא **פירוק מוקש לפני
    שמישהו יחווט אותו** ויירש את המכנה השגוי בשקט
  · **`SwingEdge_App.jsx:1642-1645`** — התיאור כאן היה מדויק, ותוקן: המסנן היה כופה
    `null` להשוואה מספרית, ולכן "R מעל 1" הסתיר עסקה חסרת-סטופ בעוד "R מעל 1-" השאיר
    אותה, בלי שום סימון על המסך
- **🆕 דפוס — מנה עם מכנה שגוי.** 27-28.07: שלוש פעמים נמדדה מנה עם מכנה שגוי
  (61% בלי stop · 47% אובדן דמו · 33% שדות סגירה). בכל המקרים המונה היה נכון והמכנה
  כלל אוכלוסייה לא רלוונטית. כל מנה בדוח או בפרומפט חייבת לציין מכנה.
  **נאכף מאז 28.07 בשתי רמות:** `CLAUDE.md` §2 "אפס מנה בלי מכנה" (חל על דוחות,
  שאילתות אד-הוק ופרומפטים — שם נולדו שניים מהשלושה), ו-`assertRatiosCarryDenominator()`
  ב-`scripts/user-analytics.mjs` (זורק, לא מדרדר — הריצה יוצאת 1 והדוח לא נשלח).
  הכתיבה המלאה, כולל התיקון העצמי 80/30→60/60 כהדגמה של הכלל על עצמו: `INCIDENTS.md` #9
- **36 עסקאות הדמו אינן בסיס בדיקה תקף לקבוצות 2–3.** מילוי 100% בכל שדה אופציונלי
  (`lessonLearned` 36/36 · `stop` 36/36 · `notes` 36/36) מול ~60% `lessonLearned`
  בעסקאות האמיתיות — חתימה של סדרה מוזרעת, לא של מסחר. כל תיקון סטטיסטי שייבדק מולן
  ייראה ירוק על התפלגות שאינה קיימת בפועל
- **ה-pageview הראשון של משתמש שמאשר הוא חסר-עוגייה** — האישור מגיע אחרי ה-`g/collect`
  הראשון ו-GA4 לא שולח מחדש. landing → הרשמה ממודלת ולא מיוחסת בעמוד הראשון.
  **מקבלים במודע** — `page_view` ידני היה יוצר ספירה כפולה
- **`OnboardingTour` (`fixed inset-0 z-[120]`) מכסה את הבאנר** בדיוק למשתמש החדש.
  הבאנר נשאר שם אחרי הסיור. **אין להעלות את z-index של הבאנר מעל 120**
- **CSP עתידי ישבור את שני ה-inline scripts ב-head** (theme resolver + GA). יידרשו
  nonce/hash + `script-src` ל-googletagmanager + `connect-src` ל-`*.google-analytics.com`
- שכבת auth רצה פעם בשעה בעוד הדיווח כל 20 דק' → אזור עיוור מובנה
- `core.hooksPath` הוא config מקומי — clone חדש נשאר בלי הגנת hooks, בשקט
- `smoke.yml` לא מקבל צעד דיסקורד (במכוון — גם מופעל על כל push ל-main, לא רק
  cron). ריצה ירוקה שקטה לגמרי; רק כשל מדווח (דרך `triage.yml`'s workflow_run watch)
- `actions/download-artifact@v7` מול `upload-artifact@v7` ב-`sentinel.yml` (שורות 93,
  117) טרם אומת בריצה אמיתית ב-Actions — v4 עלה ל-v7 בשני הצדדים יחד, אך אין הרצה
  שאישרה שההורדה עדיין תופסת את ה-artifact
- `api/send-invites.js:133` (`reportDiscord`) קורא `SENTINEL_DISCORD_WEBHOOK` שאינו קיים
  ב-Vercel env — `if (!webhook) return;` יוצא בשקט. הדיווח 📧 מעולם לא ירה משם; מה שנצפה
  ב-25.07 הגיע מ-`email-campaign.yml`
