# STATE — מצב חי

> קובץ **מצב**, לא ארכיון — מה שנסגר נגזם, לא נצבר. **תקרה קשיחה: 100 שורות.** חריגה אינה "קובץ מפורט", היא גיזום שלא נעשה. היסטוריה → `docs/audits/STATE-ARCHIVE-2026-08.md`.
> **כלל HEAD:** כל קומיט שנוגע בקובץ הזה מעדכן את שורת HEAD **באותו קומיט** (נחקק 05.08, אחרי ששורת HEAD פיגרה מאחורי הקומיט החי).

עודכן: 2026-08-08 · HEAD: 014bb15 — ⚠️ מפגר בקומיט אחד **מעצם הגדרתו** (אין hash לפני commit, ו-amend אסור). `git log -1` הוא האמת
⚠️ **סחיפת תאריכים — הוכרע 06.08: `git log` הוא מקור-האמת, שמות מסמכים נגזרים ממנו.** קיימת ב-`INCIDENTS#12` וב-**3/3 מ-`docs/plans/PLAN-2026-08-06-*`** (`cleanup`·`user-visibility`·`workflows`) שנוצרו כולם **05.08 +0300**. ⛔ **לא לשנות שמות קיימים** — שובר את ההפניות. זו רשומה, לא תיקון

**מדדים חיים (07.08, נמדד):** 41 נרשמו · **59 עסקאות** (▲3 ב-6ש — כולן של `de66dc99`) · 12 עם עסקאות · 26 סגורות · 0 שאינן USD · 0 mentorships · 5 פידבקים פתוחים · 31 תקועים (22 `joined == last_seen`) · **activation 12/41 = 29%** · power users 2.

🔴 **שימור שבוע 1 = 0/6** (`scripts/retention.sql`, 06.08). מתוך 9 בני-מדידה, 6 פעילים בשבוע 0 ו-**אפס** חזרו בשבוע 1; 5/9 רשמו עסקה תוך שעה. **הבעיה אינה האונבורדינג — היא היום השני.** ⏭️ הגל הבא

🎉 **`de66dc99` — אחד מחמשת נמעני `why_stopped` — חזר אחרי 9 ימים והזין 3 עסקאות** (ZIM/PATH/ONDS, 07.08 13:45-13:49 UTC). **ההחזרה הראשונה של הקמפיין. 1/5 נמענים חזרו** (המכנה: 5 שקיבלו `why_stopped`). ⚠️ **המדד: קמפיין נמדד בכניסות חוזרות, לא בתשובות במייל. 0 תשובות ≠ 0 אפקט** — לולא נמדדו העסקאות, הקמפיין היה נרשם ככישלון

## 🔴 עכשיו

🔴 **`navigator.clipboard` בלי בדיקת קיום — כשל שקט במסלול המנטורינג (05.08).** `SwingEdge_App.jsx:2608` — `undefined` בהקשר לא-מאובטח וב-WebView אנדרואיד: הקריאה זורקת, ה-`catch` בולע, **הקוד לא הועתק והמשתמש לא יודע** (§2). **0 mentorships** — זו הסיבה, או שאיש לא ניסה? ⛔ אין דרך להבחין בלי טלמטריה

🔴 **`github-script@v9` — 0/3, טרם הורץ על runner** (הוצמד ב-`03c343e`). ריצה ראשונה צפויה `data-guardian` **07.08 05:00 UTC**, אחריה `arch-auditor` **09.08 05:00** ו-`analyst` **09.08 06:00**. שלושתם נתיבי **דיווח** — אם v9 שבר אותם הכשל שקט מהגדרתו. ⛔ לא להריץ ידנית: הרצה מחוץ להקשר אינה מוכיחה את המסלול

---

## ⏭️ הבא בתור

**הרצף הושלם:** U → R → W → **S2 ✅** → **ידית `/api/notify` ✅** → **מחיקה מאומתת ✅** → **מחלקת הכתיבה 7/7 ✅** → עצירה.

**סדר העדיפויות מכאן — עודכן 08.08 אחרי אבחון ההון:** 1. 🔴 **אבחון מנוע ההון והשער — סשן נפרד, ראש נקי** · 2. **תיקון ההון לפי האבחון** · 3. 🔴 **GA4 — מדידת מסכים פנימיים** · 4. **קמפיין ל-12 שהתחילו ונטשו** + `why_stopped_r2` ל-21 · 5. ניגודיות הבאנר · `send-mail` v18 · אספן CSP · 42 `catch` · fontshare. ⚠️ **GA4 הוקדם מעל הקמפיינים במכוון:** אין טעם לשלוח עוד מיילים כשאי-אפשר למדוד מה קרה למי שכבר בא

🔴 **מנוע ההון והשער — 7 סתירות באבחון 11 צילומי מסך** (משתמש בדיקה, ₪10,000, 08.08): (1) "התחלה $3,330" מול "הון חשבון $3,330.20" — שני עיגולים לאותו ערך ב-0 עסקאות סגורות · (2) השער המוצג 0.3330 אינו משחזר את המספר (59,999×0.3330=$19,979.67 מול $19,980.87 מוצג; בפועל 0.33302) · (3) "$99.91 מתוך ההון $3,330" — 3%×3,330=$99.90: **בסיס מעוגל, תוצאה לא** · (4) אונבורדינג מציג "עמלה $0" כשנבחר ILS — ערבוב מטבעות · (5) שער מתוארך 07.08 מוצג ב-08.08 · (6) "סיכון לעסקה 1%" מול "מקסימום סיכון מותר 3.0%" — היחס לא מוסבר, **ומאיפה 3% כשהאונבורדינג הראה 1%** · (7) צירי עקומת ההון (3,397/3,333/3,298/3,263) — **אף אחד אינו 3,330.20, הערך היחיד שקיים**
⛔ **הביצוע: אבחון בסשן נפרד לפני כל תיקון** — לא נגענו. 📌 האינווריאנט כבר הוכרע (`DECISIONS` 08.08): מטבע הבסיס = המטבע שהמשתמש בחר להונו · שער נקפא בכל עסקה בזמן האירוע ולא מתעדכן לעולם · מטבע התצוגה הוא שכבת תצוגה בלבד. **תזוזת שער אינה רווח**

🔴 **GA4 עיוור בתוך האפליקציה — SPA עם `page_title` אחד ל-122 views, ו-key events = 0.** 18 סשנים הגיעו מ-Email (קמפיין `why_stopped`) ו**לא ידוע לאן הגיעו בתוך האפליקציה** — זו בדיוק השאלה של 21 התקועים. נדרש: `screen_view` לכל מעבר טאב + אירוע פתיחת טופס. ⛔ אפס PII

⚠️ **הליבה במענה היא טקסט בלבד — אין קישורים ואין markdown, במכוון** (כל תו נמלט; parser markdown בשרת מחזיר HTML מקלט לתוך מכתב ממותג — בדיוק מה ש-`escapeHtml` מונע). ⏭️ אם יידרש קישור — **שדה נפרד** עם allowlist דומיין, לא הרפיית ההמלטה. ⚠️ **והליגר אינו רושם באיזו תבנית נעשה שימוש:** `<h8>` חד-כיווני, ולכן ניתן להוכיח **שנשלח ומתי**, לא **מה נאמר**. ⏭️ אם תישאל — עמודה, לא הרחבת המחרוזת
⏭️ **אספן CSP — גל נפרד.** בלי `report-uri`/`report-to` אין מה לאכוף ואין מה לאמת (ראה ⚠️). ⛔ תנאי: ההפרות **לא** ינחתו ב-artifacts של Actions — הריפו ציבורי והן מכילות URLים של משתמשים.
⏭️ **Turnstile ל-`waitlist` — נבחן והוכרע 08.08: ⛔ לא מיישמים כעת.** ⚠️ **"התבנית כבר בריפו" הטעה** — `api/verify-turnstile.js` אכן קיים, אבל **`api/waitlist.js` אינו קיים**: ההרשמה היא insert ישיר מהדפדפן (`LandingPage.jsx:537`), ואין צד-שרת לאמת בו. העלות האמיתית: endpoint חדש · **2 מיגרציות** (סגירת `waitlist_anon_insert` + RPC כתיבה) · חלון פריסה שיכול לשבור את **מסלול הכניסה היחיד** · סקריפט צד-שלישי בדף שכבר נמדד ב-1.46ש חסימת רינדור. מנגד הנזק תחום: 60 שורות, קריאה לאדמין בלבד, ו-`approved_at is null` ב-RLS כבר חוסם הסלמה למייל — **התקרה היא ספירה מנופחת**. ⚠️ ה-fail-open הנדרש גם מוריד את תקרת התועלת. **התנאי: ייבנה אם וכאשר `waitlist` הופך יעד ספאם בפועל, לא לפני.** ⛔ **לא** לפידבק: משתמש מתוסכל לא יעבור CAPTCHA. `DECISIONS` 08.08
⏭️ **מדידת עלות ל-`api/ocr.js`** — ראה ⚠️; דורש טבלה, כלומר מיגרציה
⚠️ **שני מסלולי העלאת תמונה שולחים את הקובץ הגולמי — בלי הקטנה ובלי בדיקת גודל.** `handleImageUpload` (`SwingEdge_App.jsx:2860`) ו-`handleChartFileFallback` (`:3196`) קוראים `readAsDataURL(file)` ישירות, בעוד `grabChartFrame` (`:3078-3088`) **כן** מקטין ל-≤2000px ומקודד מחדש מול תקרת 6MB. צילום מסך מאנדרואיד מודרני עובר את התקרה בקלות → `400 image_too_large` מהספק. **נסרק בגל ה-OCR של 08.08 ולא תוקן בו במכוון** — נגיעה ב-`SwingEdge_App.jsx` הייתה מחוץ להיקף המשימה (§12). ⏭️ הפתרון הוא לחלץ את ההקטנה של `grabChartFrame` לפונקציה אחת ולקרוא לה משלושת האתרים
⚠️ **הפרומפט של Vision סותר את עצמו סביב מקור ה-entry, ו**במכוון** לא תוקן.** `api/ocr.js:226-230` אוסר על המודל לקרוא את ה-entry מסקאלת המחיר, בעוד `:236-237` **מחייב** לקרוא כך את ה-stop/target. ההתרה נבחנה 08.08 ונפסלה: היא הייתה מזרימה קריאת-סקאלה בלתי-מסומנת ל-`visionEntry` — ערוץ שכבר גובר על הערוץ הדטרמיניסטי החדש — ולכן דורשת גם דירוג פנימי בתוך `visionEntry`, כלומר שדה חדש בסכימה ותרחישים קפואים נוספים. ⛔ לא בגל אחד עם הערוץ השלישי
⏭️ **Stripe — המחירים נעולים ומוצגים כבר: `$8`/חודש · `25₪`/חודש קבוע** (הוכרע 08.08, `DECISIONS`; הלנדינג ב-`22d6623`). ⛔ `$5` ו-`$7` **בטלים** — `$7` נפסל מפורשות. ⚠️ **הסתייגות "צד אחד" חוזרת לתוקף ברגע שהסליקה עולה**: היום אין קוד Stripe בריפו כלל (9 אזכורים, כולם `.md`; `package.json` = `NONE`), ולכן אין צד שני לסתור — משעולה חשבון גובה, כל שינוי מחיר חייב לנחות בשני המקומות באותו גל

**פידבק פתוח שאינו מכוסה בשום מקום אחר (§10.2, מוין 06.08):**
- 🟡 **ה-OCR קרא את המחיר הנוכחי כמחיר הכניסה ואת ה-SL השאיר ריק** (`omrikapara1`, 31.07, אנדרואיד). **שני חצאים, ושניהם טופלו — אך ללא אימות מהמדווח:** (א) **התסמין** "המחיר החי כמחיר כניסה" נסגר 01.08 ב-F1–F4 (`AUDIT-2026-08-01-ocr-diagnosis.md`, `DECISIONS` ×3) — הוא היה **צד-לקוח**, אפקט ציטוט חי, ולא `api/ocr.js`. (ב) **איכות הקריאה** נסגרה 08.08: `computeEntry` היה נעול על אחוזים גלויים (`api/ocr.js:139-147`), ולכן כלי פוזיציה מסומן כהלכה שתוויתו בלי אחוזים ותווית ה-entry שלו אינה קריאה החזיר `entry: null` — ו-`computeLevels` איפס אחריו **גם את ה-SL**. "ה-SL לא נקרא" היה **נגזרת, לא כשל נפרד.** נוסף ערוץ שלישי ואחרון בהיררכיה (`entryFromLines`), יורה רק כששני הקודמים `null` ורק כששתי קריאות מתכנסות ב-`GEO_TOL`; אי-התכנסות = `null`. תרחישים I+J, 10/10 קפואים, 8/8 הישנים ביט-בביט. `DECISIONS` ×2, 08.08
  ⏳ **מה שנשאר פתוח הוא האימות, לא התיקון:** **איזה** משלושת מסלולי ה-`null` עומרי נחת בו **אינו ניתן להכרעה** — הקובץ הכיל הצהרת `console` אחת וחלון הלוג ~24 שעות, ולכן 31.07 אינו ניתן לשחזור. התיקון סוגר פער מוכח שמייצר בדיוק את התסמין שלו — **לא ייחוס מאומת**. ⛔ אין לרשום "נסגר" עד תשובת אנדרואיד ממנו (ראה ⏸️). ⛔ **ולא נשלח אליו מייל שני** — התיקון בקוד ובפרודקשן אינו עילה לפנייה: הוא כבר נשאל פעם אחת וטרם ענה, ומייל "תיקנו, תבדוק" הוא תזכורת בתחפושת. **האות שממתינים לו הוא תשובתו, לא שליחה נוספת שלנו** (`DECISIONS` 08.08)
- הגדלת תמונת הסטאפ בלחיצה · כמה צילומים ברצף (אחרי ה-OCR) · העלאת קובץ בטופס הפידבק עצמו

**קמפיין ל-12 שרשמו עסקה ראשונה ונטשו** — "עשית עסקה ולא חזרת. מה היה חסר?" שאלה אחרת מ-`why_stopped`: 0/6 שימור שבוע 0→1 אומר שהתשובה אצלם, לא אצל התקועים. ⛔ רישום בלבד, אין תבנית · **`dawidd6/action-send-mail` v3→v18 — גל נפרד משלו** (15 גרסאות ראשיות × 9 אתרי קריאה, אחד מהם התרעת הכשל של `restore-drill`; ⛔ הוצא מ-W: "גל אחד, שינוי אחד")

נוספים, מחוץ לרצף: **W — project מובייל (Pixel 5) מכסה 1–2 מתוך 6, לא שש** (בעיקר #5 `parseUA`; אמולציה משנה UA/viewport ו**אינה מסירה יכולות**, #4 דורש WebKit/מכשיר ⏸️) · ⏭️ בדיקות **הסרת** יכולת ל-#2/#3 (`addInitScript` + `delete`) · RPC פר-מטבע `avg_pnl_by_ccy` · ספי `generateProfile` מכוילים לדולר · `admin_rpcs.sql:267-268` `else 0` על מכנה ריק (§2) · שמות CSV ב-AdminPanel ב-UTC ולא מקומי

---

## ⏸️ חסום / ממתין לניב

- ✅ **הידית עבדה בפרודקשן — 2 מענים אומתו בליגר (07.08).** `reply:eebd6bb1` **12:06** ל-`a0556783290` ("הקבצים שלך התקבלו", השליחה הראשונה דרך הפאנל) · `reply:f95b837e` **19:09**. **5/5 הפידבקים מ-27.07-31.07 נענו** (המכנה: הפידבקים הפתוחים שמוינו 06.08). ⚠️ **שתיהן בצורה הישנה** — נקראות כ-`n=1`, ממשיכות להיספר בפאנל (אסרציה 18), ו**אינן חוסמות** תשובה חדשה: אין להן טביעת תוכן, ואיננו יודעים מה נכתב בהן
- ⏳ **ממתינים לאות, לא למשימה:** תשובת עומרי על `fix_mobile_upload` — **אימות אנדרואיד הוא הדבר היחיד שיסגור את באג ה-OCR** — שני חצאיו תוקנו (01.08 + 08.08), ואף אחד מהם לא אומת מולו (ראה 🟡 בפידבק למטה) · המשך פעילות `de66dc99` — **⏳ ביקור, טרם שימור: 3 עסקאות ביום אחד ואפס מאז, ולכן המדד נשאר 0/6.** ⚠️ שימור הוא חזרה **ביום אחר**, וזה בדיוק מה ש-0/6 שבוע 0→1 קיים כדי לתפוס. ⛔ אין לרשום 1/5 כשימור. ⚠️ **סתירת תאריך שלא הוכרעה:** הפרומפט של 08.08 אמר "3 עסקאות ב-06.08", והשורה הנמדדת למעלה אומרת **07.08 13:45-13:49 UTC**. ⛔ לא נמדד מחדש בגל הזה — לא לצטט אף אחד מהם כעובדה עד מדידה
- **הרצת `why_stopped_r2`** — `campaign=why_stopped_r2` · `prior_campaigns=why_stopped` · `audience=stuck_users`. ⛔ לא הורץ
- אימות ידני: אונבורדינג ב-₪ בחלון אנונימי · יתר §1-§4 של M (מטבע בייבוא · זבל→דחייה · round-trip · AdminPanel)
- 2 קבצי Sheets של `a0556783290` (IBI + אלטשולר) — חומר הגלם לתיקון הייבוא, טרם נוצל · **הגנת סיסמאות דלופות** — **פיצ'ר Supabase Pro בלבד**, לא הגדרת קונסולה. עם השדרוג
- PR #44 Dependabot **+ ענף Dependabot נוסף שנפתח מאז** — שניהם ממתינים להכרעה יחד · דוח Architecture Auditor (14 ממצאים, טרם מוינו) · **`docs/TASKS-REGISTRY.md` טרם הורד לעץ** (הפיל את שלב 5 של R; ⛔ Claude לא יוצר אותו)
- **מלאי סודות** — לא בוער, אין תסמין. ⚠️ הערך מופיע **ביום שמשהו נשבר** ואין רשימה של מה מזין את מה
- ⏸️ **שאלה מוצרית — סימן ויזואלי לעסקה פתוחה מעל 7 ימים?** ראה ⚠️ "עסקאות שלא נסגרות". ההצעה: **נקודה כתומה ביומן**. ⛔ לא התראה, לא מייל — ניב מכריע
- Stripe Test Mode · Google Search Console · Google Workspace · QA מובייל 7 סעיפים · ערכת OCR (30 גרפים) · 390 תמונות · עו"ד על `/privacy`
- ⚠️ **שדה טווח עסקה** — בקשת `omrikapara1` 31.07, נוגע בסכימה+מסננים. **המייל שאנחנו שולחים לו אומר "נכנס לתוכנית"** — אם הוא יענה ויבדוק, זה מה שהוא יחפש
- ⚠️ **`saridel` (22/22 ללא stop) — שאלת מוצר, לא משימה.** האם זו התנהגות לתקן או סגנון מסחר לגיטימי? ⛔ להכריע **לפני** שכותבים לו, אחרת המייל מניח תשובה
- **מחיקת `~/Desktop/Swing-edge`** (worktree שבור מ-30.05, הכשיל סשן 04.08). ⚠️ **לפני מחיקה — להעביר חומרי שיווק שאינם ב-`~/swingedge-creatives`.** `Backup-codes-niveven183.txt` נשאר (2FA, **לא בריפו — אומת**). הסיכון: `git add -A` משם

---

## ✅ נסגר השבוע

- **M · S1 · R · #12 · W · S2** — כולם סגורים, הפירוט המלא ב-`docs/audits/STATE-ARCHIVE-2026-08.md`. נותר חי מהם: `github-script@v9` **0/3** → 🔴 למעלה · ⏭️ `download-artifact@v7→v8` · ⚠️ שורות פידבק **קיימות** נשארו caller-asserted (S2) — המיסוך ואישור האדמין בעין הם ההגנה
- **ידית `/api/notify` · §3 הפאנל הציג מצב שאינו קיים (06.08)** — הפירוט נגזם לארכיון
- **✅ תבניות המענה (7 סוגים) · `dry_run` שחלק מפתח rate-limit עם השליחה · בדיקת GA4 ל-7 ימים — שלושתם נסגרו 08.08, הפירוט נגזם לארכיון.** מה-GA4 נשאר חי מספר אחד: **15 משתמשים פעילים ב-7 ימים** (13 ישראל, 2 ארה"ב), 2–7 ביום — תואם ל-41 הנרשמים, כלומר תיקון `blockAnalytics` (05.08) עבד ו**אין זיהום שיורי**
- **✅ רשם הדומיין — Auto-Renew אומת (ניב, 08.08).** הפריט הפתוח מ-`INCIDENTS` #4/#5 נסגר. ⛔ היה: פג תוקף = האתר יורד, לא מתדרדר
- **✅ #14 — מחלקת הכתיבה השקטה נסגרה 7/7 (08.08).** הפירוט של האתר הראשון (המחיקה, 07.08) נגזם ל-`INCIDENTS#14`; מה שנשאר חי הוא הכלל: **PostgREST מחזיר `error: null` על כתיבה שהתאימה אפס שורות**, ולכן רק ספירת שורות מפרידה כתיבה שקרתה מ-no-op. 6 האתרים שנותרו נסגרו: שמירה (`:2431`, **השורש** — INSERT בלי `await`) · סגירה (`:2564`) · עריכה (`:2589`) · מחיקה מרובה (`:2462`) · ייבוא (`:2539`) · undo-ייבוא (`:2605`). `tradeDelete.js` → **`src/lib/tradeWrite.js`**, `classifyRows` אחת טהורה = מקום אחד שמכריע "האם הכתיבה קרתה". ⚠️ **rollback השמירה הפוך מזה של המחיקה** — כשל מחיקה **מחזיר** שורה, כשל שמירה **מסיר** אותה. ⚠️ **חלקי הוא מצב שלישי** שהקוד הישן לא יכול היה לייצג: `missingIds` מחזיר רק את מה שלא נחת. **קו בסיס אדום `7/20` → `20/20`, בר-שחזור בכל רגע** דרך `scripts/fixtures/tradeWrite-legacy.mjs` (`TRADEWRITE_IMPL=... node scripts/tradeWrite-test.mjs`) — לא מספר ביומן. ⚠️ **7 אסרציות עוברות ריקנית על הקוד הישן** (3,4,5,7,11,17,19) — שומרות-רגרסיה, לא ראיה; **ושתי תחזיות בתוכנית המאושרת הופרכו במדידה** (9 נחזתה ריקנית ואינה; 11 לא נחזתה וכן). `test:write` בשרשרת החוסמת. ⚠️ **הנחת ה-chunking ("ייבוא >100 שורות מפוצל") — חופשה ולא נמצאה באף מסמך:** `grep` על כל ה-`.md` בריפו + `CONTEXT.md` החזיר **0 מקורות**; ההנחה נולדה בפרומפט עצמו, לא בשורת STATE, ולכן **אין מה לתקן כאן** — נרשם כדי שהחיפוש לא יחזור. בפועל: INSERT יחיד, אפס chunking (`SwingEdge_App.jsx:2681`), ⛔ ולא נוסף בגל הזה. `INCIDENTS#14` · `DECISIONS` 08.08
- **✅ אתרי ה-RPC בפאנל האדמין — 3/3, אותה מחלקה בקובץ אחר, נסגר 08.08.** `AdminPanel` `:960` (`admin_delete_trade`) · `:1261` (`admin_delete_feedback`) · `:1525` (`admin_delete_demo_trades`) פירקו `const { error }` בלבד וזרקו את המונה ש**כבר היה על החוט**: כל השלושה `returns int` מעל `get diagnostics _n = row_count` (`admin_rpcs.sql:404,424,444`). כולם עברו ל-`rpcCountVerified` מעל **אותה** `classifyRows` — ⛔ בלי מודול שלישי. ⚠️ `classifyRows` הורחבה לנרמל **מונה סקלרי** (RPC) לצד מערך שורות (`.select`), אחרת כל RPC היה נספר כאפס. ⚠️ **`:1525` מוחק לפי פרדיקט ולכן `expected: null`** — המספר **מדווח ולא נשפט**; אסרציית שוויון מול `demoCount` שעל המסך הייתה ממציאה כשל מקריאה ישנה. ⛔ `:1236` (`admin_set_feedback_status`) **אינו** במחלקה — הוא כבר עושה `if (!data) throw`, לא נגענו. **קו בסיס אדום `9/25`** מול `fixtures/tradeWrite-legacy.mjs` (נכשלו 21·22·24) → **`25/25`**, 1–20 הקפואות ביט-בביט. ⚠️ **22 נחזתה ריקנית והיא מפרידה** — היא תובעת `rows === 1` והישן אינו מחזיר מונה כלל. **וגם: `CLAUDE.md` §7 עודכן לשרשרת `verify` בפועל** (14 חוליות + `build`, נגזר מ-`package.json` ולא מהזיכרון). `DECISIONS` ×2, 08.08

---

## ⚠️ סיכונים פתוחים

- **`react-router` GHSA-qwww-vcr4-c8h2 (high) נשאר פתוח — סיכון מקובל, לא באג.** התיקון היחיד הוא downgrade ל-7.11.0. הפגיעות היא CSRF ב-**RSC Mode** ולאפליקציה אין RSC. נאכף כהערה ב-`package.json` ליד `dependencies`. ⛔ **מתבטל ברגע שנאמץ RSC/server actions.** `DECISIONS.md` 27.07 + 06.08
- **הסתעפויות יכולת-דפדפן ללא כיסוי (05.08).** אימות ברוחב מוקטן אינו נוגע בהן **בהגדרה** — אותו מנוע, אותו משטח API. #1 `capture` תוקן · #2 → 🔴 למעלה · **#3** `ThemeContext.jsx:42` `matchMedia` לא מוגן (`:31` כן) → WebView ישן זורק ב-`useEffect` והעץ נופל · **#4** `IOSInstallBanner.jsx:9,13,20` iOS-only, מעולם לא הורצה · **#5** `FeedbackTab.jsx:49` `parseUA()` (מכוסה) · **#6** `:1304` חשוף מול `OnboardingTour:37`
- **`a0556783290` מחק את כל 50 עסקאותיו, כולל 19 התקינות. אין לו היום אף עסקה.** **לא נטש בשקט** — דיווח, צירף Sheets, הציע שיפור. **מעורב שאבד**
- **`general-sans` מ-`api.fontshare.com`** (`index.html:56`) — ⚠️ **"בלי fallback" הופרך:** שרשרת `'General Sans','Inter','system-ui'` קיימת ב-`tailwind.config.js:31`, `tokens.css:68-69`, `index.html:155`, ו-Inter נטען ממקור אחר. ה-500 נחת על Inter — **קוסמטי, לא שובר.** הנזק האמיתי: `<link>` **חוסם-רינדור**, נמדד 06.08 **1.46ש מול 0.25ש** ל-Google Fonts — **חמור מה-500 שהוביל לכאן** ⏭️ גל נפרד. ⏸️ **חסום על ניב** — אירוח עצמי *וגם* הצמדת CDN מותנים בתנאי רישיון ש**לא ניתן היה לקרוא** (עמוד הרישיון JS-rendered, ה-CSS בלי כותרת רישיון). ⛔ לא שונה דבר. חלופה ללא רישיון: לוותר על General Sans לטובת Inter (מסיר 2 מארחים מה-CSP) — **החלטת עיצוב, לא שלי**
- **מרוץ ה-smoke — נמדד ונאכף (W · 4ב) ✅.** ⚠️ **ההנחה הופרכה: `/app` החזיר 200, לא 500** — הכישלון היה 500 מ-`api.fontshare.com` דרך ערוץ ה-console הלא-מסונן (תוקן, #13). המרוץ היה אמיתי אך רדום: המרווח deploy→בדיקות 5–21ש ב-4 ריצות הוא תוצר לוואי של `npm ci`, **לא כלל**; הסיכון **✅ כוזב, לא ❌**. `smoke.yml` ממתין ל-Deployment API (`success` ל-`github.sha`, 180ש, timeout=❌). ⚠️ **מוכיח ready, לא התפשטות alias** — אין סמן SHA באפליקציה ⏭️
- 🔴 **ה-CSP הוא Report-Only בלי אספן — כותרת שנראית כמו הגנה ואינה מייצרת אות** (`vercel.json:18`, נמדד 06.08: `report-uri|report-to` = **0**). אף דוח לא נאסף **מעולם**, ולכן "אמת שהדוח נקי" אינו ניתן למענה — לא לפני U ולא אחריו. ⛔ אין לאכוף לפני אספן + חלון תנועה נקי. ⚠️ **`api/notify.js` אינו זקוק לרשומת CSP** — serverless, קריאותיו server-to-server, מחוץ לתחום השיפוט של CSP (טעות קטגוריה שנסגרה)
- **אין בדיקה שחוסמת נתון דמוי-טוקן באנליטיקס יוצאת** (#12 בקריאה בלבד) ⏭️ · ⛔ ואין אסרציה ש**ריצת CI שולחת אפס בקשות אנליטיקס** — השומר של #13 מוכח חד-פעמית, לא חוזרת · **שורות `waitlist` ירדו ב-2 תוך 24ש** (60, ▼2) — מחיקה ידנית או ספירה שגויה ב-Growth. **לא נבדק**
- 🔴 **כל מדד GA4 מלפני 05.08 בלתי-שמיש** (#13): ≈906 page_view סינתטיים (smoke 108×5 · sentinel 122×3) מול 41 נרשמו / 12 פעילים, **בלי סמן שמפריד אותם בדיעבד**. ⚠️ **תאריך התיקון = קו הבסיס, אין להשוות לרוחבו.** ⏸️ סימון/ניקוי ב-GA4: על ניב
- ⚠️ **`Failure Alert` הוא `workflow_run` ואין לו צופה** — נעדר מטבלת ה-max-age של Watchdog **ומרשימת 11 ה-workflows של עצמו**. אם ייפול, **11 workflows מאבדים התרעה בשקט**. ⛔ לא תוקן ב-W
- **`trades."createdAt"` אינו חותמת יצירת-שורה** — אין `created_at` בצד השרת; הערך מגיע מהלקוח ובעסקה מיובאת נושא את תאריך העסקה. **40/56 שורות מוקדמות מהרשמת הבעלים** (3 משתמשים) ולכן מוצאים מכל מדידת זמן. `retention.sql` מדווח בנפרד ⛔ לא ממציא 0
- **ציד ה-egress — מוין 06.08, שלוש מהטענות תוקנו במדידה:** `api/ocr.js` **יש** לו JWT (`:323`) ותקרה 10/דק+60/שע (`:330`); חסרה **מדידת עלות**, והחור האמיתי הוא ש-`rateLimit` הוא **in-memory per-Lambda** — התקרה **רכה תחת concurrency**, וזה שונה מ"אין rate limit" ⏭️ · `api/health.js:160` `Allow-Origin: *` **מכוון ומתועד** (`:156-159`, UptimeRobot) והגוף מחזיר latency ms + שמות שירות בלבד → **סיכון מקובל** · **"8 מייל + 11 דיסקורד, אפס ליגר" שגוי**: נמדד **9/9** שולחי מייל עם `to:` קשיח → `niveven183@gmail.com`, **14** דיסקורד, והשולח היחיד למשתמשים אמיתיים (`email-campaign.yml`) **יש לו ליגר** → **סיכון מקובל** · `scripts/analyst|arch-auditor|data-guardian` שולחים אגרגטים/`file:line`/UUIDs — **לא PII** → מקובל, ⚠️ הסיכון הוא **סחיפה**: הריפו ציבורי ולוגי Actions גלויים
- **`tradeImage` (data-URL) ב-localStorage בלבד** (`supabaseClient.js:65`, **בכוונה**) — לא עובר מכשיר, והוא צרכן המכסה. ✅ **הכשל השקט ב-`:1665`/`:1670` נסגר (S2)** — `console.error` שאומר שכל השמירה המקומית מתה. ⚠️ **רישום בלבד: המשתמש עדיין לא רואה כלום ב-UI** ⏭️
- ⚠️ **ההנחות שהופרכו במדידה (08.08) — רשומה, לא משימה. 📌 הכלל: מספר או מיקום שנרשם פעם ולא נמדד שוב אינו עובדה** (`DECISIONS` 08.08): (א) `AdminPanel:1236` נרשם כזורק מונה — **בנוי נכון**, האתרים האמיתיים `:960 :1261 :1525` · (ב) הנחת ה-chunking בייבוא — **0 מקורות בכל הריפו**, נולדה בפרומפט · (ג) `tradeImagePreview` הוא **data-URL, לא objectURL** · (ד) זיהום Sentinel ב-`trades` — `SNTNL` = **0 שורות עומדות** (`restCleanup` ב-`test.afterAll` רץ גם בקריסה); 3 השורות שיוחסו לו היו `ZIM/PATH/ONDS` של `de66dc99`, **משתמש אמיתי שחזר** — ייחוס בלי מדידה כמעט מחק החזרה אמיתית מהספירה
- ⚠️ **דפוס: עסקאות שלא נסגרות — 33 מתוך 59 פתוחות.** `de66dc99`: **4/4 פתוחות**, כולל `TTWO` מ-28.07 (11 ימים). ⚠️ **הערך של היומן — R, win rate, Coach, Trading DNA — נולד רק בסגירה**, ולכן משתמש שרושם כניסות ולא סוגר אינו מקבל שום תובנה ואז נעלם. זו השערה למנגנון של 0/6 שימור, לא רק תסמין. ⏸️ שאלה מוצרית פתוחה לניב (ראה ⏸️)
- ⚠️ **ניגודיות בבאנר הריק — המסך הראשון של משתמש חדש.** טקסט ירוק כהה על רקע ירוק, כמעט בלתי קריא. ⏭️ גל נפרד, **מדידה מול WCAG AA** ולא הערכת עין
- ⚠️ **מתג השפה בלנדינג אינו כותב `document.documentElement.lang`** (נמצא בסריקת 08.08) — `AuthScreen.jsx:46` ו-`ToastProvider.jsx:58` **קוראים** משם, ורק `SwingEdge_App.jsx:1781-1782` כותב. מבקר שבוחר EN בלנדינג ולוחץ "התחבר" מקבל מסך התחברות **בעברית**. ⛔ לא תוקן — מחוץ ל-`LandingPage.jsx` ואינו קטן-ובטוח (§11) ⏭️ גל נפרד
- ⚠️ **42 `catch {}` ריקים ב-`SwingEdge_App.jsx` + אחד ב-`AdminPanel.jsx:50` (`writeJSON`)** — קובץ אחר, אותה מחלקה, נמצא בסריקה 06.08. S2 נגע ב-2 בלבד לפי גבול מפורש. ⛔ אף אחד לא נבדק — הספירה אינה הערכת חומרה ⏭️ גל נפרד
- `SwingEdge_App.jsx` (7,340) ו-`src/i18n.js` (2,893) גדולים **בכוונה** — ייפוצלו בעילה. **החלטה, לא חוב**
