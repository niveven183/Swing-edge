# TRUTH — אמת מוצרית לקופי (מקור-אמת ל-Creative)

**עודכן:** 2026-07-28
**חוק:** כל commit שמשנה יכולת פונה-משתמש מעדכן קובץ זה **באותו commit** (`CLAUDE.md` §10.4).

מקור-אמת יחיד לכתיבת קופי בריפו `swingedge-creatives`. כל שורה כאן אומתה מול הקוד,
כולל מעקב שרשרת ה-import עד לשורש האפליקציה — **קיום קוד אינו "חי"**; קומפוננטה שלא
מרונדרת, מסלול שלא נגיש או פיצ'ר שאין לו נתיב משתמש נחשבים 🔴.

**מקרא:** 🟢 חי — מותר לטעון · 🟡 חלקי — רק בניסוח המדויק שבטבלה · 🔴 לא קיים — אסור לטעון.

---

## הטבלה

| יכולת | סטטוס | סף / סייג | ניסוח מאושר לקופי | עוגן בקוד |
|---|---|---|---|---|
| בדיקת עסקה מול היסטוריה אישית בזמן הזנה | 🟢 | תג ה-verdict והביטחון מוסתר מתחת ל-10 עסקאות **סגורות** | "העסקה נבדקת מול כל מה שסחרת עד היום" | `SwingEdge_App.jsx:1707` · מרונדר `:6311` במודאל העסקה מעל כפתור Save · `IntelligenceUI.jsx:229` |
| win-rate אישי לפי setup + מצב שוק, לפני Save | 🟢 | דורש **≥3 עסקאות סגורות** באותו צירוף setup+שוק, אחרת שותק | "המספר שלך מופיע לפני שאתה לוחץ" | `DecisionCoach.js:201-221` (סף בשורה `:206`) |
| התראת מסחר רגשי | 🟢 | טריגרים: רצף הפסדים ≥3 · חלון נקמה <30 דק' · ≥4 עסקאות ביום · שעה 23:00–07:00 · ≥2 סטיות מתוכנית ב-7 ימים | "התראה רגע לפני, לא אחרי" | `TiltProtection.js:73-118` · מרונדר `SwingEdge_App.jsx:3096` · בדיקת FOMO בקוצ' `DecisionCoach.js:223-250` |
| מילוי עסקה מצילום גרף (OCR) | 🟢 | אוטומטי מלא (העלאה → מילוי). דורש ציור **Long/Short Position** של TradingView בצילום; ביטחון <40 מאפס מחירים | "פרטי העסקה נכתבים לבד מצילום אחד" | `SwingEdge_App.jsx:2381-2418` → `api/ocr.js` |
| כרטיס Trading DNA בדשבורד | 🟢 | תמיד מוצג (בסיס 50 בהיעדר נתונים) | "הדפוסים שלך, שחור על לבן" | `IntelligenceUI.jsx:44-80` · מרונדר `SwingEdge_App.jsx:3106` |
| Market Regime | 🟢 | נתוני SPY/QQQ 30 יום + 14 סקטורים; נפילה חיננית ל-snapshot ואז לתיוג המשתמש | — | `MarketRegime.js` · מחווט `SwingEdge_App.jsx:1672` · מרונדר `:3113` |
| **דוח DNA חודשי (ציון + המלצות)** | **🟢** | דורש **≥5 עסקאות סגורות** באותו חודש, אחרת מסך "אין מספיק נתונים" | "ציון לחודש, ושלושה דברים לתקן" | `MonthlyReport.js:125-131` (ציון A–F + 0–100), `:259-276` (עד 3 המלצות) · Tools → `SwingEdge_App.jsx:3917` → `:6035` · מודאל `:6551` |
| 5 שפות · RTL | 🟡 | 5 רשומות (he·en·es·pt·ar), **RTL אמיתי ב-he ו-ar**. he/en מלאות (~607 מפתחות); **es/pt/ar ~63%** (382–392) והשאר נופל לאנגלית בשקט | "5 שפות, כולל RTL מלא" — **ולא** "מתורגם במלואו" | `i18n.js:2695` רישום · `RTL_LANGS:2703` · `...en` fallback `:1331,1758,2185` |
| PWA — התקנה למסך הבית | 🟡 | manifest תקין + באנר iOS. **אין service worker בכלל** → אין offline, אין caching | "מותקן כאפליקציה מהדפדפן" — **ולא** "עובד offline" | `public/manifest.json` · `index.html:10` · `IOSInstallBanner.jsx` |
| טיימר צינון 30 דק' (Tilt) | 🟡 | הטיימר קיים ופועל, אבל **וולונטרי לחלוטין**: אינו חוסם Save, ולצדו כפתור "Clear cooldown" | רק עם תגית **"בקרוב"** אם הניסוח מרמז על נעילה | `TiltProtection.js:35-39` · כפתורים `IntelligenceUI.jsx:346,356` · נקרא לתצוגה בלבד `:300,307` |
| מדדי R (Avg R · Expectancy · Sharpe · Kelly) | 🟡 | מחושבים **רק על עסקאות שהיה להן `stop`**. עסקה בלי סטופ אינה 0R — היא מוחרגת, וכל מדד מציג לצדו כמה מהעסקאות הסגורות הוא באמת מכסה. תיק שבו לאף עסקה אין סטופ מציג `—`, לא `0.00R` | "ה-R שלך מחושב רק מעסקאות עם סטופ, ורשום לידו על כמה עסקאות הוא נשען" — **ולא** "R ממוצע על כל העסקאות" | `utils.js:calcTradeMetrics` (חוזה ה-`null`) · `statisticalModels.js` (`rStats`/`rSampleSize`) · מוצג `SwingEdge_App.jsx:3090,4580` · `MonthlyReportTab.jsx:180` · `scripts/rContract-test.mjs` |
| תוכנית Pro / מנוי בתשלום | 🔴 | **אין קוד תשלום בריפו.** `BillingModal` הוא פאנל סטטי "Beta — Free"; אין Stripe, אין `priceId`, אין דגל `isPro` | — | `src/components/BillingModal.jsx` |

---

## 🔴 אסור לטעון

חמש טענות ששוללות ישירות על ידי הקוד. כל אחת מהן בקופי = הבטחה שקרית.

1. **"עובד offline"** — אין service worker בשום מקום בריפו. האפליקציה ניתנת להתקנה, ולא יותר.
2. **"פיצ'ר Pro / בתשלום" / "שדרג ל-Pro"** — אין מנגנון תשלום כלל. **OCR חינם לכל משתמש מחובר**, מוגבל רק ב-rate limit.
3. **"נועל אותך" / "חוסם אותך מלסחור"** — הצינון וולונטרי, ניתן לביטול בלחיצה, ואינו נוגע בנתיב השמירה.
4. **"מתורגם במלואו ל-5 שפות"** — es/pt/ar מכוסות ~63%; השאר מוצג באנגלית בלי שום סימן למשתמש.
5. **"לומד את שעות המסחר שלך"** — חלון "שעה חריגה" הוא קבוע קשיח `hour < 7 || hour >= 23`, זהה לכל המשתמשים (`psychologyPatterns.js:66`).
6. **"R ממוצע על כל העסקאות שלך"** — R מוגדר רק כשיש סטופ. עסקה בלי סטופ אינה נכנסת למונה ואינה נכנסת למכנה, וכל מסך שמציג R מציג לצדו את גודל המדגם. קופי שמרמז שהמדד מכסה את כל היומן הוא הבטחה שקרית.

---

## סנכרון מול ריפו ה-Creative

`Fleet Weekly` (ראשון 07:00 UTC) מושך את hash הקומיט האחרון שנגע בקובץ זה ומשווה
אותו לערך שנשמר בריצה הקודמת. שינוי → שורה בדוח הדיסקורד:
`⚠️ TRUTH השתנה מאז הדוח הקודם — בדוק התיישנות קופי`.

ה-hash מודפס בכל דוח שבועי, ומיועד להעתקה לשדה `TRUTH_HASH` ב-`STATE.md` של
`swingedge-creatives` — שם הוא **אינפורמטיבי בלבד** (מתי הקופי סונכרן לאחרונה),
ולא מנגנון ההשוואה. ההשוואה עצמה עצמאית לחלוטין ואינה דורשת גישה לריפו הפרטי.
