# TRUTH — אמת מוצרית לקופי (מקור-אמת ל-Creative)

**עודכן:** 2026-08-10
**חוק:** כל commit שמשנה יכולת פונה-משתמש מעדכן קובץ זה **באותו commit** (`CLAUDE.md` §10.4).

מקור-אמת יחיד לכתיבת קופי בריפו `swingedge-creatives`. כל שורה כאן אומתה מול הקוד,
כולל מעקב שרשרת ה-import עד לשורש האפליקציה — **קיום קוד אינו "חי"**; קומפוננטה שלא
מרונדרת, מסלול שלא נגיש או פיצ'ר שאין לו נתיב משתמש נחשבים 🔴.

**מקרא:** 🟢 חי — מותר לטעון · 🟡 חלקי — רק בניסוח המדויק שבטבלה · 🔴 לא קיים — אסור לטעון.

---

## הטבלה

| יכולת | סטטוס | סף / סייג | ניסוח מאושר לקופי | עוגן בקוד |
|---|---|---|---|---|
| בדיקת עסקה מול היסטוריה אישית בזמן הזנה | 🟢 | תג ה-verdict והביטחון מוסתר מתחת ל-10 עסקאות **סגורות** | "העסקה נבדקת מול כל מה שסחרת עד היום" | `SwingEdge_App.jsx:1707` · מרונדר `:6311` במודאל העסקה מעל כפתור Save · `IntelligenceUI.jsx:229` |
| win-rate אישי לפי setup + מצב שוק, לפני Save | 🟢 | דורש **≥3 עסקאות סגורות** באותו צירוף setup+שוק, אחרת שותק | "המספר שלך מופיע לפני שאתה לוחץ" | `DecisionCoach.js:201-221` (סף בשורה `:206`) |
| התראת מסחר רגשי | 🟢 | טריגרים: רצף הפסדים ≥3 · חלון נקמה <30 דק' · ≥4 עסקאות ביום · שעה 23:00–07:00 · ≥2 סטיות מתוכנית ב-7 ימים | "התראה רגע לפני, לא אחרי" | `TiltProtection.js:73-118` · מרונדר `SwingEdge_App.jsx:3096` · בדיקת FOMO בקוצ' `DecisionCoach.js:223-250` |
| מילוי עסקה מצילום גרף (OCR) | 🟢 | **OCR ממלא לאחר אישור המשתמש** (העלאה → מסך אישור → מילוי). **באנלייזר שום שדה אינו נכנס לטופס עד לחיצה על "החל"**; שדה שה-OCR לא קרא מוצג כ-"לא זוהה" וניתן לתיקון בכרטיס. דורש ציור **Long/Short Position** של TradingView בצילום; ביטחון <40 מאפס מחירים. **הבאדג' מדווח מה התמלא בפועל, לא את ביטחון המודל**: 4 מצבים — מלא · חלקי 🟠 · סימבול-בלבד 🟠 · כשל 🔴. **קריאה חלקית לעולם אינה ירוקה**, ומחיר חי אינו ממלא שדה שה-OCR נכשל לקרוא. **כיוון העסקה (LONG/SHORT) נקבע מהגיאומטריה הנמדדת בגרף** כשהיא ניתנת לאימות, ולא מהתווית שהמודל כתב; **כשהכיוון שנקרא סותר את הטוגל שבטופס — הטוגל אינו זז, ומוצגת אזהרה כתומה שנוקבת בשני הכיוונים בשם.** הכרעה אוטומטית לא קיימת: הטריידר מחליט | "פרטי העסקה נקראים מצילום אחד — ואתה מאשר לפני שהם נכנסים" — **ולא** "אוטומטי מלא" ו**לא** "כל צילום מתמלא במלואו"; קריאה חלקית מסומנת ככזו ודורשת השלמה ידנית. על כיוון: **"אם הגרף אומר משהו אחר מהטוגל שלך — נגיד לך, ולא נשנה לך"** — ולא "המערכת מזהה את הכיוון אוטומטית" | `SwingEdge_App.jsx` `ocrDetection`/`ocrBadgeState`/`OcrReviewCard` → `api/ocr.js` (`sideSource`/`sideConflict`) |
| ייבוא יומן מקובץ (CSV · Excel · ברוקר) | 🟢 | זיהוי אוטומטי **לשני ברוקרים בלבד — IBI ואלטשולר שחם** — ורק בטביעת-אצבע מלאה (≥8/11 · ≥9/13 עמודות). קובץ אחר אינו נדחה: הוא עובר במסלול הגנרי עם מיפוי עמודות ידני, **ובלי שידוך FIFO**. במסלול הברוקר הסינון הוא **whitelist** של פעולות מסחר (קנייה/קניה/מכירה) — כל שורה אחרת מדולגת **ומדווחת לפי סוגה** (דיבידנד · המרת מט"ח · ריבית · משיכה), ושידוך FIFO קניות↔מכירות מייצר עסקאות סגורות. **שער שני, ברמת הנייר:** IBI רושם חיוב מס והמרת מט"ח כ**קנייה** של נייר-דמה, כך שסוג הפעולה לגיטימי ומה שאינו נייר הוא ה**נייר** — השורות האלה מזוהות בשמן ומדולגות. **יחידת המחיר נגזרת מהאריתמטיקה של השורה**: נייר בת"א נקוב באגורות ומחולק ב-100; שורה שאי אפשר להכריע לגביה נדחית (`bad_unit`) ואינה מנוחשת. **עמלות ומס אינם במודל**: אין להם עמודה בסכימה, `normalizeRow` מפיל אותם, ולכן הרווח נגזר ממחירי הקובץ בלבד ויסטה מדוח הברוקר. עסקה מיובאת נכנסת **בלי `stop`** ולכן אינה נספרת במדדי R (ראה שורת R). **התקן:** סיומות `.csv .tsv .txt .xlsx .xls .xlsm`, שורת כותרות אחת, ובמסלול הגנרי שורה לכל עסקה; חובה `ticker`+`entry`+`shares` (`synonyms.js:24`) — שורה בלי אחד מהם נדחית עם קוד סיבה ויורדת ב-CSV הדחיות; רשות: `side` `exit` `date` `exitDate` `stop` `target` `currency`; כותרות בעברית או באנגלית (`FIELD_SYNONYMS`). **עמודת מטבע נקראת** ("מטבע" / "Currency" / ₪ / $ / ILS / USD): תא ריק נופל למטבע ההון, ותא **לא ריק ולא מזוהה דוחה את השורה** (`bad_currency`) במקום להיטבע USD. הכותרת "מטבע" חייבת להיות **מדויקת** — `שווי במטבע` הוא עמודת שווי ואינו נתפס כעמודת מטבע. **התקן אינו מוצג למשתמש לפני ההעלאה** — `imp_step1_body` מסתפק ב-"CSV או Excel", והחובה מתגלה רק באזהרת שלב 2 | "קובץ מהברוקר נכנס כעסקאות סגורות — ולפני האישור אתה רואה מה נכנס, מה דולג ולמה" — **ולא** "תומך בכל הברוקרים", **ולא** "רווח והפסד כולל עמלות ומס", **ולא** "ייבוא בלחיצה" (מיפוי ידני במסלול הגנרי) | `src/import/*` · פרופילים `brokerProfiles.js:92-126` · `fifoMatch.js` (+`FEES_NOTE:27`) · UI `ImportJournalModal.jsx` (3 שלבים, סיכום בשלב 3 לפני האישור) · מחווט `SwingEdge_App.jsx:36` → מרונדר `:6913` · `scripts/import-test.mjs` |
| כרטיס Trading DNA בדשבורד | 🟢 | תמיד מוצג (בסיס 50 בהיעדר נתונים) | "הדפוסים שלך, שחור על לבן" | `IntelligenceUI.jsx:44-80` · מרונדר `SwingEdge_App.jsx:3106` |
| Market Regime | 🟢 | נתוני SPY/QQQ 30 יום + 14 סקטורים; נפילה חיננית ל-snapshot ואז לתיוג המשתמש | — | `MarketRegime.js` · מחווט `SwingEdge_App.jsx:1672` · מרונדר `:3113` |
| **דוח DNA חודשי (ציון + המלצות)** | **🟢** | דורש **≥5 עסקאות סגורות** באותו חודש, אחרת מסך "אין מספיק נתונים" | "ציון לחודש, ושלושה דברים לתקן" | `MonthlyReport.js:125-131` (ציון A–F + 0–100), `:259-276` (עד 3 המלצות) · Tools → `SwingEdge_App.jsx:3917` → `:6035` · מודאל `:6551` |
| 5 שפות · RTL | 🟡 | 5 רשומות (he·en·es·pt·ar), **RTL אמיתי ב-he ו-ar**. he/en מלאות (~607 מפתחות); **es/pt/ar ~63%** (382–392) והשאר נופל לאנגלית בשקט | "5 שפות, כולל RTL מלא" — **ולא** "מתורגם במלואו" | `i18n.js:2695` רישום · `RTL_LANGS:2703` · `...en` fallback `:1331,1758,2185` |
| PWA — התקנה למסך הבית | 🟡 | manifest תקין + באנר iOS. **אין service worker בכלל** → אין offline, אין caching | "מותקן כאפליקציה מהדפדפן" — **ולא** "עובד offline" | `public/manifest.json` · `index.html:10` · `IOSInstallBanner.jsx` |
| טיימר צינון 30 דק' (Tilt) | 🟡 | הטיימר קיים ופועל, אבל **וולונטרי לחלוטין**: אינו חוסם Save, ולצדו כפתור "Clear cooldown" | רק עם תגית **"בקרוב"** אם הניסוח מרמז על נעילה | `TiltProtection.js:35-39` · כפתורים `IntelligenceUI.jsx:346,356` · נקרא לתצוגה בלבד `:300,307` |
| מדדי R (Avg R · Expectancy · Sharpe · Kelly) | 🟡 | מחושבים **רק על עסקאות שהיה להן `stop`**. עסקה בלי סטופ אינה 0R — היא מוחרגת, וכל מדד מציג לצדו כמה מהעסקאות הסגורות הוא באמת מכסה. תיק שבו לאף עסקה אין סטופ מציג `—`, לא `0.00R` | "ה-R שלך מחושב רק מעסקאות עם סטופ, ורשום לידו על כמה עסקאות הוא נשען" — **ולא** "R ממוצע על כל העסקאות" | `utils.js:calcTradeMetrics` (חוזה ה-`null`) · `statisticalModels.js` (`rStats`/`rSampleSize`) · מוצג `SwingEdge_App.jsx:3090,4580` · `MonthlyReportTab.jsx:180` · `scripts/rContract-test.mjs` |
| אחוז הצלחה (Win Rate) — קונבנציה אחידה בכל המסכים | 🟢 | עסקה נספרת רק אם היא `CLOSED` **וגם** יש לה מחיר יציאה. עסקה שיצאה **בדיוק** בנקודת הכניסה היא **break-even** — לא זכייה ולא הפסד: היא במכנה ולא במונה, ומופיעה כתג `BE` נפרד כשהיא קיימת. זהה בדשבורד, ביומן, בלוח השנה, בכרטיס היום, ב-Playbook, ב-Analytics **ובדוח החודשי** (הצטרף 02.08; הדוח החודשי מפרסם ב**עשירית אחוז**, שאר המסכים באחוז שלם — הבדל תצוגה, לא הבדל אוכלוסייה) | "אחוז ההצלחה שאתה רואה זהה בכל מסך — ועסקה שיצאה בתיק לא נספרת לך כהפסד" — **ולא** "win rate = זכיות חלקי הפסדים" | `statisticalModels.js` (`getClosed`/`outcomeRates`) · `src/lib/tradingStats.js` (גבול הסולם 0..1→0..100) · `MonthlyReport.js` (`summarize`) · `scripts/tradingstats-test.mjs` (בייסליין קפוא + אסרשן שלמות אוכלוסייה) · `scripts/engine-test.mjs` בלוק 4 (הצלבה חוצה-מנועים) |
| עריכת עסקה קיימת נבדקת באותה ולידציית גיאומטריה כמו הזנה חדשה | 🟢 | **שמירות חדשות בלבד** — רשומה קיימת עם גיאומטריה הפוכה אינה נחסמת, אינה משתנה ואינה מסומנת. הבדיקה רצה רק כשיש `stop`: **עסקה בלי סטופ ניתנת לעריכה במלואה** (הערות, יציאה, הכול) — זו החלטה, לא פרצה | "סטופ בצד הלא נכון של הכניסה נחסם גם בעריכה, לא רק בהזנה" — **ולא** "המערכת מתקנת עסקאות קיימות" ו**לא** "חייבים סטופ כדי לערוך" | `EditTradeModal.jsx` `validate()` → `utils.js:78 validateTradeInputs` (אותה פונקציה שמגנה על `SwingEdge_App.jsx:2143`) · `scripts/engine-test.mjs` בלוק 6 |
| מטבע — שקל ודולר באותו יומן | 🟡 | **שלב א' בלבד: הצגה נכונה, לא המרה.** לחשבון מטבע (USD/ILS, בורר בהגדרות) שממנו נגזרים הון, סיכון-לעסקה וגודל פוזיציה; לכל עסקה מטבע משלה, שבייבוא **נגזר מהאריתמטיקה של השורה** — נייר בת"א נקוב באגורות, והמערכת מחלקת ב-100 לפי היחס בין שווי העסקה לשער×כמות, לא לפי ניחוש. שורה שהיחס שלה אינו 1 ואינו 0.01 **נדחית** עם קוד סיבה. **אין המרת שער חליפין בשום מקום.** יומן עם שני מטבעות מציג באנר שאומר במפורש שהסכומים אינם סכום אמיתי, ולצדו הפילוח פר-מטבע. **המטבע נבחר במסך האונבורדינג עצמו**, לצד סכום ההון, ונשמר כמטבע ההון — לא עוד הנחת דולר קשיחה (⚠️ ספי הדליים שגוזרים riskPct עדיין מכוילים לדולר, ולכן חשבון שקלי מסווג דלי אחד גבוה מדי). **המטבע שורד ייצוא וייבוא חוזר** — ל-CSV של היומן יש עמודת `Currency` אחרונה, והייבוא קורא אותה. ⛔ **המטבע נשמר לשרת רק אחרי הרצת `20260802140000_trades_currency.sql`**; עד אז הוא חי בייבוא, בתצוגה וב-localStorage בלבד. **OCR אינו מכוסה** — גרף של נייר בת"א עדיין יזרים מספרים גדולים פי 100 לטופס | "עסקה בשקלים מוצגת בשקלים, מהכניסה ועד הרווח — ואם יש לך שני מטבעות ביומן, נגיד לך שהסכום אינו סכום" — **ולא** "תמיכה מלאה בשקל", **ולא** "המרת מטבע", **ולא** "מחשב לך את התיק במטבע אחד" | `utils.js` `CURRENCY_SYMBOL`/`currencyOf`/`fmtMoney(n, currency)` · `src/import/normalizeRow.js` (`unitResolver`) · `brokerProfiles.js` (`nonSecurityKind`) · `tradingStats.js` (`pnlByCurrency`/`mixedCurrency`) · `SwingEdge_App.jsx` `accountCurrency`/`statsCcy`/`MixedCurrencyBanner` |
| אופק עסקה — שדה אופציונלי + נקודת תזכורת ביומן | 🟡 | **תזכורת עובדתית, לא המלצה.** עסקה **פתוחה** מקבלת נקודה כתומה ליד הסימבול כשמספר הימים מאז תאריך הכניסה עבר את האופק ש**המשתמש עצמו הצהיר עליו**: `horizon` פר-עסקה אם נבחר (קצר 3 · בינוני 21 · **ארוך = לעולם לא מסומן**), אחרת נגזר מסגנון המסחר שנבחר באונבורדינג (day 3 · swing/משולב/עדיין-מחפש 21). ⛔ **מי שלא הצהיר סגנון אינו מקבל שום סימן** — נמדד 10.08: 3/11 מחזיקי העסקאות הפתוחות אינם בעלי שורת `user_settings` כלל, וסף שנגזר להם היה נובע מהנחה שלא הם אמרו. הטולטיפ אומר **כמה ימים פתוחה ועל בסיס מה**, ולעולם לא "כדאי לסגור". ⛔ אין התראה, אין מייל, אין דגל שנשמר — הסימן נגזר בזמן רינדור ומשתנה מיד כשמשנים סגנון. ⚠️ **ייבוא יחיד יכול להדליק עד 14 נקודות בבת אחת** (נצפה בפועל) — לא מוסתר במכוון. ⛔ **`horizon` נשמר לשרת רק אחרי הרצת `20260810143000_trade_horizon.sql`**; עד אז הבחירה חיה בתצוגה וב-localStorage בלבד | "היומן יסמן לך עסקה פתוחה שעברה את האופק **שאתה** הגדרת — ואם הגדרת לה 'טווח ארוך', היא לא תסומן לעולם" — **ולא** "המערכת אומרת לך מתי לסגור", **ולא** "התראות על עסקאות תקועות", **ולא** "מזהה עסקאות מסוכנות" | `src/lib/tradeHorizon.js` (הגזירה הטהורה + הטולטיפ, מקור אחד לשני מסלולי הרינדור) · דסקטופ `SwingEdge_App.jsx` (שורת היומן) · מובייל `src/components/MobileTradeCard.jsx` · `supabase/migrations/20260810143000_trade_horizon.sql` · `scripts/horizon-test.mjs` (140 assertions) |
| שתי תקרות סיכון — לעסקה ולתיק | 🟢 | **סיכון/עסקה נבחר בהגדרות; תקרת התיק נגזרת ממנו ואינה ניתנת לעריכה נפרדת** — פי שניים, לא פחות מ-3% ולא יותר מ-5% (`min(5,max(3,riskPct×2))`). ⚠️ **הרצפה פעילה לכל מי שסיכונו מתחת ל-1.5%** (8/32 המשתמשים ב-0.5%), ואז התיק מרשה **שש** פוזיציות בגודל מלא ולא שלוש — לכן הממשק מציג "מינימום מערכת: 3%" בשני המקומות, בלוח הסיכונים ובהגדרות ברגע הבחירה. ⛔ **אין מספר דוגמה בטקסט ההסבר** — הוא נכון ליחס, לא למשתמש מסוים | "אתה בוחר כמה לסכן בעסקה אחת; התקרה לכל הפוזיציות הפתוחות יחד נגזרת מזה אוטומטית" — **ולא** "עד 3 עסקאות במקביל" ו**לא** אחוז לדוגמה כלשהו | `SwingEdge_App.jsx:1243` (הנוסחה) · `:4247` · `:6677` (התצוגה) · `src/data/tooltips.js:121` (`riskLimits`, 5 שפות) · `scripts/copy-contract-test.mjs` |
| תוכנית Pro / מנוי בתשלום | 🔴 | **אין קוד תשלום בריפו.** `BillingModal` הוא פאנל סטטי "Beta — Free"; אין Stripe, אין `priceId`, אין דגל `isPro` | — | `src/components/BillingModal.jsx` |

---

## 🔴 אסור לטעון

שבע טענות ששוללות ישירות על ידי הקוד. כל אחת מהן בקופי = הבטחה שקרית.

1. **"עובד offline"** — אין service worker בשום מקום בריפו. האפליקציה ניתנת להתקנה, ולא יותר.
2. **"פיצ'ר Pro / בתשלום" / "שדרג ל-Pro"** — אין מנגנון תשלום כלל. **OCR חינם לכל משתמש מחובר**, מוגבל רק ב-rate limit.
3. **"נועל אותך" / "חוסם אותך מלסחור"** — הצינון וולונטרי, ניתן לביטול בלחיצה, ואינו נוגע בנתיב השמירה.
4. **"מתורגם במלואו ל-5 שפות"** — es/pt/ar מכוסות ~63%; השאר מוצג באנגלית בלי שום סימן למשתמש.
5. **"לומד את שעות המסחר שלך"** — חלון "שעה חריגה" הוא קבוע קשיח `hour < 7 || hour >= 23`, זהה לכל המשתמשים (`psychologyPatterns.js:66`).
6. **"R ממוצע על כל העסקאות שלך"** — R מוגדר רק כשיש סטופ. עסקה בלי סטופ אינה נכנסת למונה ואינה נכנסת למכנה, וכל מסך שמציג R מציג לצדו את גודל המדגם. קופי שמרמז שהמדד מכסה את כל היומן הוא הבטחה שקרית.
7. **"win rate = זכיות חלקי הפסדים"** — המכנה הוא **כל** העסקאות הסגורות, כולל break-even. עסקה שיצאה בנקודת הכניסה יושבת במכנה ולא במונה; קופי שמתאר יחס זכיות-להפסדים מתאר מדד אחר.
8. **"המרת מטבע" / "התיק שלך במטבע אחד" / "תמיכה מלאה בשקל"** — אין שער חליפין בשום מקום בקוד. שקל ודולר מוצגים זה לצד זה, וסכום שמערבב אותם נושא באנר שאומר שהוא אינו סכום אמיתי. קופי שמבטיח נרמול בין-מטבעי מבטיח פיצ'ר שאינו קיים.

---

## סנכרון מול ריפו ה-Creative

`Fleet Weekly` (ראשון 07:00 UTC) מושך את hash הקומיט האחרון שנגע בקובץ זה ומשווה
אותו לערך שנשמר בריצה הקודמת. שינוי → שורה בדוח הדיסקורד:
`⚠️ TRUTH השתנה מאז הדוח הקודם — בדוק התיישנות קופי`.

ה-hash מודפס בכל דוח שבועי, ומיועד להעתקה לשדה `TRUTH_HASH` ב-`STATE.md` של
`swingedge-creatives` — שם הוא **אינפורמטיבי בלבד** (מתי הקופי סונכרן לאחרונה),
ולא מנגנון ההשוואה. ההשוואה עצמה עצמאית לחלוטין ואינה דורשת גישה לריפו הפרטי.
