# אבחון: מצב המערכת ומערכת ההתראות — 2026-08-11

> §8.1 **שלב 1, read-only.** ⛔ אפס שינוי קוד · אפס מיגרציה · אפס כתיבה ל-DB · אפס מייל.
> היוצא-דופן היחיד: רישום ב-`docs/STATE.md` / `docs/DECISIONS.md` לפי §10.1 ("אין פריט יתום").
> HEAD בזמן האבחון: `dea4cc1` · עץ נקי · `git pull` = up to date.
> ⚠️ הריפו ציבורי — אגרגטים ומזהים מקוצרים בלבד, אפס UUID מלא.

---

## תקציר — מה השתנה בתמונה

**ארבע הטענות שנבדקו: 3 הופרכו, 1 אוששה.** שתי המיגרציות שנרשמו כ"טרם הורצו" — **הורצו**.
ה-🔴 היחיד בקובץ המצב — **מת מזה יומיים**.

**והממצא החשוב ביותר הפוך מהנחת הפרומפט:** מערכת ההתראות **עבדה** בבוקר 11.08.
`Failure Alert` ירה **11 שניות** אחרי הכשל ושלח מייל **וגם** דיסקורד. מה שנכשל הוא
משהו אחר וחמור יותר: **65 דקות אחרי הכשל, `Watchdog` פרסם ירוק** — "כל 11 הסוכנים
המתוזמנים רצו בזמן" — בזמן שסוכן פרודקשן היה שבור.

---

## §1 — ארבע הטענות

| # | הטענה ב-`STATE` | פסק | הראיה |
|---|------------------|-----|--------|
| 1 | `20260809120000_admin_rpcs_denominators.sql` — ⏸️ טרם הורץ | ⛔ **הופרך** | `admin_trades_agg`: `nullif`×3, `else 0`×**0**. `admin_overview`: `nullif`×1, `else 0`×**0** |
| 2 | `20260810143000_trade_horizon.sql` — ⏸️ טרם הורץ | ⛔ **הופרך** | `horizon text` nullable · `trades_horizon_check` בן 3 ערכים · `status` DEFAULT `'OPEN'::text` |
| 3 | 🔴 `navigator.clipboard` בלי בדיקת קיום | ⛔ **הופרך** | `SwingEdge_App.jsx:2851-2865` — בדיקת קיום מלאה + כשל נראה |
| 4 | `arch-auditor` מצביע על `STATE.md:98`, בפועל `:99` | ✅ **אושש** | 3 מופעים: `:134` · `:141` · `:153` |

### 1 — מיגרציית ה-RPC: הורצה

```
proname            | nullif | else 0 | with_stop | real_count
admin_trades_agg   |   3    |   0    |   true    |   true
admin_overview     |   1    |   0    |     —     |     —
```

⚠️ **גם המפתחות שה-`AdminPanel` תלוי בהם קיימים.** רשימת המפתחות המוחזרים
מ-`admin_trades_agg` כפי שהיא מותקנת ב-DB:

```
total · demo_count · real_count · win_rate · avg_pnl · with_stop ·
with_setup · pct_with_stop · pct_with_setup · top_ticker · by_status
```

`with_stop` ו-`real_count` — **שניהם מפתחות עליונים**. שורת החוב ב-`STATE` שאמרה
ש-`?? total` ו-`—` "מחזיקים את שתי הצורות עד שניב מריץ" — **המתנה שהסתיימה**.

⚠️ **גבול מוצהר:** קריאה חיה ל-`admin_trades_agg()` מ-`execute_sql` נחסמה
ב-`not authorized` (`42501`) — שומר `is_admin()`, ו-`auth.uid()` הוא `null` בתפקיד
`postgres`. לכן המפתחות מוכחים **מגוף הפונקציה המותקנת**, לא מהחזרה חיה.
זה שומר עובד כשורה, לא כשל.

### 2 — מיגרציית האופק: הורצה, שני חלקיה

```
column  | type | nullable | default
horizon | text |   YES    |  null
status  | text |   YES    | 'OPEN'::text
```
```
trades_horizon_check | CHECK (horizon IS NULL OR horizon IN ('short','medium','long'))
```

שלושת החלקים ✅. **וגם הצעד השני של הסדר הכפוי בוצע:** `src/supabaseClient.js:64`
מכיל `"horizon"` ב-`TRADE_COLUMNS`, ולכן המפתח **אינו** נזרק ב-PostgREST.

🆕 **ממצא נלווה שלא נרשם: 0/59 העסקאות נושאות ערך `horizon`.** הצינור פתוח מקצה
לקצה, ואיש עוד לא השתמש בשדה. ⛔ זו מדידה, לא מסקנה — 0 כתיבות ב-5 ימים (למטה)
מסבירות את זה בלי להזדקק לבאג.

🔴 **נגזרת:** שורת הסיכון "ברירת המחדל של `status` מתה — `DEFAULT 'open'`" **אינה
נכונה יותר.** הפצצה פורקה.

### 3 — ה-🔴 היחיד בקובץ מת

```jsx
// SwingEdge_App.jsx:2851
const canCopy =
  typeof navigator !== "undefined" &&
  navigator.clipboard &&
  typeof navigator.clipboard.writeText === "function";
if (!canCopy) { toast.info(t.mentoringCopyManual); return; }
try {
  await navigator.clipboard.writeText(mentorInviteCode);
  ...
} catch {
  toast.error(t.mentoringCopyManual);   // ⛔ ה-catch אינו ריק
}
```

בדיקת קיום תלת-שלבית · כשל נראה בשני המסלולים · `setMentorCodeCopied` **אינו**
נקבע בכשל ("Copied" לא משקר). **הקובץ סתר את עצמו:** ה-🔴 בראש הקובץ מול השורה
40 שורות מתחתיו שאמרה "#2 (clipboard) נסגר בקוד 09.08". **השורה השנייה נכונה.**

### 4 — ההפניה הרקובה (אוששה)

`scripts/arch-auditor.mjs` מצביע שלוש פעמים על `docs/STATE.md:98`:

| שורה | סוג | תוכן |
|------|-----|-------|
| `:134` | `ref:` | השתקת `SwingEdge_App.jsx` |
| `:141` | `ref:` | השתקת `src/i18n.js` |
| `:153` | הערה | "AdminPanel נעדר במכוון" |

**היעד בפועל הוא `:99`.** ⚠️ והראיה לדפוס נמדדה: ההפניה הייתה `:100` ב-10.08,
`:99` ב-11.08, ותזוז **שוב** בקומיט הזה. `test:arch#5,#7` עוברות כי הן קוראות
**תוכן** ולא מספר שורה ⇒ ההפניה אנושית-בלבד ונרקבת **בשקט**, בכל גיזום.
⛔ לא תוקן — `scripts/` מחוץ להיקף read-only.

---

## §2 — מערכת ההתראות: מי שומר על השומרים

### 2.1 האירוע של הבוקר — נמדד, לא שוחזר מהזיכרון

| שעה (UTC) | מה קרה | ראיה |
|-----------|---------|-------|
| 07:44:12 | `User Analytics` נכשל (`schedule`) | run `31470184666`, conclusion `failure` |
| 07:44:22 | הריצה הסתיימה | `updatedAt` |
| **07:44:23** | **`Failure Alert` ירה והצליח** | run `31470196554`, conclusion `success` |
| 08:48:58 | **`Watchdog` פרסם ירוק** | run `31474881453` |
| 10:40:25 | תיקון ידני (`workflow_dispatch`) הצליח | run `31483248165`, 34ש' |

**צעדי `Failure Alert` בריצה `31470196554`:**

```
Email failure alert      → success
Note missing mail secrets → skipped     (⇒ הסודות קיימים)
Report to Discord        → success
```

⛔ **הנחת הפרומפט "מערכת ההתראות עצמה אינה מנוטרת" הופרכה עבור האירוע הזה.**
זמן הגילוי היה **11 שניות**, ושני הערוצים נמסרו. האירוע **לא היה שקט.**

⛔ **וגם "Watchdog היה מגיע 14:12 — 6.5 שעות" הופרך — בכיוון הפסימי.**
`Watchdog` רץ 08:48:58 ופרסם:

```json
{"description": "🐕 Watchdog — כל 11 הסוכנים המתוזמנים רצו בזמן"}
discord POST -> HTTP 204
```

**ירוק, 65 דקות אחרי הכשל.** הסיבה מדודה: `watchdog.yml:52` נותן ל-`user-analytics.yml`
סף **30 שעות**, וההצלחה האחרונה הייתה 10.08 08:12:48 ⇒ גיל **24.6ש' < 30ש'** ⇒ אין דגל.

**החישוב האמיתי של האיחור, לו `Failure Alert` היה מושבת:**
`Watchdog` הבא (12.08 ~08:48) היה רואה גיל 48.6ש' > 30ש' ומדליק.
⇒ **25.1 שעות** מהכשל ועד הצעקה — לא 6.5.
⚠️ **ובפועל אף פעם:** התיקון הידני ב-10:40 אִפֵּס את שעון ההצלחה, ולכן
`Watchdog` של 12.08 היה רואה גיל 22ש' ונשאר ירוק. **הכשל היה נמחק מההיסטוריה
של השומר.**

📌 **המסקנה המדויקת:** `Failure Alert` תפס את זה, ו-`Watchdog` **לא יכול היה**
לתפוס את זה — כי סף של 30ש' על cron יומי מדלג **תמיד** על הכשל הראשון.
זו אינה תקלה ב-`Watchdog`; זו התכונה שלו. הוא נבנה ל"מת בשקט", לא ל"נכשל".

### 2.2 טבלת הכיסוי המלאה — 17 workflows

| workflow (`name`) | קובץ | תדירות | מי מנטר **כישלון** | מי מנטר **אי-ריצה** (סף) |
|---|---|---|---|---|
| Sentinel | `sentinel.yml` | `:20,:50` כל שעה | Failure Alert | Watchdog (3ש') |
| Daily Digest | `daily-digest.yml` | יומי 04:00 | Failure Alert | Watchdog (30ש') |
| Smoke Tests | `smoke.yml` | יומי 04:00 + push | **Triage** | Watchdog (30ש') |
| Fleet Daily | `fleet-daily.yml` | יומי 06:00 | Failure Alert | Watchdog (30ש') |
| User Analytics | `user-analytics.yml` | יומי 06:30 | Failure Alert | Watchdog (30ש') |
| Watchdog | `watchdog.yml` | יומי 08:00 | Failure Alert | **Fleet Daily** (`:221`) |
| Data Guardian | `data-guardian.yml` | כל 3 ימים 05:00 | Failure Alert | Watchdog (80ש') |
| Supabase Backup | `backup.yml` | ראשון 03:00 | **Triage** | Watchdog (192ש') |
| Architecture Auditor | `arch-auditor.yml` | ראשון 05:00 | Failure Alert | Watchdog (192ש') |
| Analyst | `analyst.yml` | ראשון 06:00 | Failure Alert | Watchdog (192ש') |
| Fleet Weekly | `fleet-weekly.yml` | ראשון 07:00 | Failure Alert | Watchdog (192ש') |
| Restore Drill | `restore-drill.yml` | חודשי, 1 ב-04:00 | ⛔ **אין** (מכוון, `failure-alert.yml:6`) | Watchdog (840ש') |
| Build | `build.yml` | push / PR | **Triage** | — (אינו מתוזמן) |
| Triage | `triage.yml` | `workflow_run` | Failure Alert | — (אינו מתוזמן) |
| Email Campaign | `email-campaign.yml` | ידני בלבד | Failure Alert | — (אינו מתוזמן) |
| Health Monitor | `health.yml` | ידני בלבד | ⛔ **אין** | — (אינו מתוזמן) |
| **Failure Alert** | `failure-alert.yml` | `workflow_run` | 🔴 **אין** | 🔴 **אין** |

**אימות שמות:** 11/11 השמות ב-`failure-alert.yml:10` ו-3/3 ב-`triage.yml:9`
תואמים ל-`name:` אמיתי. ⛔ **אפס שגיאות כתיב** — כל ההפניות חיות.
(זו הייתה בדיקה נפרדת: שם שגוי ברשימת `workflows:` הוא no-op **שקט**.)

**כיסוי:** 11 (Failure Alert) + 3 (Triage) = **14/17 מנוטרים לכישלון**.
שלושת הנותרים: `Restore Drill` (מכוון + מכוסה בהתיישנות) · `Health Monitor`
(ידני בלבד) · **`Failure Alert` — החור האמיתי.**

### 2.3 🔴 `Failure Alert` — החור, ולמה הוא סגור-לעצמו

`Failure Alert` **אינו מופיע ברשימת 11 ה-workflows של עצמו**, ולכן אינו יכול
לדווח על כשל של עצמו. **וגם `Watchdog` לא יתפוס אותו** — ומהסיבה שאינה מובנת
מאליה: בדיקת העקביות ב-`watchdog.yml:101` מסמנת רק קבצים שיש בהם `schedule:`.
`failure-alert.yml` הוא `workflow_run` בלבד ⇒ **אין לו `schedule:`** ⇒ הבדיקה
שנועדה לתפוס "workflow מתוזמן לא מנוטר" **מדלגת עליו בהגדרה**.

**החשיפה, במספרים:** אם `Failure Alert` ייפול, **11/17 ה-workflows מאבדים
התרעת-כישלון בשקט**. `Triage` ימשיך לכסות 3 (Build · Smoke · Backup),
ו-`Watchdog` ימשיך לכסות התיישנות של 11 — אבל **כישלון מהיר של סוכן שרץ בזמן
לא ייראה כלל**. זה בדיוק המסלול של הבוקר, שבו `Watchdog` היה ירוק.

⚠️ **הפער היה רשום כסיכון ב-`STATE` ומעולם לא תומחר. עכשיו יש לו מספר: 11/17.**

### 2.4 שני סוכנים בלי שומר-נקודת-כניסה

| סוכן | שורה | שומר |
|------|------|-------|
| `scripts/analyst.mjs` | `:669` | ✅ `fileURLToPath(import.meta.url) === process.argv[1]` |
| `scripts/arch-auditor.mjs` | `:660` | ✅ `import.meta.url === pathToFileURL(process.argv[1]).href` |
| `scripts/daily-digest.mjs` | `:467` | ✅ `import.meta.url === pathToFileURL(process.argv[1]).href` |
| `scripts/user-analytics.mjs` | `:853` | ⛔ `main().catch(...)` חשוף |
| `scripts/data-guardian.mjs` | `:305` | ⛔ `main().catch(...)` חשוף |

**3/5 מוגנים.** מה שחסר בשניים: תנאי שמריץ את `main()` רק כשהקובץ הוא נקודת
הכניסה. **מה שזה חוסם היום:** `test:syntax` נאלץ להסתפק ב-`node --check`, שמוכיח
**פרסינג** בלבד. `import()` — שהיה תופס גם ייבוא שבור, סוד חסר בזמן טעינה, וכל
שגיאת top-level — **יריץ את שני הסוכנים האלה לפרודקשן בזמן ה-CI**. זה מה שנשבר
אם מוסיפים את הבדיקה לפני השומרים.

⚠️ **סחיפה נלווית, רישום בלבד:** שני נוסחי שומר שונים ב-3 הקבצים המוגנים.
`pathToFileURL` הוא הנוסח העמיד (רווחים/סימלינקים בנתיב); `analyst.mjs` משתמש
בישן. ⛔ לא תוקן.

---

## §3 — תמחור ארבעת הממצאים החיים (⛔ אפס תיקון, אפס המלצה)

### א. `src/components/AdminPanel.jsx` — קובץ ענק, ממצא אמיתי

**נמדד: 1,804 שורות** (הסורק נוקב ב-1,805 — הפרש של אחת, שיטת ספירה).
אינו ב-`ACKNOWLEDGED` **במכוון** (`arch-auditor.mjs:153`), כי `STATE:99` נוקב
בשניים בשמם והשתקה שלישית הייתה מרחיבה החלטה שלא התקבלה.

- **עבודה:** פיצול = גל שלם. הקובץ מכיל ≥8 קומפוננטות (`OverviewTab`,
  `WaitlistTable`…) עם state משותף דרך props ו-`onMutate` שמרענן הכול.
- **סיכון:** בינוני-גבוה. `AdminPanel` הוא **מסלול כתיבה** — 3 אתרי RPC
  מכוסים ב-`test:write`. פיצול נוגע ב-`verify` חוליה 3.
- **מה נשבר:** אנטי-flicker (§13) — קומפוננטות שיוגדרו בתוך render ירנדרו מחדש.
- ⛔ **החלטה, לא באג.** או שמפצלים, או שמוסיפים אותו ל-`STATE:99` — ואז ההשתקה
  חוקית. **שתי הדרכים דורשות הכרעה של ניב.**

### ב. `nanoid` GHSA-2v37-7h3g-55p8 — ⚠️ הופרך שהוא ממצא מוצר

```
swing-edge@1.0.1
`-- postcss@8.5.25          ← devDependency (^8.5.21)
  `-- nanoid@3.3.16
```

**`nanoid` הוא תלות טרנזיטיבית של `postcss`, שהוא `devDependency`.**
⇒ **קוד בנייה בלבד. אינו נארז ל-bundle ואינו מגיע למשתמש.**
הפגיעות: "custom generators can loop indefinitely when size is zero" — DoS
שדורש שהתוקף ישלוט ב-`size`. ב-`postcss` בזמן build אין קלט תוקף.

- **עבודה:** `fixAvailable: true` — עדכון `postcss` פוטר. קטן.
- **סיכון:** נמוך. `postcss` הוא לב ה-Tailwind pipeline; שדרוג minor דורש `build` ירוק.
- **מה נשבר:** בסבירות גבוהה כלום. הראיה תהיה `npm run verify`.
- 📌 **התיקון לתיאור:** `STATE` צודק ש**אינו** ה-pin של `react-router` ולכן אינו
  בר-השתקה תחת אותו נימוק — **אבל הנימוק הנכון שונה: זו תלות פיתוח.**
  "1 high" ו"פגיעות שמגיעה למשתמש" אינם אותו דבר.

### ג. bundle — ⚠️ **התיאור "→ code-splitting" מוטעה: הוא כבר קיים**

נמדד ב-`build` של האבחון הזה:

```
dist/assets/index-B9mYHQJC.js     1,625.07 kB │ gzip: 484.84 kB   ← החתיכה היחידה מעל הסף
dist/assets/recharts-DXAgd5RM.js    553.42 kB │ gzip: 156.07 kB   ← כבר מופרד
dist/assets/sentry-CeWs3Mgg.js      197.45 kB │ gzip:  66.11 kB   ← כבר מופרד
dist/assets/AdminPanel-DovrjNOd.js   61.32 kB │ gzip:  16.54 kB   ← כבר מופרד
dist/assets/date-fns-CL5f8NyO.js     22.59 kB │ gzip:   6.44 kB   ← כבר מופרד
dist/assets/index-BIHLg8WR.css      108.96 kB │ gzip:  19.29 kB
```

📌 **`recharts` **כבר** חתיכה נפרדת, וכך גם `AdminPanel`, `sentry` ו-`date-fns`.**
הממצא כפי שנוסח ("1,581KB + recharts 540KB → code-splitting") **מתאר עבודה שנעשתה.**
מה שנותר הוא **חתיכה אחת**: `index` ב-**1,625KB** (484KB gzip).

- **עבודה:** בינונית-גדולה, ו**שונה** ממה שהממצא מתאר: אין כאן ספרייה שמנה
  להוציא — יש את גוף האפליקציה. המועמד הוא פיצול לפי מסך (`React.lazy` על
  טאבים), שנוגע ב-`SwingEdge_App.jsx` (7,659 שורות).
- **סיכון:** בינוני-גבוה. `Suspense` boundary חדש במסלול רינדור חי.
- **מה נשבר:** מסך שנטען עצלנית בלי fallback = מסך לבן. כל `lazy` דורש
  אימות מובייל (iframe 390px, `CONTEXT.md`).
- ⚠️ **סחיפה:** 1,581→**1,625KB** ו-540→**553KB** מאז המדידה ב-09.08.

### ד. `src/components/TickerLogo.jsx:17` ללא `alt` — ⛔ **false positive**

```
:17    <img
:19      alt={`${cleanTicker} logo`}
```

**ה-`alt` קיים** — הוא פשוט בשורה 19, כי ה-`<img>` פרוס על מספר שורות.
הסורק בודק `alt=` **באותה שורה** של `<img`.

📌 **זהו בדיוק הדפוס המבני שכבר תועד ב-`STATE`** עבור "216 כפתורים ללא
`aria-label`". **אותה מחלקה, ממצא חמישי שאינו קיים.**
⇒ **3 ממצאים חיים, לא 4.** ⛔ לא תוקן (`scripts/` מחוץ להיקף).

---

## §4 — אינוונטריזציה: כל המשימות הפתוחות

⛔ **ללא סדר מומלץ** — ניב וקלוד מכריעים.

### נסגרו בפועל ולא עודכנה השורה (⚠️ ייחוס)

| פריט | נרשם | מצב אמיתי |
|------|------|-----------|
| מיגרציית `admin_rpcs` | 09.08 | ✅ הורצה |
| מיגרציית `trade_horizon` | 10.08 | ✅ הורצה (2/2 חלקים) |
| 🔴 `navigator.clipboard` | 05.08 | ✅ נסגר 09.08 |
| `status` DEFAULT `'open'` מת | 10.08 | ✅ פורק עם המיגרציה |
| חוב `AdminPanel` תלוי-RPC | 10.08 | ✅ המפתחות קיימים |
| `TickerLogo` ללא `alt` | 09.08 | ⛔ false positive |

### פתוחים — מדוד ורלוונטי

| פריט | מקור | גודל |
|------|------|-------|
| שימור שבוע 1 = 0/6 | 06.08 | גל מוצר |
| re-engagement — 12 רדומים + 21 תקועים | 09.08 | גל, חסום על הכרעה |
| גל B — תובנות ביניים | 10.08 | גדול (41 אתרי צריכה, 14 קבצים, 6 חוליות) |
| `send-mail` v3→v18 | 06.08 | גל נפרד (15 major × 9 אתרים) |
| אספן CSP | 06.08 | גל נפרד |
| 42 `catch {}` ריקים | — | לא נבדק; ספירה ≠ חומרה |
| `fontshare` חוסם-רינדור (1.46ש) | 06.08 | ⏸️ חסום על רישוי |
| code-splitting / `recharts` | 09.08 | §3ג |
| `AdminPanel` 1,804 | 09.08 | §3א — החלטה |
| `nanoid` / `postcss` | 09.08 | §3ב — קטן |
| שאילתת משפך GA4 ראשונה | 09.08 | קטן, הנתונים בשלים |
| `mentoring` — חי או קוד מת? | 09.08 | תלוי `screen_view` |
| בדיקות הסרת-יכולת (Playwright) | 09.08 | בינוני |
| `mq.addListener` | 09.08 | קטן, הרחבת סקופ |
| `handlePlaybookImageUpload:6211` | 09.08 | מסלול נפרד |
| `grabChartFrame` רוחב-בלבד | 09.08 | קטן |
| שמות CSV ב-UTC | — | קטן |
| אובייקט ברירת מחדל כפול ×6 | 10.08 | בינוני |
| `saridel` נבלע (22/22 ללא stop) | 10.08 | מוצר |
| `hydratedRef` פער לטנטי | 11.08 | לא נושך היום |
| `avg_pnl_by_ccy` / באנר ערבוב | 09.08 | בינוני |
| `menteeStats` בסיס הון של מנטור | 10.08 | חוב מוכר |
| `download-artifact` v7→v8 | — | קטן |
| `docs/TASKS-REGISTRY.md` | — | ⛔ ניב יוצר |
| `arch-auditor` `CODE_EXT` עיוור ל-`.mjs` | 09.08 | גל נפרד אחרי דוח נקי |
| **`Failure Alert` בלי צופה (11/17)** | 11.08 | §2.3 — **חדש, מתומחר** |
| **2/5 סוכנים בלי שומר** | 11.08 | §2.4 — קטן, חוסם `import()` |
| **הפניות `STATE.md:98` רקובות ×3** | 11.08 | §1.4 — קטן |

### ⏸️ חסום על ניב

`why_stopped_r2` · פנייה ל-12 הרדומים · **מסירה ל-`omrikapara1`** (§10.2) ·
3 פידבקים ללא מענה · PR #44 + ענף Dependabot · Sheets של `a0556783290` ·
Stripe Test Mode · Search Console · Workspace · QA מובייל · ערכת OCR ·
עו"ד `/privacy` · מלאי סודות · סימון GA4 · מחיקת `~/Desktop/Swing-edge` ·
הגנת סיסמאות דלופות (Pro)

### ⚠️ ניתן לאיחוד בלי ערבוב תחומים

- **חוליית CI אחת:** 2 שומרי נקודת-כניסה + שדרוג `import()` ב-`test:syntax`
- **חוליית התראות אחת:** צופה ל-`Failure Alert` + תיקון 3 ההפניות ב-`arch-auditor`
- **חוליית תלויות אחת:** `postcss`/`nanoid` + `download-artifact` + PR #44

⛔ **`send-mail` v18 נשאר לבדו** — "גל אחד, שינוי אחד" (הוכרע 06.08).

---

## §5 — שלוש המדידות שמעולם לא נמדדו

### א. `waitlist` ▼2 ואז ▼1 — ⛔ **בלתי-ניתן להכרעה, וזה הממצא**

```
rows_total | distinct_emails | approved | not_approved | first      | last       | last_7d
    61     |       61        |    61    |      0       | 2026-07-07 | 2026-08-07 |    2
```

- **דדופליקציה מופרכת:** 61 שורות = 61 מיילים ייחודיים. אין כפילויות שספירה
  שונה הייתה מסננת.
- **פילטר מופרך:** 61/61 מאושרות, 0 ממתינות. `admin_overview` סופר `count(*)` גולמי.
- **⛔ ואין דרך לדעת אם נמחקו שורות:** `id` הוא `uuid` (לא סדרתי ⇒ אין פערים
  לספור), **ואין ולו טריגר אחד על הטבלה** (נמדד: 0 טריגרים לא-פנימיים)
  ⇒ אין audit trail.

⚠️ **והרצף שנרשם סותר את עצמו:** "▼2 (60) ואז ▼1 נוסף (**61**)" — מ-60 ל-61
הוא **עלייה**. אחד משני המספרים שגוי במקור.

📌 **הפסק:** השאלה "מחיקה או ספירה שגויה" **אינה ניתנת למענה בדיעבד** —
לא היום ולא בעוד חודש. זו אינה עצלות מדידה; **זו היעדר מנגנון.**
⛔ אין לרשום אף אחת משתי ההשערות כעובדה. הערך היחיד הידוע: **61, היום.**

### ב. סתירת התאריך של `de66dc99` — ✅ **הוכרעה**

```
ticker | date       | createdAt                     | status
TTWO   | 2026-07-28 | 2026-07-28 10:27:17.547+00    | OPEN
ONDS   | 2026-08-06 | 2026-08-06 13:45:47.803+00    | OPEN
PATH   | 2026-08-06 | 2026-08-06 13:47:00.107+00    | OPEN
ZIM    | 2026-08-06 | 2026-08-06 13:49:55.048+00    | OPEN
```

**התאריך הוא 06.08. הפרומפט של 08.08 צדק, והשורה ב-`STATE` שגויה.**
⚠️ השעות (13:45 / 13:47 / 13:49 UTC) תואמות **מדויק** — נשמר השעון, הוחלף היום.
זו סחיפת-העתקה, לא מדידה סותרת.

**נלווה:** 4/4 פתוחות ✅ · הוותיקה `TTWO` מ-28.07 = **14 ימים** נכון ל-11.08
(נרשם 13 — תלוי-תאריך, זז מאליו).

### ג. `activation` — המכנה נמדד מחדש

**12/46 = 26%** — מתוך **כלל הרשומים ב-`auth.users`**.
(12 = `count(distinct user_id)` ב-`trades`.)

- הערך שנרשם, `12/41 = 29%`, השתמש במכנה מ-06.08.
- מכנה חלופי הוגן: 42 משתמשים נרשמו **לפני** 07.08 ⇒ **12/42 = 29%**.
- ⚠️ **המונה לא זז מ-06.08.** כל ירידה באחוז היא **גידול במכנה בלבד** —
  4 נרשמים חדשים שאיש מהם לא רשם עסקה. ⛔ זו אינה הרעה בהתנהגות.

### ד. 🆕 ממצא שלא היה ברשימה: **חמישה ימים, אפס כתיבות**

```
last_trade_at | 2026-08-06 13:49:55+00
trades_48h    | 0
trades_7d     | 3          ← בדיוק ONDS/PATH/ZIM של de66dc99
```

**העסקה האחרונה בכל ה-DB היא מ-06.08 13:49.** הדפוס שנרשם כ-"0 עסקאות
ב-48 שעות" הוא בפועל **0 עסקאות ב-5 ימים**, ו-3 העסקאות של השבוע כולן של
משתמש **אחד** ובאותן 4 דקות.

⚠️ **⛔ אין לקרוא לזה רגרסיה בלי מדידה נוספת** — 46 משתמשים ו-0 כתיבות הוא
גם הדפוס שהיה לפני. הרישום כאן הוא **תיקון גודל**, לא מסקנה חדשה.

---

## נספח — כל ההנחות שהופרכו באבחון הזה

| # | ההנחה | מקור | מה נמדד |
|---|--------|------|----------|
| 1 | מיגרציית `admin_rpcs` טרם הורצה | `STATE` | הורצה |
| 2 | מיגרציית `trade_horizon` טרם הורצה | `STATE` | הורצה, 2/2 חלקים |
| 3 | `navigator.clipboard` בלי בדיקת קיום | `STATE` 🔴 | מוגן מלא מ-09.08 |
| 4 | `status` DEFAULT `'open'` — פצצה חיה | `STATE` | `'OPEN'` בפועל |
| 5 | **מערכת ההתראות אינה מנוטרת** | פרומפט | 14/17 מנוטרים; החור היחיד = `Failure Alert` |
| 6 | **האירוע היה שקט** | פרומפט | מייל+דיסקורד ב-**11 שניות** |
| 7 | **`Watchdog` היה מגיע תוך 6.5ש'** | פרומפט | פרסם **ירוק** ב-08:48; אמת = 25.1ש', ובפועל **אף פעם** |
| 8 | `TickerLogo:17` ללא `alt` | סורק | `alt` ב-`:19` — FP מבני |
| 9 | `nanoid` = ממצא מוצר | סורק | `devDependency` — לא נארז |
| 10 | תאריך `de66dc99` = 07.08 | `STATE` | **06.08** |
| 11 | "0 עסקאות ב-48ש'" | `STATE` | **5 ימים** |
| 12 | `activation` 12/41 | `STATE` | **12/46 = 26%** |
| 13 | bundle "→ code-splitting" (עבודה שלא נעשתה) | סורק | `recharts`·`sentry`·`AdminPanel`·`date-fns` **כבר מופרדים**; נותרה חתיכת `index` |

### סחיפת מדידה נלווית (רישום בלבד)

| קובץ | ב-`STATE` | נמדד 11.08 |
|------|-----------|-------------|
| `SwingEdge_App.jsx` | 7,504 (09.08) | **7,659** |
| `src/i18n.js` | 2,894 (09.08) | **2,900** |
| `AdminPanel.jsx` | 1,805 (סורק) | **1,804** |

📌 מאשש את כלל 08.08: **מספר שנרשם פעם ולא נמדד שוב אינו עובדה.**

---

## מה האבחון הזה **לא** עשה

⛔ אפס שינוי קוד · אפס מיגרציה · אפס כתיבה ל-DB · אפס מייל · אפס המלצת סדר.
⛔ שלוש ההפניות ב-`arch-auditor.mjs` **לא תוקנו** — `scripts/` מחוץ להיקף.
⛔ `AdminPanel`, `nanoid`, ה-bundle — **תומחרו בלבד.**
