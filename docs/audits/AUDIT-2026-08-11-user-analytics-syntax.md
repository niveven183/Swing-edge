# אבחון — קריסת `user-analytics` בריצה 31470184666

**תאריך:** 2026-08-11 · **HEAD בזמן האבחון:** `b3fef54` · **HEAD שנכשל:** `d7e4a4d`
**סוג:** §8.1 שלב 1 — אבחון read-only. ⛔ אפס תיקון בוצע.

---

## §0 · פסק דין בשורה אחת

`scripts/user-analytics.mjs` **אינו מתקמפל**. תווי גרש-הפוך (`` ` ``) שהוכנסו בגל
האופק לתוך הערות SQL — בתוך template literal — סוגרים את המחרוזת באמצע, והקובץ
נכשל בפרסינג לפני שהריצה מתחילה. **ההשערה של ניב נכונה בכיוון ושגויה במנגנון.**

---

## §1 · שורת השגיאה — verbatim

`gh run view 31470184666 --log-failed`, שלב `Compose analytics report`:

```
2026-08-11T07:44:19.8274053Z file:///home/runner/work/Swing-edge/Swing-edge/scripts/user-analytics.mjs:320
2026-08-11T07:44:19.8276145Z       `${CTE}
2026-08-11T07:44:19.8277043Z SyntaxError: missing ) after argument list
2026-08-11T07:44:19.8278433Z     at compileSourceTextModule (node:internal/modules/esm/utils:346:16)
2026-08-11T07:44:19.8279322Z     at ModuleLoader.moduleStrategy (node:internal/modules/esm/translators:110:18)
2026-08-11T07:44:19.8280084Z     at #translate (node:internal/modules/esm/loader:559:20)
2026-08-11T07:44:19.8289051Z Node.js v22.23.1
2026-08-11T07:44:19.8321695Z ##[error]Process completed with exit code 1.
```

**`compileSourceTextModule` היא שורת המפתח.** זו שגיאת *קימפול*, לא ריצה. הקובץ
נכשל בפרסינג — אף שורת קוד בו לא רצה. מכאן שלושה דברים נגזרים ישירות:

1. **ה-SQL אינו הגורם, ואינו יכול להיות.** השאילתה מעולם לא נשלחה. הבדיקה
   שניב הריץ בפרודקשן (`... where status='OPEN' and date < (current_date - 30)`
   → 15, ללא שגיאה) מאשרת את אותה מסקנה מכיוון שני ובלתי-תלוי.
2. **`SUPABASE_DB_URL` אינו הגורם.** הסוד הוזרק (`SUPABASE_DB_URL: ***` בלוג),
   ובכל מקרה לא נקרא — `const` בשורה 22 לא הגיע לביצוע.
3. **מנגנון השגיאה של הסקריפט עצמו עיוור לזה.** ה-catch העליון
   (`user-analytics.mjs:853`, `::error::user-analytics failed`) הוא קוד בתוך
   הקובץ. קובץ שלא התקמפל אינו יכול לתפוס את שגיאת הקימפול של עצמו. ⚠️ זו לא
   פשלה בכתיבת ה-catch — זו תקרה מבנית של כל טיפול-שגיאות in-file.

---

## §2 · שחזור מקומי

```
$ node scripts/user-analytics.mjs
/Users/nivhareven/Swing-edge/scripts/user-analytics.mjs:320
      `${CTE}
SyntaxError: missing ) after argument list
```

שחזור מלא, **ללא שום env** — לא `SUPABASE_DB_URL`, לא `DRY_RUN`. אותה שורה,
אותה שגיאה. `node --check scripts/user-analytics.mjs` נכשל זהה. הכשל דטרמיניסטי
ובלתי-תלוי בסביבה, כפי שמתחייב משגיאת קימפול.

---

## §3 · השורש — המנגנון המדויק

הבלוק (`gatherStuck`, `user-analytics.mjs:317-344`). ה-literal נפתח בשורה 320:

```js
319  const rows = q(
320        `${CTE}
...
330           -- ⚠️ הגיל של עסקה פתוחה נמדד מ-`date` (תאריך הכניסה) ולא מ-`createdAt`.
331           -- scripts/retention.sql:10 כבר קובע ש-`createdAt` אינו חותמת יצירת-שורה:
332           -- הייבוא גוזר אותו מ-`date` (import/normalizeRow.js:179), ו-40/59 השורות
333           -- נושאות את החתימה הזו. נמדד 2026-08-10: 59/59 מקיימות
334           -- `"createdAt"::date = date`, ולכן המספר אינו זז היום — התיקון הוא נגד
335           -- סחיפה עתידית, ברגע שתיווצר שורה שבה השתיים נבדלות.
...
344        `);
```

**`--` הוא הערה ב-SQL. הוא אינו הערה ב-JavaScript.** בתוך template literal אין
תחביר הערות בכלל — הכול תוכן מחרוזת. הפרסר של JS רואה רק דבר אחד: גרש-הפוך.

הגרש-ההפוך הראשון בשורה 330 (זה שלפני `date`) **סוגר את ה-literal שנפתח ב-320.**
מאותו תו והלאה הפרסר קורא קוד, לא מחרוזת: `date` הוא מזהה חשוף, הטקסט העברי
שאחריו הוא טוקנים חשופים, וכולם יושבים בתוך רשימת הארגומנטים של `q(` שנפתחה
בשורה 319 ומעולם לא נסגרה כשורה. מכאן `missing ) after argument list`.

**ספירה:** 12 גרשים-הפוכים בבלוק — 320:1 · 330:4 · 331:2 · 332:2 · 334:2 · 344:1.
המספר **זוגי**, ולכן הם "מזדווגים" — אבל בהיסט של אחד. הזיווג המכוון
(320↔344) הופך לשרשרת (320↔330a, 330b↔330c, …, 334b↔344), וכל מה שביניהם נקרא
כקוד. ⚠️ **המסקנה החשובה: ספירה זוגית אינה בטיחות.** גרשיים מאוזנים היו נותנים
מחרוזת שמתקמפלת ומכילה SQL שגוי — כשל שקט במקום כשל צועק. כאן היינו בני-מזל.

---

## §4 · ההשערה של ניב — ✅ בכיוון · ❌ במנגנון

> "אולי ההערות בעברית שהוספתי — רב-שורתיות בתוך template literal."

**מה שנכון:** ההערות הן הגורם. הן נוספו בגל האופק, ובלעדיהן הקובץ מתקמפל.

**מה שאינו נכון:** לא העברית ולא הריבוי-שורות אשמים. שניהם תקינים לחלוטין בתוך
template literal — literal *נועד* להשתרע על שורות, והוא UTF-8. הגורם היחיד הוא
**תו הגרש-ההפוך**, ששימש כאן לציטוט שמות עמודות בסגנון markdown.

**הפרדה בניסוי מבוקר — לא נטען, נמדד:**

```
# משתנה בדיקה — אותה הערה, עם גרשיים הפוכים:
$ node --check /tmp/bt-repro.mjs
/private/tmp/bt-repro.mjs:4
  `${CTE}
SyntaxError: missing ) after argument list

# בקרה — הערה עברית רב-שורתית, ללא גרשיים הפוכים:
$ node --check /tmp/bt-control.mjs
CONTROL PASSES — Hebrew + multi-line are harmless
```

שני הקבצים זהים למעט הגרשיים. אחד נופל, אחד עובר. זו ההפרדה.

---

## §5 · הריצה האחרונה שהצליחה — אנחנו הגורם

`gh run list --workflow="User Analytics" --limit 12`:

| # | תאריך (UTC) | תוצאה | HEAD |
|---|---|---|---|
| 31470184666 | 2026-08-11 07:44 | ❌ failure | `d7e4a4d` |
| 31369021060 | 2026-08-10 08:12 | ✅ success | `c4810f17` |
| 31301284640 | 2026-08-09 07:30 | ✅ success | `6cb43c47` |
| 31246211901 | 2026-08-08 07:27 | ✅ success | `291a938b` |

ועוד 8 ריצות ירוקות רצופות אחורה עד 2026-07-31. **12/12 הריצות שנבדקו: 11 ירוקות
עד `c4810f17`, ואז הראשונה ב-`d7e4a4d` נופלת.**

`d7e4a4d` נוצר ב-2026-08-10 17:39 — **אחרי** הריצה הירוקה של 08:12 באותו יום
ולפני הריצה שנפלה. הגבול חד: הכשל הראשון הוא הריצה הראשונה שהריצה את הקוד שלנו.
⚠️ **אנחנו הגורם. אין נסיבה מקלה.**

**כמה אבד:** ריצה אחת מתוך אחת מאז הקומיט (1/1). ה-email וסיכום ה-Discord של
2026-08-11 לא נשלחו, וכל השלבים שאחרי דולגו.

**מצב ה-day-over-day:** שלב שמירת ה-cache דולג, ולכן `.analytics-state/prev.json`
נשאר של 2026-08-10. ⚠️ אבל זה **לא** יהפוך לשקר: `readPrevState` שומר `date`
בקובץ, ו-`user-analytics.mjs:623` מדפיס `(בסיס ההשוואה: <תאריך>)` בגוף הדוח.
הדוח הבא יכריז על הפער במקום להסתיר אותו. אין כאן פריט לתיקון.

---

## §6 · האם השינוי שלנו הוא הגורם היחיד?

**כן.** `git log c4810f17..d7e4a4d -- scripts/user-analytics.mjs` מחזיר קומיט
אחד בלבד — `d7e4a4d`. ה-diff המלא של הקובץ בקומיט הוא hunk יחיד:

```diff
+         -- ⚠️ הגיל של עסקה פתוחה נמדד מ-`date` (תאריך הכניסה) ולא מ-`createdAt`.
+         -- scripts/retention.sql:10 כבר קובע ש-`createdAt` אינו חותמת יצירת-שורה:
+         -- הייבוא גוזר אותו מ-`date` (import/normalizeRow.js:179), ו-40/59 השורות
+         -- נושאות את החתימה הזו. נמדד 2026-08-10: 59/59 מקיימות
+         -- `"createdAt"::date = date`, ולכן המספר אינו זז היום — התיקון הוא נגד
+         -- סחיפה עתידית, ברגע שתיווצר שורה שבה השתיים נבדלות.
          (SELECT count(DISTINCT user_id) FROM clean
-            WHERE status = 'OPEN' AND "createdAt" < now() - interval '30 days'),
+            WHERE status = 'OPEN' AND date < (current_date - 30)),
          (SELECT count(*) FROM clean
-            WHERE status = 'OPEN' AND "createdAt" < now() - interval '30 days'),
+            WHERE status = 'OPEN' AND date < (current_date - 30)),
```

10 שורות: 6 הערה, 4 שינוי עמודה. **שינוי העמודה תקין** — ניב אימת אותו בפרודקשן,
ובכל מקרה הוא לא הגיע לריצה. **שש שורות ההערה הן כל הכשל.** אין גורם שני.

---

## §7 · סריקה — האם סקריפט סוכן אחר עלול ליפול כך?

**שתי סריקות, כי אחת אינה מספיקה** (§3 — ספירה זוגית שורדת בדיקת תחביר):

**1 · בדיקת תחביר על כל הקבצים.** `node --check` על כל `scripts/*.mjs`, `api/*.js`
ו-`api/_lib/*.js`:

```
FAIL scripts/user-analytics.mjs: /Users/nivhareven/Swing-edge/scripts/user-analytics.mjs:320
=== syntax scan done ===
```

**קובץ אחד נופל. כל השאר עוברים.** אין כשל קימפול שני חבוי בריפו.

**2 · חיפוש הדפוס עצמו** — גרש-הפוך בתוך שורת הערת SQL,
`grep -n '^\s*--.*\`' scripts/*.mjs api/_lib/*.js`:

```
scripts/user-analytics.mjs:330 · :331 · :332 · :334
```

**4 התאמות, כולן באותו בלוק.** אף סקריפט סוכן אחר — `analyst.mjs`,
`daily-digest.mjs`, `data-guardian.mjs`, `arch-auditor.mjs` — אינו נושא את הדפוס,
למרות שכולם קיבלו נגיעות בגלים האחרונים (`ac9b38f` נגע בשלושה מהם). **הדפוס
ייחודי לגל האופק ואינו נוהג שהתפשט.** ⚠️ אבל שום דבר בריפו אינו *מונע* אותו
מלחזור — ראה §8.2.

---

## §8 · שני פריטי המרשם

### 8.1 · `upsertBlob` בולע שגיאות — אותו מחלקה כמו INCIDENTS#14

`src/lib/userSettings.js:75-85` — נמצא באבחון ההתמדה של אתמול, נרשם כאן שוב כדי
שלא ייפול בין הכיסאות:

```js
async function upsertBlob(userId, blob, client) {
  if (!client) return;
  try {
    await client.from(TABLE).upsert({ ... });
  } catch (e) {
    console.error("userSettings: upsert failed", e);
  }
}
```

`supabase-js` **מחזיר** שגיאות כערך ולא זורק אותן. `catch` תופס רק חריגות רשת;
כישלון RLS או הפרת constraint חוזרים כ-`{ error }` — ו-`upsertBlob` אינו מפרק
את התשובה כלל. ⚠️ ההגדרה נכשלת, המשתמש רואה הצלחה, ואיש אינו יודע.

**זו סטייה מקומית, לא נוהג בית:** `loadSettings` (`:94`) ו-`migrateFromLocalStorage`
(`:145`) — באותו קובץ — כן מפרקים `{ error }`, וכך גם `tradeWrite.js:39`.
3 מתוך 4 מסלולי הכתיבה/קריאה במודול תקינים; זה הרביעי.

### 8.2 · `npm run verify` ירוק בעוד `scripts/` שבור

**הממצא החד:** מתוך 20 חוליות ה-verify, **חוליה אחת בדיוק נוגעת בקובץ הזה** —
`test:horizon`, בלוק 10 — והיא נוספה **באותו קומיט שהרס אותו**:

```js
// scripts/horizon-test.mjs:213
const ua = readFileSync(new URL("../scripts/user-analytics.mjs", import.meta.url), "utf8");
check("שאילתת stuck-open אינה נשענת על createdAt", !/.../.test(stuck));
```

`readFileSync` — הקובץ נקרא כ**טקסט** ונבדק ב-regex. הוא **לעולם אינו מיובא ואינו
נפרס.** לכן שתי ה-assertions עברו על קובץ שאינו מתקמפל: הן שאלו "האם המחרוזת
מכילה `status = 'OPEN' AND date`" — והיא כן מכילה. הבדיקה ענתה נכונה על השאלה
שנשאלה, והשאלה הייתה הלא-נכונה.

**התמונה הרחבה:** אף חוליה מ-20 אינה מריצה או מייבאת סקריפט סוכן
(`user-analytics` · `analyst` · `data-guardian` · `daily-digest` · `arch-auditor`).
`build` בונה את ה-frontend ואינו נוגע ב-`scripts/`. **הכיסוי של סוכני ה-CI
בשרשרת ה-verify הוא 0 מתוך 5.**

**מה זה אומר על גל האופק:** `verify` דיווח 140/140 ב-`test:horizon` ו-20/20
חוליות — **והדיווח היה נכון**. אף בדיקה לא שיקרה. פשוט אף אחת מהן לא נשאלה האם
הקובץ מתקמפל.

**⚠️ הוכחה, לא טענה** — `npm run verify` הורץ עכשיו, 11.08, כשהקובץ **שבור**:

```
10 · user-analytics — stuck-open לפי date
  ✓ שאילתת stuck-open אינה נשענת על createdAt
  ✓ שאילתת stuck-open נשענת על date

✅ horizon: 140/140 assertions passed
...
✓ built in 6.99s
```

השרשרת ירוקה מקצה לקצה — 20/20 חוליות ו-`build` — **על ריפו שבו סוכן CI אינו
מתקמפל.** זה מדד את הפער ולא תיאר אותו.

### 8.3 · הכשל אינו שקט — אבל האיחור אמיתי

`watchdog.yml:52` מחזיק `[user-analytics.yml]=30` — סף גיל של 30 שעות על
**ההצלחה** האחרונה (`?status=success&per_page=1`). הצלחה אחרונה: 2026-08-10 08:12
UTC → הווטש-דוג יצעק החל מ-2026-08-11 14:12 UTC, כ-6.5 שעות אחרי הכשל.

⚠️ **הווטש-דוג מודד התיישנות, לא כישלון.** ריצה שנופלת מדי יום תיראה לו כמו
ריצה שלא רצה — התוצאה זהה, אבל תמיד באיחור של עד 30 שעות. במקרה הזה ניב ראה
את הכשל לפניו.

---

## §9 · מסלולי תיקון — מתוארים, ⛔ לא נבחרו

לשלב 2. אין כאן המלצה.

| # | מסלול | היקף |
|---|--------|------|
| A | להסיר את הגרשיים מ-6 שורות ההערה | הקובץ בלבד · מחזיר ירוק · אינו מונע חזרה |
| B | להוציא את ההערה מחוץ ל-literal (מעל `q(`) | הקובץ בלבד · ההערה שורדת כהערת JS |
| C | חוליית `node --check` על `scripts/**/*.mjs` ב-verify | תופסת ספירה אי-זוגית · ⛔ לא זוגית (§3) |
| D | `arch-auditor` יאסור גרש-הפוך בתוך template literal | תופס גם זוגי · דורש נגיעה בסורק |
| E | `upsertBlob` יפרק `{ error }` כמו 3 אחיו | §8.1 · נפרד מהכשל הזה |

---

## §10 · הנחות שהאבחון בדק

| # | הנחה | פסק |
|---|-------|-----|
| 1 | ה-SQL החדש הפיל את הסקריפט | ❌ הופרך — שגיאת קימפול, ה-SQL לא נשלח |
| 2 | ההערות שהוספנו הן הגורם | ✅ אושש |
| 3 | הסיבה היא עברית / ריבוי-שורות | ❌ הופרך בניסוי מבוקר (§4) |
| 4 | הסיבה היא תו הגרש-ההפוך | ✅ אושש בניסוי מבוקר |
| 5 | הריצה האחרונה שהצליחה קדמה ל-`d7e4a4d` | ✅ אושש — `c4810f17`, 2026-08-10 08:12 |
| 6 | השינוי שלנו הוא הגורם היחיד | ✅ אושש — hunk אחד, קומיט אחד |
| 7 | סקריפטים אחרים עלולים ליפול כך | ❌ הופרך *כרגע* — 0/4 נושאים את הדפוס |
| 8 | `verify` ירוק כי אינו מכסה `scripts/` | ✅ אושש — 0/5 סוכנים מכוסים |
| 9 | הכשל שקט | ❌ הופרך חלקית — ווטש-דוג יתפוס, באיחור עד 30ש' |
| 10 | ה-day-over-day יישבר בריצה הבאה | ❌ הופרך — בסיס ההשוואה מודפס (`:623`) |

## §11 · מה האבחון לא קבע

- **מה לתקן** — §9 מתאר, ⛔ אינו בוחר. שלב 2.
- **האם `upsertBlob` נכשל אי-פעם בפועל** — אין לוג צד-שרת שיכול להוכיח.
- **האם הווטש-דוג צריך לתפוס failure ולא רק staleness** — נצפה, לא הוכרע.
