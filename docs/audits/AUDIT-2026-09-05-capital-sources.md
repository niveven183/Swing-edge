# AUDIT 2026-09-05 — מקורות ההון · אבחון read-only

| | |
|---|---|
| תאריך | 2026-09-05 |
| HEAD | `66f261e` |
| עץ | נקי (`git status --porcelain` ריק לפני ואחרי) |
| סוג | **אבחון read-only** — ⛔ אפס תיקון · ⛔ אפס DB · ⛔ אפס migration |
| רמה (§15) | **T3** · תשובות: הפיכות=לא · אמון=**כן** · אבטחה=לא · רוחב=**כן** · ודאות=**כן** ⇒ 3 ⇒ T3 |
| קבצים שנגעו בהם | ⛔ **אפס.** רק קריאה + סקריפטי מדידה מחוץ לריפו (`/tmp/*.mjs`, לא נשמרו) |

⚠️ **הכלל שהנחה את הדוח:** שישה ממצאים נמדדו **בנפרד**, ואיחודם לסיפור אחד
מופיע רק ב-§8 — ⛔ אחרי שכל אחד עמד בזכות עצמו.

---

## 0. תקציר מנהלים — מה הופרך

⛔ **שלוש הנחות שהיו בפרומפט או בקריאה הראשונה — הופרכו במדידה:**

| # | ההנחה | הפסיקה | ההוכחה |
|---|-------|--------|---------|
| 1 | `$831` ו-`$876.89` הם אותו מספר שנסחף | ⛔ **הופרכה** | הם **שתי כמויות שונות בתכנון**: בסיס-הון בספוט מול הון-נוכחי בקיבוע-עבר. `876.89 − 45.66 = 831.23` |
| 2 | תיקון `capitalCurrency` ל-USD יחזיר את לוח הסיכונים לחיים | ⛔ **הופרכה** | `deriveInstrumentCurrency({currency:'ILS'})` → `state:"contradicted"` ⇒ `matchesCapital` **false מול USD וגם מול ILS**. התווית השמורה היא הראיה הסותרת |
| 3 | הזזת עמודות בלוח הסיכונים (Q5) | ⛔ **הופרכה בקוד** | 9 `<th>` מול 9 `<td>`, 1:1 לפי סדר. אין היסט אפשרי |

✅ **מה כן נמדד כשורש:** עובדה אחת במעלה הזרם — `capital === 2500`
(`=== DEFAULT_CAPITAL`) **וגם** `capitalCurrency === "ILS"` — מזינה את כל שישה
הסימפטומים דרך **חמישה** אתרי תצוגה נפרדים.

🔴 **מה חסום:** מספר המשתמשים המושפעים בפרודקשן. ⛔ אין `SUPABASE_SERVICE_ROLE_KEY`,
⛔ אין `SENTINEL_QA_*`, והפרומפט אוסר גישה ל-DB. ראה §2.4 — SQL מוכן, לא הורץ.

---

## 1. Q1 — מיפוי כל מקורות ההון

### 1.1 טבלת המקורות

| # | מקור | מיקום | מי כותב | מי קורא | מתי |
|---|------|-------|---------|---------|------|
| S1 | `capital` (state) | `:1311-1313` | `setCapital` — **שלושה אתרים בלבד**: `:1184` (onboarding) · `:1748` (הידרציה) · `:7180` (הגדרות) | `displayCapital` · `equityBaseD` · `capitalShown` · `riskPct` · `sizePosition` · `:5880` · `:944` | seed מ-`localStorage` ב-mount; דריסה בהידרציה |
| S2 | `capitalCurrency` (state) | `:1333-1339` | `:1201` (onboarding, whitelist) · `:1758-1763` (הידרציה + back-compat) · `:7204` (בורר הגדרות) | `convert()` · `resolveEquityBase` · `matchesCapital` · `capSym` · `currency:` בכתיבה ידנית | seed מ-`swingEdgeCapitalCurrency` \|\| `swingEdgeAccountCurrency` \|\| `"USD"` |
| S3 | `accountCurrency` (state) | — | בורר הגדרות | יעד ההמרה בלבד | — |
| S4 | `localStorage.swingEdgeCapital` | דפדפן | `:1185` · `:7181` | seed של S1 ב-`useState` **בלבד** | mount |
| S5 | `localStorage.swingEdgeSettings` (mirror) | דפדפן | `saveSettings` · `loadSettings` · `migrateFromLocalStorage` | `readMirror()` — fallback לקריאה שנכשלה | כל שמירה |
| S6 | `user_settings.settings->>'capital'` (jsonb) | Supabase | `upsertBlob` (debounce 1000ms) | `loadSettings` ← `:1747` | הידרציה + כל שינוי |
| S7 | `DEFAULT_CAPITAL = 2500` | `src/utils.js:6` | — | fallback ב-`:1311` **וגם** תנאי הבאנר `:1806` | כש-`localStorage` ריק/לא-מספרי |
| S8 | `_capitalAtEntry` | `:2870` | חותם `capital` בזמן כתיבה ידנית | תיעוד היסטורי; ⛔ **אינו** מזין תצוגה | פר-עסקה |
| S9 | `capitalMaybeClobbered` | `:1222`, נקבע `:1806-1810` | `status === "ok" && s.capital === DEFAULT_CAPITAL` | באנר האזהרה | הידרציה |

### 1.2 חמשת אתרי התצוגה — ⛔ **הם ⛔ אינם אותו מספר**

| מה על המסך | קוד | הביטוי | הכמות |
|-------------|-----|---------|--------|
| `₪2,500` (מחשבון פוזיציה) | `:5880` | `Auto: {capSym}{capital.toLocaleString()}` | **`capital` גולמי + סמל `capitalCurrency`.** ⛔ אפס המרה |
| `$831` (לוח סיכונים) | `:4791` | `{t.fromCapital} {dispSym}{Math.round(capitalShown)}` | `capitalShown = displayCapital` = `convert(capital, ILS→USD, ספוט)` |
| `$876.89` (כותרת) | `:4181`·`:4417`·`:8258` | `fmtBalance(curEquity, dispCcy)` | `curEquity = equityBase + closedPnL + openPnL`, ו-`equityBase` = **קיבוע עבר** (`resolveEquityBase`) |
| `₪2,500 = $831` (תג FX) | `:944` | `{capSym}{capital} = {dispSym}{Math.round(shown)}` | שני הראשונים זה לצד זה |
| הפסד פוטנציאלי | `:7816` | `{capSym}{Math.round(effPotLoss)}` | נגזר מ-`capital` הגולמי |

### 1.3 החשבון

```
capital = 2500 · capitalCurrency = "ILS" · accountCurrency = "USD"

$831  = Math.round(displayCapital)  ⇒  displayCapital ∈ [830.5, 831.5)
                                    ⇒  שער ILS→USD משתמע ∈ [3.0066, 3.0102]

$876.89 − (5.20 + 40.46) = $831.23  ⇒  equityBase = 831.23
```

### 1.4 הפסיקה על «single source of truth»

⛔ **לא רוככה. הפסיקה מפוצלת:**

- ✅ **כמקור נתון — מתקיים.** יש `capital` אחד ו-`capitalCurrency` אחד. `setCapital`
  נקרא משלושה אתרים בלבד, כולם מנוטרים. ⛔ אין חישוב inline של הון.
- ⛔ **כחוזה תצוגה — ⛔ אינו מתקיים.** אותה עובדה מודפסת בשלוש יחידות שונות
  (גולמי · ספוט · קיבוע-עבר) בלי שהמסך אומר איזו היא איזו. `$831` ו-`$876.89`
  **⛔ אינם סתירה** — הם שתי כמויות מתוכננות — אבל **⛔ שום דבר על המסך אינו אומר
  זאת**, ולכן הם נקראים כסתירה. זה ⛔ אינו באג חשבוני; זה חוזה תצוגה חסר.
- 🔴 **`₪2,500` מול `$831` — כאן כן יש כשל.** אותו רגע, אותו מסך, שני סמלי מטבע
  שונים על אותה כמות בדיוק (`capital`). `:5880` ⛔ אינו מכבד את `dispCcy` בכלל.

---

## 2. Q2 — מאיפה מגיע ה-`₪`

### 2.1 שלושת אתרי הכתיבה של `capitalCurrency`

| אתר | קוד | האם יכול לייצר `ILS` בלי כוונת המשתמש |
|-----|-----|------------------------------------------|
| Onboarding | `:1197-1201` | ⛔ לא — whitelist `cc === "USD" \|\| cc === "ILS"`, מגיע מבחירה מפורשת |
| Back-compat בהידרציה | `:1758-1763` | ✅ **כן** — `capCcy` נופל ל-`s.accountCurrency` כשאין `capitalCurrency` בבלוב. משתמש ותיק שבחר **תצוגה** בשקלים לפני פיצול T10 מקבל את השקל כמטבע ה**נתון** |
| בורר הגדרות | `:7203-7205` | ⛔ לא — בחירה מפורשת, ⛔ ובלי המרה של המספר |

🔴 **האתר החשוד: `:1758-1763`.** הוא הופך «אני רוצה לקרוא בשקלים» ל-«ההון שלי
נקוב בשקלים» — ⛔ שתי טענות שונות שפיצול T10 נועד להפריד.

### 2.2 מה `ILS` עושה במורד הזרם — אתר הכתיבה הידנית

`:2851` — כל עסקה שנכתבת ידנית נחתמת:

```js
currency: capitalCurrency,                        // :2851
currency_source: CURRENCY_SOURCE.MANUAL_CAPITAL,  // :2856
_capitalAtEntry: capital,                          // :2870
```

⚠️ ההערה בקוד עצמו כבר מודה ש**זו הנחה ולא ראיה** (`B-129`). ⇒ **`capitalCurrency = "ILS"`
מדביק `currency: 'ILS'` לכל עסקה ידנית — כולל `NVDA` ו-`BE`.**

### 2.3 המדידה שסוגרת את Q3 (ראה §3)

```
currency:'ILS'                          → {"code":null,"state":"contradicted","reason":"ils_never_measured"}
                                          matchesCapital(USD): false | matchesCapital(ILS): false
currency:'USD'                          → {"code":"USD","state":"assumed"}   matchesCapital(USD): true
אין שדה currency                        → {"code":"USD","state":"assumed"}   matchesCapital(USD): true
currency:'ILS' + MANUAL_CAPITAL         → {"code":null,"state":"contradicted"} שניהם false
```

### 2.4 כמה משתמשים בפרודקשן — 🔴 **חסום**

⛔ **לא נמדד.** אין `SUPABASE_SERVICE_ROLE_KEY`, אין `VITE_SUPABASE_URL` בסביבה
(`.env` מחזיק שמות בלבד), והפרומפט אוסר במפורש «⛔ אפס DB». ⛔ **ולא נוחש.**

⚠️ **ו-`METRICS.md` ⛔ אינו יכול לענות:** `M-009` מדד `capital` בלבד
(`4/39` שורות `user_settings` נושאות בדיוק `2500`; `3/39` נושאות `10000`;
`capital` חסר ב-`0/39`). ⛔ **התפלגות `capitalCurrency` ⛔ מעולם לא נמדדה.**

**ה-SQL להרצה ידנית (ניב בלבד, SQL Editor) — שלוש שאלות, שלושה מכנים:**

```sql
-- (א) התפלגות capitalCurrency — האוכלוסייה: כל שורות user_settings
SELECT COALESCE(settings->>'capitalCurrency', '(חסר)') AS cap_ccy,
       COUNT(*) AS n,
       COUNT(*) OVER () AS denom_rows
FROM public.user_settings
GROUP BY 1 ORDER BY n DESC;

-- (ב) capitalCurrency שאינו תואם למטבע העסקאות
-- האוכלוסייה: משתמשים עם >=1 עסקה  ⛔ ולא כל השורות
WITH per_user AS (
  SELECT t.user_id,
         COUNT(*) AS n_trades,
         COUNT(*) FILTER (WHERE t.currency = 'ILS') AS n_ils,
         COUNT(*) FILTER (WHERE t.currency = 'USD' OR t.currency IS NULL) AS n_usd
  FROM public.trades t GROUP BY t.user_id
)
SELECT COUNT(*) FILTER (WHERE us.settings->>'capitalCurrency' = 'ILS' AND p.n_usd > 0) AS ils_cap_with_usd_trades,
       COUNT(*) FILTER (WHERE us.settings->>'capitalCurrency' = 'ILS')                 AS ils_cap_any,
       COUNT(*)                                                                        AS denom_users_with_trades
FROM per_user p JOIN public.user_settings us ON us.user_id = p.user_id;

-- (ג) עסקאות שנחתמו currency='ILS'  ⇒  contradicted  ⇒  יוצאות מלוח הסיכונים
-- האוכלוסייה: כל העסקאות הפתוחות
SELECT COUNT(*) FILTER (WHERE currency = 'ILS') AS ils_stamped,
       COUNT(*)                                 AS denom_open_trades
FROM public.trades WHERE status = 'OPEN';
```

⚠️ **תשובת (ג) היא המספר שקובע את היקף מסלול-התיקון**, ⛔ לא (א).

---

## 3. Q3 — האם `0.00%` / `—` הם תוצאה של Q2

### 3.1 השרשרת המדודה (`:4708-4712`)

```js
const derived  = deriveInstrumentCurrency(t);
const sameCcy  = matchesCapital(derived, capitalCurrency);
const hasStop  = sameCcy && t.stop != null && t.shares > 0;
const riskDollar = hasStop ? Math.abs(t.entry - t.stop) * t.shares : null;
const riskPct    = hasStop && capital > 0 ? (riskDollar / capital) * 100 : null;
```

`:4719` — `unverifiedCcyCount = openRisks.filter(t => !t.sameCcy).length` ⇒ הבאנר
«2 עסקאות פתוחות נסחרות במטבע לא מאומת».

### 3.2 ✅ אושש — ⛔ **וההמלצה שבפרומפט הופרכה**

✅ **אושש:** `0.00%` · `$Risk —` · `%Risk —` · `R/R —` · הבאנר — **חמישה סימפטומים
משער אחד**, `matchesCapital`. ⛔ אין באג נפרד בלוח הסיכונים.

⛔ **הופרך:** «תיקון ההון מחזיר את לוח הסיכונים לחיים בלי לגעת בו».
⛔ **הוא ⛔ אינו מחזיר.** המדידה ב-§2.3: `currency:'ILS'` נגזר ל-`contradicted`,
ו-`isAggregatable(contradicted) === false` ⇒ `matchesCapital` **false מול כל הון
שהוא** — גם ILS וגם USD.

⇒ **הפיכת `capitalCurrency` ל-USD לבדה משנה כתיבות עתידיות בלבד.** שתי העסקאות
הקיימות נושאות `currency: 'ILS'` **בשורת ה-DB**, וזו הראיה הסותרת עצמה. הן יישארו
`—` עד שהתווית עצמה תשתנה.

⚠️ **וזו התנהגות תקינה של השער, ⛔ לא באג בו.** ההערה ב-`:4870` בקוד קובעת
במפורש שאתר `Risk $` הוא «האתר התקין היחיד מה-10» **בזכות** השער הזה. ⛔ הסרתו
מפילה אותו לאותו חוב.

---

## 4. Q4 — «עובר טאב וחוזר»

⚠️ **הופרדו שתי תופעות. ⛔ הן ⛔ אינן אחת.**

### 4.1 תופעה (א) — equity זז כי המחירים התרעננו

✅ **צפוי. ⛔ לא באג.** `:2032-2063` ו-`:2090-2126` הם שני pollers עם
`visibilitychange` (`:2050`, `:2113`). שניהם מרעננים **מחירים**.
⛔ **נמדד: אף אחד מהם ⛔ אינו נוגע ב-`capital`.**

### 4.2 תופעה (ב) — הערך חוזר לערך קודם/ברירת-מחדל

🔴 **בדפדפן: ⛔ לא שוחזרה — חסום.** ⛔ אין `SENTINEL_QA_EMAIL`/`SENTINEL_QA_PASSWORD`
בסביבה, והפרומפט אוסר חשבון אמיתי. ⛔ **לא נוחש.**

**מה כן נמדד מהקוד — ארבע הבדיקות שהפרומפט ביקש:**

| הבדיקה | הממצא | פסיקה |
|---------|--------|--------|
| האם `visibilitychange` מפעיל מחדש הידרציה | אפקט ההידרציה `:1707-1818` תלוי ב-`[authUser?.id]` — **מחרוזת**. `TOKEN_REFRESHED` ב-`onAuthStateChange` (`:1239-1246`) מחליף אובייקט session אך `authUser?.id` **זהה** ⇒ ⛔ אין re-run | ⛔ **הופרך** |
| האם `hydratedRef` שומר | ✅ כן — `:1826` `if (!authUser?.id \|\| !hydratedRef.current) return;`. ⚠️ **אבל `hydratedRef.current` ⛔ לעולם אינו מאופס ל-`false`** בהחלפת משתמש (`:1812`). חוב סמוי, ⛔ לא המנגנון כאן | ⚠️ חלקי |
| האם `saveSettings` נדחה יורה אחרי `pagehide` ודורס | `flushSettings` (`:1853-1864`) חסום ב-`writable` Set. `saveSettings` מוסיף ל-`writable` — אבל רק כשהאפקט כבר עבר את שער `hydratedRef` | ⛔ **לא נמצא מסלול דריסה** |
| מרוץ קריאה/כתיבה | debounce 1000ms; הכתיבה נבנית מ-state שהידרציה כבר עדכנה | ⛔ **לא נמצא** |

### 4.3 🔴 המנגנון שכן נמדד — **היפוך תצוגה, ⛔ לא חזרת state**

`useFxRates` מתחיל ב-`status:"loading"`, `table:null`. המדידה:

```
convert(2500,'ILS','USD', table=null) = {"value":null,"reason":"no_rate_table"}
resolveEquityBase(fxTable=null)       = {"ok":false,"reason":"no_rate_table","value":2500,"currency":"ILS"}
fxOk=false  dispCcy=ILS  capitalShown=2500  equityBase=2500
header fmtBalance(curEquity, dispCcy) = ₪2,500.00
```

⇒ **בכל טעינה/החזרת-טאב שבה טבלת ה-FX ⛔ אינה מוכנה, הכותרת מדפיסה `₪2,500.00`,
ואז מתהפכת ל-`$876.89` כשהשער מגיע.**

⚠️ **וזה בדיוק «מתחלף חזרה» מנקודת מבט המשתמש** — הוא רואה `₪2,500` (בדיוק
`DEFAULT_CAPITAL`), ואז מספר אחר.

⛔ **אבל ⛔ אין כאן דריסה:** `capital` נשאר `2500`, `capitalCurrency` נשאר `ILS`,
ו-⛔ **שום דבר ⛔ אינו נכתב ל-DB.** זהו `fxOk === false` ב-`:2198-2229` — נסיגה
מכוונת שמעדיפה מספר לא-מומר על מספר מנוחש.

**פסיקה על (ב):** ⚠️ **מנגנון מועמד יחיד נמדד** ומסביר את התסמין במלואו,
⛔ **אך ⛔ לא הוכח שזה מה שניב ראה** — לכך נדרשת שחזור בדפדפן, ⛔ שהוא חסום.
🔴 **נותר לא-ידוע.** ⛔ ⛔ **אסור לסגור פריט על סמך §4.3 לבדו.**

---

## 5. Q5 — יישור עמודות בלוח הסיכונים

### 5.1 המדידה

`:4837` — **9 כותרות:** `["Ticker","Side","Entry","Stop","Shares","Risk $","Risk %","R/R","Bar"]`

`:4859-4877` — **9 תאים**, לפי הסדר:

| # | `<th>` | `<td>` | קוד |
|---|--------|--------|-----|
| 1 | Ticker | `{t.ticker}` | `:4859` |
| 2 | Side | badge `{t.side}` | `:4860` |
| 3 | **Entry** | `{fmtPaperPrice(t.entry, t)}` — **ללא תנאי** | `:4865` |
| 4 | **Stop** | `{t.hasStop ? fmtPaperPrice(t.stop,t) : "—"}` | `:4866` |
| 5 | Shares | `{t.shares}` | `:4867` |
| 6 | Risk $ | `{t.hasStop ? … : "—"}` | `:4872` |
| 7 | Risk % | `{t.hasStop ? … : "—"}` | `:4873` |
| 8 | R/R | `{t.rrRatio !== null ? … : "—"}` | `:4874` |
| 9 | Bar | `<div style={{width: barWidth%}}/>` | `:4877` |

### 5.2 ⛔ **הזזת עמודות — הופרכה**

**1:1, אותו סדר, ⛔ אין `colSpan`, ⛔ אין רינדור מותנה של תא שלם.** ⛔ **היסט ⛔ אינו
אפשרי מבנית.**

### 5.3 מה כן קורה — ומה נותר לא ידוע

`Entry` מרונדר **תמיד**. `Stop` מרונדר `—` בדיוק כש-`hasStop === false` — **וזה
תנאי Q3.** ⇒ במסך שצולם, השורה היא `NVDA · LONG · 225.16 · — · 1 · — · — · — · ▭`.

⚠️ **מה שדווח («225.16 תחת Stop, Entry ריק») ⛔ אינו ניתן לשחזור מהמקור.**

🔴 **נותר לא-ידוע.** השערה **שלא נאמתה**: `dir="rtl"` גלובלי (`:1974`, `:3979`)
ו-⛔ אין `dir="ltr"` על הטבלה; עם `text-start` (=ימין ב-RTL) התוכן נצמד לימין התא,
ו-`225.16` יושב פיזית **צמוד** ל-`—` שמשמאלו. ⛔ **זו השערה, ⛔ לא ממצא.**

**מה נדרש כדי להכריע:** מדידת `cellIndex` ב-DOM החי (⛔ לא צילום), או צילום עם
`Entry`/`Stop` שניהם מלאים. ⛔ **ו-RTL ⛔ אינו תירוץ** — הוא השערה שממתינה למדידה,
⛔ ולא הסבר שהתקבל.

---

## 6. Q6 — `5.20 → +$5.00` · `40.46 → +$40.00`

### 6.1 הנוסחה

`:4573-4585` — `livePnl = (currentPrice − tr.entry) × tr.shares` (LONG).
`:4617` — עמודת `Now` משתמשת ב**אותו** binding `currentPrice`.

🔴 **`:4621` — האתר:**

```jsx
{liveD ? (liveD.ok ? fmt$(Math.round(liveD.value), liveD.currency) : "—") : "..."}
```

`:5269` — אתר שני זהה (טבלת הג'ורנל).

### 6.2 המדידה — שלוש ההשערות

```
(230.36 − 225.16) × 1 = 5.2000  | Math.round → 5  | fmt$(5)  = +$5.00   | fmt$(5.2)   = +$5.20
(252.87 − 212.41) × 1 = 40.4600 | Math.round → 40 | fmt$(40) = +$40.00  | fmt$(40.46) = +$40.46
shares=2 → 10.40  ⇒ +$10.00 ;  80.92 ⇒ +$81.00
```

| השערה | פסיקה | ההוכחה |
|--------|--------|---------|
| `Math.round` / עיגול | ✅ **אוששה — הסיבה היחידה** | `fmt$(Math.round(5.2))` מחזיר בדיוק `+$5.00` |
| `shares ≠ 1` | ⛔ **הופרכה** | `shares=2` היה מדפיס `+$10.00` / `+$81.00`. ⇒ `shares === 1` בדיוק |
| מחיר שונה מזה שתחת `Now` | ⛔ **הופרכה מבנית** | `:4617` ו-`:4576` קוראים את **אותו** `currentPrice` |

⚠️ **הכשל שקט בדיוק כי `fmt$` מוסיף `.00`** — `+$5.00` נראה כמו סכום מדויק
לסנט, בעוד הסנטים כבר נמחקו. זהו כשל שקט לפי §2. ⚠️ ו-`title=` על אותו
`<span>` (`spotRefusalText(tr, livePnl)`) מקבל את הערך ה**לא** מעוגל ⇒ המסך
והטולטיפ חלוקים.

⛔ **`src/components/MobileTradeCard.jsx` ⛔ אינו מכיל את התבנית** — ⛔ אין בו
`Math.round` ו⛔ אין `livePnl`. שני האתרים חיים ב-`SwingEdge_App.jsx` בלבד.

---

## 7. Q7 — הצלבה מול גל `W-CAP`

⛔ **⛔ לא נסגר ו⛔ לא נפתח שום פריט מרשם.** זו הכרעת ניב.

| | |
|---|---|
| **מה משותף** | `capital === 2500 === DEFAULT_CAPITAL` — בדיוק העובדה ש-`B-268` נפתח עליה. `:1806-1810` מדליק `capitalMaybeClobbered` על התנאי הזה בדיוק |
| **מה ⛔ אינו משותף** | `capitalCurrency === "ILS"` — ⛔ **ציר שלישי ש-`W-CAP` ⛔ לא נגע בו.** `M-009` מדד `capital` בלבד; ⛔ התפלגות `capitalCurrency` ⛔ מעולם לא נמדדה |
| **`B-268` / `C-036`** | `STATE.md:22` — הפתוח שנותר הוא **אימות עין** + היעדר UPDATE/שחזור-אוטומטי. `test:hydration` (`D-064`) מכסה את שער ההידרציה, ⛔ **ולא את המטבע** |
| **פסיקה** | ⚠️ **אותה משפחה, ⛔ לא אותו שורש.** `W-CAP` שאל «מי דרס את המספר»; כאן השאלה היא «מה **המספר אומר**». תיקון `B-268` ⛔ אינו נוגע ב-`ILS` |

---

## 8. איחוד — ⛔ **רק אחרי ששישה נמדדו בנפרד**

```
                 capital = 2500          capitalCurrency = "ILS"
                      │                            │
        ┌─────────────┼──────────────┐             ├──────────────┬─────────────┐
        ▼             ▼              ▼             ▼              ▼             ▼
    :5880 ₪2,500  :4791 $831   :4417 $876.89   currency:'ILS'  capSym='₪'   fxOk=false
      (גולמי)      (ספוט)      (קיבוע-עבר)      על כל עסקה                  בזמן loading
                                                     │                            │
                                                     ▼                            ▼
                                            contradicted ⇒                  כותרת מדפיסה
                                            matchesCapital=false            ₪2,500.00 ואז
                                                     │                      מתהפכת ל-$876.89
                                    ┌────────────────┼────────────────┐        (Q4-ב מועמד)
                                    ▼                ▼                ▼
                              0.00% · $Risk —   %Risk — · R/R —   באנר «2 עסקאות»
```

**Q6 (`Math.round`) ⛔ אינו בעץ הזה.** הוא באג עצמאי, ⛔ בלתי-תלוי במטבע ובהון,
ויירה על כל משתמש עם P&L שאינו שלם. ⛔ **איחודו לסיפור ההון היה מסתיר אותו.**

**Q5 ⛔ אינו בעץ הזה.** הוא הופרך בקוד ונותר לא-ידוע כתצפית.

---

## 9. מה נשמר ומה ישתנה — כל מסלול תיקון אפשרי

⚠️ **⛔ אין כאן המלצה.** חמישה מסלולים בלתי-תלויים, כל אחד עם מה שנשמר, מה שזז,
ומסלול חזרה.

### מסלול A — `:5880` יכבד את `dispCcy`

| | |
|---|---|
| **מה משתנה** | מחשבון הפוזיציה יציג `$831` במקום `₪2,500` כש-`fxOk` |
| **✅ מה נשמר** | `capital` · `capitalCurrency` · DB · כל חישוב הסיכון (שעובד על `capital` הגולמי) |
| **🔴 הסיכון** | גודל הפוזיציה נגזר מ-`capital` הגולמי. הצגת מספר מומר לצד גודל שחושב מהגולמי היא **סתירה חדשה** אם ההמרה לא נכנסת גם לחישוב |
| **⛔ חסם קשה** | `CLAUDE.md` §7: ⛔ **אסור `\|\| 1` / `?? 1` במסלול גודל הפוזיציה.** `test:instrument` (252) חוסם |
| **חזרה** | revert קומיט. ⛔ אפס נזק מתמשך — תצוגה בלבד |

### מסלול B — `capitalCurrency` יתוקן ל-`USD` (הגדרות או מיגרציה)

| | |
|---|---|
| **מה משתנה** | `capSym`→`$` · `fxOk`→true תמיד · `:5880` יציג `$2,500` |
| **🔴 מה ⛔ אינו משתנה** | ⛔ **לוח הסיכונים נשאר `—`.** נמדד §2.3: עסקאות עם `currency:'ILS'` הן `contradicted` ו-`matchesCapital` **false גם מול USD** |
| **🔴 הסיכון החמור** | `capital = 2500` שנקרא כ-`$2,500` במקום `₪2,500` הוא **הון גדול פי ~3**. ⇒ כל `riskDollar`/`riskPct`/גודל פוזיציה **משתנה בשקט**. ⛔ **החלטת מוצר, ⛔ לא ריפקטור** |
| **חזרה** | אם דרך ההגדרות — הפיך בקליק. אם מיגרציה — ⛔ **בלתי הפיך בלי גיבוי `user_settings`** |

### מסלול C — התווית `currency` על עסקאות קיימות

| | |
|---|---|
| **מה משתנה** | `UPDATE trades SET currency='USD' WHERE …` ⇒ `assumed` ⇒ לוח הסיכונים חוזר לחיים |
| **⛔ החסם המוחלט** | `CLAUDE.md` §12: Claude Code כותב `.sql` **בלבד**. ⛔ ניב מריץ. **⛔ ⛔ ובלי §2.4(ג) שנמדד, ⛔ אין מכנה ⇒ ⛔ אין `WHERE`** |
| **🔴 הסיכון** | משתמש שבאמת סוחר בת"א יאבד את התווית **הנכונה** שלו. `currency_source: MANUAL_CAPITAL` הוא הראיה שהתווית הייתה **הנחה** — הוא ⛔ **אינו** מבדיל בין הנחה נכונה לשגויה |
| **חזרה** | ⛔ **רק** אם התווית הישנה נשמרת בעמודה/גיבוי **באותה טרנזקציה** |

### מסלול D — `Math.round` ב-`:4621` + `:5269` (Q6)

| | |
|---|---|
| **מה משתנה** | `+$5.00`→`+$5.20` · `+$40.00`→`+$40.46` |
| **✅ מה נשמר** | ⛔ **הכל.** ⛔ אפס DB · אפס state · אפס חישוב. שינוי פורמט טהור על שני `<span>` |
| **⚠️ מה עוד זז** | ⛔ **כלום — נמדד.** `MobileTradeCard.jsx` ⛔ אינו מכיל את התבנית. שאר 30+ אתרי `Math.round` הם גרפים/סטטיסטיקות שהעיגול בהם מכוון (`:2963` נושא הערה מפורשת «נשמר בדיוק כפי שהיה») |
| **🔴 הסיכון** | ⛔ **הנמוך ביותר מהחמישה.** ⚠️ עדיין ⛔ אינו T1: המשתמש רואה מספר כסף חדש |
| **חזרה** | revert. ⛔ אפס עקבות |

### מסלול E — `fxOk === false` יאמר «טוען» במקום להדפיס `₪2,500` (Q4-ב)

| | |
|---|---|
| **מה משתנה** | בזמן `status:"loading"` הכותרת תציג מצב-טעינה במקום מספר-שאינו-מומר |
| **✅ מה נשמר** | `capital` · DB · הנסיגה עצמה (⛔ **אסור להחליפה בשער מנוחש** — זה בדיוק `R-2`) |
| **🔴 הסיכון** | `loading` ו-`unavailable` הם ⛔ **⛔ לא אותו מצב**. ב-`unavailable` המספר הלא-מומר הוא **הטוב ביותר שקיים** ו⛔ אסור להסתירו. ⇒ חייב להבחין ביניהם |
| **⛔ חסם** | ⛔ **⛔ לא לפני ששחזור (ב) בדפדפן מוכיח שזה מה שניב ראה.** §4.3 הוא מועמד, ⛔ לא ממצא |
| **חזרה** | revert. תצוגה בלבד |

---

## 10. ממצאים סביבתיים (§11) — ⛔ **⛔ לא תוקנו, ⛔ לא נסגרו**

| # | ממצא | מדידה | למה ⛔ לא תוקן כאן |
|---|-------|--------|---------------------|
| E1 | `:4618` — `fmtPaperPrice(tr.stop, tr)` **ללא שומר** | `Number(null) === 0` וסופי ⇒ stop ריק מרונדר **`$0`**. `undefined` מרונדר `—`. ⇒ מספר מומצא (`R-2`) | ⛔ אינו קשור למשימה (§11 דורש שלושתם) |
| E2 | `hydratedRef.current` ⛔ לעולם ⛔ אינו מאופס ל-`false` (`:1812`) | החלפת משתמש באותו טאב ⇒ הכתיבה של המשתמש **הישן** עוברת את השער | ⛔ לא ודאי ב-100% שהוא נגיש (`authUser?.id` מפעיל את האפקט) |
| E3 | `:1758-1763` — back-compat נופל ל-`accountCurrency` | הופך «אני קורא בשקלים» ל-«ההון שלי בשקלים». מועמד **המקור** ל-Q2 | ⛔ תיקון = מסלול B ⇒ החלטת מוצר |
| E4 | `title=` ב-`:4621` מקבל את `livePnl` הלא-מעוגל | המסך אומר `+$5.00`, הטולטיפ `5.2` | חלק ממסלול D |

⚠️ **⛔ אף אחד מהארבעה ⛔ לא נרשם ל-`STATE`/`BACKLOG` בקומיט הזה** — הפרומפט אוסר
פתיחת פריטי מרשם. ⚠️ **§10.1 ⛔ אינו מבוטל בכך**: הם חייבים לקבל שורה בגל
**הראשון** שאינו read-only, ⛔ אחרת הם קיימים בזיכרון של ניב בלבד (`R-5`).

---

## 11. מה נותר לא ידוע

| # | פתוח | מה חוסם | מה יסגור |
|---|------|----------|-----------|
| U1 | כמה משתמשים נושאים `capitalCurrency` שאינו תואם | ⛔ אין service-role key; הפרומפט אוסר DB | שלוש שאילתות §2.4, **עם המכנה שלהן** |
| U2 | האם (ב) הוא §4.3 או משהו אחר | ⛔ אין `SENTINEL_QA_*` | שחזור בדפדפן: טעינה · הסתרת טאב · חזרה · מדידת `capital` לפני/אחרי |
| U3 | מה באמת רונדר תחת `Stop`/`Entry` בצילום | ⛔ אין DOM חי | `cellIndex` ב-DOM, ⛔ לא צילום |
| U4 | האם `capital=2500` כאן הוא דריסת `B-268` או ערך אמיתי | ⛔ אין DB | `updated_at` בשורת המשתמש מול חלון הדריסה של `M-009` |

---

## 12. REPORT

```
hash                : 66f261e  (עץ נקי לפני ואחרי)
רמה                 : T3 · תשובות: לא/כן/לא/כן/כן
קבצי src שנגעו בהם  : 0
DB                  : 0 שאילתות
migration           : 0

מקורות הון          : 9 (S1–S9) · 5 אתרי תצוגה
  ₪2,500  :5880   capital גולמי + capSym          ⛔ אפס המרה
  $831    :4791   Math.round(displayCapital)      ספוט
  $876.89 :4417   fmtBalance(curEquity, dispCcy)  קיבוע-עבר + P&L
  ⇒ ⛔ הם ⛔ אינם אותו מקור. 876.89 − 45.66 = 831.23

תופעה (ב)           : 🔴 ⛔ לא שוחזרה — חסום (⛔ אין SENTINEL_QA_*)
                      מועמד יחיד נמדד: fxOk=false בזמן loading ⇒ ₪2,500.00
                      ⇒ היפוך תצוגה · ⛔ אפס דריסת state · ⛔ אפס כתיבת DB

מה הופרך            : 1. «$831 ו-$876.89 = אותו מספר שנסחף»          ⛔ הופרך
                      2. «תיקון ההון מחזיר את לוח הסיכונים»          ⛔ הופרך
                         (currency:'ILS' ⇒ contradicted ⇒ false מול USD וגם ILS)
                      3. «הזזת עמודות» (Q5)                          ⛔ הופרך (9 th / 9 td, 1:1)
                      4. «shares ≠ 1» (Q6)                            ⛔ הופרך (2 ⇒ +$10.00)
                      5. «מחיר שונה מ-Now» (Q6)                       ⛔ הופרך (אותו binding)
                      6. «visibilitychange מפעיל הידרציה»             ⛔ הופרך (deps=[id])

מה אושש             : Q3 — 5 סימפטומים משער אחד (matchesCapital)
                      Q6 — Math.round(:4621, :5269) הסיבה היחידה. shares=1 בדיוק

משתמשים מושפעים     : 🔴 חסום. ⛔ אין service-role key · הפרומפט אוסר DB
                      METRICS ⛔ אינו יכול לענות: M-009 מדד capital בלבד;
                      התפלגות capitalCurrency ⛔ מעולם לא נמדדה
                      SQL מוכן ב-§2.4 (3 שאילתות, 3 מכנים) — ⛔ לא הורץ

פריטי מרשם          : 0 נפתחו · 0 נסגרו
עצרתי               : ✅
```

---

## 13. Q8 — טקסט לבן על רקע לבן במצב בהיר · אבחון read-only

> ⚠️ **סטייה מהפרומפט, מוצהרת:** הפרומפט ביקש «סעיף §10». **§10 · §11 · §12 כבר
> תפוסים** בקובץ הזה (ממצאים סביבתיים · מה נותר לא ידוע · REPORT). מספור כפול הוא
> בדיוק הסחיפה השקטה ש-`test:registry` נבנה לחסום ⇒ הסעיף נכתב כ-**§13**
> ו⛔ שום סעיף קיים ⛔ לא נדרס. הממצאים חיים באותו קובץ, כפי שנדרש.

**רמה: T2 · תשובות: לא/לא/לא/לא/כן** — שאלה 5 בלבד («הנחה שלא נמדדה»).
⛔ **0 קבצי קוד שונו · 0 DB · 0 פריטי מרשם נפתחו או נסגרו.**
`HEAD` בזמן המדידה: `b21257a`.

### 13.0 תקציר — 🔴 **ההנחה שבפרומפט הופרכה, והתסמין אושש בשורש אחר**

| טענה בפרומפט | פסיקה | המדידה |
|---|---|---|
| `:6628` נושא `text-white` קשיח ⛔ ללא `dark:` | ✅ **אושש כעובדה מילולית** | `<h2 … tracking-tight text-white>` — ⛔ אין `dark:` בשורה |
| ⇒ ולכן הכותרת ⛔ אינה נראית | 🔴 **הופרך** | `.text-white` **ממופה מחדש** ל-`var(--text-primary)` = `#1B2419` ⇒ **15.24:1** על `--bg` |
| «108 מופעי `text-white` מול 1 `dark:`» = 107 באגים | 🔴 **הופרך כמסקנה** | המכנה נכון, ההסקה לא. `dark:` לכל קומפוננטה ⛔ **אינו** החוזה כאן — ראה §13.1 |
| `:6639` «עושה זאת נכון» | ✅ **אושש** | `dark:bg-[var(--v3-bg-panel)]` הוא **התבנית הנכונה** לטוקן חד-ערכתי (§13.3) |
| ⛔ אינו רגרסיה של `B-185` | ✅ **אושש** | הזרע/הרינדור של `B-185` ⛔ אינם נוגעים בצבע; `--v3-*` קיים מלפני כן |

🔴 **התסמין «לבן על לבן» הוא אמיתי — אבל הוא ⛔ אינו `text-white`.**
הוא **`var(--v3-text-hi)` = `#FFFFFF`**, טוקן שמוגדר **פעם אחת** ב-`:root`
ו⛔ **לעולם אינו מוגדר מחדש** תחת `.dark` ⇒ הוא לבן **בשתי הערכות**.
**11/11 = 100%** משימושיו חשופים, **0/11 = 0%** מוגנים ב-`dark:`.

### 13.1 החוזה בפועל — למה היעדר `dark:` ⛔ אינו באג בפני עצמו

`src/design/tokens.css:155-158` מצהיר במפורש:
> «…the global restyle layer in index.css maps Tailwind/hex classes to these
> variables, so dark mode flows through the entire app **without per-component
> dark: variants**»

`src/index.css:96-99` מממש: `.text-white, .text-slate-50, .text-slate-100,
.text-gray-50, .text-gray-100 { color: var(--text-primary); }`

**המדידה המכריעה — על הבייטים הבנויים, ⛔ לא על המקור** (`DECISIONS` 27.08):
`dist/assets/index-DhOqc1uQ.css` (בנייה 05.09 15:03):

| כלל | offset בבתים | ערך |
|---|---|---|
| Tailwind `.text-white` | **53,396** | `rgb(255 255 255 / …)` |
| override `.text-white,…` | **66,261** | `var(--text-primary)` |

אותה ספציפיות (`0,1,0`) · ה-override **מאוחר ב-12,865 בתים** ⇒ **ה-override מנצח.**
⇒ `text-white` במצב בהיר מרונדר `#1B2419`. **קריא.**

✅ **הבנייה אומתה מול `HEAD`, ⛔ לא הונחה:** `npm run verify` בסוף הגל הפיק
`dist/assets/index-DhOqc1uQ.css` — **אותו שם-hash בדיוק** שעליו נמדדו שני
ה-offsets. ⇒ הבייטים שנמדדו הם הבייטים ש-`b21257a` מייצר. **⛔ עדיין ⛔ אינו
אימות עין** (§13.8-1) — הוא מוכיח **מה נשלח**, ⛔ לא מה שהמשתמש רואה.

### 13.2 ① הספירה והסיווג — עם מכנה ושם אוכלוסייה

**אוכלוסייה א׳ — כל מופע של המחרוזת `text-white` ב-`SwingEdge_App.jsx` (מכנה = 108):**

| צורה | מונה/מכנה | פסיקה |
|---|---|---|
| `text-white` חשוף | **89/108 = 82.4%** | ✅ ממופה ל-`--text-primary` ⇒ **15.24:1** — תקין |
| `hover:text-white` | **16/108 = 14.8%** | ✅ ממופה (אותו סלקטור) — תקין |
| `group-active:text-white` | **2/108 = 1.9%** | ✅ ממופה — תקין |
| `dark:text-white` | **1/108 = 0.9%** | ✅ תקין |

⇒ המספרים **108** ו-**1** שבפרומפט **אוששו בדיוק** במדידה חוזרת.
🔴 **אבל 0/108 = 0% מהם הם באג ניגודיות.** המכנה נמדד נכון; ההסקה ממנו שגויה.

**89 החשופים לפי הקשר-רקע** (מכנה = 89 שורות נושאות):

| קטגוריה | מונה/מכנה | פסיקה |
|---|---|---|
| על שורה עם `dark:` או `var(--` | 22/89 = 24.7% | תקין |
| עם רקע כהה מפורש באותה שורה | 4/89 = 4.5% | ⚠️ **ניגודיות הפוכה** — טקסט כהה על כפתור צבעוני כהה |
| ⛔ ללא רקע וללא טוקן | 63/89 = 70.8% | תקין (יורש רקע בהיר + טקסט כהה) |

**אוכלוסייה ב׳ — מחלקות `text-slate-*` (מכנה = כל מופע ב-`SwingEdge_App.jsx`):**

| מחלקה | מופעים באפליקציה \| ב-`src/` | ממופה ל- | ניגודיות על `--bg` | AA |
|---|---|---|---|---|
| `text-slate-100` | 1 \| — | `--text-primary` `#1B2419` | **15.24:1** | ✅ |
| `text-slate-200`/`300` | 6 \| 32 | `--text-secondary` `#4A5848` | **7.20:1** | ✅ |
| `text-slate-400` | 34 \| 96 | `--text-tertiary` `#6B7868` | **4.43:1** | ⚠️ גדול-בלבד |
| `text-slate-500` | **93** \| 126 | `--text-muted` `#9CA89A` | **2.36:1** | 🔴 **נכשל** |
| `text-slate-600` | 85 \| 73 | ⛔ **לא ממופה בבהיר** (`M-01` הוא `.dark`-scoped) | `#475569` ⇒ ~8:1 | ✅ |

**אוכלוסייה ג׳ — `bg-white` (מכנה = 98 מופעים ב-`SwingEdge_App.jsx`):**
`bg-white/5` ⛔ **אינו ממופה** — נשאר `#ffffff0d` בבנייה. במצב בהיר, שדה קלט
מאבד את הגבול הוויזואלי שלו. ⚠️ **הטקסט בתוכו קריא** (`--text-primary`) ⇒
זהו ממצא **אפורדנס**, ⛔ **לא ממצא נראוּת**. ⛔ **לא תוקן.**

`text-black`: **1/1** מופע, על שורה מודעת-ערכות. תקין.

### 13.3 🔴 השורש האמיתי — משפחת `--v3-*` היא **חד-ערכתית**

`grep '\-\-v3-' src/design/tokens.css` ⇒ **21 טוקנים, כולם ב-`:root` בלבד.**
מספר ההגדרות-מחדש תחת `.dark`: **0/21 = 0%.**
⇒ כולם נושאים ערכים **שתוכננו לרקע כהה** — גם כשהמסך בהיר.

| טוקן | ערך | על `--surface` `#FFFFFF` | AA | חשוף \| מוגן `dark:` |
|---|---|---|---|---|
| `--v3-text-hi` | `#FFFFFF` | **1.00:1** | 🔴 **בלתי נראה** | **11 \| 0** |
| `--v3-text-mid` | `#C7D0CC` | **1.58:1** | 🔴 נכשל | **45 \| 0** |
| `--v3-text-lo` | `#7A8783` | 3.74:1 | ⚠️ גדול-בלבד | 133 \| 0 |
| `--v3-accent` | `#00C076` | 2.39:1 | 🔴 נכשל | 157 \| 4 |
| `--v3-loss` | `#F43F5E` | 3.67:1 | ⚠️ גדול-בלבד | 120 \| 0 |
| `--v3-info` | `#06b6d4` | 2.43:1 | 🔴 נכשל | 76 \| 0 |
| `--v3-purple` | `#A78BFA` | 2.72:1 | 🔴 נכשל | 30 \| 0 |
| `--v3-warn` | `#F59E0B` | 2.15:1 | 🔴 נכשל | 46 \| 0 |
| `--v3-line` | `rgba(255,255,255,.08)` | גבול נעלם | 🔴 | 27 \| 19 |
| `--v3-bg-panel` | `#0A100D` | פאנל שחור בבהיר | 🔴 | 9 \| **61** |

⚠️ **`--v3-bg-panel` הוא ההוכחה שהתבנית הנכונה קיימת ומיושמת** — **61/70 = 87%**
משימושיו **כן** מוגנים ב-`dark:`, וזה בדיוק `:6639` שהפרומפט ציין. ⇒ הפער
⛔ **אינו חוסר ידע**, הוא **כיסוי חלקי**: הרקעים הומרו, הטקסטים ⛔ לא.

### 13.4 ③ אילו אזורים ⛔ אינם קריאים במצב בהיר

🔴 **נמדד בקוד בלבד. ⛔ אין כאן אימות עין — ראה §13.7.**

**דרגה 1 — `--v3-text-hi` `#FFFFFF` על רקע לבן, `1.00:1` (11/11 חשופים):**

| אזור | צמתים | מה בדיוק |
|---|---|---|
| טאב **פנקס** (`tab === "notebook"`) | **4** | `NotebookTab.jsx:90` כותרת `h2` · `:101` + `:165` **טקסט שהמשתמש מקליד** בשני ה-`textarea` · `:184` גוף הרשומה |
| טאב **סקירה שבועית** (`tab === "weeklyReview"`) | **6** | `WeeklyReviewTab.jsx:138` כותרת · `:29` **ברירת המחדל של `tone` בכל `Stat`** · `:179`/`:190` שם הסטאפ · `:206`/`:223` שתי הודעות המנוע |
| **כרטיס עסקה במובייל** | **1** | `MobileTradeCard.jsx:81` — **הטיקר** |

⚠️ **`WeeklyReviewTab.jsx:29` הוא המסוכן ביותר** — `tone = "text-[var(--v3-text-hi)]"`
היא **ברירת מחדל של פרמטר** ⇒ כל `<Stat>` שלא קיבל `tone` מפורש נעלם, והמנייה
`6` ⛔ **אינה** מספר הצמתים על המסך.

**דרגה 2 — `--v3-text-mid` `1.58:1` (39 חשופים ב-`SwingEdge_App.jsx`):**
`SETTINGS` 21 · `ADMIN`+מודלים 9 · `MENTORING` 5 · ניווט/סרט 2 · `ANALYTICS` 2.

**דרגה 3 — `text-slate-500` → `--text-muted` `2.36:1` (93 באפליקציה):**
🔴 **זה כולל את `:6626` — התווית `«מודיעין שוק»` עצמה**, בדיוק מעל ה-`h2` שהפרומפט
הצביע עליו:
```
:6626  <span className="… text-slate-500">«מודיעין שוק»</span>   ⇒ 2.36:1  🔴
:6628  <h2 className="… text-white">«מה השוק עושה עכשיו»</h2>    ⇒ 15.24:1 ✅
:6631  <p className="text-xs text-slate-500">…</p>               ⇒ 2.36:1  🔴
```
⇒ **המועמד המדוד לתסמין שנצפה הוא `:6626`/`:6631`, ⛔ לא `:6628`.**
⚠️ **מועמד, ⛔ לא ממצא** — הצילום ⛔ לא נבדק על ידי, והמילה «כותרת» ⛔ אינה חד-משמעית.

**⛔ לא ידוע:** כמה מ-`--v3-text-lo` (133) ו-`--v3-loss` (120) נופלים בפועל —
הסף תלוי-viewport (`M-008`: `5/5` נכשלים ב-`390×844` מול `2/5` ב-`1440×900`).
⛔ **לא נמדד כאן.**

### 13.5 ④ הצלבה מול `B-245` · `B-254` — ⛔ **אפס פריטים נפתחו או נוגעו**

| פריט | פסיקה | הנימוק המדוד |
|---|---|---|
| **`B-245`** (ניגודיות AA בבהיר) | ✅ **אושש — אותה מחלקה** | `M-008`: `27/37 = 73%` צמתים מתחת ל-`4.5:1` **בבהיר**, ובפירוק המנגנוני שלו **`18/27 = 67%` הם «טוקן-מתחת-ל-AA»** — בדיוק המנגנון של §13.3/§13.4 |
| **`B-245`** — שורש | 🔴 **⛔ אותה שורה** | יעד `B-245` = `tokens.css:29` (`--text-muted`). «לבן על לבן» = `tokens.css:124` (`--v3-text-hi`). ⚠️ **`AUDIT-Q1-…-2026-08-27` §91 כבר רשם** ש-`--v3-accent`/`--v3-loss` הם `:122`/`:128` «⛔ **לא `:29`**, שהוא היעד של `B-245`» |
| **`B-247`** (שתי מערכות עיצוב במקביל) | ✅ **אושש — ההתאמה ההדוקה ביותר** | `B-247` מתאר בדיוק «v3 parallel layer · ⛔ בלי רשימת מסכים, תאריך, או תנאי עצירה». `0/21` הגדרות-מחדש ל-`.dark` הן **התוצאה הצפויה** שהוא חוזה |
| **`B-254`** (`Inter` inline שובר RTL) | 🔴 **הופרך — מחלקה אחרת** | `B-254` הוא **גופן ו-RTL** (`--font-hebrew`), ⛔ אפס רכיב צבע או ניגודיות. הצומת שלו (`:3959`/`:3979`) ⛔ אינו מופיע באף אחת מאוכלוסיות §13.2–§13.4 |

⛔ **⛔ לא איחדתי · ⛔ לא פתחתי · ⛔ לא סגרתי.** הכרעת ניב.
⚠️ **ההערה המבצעית:** אם `B-245` מתוקן ב-`tokens.css:29` בלבד, **`11` צמתי
ה-`--v3-text-hi` נשארים `1.00:1`** — התיקון ייראה כמו הצלחה ו⛔ לא יגע בלבן-על-לבן.

### 13.6 ⑤ ווידג'ט TradingView ריק — 🔴 **הקשר לערכת הנושא מופרך**

| ממצא | מדידה |
|---|---|
| `theme: "dark"` **קשיח** ב-`:1906` | ⛔ אינו קורא `themeResolved`. גם `toolbar_bg: "#0d1424"` קשיח ב-`:1909` |
| deps של האפקט | `[tab, chartSymbol, chartInterval, chartStyle]` — ⛔ **אפס תלות בערכת הנושא** |
| ⇒ הקשר ל-`O∅ H∅ L∅ C∅` | 🔴 **מופרך.** `theme` הוא אפשרות **רינדור**; הנרות מגיעים משרתי TradingView דרך סקריפט ההטמעה. משתנה שהאפקט ⛔ אינו תלוי בו ⛔ אינו יכול לרוקן נתונים |
| ⚠️ מה **כן** אושש | הגרף **נשאר כהה במצב בהיר** — פגם צימוד-ערכות אמיתי ונפרד, `SwingEdge_App.jsx:1906` |
| ⚠️ הקשר ההיסטורי | ההערה ב-`:1876-1878` מתעדת שה-widget הישן (`tv.js`) נתן **בדיוק** `OHLC ∅`, וההטמעה הזו הייתה **התיקון** ⇒ אם התסמין חזר, זו **נסיגה או כשל צד-שלישי**, ⛔ לא ערכת נושא |

⇒ **ממצא נפרד, ⛔ לא נמדד מעבר למסלול הקוד, ⛔ לא תוקן, ⛔ לא נפתח כפריט.**

### 13.7 ⑥ 🔴 הצלבה מול מסלולי `§9` — **התנגשות אחת מדודה**

| מסלול | צומת | נוגע ב-Q8? |
|---|---|---|
| **A** — `:5880` יכבד `dispCcy` | `:5880` = `text-slate-700` (⛔ לא ממופה בבהיר ⇒ ~8:1). השכן `:5878` = `text-white` (ממופה) | ⛔ **אין התנגשות** |
| **B** — `capitalCurrency`→`USD` | הגדרות/מיגרציה | ⛔ **אין** — אפס CSS |
| **C** — תווית `currency` | `.sql` בלבד | ⛔ **אין** |
| **D** — `Math.round` ב-`:4621` + `:5269` | 🔴 **שתי השורות נושאות `text-[var(--v3-accent)]` ו-`text-[var(--v3-loss)]` חשופים** — `2.39:1` ו-`3.67:1` בבהיר | 🔴 **התנגשות — אותה שורה בדיוק** |
| **E** — `fxOk===false` יאמר «טוען» | אזור `:5820` = `text-white` (ממופה) | ⛔ **אין התנגשות** |

🔴 **ההתנגשות D↔Q8 היא על אותה שורה בשני צירים שונים:** `D` משנה את **התוכן**
(`Math.round` ⇒ `+$5.20`), `Q8` ישנה את ה-**`className`**. ⛔ **אינה חוסמת** —
אבל שני גלים שנוגעים ב-`:4621`/`:5269` בנפרד יסחפו זה את מספרי השורות של זה,
וזה בדיוק `B-280`. ⇒ **מי שמבצע שני, מאמת את השורה מחדש לפי תוכן ו⛔ לא לפי מספר.**

### 13.8 מה נותר ⛔ לא ידוע

1. 🔴 **⛔ אין אימות עין.** כל §13 הוא קוד + CSS בנוי. `DECISIONS` 27.08 קובע
   שקריאת CSS סטטי ⛔ אינה קובעת מה מרונדר ⇒ **כל «⛔ אינו קריא» כאן הוא
   מועמד מדוד, ⛔ לא ממצא חי.**
2. ⛔ **⛔ לא נמדד איזה מהשניים — `:6626` או `:6628` — הוא «הכותרת» שבצילום.**
3. ⛔ **② ⛔ אין נתון על כמה משתמשים נפתחים בבהיר** — ראה §13.9.
4. `--v3-text-lo` (133) ו-`--v3-loss` (120): הסף תלוי-viewport, ⛔ לא נמדד.
5. `O∅ H∅ L∅ C∅` — הסיבה בפועל ⛔ לא נמדדה (צד שלישי).

### 13.9 ② ברירת המחדל של `useTheme()` — ⛔ **ואין נתון על ההתפלגות**

`src/contexts/ThemeContext.jsx`:

| שורה | ממצא |
|---|---|
| `:25` | ברירת המחדל היא **`'auto'`** |
| `:31` | `auto` נגזר מ-`window.matchMedia('(prefers-color-scheme: dark)')` ⇒ **מערכת ההפעלה** |
| `:33` | `matchMedia` זורק ⇒ **`'light'`** |
| `:86` | ⛔ אין `Provider` ⇒ `resolved: **'light'**` |

🔴 **`light` הוא הנפילה בכל שלושת מסלולי הכשל.** מי שלא נגע במתג ומערכת ההפעלה
שלו בהירה — נפתח בבהיר.

🔴 **כמה משתמשים בפועל: ⛔ אין נתון, וזו מדידה שאי-אפשר לבצע.**
`ThemeContext.jsx:67` כותב ל-`localStorage` **בלבד**. המדידה:
`grep -rn "swingedge-theme-mode" src/ api/` ⇒ **1/1 = 100%** מהאתרים הוא
`ThemeContext.jsx` עצמו. **0 כתיבות ל-`user_settings` · 0 קריאות `track*`.**
⇒ ⛔ **הערכה ⛔ אינה אפשרית משום מקור קיים** — ⛔ לא DB, ⛔ לא אנליטיקס.
⚠️ **⛔ לא רשמתי זאת כמדידה חסרה ב-`METRICS` — פתיחת פריט היא הכרעת ניב.**

### 13.10 REPORT · Q8

```
hash                : b21257a (עץ נקי, pull=up-to-date)
רמה                 : T2 · לא/לא/לא/לא/כן
grep מדידה חוזרת    : text-white  = 108  ✅ אושש בדיוק
                      dark:text-white = 1 ✅ אושש בדיוק
                      פירוק: 89 חשוף + 16 hover: + 2 group-active: + 1 dark: = 108

ההנחה בפרומפט       : 🔴 הופרכה — .text-white ממופה ל-var(--text-primary)
                      בנוי: Tailwind @53,396  |  override @66,261  ⇒ override מנצח
                      :6628 = 15.24:1  ⇒ קריא לחלוטין

השורש האמיתי        : --v3-text-hi = #FFFFFF · 0/21 טוקני v3 מוגדרים תחת .dark
                      1.00:1 על לבן · 11/11 = 100% חשופים · 0/11 מוגנים

אזורים לא קריאים    : פנקס (4) · סקירה שבועית (6, כולל ברירת-מחדל של tone)
                      כרטיס מובייל (1) · v3-text-mid 39 · text-slate-500 93
                      ⚠️ המועמד לתסמין הוא :6626 (2.36:1), ⛔ לא :6628

useTheme ברירת מחדל : 'auto' ⇒ OS. light בכל 3 מסלולי הכשל (:33, :86)
                      התפלגות משתמשים: ⛔ אין נתון — localStorage בלבד, 0 DB, 0 track

B-245               : ✅ אושש (אותה מחלקה, M-008 18/27 טוקן-מתחת-ל-AA)
                      🔴 שורש שונה: :29 מול :124
B-247               : ✅ אושש — ההתאמה ההדוקה ביותר
B-254               : 🔴 הופרך — גופן/RTL, אפס רכיב צבע

TradingView         : 🔴 קשר לערכה מופרך (deps ⛔ אינם כוללים theme)
                      ✅ אושש נפרד: theme:"dark" קשיח :1906 ⇒ גרף כהה בבהיר

התנגשות עם A–E     : 🔴 מסלול D — :4621 + :5269 נושאות v3-accent/v3-loss חשופים
                      אותה שורה, className מול תוכן. ⛔ אינה חוסמת · B-280 רלוונטי
                      A · B · C · E — ⛔ אין התנגשות

פריטי מרשם          : 0 נפתחו · 0 נסגרו · 0 נגעתי
קבצי קוד            : 0 שונו
עצרתי               : ✅
```
