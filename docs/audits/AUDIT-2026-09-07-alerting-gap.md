# AUDIT 2026-09-07 — פער ההתראה: המוצר היה שבור 18 שעות ו⛔ אף מוניטור לא ידע

> **§8.1 שלב 1 — אבחון read-only.** ⛔ **0 קבצי קוד · 0 שינויי workflow · 0 חוקי Sentry ·
> 0 webhooks · 0 משתני סביבה · 0 התראות חדשות · 0 ספים שהוזזו.**
> **רמה: `T3` · תשובות: 1 הפיכות / 1 אמון / 1 אבטחה-וגישה / 0 רוחב-קוד / 1 ודאות.**
> (שאלות 1·3 הן טריגר-יחיד ⇒ `T3` בכל מקרה. «רוחב» = 0 כי ⛔ לא נגעתי בקוד כלל.)
>
> 🔴 **מה שנאסר במפורש והתקיים:** ⛔ לא נוצרה התראה · ⛔ לא נסגר issue ב-Sentry ·
> ⛔ לא `--no-verify` · ⛔ לא force-push.
>
> **HEAD בזמן האבחון: `dcf508d` · עץ נקי.**

---

## 0. הכלל שקבע את כל האבחון

הפרומפט הזהיר: ⛔ **אסור להסיק «Sentry עבד».** נמדד, ⛔ לא הונח — והמדידה **מאששת את
האזהרה ומחמירה אותה**.

`SWING-EDGE-A` ⛔ **אינו issue חדש.** אותה חתימה בדיוק —
`TypeError: Cannot read properties of null (reading 'toFixed')`, `transaction = /app` —
נפתחה כ-**New issue** ב-**2026-07-24T06:12:31Z**, release `1838b42d…`, ואז **נסגרה**.
המייל של 07.09 אומר במפורש: *"Sentry marked SWING-EDGE-A as a regression in 51a12d24bbcd"*.

⇒ **הערוץ היחיד שהתריע הוא ערוץ ה-regression.** ⛔ **אין בידי מדידה שערוץ «issue חדש»
היה מתריע** — הוא היה צריך לירות ב-24.07 וזו ⛔ אינה ראיה למה שקורה **היום**, כי ⛔ לא
נמדדו חוקי ההתראה עצמם (§A2, חסום). **תקלה חדשה = שאלה פתוחה.** ⛔ לא נסגרה כאן.

⚠️ **והמחלקה בת 44 יום.** `firstSeen` → הקומיט ששיגר = **44 יום 12:55:58**. באג שנסגר
פעם אחת וחזר, ⛔ ולא באג שנולד אתמול.

---

## 1. `A1` — ציר הזמן, UTC

> ⚠️ **מלכודת מדידה שנתפסה תוך כדי, ופעמיים:** `git log --date=format:'…UTC'` מדפיס את
> **אזור-הזמן של הקומיט** (כאן `+03:00`) ומדביק עליו את התווית `UTC` שכתבתי ביד ⇒ כל
> חותמת יוצאת **גדולה ב-3 שעות** ונראית תקינה. הצורה הנכונה היא `--date=format-local:`
> עם `TZ=UTC`, ואומתה מול `%cI` הגולמי (`2026-09-06T22:08:29+03:00` ⇒ `19:08:29Z`).
> ⛔ **תווית ⛔ אינה המרה.** זהו `R-3` בצורתו הטהורה — סימן שנכתב ביד על מדידה.

| # | שעה (UTC) | אירוע | מקור המדידה |
|---|-----------|-------|--------------|
| 0 | 2026-07-24 06:12:31 | **Sentry — New issue** `SWING-EDGE-A` (7630370819). חתימה זהה. `handled=yes` · `mechanism=generic` · `transaction=/app` · release `1838b42d…` · `alert_rule_id=17075516` | Gmail thread `19f92c10d6a92172` |
| — | ⛔ **לא נמדד** | מתי ומי **סגר** את ה-issue | ⛔ חסום (§A2) |
| 1 | **2026-09-06 19:08:29** | **`70816e1`** — גל `G2` נדחף. השער הוסר; הבאג בפנים | `git show -s --format=%cd --date=format-local` |
| 2 | 2026-09-06 19:23:08 | `51a12d2` — הרילס ש-Sentry ייחס לו את הרגרסיה | git + גוף המייל |
| 3 | 2026-09-06 19:33:56 | סנטינל `1/6` (`34055288411`) — `success`. Sentry-layer: **«אין issues חדשים (35 דק')»** | `gh run view --log` |
| 4 | 2026-09-06 21:45:49 | סנטינל `2/6` (`34062038514`) — `success` · **שכבה מאומתת רצה** (`MIN=45`) · ירוקה | log |
| 5 | 2026-09-06 23:13:40 | סנטינל `3/6` (`34066280791`) — `success` · ⛔ ללא שכבה מאומתת (`MIN=13`) | log |
| 6 | 2026-09-07 01:03:47 | סנטינל `4/6` (`34071773367`) — `success` · **מאומתת** (`MIN=3`) · ירוקה | log |
| 7 | **2026-09-07 06:09:04** | סנטינל `5/6` (`34089557392`) — `success` · **המאומתת האחרונה** · ירוקה | log |
| 8 | 2026-09-07 12:41:23 | סנטינל `6/6` (`34123229599`) — `success` · ⛔ ללא שכבה מאומתת (`MIN=41`) | log |
| 9 | **2026-09-07 13:43:09** | 🔴 **מייל Sentry — «Regression»**. ההתראה **היחידה** שנמדדה בכל החלון | Gmail |
| 10 | ⛔ **לא נמדד** | הרגע שבו אדם ידע. ⛔ **לא הונח** — ⛔ אין חותמת לדיווח המשתמש בידי | — |
| 11 | 2026-09-07 13:54:14 | `watchdog.yml` רץ — `success`. ⚠️ **11 דקות אחרי ההתראה** ⇒ ⛔ לא היה יכול להקדים אותה | `gh run list` |
| 12 | 2026-09-07 14:02:02 | `1b8e44b` — hotfix, `2/6` הצרכנים | git |
| 13 | 2026-09-07 14:10:22 | `1ebb559` — `6/6` הצרכנים | git |
| 14 | 2026-09-07 14:19:36 | `dcf508d` — רישום; `C-042` נשאר פתוח | git |

**הפערים המדודים:**

| מדד | ערך |
|-----|------|
| שיגור → **התראה** (`70816e1` → מייל Sentry) | **18:34:40** (`1,114.67` דק') |
| שיגור → **תיקון ראשון** (`70816e1` → `1b8e44b`) | **18:53:33** |
| שיגור → `6/6` צרכנים (`1ebb559`) | **19:01:53** |
| ריצה מאומתת אחרונה → התראה | **7:34:05** |

🔴 **הפרכת הנחה בפרומפט:** הכותרת אמרה **«~20 שעות»**. המדוד הוא **18:34:40** לשיגור→התראה
ו-**18:53:33** לשיגור→תיקון. ⇒ ההנחה **גדולה ב-~1:06** מהמדוד. ⚠️ ⛔ **זה ⛔ אינו מרכך את
הממצא** — הוא מקבע אותו במספר שאפשר לחזור אליו.

---

## 2. `A2` — חוקי ההתראה של Sentry · אינטגרציית Discord · Allowed Domains

🔴 **חסום. ⛔ לא נמדד ו⛔ לא נוחש.**

הכותרת בפרומפט הצהירה `Connectors: Sentry MCP (read-only)`. **⛔ אין MCP כזה מחובר** —
נבדק בשתי שאילתות `ToolSearch` על מרחב הכלים; ⛔ אף כלי `sentry` ⛔ אינו קיים בסשן.
⛔ **גם ⛔ אין `SENTRY_AUTH_TOKEN` בידי** (§12: Code ⛔ אינו נוגע בסודות), ולכן ⛔ אין דרך
לקרוא את `/api/0/organizations/swingedge/alert-rules/`.

**מה כן שוחזר, ממקור עצמאי — תיבת המייל של ניב (Gmail MCP):**

- ⛔ **קיים לפחות חוק אחד** — `alert_rule_id=17075516` מופיע בקישורי ה-unsubscribe של מייל
  ה-New-issue מ-24.07.
- **הערוץ שנמדד הוא מייל.** ⛔ **אין בידי ראיה לאינטגרציית Sentry→Discord** בשום כיוון:
  ⛔ לא הוכחה קיימת ו⛔ לא הוכחה חסרה.
- **מה ש⛔ אי-אפשר לדעת בלי הקונסולה:** האם החוק חל על `New issue` בלבד או גם על
  `Regression`; מה ה-`environment`/`level` שלו; מה ה-throttle; ומה רשומות ב-**Allowed
  Domains** (רלוונטי ישירות — דומיין חסר ⇒ אירועי דפדפן נדחים בשקט).

⚠️ **הממצא שכן נמדד ואינו תלוי בקונסולה: האירוע נשא `handled = yes`.** boundary תופס את
הזריקה ⇒ Sentry מסמן אותה כמטופלת. ⛔ **מה זה עושה לחוק ההתראה — ⛔ לא נמדד**, אבל זו
בדיוק העדות שצריך לבדוק מולה, ⛔ לא להניח לגביה.

---

## 3. `A3` — Health Monitor: הופרך במפורש

**הטענה שנבדקה: «ה-Health Monitor היה ירוק לאורך הקריסה».** 🔴 **הופרכה — ⛔ הוא לא היה
ירוק, הוא ⛔ לא רץ בכלל.**

- `.github/workflows/health.yml` נושא **`on: workflow_dispatch:` בלבד**. ה-`schedule` הוסר
  ב-**25.07** בקומיט שמסביר בעצמו: *"DEPRECATED (2026-07-25): superseded by Sentinel"*.
- **הריצה האחרונה: `2026-07-25T14:27:03Z`** ⇒ **43 יום 4:41:26** לפני השיגור.
- **`0` ריצות בחלון.**
- ⚠️ **וגם לו רץ — ⛔ לא היה רואה.** `api/health.js` בודק Supabase · Finnhub · TwelveData ·
  CoinGecko · Frankfurter ומחזיר JSON מפונקציית serverless. ⛔ **הוא ⛔ אינו מריץ React ולו
  שורה אחת** ⇒ ⛔ אין לו שום מסלול לראות `ErrorBoundary` בדפדפן. **זה חסם מבני, ⛔ לא
  תזמוני** — התדר ⛔ אינו רלוונטי כאן.

⚠️ **נגזרת: `B-278` (פתוח מ-29.08) מדויק ומחריף** — `docs/AGENTS.md:10` עדיין מפרסם
«Health Monitor … 30 דק'». מסמך שמצהיר על סוכן שאינו רץ הוא בדיוק מה שמייצר את המשפט
«אבל ה-Health Monitor היה ירוק».

---

## 4. `A4` — UptimeRobot

🔴 **הקונסולה חסומה. ⛔ לא נוחש.** ⛔ אין בידי credentials, ⛔ ואין דרך לקרוא מה מוגדר שם:
כמה מוניטורים · HTTP או keyword · על איזה URL · מה ה-interval בפועל · האם היה alert.

**מה שכן נמדד — מהריפו בלבד, ו⛔ זה חצי תשובה:**

- `docs/AGENTS.md:14` ו-`docs/ARCHITECTURE.md:80` מצהירים ששני המקורות מכוונים
  ל-**`/api/health` כל 5 דקות**.
- `api/health.js:156-159` מתעד את `Access-Control-Allow-Origin: *` **כדי** ש-UptimeRobot
  ידגום מ-origin לא-ידוע (`DECISIONS` 06.08 · `B-112` ❄️).
- ⇒ **אם התצורה היא כמתועד, בדיקת-HTTP על `/api/health` ⛔ אינה יכולה לראות את הקריסה
  הזו במבנה.** הדף מחזיר **HTTP 200** גם כשה-boundary מציג `<p>` — הכשל הוא **אחרי**
  ההידרציה, בתוך React. ⛔ **ולא נדרש ניחוש כדי לקבוע זאת** — זה נגזר מ-`api/health.js`.
- ⛔ **מה ש⛔ אי-אפשר לגזור מהריפו: האם קיים keyword-monitor**, ואם כן — על איזו מחרוזת.

🔴 **וממצא שנשען על תיעוד קיים בלבד:** `INCIDENTS#5` (07.07, השבתת הדומיין) רשם תחת
**Prevention**: *"Add an UptimeRobot monitor on the domain itself (not just the app health
endpoint)"*. ⛔ **⛔ אין בריפו שום ראיה שזה בוצע** — לקח שנרשם ושביצועו ⛔ אינו ניתן
לאימות ממקום שאנחנו שולטים בו. ⚠️ **⛔ וזה ⛔ אינו האשמה — זו מדידה של גבול**: מוניטור
שחי רק בקונסולה של ספק חיצוני הוא מוניטור ש⛔ אין עליו שער.

---

## 5. `A5` — הסנטינל: **שתי** חסימות בלתי-תלויות

### 5.1 ביצוע — `6/37 = 16.2%`

`sentinel.yml:58-61` מתזמן `cron: '20,50 * * * *'` ⇒ בחלון של 18:34:40 היו **37 משבצות
מתוזמנות**. **בוצעו 6.** ⇒ **`6/37 = 16.2%` מהמשבצות המתוזמנות בחלון.**
**`6/6` הריצות `success`** ⇒ ⛔ **אין כאן כשל שאפשר להיתלות בו** (עקבי עם `M-010`:
`33/336 = 9.8%`, ועם `B-275`).

**כיסוי הדגימה של שכבת Sentry:** כל ריצה מסתכלת 35 דקות אחורה, והפערים בין הריצות
**גדולים מ-35 דקות בכל אחד מהחמישה** ⇒ ⛔ אין חפיפה ⇒ **`210 / 1,114.67` דק' = `18.8%`
מהחלון נדגם, ו-`81.2%` ⛔ לא נדגם כלל.**

**השכבה המאומתת:** `sentinel.yml:118-134` — `MIN >= 45 || MIN <= 9`. מהריצות: `45`·`3`·`9`
עברו, `33`·`13`·`41` לא ⇒ **`3/6` ריצות הריצו את הדפדפן המחובר.** האחרונה ב-`06:09:04Z`,
כלומר **7:34:05 לפני ההתראה**.

### 5.2 🔴 החסימה הראשונה — השאילתה ⛔ **אינה יכולה** לתפוס רגרסיה

`sentinel.yml:378-395`:

```
--data-urlencode "query=is:unresolved firstSeen:-35m"
```

`firstSeen` של `SWING-EDGE-A` הוא **2026-07-24T06:12:31Z** ו⛔ אינו זז ברגרסיה. ⇒ השאילתה
⛔ **לא יכלה להחזיר אותו באף אחת מ-6 הריצות, בשום שעה, בשום תדר.**

**נמדד — ⛔ לא הוסק:** `6/6` הריצות הדפיסו **«אין issues חדשים (35 דק')»** בזמן שהאירועים
נצברו ב-Sentry.

⚠️ **ולכן «להעלות את תדר הסנטינל» ⛔ אינו פותר את זה.** גם ב-`48/48` ריצות ביום, `48/48`
היו מדפיסות «אין issues חדשים». ⛔ **החסימה היא בשאילתה, ⛔ לא בשעון.**

⚠️ **ואפילו לו הייתה תופסת** — `add_fault "Sentry" "sentry|new_issue" "amber"` ⇒ **כתום, ⛔
לא אדום.**

### 5.3 🔴 החסימה השנייה — הפיקסצ'ר. **זו הסיבה שהסנטינל היה ירוק**

⚠️ **⛔ לא התדר, ⛔ ולא השאילתה.** גם ריצה מאומתת שרצה בול ברגע הנכון הייתה **עוברת**:

`tests-sentinel/sentinel-auth.spec.js:36`:
```js
const FIXED_TRADES = ['AAPL', 'NVDA', 'BTC-USD'];
```
**`3/3` נקובות ב-USD**, ⛔ **`0/3` במצב `contradicted`.** `grep -rE "contradicted|unverified_instrument"`
על `tests-sentinel/` ועל `tests/` מחזיר **⛔ כלום.**

⇒ בחשבון ה-QA `conv.reason` ⛔ **לעולם אינו** `unverified_instrument`/`no_rate` ⇒
`riskPct === null` ⛔ **לעולם אינו נוצר** ⇒ `null.toFixed` ⛔ לעולם אינו נזרק.

**וזה מדיד לשני הכיוונים — הנה ההוכחה שהוא *היה* אדום אילו הפיקסצ'ר ייצג:**

1. `SwingEdge_App.jsx:1410-1417` — `useState(() => … return "dashboard")` ⇒ `dashboard` היא
   **לשונית ברירת המחדל** (`NAV_KEYS:1039-1047`, ראשונה).
2. `:4420` `{tab === "dashboard" && (` … לוח הסיכון ב-`:4746-4880` והצרכנים שקרסו
   ב-`:4930-4961` — **כולם בתוך לשונית ברירת המחדל** ⇒ הקריסה מתרחשת ב**טעינה**, ⛔ ללא
   שום אינטראקציה.
3. `spec.js:110-168` — `page.on('pageerror')` נאסף, ו-`pageErrors` לא-ריק ⇒ **אדום**.
4. `spec.js:418-435` — בדיקת רינדור הג'ורנל, `browser-auth|journal-render`, **אדום**.
5. `spec.js:496-508` — בדיקה 6 **לוחצת במפורש** `[data-tour-tab="dashboard"]` — הלשונית
   שמארחת את הפאנל הקורס — ואז `openJournal(page)` ⇒ **אדום**.

⇒ **`3` בדיקות נפרדות היו נופלות.** ⛔ **הסנטינל ⛔ לא «פספס» — הוא נשאל שאלה על ג'ורנל
ש⛔ אינו מכיל את המקרה.**

🔴 **הכללה: ייצוגיות הפיקסצ'ר היא נקודת-עיוורון של הניטור, ⛔ לא של הבדיקות.** בדיקה
ירוקה על אוכלוסייה שאינה מכילה את התופעה ⛔ אינה ראיה — היא **מכנה שאינו יכול להכיל את
מה שנספר** (§2). ⛔ **⛔ ואין בריפו שום שער שמודד את ייצוגיות הפיקסצ'ר.**

### 5.4 מדוע ⛔ אף מנגנון-על לא צעק

- `failure-alert.yml:21-27` — `on: workflow_run:` ל-13 workflows כולל sentinel, עם
  `if: conclusion == 'failure'`. **`6/6` הריצות `success`** ⇒ **שקט מבני**, ⛔ לא תקלה.
- `watchdog.yml:48-53` — `MAXAGE=([sentinel.yml]=3 …)`, כלומר הוא **כן** נועד לתפוס סנטינל
  מיושן. ⛔ **`0` ריצות watchdog בחלון** (קודמת `2026-09-06T11:53:47Z`, הבאה
  `2026-09-07T13:54:14Z` — **11 דקות אחרי ההתראה**). ⇒ 🔴 **השומר על השומר סובל מאותה
  הידרדרות scheduler בדיוק** (`M-012`) ⇒ ⛔ **אינו שכבה בלתי-תלויה.**
- `fleet-weekly.yml:97-103` — מושך issues מ-Sentry, אבל `cron: '0 7 * * 0'` = **שבועי** ⇒
  זמן-תגובה עד 7 ימים. **דוח, ⛔ לא התראה.**

---

## 6. `A6` — ה-Error Boundary: ⛔ **כלום**, וזה הממצא

`src/main.jsx:97`:
```jsx
<Sentry.ErrorBoundary fallback={<p>משהו השתבש. רענן את הדף.</p>}>
```

**נמדד: `grep -cE "onError|beforeCapture|showDialog|setTag" src/main.jsx` ⇒ `0`.**

| מה נבדק | קיים? |
|----------|-------|
| `onError` — hook לצינור התראה משלנו | ⛔ **לא** |
| `beforeCapture` — הוספת תגים/הקשר לאירוע | ⛔ **לא** |
| תג/`fingerprint`/`level` שמבדילים boundary מכל שגיאה אחרת | ⛔ **לא** |
| `showDialog` — הזמנת המשתמש לדווח | ⛔ **לא** |

⇒ **מה שהבדיל את האירוע הזה ב-Sentry: ⛔ כלום שאנחנו שמנו.** כל מה שהיה שם הגיע
מ-Sentry עצמו — `transaction=/app` · `handled=yes` · `mechanism=generic` · `sentry:release`.

**ו-`Sentry.init` (`:38-75`):** `tracesSampleRate: 0.1` · `replaysOnErrorSampleRate: 1.0` ·
`enabled: import.meta.env.PROD` · ⛔ **אין `beforeSend`** ⇒ ⛔ אין נקודת-יירוט צד-לקוח
שיכלה לנתב את האירוע לשום מקום נוסף.

🔴 **הכללה:** ה-boundary **ראה את הכשל בפועל** — הוא זה שתפס אותו והחליף את המסך.
⇒ **השכבה היחידה שידעה בוודאות, בזמן אמת, במחשב של המשתמש — ⛔ לא הייתה מחוברת לשום
ערוץ.** זמן-ההתראה שלה הוא **∞**.

⚠️ **ומחרוזת ה-fallback ⛔ אינה נאמרת בשום בדיקה.** `grep -rn "משהו השתבש"` על `*.js`
`*.mjs` `*.jsx` `*.yml` מחזיר **`2` אתרי מקור בלבד** (`main.jsx:97`, ומחרוזת שונה
ב-`LandingPage.jsx:101`) — ⛔ **`0` אסרציות.** ⇒ המחרוזת שהמשתמש **בפועל ראה** במשך
18 שעות ⛔ אינה מנוטרת ע"י אף שכבה.

---

## 7. `A7` — מפת ערוצי Discord: מה **⛔ אינו** מחווט

**נמדד:** `14` workflows מפנים ל-webhook של Discord, ו**כולם** לאותו סוד יחיד —
`secrets.SENTINEL_DISCORD_WEBHOOK` (`sentinel.yml:479` · `failure-alert.yml:72` · ועוד).

| מקור אירוע | ערוץ Discord? | מדידה |
|-------------|----------------|--------|
| כשל workflow (13 מנוטרים) | ✅ דרך `failure-alert.yml` | קיים בקובץ |
| ממצאי סנטינל | ✅ ישירות | `sentinel.yml:479-513` |
| workflow מיושן | ✅ דרך `watchdog.yml` | קיים — ⛔ **אך ⛔ לא רץ בחלון** |
| דוחות תקופתיים | ✅ `fleet-daily`/`fleet-weekly`/`daily-digest` | קיים |
| 🔴 **שגיאת דפדפן אצל משתמש אמיתי** | ⛔ **לא** | ⛔ 0 מסלול מ-`main.jsx` |
| 🔴 **Error Boundary נפתח** | ⛔ **לא** | `onError` = 0 (§A6) |
| 🔴 **Sentry — issue חדש / רגרסיה** | ⛔ **לא נמדד** | ⛔ חסום (§A2) — ⛔ אין ראיה לכאן או לכאן |
| 🔴 **מוניטור חיצוני (UptimeRobot)** | ⛔ **לא נמדד** | ⛔ חסום (§A4) |

🔴 **התבנית:** ⛔ **כל** מה שמחווט ל-Discord הוא **CI על עצמנו**. ⛔ **אפס** ערוצים
מחווטים ל**דפדפן של המשתמש**. הערוץ היחיד שנמדד כפעיל מכיוון המשתמש הוא **מייל Sentry
לתיבה של ניב** — כלומר **התראה שדורשת שאדם יפתח מייל**, ⛔ לא push.

⚠️ **ונקודת-כשל יחידה:** `14/14` דרך אותו סוד. ⛔ **⛔ לא נמדד** אם ה-webhook תקף (זה
דורש שליחה — פעולה, ⛔ אסורה כאן).

---

## 8. `A8` — איזו חוליה ב-`verify` יכלה לתפוס? **⛔ אף אחת. וזה מדיד.**

**`package.json:40` — מחרוזת ה-`verify` ב-`51a12d2` וב-`dcf508d` היא בייט-לבייט זהה**
(`md5` זהה). ⇒ 28 החוליות שרצו על הקומיט ששיגר את הבאג הן **בדיוק** אלה שרצות היום.

**החוליה הרלוונטית היחידה היא `test:equitystate`** — היא נוספה ע"י `G2` **מעל אותו לוח
סיכון בדיוק**. מה היה בה:

| בקומיט | `grep -c "riskPct" scripts/equity-state-test.mjs` |
|--------|--------------------------------------------------|
| `51a12d2` (ששיגר) | **`0`** |
| `dcf508d` (היום) | **`9`** |

`S1`–`S4` שהיו שם קוראות **בייטים** של ה-JSX (`usesRIC` · `keepsMC` · `noCoalesce` ·
`figGated`). ⛔ **⛔ אף אחת מהן ⛔ אינה מריצה תא**, ולכן ⛔ אף אחת מהן ⛔ לא יכלה לזרוק.

🔴 **וזה ⛔ אינו «חסרה בדיקה» — זה כשל של סוג-הראיה.** האסרציות היו **על אותו קובץ, על
אותו פאנל, באותו גל**. הן היו ירוקות. הן ⛔ פשוט ⛔ לא מהסוג שיכול להיכשל על הבאג הזה.
⚠️ **וההערה שמעל השורה הקורסת חזתה את זה במילים** (`INCIDENTS#20`) ⇒ **הידע היה בריפו,
בצורת פרוזה, ⛔ ולא בצורת שער.**

**מה חסר — ⛔ נאמר, ⛔ לא נבנה** (§8.1 שלב 1):
1. הרצה של הביטוי בתא (זה **נבנה בדיעבד** ב-`R1`–`R4`/`P1`–`P4`, `30→38`) — ⛔ אך רק על
   **הפאנל הזה**. ⛔ **`63` אתרי `.toFixed` ב-`SwingEdge_App.jsx` נשארים ללא כיסוי.**
2. שער על **ייצוגיות הפיקסצ'ר** — ⛔ ⛔ אינו קיים בשום צורה.
3. ⚠️ **ו⛔ לא: «עוד שומרים».** על קובץ ~6,800 שורות שומר שיישכח הוא ודאות סטטיסטית
   (`B-304`).

---

## 9. 🔴 טבלת שש השכבות — חובה

| שכבה | מה מדדה **בפועל** | האם ראתה | תוך כמה זמן הייתה מתריעה |
|-------|---------------------|-----------|---------------------------|
| **1. Sentry** | קלטה את האירועים ב-`/app`; שלחה **מייל אחד** | ✅ **ראתה** · ❌ באיחור | **18:34:40** — ⛔ **ולא כי היא איטית.** הערוץ שירה הוא **regression**; ⛔ לא נמדד מה מפעיל התראה על issue חדש (§A2 חסום). ⚠️ ההתראה היא **מייל לתיבה** ⇒ זמן-התגובה תלוי במתי אדם פותח מייל |
| **2. Error Boundary** | **תפסה את הזריקה** והחליפה את המסך ב-`<p>` | ✅ **ידעה בוודאות, ראשונה** · ❌ ⛔ לא דיווחה | **∞** — `onError`·`beforeCapture`·תגים·`showDialog` = **`0/4`**. השכבה הקרובה ביותר לאמת היא היחידה שאינה מחוברת לכלום |
| **3. Health Monitor** | ⛔ **`0` ריצות.** אחרונה `2026-07-25T14:27:03Z` = **43 יום** קודם | ❌ | **∞ — פעמיים.** (א) `on: workflow_dispatch` בלבד מאז 25.07. (ב) ⚠️ **וגם לו רץ כל 30 דק' — `api/health.js` ⛔ אינו מריץ React** ⇒ חסם **מבני**, ⛔ לא תזמוני |
| **4. UptimeRobot** | 🔴 **⛔ לא נמדד — הקונסולה חסומה. ⛔ לא נוחש** | ⛔ **לא ידוע** | ⛔ **לא ידוע.** מה שכן נגזר מהריפו: אם היעד הוא `/api/health` כמתועד (`AGENTS.md:14`), הוא מחזיר **200** גם כשה-boundary על המסך ⇒ ⛔ **אינו יכול** לראות. keyword-monitor — ⛔ לא ידוע אם קיים |
| **5. Sentinel** | `6/37` ריצות · `3/6` מאומתות · **`6/6` הדפיסו «אין issues חדשים»** | ❌ | **∞ בתצורה הנוכחית** — **שתי** חסימות **בלתי-תלויות**: (א) `firstSeen:-35m` ⛔ **לא יכול** להתאים רגרסיה — ⛔ גם ב-`48/48` ריצות · (ב) הפיקסצ'ר `AAPL/NVDA/BTC-USD` ⛔ **אינו מייצר** `riskPct===null`. ⚠️ **החסימה (ב) היא הדומיננטית** — נמדד ש-`3` בדיקות היו אדומות אילו ייצג |
| **6. `verify` (28 חוליות)** | ירוקות על `70816e1`/`51a12d2`. `verify` **בייט-זהה** להיום | ❌ | **∞** — `riskPct` מופיע **`0`** פעמים ב-`equity-state-test.mjs` ב-`51a12d2`. `S1`–`S4` קראו **בייטים** של אותו פאנל בדיוק ⇒ ⛔ כשל **סוג-ראיה**, ⛔ לא כיסוי |

**סיכום המדוד:** `1/6` שכבות התריעה — ומאותה אחת, ⛔ **אין ראיה שהיא הייתה מתריעה על
תקלה שאינה רגרסיה.** `2/6` חסומות מלהימדד. `3/6` נמדדו כעיוורות **מבנית** — ⛔ לא
כמושבתות זמנית.

---

## 10. חלופות — ⛔ **הצגה. ⛔ אין המלצה. ⛔ לא בוצע דבר.**

> ⛔ **⛔ אין כאן דירוג ו⛔ אין «מומלץ».** כל חלופה עומדת עם **מה היא תופסת · תוך כמה זמן ·
> עלות · כשל שקט («נכשל ב-3 בלילה ואיש ⛔ רואה») · מבחן המטה-מטרה (§3)**. הכרעה = ניב.
> ⚠️ **⛔ ואף אחת ⛔ אינה מחליפה אחרת** — הן חוסמות **חסמים שונים**, וזו בדיוק הנקודה:
> §5 מדד **שתי** חסימות בלתי-תלויות באותה שכבה.

### חלופה `א` — `onError` על ה-Boundary ⇒ webhook

- **מה תופסת:** **כל** קריסת boundary אצל **כל** משתמש אמיתי, ⛔ ללא תלות בפיקסצ'ר,
  בשאילתה, ב-cron או בספק. השכבה היחידה שכבר **יודעת** (§A6).
- **תוך כמה זמן:** **שניות.** ⛔ אין דגימה ו⛔ אין תור.
- **עלות:** ⛔ 0 תשתית. ~5 שורות ב-`main.jsx`. ⚠️ **⛔ אך `main.jsx` נשלט ע"י `test:analytics`
  (17 אסרציות) ו-§12** ⇒ **דורש תוכנית ואישור**, ⛔ לא עריכה.
- 🔴 **כשל שקט:** ⛔ **חמור, ומדיד מראש.** (1) webhook חשוף בבנדל ⇒ ניתן להצפה ע"י כל אחד;
  ⇒ **חייב proxy** (`api/`), כלומר עוד מסלול שיכול ליפול. (2) ⛔ אם ה-fetch נכשל — ⛔ **אין
  מי שידווח על המדווח**, וזו בדיוק `R-2` בשכבת ההתראה. (3) קריסת-לולאה ⇒ מבול.
- **§3 — תחזוקה:** ✅ **מקטין ידני** — מסלול אחד, ⛔ ללא תלות בייצוגיות ובתדר.
  ⚠️ **⛔ אך מוסיף שכבה שצריך לאמת שהיא חיה** ⇒ ⛔ **אינו מקטין אם ⛔ אין לו heartbeat** —
  ובדיוק זה הכשיל את הסנטינל (`sentinel.yml:11`).

### חלופה `ב` — חוק התראה ב-Sentry ⇒ Discord

- **מה תופסת:** כל issue שעומד בחוק, כולל **רגרסיות** — כלומר את המקרה **הזה**.
- **תוך כמה זמן:** דקות (Sentry מקבץ).
- **עלות:** ⛔ 0 קוד · ⛔ 0 CI. הגדרה בקונסולת Sentry. **⛔ ניב בלבד** (§12 · A2 חסום).
- 🔴 **כשל שקט:** (1) החוק חי בקונסולת ספק ⇒ ⛔ **אין שער בריפו שיכול לראות שהוא שונה או
  נמחק**. (2) **`handled = yes`** — נמדד על האירוע ⇒ חוק שמסנן לפי `unhandled` יפספס
  **בשקט**. (3) Allowed Domains חסר ⇒ אירועים נדחים בשקט (⛔ לא נמדד).
- **§3 — תחזוקה:** ✅ **הכי מקטין ידני** — ⛔ 0 קוד לתחזק, ⛔ 0 CI לתקן.
  🔴 **⛔ ובדיוק בגלל זה הוא הכי חשוף ל-`R-6`**: הגדרה שחיה **רק** מחוץ לריפו ⛔ אינה ניתנת
  לביקורת ב-`git log` — וזה מה שהפך את §A2 לחסום.

### חלופה `ג` — תיקון שאילתת `Layer B` בסנטינל

- **מה תופסת:** מסירה את חסימה (א) של §5.2 — `is:unresolved` על `lastSeen` / הוספת
  `is:regression` במקום `firstSeen:-35m`.
- **תוך כמה זמן:** 🔴 **⛔ לא משתפר מהותית** — עדיין כבול ל-`6/37` הריצות
  (`M-010`: `9.8%`), כלומר **חציון של שעות**.
- **עלות:** שורה אחת ב-`sentinel.yml`.
- 🔴 **כשל שקט:** ⛔ **⛔ אינו נוגע בחסימה השנייה כלל** (הפיקסצ'ר) ⇒ ⚠️ **הסכנה האמיתית
  היא שהוא ייקרא כתיקון** ויסגור את הפריט בזמן ש-`81.2%` מהחלון ⛔ עדיין ⛔ אינו נדגם.
  ⚠️ ובנוסף: `add_fault` שם הוא **כתום ⛔ ולא אדום** ⇒ ⛔ לא היה מגיע ל-`failure-alert`.
- **§3 — תחזוקה:** ⚖️ **ניטרלי** — ⛔ לא מוסיף ו⛔ לא מוריד עבודה. **מסיר טענת-שווא**, וזה
  ערך בפני עצמו.

### חלופה `ד` — הרחבת הפיקסצ'ר של ה-QA

- **מה תופסת:** את הקריסה **הזו** ואת כל משפחת `conv.reason ∈ {unverified_instrument,
  no_rate}` — **נמדד** ש-`3` בדיקות היו אדומות (§5.3). ⇒ **החלופה היחידה שהוכח עליה
  שהייתה תופסת.**
- **תוך כמה זמן:** ⛔ **כבול ל-`3/6` הריצות המאומתות** ⇒ במקרה הזה **7:34:05** מהמאומתת
  האחרונה. ⚠️ ⇒ **תופסת בוודאות, ⛔ אך מאוחר.**
- **עלות:** עסקה או שתיים בחשבון ה-QA + שורה ב-`FIXED_TRADES`. ⚠️ **נוגע בנתוני DB של
  חשבון QA** ⇒ §12.
- 🔴 **כשל שקט:** ⛔ **הגרוע ביותר, כי הוא רקורסיבי** — הפיקסצ'ר החדש מכסה את המקרה
  **שכבר קרה**. ⛔ **⛔ אין שער שמודד ייצוגיות** ⇒ המקרה **הבא** ייפול באותה צורה בדיוק,
  והסנטינל יהיה ירוק שוב.
- **§3 — תחזוקה:** 🔴 **מגדיל ידני לאורך זמן** — כל פיצ'ר חדש דורש זכירה אנושית להרחיב
  את הפיקסצ'ר. זו בדיוק «תלות בזיכרון אנושי» (§2).

### חלופה `ה` — keyword-monitor חיצוני על מחרוזת ה-fallback

- **מה תופסת:** את התסמין שהמשתמש **בפועל רואה** — «משהו השתבש» על `/app` — ⛔ ללא תלות
  ב-Sentry, ב-CI, בפיקסצ'ר או ב-scheduler של GitHub. **השכבה היחידה הבלתי-תלויה לגמרי.**
- **תוך כמה זמן:** ⛔ **תלוי אם המוניטור מריץ JS.** 🔴 **בדיקת-HTTP פשוטה ⛔ לא תראה כלום** —
  המחרוזת מרונדרת ע"י React **אחרי** ההידרציה ⛔ ואינה ב-HTML הראשוני. ⇒ נדרש דפדפן אמיתי
  ⇒ ⛔ **⛔ לא ניתן לממש ב-UptimeRobot רגיל** ⇒ בפועל: עוד job שמריץ Playwright.
- **עלות:** אם UptimeRobot — ⛔ 0 קוד, אבל **⛔ לא מספיק** (ראה למעלה). אם Playwright —
  דקות Actions, ו**⛔ חוזר להיות כבול לאותו scheduler** של `M-012`.
- 🔴 **כשל שקט:** המחרוזת היא **ליטרל ב-JSX** ⛔ ללא שום אסרציה (§A6 — `0` אסרציות) ⇒
  שינוי נוסח ⇒ המוניטור עובר לירוק-לנצח **בשקט**. ⚠️ זהו `R-3` (אימות על טקסט מקור, ⛔ לא
  על התנהגות) בתחפושת מוניטור.
- **§3 — תחזוקה:** ⚖️ **תלוי-מימוש.** חיצוני = מקטין אך **⛔ בלתי-נראה לריפו** (`R-6`).
  פנימי = נראה, אבל **יורש את הכשל שהוא בא לכסות**.

### חלופה `ו` — ⛔ ללא שינוי

- **מה תופסת:** מה שנמדד: **`1/6` שכבות, ורק בתרחיש רגרסיה.**
- **תוך כמה זמן:** **~18.5 שעות**, וזה **⛔ ⛔ אינו רצפה** — הרגרסיה ⛔ הייתה מקרה **טוב**:
  היא ירתה ערוץ ייעודי. תקלה **חדשה** ⛔ לא נמדדה כלל.
- **עלות:** ⛔ 0.
- 🔴 **כשל שקט:** **זה כל הפריט.** ⚠️ **וההנמקה של 29.08 ⛔ אינה תקפה עוד** — אז נבחרה
  `D` («ללא שינוי») כשהעלות הייתה **תיאורטית**. היום היא נמדדה: **18:34:40 של מוצר שבור.**
- **§3 — תחזוקה:** 🔴 **מגדיל ידני** — ⛔ אין רשת ⇒ **הגילוי הוא ניב או משתמש.** ⚠️ **וזה
  מה שקרה: `C-042` נכשל, ו⛔ לא על ידינו — המשתמש הריץ אותו במקומנו.**

---

## 11. סריקה סביבתית (§11) — נמדד, ⛔ **לא תוקן**

⛔ שלושת התנאים ⛔ אינם מתקיימים (זהו שלב-1 של §8.1 ⇒ **⛔ אפס תיקון**). כל פריט ⇒ `STATE`.

1. 🔴 **`INCIDENTS#5` Prevention ⛔ אינו ניתן לאימות מהריפו** — «UptimeRobot on the domain
   itself». ⛔ **אין ראיה שבוצע.** ⇒ לקח שאין עליו שער (`R-6`).
2. ⚠️ **`14/14` ערוצי Discord על סוד **יחיד** `SENTINEL_DISCORD_WEBHOOK`** — נקודת-כשל
   אחת לכל רשת ההתראות. ⛔ תקפותו ⛔ לא נמדדה (מחייב שליחה = פעולה).
3. ⚠️ **`docs/AGENTS.md:14` מפרסם UptimeRobot «5 דק'»** באותה טבלה שבה `:10` מפרסם Health
   Monitor שאינו רץ (`B-278`) ⇒ ⛔ **⛔ אין ראיה שהשורה השנייה מדויקת יותר מהראשונה.**
4. ⚠️ **`watchdog.yml` ⛔ אינו שכבה בלתי-תלויה** — `MAXAGE[sentinel]=3` נכון, אבל
   ה-watchdog עצמו סובל מ-`M-012` ⇒ `0` ריצות בחלון. **שומר שנופל עם מי שהוא שומר.**
5. ⚠️ **`fleet-weekly` מושך Sentry שבועית** — ⛔ נקרא כערוץ Sentry ו⛔ אינו כזה (עד 7 ימים).

---

## 12. מה ⛔ **לא** נמדד — מוצהר, ⛔ לא מוסתר

| # | מה | למה |
|---|-----|------|
| 1 | חוקי ההתראה של Sentry · אינטגרציית Discord בצד Sentry · Allowed Domains · `environment`/throttle | ⛔ **אין Sentry MCP** (הופרך בכלים) · ⛔ אין token (§12) |
| 2 | תצורת UptimeRobot — כמה מוניטורים · HTTP/keyword · על איזה URL · האם התריע | ⛔ אין גישה לקונסולה |
| 3 | מתי ומי **סגר** את `SWING-EDGE-A` בין 24.07 ל-06.09 | נגזר מ-1 |
| 4 | החותמת שבה **אדם** ידע | ⛔ אין בידי חותמת לדיווח המשתמש |
| 5 | האם ה-webhook של Discord תקף **כרגע** | דורש שליחה = **פעולה** ⇒ אסור כאן |
| 6 | כמה משתמשים **בפועל** ראו את המסך השבור ב-18.5 השעות | דורש שאילתת אנליטיקה/DB — ⛔ מחוץ לגדר האבחון |

⚠️ **⛔ אף אחד מהשישה ⛔ לא הוחלף בהנחה.** «⛔ לא נמדד» ⛔ אינו «⛔ לא קיים».
