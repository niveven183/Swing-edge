# AUDIT 2026-09-09 — `B-312`: מדוע `4/4` הלוקייטורים מתים, ומה עוגן יציב חייב להיות

> **אבחון read-only (§8.1 שלב 1). ⛔ אפס תיקון — אף קובץ קוד לא נגע בו לפני דחיפת המסמך הזה.**
> רמה: **T3** · תשובות **כן/לא/כן/כן/כן**. הפיכות = `כן` כי מבחן הירי (④) כותב עסקה
> ל-`public.trades` בפרודקשן; אבטחה = `כן` כי הריצה דורשת `SENTINEL_QA_*` + `SUPABASE_ANON_KEY`.
> HEAD בזמן המדידה: `ad965fc`.

---

## 1. מה נשאל

הפרומפט של `B-312` נשא **הנחה מוצהרת**: «`\b` נכשל כי `TickerLogo` מזליג `SN` ללא רווח».
§8.1 מחייב להפריך או לאשש כל הנחה שבפרומפט **לפני** ביצוע. זה מה שנמדד.

## 2. הממצא — ההנחה **אוששה**, ובמדידה ישירה

### 2.1 המנגנון, בשלושה חלקים שנמדדו בנפרד

**(א) המקור מרנדר טקסט.** `src/components/TickerLogo.jsx:8-14` — כשה-`onError` יורה,
הענף מחזיר `<div>{cleanTicker.slice(0, 2)}</div>`. זהו **צומת טקסט**, ⛔ תמונה.

**(ב) הוא צמוד לטיקר.** `SwingEdge_App.jsx:5335` — `<TickerLogo …/>{t.ticker}` יושבים
זה לצד זה ב-JSX ⇒ ⛔ **אין צומת-טקסט מפריד ביניהם**. ה-`gap-1.5` הוא CSS, ⛔ טקסט.

**(ג) המחבר של Playwright ⛔ מוסיף מפריד.** נמדד ב-Chromium אמיתי (`playwright 1.61.1`)
מול ה-DOM המדויק:

| מה נקרא | הערך שנמדד |
|---------|-------------|
| `innerText` של ה-`<tr>` | `"SN\nSNTNL1\n\t2026-09-09"` — **יש** מפריד |
| `textContent` של ה-`<tr>` | `"SNSNTNL12026-09-09"` — **אין** מפריד |
| `hasText: /SNSNTNL1/` | **`1`** ⇒ המחבר של `hasText` ⛔ מוסיף מפריד |
| `hasText: /SN SNTNL1/` | **`0`** ⇒ אישוש הדדי לשורה מעל |
| `hasText: /\bSNTNL1\b/` | **`0`** ⇒ **הכשל** |

🔴 **זו הנקודה שהערה מול המחרוזת ⛔ יכולה לראות:** `innerText` **כן** נושא רווח,
ולכן «`\b` יעבוד» הוא ניחוש סביר — והוא שגוי, כי `hasText` קורא ערוץ אחר.
⚠️ **`deleteRow:376` משתמש ב-`innerText`** ⇒ שומר ה-`rowText.includes(sym)` שלו **היה עובר**;
הכשל כולו יושב ב-`rowsFor`.

### 2.2 המנייה המלאה — Chromium אמיתי, מול צורת התא של `:5335`

| לוקייטור | `SNTNL` | `SNTNL1` | `AAPL` |
|-----------|---------|----------|--------|
| `hasText: /\b…\b/` (**הקיים**) | **`0`** 🔴 | **`0`** 🔴 | `1` ✅ |
| `hasText: '…'` (תת-מחרוזת, **הקודם**) | **`2`** 🔴 | `1` | `1` |
| `has: [data-testid="trade-ticker-…"]` (**המוצע**) | **`1`** ✅ | **`1`** ✅ | **`1`** ✅ |

🔴 **`AAPL` היא זרוע הבקרה שמכריעה:** `\b` **עובד** עליה. ⇒ המבדיל הוא **תג-הגיבוי**,
⛔ הרגקס לבדו. המקור להבדל נמדד ישירות ב-HTTP:

| `financialmodelingprep.com/image-stock/…` | סטטוס |
|---|---|
| `SNTNL.png` | **404** ⇒ תג-אותיות **תמיד** |
| `SNTNL1.png` | **404** ⇒ תג-אותיות **תמיד** |
| `AAPL.png` | **200** ⇒ `<img>`, ו-`alt` ⛔ נכנס ל-`textContent` |

⇒ **`2/2` טיקרי הבדיקה נופלים תמיד · `1/1` הטיקר האמיתי ⛔ נופל לעולם.**
🔴 **⇒ `3/3` העסקאות הקדושות ⛔ יכלו לחשוף את הפגם** — הן ⛔ נבדקות ב-`\b` כלל.

### 2.3 ההערה `:252-256` — טענת-אימות ש⛔ הייתה אימות

הקובץ כותב «`/\bSNTNL\b/` rejects "SNTNL1"» ו-**«Verified mutually exclusive»**.
המדידה: הוא דוחה את **שניהם**. ⚠️ **הטענה נבדקה מול המחרוזת שהמפתח דמיין, ⛔ מול ה-DOM** —
‹R-3›. ⛔ **היא ⛔ מתוקנת-חלקית**: כל עוד היא מתארת רגקס, היא מתארת מנגנון שיוסר.

## 3. הכיוון — עוגן שאינו תלוי בטקסט-אחים

⛔ **חזרה לתת-מחרוזת אינה אפשרות** — נמדד `2` שורות ל-`'SNTNL'` ⇒ שומר «בדיוק `1`»
ב-`deleteRow:374` רואה `2` ומסרב. **זו הסיבה שהגל שינה אותה מלכתחילה.**

העוגן: `data-testid` על תא הטיקר (`:5335`) הנושא את `t.ticker` **כערך תכונה**.
השוואת-תכונה היא **שוויון מלא**, ⛔ התאמת-מחרוזת ⇒ `SNTNL` ו-`SNTNL1` בלעדיים
זה לזה **בבנייה**, ⛔ בטענה. נמדד `1`/`1`/`1` בטבלה ב-§2.2.

⚠️ **תקדים בקובץ עצמו:** `[data-testid="journal-table"]` (`:5280`) נולד מאותו שורש
בדיוק (`B-146` — `table.w-full.text-xs` תפס `6` טבלאות). זו אותה תרופה, שכבה אחת פנימה.

**רדיוס:** `rowsFor` · `sntnlRows` · `testRows` · `deleteRow` — **`4/4`** נגזרים מאותו `\b`
⇒ **`4/4`** עוברים לעוגן. ⛔ **אחד שיישאר על טקסט משאיר את הפגם בחיים במסלול אחד.**

## 4. 🔴 מבחן הירי (④) — **⛔ נצפה — חסום**

הפרומפט מחייב הרצה מול פרודקשן עם `3` זרועות בקרה. **נמדד שאי-אפשר:**

| סוד | אורך ב-env | שורות לא-ריקות ב-`.env` |
|-----|-----------|--------------------------|
| `SENTINEL_QA_EMAIL` | `0` | `0` |
| `SENTINEL_QA_PASSWORD` | `0` | `0` |
| `SUPABASE_URL` | `0` | `0` |
| `SUPABASE_ANON_KEY` | `0` | `0` |
| `VITE_SUPABASE_URL` | `0` | `0` |
| `VITE_SUPABASE_ANON_KEY` | `0` | `0` |

**`6/6` חסרים.** ⛔ אין התחברות · ⛔ אין `SNTNL1` בפרודקשן · ⛔ אין מסע.
§12 — סודות הם של ניב בלבד.

⚠️ **ומה שכן נמדד ⛔ מחליף את זה.** המדידה ב-§2.2 רצה ב-Chromium אמיתי מול **שחזור**
של צורת התא, ⛔ מול האפליקציה הבנויה, ⛔ מול React, ⛔ מול נתונים מ-Supabase.
היא מוכיחה **סמנטיקת לוקייטור**, ⛔ **מסע**. 🔴 **⇒ `B-312` נשאר פתוח**, וסגירתו
תלויה בריצת סנטינל אמיתית ש-`3` הבקרות שלה נצפו. ⛔ **«הקוד נראה נכון» ⛔ סוגר** —
זה בדיוק הכשל שייצר את `D-077`.

## 5. `D-077` — הפגם הוא הסיווג, ⛔ התוכן

`D-077` (`DONE.md:143`) **הצהיר במפורש** ש«הספק ⛔ הורץ מול Supabase אמיתי · ⛔ דפדפן ·
⛔ התחברות», ונסגר כ-**`אומת-מקור`**. הריצה האמיתית הראשונה נפלה **`71` דקות** אחר כך.

🔴 **ההצהרה הייתה נכונה. הסיווג היה שגוי.** `CLAUDE.md` §15 מתיר `אומת-מקור` רק
ל-**`T3` בלי מסך מושפע**. ל-`B-305` **היה** מסך מושפע: כל מהותו היא לוקייטורים
שרצים בדפדפן. ⇒ **גבול שהוצהר ⛔ גבול שכוסה** — הצהרת-פער ⛔ מסמיכה סגירה,
היא רק מתעדת אותה.

⛔ **`D-077` ⛔ נמחק** (§14 — מזהה לא נמחק ולא ממוחזר). הנדרש: **שורת תיקון מתוארכת**
שנושאת את הדלתא בלבד, ו-`INCIDENTS#21` שנושא את ההכללה.

## 6. `STATE` — תקציב

`wc -c docs/STATE.md` = **`15,948` / `16,000`** ⇒ **`52` בתים פנויים.**
⇒ גיזום עותק-שני **לפני** כל הוספה.

## 7. פסק דין

⚠️ **בצע עם הגנה.** ההנחה שבפרומפט אוששה במדידה ישירה; הכיוון (`data-testid`) נמדד
`1`/`1`/`1`. ⛔ **⛔ סגירת `B-312`** — ④ חסום ומוצהר, ו-`C-044` נשאר פתוח.
