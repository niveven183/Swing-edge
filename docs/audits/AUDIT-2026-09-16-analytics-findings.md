# AUDIT 2026-09-16 — ממצאי «ניתוח ביצועים» (F1–F5)

**סוג:** אבחון read-only (§8.1 שלב 1). ⛔ **אפס תיקון.** ⛔ אף קובץ ב-`src/` · `api/` · `scripts/` לא נגע.
**מקור התצפיות:** צילומי מסך של ניב מפרודקשן, 16.09 19:45–19:48.
**עץ:** `ec32e0e` · **סיווג המשימה:** `T3` (אמון=כן · רוחב=כן · ודאות=כן).
**כלי המדידה:** קריאת בייטים בעץ + probe שייבא את `computeTradingStats` ה**אמיתי** מ-`src/lib/tradingStats.js` והוזן בשלוש העסקאות הסגורות שבצילום (`−300` · `−350` · `+130`).

⚠️ **דוח זה מודד ו⛔ אינו קובע מה מתקנים.** כל ממצא נרשם כ-`B-` ב-`BACKLOG` ומדורג; ההכרעה מה נכנס לגל היא של ניב.

---

## 0 · פלט ה-probe (המדידה שעליה נשען כל הדוח)

אוכלוסייה: **3 עסקאות סגורות** מהצילום (`−300` · `−350` · `+130`), הסטאפ ⛔ לא נרשם באף אחת.

```
bySetup      : [{"name":"Unknown","count":3,"winRate":33.33333333333333}]
topEdges     : [{"name":"? + Neutral","n":3,"wr":33.33333333333333,"pnl":-520}]
antiEdges    : [{"name":"? + Neutral","n":3,"wr":33.33333333333333,"pnl":-520}]
profitFactor : 0.2 | avgWin 130 | avgLoss 325 | maxDD$ 650
equityCurve  : ["2026-06-10","2026-07-29","2026-07-29"]
```

---

## א · `C-049` — הכרעת ניב 16.09

### ① `C-049` ⇒ **נשאר פתוח**, והנימוק נרשם

הבדיקה כפי שנוסחה מודדת `+$130.00` · `−$325.00` · `−$650.00` על **הג'ורנל של ניב**.
ה-probe מדד את שלושת המספרים האלה מהמודול האמיתי:

| מספר | מונה / מכנה | אוכלוסייה | ערך |
|------|--------------|-----------|-----|
| `avgWin` | `130 / 1` | 1 עסקה רווחית מתוך 3 סגורות | **`130`** — שלם |
| `avgLoss` | `(300+350) / 2` | 2 עסקאות מפסידות מתוך 3 סגורות | **`325`** — שלם |
| `maxDD$` | `300+350` | רצף ההפסדים היחיד ב-3 הסגורות | **`650`** — שלם |

🔴 **שלושתם שלמים בדיוק.** `Math.round` על מספר שלם הוא **פעולת זהות**.
⇒ המסך מדפיס `+$130.00` · `−$325.00` · `−$650.00` **בשני העצים** — עם `Math.round` ובלעדיו.
⇒ **הבדיקה ⛔ אינה מבחינה בין תוקן ללא-תוקן. היא מודדת אפס.**

⚠️ זו בדיוק התבנית של האינווריאנטות ⚪ ב-`test:hydration`: ירוק בשני העצים ⛔ אינו ראיה לתיקון.

### ② מדד הסגירה מוחלף

מדד חדש (נכתב ל-`docs/CHECKS.md`):
**נדרשת עסקה סגורה שערכיה נושאים אגורות** ⇒ `avgWin` **או** `avgLoss` **או** `maxDD` ⛔ **אינם שלמים** ⇒ המסך מציג את האגורות.

⛔ **תצפית `.00` ⛔ אינה סוגרת את `C-049`.** על אוכלוסייה שלמה `.00` הוא הפלט ה**נכון**, ולקרוא אותו כראיה זה `R-4`.

### ③ `B-321` — ⛔ **בלי מצבה**

⛔ **לא נכתבה שורת `DONE`.** `B-321` נשאר **חסום על `C-049`**.
`5/14` האתרים שתוקנו הם עבודה מדודה ⛔ אך ⛔ אינם גל סגור כל עוד ⛔ אין אימות עין שיכול להיכשל (§15, `T3`).

### ④ ✅ אישוש — **bidi תקין בג'ורנל**

בצילום הג'ורנל: `−$325.00` — **מינוס משמאל**, במקום הנכון.
⇒ באג ה-bidi **מוגבל לכרטיסי «ניתוח ביצועים»** ו⛔ אינו רוחבי בכל האפליקציה. ראה `F4` לשורש.

### ⑤ ✅ אישוש — שלושה מספרים **נמדדו נכונים**

| מוצג | מונה / מכנה | אוכלוסייה | מאומת |
|------|--------------|-----------|--------|
| `PF 0.20` | `130 / 650` | רווח גולמי ÷ הפסד גולמי, 3 סגורות | ✅ `0.2` |
| `7.7 ימים` | `(9 + 14 + 0) / 3` | משך החזקה ממוצע, 3 סגורות | ✅ `7.67` |
| `−$417.92` | `(1531.04 − 1740) × 2` | P&L פתוח, פוזיציה פתוחה אחת | ✅ `−417.92` |

---

## ב · חמישה ממצאים — **נמדדו, ⛔ לא תוקנו**

### 🔴 F1 — «לקחים חכמים» ממליץ **להגדיל פוזיציה על סטאפ מפסיד** ⇒ `B-326` (דירוג 1)

**התצפית:** «`Unknown` הוא הסטאפ החזק ביותר שלך · 33% הצלחה על פני 3 עסקאות · התמקד יותר בתבנית הזו» + «**הגדל את גודל הפוזיציה**» — על אותו מסך שמציג `PF 0.20` ו«ההפסדים גדולים מהרווחים».

**השורש — `SwingEdge_App.jsx:750-851`, `generateSmartLessons`:**

```js
const bestSetup = [...stats.bySetup].sort((a, b) => (b.winRate - a.winRate) || (b.count - a.count))[0];
if (bestSetup && bestSetup.count >= 2) {
  // type: "strength" · «הסטאפ החזק ביותר שלך» · «הגדל את גודל הפוזיציה»
}
```

**תשובות למדידה שהתבקשה:**

1. **מה הסף למחמאה?** `count >= 2` — **וזה הכול.** ⛔ **אין רצפת win-rate.** הבחירה היא `argmax(winRate)` על **קבוצה בגודל 1**, כלומר «החזק ביותר» = «היחיד».
2. **האם `Unknown` (= לא נרשם) מוחרג?** ⛔ **לא.** `src/lib/tradingStats.js:417` — `const key = (raw ?? "").toString().trim() || "Unknown";` ⇒ סטאפ ש⛔ **לא נרשם** הופך לקבוצה **שוות-זכויות** בשם `Unknown`, ומשם ל«הסטאפ החזק ביותר שלך». זהו `R-2` בצורתו הקלאסית: היעדר-נתון שהומר לנתון.
3. **האם `PF<1` מבטל?** ⛔ **לא.** ⛔ אין שום קריאה ל-`profitFactor` במסלול הזה. `PF 0.20` יושב על אותו מסך ו⛔ אינו נוגע בהחלטה.

**מחמיר — המנוע המחמיר מושתק ע"י המנוע המתירני:**
`src/intelligence/core/AdaptiveLessons.js` מחזיק את הדפוס `best_setup_underused` ש**כן** גודר: `if (!top || top.winRate < 55) return null;` יחד עם `minSample: MIN_SAMPLE_EDGE`.
אבל `SwingEdge_App.jsx:2646-2664` ממזג `base` (המתירני) עם `adaptive` (המחמיר) ו**מנפה כפילויות לפי `channel: "best_setup"`** — ו-`base` **קודם**.
⇒ 🔴 **המנוע שנבנה כדי להימנע מהמחמאה הזו מנוצח ע"י המנוע שמייצר אותה.**

**למה זה חמור מכולם:** זו ⛔ אינה תצוגה שגויה — זו **הוראה שפוגעת בכסף**. משתמש שעושה מה שנכתב מגדיל חשיפה על דפוס שהמדידה שלנו עצמה מסמנת כמפסיד.

---

### 🔴 F2 — `ה-Edges שלך` ו-`דפוסים להימנע מהם` נושאים **שורה זהה** ⇒ `B-327` (דירוג 2)

**התצפית:** אותה שורה בדיוק בשני הפאנלים — `WR 33%` · `$520-` · `3 עסקאות` · `נייטרלי`.

**השורש — `src/lib/tradingStats.js:512-558`, `findEdges`:**

| צד | `minCount` | סינון איכות |
|----|-----------|--------------|
| `top` | `3` | 🔴 **⛔ אין.** ממיין לפי score ולוקח `slice(0,5)` |
| `anti` | `2` | `winRate < 50 \|\| avgR < 0` |

**תשובה למדידה שהתבקשה — האם הסלקטורים משלימים?**
⛔ **לא. הם ⛔ אינם משלימים, וזה ⛔ אינו מקרה קצה.**
`top` פירושו «כל מה שיש, עם `n≥3`, מדורג» — ⛔ ולא «מה שטוב».
⇒ צירוף בודד יכול — וכאן **בפועל** — להופיע ב**שניהם**.

**שוחזר ב-probe:** `topEdges` ו-`antiEdges` החזירו את אותו אובייקט בדיוק: `? + Neutral` · `n=3` · `wr=33.33` · `pnl=−520`.

⇒ הפאנל «ה-Edges שלך» מציג **חסרון** תחת כותרת של **יתרון**. אותו `R-2`: היעדר יתרון הוצג כיתרון.

---

### 🔴 F3 — ציר ה-X של עקומת ההון ⛔ אינו ממוין ⇒ `B-328` (דירוג 3)

**התצפית:** `06-09` → `07-29` → `06-10` → `07-29`.

**מה נמדד:**

1. **המיון בקוד ⛔ אינו שבור.** `SwingEdge_App.jsx:368-389`, `generateEquityCurve`:
   `getClosed(trades).sort((a,b) => (realizedAt(a) ?? 0) - (realizedAt(b) ?? 0))` ⇒ **עולה, מובטח**.
2. **`realizedAt` ו-`realizedDayKey` ⛔ אינם יכולים לחלוק על הסדר** — `src/utils.js:34-43`, שניהם קוראים את אותה עדיפות שדות `trade?.closedAt || trade?.date`.
3. **ה-probe אישר:** `["2026-06-10","2026-07-29","2026-07-29"]` — עולה.
4. 🔴 **הרב-קבוצה על המסך זהה לרב-קבוצה הצפויה** — `{06-09, 06-10, 07-29, 07-29}` (כולל עוגן ה-`firstRaw − 86_400_000`). ⇒ **רק הסדר החזותי שונה.**

⇒ **זו בעיית רינדור, ⛔ לא בעיית נתונים.**

**ההשערה המובילה (⛔ טרם אומתה):** `date` הוא דומיין **אורדינלי**. `scalePoint` של d3, שבו Recharts משתמש, **מנפה כפילויות בדומיין** ⇒ שתיים מתוך שלוש הסגירות נופלות על `2026-07-29` ומתמוטטות לעמדת-x אחת.

**נמדד בנוסף:** שני ה-`XAxis` (`:4608` · `:6166`) נושאים `tickFormatter={v => … v.slice(5)}` ⇒ **השנה נזרקת**, והתווית היא `MM-DD` בלבד. שנתיים שונות ייראו כאותו תאריך.

⚠️ **אישוש מכריע דורש דפדפן אמיתי** — אותו גבול בדיוק של `C-036`·`C-038`·`C-039`·`C-041`·`C-043`·`C-049`.

---

### 🔴 F4 — bidi: מינוס מימין בכרטיסי «ניתוח ביצועים», משמאל ב«מטריצת סטאפים» ⇒ `B-329` (דירוג 4)

**התצפית:** `$520.00-` בכרטיס · `-$520.00` במטריצה · **על אותו מסך**.

**תשובה למדידה שהתבקשה — מה שונה בין שני האתרים?**

**המחרוזת זהה ותקינה בשניהם.** `src/utils.js:181-188`, `money()` בונה `${sign}${sym}${abs}` ⇒ `"-$520.00"`. ⛔ **הבאג ⛔ אינו במחרוזת.**

**מה שמפריד הוא CSS — `src/index.css:41-45`:**

```css
[dir="rtl"] .font-mono      { direction: ltr; unicode-bidi: isolate; text-align: inherit; }
[dir="rtl"] .recharts-wrapper { direction: ltr; }
```

בהקשר RTL **חשוף**, אלגוריתם ה-bidi (UBA) פותר את `-` הפותח כ-ES→ON (W6)→R (N1) ⇒ רמה 1; המספר ⇒ רמה 2; סידור-מחדש L2 מרנדר `-$520.00` בתור `$520.00-`.

| אתר | מחלקה | תוצאה |
|-----|-------|--------|
| מטריצת סטאפים, `SwingEdge_App.jsx:6664` | `…text-end font-mono font-bold…` | ✅ **מוגן** ⇒ `-$520.00` |
| «היום הכי טוב», `SwingEdge_App.jsx:6334` | `text-xs text-slate-500 mt-1` | 🔴 **⛔ מוגן** ⇒ `$520.00-` |

🔴 **המסקנה החמורה:** `.font-mono` הוא **שומר ה-bidi של כל הכסף באפליקציה** — והוא **מרומז ו⛔ אינו נאכף**. ⛔ אין שער, ⛔ אין בדיקה, ⛔ אין תיעוד. מי שכותב כרטיס חדש בלי `font-mono` מקבל מינוס במקום הלא-נכון **בשקט**.

**מדידת רוחב:** נספרו **29** אתרי קריאה לפורמט-כסף ב-`SwingEdge_App.jsx`. **22/29** ⛔ אינם נושאים `font-mono` בחלון 6 שורות כלפי מעלה; מתוך ה-22, ≈**9** יושבים בתוך `recharts` ומכוסים ע"י הכלל הנפרד ב-`:45`.

⚠️ **והמדידה הזו עצמה ⛔ אינה אמינה ורושמים זאת:** היוריסטיקת-הקרבה נתנה **חיוב-שווא** על שורה `6334` — בדיוק הבאג שניב צילם — כי שורה `6333` היא **אח** הנושא `font-mono`, ⛔ ולא **אב**. ⇒ המספר `7/29 מוגנים` הוא **חסם עליון**, והמצב האמיתי גרוע ממנו. ⛔ **ספירה לפי קרבת-שורות ⛔ אינה יכולה להחליף שער שבודק את עץ ה-DOM.**

---

### 🔴 F5 — עמודה בגובה `33%` הנושאת תווית `50%` ⇒ `B-330` (דירוג 5)

**התצפית:** בגרף `אחוז הצלחה לפי סטאפ`, עמודה בגובה ~`33%` ולידה `50%`, בזמן ששאר המסך אומר `33%`.

**השורש — `SwingEdge_App.jsx:6265-6295`:**

```jsx
<ReferenceLine y={50} label={{ value: "50%", position: "insideTopRight" }} />
<Bar dataKey="winRate" label={{ position: "top", formatter: … `${count}t` }} />
```

**תשובות למדידה שהתבקשה:**

1. **מקור ה-`50%`:** תווית ה-**`ReferenceLine`** ב-`y=50` — קו-ייחוס, ⛔ **לא תווית עמודה**. הוא ממוקם `insideTopRight`, ועל עמודה בודדת הוא נוחת **צמוד לעמודה** ונקרא כשייך לה.
2. **גובה העמודה נכון:** `winRate = Math.round(33.33) = 33` — **מונה `1` ניצחון / מכנה `3` עסקאות**, אוכלוסייה: כל הסגורות בקבוצת `Unknown`.
3. **תווית העמודה עצמה** היא `3t` — **הספירה**, ⛔ לא האחוז.

**מחמיר — התיעוד סותר את הקוד:** ה-`InfoTooltip` ב-`:6274` מצהיר «גובה העמודה = מספר עסקאות» בזמן ש-`dataKey="winRate"`. ⇒ 🔴 **המקרא שמסביר את הגרף מסביר גרף אחר.**

---

## ⑦ · קוסמטי — **נרשם בלבד, ⛔ לא תוקן**

| # | ממצא | נמדד |
|---|-------|-------|
| 1 | `Unknown` ⛔ לא מתורגם | `labelFor("setup","Unknown","he")` → `"Unknown"`; לשם השוואה `labelFor("emotion","Neutral","he")` → `"נייטרלי"`. `src/i18n.js:2884-2888` **נכשל-פתוח**: מחזיר את ה-`value` כשאין רשומה ב-`DISPLAY_LABELS` |
| 2 | ציר «רגש» פרוש על `$4` | הציר מתוח על טווח P&L זעיר ⇒ רעש נראה כמו אות |
| 3 | «היום הכי טוב» מציג שלילי | `SwingEdge_App.jsx:6300-6302` ממיין `byDayOfWeek` לפי `totalPnL` יורד ולוקח `[0]`. כשכל הימים שליליים, «הטוב ביותר» הוא **הפחות גרוע** ומוצג כהישג |

⚠️ פריט 3 הוא קוסמטי **בגבול** — הוא מציג מספר שלילי תחת מסגור חיובי, וזו אותה משפחה של `F1`.

---

## דירוג — ⛔ **חמישה שורשים שונים, ⛔ לא מאוחדים**

| דירוג | מזהה | ממצא | שורש |
|-------|------|-------|-------|
| 1 | `B-326` | המלצה להגדיל פוזיציה על סטאפ מפסיד | `generateSmartLessons` בלי רצפה + `Unknown` כקבוצה + dedupe שמשתיק את המנוע המחמיר |
| 2 | `B-327` | שורה זהה ב-Edges וב-Anti-Edges | `findEdges.top` בלי סינון איכות |
| 3 | `B-328` | ציר X של עקומת ההון ⛔ ממוין | דומיין אורדינלי עם כפילויות ב-Recharts + `slice(5)` שזורק שנה |
| 4 | `B-329` | מינוס מימין בכרטיסי הביצועים | `.font-mono` כשומר-bidi מרומז ו⛔ לא נאכף |
| 5 | `B-330` | תווית `50%` על עמודה בגובה `33%` | תווית `ReferenceLine` ב-`insideTopRight` + tooltip שסותר את הקוד |

⛔ **איחוד אסור.** `B-326` הוא לוגיקת המלצה · `B-327` הוא סלקטור · `B-328` הוא סקאלה · `B-329` הוא CSS · `B-330` הוא תווית גרף. תיקון אחד ⛔ אינו נוגע באחרים.

---

## גבולות הדוח הזה

- ⛔ **שום ממצא ⛔ לא אומת בדפדפן.** כל המדידות הן קריאת בייטים + probe ב-Node. `F3` ו-`F4` נשענים על התנהגות רינדור אמיתית ⇒ אישוש מכריע דורש מסך — אותו גבול של `C-036`·`C-038`·`C-039`·`C-041`·`C-043`·`C-049`.
- ⛔ **ספירת ה-`font-mono` ב-`F4` היא חסם עליון ו⛔ לא מדידה** — היוריסטיקת-הקרבה נתנה חיוב-שווא מוכח.
- ⛔ **⛔ נכתב שום קוד.** ⛔ אין תיקון, ⛔ אין שער חדש, ⛔ אין `DONE`.
