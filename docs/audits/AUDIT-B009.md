# AUDIT-B009 — יומן ריק: מה המוצר ממציא בפועל

> **שלב 1 · אבחון read-only.** ⛔ אפס שינוי קוד. כל מספר כאן נמדד מהעץ ב-`3944340`.
> נכתב לפני כל נגיעה בקוד, לפי `CLAUDE.md` §8.1.

| | |
|---|---|
| תאריך | 16.08.2026 |
| בסיס | `3944340` · `git pull --ff-only` נקי · עץ נקי |
| סיווג §15 | **T3** · תשובות: לא/כן/לא/כן/כן |
| מצב | ⏸️ **עצירה לאישור** — שלב 2 טרם התחיל |

**חמש השאלות:** הפיכות **לא** (אין DB/מייל/מחיקה — רינדור בלבד) · אמון **כן**
(מספר שקרי שהמשתמש רואה) · אבטחה **לא** · רוחב **כן** (`tradingStats.js` +
צרכנים) · ודאות **כן** (⛔ לא נמדד מי צורך את `EMPTY_STATS`). 3/5 ⇒ `T3`.

---

## 0 · הנחות שבפרומפט — אושרו והופרכו

⚠️ שלוש מתוך חמש ההפניות שבפרומפט ובבקלוג שגויות. סחיפת שורות, ⛔ לא שגיאה מהותית.

| הנחה | מקור | פסק | מדידה |
|------|-------|-----|--------|
| `isEmpty` **קיים** | `B-009` | ✅ **אושש** | `tradingStats.js:276` (`false`) · `:301` (`true`) |
| `isEmpty` ב-`:254,279` | `B-009` | ❌ **הופרך** | בפועל `:276,301` — סחיפה של 22 שורות |
| `src/lib/statisticalModels.js` | פרומפט | ❌ **הופרך** | הנתיב ⛔ אינו קיים. בפועל `src/intelligence/utils/statisticalModels.js` |
| `lastWeekStats` — 3 מפתחות בלי `beRate` | `B-065` | ✅ **אושש** | `:296` פולט 3 · `summarize()` ב-`:370` פולט 4 |
| CTA ב-`SwingEdge_App.jsx:3896` | `B-009` | ❌ **הופרך** | `:3890–3902` הוא **התראת מחיר** (`alertTriggered`), ⛔ לא CTA. ה-CTA האמיתי — §4 |
| `TradeDNA.js:91,136` ⇒ 50/50/50/50 | `B-009` | ⚠️ **אושש בתוכן** | `:91`/`:136` הן **כותרות סעיף**; ההחזרות ב-`:96` ו-`:141` |
| `B-061` ב-`statisticalModels.js:192` | `B-061` | ❌ **הופרך** | `:192` הוא `sharpeR`. הקוד **היגר** — §5 |
| 33 מ-46 משתמשים ללא עסקה | פרומפט | ⬜ **לא נמדד** | טענת DB. לאבחון הזה אין גישת DB ⇒ ⛔ אינה מאוששת כאן |

---

## D1 · `isEmpty` — תעודה בלי צרכן

**מדידה — כל מופע בריפו, 5/5:**

```
scripts/tradingstats-test.mjs:99    if (s.isEmpty) return;
scripts/tradingstats-test.mjs:124   eq("isEmpty", s.isEmpty, true);
scripts/tradingstats-test.mjs:498   eq("dormant — journal is NOT empty", d.isEmpty, false);
src/lib/tradingStats.js:276         isEmpty: false,
src/lib/tradingStats.js:301         isEmpty: true,
```

**2 יצרנים · 3 מופעים במבחן · 0 צרכנים ב-`src/` · 0 ב-`SwingEdge_App.jsx` · 0 ב-`api/`.**

⇒ `B-009` צודק לחלוטין: הדגל **קיים** ו⛔ **אינו נצרך**. זהו דפוס `B-154` בדיוק —
תעודה שנכתבה, נבדקה, ואיש אינו קורא אותה.

🔴 **והצרכן היחיד משתמש בו כדי ⛔ לא לבדוק.** `tradingstats-test.mjs:99` הוא
`ratesSumTo100`, ושורתו הראשונה היא `if (s.isEmpty) return;` — כלומר הפיקסטורה
הריקה **פטורה** מאסרציית ה-partition. הדגל היחיד שמסמן "אין נתון" משמש היום
להחרגת המקרה הריק מהשער שהיה תופס אותו.

**מה הוא מחזיר בכל מצב:** `true` ⟺ `getClosed(trades).length === 0` (בדיקה
ב-`:47,50`). ⚠️ `isEmpty` הוא טענה על **סגורות**, ⛔ לא על היומן: יומן עם 5
פוזיציות פתוחות ו-0 סגורות מקבל `isEmpty: true` ו-`openTrades: 5` יחד.

---

## D2 · `EMPTY_STATS` — הפרדה בין שגוי לנכון-באפס

`src/lib/tradingStats.js:284–303`. ⚠️ ⛔ **לא הכול צריך להפוך ל-`null`** — הפיכה
גורפת היא סירוב, והוא כישלון בדיוק כמו המצאה.

### א. ⛔ שגוי — מנה עם מכנה אפס (⇒ `null`)

| מפתח | ערך היום | למה שגוי |
|------|----------|-----------|
| `winRate` | `0` | "0% הצלחה" על `0W/0L` — טענה שהמשתמש הפסיד בכל עסקאותיו |
| `lossRate` | `0` | אותו כשל, ראי |
| `beRate` | `0` | אותו כשל |
| `profitFactor` | `0` | `0/0`. ⚠️ במסלול החי `profitFactorFromPnls` מחזיר `Infinity` על רווח-בלי-הפסד ⇒ `0` כאן הוא ערך **שלישי** שאינו נגזר |
| `planFollowedWR` | `0` | מנה על אוכלוסייה ריקה |
| `planIgnoredWR` | `0` | מנה על אוכלוסייה ריקה |
| `lastWeekStats.winRate` | `0` | §D3-ב — סותר את `summarize()` באותו קובץ |
| `lastMonthStats.winRate` | `0` | אותו כשל |

### ב. ✅ נכון-באפס — נשאר `0` (מדידה אמיתית)

`totalTrades` · `total` · `openTrades` · `wins` · `losses` · `be` · `totalPnL` ·
`totalWin` · `totalLoss` · `rSampleSize` · `lastWeekStats.count` ·
`lastWeekStats.pnl` · `maxWinStreak` · `maxLossStreak` · `bestStreak` ·
`streakRuns: []` · `equityCurve: []` · `bySetup/byEmotion/byMarket/byEntryQuality/byDayOfWeek: []` ·
`topEdges/antiEdges: []` · `closedMetrics: []` · `pnlByCurrency: {}` ·
`tradesByCurrency: {}` · `currencies: []` · `mixedCurrency: false`

**מבחן ההבחנה:** אלה **ספירות ואוספים**, ⛔ לא מנות. "ספרתי 0 עסקאות" הוא מדידה;
"שיעור ההצלחה 0%" הוא מנה בלי מכנה. `tradingstats-test.mjs:604` כבר מקבע את
ההבחנה: `יומן ריק ⇒ פילוח ריק, ⛔ לא אפס מזויף`.

### ג. ✅ כבר תקין — התקדים בתוך הקובץ

`avgR: null` (`:290`). ⇒ הדפוס **כבר קיים** ב-`EMPTY_STATS` עצמו, כבר בבייסליין
הקפוא (`tradingstats-baseline.json:22`), וכבר עובר במבחן (`:127`).

### ד. ⬜ להכרעה — ⛔ לא חד-משמעי

| מפתח | ערך | הטיעון לשני הכיוונים |
|------|-----|----------------------|
| `avgWin` · `avgLoss` | `0` | ממוצע על קבוצה ריקה ⛔ אינו 0 ⇒ נוטה ל-`null`. ⚠️ אבל `fmt$` מקבל מספר, ו-`fmtPrice(null)` ⇒ `"$0"` (`B-019`) |
| `bestWin` · `worstLoss` | `0` | קיצון על קבוצה ריקה. אותו שיקול |
| `returnPct` | `0` | ⚠️ **תשואה של 0% על הון שלא נסחר היא מדידה נכונה** ⇒ נוטה להישאר `0` |
| `currentStreak` | `0` | "אין רצף" = 0. נוטה להישאר `0` |
| `maxDrawdown` · `maxDD` · `currentDrawdown` | `0` | ⚠️ נוגע ב-`B-061` — §5 |
| `avgHoldHours` · `avgHold` | `0` | ממוצע על קבוצה ריקה. אותו שיקול כמו `avgWin` |
| `planAdherence` | `0` | מנה `planYes/closed` = `0/0` ⇒ נוטה ל-`null` |

⇒ **קבוצה ד׳ היא ההכרעה שאני מבקש מניב בשלב 2**, ⛔ לא החלטה שאקבל לבד.

---

## D3 · אתרי הצריכה — מי מדפיס מה

### א. 🔴 שכבת התצוגה **כבר תוקנה** ומחכה למנוע

`src/utils.js:305–307` — הערה קיימת בקוד, ⛔ לא פרשנות שלי:

> `Number(v) || 0` turned a missing value into a confident "0%". A real 0 and
> "we never measured this" are different claims, and only one of them is a
> number. **Empty portfolios still read 0% — EMPTY_STATS carries a numeric 0.**

**מדוד:** `formatPct(null)` ⇒ `"—"` · `formatReturnPct(null)` ⇒ `"—"` ·
`fmtR(null)` ⇒ `"—"` · `fmtNum(null)` ⇒ `"—"`.

⇒ הפורמטרים **מוכנים**. `EMPTY_STATS` הוא החוליה החסרה היחידה, והקוד עצמו כבר
מצביע עליה בשמה. ⇒ **תיקון המנוע לבדו מייצר `"—"` בכל אתרי הרינדור, בלי לגעת
באף `.jsx`.** זו בדיוק ה"תת-קבוצה המינימלית שאינה גל" ש-`B-009` מתאר.

### ב. שני מקורות אמת ל-`summarize` — `B-065` אושש

| מקור | קוד | פלט |
|------|-----|------|
| `summarize([])` | `:370` | `{count:0, pnl:0, winRate:null, beRate:null}` — **4 מפתחות** |
| `EMPTY_STATS` | `:296–297` | `{count:0, pnl:0, winRate:0}` — **3 מפתחות** |

⇒ הם חלוקים **גם בצורה וגם בערך**, ו-`summarize()` הוא הצודק. `EMPTY_STATS`
משכפל את התפקיד במקום לקרוא לו.

⚠️ **אבל `B-065` מניח צרכן שאינו קיים.** מדוד — `lastWeekStats` ו-`lastMonthStats`:
**0 צרכנים ב-`src/`.** מופיעים רק ב-`tradingstats-test.mjs` וב-בייסליין.
⇒ הכשל **אמיתי בחוזה** ו⛔ **בלתי-נראה למשתמש היום**.

### ג. מפתחות עם **0 צרכנים ב-`src/`** — נמדד על כל הריפו

`planFollowedWR` · `planIgnoredWR` · `planAdherence` · `lastWeekStats` · `lastMonthStats`

⇒ 5 מפתחות שקיימים, נבדקים ב-13 אסרציות, מוקפאים בבייסליין — ו⛔ אינם מוצגים
לאיש. תיקונם הוא **תיקון חוזה**, ⛔ לא תיקון UX. חשוב לתעדוף.

### ד. אתרי רינדור אמיתיים

| # | מיקום | מה מודפס ביומן ריק | פסק |
|---|--------|--------------------|------|
| 1 | `SwingEdge_App.jsx:4265` — כרטיס Win Rate בלוח | `formatPct(0)` ⇒ **`"0%"`** · תת-כותרת `"0W / 0L"` | ⛔ **מומצא** |
| 2 | `:4266` — כרטיס Avg R | `fmtR(null)` ⇒ **`"—"`** | ✅ **כבר תקין** — ההוכחה שהדפוס עובד |
| 3 | `:4263` — Net P&L, `trendText` | `formatReturnPct(0)` ⇒ `"0.00%"` | ⬜ קבוצה ד׳ |
| 4 | `:4262` — Account Equity | `fmtBalance(equityBase)` — בסיס ההון שהמשתמש הצהיר | ✅ מדידה |
| 5 | `:4267` — Streak | `0` · `"best: 0"` | ⬜ קבוצה ד׳ |
| 6 | `:4816` — PRO STATS BAR ביומן | מגודר ב-`closedTrades.length > 0` | ⚠️ **דולף** — §6 |
| 7 | `:4842` — Profit Factor ביומן | מגודר ב-`Number.isFinite` ⇒ `0` **עובר** ומודפס `"0.00"` | ⛔ **מומצא** (מאחורי §6) |
| 8 | `:4311` — `DNACard dna={aiDNA}` בלוח | `50 / 50 / 50 / 50` | ⛔ **מומצא ×4** — §5 |
| 9 | `:4312` — `GrowthChart current={aiGrowth.total}` | `50` | ⛔ **מומצא** — §5 |
| 10 | `:4111` — `DNACard dna={menteeDNA}` | מגודר ב-`menteeRealTrades.length === 0` | ✅ מגודר כראוי |

**קריאות ל-`useTradingStats` — 3/3:** `:2213` (`stats`) · `:2314` (`journalStats`) ·
`WeeklyReviewTab.jsx:59` (`wStats`).

---

## D4 · היקף — מה נכנס ומה יוצא

| מזהה | אותו משטח קוד? | פסק | נימוק מדוד |
|------|----------------|------|-------------|
| **`B-009`** | — | ✅ **הגל** | `EMPTY_STATS` + `isEmpty` |
| **`B-064`** | **כן** — אותן שורות | ✅ **נכנס** | `:289,294` הן בדיוק השורות. הבקלוג עצמו אומר "יש לסגור איתו" |
| **`B-065`** | **כן** — `:296–297` | ✅ **נכנס** | אותה פונקציה. סגירה חלקית = "חצי חוזה" כלשון הפריט |
| `B-019` | לא | ⛔ **יוצא** | `fmtPrice` — 3 אתרי קריאה, כולם עם מספר. ⚠️ **הופך לרלוונטי** אם קבוצה ד׳ תוכרע ל-`null` (`avgWin` ⇒ `:781,786`) ⇒ **תלוי-החלטה, ⛔ לא מחוץ לתמונה** |
| `B-021` | לא | ⛔ **יוצא** | `DayTradesModal.jsx:47–52` — `catch` ריק. קובץ אחר, כשל אחר (בליעת חריגה, ⛔ לא מנה) |
| `B-060` | לא | ⛔ **יוצא** | `src/utils.js` — בדיקת falsy על `exit`. אוכלוסייה, ⛔ לא מנה |
| **`B-061`** | **חלקית** | ⚠️ **להכרעה** | §5 |
| `B-063` | לא | ⛔ **יוצא** | רצף BE — לוגיקה על יומן **מלא** |
| `B-067` | לא | ⛔ **יוצא** | `fmt$0` על ציר גרף. פורמטר, ⛔ לא `EMPTY_STATS` |

---

## D5 · 🔴 מה משתמש **חדש** רואה בפועל

**טאב ברירת המחדל = `"dashboard"`** (`SwingEdge_App.jsx:1351–1358`) ⇒ הלוח הוא
המסך הראשון, ⛔ לא היומן.

**מנייה — כל מספר מומצא על המסך הראשון:**

| # | מה | ערך | מקור |
|---|-----|------|--------|
| 1 | Win Rate | `"0%"` ליד `"0W / 0L"` | `EMPTY_STATS.winRate` |
| 2 | DNA · Risk | `50` | `TradeDNA.js:141` |
| 3 | DNA · Discipline | `50` | `TradeDNA.js:141` |
| 4 | DNA · Consistency | `50` | `TradeDNA.js:141` |
| 5 | DNA · Growth | `50` | `TradeDNA.js:141` |
| 6 | Growth Score | `50` | `GrowthTracker.js:36,61,73,92` |

⇒ **6 מספרים מומצאים · מתוכם 1 בהיקף `EMPTY_STATS` · 5 בשכבת ה-intelligence.**

🔴 **הממצא החשוב ביותר של האבחון הזה:** רוב המספרים המומצאים שמשתמש חדש רואה
⛔ **אינם** ב-`EMPTY_STATS`. הם ב-`TradeDNA.computeScores` וב-`GrowthTracker`,
ששניהם מחזירים `50` — ⚠️ ו-`50` גרוע מ-`0`: `0%` נראה כמו "אין נתון" לעין
סלחנית, אבל `50` נראה כמו **ציון אמיתי בינוני**, והוא מוצג בכרטיס עם
`data-tour="trading-dna"` — כלומר הסיור המודרך של המוצר **מצביע עליו**.

⚠️ שני אלה ⛔ **אינם** ב-`B-009` ו⛔ אינם בבקלוג כפריט. ⇒ §10.1 — דורש שורה.

---

## D6 · בייסליין — הכשל הצפוי, בשמו

**כן, קיים.** `scripts/fixtures/tradingstats-baseline.json` · השוואת
`assert.deepStrictEqual` לכל תרחיש (`tradingstats-test.mjs:688`).

**מצב נוכחי — ירוק:** `271/271` אסרציות עוברות · `8/8` תרחישים deep-equal.

**מה ייפול בתיקון — צפייה אדומה לגיטימית, ⛔ לא סיבה לרכך:**

| # | אסרציה | היום | אחרי |
|---|---------|-------|-------|
| 1 | `tradingstats-test.mjs:127` `eq("winRate", s.winRate, 0)` | `0` | `null` |
| 2 | `:128` `eq("profitFactor", s.profitFactor, 0)` | `0` | `null` |
| 3 | `:137` `eq("beRate", s.beRate, 0)` | `0` | `null` |
| 4 | בייסליין · תרחיש `empty` deep-equal | תואם | **מתפצל** |

⚠️ **`4` הוא היחיד שדורש נוהל.** המבחן עצמו מתעד אותו (`:692–694`): מחיקת קובץ
הבייסליין, ריצה חוזרת, **וסקירת ה-diff במסמך התוכנית**. ⇒ ה-diff המלא ייכנס
לתוכנית שלב 2, ⛔ לא ייקלט בשקט.

**מה ⛔ לא ייפול — נבדק:**
- `ratesSumTo100` (`:99`) — `if (s.isEmpty) return;` מדלג ⇒ `null+null+null` ⛔ אינו נבדק
- `SCALARS` (`:469`) — `s[k] === null || Number.isFinite(s[k])` ⇒ **מקבל `null` במפורש**
- בלוק 8 (A5, `:472–549`) — בודק `dormant`/`allOnPlan`, ⛔ לא `empty`

⇒ **תשתית ה-`null` כבר בנויה בכל שלוש השכבות** (פורמטרים · אסרציות · בייסליין).
`EMPTY_STATS` הוא החריג היחיד שנותר.

---

## סריקה סביבתית (§11) — ⛔ לא תוקן, ניב מחליט

### 1. ⚠️ הגדר על האוכלוסייה הלא-נכונה — דליפה שאינה בבקלוג

`SwingEdge_App.jsx:4816` מגדר על **`closedTrades`** (מ-`realTrades`, **לא מסונן**,
`:2202`) ומדפיס **`journalStats`** (מ-`filteredRealTrades`, **מסונן**, `:2310,2314`).

⇒ משתמש עם יומן **מלא** שמסנן לסטאפ בלי עסקאות סגורות: הגדר נפתח, `journalStats`
הוא `EMPTY_STATS`, וכל שבעת התאים מציגים אפסים מומצאים — כולל
`profitFactor` ⇒ `"0.00"` (`Number.isFinite(0)` הוא `true`, `:4842`).

🔴 זהו **המסלול היחיד שבו משתמש פעיל רואה את `EMPTY_STATS`**, והוא ⛔ לא מכוסה
באף פריט בבקלוג. ⚠️ ⛔ לא תיקנתי — זה שינוי JSX, ⛔ מחוץ להיקף האבחון.

### 2. `TradeDNA` + `GrowthTracker` מחזירים `50` — §D5

⛔ אין להם פריט בבקלוג. §10.1 דורש שורה.

### 3. `isEmpty` משמש כדי **לדלג** על אסרציה

`tradingstats-test.mjs:99`. ⛔ לא באג, אבל ראוי לתשומת לב: אחרי התיקון,
`ratesSumTo100` על יומן ריק תוכל להיבדק כ-`null` מפורש במקום לדלג.

---

## מה אני מבקש מניב — ⛔ לפני שלב 2

1. **הכרעת קבוצה ד׳ (§D2-ד)** — 7 מפתחות. ⛔ לא אחליט לבד; זו החלטת מוצר.
2. **היקף `B-061`** — פנימה או החוצה (§5 · `tradingStats.js:127`).
3. **`TradeDNA`/`GrowthTracker` (§D5)** — פריט נפרד ל-`BACKLOG`, או בהיקף.
4. **דליפת הפילטר (§סריקה·1)** — פריט נפרד, או בהיקף.

**המלצתי:** גל צר — `EMPTY_STATS` בלבד (קבוצה א׳ + הכרעת ד׳), `B-009`+`B-064`+`B-065`
יחד. שלושת האחרים ⇒ `BACKLOG` עם מזהים חדשים. הנימוק: שכבת התצוגה כבר מוכנה
(§D3-א), ולכן הגל אינו נוגע באף `.jsx` — וזה בדיוק מה שהופך אותו ל"תת-קבוצה
מינימלית שאינה גל".

---

## §5 · `B-061` — הקוד היגר

`B-061` מפנה ל-`statisticalModels.js:192`. **`:192` הוא `sharpeR`.** הביטוי חי היום
בשני מקומות:

| מיקום | קוד | יחס ל-`B-009` |
|--------|-----|----------------|
| `src/lib/tradingStats.js:127` | `const ddPct = peak > 0 ? ((peak - equity) / peak) * 100 : 0;` | **בתוך `computeTradingStats`** — אותה פונקציה |
| `src/intelligence/utils/statisticalModels.js:211` | `const dd = peak > 0 ? (peak - v) / Math.abs(peak) : 0;` | קובץ אחר |

⇒ `B-061` **חופף חלקית** ל-`B-009`. ⚠️ אבל הוא כשל על יומן **מלא** (חשבון שמעולם
לא עלה מעל נקודת הפתיחה), ⛔ לא על יומן ריק. ⇒ **המלצתי: החוצה**, עם עדכון
המיקום בבקלוג כדי שלא ייעלם.

---

## הצהרת שלב

⛔ **אפס שינוי קוד בוצע.** הפקודות היחידות שהורצו: `git pull --ff-only` ·
`git status` · `grep`/`sed` · `npm run test:tradingstats` (קריאה — קובץ הבייסליין
קיים ⇒ המבחן ⛔ אינו כותב).

⏸️ **ממתין לאישור ניב על ארבע ההכרעות.** שלב 2 (Plan Mode → `docs/plans/`) יתחיל
רק אחריו.
