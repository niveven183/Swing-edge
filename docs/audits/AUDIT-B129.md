# AUDIT-B129 — שלושת מסלולי הכתיבה ומקור תווית המטבע

> **שלב 1 · אבחון read-only.** ⛔ אפס שינוי קוד · ⛔ אפס כתיבה ל-DB.
> נמדד 16.08 · `HEAD` בתחילת האבחון `493cfc4` · סיווג §15 **T3** (תשובות 5/5).
> ⚠️ הריפו **ציבורי** ⇒ כל מספר כאן **מצרפי**. ⛔ אין מזהי משתמש · ⛔ אין טיקרים.

---

## 0 · תקציר — ארבע הנחות שבפרומפט הופרכו במדידה

| # | ההנחה כפי שנרשמה | מה נמדד | מסקנה |
|---|---|---|---|
| 1 | «OCR כותב בלי תווית מטבע כלל» | `api/ocr.js` ⛔ **אינו מסלול כתיבה**. שלושת צרכניו קוראים `setForm` ⇒ **מילוי טופס**. השורה נכתבת אח"כ ע"י המסלול הידני | 🔴 **הופרכה** |
| 2 | «`grep -c currency api/ocr.js` = 0 ⇒ פגם» | 0 **נכון כעובדה**, ⛔ אך אינו פגם: מסלול שאינו כותב אינו אמור לשאת תווית | 🔴 **הופרכה כראיה** |
| 3 | «עסקאות קיימות **בלי** תווית» | **67/67 השורות נושאות תווית.** `null` = **0** · מחרוזת ריקה = **0** | 🔴 **הופרכה** |
| 4 | «הייבוא הוא המימוש הנכון היחיד ⇒ ליישר אליו» | **13/13 השורות השגויות נכתבו ע"י מסלול הייבוא עצמו** | 🔴 **הופרכה** |

⇒ **הפגם אינו תווית חסרה. הפגם הוא תווית `USD` שנכתבת ללא תנאי.**
דלת פתוחה — כן; ⛔ אך לא הדלת שנרשמה, ו⛔לא במסלול שנרשם.

---

## 1 · D1 — שלושת המסלולים זה מול זה

⚠️ נמדד לפי **מה שנכתב בפועל**, ⛔ לא לפי הכוונה בקוד. שני מסלולי הכתיבה
היחידים הם `insertTradeRow` (`SwingEdge_App.jsx:2785`) ו-`insertTradeRows`
(`:3034`); שניהם עוברים דרך סריאלייזר יחיד `tradeForSupabase`
(`src/supabaseClient.js:77`).

| | **ידני** | **ייבוא** | **OCR** |
|---|---|---|---|
| בונה עסקה | `SwingEdge_App.jsx:2720` | `src/import/normalizeRow.js:173` (+`fifoMatch.js:58` יורש ב-`...buy`) | ⛔ **אינו בונה** |
| כותב ל-DB | ✅ `:2785` | ✅ `:3034` | ⛔ **אינו כותב** |
| מקור התווית | `currency: capitalCurrency` (`:2737`) — מטבע ה**הון** | סולם 4 דרגות (§2) | ⛔ אין. מחזיר `{ticker,entry,stop,target,side,…}` (`api/ocr.js:660`) |
| כשהמקור שותק | ⛔ אין מצב שותק — לטופס **אין שדה מטבע** | נופל לדרגה 3 ואז 4 | לא ישים |
| מה נכתב בפועל | `capitalCurrency` תמיד | בפרודקשן: **`USD` ב-100%** (§3) | לא ישים |
| מצב סירוב | ⛔ אין | ✅ `bad_currency` — **רק** כשהתא קיים ובלתי-קריא | לא ישים |

**מסקנת D1.** ⛔ **אין שלושה מסלולים — יש שניים.** ה-OCR הוא **קדם-מילוי** של
הטופס הידני (`handleImageUpload` `:3369` · האנלייזר `:3465` · מודל היומן
`:3657` — שלושתם `setForm`), ולכן שורה שמקורה בצילום מסך יוצאת מהמסלול
**הידני** ונושאת `capitalCurrency`. ⇒ `C-022`, שמונה שלושה מסלולים, מונה
ישות שאינה קיימת.

---

## 2 · D2 — סולם הראיות של הייבוא: האם הוא מבחין «ידוע» מ«הונח»?

`src/import/normalizeRow.js:114-138`, ארבע דרגות:

| דרגה | מקור | סוג |
|---|---|---|
| 1 | `unitResolver` של פרופיל הברוקר — נגזר מהאריתמטיקה של השורה | **ראיה** |
| 2 | תא מטבע ממופה — הקובץ מצהיר | **ראיה** |
| 3 | `opts.defaultCurrency` = מטבע ה**הון** | **הנחה** |
| 4 | `"USD"` ליטרלי | **הנחה** |

🔴 **התשובה: לא — הוא ⛔ אינו מבחין.** ארבע הדרגות נכתבות לאותו שדה `currency`
כמחרוזת שטוחה (`:181`). ⛔ **אין שום שדה שמתעד איזו דרגה ירתה** — נבדק בכל
מסלולי הכתיבה: `currencySource` · `currency_source` · `ccySource` ·
`assumedCurrency` · `currencyOrigin` = **0 מופעים**. (ה-`source:` שב-`api/ocr.js:148`
הוא גזירת **מחיר כניסה**, ⛔ לא מטבע.)

⇒ תווית שנקנתה בראיה (דרגה 1) ותווית שנוחשה (דרגה 4) **בלתי-נבדלות אחרי
הכתיבה**. זהו `R-1` במלואו, בתוך המימוש שהוגדר «הנכון היחיד».

⚠️ **ודרגה 1 מותנית בהתאמת פרופיל.** קיימים **שני** פרופילים בלבד — `ibi`
(`brokerProfiles.js:154`) ו-`altshuler` (`:188`), ושניהם דורשים עמודות
`value`/`qty`/`rate`. קובץ גנרי ⇒ `unitResolver` הוא `null` ⇒ `divisor = 1`
**וגם** נפילה לדרגה 3/4. כלומר המנגנון שיודע אגורות והמנגנון שקובע מטבע
**נכשלים יחד, באותו תנאי**.

> **מענה ישיר לשאלת D2 («אם הוא עצמו מפוקפק — ⛔ אין למה ליישר»):**
> הסולם **תקין בסדר העדיפויות** ו**פגום בפלט** — הוא מוחק את דרגתו שלו.
> ⇒ יישור שני המסלולים אליו **כפי שהוא** משכפל את הפגם במקום לסגור אותו.

---

## 3 · D3 — מלאי בפועל (Supabase, read-only, מצרפי)

**`public.trades` — 67 שורות · 13 משתמשים · 0 שורות דמו.**

| מדד | ערך |
|---|---|
| שורות עם תווית | **67/67 = 100%** |
| שורות עם `null` | **0/67** |
| שורות עם מחרוזת ריקה | **0/67** |
| ערכי תווית **נבדלים** | **1** — `USD` בלבד |

🔴 **`ILS` ⛔ לא נכתב מעולם. אפס פעמים מתוך 67.**

### 3.1 היכן התווית שגויה

| צורת טיקר | שורות | משתמשים | טווח `entry` | `entry > 1000` | סגורות |
|---|---|---|---|---|---|
| אלפביתי אמריקאי | 51/67 | 12 | 7.53 – 397.80 | 0/51 | 28 |
| **מספרי (מזהה ת"א)** | **13/67** | **1** | **1,784 – 11,370** | **13/13** | **0** |
| אחר | 3/67 | 3 | 58.63 – 95,000 | 2/3 | 1 |

**13/67 השורות המספריות נושאות `USD` על מחירים בסדר-גודל אגורות** — 1,784
עד 11,370 הוא בדיוק החתימה של נייר ת"א הנקוב ב-₪17.84–₪113.70. כל 13 **פתוחות**
⇒ ⛔ אין עדיין נזק ב-P&L ממומש, **אך** הן חיות בהון ובחשיפה.

⚠️ **13/67 ⛔ ואינו 16/61 שנרשם ב-`B-129`.** האוכלוסייה גדלה 61→67; מספר
השורות המספריות **13**. אימות בלתי-תלוי: ההערה ב-`src/lib/instrumentCurrency.js:188-190`
מתעדת «יומן של **13 ניירות ת"א** + 2 אמריקאיים, כל 15 נשאו תווית USD» — תואם
מדידה זו במדויק.

### 3.2 ייחוס מסלול — B-071 מאושש, וייחוס חלקי בכל זאת

⚠️ **`B-071` מאושש: ⛔ אין עמודת מקור.** `information_schema` מחזיר **27
עמודות** (25 + `currency` ב-T9 + `horizon`); ⛔ אין `source` · ⛔ אין
`import_batch_id`.

**ייחוס חלקי אפשרי בכל זאת** — הייבוא מטביע `createdAt` **סינתטי** בשעה
`14:30` מקומית (`normalizeRow.js:179`), בעוד הידני מטביע `new Date()` אמיתי:

| חותמת `createdAt` מקומית | שורות | מזה מספריות |
|---|---|---|
| **בדיוק `14:30:00`** ⇒ ייבוא | **44/67** | **13** |
| שעות מפוזרות אמיתיות ⇒ ידני | 23/67 | 0 |

🔴 **כל 13 השורות השגויות נשאו את חתימת הייבוא — 13/13.**

⛔ **מה ⛔ אי-אפשר לדעת, ו⛔לא אשער:** **מתי** הן נכתבו. עבור שורות ייבוא
`createdAt` הוא **תאריך העסקה**, ⛔ לא זמן הכתיבה, ו⛔אין בסכימה שום עמודת
זמן-כתיבה. ⇒ **בלתי-מדיד** אם הן קדמו למיגרציית T9 (02.08) או לא.

---

## 4 · D4 — מה קורה במורד הזרם

⚠️ **הממצא המפתיע: שכבת הקריאה כבר מיישמת «הצהרה» מלאה, ו⛔אינה סומכת על
התווית השמורה.** `src/lib/instrumentCurrency.js` מגדיר ארבעה מצבי ראיה
(`:51-56`) וגוזר **בזמן קריאה, ⛔ לעולם אינו שומר** (`:116`).

| צרכן | מה קורה לשורה מספרית עם `USD` שגוי | מצב |
|---|---|---|
| `deriveInstrumentCurrency:139` | טיקר מספרי ⇒ `AMBIGUOUS/numeric_ticker` — ⛔ **לא** `ILS` ו⛔**לא** `USD` | ✅ **כבר מטופל** |
| `isAggregatable:169` | `AMBIGUOUS` ⇒ **false** ⇒ מוחרג מהמצרפי | ✅ **כבר מטופל** |
| `isMixedCurrency:192` | ולו שורה אחת לא-מאומתת ⇒ **true** | ✅ **כבר מטופל** |
| הכותרת / הבאנר / ה-PDF | אחרי `B-119`/`B-142`: מוחרג **ונספר** בתג | ✅ **כבר מטופל** |
| 🔴 **`currencyOf` (`src/utils.js:164`)** | `CURRENCY_SYMBOL[t.currency] ? t.currency : "USD"` — **קורא-תווית ישיר**, **36 אתרי קריאה** | 🔴 **עדיין דולף** |

🔴 **הדליפה שנותרה היא הסמל, ⛔ לא המצרף.** מחיר נייר של שורת ת"א יודפס
`$1,784` במקום `₪17.84` — התווית השמורה קובעת את הסמל ב-36 אתרים, בעוד
המצרפים כבר מחריגים אותה. ⇒ מספר **בודד** שגוי על המסך, בתוך מצרף **נכון**.

### 4.1 🔴 ממצא נוסף — שלוש הערות בקוד **נסחפו** מול הסכימה החיה

⚠️ **⛔ אינן שגויות-מלידה — הן התיישנו ב-12.08.** מיגרציית `D-026`
(`supabase/migrations/20260812120000_trades_currency_widen.sql:35`) מריצה
`alter column currency drop default;` **במכוון**, והנימוק כתוב בה (`:18`):
«`default 'USD'` אומר "אם לא ידעת, זה דולר"». המיגרציה **הורצה ואומתה**
(`docs/STATE.md:37`).

| מיקום | מה ההערה מצהירה **היום** | הסכימה החיה |
|---|---|---|
| `src/supabaseClient.js:56-57` | «text, **default 'USD'**» | `is_nullable: YES` · `column_default: **null**` |
| `src/import/normalizeRow.js:120-121` | «matching the column's **`default 'USD'`**» | ⬆ אותו דבר |
| `src/utils.js:162-163` | «the migration's **`default 'USD'`** says the same thing **on the server side**» | ⬆ אותו דבר |

🔴 **ההשלכה מעשית, ⛔ ואינה סגנונית.** שלוש הנמקות עצמאיות נשענות על ערובת-שרת
שהוסרה לפני ארבעה ימים. ⇒ דרגה 4 (`"USD"` הליטרלי בלקוח) היא כעת **הדבר
היחיד** שמונע `null`, ו⛔אין עוד רשת מתחתיה. מסלול כתיבה עתידי שישמיט את השדה
יכתוב **`null`** — ו-`currencyOf` (`src/utils.js:164`) יציג `"USD"` בכל זאת
⇒ **`null` ו-`USD` בלתי-נבדלים על המסך.** ‹`R-1`› ‹`R-6`›

---

## 5 · D5 — `C-022`

**מה הבדיקה אומרת** (`docs/CHECKS.md:42`): «אחרי גל על מסלול כתיבה — שלושת
המסלולים מיושרים», תנאי-גלישה: כל גל שנוגע במסלול שכותב ל-`trades`.

**האם בוצעה:** ⛔ **לא.** עמודת התוצאה נושאת אבחנה (🔴 «⛔ אינם מיושרים»)
מ-13.08, ⛔ לא ביצוע. ⚠️ §11.2 החדש דורש **ביצוע** ⛔ ולא אזכור ⇒ במצבה
הנוכחי `C-022` ⛔ **אינה יכולה** לשמש מדד סגירה כפי ש-`NEXT` #1 מניח.

🔴 **ובנוסחה הנוכחי היא ⛔ אינה ניתנת לירוק לעולם** — היא דורשת יישור **שלושה**
מסלולים, ואחד מהם (OCR) ⛔ אינו מסלול כתיבה (§1). ⇒ **`C-022` עצמה טעונה
ניסוח מחדש**, וזו הכרעה שאני ⛔ לא מבצע כאן.

**מה ייחשב ירוק — הצעה לאישור, ⛔ לא הכרעה:** ששני מסלולי הכתיבה יסכימו על
**מקור** התווית ועל **דרגת הראיה** שלה, ושהדרגה תהיה **נצפית** אחרי הכתיבה
(היום ⛔ אינה — §2).

---

## 6 · D6 — 🔴 חסימה מול הצהרה

⛔ **⛔ אינני מכריע. שתי האפשרויות עם עלותן המדודה.**

⚠️ **התיקון שהשאלה מניחה — «⛔ אין כתיבה בלי תווית» — ⛔ אינו חל על שום
שורה שנמדדה.** 67/67 **יש** תווית (§3). חסימה על תווית **חסרה** מגינה מפני
מצב ב-**0/67**, ובעוד 13/67 השורות השגויות היו **עוברות** אותה — הן נושאות
`USD` תקין-צורנית. ⇒ **החסימה בניסוחה זה ⛔ אינה נוגעת בפגם.**

| | **א׳ · חסימה** | **ב׳ · הצהרה** |
|---|---|---|
| מה | ⛔ אין כתיבה בלי **ראיה** (דרגה 1–2). דרגה 3–4 ⇒ סירוב | דרגת הראיה **נכתבת לצד** התווית |
| עלות מדודה | 🔴 **44/67 שורות הייבוא היו נדחות** — ⛔ אין פרופיל תואם ⇒ ⛔ אין דרגה 1. ⇒ ייבוא CSV גנרי מפסיק לעבוד | 🔴 **עמודה חדשה** ⇒ מיגרציה (§12: ניב מריץ). ⇒ כל צרכן צריך לדעת על מצב שלישי |
| למשתמש שאינו אשם | 🔴 **נשבר** — קובץ גנרי תקין נדחה | ✅ ⛔ אינו נשבר — נכתב עם דרגה מוצהרת |
| האם סוגר את 13 השורות | ⛔ **לא** — קיימות, ⛔ לא נכתבות מחדש | ⛔ **לא** — ⛔ אך הופך אותן ל**ניתנות לזיהוי** |
| ⚠️ כפילות מול הקיים | 🔴 **גבוהה** — `deriveInstrumentCurrency` כבר מסרב בקריאה (§4) | ⚠️ בינונית — הגזירה כבר מצהירה; החידוש הוא **התמדה** |

⚠️ **ציר שלישי שהמדידה חושפת, ו⛔אינו אף אחת מהשתיים:** מאחר ששכבת הקריאה
**כבר** מסווגת ומחריגה נכון (§4), ייתכן שהפער האמיתי הוא **36 אתרי
`currencyOf`** — קורא-תווית שלא עבר את המעבר שהמצרפים עברו. תיקון שם ⛔ אינו
נוגע בכתיבה, ⛔ אינו דורש מיגרציה, ו⛔אינו שובר ייבוא. **מוצג למדידה, ⛔ לא
כהמלצה.**

---

## 7 · D7 — שכנים

| פריט | אותו משטח? | מדידה |
|---|---|---|
| **`B-080`** — OCR במלכודת מטבע | ✅ **כן, ואותו קובץ** | `buildPrompt` (`api/ocr.js:245+`) ⛔ אינו מזכיר יחידה/מטבע. ⚠️ **אך המסקנה מתהפכת:** מאחר שה-OCR מזין את הטופס הידני, מספר שנקרא באגורות נכנס לשדה ומקבל `capitalCurrency`. ⇒ `B-080` הוא **הרגל השלישית של אותו פגם**, ⛔ לא פריט נפרד ⇒ **מועמד חזק לאותו גל** |
| **`B-069`** — ולידציה טאוטולוגית | ⚠️ אותו **קובץ**, ⛔ לא אותו פגם | אושש שהוא **חי**: `:157` גוזר `side` מהגיאומטריה, `:161` מאמת מולה ⇒ ⛔ אינו יכול להיכשל. ⛔ אינו נוגע במטבע ⇒ **נפרד** |
| **`B-070`** — 4 מימושי גיאומטריה | ⛔ **לא** | `utils.js` · `DecisionCoach` · `localAI` — משטח **כיוון**, ⛔ לא מטבע ⇒ **נפרד** |
| **`B-071`** — אין סימון מקור | ✅ **כן** | אושש (§3.2). ⚠️ אם ייבחר מסלול **ב׳** (הצהרה), שתי העמודות נוגעות באותה מיגרציה ⇒ **הכרעה נדרשת: גל אחד או שניים** |

⛔ **⛔ לא תוקן דבר מהם.**

---

## 8 · מה נשמר / מה משתנה — כלל הברזל של הגל

**באבחון הזה: ⛔ אפס שינוי.** ⛔ אפס קוד · ⛔ אפס `UPDATE` · ⛔ אפס `INSERT` ·
⛔ אפס מיגרציה. הקובץ הזה הוא התוצר **היחיד**.

🔴 **ולעניין 13 השורות — ⛔ אינן ממצא חדש, והן כבר חסומות.**
`docs/STATE.md:37` נושא אותן כ**גל ב׳**: «13 שורות ת"א באגורות — ⛔ **חסום על
הודעה מוקדמת למשתמש**» (`B-005` · `B-121`). ⇒ המדידה כאן **מאששת פריט קיים**
במספר מדויק, ⛔ ואינה פותחת חזית.

⚠️ **המסקנה עבורן היא `UPDATE` ⇒ אני עוצר ושואל (כלל הברזל), והמלצתי ⛔ לא
לבצעו בגל הזה.** תיקון ל-`ILS` דורש גם **חלוקה ב-100** (אגורות→₪) ⇒ כתיבה
מחדש של `entry`/`stop`/`target`/`exit` בשורות של משתמש **חי** — ובדיוק לכן
הפריט חסום על **הודעה מוקדמת**, ⛔ לא על קוד. ⚠️ ובנוסף `B-121` רושם ש**מסלול
הקריאה טרם יודע לחלק ב-100** ⇒ תיקון הכתיבה לבדו היה מייצר ₪17.84 שיוצג
שגוי ממילא.

**נתיב חזרה** לכל שינוי עתידי בהן: 🔴 **⛔ אין.** ⛔ אין גיבוי פר-שורה ו⛔אין
עמודת מקור (§3.2) ⇒ זו בדיוק הסיבה ש-`B-071` חסום-על-ניב.

---

## 9 · הכרעות שאני מבקש מניב — ⛔ ולא מבצע

1. **`C-022` בניסוחה הנוכחי אינה בת-ירוק** (§5). לנסח מחדש לשני מסלולים?
2. **א׳ / ב׳ / הציר השלישי** (§6) — או שילוב.
3. **13 השורות** — להשאיר (המלצתי) או לגעת? נגיעה = `UPDATE` + חלוקה ב-100.
4. **`B-080` באותו גל?** (§7 — הוא רגל של אותו פגם).
5. **`B-071` באותה מיגרציה** אם נבחר מסלול ב׳?

⚠️ **`NEXT` ⛔ לא עודכן** — הגל טרם נסגר.
