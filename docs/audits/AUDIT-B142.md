# AUDIT — `B-142` · ההון ה**סגור** לא-מומר תחת סמל החשבון

**תאריך:** 2026-08-14 · **HEAD:** `db6a775` · **סיווג:** T3 (תשובות: לא/כן/לא/כן/כן = 3)
**סוג:** read-only. ⛔ אפס שינוי קוד. ⛔ אפס תיקון תוך כדי.

**כלי המדידה:** בדיקן `node` חד-פעמי (`/tmp/b142-probe.mjs`, ⛔ לא מקומט) שמייבא את
**פונקציות הריפו עצמן** — `resolveEquityBase` · `makeConvertingCalc` ·
`computeTradingStats` · `livePnlAmount` · `convert` — ומריץ אותן על שמונה מצבי מטבע.
⚠️ **תיקוף הבדיקן:** ארבעת המצבים S1–S4 החזירו `10,124.75` · `10,274.25` ·
`20,274.25` · `5,124.75` — **בדיוק** ארבעת המספרים שאומתו בעין ב-`C-023`/`D-032`
ב-13.08. ⇒ הבדיקן משחזר את המסלול האמיתי, ⛔ ואינו מודל מקביל.

---

## 0. תקציר מנהלים — שישה ממצאים, שלושה מהם משנים את התוכנית

| # | ממצא | השפעה |
|---|------|--------|
| 🔴 **F1** | **אסרציה `13.9b` ⛔ אינה קו קפוא על `B-142`.** הקו הקפוא שלה **הוסר כבר ב-12.08 (גל ג׳)**; מה שיושב שם היום (`:470-501`) הוא **הצהרת ההסרה** + אסרציות שמקבעות התנהגות **נכונה** (`accountAmount` מסרב על נייר לא-מאומת ⇒ `"—"`). ⛔ **אין לה שום נגיעה ב-`stats.currentEquity`.** `CLAUDE.md` §7 כבר אומר זאת מפורשות | ⛔ **הדרישה "להסיר את 13.9b" ⛔ אינה ניתנת לביצוע** — אין מה להסיר. הסרתה היום תמחק אסרציה **ירוקה** שמגינה על התנהגות תקינה |
| 🔴 **F2** | **הסימן שנרשם ב-`B-142` שגוי.** במצב החמישי הכותרת מדפיסה **₪** ⛔ ולא `$10,500.00`: `dispCcy` נופל ל-`capitalCurrency` **בכוונת מכוון** (`:2047`). ה-`$` שייך ל-`statsCcy` — כלומר ל-**PDF ולדוחות**, ⛔ לא ל-`fmtBalance(curEquity, dispCcy)`. הערת `A8` בטסט אומרת זאת בעצמה: *"ההון הסגור מוצג לא מומר תחת ₪"* | ⚠️ **האתר הואשם בטעות.** תוכנית שתרדוף אחרי הסמל בכותרת תתקן מסך שאינו שבור בסימן |
| 🔴 **F3** | **הפגם הוא חיבור חוצה-יחידות בתוך `currentEquity` עצמו, ⛔ לא "מספר נכון עם סמל שגוי".** נמדד S5: `equityBase` = 10,000 **RAW ILS** (`resolveEquityBase` מסרב להמיר ומחזיר את ההון **בלתי-נגוע**, `equityBase.js:80-81`) **+** `totalPnL` = 100 **USD** ⇒ `currentEquity` = 10,100 שאינו נקוב בשום מטבע | 🔴 **חמור מהרשום.** יישור סמל ⛔ אינו מתקן סכום |
| 🔴 **F4** | **הפגם יורה גם כש-`fxOk === true`.** נמדד S7/S8: עסקה אחת שההמרה שלה סורבה (נייר לא-מאומת · אין קיבוע ליום) חוזרת מ-`makeConvertingCalc` **גולמית** (`useFxRates.js:245-247`) ונכנסת ל-`totalPnL`. `currentEquity` = **20,300** במקום **20,200** — **₪100 עודף, שקט** | ⛔ **אינו מוגבל למצב "אין שער".** משטח רחב מהרשום ב-`BACKLOG` |
| 🔴 **F5** | **במצב החמישי ⛔ אין שום גילוי.** נמדד: `mixedCurrency === false`, `currencies === ["USD"]` ⇒ `MixedCurrencyBanner` (`:4144`) ⛔ **אינו נרנדר**. הצד ה**חי** באותה כותרת מסרב **ומונה** (`unconvertedCount`, תג עם מספר, `:4177`); הצד ה**סגור** מחבר בשקט | 🔴 **כשל שקט** (`CLAUDE.md` §2), ו**מנה בלי מכנה** — הבאנר, כשהוא כן יורה, ⛔ אינו נושא מונה |
| ⚠️ **F6** | **אותו מספר, שני סמלים.** נמדד S8: הכותרת `fmtBalance(curEquity, dispCcy)` ⇒ **₪20,300.00**; ה-PDF מקבל את **אותו** `curEquity` עם `statsCcy(stats)` (`:7242`) ⇒ **$20,300.00** | זהו בדיוק הפגם ש-`AUDIT-B119` §3 רשם כדורש מזהה משלו — **עכשיו נמדד בערך** |

---

## 1. מפת כל אתר שמדפיס את `stats.currentEquity` או נגזרת שלו

**האוכלוסייה סגורה.** `currentEquity` נצרך ב**מקום אחד בלבד** בקוד האפליקציה
(`:2366`), ומשם הכול נגזר מ-`curEquity`.

```
src/lib/tradingStats.js:224   currentEquity: capital + totalPnL     ← המקור
SwingEdge_App.jsx:2366        curEquity = stats.currentEquity + openPnL.value
```

| # | אתר | שורה | מה מודפס | הסמל שמודבק | המטבע בפועל |
|---|-----|------|-----------|--------------|--------------|
| **A** | סרגל עליון | `:3920` | `fmtBalance(curEquity, dispCcy)` | **`dispCcy`** | ⚠️ מעורב (F3/F4) |
| **B** | `StatCard` הון | `:4156` | `fmtBalance(curEquity, dispCcy)` + `trend={curEquityReturnPct}` | **`dispCcy`** | ⚠️ כנ״ל |
| **C** | פוטר | `:7955` | `fmtBalance(curEquity, dispCcy)` | **`dispCcy`** | ⚠️ כנ״ל |
| **D** | קריאת PDF | `:7242` | `exportMonthlyPDF(…, curEquity, statsCcy(stats), …)` | 🔴 **`statsCcy`** | ⚠️ כנ״ל |
| **E** | גוף ה-PDF | `:470` → `:574` · `:625` | `sym + curEquity.toLocaleString()` | יורש מ-**D** | ⚠️ כנ״ל |
| **F** | `curEquityReturnPct` | `:2382-2385` | `(curEquity − equityBase) / equityBase` | — | 🔴 מונה מורעל |
| **G** | `sub` בכרטיס ההון | `:4156` | `dispSym + equityBase` | **`dispCcy`** | ⚠️ `equityBase` עלול להיות `capitalCurrency` |

🔴 **`D` ⛔ אינו מסכים עם `A`/`B`/`C`.** ארבעת האתרים נושאים את **אותו מספר** בדיוק —
זו הייתה מטרת `B-119` והיא הושגה — אך `D` מדביק עליו **סמל אחר**. נמדד S8: ₪ מול $.

### אתרי `statsCcy` שאינם `curEquity` — ⛔ מחוץ להיקף, אך באותו פגם

`:4160` (`netPnlClosed`) · `:4163` (`dailyPnl`) · `:4236` · `:4253` · `:4712` ·
`:4716` · `:4734` · `:4834` · `:5731` · `:5771` · `:5781-5836` · `:5922` · `:5976` ·
`:5993` · `:6121-6149` · `:6255` · `:6297-6309` · `:7302` · `:7321`.
**28 אתרים.** כולם מדפיסים `statsCcy` בעוד הכותרת שלידם מדפיסה `dispCcy`.

---

## 2. מה בדיוק קורה כשההמרה נכשלת — נמדד, ⛔ לא הונח

### 2.1 שלושה מסלולי כשל **נפרדים**, שלוש התנהגויות **שונות**

| מסלול | הפונקציה | מה חוזר | באיזה מטבע | מי מדביק סמל |
|--------|-----------|----------|-------------|---------------|
| **בסיס ההון** | `resolveEquityBase` (`equityBase.js:80-81`) | `cap` **בלתי-נגוע** | 🔴 `capitalCurrency` | `dispCcy` / `statsCcy` |
| **P&L סגור** | `makeConvertingCalc` (`useFxRates.js:245-247`) | `m` **גולמי**, בתווית שלו | 🔴 מטבע ה**נייר** | כנ״ל |
| **P&L חי** | `livePnlAmount` (`useFxRates.js:218-228`) | ✅ `{ok:false, value:null}` | — | ⛔ **אינו נכנס לסכום** |

⚠️ **המסלול השלישי הוא היחיד שמסרב.** זה מה ש-`B-119` תיקן, ⛔ וזה בדיוק הפער.
שני הראשונים מחזירים **מספר** — ומספר, בניגוד ל-`null`, נכנס לחיבור בלי להתנגד.

### 2.2 המדידה — S5, המצב החמישי (הון ₪ · חשבון $ · אין שער)

```
equityBase      = 10000.00   denom: RAW ILS      ← לא הומר, לא מוצהר
totalPnL        =   100.00   pnlByCurrency={"USD":100}
currentEquity   = 10100.00   ← ₪10,000 + $100
openPnL         =     0.00   live.ok=false reason=fx_fallback   ✅ מסרב נכון
curEquity       = 10100.00
PRINTED AS      dispCcy=ILS  |  statsCcy=USD  |  fxOk=false
mixedCurrency   = false      ⇒ MixedCurrencyBanner ⛔ אינו נרנדר
```

⇒ הכותרת מציגה **₪10,100.00**. ⛔ אין תג, ⛔ אין באנר, ⛔ אין `—`.
**זהו המספר היחיד במוצר שמחבר שני מטבעות ⛔ בלי לומר זאת.**

### 2.3 המדידה — S7/S8, `fxOk === true` (F4)

```
S7 נייר לא-מאומת:   equityBase=20000 (ILS) + totalPnL=300  ⇒ currentEquity=20300
                    חיבור כן: 20000 + 200 = 20200        ⇒ 🔴 עודף ₪100
                    pnlByCurrency={} currencies=[] mixed=true   ⇒ באנר **כן** יורה
                    statsCcy=ILS(fallback) = dispCcy=ILS        ⇒ הסמלים מסכימים

S8 אין קיבוע ליום:  currentEquity=20300 (אותו עודף ₪100)
                    pnlByCurrency={"USD":100} currencies=["USD"] mixed=true
                    statsCcy=USD  ≠  dispCcy=ILS        ⇒ 🔴 ₪20,300 בכותרת · $20,300 ב-PDF
```

⚠️ **S6 (הון $ · חשבון ₪ · אין שער) ⛔ אינו יורה** — נמדד: `equityBase` נשאר RAW USD
ו-`totalPnL` הוא USD ⇒ **עקבי פנימית**, ו-`dispCcy=USD` ⇒ הסמל נכון.
🔴 **האסימטריה היא הממצא:** הפגם דורש `capitalCurrency !== PAPER_BASE`.

---

## 3. איפה היישור — ומה עולה כל דרך

### ⛔ דרך א׳ — `src/lib/tradingStats.js` (המקור). **נפסלת.**

ההאב הוא **פונקציה טהורה** שמקבלת `(trades, capital, calcTradeMetrics)`. הוא
⛔ **אינו יודע** מהו `accountCurrency`, מהו `dispCcy`, ומהי `fxTable`. ההערה
ב-`:221-223` אוסרת זאת מפורשות: *"open P&L needs live prices, which are component
state and must not enter this pure stats hub."*

**עלות:** הזרמת הקשר-מטבע ל-4 אתרי קריאה (`stats` · `menteeStats` · `monthStats` ·
`journalStats`, `:2155`/`:2162`/`:2176`/`:2256`) + `scripts/fixtures/tradingstats-baseline.json`
(8 מופעי `currentEquity` קפואים) + `test:tradingstats`. **רוחב-נגיעה: גדול, וסותר ארכיטקטורה מוצהרת.**

### ✅ דרך ב׳ — תפר ההכרעה בצרכן, בתבנית `livePnlAmount`. **מומלצת.**

`B-119` כבר הוכיח את התבנית: ההכרעה חיה כ**פונקציה טהורה ב-`useFxRates.js`**
(ניתנת לאסרציית **ערך** ב-node), והצרכן קורא לה. שני תת-פגמים ⇒ שתי הכרעות:

| תת-פגם | היום | הכיוון |
|---------|-------|---------|
| בסיס ההון לא-מומר בשקט | `resolveEquityBase` מחזיר `number` | להחזיר **הכרעה** (`{ok, reason, value, currency}`) שהצרכן יכול להציג |
| P&L סגור לא-מומר בשקט | `makeConvertingCalc` מחזיר `m` גולמי | **למנות** את הסירובים, כמו `openPnL.unconvertedCount` |

⚠️ **שאלה פתוחה לתוכנית, ⛔ לא לאבחון:** האם המנייה יושבת ב-`makeConvertingCalc`
(שמסמן `m`) ונאספת ב-`computeTradingStats`, או שהצרכן מריץ מעבר שני. הראשונה
נוגעת בהאב הטהור אך **בלי** הקשר-מטבע; השנייה כופלת מעבר על כל העסקאות.
**⛔ אינה מוכרעת כאן.**

### ⚠️ דרך ג׳ — אתרי ההדפסה בלבד (יישור `statsCcy`←`dispCcy`, כפי ש-`NEXT` #1 מציע)

🔴 **⛔ אינה מתקנת את הפגם.** F3/F4 מוכיחים שהמספר עצמו שגוי (₪100 עודף,
חיבור ₪+$). יישור סמלים היה הופך שני מספרים שגויים לעקביים זה עם זה —
**וזה גרוע יותר**, כי הוא מסלק את הסתירה הגלויה שהיא הרמז היחיד.
⚠️ **הצעת ה-`NEXT` נמדדה ונמצאה לא-מספקת.**

---

## 4. אסרציות שמקבעות את ההתנהגות הנוכחית — רשימה סגורה

| אסרציה | קובץ | מה היא מקבעת | האם התיקון יפיל אותה |
|---------|-------|---------------|------------------------|
| **`13.9b`** | `instrument-currency-test.mjs:470-501` | `accountAmount` מסרב על נייר לא-מאומת ⇒ `"—"` | ⛔ **לא.** ⛔ אינה נוגעת ב-`currentEquity`. **F1** |
| `13.9b′` | `:502-526` | ההמרה בשער יום-המימוש (1750) | ⛔ לא |
| `13.9b″` | `:527-563` | ארבע דחיות מובחנות של `accountAmount` | ⛔ לא |
| **`13.9b‴`** | `:564-594` | 🧊 **המצרף byte-identical** — `makeConvertingCalc` על 5 מצבים, כולל *"מצרף: נייר לא מאומת ⇒ ⛔ לא מומר"* = **500** | ⚠️ **כן, אם דרך ב׳ תשנה את הערך המוחזר.** מנייה בלבד ⛔ לא תפיל |
| `13.9` (מנייה) | `:434-468` | `acctCount(app) === 1` (`riskDollar`) | ⚠️ **כן, אם התיקון יוסיף/יסיר `currencyOf(` ב-app** |
| `A1`–`A8` | `:~770-830` | הפרדת spot/יום-כניסה + `fx_fallback` | ⛔ לא. **`A8` מתעדת את המצב החמישי ומאשרת את F2** |
| `A6` | `:~800` | 🧊 מסלול ה**עבר** ⛔ לא זז (1750) | ⛔ לא, כל עוד `accountAmount` לא נגעה |
| `fx-test.mjs:252` | — | `statsCalm.currentEquity === statsMoved.currentEquity` (spot ⛔ לא מזיז בסיס) | ⚠️ **כן, אם `resolveEquityBase` תשנה חתימה** |
| `tradingstats-test.mjs` | `:110` · `:155` · `:157` · `:408` · `:445` | ערכי `currentEquity` על calc **זהותי** | ⛔ לא בדרך ב׳ · ⚠️ **כן בדרך א׳** |
| `fixtures/tradingstats-baseline.json` | 8 מופעים | קו קפוא מספרי | ⛔ לא בדרך ב׳ · 🔴 **כן בדרך א׳** |

🔴 **מסקנה — F1:** ⛔ **אין היום שום אסרציה שמקבעת את הפגם של `B-142`.**
ההתנהגות שגויה ו**בלתי-מכוסה** כאחד. ⇒ התוכנית ⛔ אינה יכולה לכלול "הסרת 13.9b";
היא **חייבת** לכלול אסרציות **חדשות** שייצפו אדומות.

---

## 5. הפרדה — יומן חד-מטבעי מול מעורב/אין-שער

| מצב | `fxOk` | `dispCcy` | הפגם יורה? | נמדד |
|------|--------|-----------|--------------|-------|
| **S1** הון $ · חשבון $ | ✅ | USD | ⛔ **לא** — `identity`, byte-identical | `10,124.75` ✅ |
| **S2** הון ₪ · חשבון ₪ | ✅ | ILS | ⛔ **לא** | `10,274.25` ✅ |
| **S3** הון $ · חשבון ₪ | ✅ | ILS | ⛔ לא (כשכל ההמרות מצליחות) | `20,274.25` ✅ |
| **S4** הון ₪ · חשבון $ | ✅ | USD | ⛔ לא | `5,124.75` ✅ |
| 🔴 **S5** הון ₪ · חשבון $ · **אין שער** | ❌ | ILS | 🔴 **כן** — ₪10,000 + $100, **אפס גילוי** | `10,100.00` |
| **S6** הון $ · חשבון ₪ · **אין שער** | ❌ | USD | ⛔ **לא** — עקבי פנימית | `10,100.00` |
| 🔴 **S7** הון $ · חשבון ₪ · נייר לא-מאומת | ✅ | ILS | 🔴 **כן** — עודף ₪100, באנר יורה בלי מונה | `20,300` |
| 🔴 **S8** הון $ · חשבון ₪ · אין קיבוע ליום | ✅ | ILS | 🔴 **כן** — עודף ₪100 **+** ₪/$ בשני אתרים | `20,300` |

**⛔ אפס חשיפה ליומן חד-מטבעי.** S1 ו-S2 עוברים ב-`identity` ו⛔ אינם נוגעים בשער כלל.
⚠️ **43/46 המשתמשים הדולריים (S1) ⛔ אינם חשופים** — כפי ש-`F2` של `B-119` כבר קבע.
🔴 **החשיפה היא S5 · S7 · S8**, ושתיים מהשלוש קורות ב-`fxOk === true`.

---

## 6. פריטים שאינם `B-142` — דורשים מזהה משלהם (§10.1)

| ממצא | שורה | מדוע ⛔ אינו חלק מהגל |
|-------|------|------------------------|
| **F6 — `statsCcy` מול `dispCcy` ב-28 אתרים.** אותו מספר, שני סמלים | `:1287` · `:7242` + 28 | ⛔ אינו נגרם מההמרה; קיים גם על יומן שכל המרותיו הצליחו. `AUDIT-B119` §3 כבר רשם אותו ⇒ **טרם קיבל מזהה** |
| **`MixedCurrencyBanner` בלי מונה** | `:841-850` | הבאנר אומר "לא סכום" ⛔ ואינו אומר **כמה** שורות — `CLAUDE.md` §2 "אין מנה בלי מכנה" |
| **`curEquityReturnPct` על מונה מורעל** | `:2382-2385` | יורש את F3/F4 ⇒ ייתקן לבד אם המונה ייתקן. ⚠️ לאימות, ⛔ לא לתיקון נפרד |

⇒ שלושתם ייכנסו ל-`docs/BACKLOG.md` בקומיט של סגירת הגל.

---

## 7. מה נמדד מול מה שהונח

| הנחה (בפרומפט / ב-`BACKLOG` / ב-`NEXT`) | פסק | הראיה |
|------------------------------------------|------|--------|
| *"אסרציה `13.9b` היא קו קפוא ⇒ יש להסירה במפורש"* | 🔴 **מופרכת** | `13.9b` הוסרה ב-12.08 (גל ג׳). מה שיושב שם מקבע התנהגות **נכונה** ו⛔ אינו נוגע ב-`currentEquity`. `CLAUDE.md` §7 אומר זאת |
| *"הכותרת הראתה `$10,500.00`"* (מצב חמישי) | 🔴 **מופרכת** | הכותרת מדפיסה `dispCcy` = `capitalCurrency` = **₪**. הערת `A8` בטסט מאשרת. ה-`$` שייך ל-`statsCcy` (PDF) |
| *"יישור `statsCcy` ל-`dispCcy`, כפי שנעשה ל-`openPnL`"* (`NEXT` #1) | 🔴 **לא מספקת** | F3/F4: המספר עצמו שגוי. יישור סמלים מסתיר את הסתירה במקום לתקן את הסכום |
| *"הפגם הוא ב-`stats.currentEquity`"* | ✅ **אוששה** — ומורחבת | ⛔ אינו רק תווית: `currentEquity` הוא **סכום חוצה-יחידות** |
| *"⛔ אינו רגרסיה של `B-119`"* | ✅ **אוששה** | `livePnlAmount` מסרב נכון (`fx_fallback`) בכל המצבים שנמדדו. `openPnL` = 0 ומונה |
| *"נוגע רק במצב אין-שער"* (משתמע) | 🔴 **מופרכת** | S7/S8 יורים ב-`fxOk === true` |
| *"אפס חשיפה ליומן חד-מטבעי"* | ✅ **אוששה** | S1/S2 — `identity`, byte-identical |

---

## 8. ⛔ מה **לא** נמדד — ונשאר פתוח לשלב 2

- **אימות עין.** האבחון כולו סטטי + בדיקן `node`. `C-023` במצב S5/S7/S8 ⛔ **לא צולם**
  בסשן הזה. ⚠️ המספרים לעיל הם מדידת **קוד**, ⛔ לא מדידת **מסך**.
- **כמה משתמשים בפועל ב-S5/S7/S8.** ⛔ לא נשאלה שאילתת DB. `B-119` מדד 43/46 דולריים
  ⇒ המשטח ≤3 משתמשים, ⛔ **אך זה מספר מ-13.08 ולא נמדד מחדש**.
- **התנהגות `menteeStats` / `monthStats` / `journalStats`** — הם צורכים את אותו
  `equityBase` ואת אותו `stableCalcTradeMetrics`, ⇒ **צפוי** שיירשו את הפגם.
  ⛔ **לא נמדד.**

---

**עצירה.** שלב 2 (תוכנית) ⛔ לא החל.
