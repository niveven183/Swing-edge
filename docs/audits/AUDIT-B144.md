# AUDIT-B144 — `pnlByCurrency` · `tradesByCurrency` · `mixedCurrency` נגזרים ממטריקות שאחרי ההמרה

**תאריך:** 2026-08-20 · **HEAD:** `ae01a0d` · **שלב:** 1 מתוך 2 — **אבחון read-only**
**סיווג §15:** `T3` · תשובות: **לא / כן / לא / כן / כן** (3 מתוך 5)

| # | שאלה | תשובה | נימוק |
|---|------|-------|-------|
| 1 | הפיכות | לא | הגזירה רצה בזמן קריאה ולעולם אינה נשמרת. ⛔ אין DB · אין מייל · אין מחיקה |
| 2 | אמון | **כן** | הבאנר מדבר על כסף. באנר שמדפיס `$-200.00 (2)` מעל `totalPnL = ₪100` הוא מספר שגוי שהמשתמש ⛔ אינו יכול לזהות |
| 3 | אבטחה | לא | ⛔ אין `auth` · RLS · secrets · `api/` · קלט לא-מסונן |
| 4 | רוחב | **כן** | `src/lib/tradingStats.js` + `SwingEdge_App.jsx` — מודול שארבעה מופעי hub תלויים בו |
| 5 | ודאות | **כן** | המדידה מ-15.08 בת 5 ימים; `B-143` · `B-162` · `B-166` נסגרו מאז |

⛔ **אפס שינוי קוד בשלב הזה.** הרתמות למדידה נכתבו ב-`/tmp` בלבד (`/tmp/b144-repro.mjs`,
`/tmp/b144-block11.mjs`) ו⛔ אינן נקומטות. `git status` נמדד ריק על `src/` ועל `scripts/`.

---

## 0 · שתי הפרכות של הנחות שבפרומפט עצמו

⚠️ **§8.1 דורש להפריך או לאשש כל הנחה שבפרומפט. שתיים לא עמדו:**

**(א) מספרי השורות.** הפרומפט נוקב ב-`tradingStats.js:53` · `:78-95` · והלולאה ב-`:81-85`.
`:53` **מאושש** (`const metrics = closed.map(...)`). `:78-95` הוא היום בלוק ההערה של
`aggregatable`, ו**הלולאה חיה ב-`:109-119`**, ⛔ לא ב-`:81-85`. ⇒ המספרים נסחפו ב-`B-143`
(116 הוספות). **כל ההפניות למטה נמדדו על `ae01a0d`.**

**(ב) המכנה `48 משתמשים` (D7).** ⛔ **אינו ניתן לאישוש מהרפו.** המדידה החיה
היחידה היא `docs/STATE.md:23` — **46 ב-`auth.users`**, נמדד 11.08–12.08. ⛔ אין
בשום קובץ מספר 48. ⇒ **כל השיעורים למטה נקובים במכנה 46 ותאריך המדידה מוצהר.**
אם 48 הוא מספר טרי שבידי ניב — המכנה זז ו-D7 צריך מדידה חוזרת, ⛔ לא הכללה.

---

## D2 · מה `B-143` שינה בקובץ — ולמה חצי מהפיצול **כבר קיים**

`git diff e5afdc7~1 e5afdc7 -- src/lib/tradingStats.js` ⇒ **116 הוספות · 36 מחיקות.**
נוספו `aggregatable` · `fxAggregatableCount` · `fxUnconvertedCount` · `aggRates`.

🔴 **והמדידה המכריעה: הגל ⛔ לא נגע באף שורה של הלולאה.** סינון ה-diff על
`pnlByCurrency|tradesByCurrency|mixedCurrency|derivations` יצא **ריק** ⇒ אישוש מדוד
להצהרה ב-`DONE` D-043 («⛔ `mixedCurrency` עצמו ⛔ לא נגע»).

⚠️ **התשובה לשאלת ה-`R-6` שבפרומפט: כן — ציר אחד מהשניים כבר קיים, ⛔ ואינו מנגנון שני.**
בקובץ יושבות **שתי חלוקות שונות של אותה אוכלוסייה**, ורק אחת מהן מיושמת:

| ציר | הביטוי הקיים | מה הוא עונה |
|-----|---------------|--------------|
| **יחידת התצוגה** | `metrics.filter(m => !m.fxUnconverted)` (`:86`) | "האם המספר הזה נקוב ב-`dispCcy`" |
| **תווית הנייר** | `deriveInstrumentCurrency(m)` (`:109`) | "באיזה מטבע נסחר הנייר" |

⇒ ציר יחידת-התצוגה **קיים ונבדק** (`aggregatable`), והוא בדיוק הציר ש-`pnlByCurrency`
צריך. ⛔ **אל תיבנה חלוקה שלישית.** הפגם הוא שהלולאה ב-`:112-117` מקבצת **כסף שעבר
בציר הראשון** לפי **מפתח שנגזר מהציר השני**.

🔴 **וחסם מדוד על כל תיקון: `computeTradingStats(trades, capital, calcTradeMetrics)`
⛔ אינו מקבל `dispCcy`.** שלושה פרמטרים, נמדד ב-`:42`. יחידת התצוגה ⛔ אינה בהישג ידו
של ה-hub אלא דרך שדה `currency` של איבר **מומר** — וביומן שכולו `identity` ⛔ אין
איבר כזה. ⇒ זו הכרעת עיצוב לניב (§6 למטה), ⛔ לא פרט מימוש.

---

## D1 · הצרכנים — מי צורך כל תוצר, ומה הוא **צריך**

**נמדד בגריפ על כל הרפו** (⚠️ קובץ האפליקציה יושב ב**שורש** `SwingEdge_App.jsx`,
⛔ לא ב-`src/` — גריפ על `src/` לבדו מחזיר 0 צרכנים ומטעה):

| תוצר | צרכן קוד-מוצר | מה הצרכן **צריך** | היום קורא |
|------|----------------|---------------------|-----------|
| `mixedCurrency` | `SwingEdge_App.jsx:889` — שער הרינדור | האם להזהיר | **מעורב** — נדלק גם על יחידה אחת |
| `currencies` | `:906` — הציר שעליו רוכב הפילוח | ⚠️ תלוי בהכרעה (§6) | תווית נייר של המומרים בלבד |
| `pnlByCurrency` | `:907` — **כסף** דרך `fmt$(…, c)` | **יחידת תצוגה** — הסמל מודפס מהמפתח | תווית נייר ⇒ **סמל שגוי** |
| `tradesByCurrency` | `:907` — המכנה בסוגריים | **תווית נייר** (§2 · `B-142`) | תווית נייר, ⛔ אך על אוכלוסייה חסרה |

🔴 **צרכן פונה-משתמש אחד בדיוק — `MixedCurrencyBanner` (`:884-913`), ו⛔ אין שני.**
⛔ אפס צרכנים ב-`src/components/` · ⛔ אפס ב-`api/` · ⛔ אפס ב-`scripts/` פרט לטסטים.
מרונדר ב-**3 אתרים**: `:4297` (לוח) · `:4790` (יומן) · `:5893`.

⚠️ **אישוש למה ש-`B-143` מחק (הפרומפט ביקש):** `statsCcy` ⛔ **אינו קיים בקוד** —
0 מופעים. `stats.currencies` נותר עם **צרכן אחד** (`:906`) ו⛔ אינו מניע יותר שום
סמל כותרת, בדיוק כפי ש-`PLAN-B143` §10.5 חזה. ⇒ **⛔ אין יותר התכנסות לשבור.**

🔴 **ו-4/4 מופעי ה-hub בפרודקשן מוזנים ב-calc הממיר:** `stats` (`:2264`) ·
`menteeStats` (`:2271`) · `monthStats` (`:2285`) · `journalStats` (`:2365`) — כולם
`stableCalcTradeMetrics` = `makeConvertingCalc(paperAcctTable, dispCcy, paperAcctStatus)`
(`:2202-2204`). ⇒ **המסלול השבור הוא המסלול היחיד שקיים בפרודקשן.**

---

## D5 · המנגנון — `deriveInstrumentCurrency` על הגולמי מול המומר

**התפר, נמדד ב-`src/hooks/useFxRates.js:268-269`:**

```js
if (d.reason !== "converted") return d.ok ? m : { ...m, fxUnconverted: d.reason };
return { ...m, pnl: d.value, currency: d.currency };
```

⇒ **התיוג מחדש קורה ב-`converted` בלבד.** `identity` מחזיר `m` כפי שהוא · סירוב מוסיף
שדה בלבד. ⛔ אין תיוג מחדש בשני המסלולים האחרונים.

**המדידה (`/tmp/b144-repro.mjs`, יומן 2 ניירות USD, תצוגת ILS, שער לשני הימים):**

| נייר | `currency` בגולמי | הגזירה | `currency` במומר | הגזירה |
|------|--------------------|--------|-------------------|--------|
| `AAPL` | `"USD"` | `USD` · `assumed` · `no_evidence_against` | **`"ILS"`** | **`null` · `contradicted` · `ils_never_measured`** |
| `MSFT` | `"USD"` | `USD` · `assumed` · `no_evidence_against` | **`"ILS"`** | **`null` · `contradicted` · `ils_never_measured`** |

🔴 **זו התשובה המדויקת לשאלה שבפרומפט — «מדוד מה הוא *כן* מבחין»:**
`deriveInstrumentCurrency` ⛔ **אינו** מחזיר `ILS` על טיקר אלפביתי — נכון, ההנחה
ההפוכה הופרכה ב-`D-040`. ⚠️ **אבל הוא מחזיר `null`/`contradicted`**, כי ענף 4
(`instrumentCurrency.js:182`) קורא `trade.currency === "ILS"` כ**ראיה נגד עצמה**.
הענף הזה נכתב כדי לתפוס תווית שנחתמה מהעדפת החשבון — ו-`makeConvertingCalc`
כותב **בדיוק את אותה מחרוזת** מסיבה אחרת לגמרי. ⇒ **הגזירה ⛔ אינה "טועה במטבע";
היא נופלת ל-`isAggregatable === false` ⇒ האיבר נשמט מהלולאה כליל** (`:113`).

⇒ **הפגם ⛔ אינו "מפתח שגוי". הוא אוכלוסייה שנשמטת בשקט, ובאותה נשימה מדליקה
את הבאנר** (`isMixedCurrency` סופר `hasUnverified`, `instrumentCurrency.js:231`).

---

## D3 · 🔴 הכשל **משתחזר** על `ae01a0d` — ובמצב אחד הוא **חמור מהמתועד**

`/tmp/b144-repro.mjs`. יומן: 2 ניירות USD, `+$200` (10.02) · `+$100` (11.02). הון 40,000.

| מצב | `pnlByCurrency` | `tradesByCurrency` | `mixed` | `totalPnL` | **הבאנר בפועל** |
|------|------------------|--------------------|---------|------------|------------------|
| **A** · calc גולמי | `{USD:300}` | `{USD:2}` | `false` | 300 | *(מוסתר)* ✅ |
| **B** · ILS, שער ליום אחד ⇒ 1 מומר + 1 מסורב | `{USD:100}` | `{USD:1}` | `true` | **740** | **«$100.00 (1)»** 🔴 |
| **C** · ILS, שער לשני הימים ⇒ **הכול מומר** | **`{}`** | **`{}`** | **`true`** | **1110** | **«»** 🔴🔴 |
| **D** · תצוגת USD (`identity`) | `{USD:300}` | `{USD:2}` | `false` | 300 | *(מוסתר)* ✅ |

✅ **מצב B הוא המדידה מ-15.08, תו בתו** — «יומן של 2 ניירות USD הציג באנר
`+$100.0…`». ⛔ **`B-143` לא סגר אותו.** נמדד היום, ⛔ לא הונח.

🔴 **מצב C הוא ממצא חדש ו⛔ אינו ב-`PLAN-B143` §10.2** — שהציג את `B` בלבד.
כשההמרה **מצליחה לגמרי** (המצב ה**תקין** של משתמש ILS עם טבלת שערים עובדת),
**כל** האיברים נופלים ל-`contradicted` ⇒ הפילוח **ריק**, `currencies === []`,
ו-`mixedCurrency === true`. ⇒ **הבאנר נדלק על יומן חד-מטבעי לחלוטין, ומדפיס
שורת כסף ריקה** (`[].join("  ·  ") === ""`). ⚠️ **וזה ⛔ אינו תלוי-סירוב** —
בדיוק כפי ש-`PLAN-B143` §10.2 חזה על התיקון הנאיבי, רק שכאן זה מצב ה**קיים**.

⚠️ **מצב D הוא ההוכחה שלאבחנה יש שיניים** — תצוגת USD (`identity`, ⛔ בלי תיוג
מחדש) יוצאת **נכונה**. ⇒ הפגם תלוי אך ורק ב-`dispCcy !== מטבע הנייר`.

---

## D6 · האסרציות — 🔴 `R-3` בצורתו הטהורה

**נמדד: 4 בלוקים נוגעים בשלושת התוצרים, ו-⛔ אף אחד מהם ⛔ אינו מזין calc ממיר.**

| מקום | מזין | נוגע בתוצרים | יתהפך? |
|------|-------|---------------|---------|
| `tradingstats-test.mjs` §9 (`:645-692`) | `calcTradeMetrics` **גולמי** | ✅ 11 אסרציות ערך | ⛔ **לא** |
| `instrument-currency-test.mjs` §5 (`:176-192`) | `calcTradeMetrics` **גולמי** | ✅ 3 אסרציות | ⛔ **לא** |
| `instrument-currency-test.mjs` `:273-275` | קריאת **מקור** | `pnlByCurrency` בלי `currencyOf` | ⛔ **לא** |
| §10 — הבייסליין הקפוא (8 תרחישים) | `calcTradeMetrics` **גולמי ב-8/8** | ✅ בכל 8 | ⛔ **לא** |
| `tradingstats-test.mjs` §11 (`:913-1039`) | 🔴 **`makeConvertingCalc(TABLE,"ILS","ready")`** | ⛔ **אפס** | — |

🔴 **הממצא: §11 הוא הבלוק **היחיד** שמזין calc ממיר ל-hub — ו⛔ אינו אומר מילה
על ארבעת התוצרים.** מדדתי מה הפיקסצ'ר שלו **כן** מייצר (`/tmp/b144-block11.mjs`):

```
pnlByCurrency    : {"USD":-200}      ← בעוד totalPnL = ₪100
tradesByCurrency : {"USD":2}
currencies       : ["USD"]
mixedCurrency    : true
BANNER would render: «$-200.00 (2)»
```

⇒ **מעל 30 אסרציות ירוקות באותו בלוק בדיוק, על אותו אובייקט `stats` בדיוק, בעוד
הבאנר היה מדפיס `$-200.00 (2)` מעל סכום של `₪100`.** שני האיברים בסל ה-`$` הם
דווקא ה**מסורבים** (−300 ו-+100 דולר); שני ה**מומרים** (₪200 · ₪−100) ⛔ אינם
בשום סל. **הפגם חי מתחת לשער ירוק כי האסרציות בודקות מסלול שבו הוא ⛔ אינו
יכול להתבטא** — `R-3`, ובדיוק אותה צורה שנתפסה ב-`D-044` (fixture 11).

✅ **מה ⛔ קפוא ו⛔ אינו חוסם — נמדד:** הבייסליין הקפוא (`v7`, 8 תרחישים) בנוי
`calcTradeMetrics` **גולמי ב-8/8** ⇒ **תיקון שמשמר את התנהגות ה-calc הגולמי ⛔
אינו מזיז את הבייסליין.** ⚠️ נאמר כמדידה, ⛔ לא כהיתר: `13.9b` · `13.9b‴` ·
בלוק 14 (`A1`–`A8`) יושבים ב-`test:instrument` ונוגעים ב-`accountAmount`/spot,
⛔ לא בלולאה הזו — ואין להם קשר לתיקון כל עוד `useFxRates.js:268-269` ⛔ לא נוגע.

---

## D4 · העותק פונה-המשתמש — מה מכריז, ומה נכון

**המצב אחרי `B-143` (5 שפות · `en:291-292` · `he:1001-1002` · `es:1707-1708` ·
`pt:2168-2169` · `ar:2632-2633`):**

- **כותרת:** «This journal holds more than one currency»
- **גוף:** «Totals below include only the trades we could convert to your display
  currency. Any we could not are left out and counted, never guessed. Per-trade
  figures are correct.»

**ההכרעה המדודה — הגוף שרד את `B-143`, ⛔ הכותרת לא:**

| | טענה | מול מצב C (הכול מומר) |
|---|-------|------------------------|
| **גוף** | «הסכומים כוללים רק את מה שהומר · מה שלא — מוחרג ונספר» | ✅ **נכון** — 0 הוחרגו, הטענה ריקה ⛔ אך ⛔ לא שקרית |
| **כותרת** | «ביומן הזה יש יותר ממטבע אחד» | 🔴 **שקר** — יומן חד-מטבעי, יחידה אחת |

⇒ **התשובה לשאלה שבפרומפט: הגוף שנכתב מחדש ב-`B-143` ⛔ אינו צריך לזוז.
ה**כותרת** היא הטענה השבורה, והיא ⛔ לא נגעה בגל הקודם.** ⚠️ **וההיקף תלוי
בהכרעה 3 של §6** — אם `mixedCurrency` יוגדר מחדש כ"מוכח חד-יחידה" אז הכותרת
נשארת נכונה **בהגדרה** ו⛔ אין נגיעה ב-i18n כלל; אם היא תמשיך לירות על
"יש מה שלא הומר" — הכותרת חייבת להיכתב מחדש ב-**5 שפות** + אסרציה שתופסת את
הנוסח הישן **בכל שפה בנפרד** (הצורה שנבנתה ב-`D-043`,
`instrument-currency-test.mjs:320-337`). ⛔ **אני ⛔ מכריע.**

---

## D7 · ההשלכה — במספרים, עם מכנה ותאריך

**התנאי המדוד לפגם** (ממצב D למול B/C): נדרש **לפחות איבר אחד `converted`**
⇒ `dispCcy ≠ מטבע הנייר`. וכיוון ש-`PAPER_BASE === "USD"` ו-`MEASURED ILS`
נמדד **0/61**, כל נייר מאומת נגזר ל-`USD` ⇒ **התנאי הוא `dispCcy === "ILS"`**.
`dispCcy = fxOk ? accountCurrency : capitalCurrency` (`SwingEdge_App.jsx:2136`).

| מדידה | מספר | מקור · תאריך |
|--------|-------|---------------|
| משתמשים ב-`auth.users` | **46** | `STATE.md:23` · 11.08–12.08 |
| עם עסקאות בכלל | **13/46** | `STATE.md:23` · 11.08–12.08 |
| `capitalCurrency = 'ILS'` | **3/46** | `PLAN-…-capital-engine:15` (3/32) · `AUDIT-B142:179` (43/46 דולריים) |
| מתוכם **מציגים** ב-ILS | **1/3** — 2/3 מציגים ב-USD | `AUDIT-2026-08-09-capital-fx.md:128` |
| מתוך 3 ה-ILS — כמה עם עסקאות | **0/3** | `AUDIT-2026-08-09-capital-fx.md:400` · 09.08 |

⇒ **הפסק המדוד: `0` משתמשים רואים את הפגם היום — הוא **רדום**, ⛔ לא פעיל.**
43/46 הדולריים יוצאים במצב D (נכון · `identity`) · 2/3 השקליים מציגים ב-USD
⇒ גם הם מצב D · ו-1/3 שנותר ⛔ אין לו עסקאות.

⚠️ **ושלוש הסתייגויות שחייבות להיאמר, ⛔ ולא להוחלק:**

1. 🔴 **`0/3` בן 11 יום** (09.08), והמכנה `46` בן 8 (12.08). ⛔ **לא שאלתי את
   ה-DB** — שלב 1 הוא read-only ו§12 קובע שניב מריץ. השאילתה שמכריעה:
   `select capital_currency, account_currency, count(*) from user_settings group by 1,2;`
   בצירוף `count(*)` על `trades` פר משתמש. **פריט אחד שנרשם ואינו מונח.**
2. ⚠️ **הפגם ⛔ אינו תלוי-נפח: משתמש ILS **אחד** עם עסקה **אחת** רואה אותו
   מיד ובאופן קבוע** (מצב C — טבלת שערים תקינה ⇒ באנר קבוע עם שורת כסף ריקה).
   ⇒ המדידה `0` היא **מקריות של אימוץ**, ⛔ לא הגנה. `T3` נשאר `T3`.
3. 🔴 **וההשלכה הכי חשובה לשלב 2 — אימות העין חוסם:** `T3` דורש אימות
   עין/דפדפן (§15), ואימות הבאנר דורש יומן ILS מומר של משתמש **מאומת**
   בפרודקשן. **⛔ אין בידי Code ליצור אותו** — זה **בדיוק** החסם שהשאיר את
   `C-023` פתוחה ואת `D-043` באימות חלקי. ⇒ **הכרעה 4 בשלב 2 (§6).**

---

## §6 · ארבע הכרעות שאני ⛔ מכריע — לניב, לפני שורת קוד

⚠️ **§8.1: «ביצוע נכתב מהממצאים המדודים ו⛔ לא מההשערות».** ארבע הכרעות
⛔ אינן נגזרות מהמדידה ולכן ⛔ אינן שלי:

**1 · איך `pnlByCurrency` יידע את יחידת התצוגה.** ה-hub ⛔ אינו מקבל `dispCcy`
(D2). שתי דרכים: **(א)** פרמטר רביעי ל-`computeTradingStats` — מפורש, ⛔ אך נוגע
בחתימה שכל 4 מופעי ה-hub + `useTradingStats` + הטסטים קוראים · **(ב)** גזירה
מ-`currency` של איבר מומר — ⛔ אפס שינוי חתימה, ⛔ אך ⛔ אינו קיים ביומן
שכולו `identity`. ⚠️ **(ב) הוא ניחוש בתחפושת במצב הקצה.** נטייתי ל-(א).

**2 · על איזה ציר רוכב `currencies` (`:906`).** הוא הציר שהבאנר מפלח לפיו,
ו-`pnlByCurrency` ו-`tradesByCurrency` יימצאו על **צירים שונים** אחרי הפיצול
⇒ מפתח אחד ⛔ אינו יכול לשרת את שניהם. ⇒ **הבאנר צריך שתי שורות, ⛔ לא אחת?**
זו הכרעת **מוצר**, ⛔ לא ריפקטור.

**3 · מה `mixedCurrency` מכריז.** "יש יותר מיחידת-תצוגה אחת" (⇒ ⛔ לא נדלק
במצב C, והכותרת נשארת נכונה, ⛔ אפס נגיעה ב-i18n) **או** "יש נייר שמטבעו לא
אומת" (⇒ הכותרת נכתבת מחדש ב-5 שפות). ⚠️ **זו ההכרעה שקובעת אם `B-144` נוגע
ב-i18n בכלל** — ולכן קובעת את גודלו.

**4 · איך שלב 2 מאומת, בהינתן ש-D7.3 חוסם עין בפרודקשן.** שלוש דרכים:
**(א)** `T3` בלי מסך מושפע ⇒ **`אומת-מקור`** (§15) — קריאת התוצר מ-`origin`
+ מדידה חוזרת עצמאית · **(ב)** ניב יוצר יומן ILS זמני בפרודקשן ומצלם ·
**(ג)** אימות עין על `localhost` עם יומן מוזרק. ⚠️ **⛔ אין לסגור את הגל
ב-«אסרציות ירוקות» לבד — זה בדיוק `D-011`** (105 אסרציות ירוקות מעל מסך שגוי).

---

## §7 · ניתוח השלכות §8 — הפילטר תפס

| ציר | מדידה |
|-----|--------|
| משתמשים | **0 היום** (D7) · **1/1 מיד** ברגע שמשתמש ILS יפתח עסקה |
| נתונים | ⛔ **אפס.** הגזירה בזמן קריאה, ⛔ לעולם אינה נשמרת. ⛔ אין מיגרציה |
| עלות | ⛔ אפס — ⛔ אין רשת · ⛔ אין מעבר שני (הגזירה כבר רצה) |
| תקרות ספק | ⛔ לא נוגע |
| אבטחה | ⛔ לא נוגע |
| תחזוקה §3 | ✅ **מקטין** — מסיר באנר-רעש קבוע ממשתמשי ILS |
| הפיכות | ✅ מלאה — `git revert` על קובץ אחד או שניים |
| כשל שקט | 🔴 **זה עצם הפריט.** הדרך שהוא נכשל בשקט **היום** היא D6: 30 אסרציות ירוקות על אותו אובייקט. ⇒ **תנאי סגירה: אסרציה שנצפתה אדומה על שלושת המצבים B · C · D** — ⛔ לא רק על B |

**פסק דין: ⚠️ בצע עם הגנה** — בכפוף לארבע ההכרעות של §6.

---

## §8 · סריקה סביבתית §11 — ממצא שדווח ו⛔ לא תוקן

⚠️ **`instrument-currency-test.mjs:1352`** מאמת נוכחות של
`/mixedCurrencyCounts|tradesByCurrency/` **בקוד המקור** של `SwingEdge_App.jsx`.
`mixedCurrencyCounts` נמדד **1/1 מופעים בריפו כולו — בתוך ה-regex עצמו**
(`grep -rn` על `*.js`·`*.jsx`·`*.mjs`, ללא `node_modules`) ⇒ החלופה הראשונה
⛔ **אינה יכולה להתאים לעולם**, והאסרציה נישאת כולה ע"י `tradesByCurrency`.

🔴 **והממצא חד יותר ממה שנרשם תחילה: זה אותו פלט שהאבחון הזה הוכיח כשבור.**
במצב **C** (המרה **מצליחה** — משתמש ₪ עם שער לכל יום) `tradesByCurrency === {}`
⇒ הבאנר יורה עם שורת-כסף **ריקה**, בעוד האסרציה נשארת **ירוקה** כי המזהה
**מופיע** בטקסט המקור של `SwingEdge_App.jsx`. ⇒ `R-3` בצורתו הטהורה, ועל השער
**היחיד** שמכסה את הבאנר — כלומר השער הזה הוא חלק מהסיבה שהפגם חי 5 ימים.

⛔ **לא תוקן** — ⛔ אינו המשימה, ⛔ ואינו עומד בשלושת תנאי §11 (אינו "קטן ובטוח"
בתוך אבחון read-only). ✅ **נרשם `B-170` ב-`docs/BACKLOG.md` (⚪ חוב, `‹R-3›`)
באותו קומיט** — §10.1. ⚠️ **והוא נעול לשלב 2:** תיקון שיסתמך על האסרציה הזו
יסתמך על שער שכבר הוכח עיוור ⇒ שלב 2 **חייב** להחליפה בבדיקת **ערך** (מונה מדוד
על יומן ממומר), ⛔ לא בבדיקת נוכחות מזהה.

---

## §9 · מה נמדד ומה ⛔ לא — הצהרה

✅ **נמדד:** הלולאה ומיקומה · diff של `B-143` · 4/4 מופעי hub · 3/3 אתרי רינדור
· התפר ב-`useFxRates:268-269` · הגזירה גולמי↔מומר · 4 מצבי שחזור · הפילוח של
פיקסצ'ר §11 · 5 קבצי אסרציות · 5 שפות i18n.

⛔ **⛔ לא נמדד, ומוצהר:**
1. **ה-DB.** ⛔ אפס `SELECT`. כל מספרי D7 מהרפו, עם תאריכיהם. **המספר `48` שבפרומפט
   ⛔ לא אושש.**
2. **הדפדפן.** ⛔ המסך לא נפתח. ארבעת המצבים נמדדו ב-node על אותו `computeTradingStats`
   שהאפליקציה מייבאת, ושורת הבאנר **חושבה ידנית מ-`:906-908`** — ⛔ אינה צילום.
3. **`menteeStats` · `monthStats`.** נמדד שהם מוזנים באותו calc; ⛔ לא נמדד מה
   הפגם עושה **בהם** בנפרד.

---

**Close:** `npm run test:registry` · push · **עצירה לאישור.** ⛔ אפס נגיעה ב-`NEXT`
עד שהגל ייסגר. ⛔ אפס נגיעה ב-`aggregatable`/`aggRates`.
