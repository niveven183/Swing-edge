# AUDIT B-162 — שלב 1, אבחון read-only

**תאריך:** 19.08 · **HEAD בזמן המדידה:** `bf7d6de` · **סיווג §15:** `T3` · תשובות **לא / כן / לא / כן / כן** (3/5)
— (1) הפיכות: לא · (2) **אמון: כן** — זה השער שמכריע אם שורת `T3` נחשבת מאומתת · (3) אבטחה: לא ·
(4) **רוחב: כן** (ספק ⇒ כן) — שער חוסם שכל שבעת קבצי המרשם תלויים בו · (5) **ודאות: כן** — הנחות הפרומפט לא נמדדו.

⛔ **אפס שינוי קוד בשלב הזה.** ⛔ אפס נגיעה ב-`docs/DONE.md`. ⛔ אפס ריכוך.

---

## 0 · השיטה — ולמה בלעדיה כל מספר כאן חסר-ערך

המדידה ⛔ **אינה** מקלידה מחדש את הרגקסים או את המסווג. Harness ב-`/tmp` (⛔ מחוץ לריפו)
**חולץ טקסטואלית** את שורות המקור `56` · `96` · `444`–`446` · `454`–`456` · `483`–`499`
מ-`scripts/registry-test.mjs` ומריץ אותן כמודול-נתונים.

**הוכחת נאמנות:** `markerVerdict` המחולץ ≡ `markerVerdict` המיובא מהמודול האמיתי על
**11/11** שורות ה-`T3` החיות — **0 הפרשים**. בלי השוויון הזה, כל טבלה למטה מודדת קוד אחר.

**מכנים מוצהרים:** `1,551` תאים מ-4 קבצי מרשם · `43` שורות `DONE` · **`11`** שורות `T3` לא-רטרו ·
`11` fixtures ב-`11.3`. השער כרגע **ירוק 29/29**.

---

## D1 · `DENIED` (:445) מול `DENY_NEAR` (:454)

```
DENIED    = לא\**\s*\**\s*אומת(\s+בעין|-מקור)
DENY_NEAR = (לא\s*\**\s*אומת|לא\s*\**\s*בוצע|טרם\s*\**\s*בוצע|טרם\s*\**\s*הופעל|פתוחה|ממתינה|אינו בידי|אינה בידי)
```

**אמפירית — `3` התאמות `DENIED` בכל הקורפוס (מכנה `1,551` תאים):**

| מקום | היסט | מחרוזת | `DENY_NEAR` על אותה מחרוזת |
|------|------|---------|----------------------------|
| `DONE`/`D-009`[4] | @37 | ⟪לא אומת בעין⟫ | כן |
| `DONE`/`D-011`[4] | @42 | ⟪לא אומת בעין⟫ | כן |
| `CHECKS`/`C-026`[4] | @949 | ⟪לא אומת בעין⟫ | כן |

⇒ **`0` מחרוזות שרק `DENIED` תופס.** בקורפוס החי — **`DENIED` ⊆ `DENY_NEAR`**, 3/3.

⚠️ **אבל ההכלה ⛔ אינה צורנית, והפער מדוד:** `DENIED` מרשה `\**\s*\**\s*` בין «לא» ל«אומת»,
ו-`DENY_NEAR` מרשה `\s*\**\s*` בלבד ⇒ מפריד שמתחיל בכוכבית **ומכיל רווח פנימי** נתפס רק ב-`DENIED`:

| מפריד | `DENIED` | `DENY_NEAR` |
|-------|----------|-------------|
| `" "` · `""` · `"**"` · `"  "` · `"\n"` | ✔ | ✔ |
| `"* *"` | ✔ | **✘** |
| `"** **"` | ✔ | **✘** |

⇒ **המסקנה של D1 היא ⛔ לא «מחק»:** ההכלה מתקיימת על הקורפוס של היום ו⛔ אינה מובטחת
על נוסח עתידי. ניסוח `לא** **אומת בעין` יעבור את `denied()` ויֵחסם רק ב-`:485`.

---

## D2 · כל שורת `T3` ב-`DONE` — עם `:485` ובלעדיו

| מזהה | אורך תא | עם `:485` | בלי `:485` | Δ |
|------|---------|-----------|------------|---|
| `D-032` | 1,047 | עובר · סמן מפורש: אומת בעין | עובר · זהה | — |
| `D-033` | 1,896 | עובר · אומת בעין | עובר · זהה | — |
| `D-034` | 1,035 | עובר · אומת-מקור | עובר · זהה | — |
| `D-036` | 3,402 | עובר · אומת בעין | עובר · זהה | — |
| `D-037` | 1,425 | עובר · אומת-מקור | עובר · זהה | — |
| `D-038` | 1,601 | עובר · אומת-מקור | עובר · זהה | — |
| `D-039` | 2,929 | עובר · אומת-מקור | עובר · זהה | — |
| `D-040` | 2,407 | עובר · אומת בעין | עובר · זהה | — |
| `D-041` | 3,879 | עובר · אומת בעין | עובר · זהה | — |
| `D-042` | 1,932 | עובר · אומת בעין | עובר · זהה | — |
| `D-043` | 5,321 | עובר · אומת בעין | עובר · זהה | — |

🔴 **`11/11` זהות. ⛔ אף שורה חיה ⛔ אינה מבחינה בין הגרסאות** — כולל `D-043` עצמה, שכן
התא **החי** ⛔ **אינו מכיל `DENIED` כלל** (ראה `D4`). ⇒ **⛔ אין שורה שנופלת היום וצריכה ליפול**;
המחיקה ⛔ אינה מזיקה לקורפוס, ו⛔ גם אינה מוכיחה דבר עליו.

---

## D3 · 11 ה-fixtures ב-`11.3` — הכיסוי, והחור

| # | fixture | `DENIED` בתא | עם `:485` | בלי `:485` |
|---|---------|--------------|-----------|------------|
| 1 | אזכור בדיקה שבוצעה | ⛔ אין | עובר | עובר |
| 2 | אזכור בדיקה שלא בוצעה | ⛔ אין | נופל | נופל |
| 3 | אזכור בדיקה שאינה קיימת | ⛔ אין | נופל | נופל |
| 4 | הכחשה: ⛔ לא בוצעה | ⛔ אין | נופל | נופל |
| 5 | הכחשה: ⛔ טרם בוצעה | ⛔ אין | נופל | נופל |
| 6 | הכחשה: ⛔ פתוחה | ⛔ אין | נופל | נופל |
| 7 | נוסח `D-036` שעבר בטעות | ⛔ אין | נופל | נופל |
| 8 | סמן מפורש תקין | ⛔ אין | עובר | עובר |
| 9 | `אומת-מקור` תקין | ⛔ אין | עובר | עובר |
| 10 | הכחשת הסמן המפורש | **@2** | נופל | נופל *(נימוק אחר)* |
| 11 | הכחשה **רחוקה** ⛔ אינה פוסלת | ⛔ אין | עובר | עובר |

🔴 **`1/11` בלבד מפעיל את `:485`, והוא היחיד — ובקרבה.** fixture 11, שכותרתו «הכחשה רחוקה»,
משתמש ב-`⛔ לא בוצע` — אוצר מילים של **`DENY_NEAR` בלבד**, ש-`DENIED` ⛔ **אינו** תופס.
⇒ **הוא ⛔ אינו בודק את `:485` כלל**, ולמרות שמו ⛔ אינו יכול לתפוס את `B-162`.

🔴 **מסקנה: מחיקת `:485` משאירה `11/11` ירוק.** אוסף ה-fixtures ⛔ **עיוור לחלוטין** לשורה הזו.
**זה החור שאִפשר ל-`B-162` להיוולד** — ⛔ לא ניסוח, ⛔ לא שכחה: ⛔ **אין** fixture שבו `DENIED`
יורה **רחוק** מסמן תקין. **fixture כזה חובה בשלב 2.**

---

## D4 · 🔴 מקרה `D-043` עצמו

### מה שכן ניתן לשחזור, מדוד

**קטע A — הסמן**, בתחילת התא (היסט גולמי 0, היסט אחרי `strip` = **4**):

> `✅ **אומת בעין ב-PDF, כולל ה**ציר** — `C-029`.**`

**קטע B — משפט ההצהרה**, היסט גולמי **919** בתא בן 5,321 תווים:

> `⚠️ **ומה נותר בלתי-נצפה, מוצהר ⛔ ולא מוחלק:** **מסך האפליקציה החי ⛔ נותר ללא פתיחה בדפדפן** — הוא נשען על **מקור + אסרציות** בלבד; ⇒ `C-023` נותרה על מצבה מ-15.08.`

**המרחק:** `919 − 4 = ` **915 תווים** גולמי · **604 תווים** אחרי `strip` · חלון הקרבה **±90**.
⇒ המרחק גדול פי **6.7** מהחלון, ולכן `denied()` על הסמן = **`false`**.

### 🔴 ההפרכה: הנוסח שנפל ⛔ אינו בריפו

`DENIED` בתא ה**חי** של `D-043`: **`0` התאמות.** בשורת ההצהרה שבתחתית `docs/DONE.md`: **`0`.**
`git log -S` מוצא **קומיט אחד בלבד** שנגע ב-`D-043` (`37e7f13`) — וכבר בנוסח המעוקף.
⇒ **הנוסח שנפל ב-17.08 מעולם לא נקומט ו⛔ אינו ניתן לאחזור מהריפו.**

**השחזור המדויק, במיקום שנמדד** (אותו משפט, עם אוצר-המילים שהיה חייב להיות שם כדי ש-`DENIED` יירה):

| נוסח מוחדר @919 | `DENIED` @ | `EYE` @ | מרחק | `denied()` על הסמן | פסק `A` (היום) |
|------------------|-----------|---------|-------|--------------------|----------------|
| `המסך החי ⛔ לא אומת בעין` | 617 | 4 | **613** | `false` | 🔴 **נופל** |
| `⛔ לא אומת בעין` | 608 | 4 | 604 | `false` | 🔴 **נופל** |
| `⛔ לא אומת-מקור` | 608 | 4 | 604 | `false` | 🔴 **נופל** |

🔴 **המנגנון מוכח בשלמותו:** הסמן **אינו מוכחש** (`denied()` = `false` בחלון ±90), והדבר
ה**יחיד** שמפיל את השורה הוא `:485`. ⇒ הענף של הקרבה **בלתי-נגיש** — ההנחה שבפרומפט **אוששה**.

### ⚠️ שתי הפרכות/תיקונים לפרומפט ולשורת `B-162`

1. **«~1,200 תווים משם» ⛔ אינו מדויק.** הנמדד: **915** גולמי / **604** אחרי `strip`.
   המספר ⛔ אינו משנה את המסקנה (כל אחד מהם ≫ 90), אבל הוא **נרשם כפי שנמדד**.
2. 🔴 **המעקף היה ⛔ לא רק מבני — הוא גם לקסיקלי.** `B-162` מצהיר שהמעקף הוא העברת
   ההצהרה לסעיף התחתון. המדידה: **גם התא וגם שורת ההצהרה** מחזירים
   `DENIED = false` **וגם** `DENY_NEAR = false`. הניסוחים שנבחרו —
   ⟪נותר ללא פתיחה בדפדפן⟫ · ⟪⛔ לא נפתח בדפדפן⟫ · ⟪⛔ אין בידי Code⟫ —
   ⛔ **אף אחד מהם ⛔ אינו באוצר המילים של השער**, בעוד ⟪אינו בידי⟫ **כן** בו.
   ⇒ ההצהרה הכנה נכתבה במילים שהשער ⛔ אינו יודע לקרוא. **זה בדיוק התמריץ ההפוך
   ש-`B-162` מתאר**, וכעת הוא מדוד ⛔ ולא משוער.

---

## D5 · המקרה ההפוך — מה `:485` מגן עליו

| # | תא | כוונה | `A` (היום) | `B` (בלי `:485`) |
|---|-----|-------|-----------|------------------|
| N1 | `D-043` משוחזר — הכחשה רחוקה שתופסת `DENIED` | **צריך לעבור** | 🔴 נופל | ✔ עובר |
| N2 | הכחשה **קרובה** לסמן + מצביע `C-091` **שבוצע**, 400 תווים משם | ? — **ראה ההכרעה** | ✘ נופל | 🔴 **עובר** |
| N3 | הכחשה קרובה לסמן ראשון + סמן **שני** תקין רחוק | צריך לעבור | 🔴 נופל | ✔ עובר |
| N4 | הכחשה בלבד, ⛔ בלי שום סמן | צריך ליפול | ✘ נופל | ✘ נופל |

🔴 **תשובת D5: כן, קיים מקרה הפוך — `N2` — והמחיקה לבדה פותחת בו חור.**
תא שמכריז ⟪⛔ לא אומת בעין⟫ ומצטט 400 תווים משם בדיקה שבוצעה **יעבור** אם `:485` יימחק,
כי ענף ה-`C-0\d\d` (`:492`–`:497`) בודק קרבה **סביב המצביע**, ⛔ לא סביב ההכחשה.

⇒ **`B` (מחיקה) פסול.** הוא סוגר את `B-162` ופותח כשל-אישור — בדיוק ‹R-3› בכיוון המסוכן.

---

## D6 · מה קרה ב-`B-149`, ולמה `:485` שרד

`git blame` על `:483`–`:499` ⇒ **כולן** `f7926fd` (15.08, הגל של `B-149`/`D-039`).
אבל `git show f7926fd^` מראה שהלוגיקה **קדמה** לו:

```js
// לפני f7926fd — :426-430
const EYE    = /(אומת בעין|אומת-מקור|צילום|C-0\d\d)/;
const DENIED = /לא\**\s*\**\s*אומת(\s+בעין|-מקור)/;
  .filter((d) => { const proof = strip(…); return !EYE.test(proof) || DENIED.test(proof); });
```

**`DENIED` ⛔ לא השתנה כלל** — ה-regex ב-`:427` הישן **זהה בית-בבית** ל-`:445` של היום.
העיצוב הישן היה **וטו על כל התא**, ⛔ בלי מושג של קרבה.

**מה `f7926fd` עשה:** הסיר את `צילום` ואת `C-0\d\d` מ-`EYE` · **הוסיף** את `DENY_NEAR`+`NEAR_W`+`denied()`
(כלל הקרבה) · הוסיף את ענף ה-`CHECKS` החוצה-קובץ · ועטף הכל ב-`markerVerdict` —
⛔ **אבל העביר את הווטו על כל התא כפי שהוא, כשורה הראשונה בפונקציה.**

🔴 **התשובה ל-D6 — ולמה זה ⛔ לא היה מקרי:** גוף הקומיט של `f7926fd` מצהיר במפורש
⟪סוגרת את M4 **בלי להרחיב את `DENIED`**⟫. ⇒ `DENIED` הוצא מההיקף **בכוונה**,
ולכן הווטו הישן הועתק פנימה במקום להיבחן מול הכלל החדש שנולד **באותו קומיט**.

⇒ **‹R-6› מיום הלידה — ומדויק יותר:** מאותו רגע קיימים **שני עותקים של המושג «הכחשה»** —
`DENIED` (כל התא) ו-`DENY_NEAR`+`denied()` (±90) — **בלי בעלים**, וכשהם נחלקים, הראשון גובר
כי הוא **תנאי-קדם**. ⚠️ **ו⛔ שום דבר לא אילץ את גילויו:** אין fixture שמבחין (D3),
ו⛔ אין שורה חיה שמבחינה (D2). ⇒ הוא שרד **בשקט** — ‹R-4› על השער עצמו.

---

## ההכרעה שחייבת להתקבל לפני שלב 2 — 🔴 ניב בלבד

**`N2`: תא שמכחיש במפורש אימות-עין/מקור, ומצטט בדיקה `C-0xx` שבוצעה בפועל — עובר או נופל?**

| קריאה | נימוק | תוצאה |
|--------|--------|--------|
| **נופל** | הצהרה מפורשת «⛔ לא אומת בעין» היא הודאה, ומצביע לבדיקה ⛔ אינו מבטל אותה | וטו נשמר, ⛔ אך אחרי לולאת ה-`EYE` |
| **עובר** | אזכור בדיקה **שבוצעה** הוא סמן לגיטימי ועצמאי (`:495`); ההכחשה מתייחסת ל**ערוץ אחר** | הווטו יורד לגמרי, ו-`N2` ⛔ אינו חור |

⛔ **⛔ לא הכרעתי בעצמי** — זו הגדרה של «מה נחשב מאומת», ⛔ לא באג.

---

## הכיוון המדוד — ⛔ לא מיושם, להצגה בלבד

ארבעה מועמדים נמדדו על **11 שורות `T3` חיות + 11 fixtures + 4 מקרי-קצה**:

| וריאנט | מה | 11 שורות חיות | 11 fixtures | `N1` | `N2` | `N3` | `N4` |
|--------|-----|---------------|-------------|------|------|------|------|
| **A** — היום | הווטו כתנאי-קדם (`:485`) | 11/11 עובר | 11/11 | 🔴 **נופל** | ✘ | 🔴 **נופל** | ✘ |
| **B** — מחיקה | ⛔ אין וטו | 11/11 עובר | 11/11 | ✔ | 🔴 **עובר** | ✔ | ✘ |
| **C** — לסוף | הווטו לפני ה-`return` האחרון | 11/11 עובר | 11/11 | ✔ | 🔴 **עובר** | ✔ | ✘ |
| **D** — אחרי לולאת `EYE` | הווטו בין `:488` ל-`:489` | 11/11 עובר | 11/11 | ✔ | ✘ | ✔ | ✘ |

🔴 **רק `D` מספק את שני הכיוונים** — והוא **בדיוק** מה שההערה ב-`:448`–`:453` כבר מצהירה:
הווטו הגלובלי חל **רק כשאין אף סמן חיובי בלתי-מוכחש**, ⛔ לא כתנאי-קדם.
⚠️ **`C` ⛔ אינו מספיק** — הוא נופל אחרי ענף ה-`C-0xx`, ולכן `N2` בורח דרך `:495`.
**`D` תקף רק אם ההכרעה למעלה היא «נופל».** אם ניב יכריע «עובר» — `B` הוא הנכון.

**⚠️ וזה ⛔ אינו מספיק לבדו.** `D2` הוכיח ש⛔ **אף שורה חיה ⛔ אינה מבחינה** ⇒ שינוי
בלי fixtures חדשים הוא שינוי **בלתי-נצפה**, שנופל בדיוק לתוך ‹R-4›. **חובה בשלב 2:**

| fixture חדש | תא | ציפייה | מה הוא מוכיח |
|-------------|-----|--------|---------------|
| **12** | הכחשה **שתופסת `DENIED`** ≫90 תווים מסמן תקין (שחזור `D-043`) | **עובר** | סוגר את `B-162` · ⛔ נצפה **אדום** ב-`A` |
| **13** | הכחשה קרובה לסמן + מצביע `C` שבוצע רחוק (`N2`) | לפי ההכרעה | חוסם את החור ש-`B`/`C` פותחים |
| **14** | הכחשה קרובה לסמן ראשון + סמן שני תקין רחוק (`N3`) | **עובר** | ⛔ מונע ריפוד-יתר של הווטו |

⚠️ **fixture 11 חייב לתקן את שמו או את תוכנו** — הוא נקרא «הכחשה רחוקה» ו⛔ אינו מכיל
`DENIED` ⇒ שם שמבטיח כיסוי שאינו קיים הוא ‹R-4› בטקסט.

⛔ **הסדר בשלב 2, ⛔ לא הפוך:** fixture 12 נכתב ו**נצפה אדום** על הקוד של היום → ואז `D` →
ואז ירוק. שינוי שנצפה ירוק בלבד ⛔ אינו הוכחה שהוא עשה משהו.

---

## §11 · סריקה סביבתית — ממצא שלא תוקן

🔴 **סעיף ההצהרות שבתחתית `docs/DONE.md` יושב מחוץ למשטח הקריאה של השער.**
`ROW = /^\|\s*([BDC]-\d{3})\s*\|/` דורש `|` פותח ⇒ שורה שמתחילה ב-`- 🔴 **`D-043`…` ⛔ **אינה
נקראת לעולם** ע"י `defs()`, ואף אסרציה ⛔ אינה נוגעת בה.
⇒ המעקף ש-`B-162` עצמו ממליץ עליו («ההצהרה יורדת לסעיף ההצהרות בתחתית `docs/DONE.md`»)
מעביר את ההודאה הכנה למקום שהשער ⛔ **אינו יכול** לראות. השער נשאר ירוק כי הוא מודד
**נוכחות סמן בתא**, ⛔ לא **אמת** — ‹R-4›, על השער שנועד לאכוף אמת.
⇒ **⛔ לא תוקן כאן** (מחוץ להיקף, ודורש הכרעה). נרשם **`B-165`**.

---

## מה ⛔ לא נעשה, מוצהר

- ⛔ **אפס שינוי ב-`scripts/registry-test.mjs`** · ⛔ אפס נגיעה ב-`docs/DONE.md` · ⛔ אפס ריכוך של `EYE`/`DENIED`/`DENY_NEAR`.
- ⛔ **הנוסח המקורי שנפל ב-17.08 ⛔ אינו בריפו** — השחזור ב-`D4` הוא **הזרקת אוצר-המילים
  למיקום שנמדד** בתא האמיתי, ⛔ ולא תא מסונתז. ⚠️ ההבדל מוצהר ⛔ ולא מוחלק.
- ⛔ **הכרעת `N2` ⛔ לא נלקחה** — ובלעדיה שלב 2 ⛔ אינו יכול להתחיל.
