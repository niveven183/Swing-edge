# AUDIT-B171 — `warnings:["twelvedata"]` מכווץ שלוש סיבות לאות אחת: אבחון read-only

**תאריך:** 2026-08-23 · **HEAD במדידה:** `5ff324a` · **סוג:** T3 שלב 1 (אבחון בלבד, אפס שינוי קוד)

**סיווג §15:** רמה: T3 · תשובות: לא(הפיכות)/**כן**(אמון — צרכן מקבל ירוק על אי-ידיעה)/**כן**(אבטחה — `api/`)/לא(רוחב — שלב 1 לא נוגע בקוד)/כן(ודאות — 81.3% לא נמדד מחדש) ⇒ שאלה 2 ו-3 הן טריגר-יחיד ⇒ **T3** מוצדק.

---

## תקציר מנהלים

`checkTwelveData` (`api/health.js:86-96`) אכן מכווץ שלושה מסלולים שונים ל-`warnings:["twelvedata"]` זהה. **אך המדידה הזו הופכת את ההשערה המובילה שישבה ב-`B-171`:** המפתח **אינו** חסר קבוע בפרודקשן. ארבע ראיות עצמאיות — טיימינג של הבדיקה עצמה, ותדירות לא-100% על פני 40 ריצות טריות — מצביעות יחד על **כשל לסירוגין** (rate-limit / מכסת קרדיטים ברמת ה-API, מסלול ⓷), **לא** על "לא נשאלה שאלה כלל" (מסלול ⓵).

**וממצא חדש, מעבר למה שהתבקש:** TwelveData **אינו** רק פרט בבדיקת-בריאות — הוא בשימוש ישיר ופעיל ב-`api/quote.js` להיסטוריית-שבועי/חודשי של **כל** המניות (sparklines). מדידה חיה **עכשיו** (23.08) מראה ששלושה סמלים שונים (SPY, AAPL, MSFT) מחזירים `null` בדיוק באותו זמן שבו `/api/health` מדווח `warnings:["twelvedata"]`, בעוד קריפטו (BTC-USD, ETH — CoinGecko) עובד תקין. **זו לא תקלת-דיווח בלבד — זו הפיצ'ר עצמו שבור בפרודקשן ברגע זה**, וכיוון (ב) שהוצע ("הסרת twelvedata מהבדיקה אם אינו בשימוש") נפסל לחלוטין: הוא בשימוש, והבדיקה היא ההתראה **היחידה** שקיימת על תקלה אמיתית.

⛔ **לא בוצע שינוי קוד.** הדוח הזה מדידה בלבד.

---

## D1 — שלושת המסלולים ב-`checkTwelveData`, בשמם

```js
// api/health.js:86-96
async function checkTwelveData() {
  const key = process.env.MARKETDATA_API_KEY || "";
  if (!key) return false;                                    // ⓵ :88
  const r = await timedFetch(`${TWELVEDATA_BASE}/time_series?...`, ...);
  if (!r.ok) return false;                                    // ⓶ :93
  const d = await r.json();
  return d?.status !== "error";                                // ⓷ :95 (false כש-status==="error")
}
```

| מסלול | שורה | מה קרה בפועל | מה הצרכן **צריך** לדעת |
|---|---|---|---|
| ⓵ `!key` | `:88` | **אין ניסיון רשת בכלל** — סוד חסר/ריק בסביבת הריצה | תקלת **תצורה** — צריך אדם שמגדיר secret ב-Vercel |
| ⓶ `!r.ok` | `:93` | בקשה יצאה, HTTP לא-2xx חזר (429/5xx/timeout) | תקלת **ספק חיצוני** — זמנית, לא דורשת פעולה אנושית |
| ⓷ `d.status==="error"` | `:95` | בקשה יצאה, HTTP 200 חזר, אבל **גוף** ה-JSON מסמן שגיאה (למשל מכסת קרדיטים) | תקלת **ספק חיצוני** — לרוב מכסה, גם היא זמנית אך עלולה לחזור על עצמה עד איפוס המכסה |

⚠️ **שלושתם אינם שווי-ערך:** ⓵ דורש פעולה אנושית ב-Vercel (הגדרת secret); ⓶/⓷ הם "מעקב בלבד" לפי `sentinel.yml:307` ("ללא פעולה"). כיווץ שלושתם לאות `"twelvedata"` אחת מוחק בדיוק את ההבחנה שקובעת אם מישהו צריך לפעול.

`run()` (`:137-145`) מודד `ms` סביב הקריאה ל-`fn()` **ללא תלות בענף שבו היא חזרה** — `checks.twelvedata` תמיד נושא מספר, גם כשמדובר במסלול ⓵ שלא שלח בקשה כלל.

---

## D2 — המסלול החי עכשיו: לא ⓵, בביטחון גבוה — לא כי נבדק ישירות, אלא כי שתי מדידות עצמאיות סותרות אותו

**⛔ לא בוצעה בדיקת `קיים/לא קיים` ישירה מול Vercel** — אין CLI מקומי מקושר (`vercel env ls` נכשל: `Your codebase isn't linked to a project`), אין טוקן ב-`env` המקומי, ואין כלי MCP ל-Vercel שמפרט שמות משתני-סביבה (נבדק: `get_project` לא מחזיר אותם; אין `list_env_vars`). **זו מגבלת-כלים, לא ממצא** — לא הונח שום דבר על בסיס זה.

**במקום זאת — שתי מדידות עקיפות, שתיהן פוסלות ⓵:**

**1. טיימינג.** `if (!key) return false` הוא קוד סינכרוני, אפס I/O — עלות מדידה תת-מילישנייה. נמדדו **4 קריאות עצמאיות** ל-`/api/health` בפרודקשן (מעבר ל-cache TTL של 20ש', כל קריאה רעננה):

| מתי | `twelvedata` (ms) | להשוואה: `frankfurter` אמיתי (ms) |
|---|---|---|
| 20.08 (רשום ב-`B-171`) | 25 | — |
| 23.08 (רשום ב-`B-171`) | 38 | — |
| 23.08, כאן | 40 | 29 |
| 23.08, כאן (+22ש') | 47 | 24 |

ארבע המדידות יושבות ב-25–47ms — **טווח תואם לספקים שבאמת נשלחה אליהם בקשה** (`frankfurter` האמיתי: 23–29ms), **לא** תואם ל"אפס I/O". מסלול ⓵ היה מייצר `ms` קרוב ל-0, לא 25–47.

**2. תדירות לא-100%.** אם המפתח היה חסר **קבוע**, `checkTwelveData` הייתה חוזרת `false` ב-**100%** מהריצות, תמיד, ללא יוצא מן הכלל — אין נתיב קוד שמאפשר לה להצליח בלי מפתח. נמדדו **40 ריצות Sentinel רצופות וטריות** (22.08 17:39Z → 23.08 17:58Z, `gh run view --log`, שורת `"label":"API/health"` בכל ריצה):

| תוצאה | מספר ריצות | אחוז |
|---|---|---|
| `warnings` מכיל `twelvedata` | 34 | 85.0% (34/40) |
| נקי לגמרי (`"ok · Supabase תקין"`) | 5 | 12.5% (5/40) |
| `warnings` — `coingecko` בלבד (לא `twelvedata`) | 1 | 2.5% (1/40) |

**5/40 ריצות הצליחו לגמרי** — אין הסבר לכך תחת "מפתח חסר קבוע". תוצאה לא-100% על ריצות עוקבות (לא cache — חלון 20ש' מול מרווח ריצות של 20-40 דק') היא בדיוק החתימה של **כשל לסירוגין ברמת ה-API** (⓶/⓷), לא של תצורה חסרה שאינה יכולה להשתנות בין ריצה לריצה.

🔴 **מסקנה, בביטחון גבוה אך לא מוחלט (ללא גישה ישירה ל-Vercel):** המפתח **קיים**. הכשל הוא ⓶ או ⓷ — סביר ביותר ⓷ (מכסת קרדיטים/rate-limit ברמת TwelveData), נתמך ע"י D4 למטה: יש **שלושה** צרכנים לפחות שצורכים קרדיט TwelveData (הבדיקה עצמה, Sentinel שמריץ אותה פעמיים בשעה, וה-history endpoint הפרודקשני עצמו) — עומס-יתר על מכסה חינמית הוא ההסבר הפשוט ביותר שמתאים לשתי המדידות. **זו אינה תקלת-תצורה חיה** (אין `!key`) ⇒ אין עצירה לפי הנחיית D2 בפרומפט.

---

## D3 — צרכני `/api/health`, רשימה סגורה

| צרכן | מה הוא קורא מהגוף | מה קורה עם `warnings` |
|---|---|---|
| **Sentinel** (`sentinel.yml:296-310`) | `status` · `failing` · `warnings` | `[ -n "$warnings" ]` ⇒ `add_fault ... severity=yellow` + `add_summary`. ⚠️ **yellow לעולם לא שולח Discord** (`:535` — רק `red`/`amber` שולחים) — מדוד/נספר, לא מתריע |
| **daily-digest.mjs** (`gatherLive` `:106-144`, הדפסה `:324-331`) | `status` · `failing` · `warnings` | שורת "אזהרות: twelvedata" מודפסת **רק אם** `pipelineOk===false` **או** `status==="degraded"` — כלומר **לא** מודפסת מ-`warnings` בפני עצמו. ⚠️ ראה D7 — **כרגע כן מודפסת**, כי `pipelineOk` באמת `false` מסיבה אחרת (D4) |
| **fleet-weekly.yml** (`:75-88`) | `status` **בלבד** | ⛔ **לא קורא `warnings` כלל** — "🟢 אין תקלות אדומות · health=ok" גם כש-`warnings=[twelvedata]` |
| **health.yml** (deprecated, `workflow_dispatch` בלבד) | קוד HTTP בלבד (`$code != 200`) | ⛔ לא מפרסר JSON כלל, לא רץ אוטומטית (אין `schedule:`) |
| **UptimeRobot** (חיצוני, מוזכר ב-`docs/ARCHITECTURE.md:80`) | לא ניתן לאימות מהריפו — תצורה חיצונית | ⚠️ **לא מאומת** — לפי התנהגות טיפוסית של השירות, סביר שנבדק רק קוד HTTP, לא גוף התשובה, אך זו הנחה שלא נמדדה ⛔ ומסומנת ככזו |

⚠️ **שינוי סכימה (D6-א) חייב לעבור את חמשת השורות האלה בלי לשבור אף אחת** — Sentinel ו-daily-digest משתמשים בבדיקת "לא ריק" בלבד (format-agnostic), אבל UptimeRobot אינו ניתן לאימות מכאן.

---

## D4 — TwelveData בשימוש בפרודקשן: כן, פעיל, ושבור עכשיו

`api/quote.js` (לא `docs/ARCHITECTURE.md:81`, שמתעד רק Finnhub+CoinGecko ומחסיר את המצב הזה — ⚠️ תיעוד לא מעודכן, ראה §11 בדיווח) — `TWELVEDATA_BASE` בשימוש ב-`fetchEquityHistory` (`:246-264`), הנתיב שמזין **את כל** ה-sparklines/היסטוריית-שבועי למניות (`mode=history`). קריפטו הולך ל-CoinGecko (`fetchCryptoHistory`, `:270-297`) — נתיב עצמאי לגמרי.

**מדוד ישירות, `curl` על פרודקשן, 23.08:**

```
GET /api/quote?history=1&symbols=SPY,BTC-USD
  → SPY:      MISSING/NULL
  → BTC-USD:  8 נקודות (תקין)

GET /api/quote?history=1&symbols=AAPL,MSFT,BTC-USD,ETH
  → AAPL:     MISSING/NULL
  → MSFT:     MISSING/NULL
  → BTC-USD:  8 נקודות (תקין)
  → ETH:      8 נקודות (תקין)
```

**כל** סמל מניה שנבדק (SPY, AAPL, MSFT) חוזר `null`; **כל** סמל קריפטו (BTC-USD, ETH) תקין. זה לא מדגם — זה עקבי, נמדד פעמיים במרווח דקות, באותו זמן שבו `/api/health` מדווח `warnings:["twelvedata"]`.

🔴 **מה זה אומר:** אין מקום לכיוון "TwelveData לא בשימוש, אפשר להסיר מהבדיקה" (D6-ב). ההפך — ה-endpoint שמבטא את הכשל הזה בפני משתמש **קיים ופעיל עכשיו**: כל מי שפותח sparkline של מניה בפאנל השבועי/חודשי מקבל כלום. הבדיקה ב-`api/health.js` היא, נכון לרגע זה, **המנגנון היחיד שמעיד** על התקלה הזו — למרות ש-`warnings` (D3) לא מתריע (yellow, לא red/amber).

---

## D5 — 81.3% נמדד מחדש: 85.0% (34/40), טרי, 22.08 17:39Z→23.08 17:58Z

ראה טבלה ב-D2. **לא 100%** — עקבי עם המסקנה ב-D2 (כשל לסירוגין, לא תצורה קבועה). מספר קרוב למקורי (81.3%→85.0%) — התופעה נמשכת ולא חלפה מעצמה בין 21.08 ל-23.08.

---

## D6 — כיוונים, לפי מה שנמדד

**(א) הפרדת שלוש הסיבות לערכים נבדלים** (למשל `warnings:["twelvedata:no_key"]` מול `"twelvedata:api_error"`).
✅ נראה בטוח מול D3: Sentinel בודק `[ -n "$warnings" ]` (לא-ריק, לא-תלוי-תוכן) ו-daily-digest רק מצרף (`join`) — שניהם format-agnostic. ⚠️ **הסיכון היחיד המזוהה הוא UptimeRobot**, שלא ניתן לאימות תצורה מהריפו (D3) — אם הוא עושה string-match מדויק על גוף התשובה (לא סביר לשירותי uptime טיפוסיים, אבל לא מאומת), שינוי צורה עלול לשבור אותו בשקט.

**(ב) הסרת twelvedata מהבדיקה אם אינו בשימוש.**
⛔ **נפסל.** D4 מוכיח שהוא בשימוש פעיל ושבור עכשיו. הסרתו מהבדיקה תסיר את ההתראה **היחידה** הקיימת (חלשה ככל שתהיה) על תקלה אמיתית שמשתמשים חווים עכשיו.

**(ג) שינוי מה `status` מצהיר.**
⚠️ **המשקל שלו עלה** לאור D4/D5: התכנון המקורי ("blip בודד לא יעיר אותנו") הגיוני ל-⓶/⓷ **חד-פעמיים**; אבל 85% על פני 40 ריצות רצופות **אינו** blip — הוא מצב נמשך. שינוי `status` ל-`ok`/`degraded` ישפיע ישירות על Sentinel (red-fault `:300-303`) **וגם** על fleet-weekly (`:82-88`, קורא `status` בלבד — היחיד בין החמישה שכן ירגיש שינוי כזה מיד). דורש תיאום עם שני הצרכנים האלה, לא רק תיוג.

**(ד) — כיוון רביעי, לא הוצע במקור: לתקוף את שורש הכשל ב-TwelveData עצמו, לא רק את התיוג.** D4 קובע שזו לא רק בעיית-ניסוח בדיקת-בריאות — זו תקלת פיצ'ר חי. D2 מציע (לא מוכיח) מכסת-קרדיטים כגורם סביר, ולפחות שלושה צרכנים (הבדיקה עצמה, Sentinel פעמיים בשעה, וה-history endpoint) צורכים קרדיט מאותו מפתח. תיוג נכון (א) ומדיניות `status` נכונה (ג) לא יתקנו sparkline ריק למניה — רק אבחון-שורש (⛔ מחוץ לגבולות הדוח הזה — דורש session נפרד, כולל ⛔ אין נגיעה ב-secrets/הגדרות Vercel per §12) יכול.

---

## D7 — B-174: אותו משטח? מדוד, לא — אבל קרוב

`B-174` הוא הנחיה **מפורשת** ב-system prompt של `daily-digest.mjs:381` להתעלם מ-`conclusion` שאינו `"failure"`/`null` בנתוני CI — כשל-ציות מכוון, לא כשל-ידיעה.

`gatherLive()` (D3) **כן** צורכת `warnings` — אין שם שום הנחיה להתעלם. ⚠️ **אבל יש נקודת-דמיון אמיתית:** התנאי ב-`:324` (`pipelineOk===false || status==="degraded"`) אומר ש-`warnings` בפני עצמו **לעולם לא** מדליק את השורה — הוא רק "נספח" לשורה שכבר הודלקה מסיבה אחרת. אם `pipelineOk` היה `true` (למשל אם ה-cache החם מ-`_histCache` היה עדיין מכיל SPY מריצה קודמת), הדיגסט היה **שקט לגמרי** על `warnings:["twelvedata"]`, בדיוק כמו ש-`fleet-weekly.yml` שקט עליו תמיד (D3). זה לא הנחיה מפורשת כמו `B-174` — זו **תלות שבירה**: הראות של הדיגסט לתקלה הזו מותנית בכשל-משנה שאינו קשור ישירות ל-`warnings` עצמו.

⛔ לא תוקן — נרשם בלבד, כנדרש בסיווג T3 שלב 1.

---

## D8 — מה בדיוק נכשל בקריאה ל-TwelveData: ⛔ לא ניתן לצטט קוד HTTP ספציפי — הקוד עצמו זורק אותו לפני שהוא נכתב לשום מקום

**נבדק ישירות ב-Vercel (`get_runtime_errors`, פרויקט `prj_JaOaPmjgAQSN8yL8UGmF2SHobQVk`, חלון 7 ימים):** 3 קבוצות שגיאה בלבד בכל הפרויקט, **0 מהן twelvedata**:

| קבוצה | מופעים | משתמשים | מקור |
|---|---|---|---|
| `Finnhub quote failed: HTTP 429` | 2622 | 18 | `api/quote.js` (real-time quote, ⛔ לא history) |
| `[DEP0169] url.parse() deprecated` | 623 | 191 | Node runtime warning |
| `Finnhub health check failed: HTTP 429` | 7 | 7 | `api/health.js` |

`get_runtime_logs` עם `query: "twelvedata"` (חלון 24 שעות, production) — **0 שורות**.

**זו לא תקלת-חיפוש — זו ראייה ישירה בקוד שהמידע נזרק במכוון:**
- `checkTwelveData` (`api/health.js:86-96`) — ⛔ **אין `console.error`/`console.warn` באף אחד משלושת המסלולים.** להשוואה: `checkFinnhub` (`:74-83`) כן קורא `console.error` על `!r.ok` — וזו הסיבה היחידה שה-429 של Finnhub נראה בטבלה למעלה ושל TwelveData לא.
- `fetchEquityHistory` (`api/quote.js:246-264`, ה-endpoint הפרודקשני) — אותו דפוס בדיוק: `if (!r.ok) return {}` (`:252`) ו-`if (data.status === "error") return {}` (`:255`), **בלי שורת לוג אחת**. גם אם `fetchWithTimeout` היה זורק (timeout רשת), אין `try/catch` סביב הקריאה, וה-`.then()` ב-`handleHistory` (`:321-331`) שמכיל אותה יושב בתוך `Promise.allSettled` (`:340`) שלעולם לא בודק `status:"rejected"` — גם תקלת-רשת שקטה מתכנסת ל-`null` בלי עקבות.

**מה כן נמדד, מבחוץ (ללא נגיעה במפתח האמיתי):** תיעוד TwelveData הפומבי (twelvedata.com/docs, twelvedata.com/pricing — לא הפרויקט שלנו) קובע: `401`=מפתח שגוי/חסר · `429`=מכסת קרדיטים/rate-limit (Free: **8 קרדיטים/דקה, 800/יום**) · `403`=endpoint לא כלול בתוכנית. `checkTwelveData`'s `if (!r.ok) return false` (⓶) תופס **את כולם באותה צורה** — גם אילו הלוג היה קיים, קוד 401 מול 429 מול 403 עדיין לא היו נבדלים בענף הזה כפי שהוא כתוב היום.

🔴 **מסקנה D8, ישירה: לא ניתן לצטט את השגיאה בפועל — הבקשה יוצאת, מקבלת תשובה, והתשובה **נזרקת** לפני שהיא מגיעה לכל מקום שנקרא-לו.** זו לא מגבלת-כלים כמו ב-D2 (שם MCP חסר יכולת) — זו עובדה על ה**קוד עצמו**: `r.status` ו-`d.message` (הגוף המדויק שהתיעוד החיצוני מראה שקיים, למשל `"API request limit reached for your key"`) נקראים ומיד מושלכים. הדרך היחידה לצטט אותם היא הוספת לוג זמני ופריסה — שינוי קוד, אסור בשלב 1 — או בדיקה ישירה בלוח-הבקרה של TwelveData (תחום ניב, לא Vercel/Code).

---

## D9 — מתי זה נשבר: ⛔ לא באירוע אחד — קיים מיום 1 של הפיצ'ר (04-05.07), בתדירות שכמעט הכפילה עצמה

**ציר זמן הקוד:** הפיצ'ר עצמו (`fetchEquityHistory`, weekly market overview) נוסף `04.07` (`ade3d39`). `checkTwelveData` נוסף למחרת, `05.07 16:35` שעון ישראל (`2ded033`).

**מקור נתונים שלא נבדק קודם: `health.yml` (Health Monitor, כיום deprecated) רץ בלוח-זמנים כל 30 דקות מ-`05.07` ועד `25.07` (אז Sentinel החליף אותו), ו-`cat /tmp/health.json` (`health.yml:36`) הדפיס את **גוף התשובה המלא** ללוג — כולל `warnings[]` — בכל ריצה. זו אוכלוסייה מלאה, ⛔ לא מדגם, של 05.07–25.07:**

- **282 ריצות נמשכו, 280 תקפות** (2 קדמו לקומיט שהוסיף את שדה ה-`warnings`).
- **95/280 (33.9%)** נשאו `"twelvedata"` בתוך `warnings[]` — **מיום 1**.
- **פירוט יומי** (05.07→25.07, כל ריצות היום, ⛔ לא מדגם-אחד-ליום): תנודתיות גבוהה, **לא** מגמה שטוחה — ימים נקיים **לגמרי** (0/N ב-`11.07`·`12.07`·`18.07`·`25.07`) לצד ימים שמגיעים ל-**55-73%** (`06-10.07`, `13.07`, `19-21.07`, `24.07`). התבנית `0%↔73%` **פוסלת שוב** "מפתח מת קבוע" — מפתח מת מייצר 100% תמיד, ⛔ לא ימים נקיים לגמרי לצד ימים ברמת-משבר.

**השוואה למדוד עכשיו (D2/D5): 81.3%→85.0%.** ⇒ **התדירות לא נשארה קבועה — היא עלתה מ-~34% (שלושת השבועות הראשונים) ל-~85% (עכשיו), פי ~2.5, על פני כ-7 שבועות.** 🔴 **וזה המספר שקובע פגיעה, כפי שנדרש:** זו ⛔ **לא** "שבורה שבועיים" — היא נמצאת, לסירוגין, **מהיום הראשון של הפיצ'ר**, ומחמירה. ⚠️ **הסבר סביר (⛔ לא מוכח, D8 חוסם הוכחה ישירה):** מכסת-קרדיטים יומית קבועה מול צריכה שגדלה עם צמיחת האפליקציה (יותר משתמשים ⇒ יותר קריאות ל-`fetchEquityHistory` ⇒ המכסה מתרוקנת מוקדם יותר ביום ⇒ יותר נקודות-זמן נדגמות אחרי האפס) — עקבי עם שני מקורות הנתונים, לא מאושר בלעדיהם.

⛔ **מספר משתמשים מדויק שנפגעו — לא נמדד; אין טלמטריה per-user על history שנכשל.** מה שכן נמדד: חלון-הפגיעה הוא שבועות, לא ימים.

---

## D10 — מה המשתמש רואה: לשונית "מודיעין שוק" (Market Intel), ⛔ לא מסך הנחיתה · לא ספינר תקוע — כרטיס-שלד או השמטה שקטה

*(נמדד ע"י אג'נט Explore על `src/priceService.js` ו-`SwingEdge_App.jsx`, קריאת-קוד בלבד, לא בוצע שינוי)*

- **נתיב:** `fetchMarketOverview()` (`src/priceService.js:544-579`) ← `fetchChartResults(..., {history:true})` (`:555`) ← `/api/quote?history=1` (`:105-110`). נצרך מ-`SwingEdge_App.jsx:2032` (טאב `"intel"`, `:6512`).
- **מיקום:** לשונית **"מודיעין שוק"** — הפריט השישי מתוך שמונה ב-`NAV_KEYS` (`:1044`). ⛔ **לא** הטאב שנטען כברירת-מחדל (`"dashboard"`, `:1408`) — משתמש חייב לנווט אליו במפורש.
- **מה קורה על `null`:** `parseChartResult` (`priceService.js:122-193`) מחזיר `null`, וה-merge guard (`:559-560`, הערה מפורשת בקוד: *"null / invalid / <2 closes NEVER overwrites a previously-known value"*) פשוט **לא כותב** לסמל הזה. שתי תוצאות שונות לפי מיקום:
  - **שורת "Market Pulse" (מדדים):** כל מדד מוגדר תמיד מרונדר (`:6731-6736`); אם חסר — נופל ל-`MarketPulseCardSkeleton` (`:1123-1129`, `:6733,6735`) — **כרטיס-קו-מקווקו, ⛔ לא ספינר, ⛔ לא שגיאה**.
  - **שורת סקטורים/נושאים:** סמל חסר **מושמט מהתצוגה לגמרי** (`pick()`, `priceService.js:525`, `.filter(Boolean)`) — ⛔ **בלי סימן חזותי כלשהו** שמשהו חסר.
  - **הפאנל כולו:** ספינר (`RefreshCw`, `:6718-6722`) מוצג **רק** אם `indices.length===0 && sectorsThemes.length===0` — כלומר רק בטעינה הראשונה-אי-פעם. אחרי שסמל אחד כלשהו הצליח פעם אחת, הפאנל ⛔ לעולם לא חוזר לספינר — סמל שנכשל תמיד פשוט יושב כשלד/מושמט, ללא הגבלת זמן.
- **retry:** ⛔ אין retry per-symbol. יש retry על **כל הקריאה** אם היא זורקת (`SwingEdge_App.jsx:2034-2036`, אחרי 15ש') — אבל `fetchMarketOverview` "never throws" לפי תיעוד-הקוד שלה (`:542`), כך שמסלול זה כמעט לעולם לא יורה על `null` בודד. ההתאוששות היחידה בפועל: פולינג רגיל דרך `getOverviewRefreshInterval` (`:2040,2061`).

---

## D11 — האם יש fallback: ⛔ אין, ומתועד ככה **בקוד עצמו** מיום ההטמעה — Finnhub לא מסוגל

הערת המחלקה שהוסיפה את הפיצ'ר (`api/quote.js`, קומיט `ade3d39`, `04.07`):

> `// Powers the Market Overview panel. Finnhub's free tier has no candle endpoint, so equities come from Twelve Data /time_series and crypto from CoinGecko /market_chart.`

**מאומת בקוד, לא רק בהערה:** `api/quote.js` קורא ל-Finnhub בשני מקומות בלבד — `/quote` (מחיר-חי, `:145-146`) ו-`/search` (`:378-379`). ⛔ **לעולם לא `/stock/candle`** (endpoint ההיסטוריה של Finnhub). נבדק ישירות (טוקן דמה, ⛔ ללא נגיעה במפתח האמיתי) — ה-endpoint קיים ומחזיר `401 {"error":"Invalid API key."}`, כלומר הנתיב תקף ברמת ה-routing; ⚠️ **לא אומת עצמאית מול תיעוד המחירים של Finnhub אם ה-tier שלנו כולל אותו** — `WebFetch` על `finnhub.io/pricing` לא חשף פירוט tier (עמוד מבוסס-JS), כך שההסתמכות היא על הערת-הקוד המקורית בלבד, ⛔ לא על אימות-מקור שני עצמאי.

🔴 **מסקנה D11: אין נתיב-גיבוי היום.** אם ל-Finnhub באמת אין candle ב-tier הנוכחי (כפי שהמטמיע המקורי תיעד), אין דרך "פשוט להחליף ספק" בלי שדרוג תוכנית/ספק שלישי — זה מחוץ לגבולות שלב 1 (§12: לא נוגעים בהגדרות ספק/סודות).

---

## לקחים — ה"הפרכה" מ-20.08 הייתה שגויה, ונרשמת כאן כנדרש

**מה נכתב אז (מצוטט משורת `B-171` הקיימת ב-`BACKLOG.md`):** מדידת נקודה בודדת (`twelvedata=25ms`) הובילה למסקנה שהמסלול המהיר-יחסית "**לא שאל שאלה כלל**" (⓵, מפתח חסר) — והושוותה במפורש למחלקת `B-147` ("סוכן ממציא סיבה מעל מה שנמדד"), כלומר **הוצג כרעש-דיווח שאינו דורש דחיפות**, ⛔ לא כתקלה אמיתית פעילה.

**מה נמדד עכשיו, ומפריך את זה:** D2 (טווח 25-47ms תואם-בקשה-אמיתית, לא אפס-I/O) + D9 (33.9%→85.0%, קיים מיום 1, ⛔ לא "אירוע") + D4 (sparkline-מניה **ריק בפרודקשן עכשיו**, לא רק תווית) — שלושתם יחד קובעים ש-`warnings:["twelvedata"]` **אמר אמת ברוב הריצות**, במשך שבעה שבועות, ותיאר תקלת-פיצ'ר אמיתית שמשתמשים חווים. 🔴 **ההפרכה מ-20.08 טעתה בכיוון ההפוך המדויק שממנו `B-147` מזהיר** — לא "אמצא סיבה שלא נמדדה", אלא **דחיית סיבה אמיתית מתוך קריאה שגויה של מדד יחיד**.

**מה בדיוק היה שגוי בהיסק:** נקודת-מדידה בודדת (`25ms`) נלקחה כהוכחה על **הענף שרץ** (⓵ מול ⓶/⓷), בלי לבדוק את המשתנה הזמין שכבר ישב באותה שורה — `frankfurter=23ms`, קריאת-רשת **אמיתית ומהירה עוד יותר**. ההערה הזו כתובה כבר בשורת `B-171` הקיימת ("«מהיר ⇒ לא נשלח» ⛔ אינו מבחן תקף") — אבל התיקון-בזמנו עצר בתיקון-הניסוח ולא חזר לבדוק אם ה**מסקנה התפעולית** (רמת-דחיפות, השוואה ל-`B-147`) עדיין תקפה לאחר שהראיה שתמכה בה קרסה. היא לא נבדקה מחדש עד למשימה הזו.

🔴 **הכלל הכללי לסשנים הבאים:** מדידת-תזמון בודדת יכולה לשלול **מסלול-קוד ספציפי** (וזה תקף — D2 עדיין משתמש באותה שיטה), אבל **אינה** רישיון למסקנה על **חומרה/דחיפות** של האזהרה כולה. חומרה נמדדת בנפרד — תדירות (D5/D9) והשפעה-בפועל (D4/D10) — בדיוק כפי שבוצע כאן. אזהרה לא יורדת בדרגה על סמך מדגם-אחד.

⛔ **לא בוצע שינוי קוד עקב הלקח הזה.** נרשם כדי שהטעות לא תיוולד מחדש מהזיכרון (`CLAUDE.md` §2, "אפס תלות בזיכרון אנושי").

---

## 🆕 ממצא נלווה, מחוץ להיקף D1-D11, נרשם ⛔ ולא תוקן: `B-176`

תוך כדי D8 (`get_runtime_errors`), התגלה: `Finnhub quote failed: HTTP 429` — **2622 מופעים, 18 משתמשים**, `api/quote.js` (מחיר-חי, ⛔ **לא** TwelveData/history — ספק ומנגנון נפרדים לגמרי). זה **לא** נחקר כאן (חורג מהיקף B-171) — **נרשם כ-`B-176` ב-`docs/BACKLOG.md`** לפי §10.1, כולל הפרדה מפורשת מ-B-171 בגוף הרשומה. ⛔ אין נגיעה בקוד/סודות בעקבות הממצא הזה.

---

## סיכום ממצאים מול חמש השאלות ב-D6

| כיוון | חוסם עיקרי | תלוי ב־ |
|---|---|---|
| (א) הפרדת סיבות | UptimeRobot לא מאומת | אישור ניב על הסיכון השיורי, או אימות ידני של תצורת UptimeRobot |
| (ב) הסרה | ⛔ פסול — הוכח בשימוש | — |
| (ג) שינוי `status` | תיאום עם Sentinel + fleet-weekly | החלטת-מוצר: מה נחשב "לא חד-פעמי" |
| (ד) שורש-הכשל ב-TwelveData | גישה ל-Vercel/מכסת TwelveData | session נפרד, ⛔ מחוץ לגבולות דוח זה |

---

## מגבלות המדידה (לתעד בכנות)

- **D2 אינה בדיקת `קיים/לא קיים` ישירה** כפי שהתבקש — אין כלי זמין (לא CLI מקושר, לא MCP עם `list_env_vars`). המסקנה נשענת על שתי מדידות עקיפות (טיימינג + תדירות לא-100%), לא על קריאה ישירה מ-Vercel.
- D5 נמדד על 40 ריצות (לא 91 כמו המדידה המקורית) — מדגם טרי וקרוב בזמן, לא אותו חלון בדיוק.
- UptimeRobot (D3) אינו מאומת מהריפו כלל — תצורה חיצונית.

---

**עצרתי לאישור.** אין שינוי קוד. הכיוונים ב-D6 ממתינים להחלטת ניב לפני מעבר לשלב 2 (Plan Mode).
