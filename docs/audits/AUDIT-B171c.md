# AUDIT-B171c — הגרף השעתי: D19 (האם 2d95218 בפרודקציה), מרווח בפועל, ספירת setInterval

**תאריך:** 2026-08-24 · **HEAD במדידה:** `9a6ff12` · **סוג:** T3 שלב 1 (אבחון בלבד, אפס שינוי קוד)

**סיווג §15:** רמה: T3 · תשובות: לא(הפיכות)/**כן**(אמון — הכרעה אם התיקון בפרודקציה או לא נשענת ישירות על המדידה כאן)/לא(אבטחה — קריאה בלבד)/לא(רוחב — מסמך בלבד)/**כן**(ודאות — הנחת ניב "השוק סגור ב-19:00" ו"מרווח ~2 דק'" לא נמדדו, ראה תיקון-הנחות למטה) ⇒ שאלה 5 טריגר-יחיד ⇒ **T3** מוצדק.

⛔ **לא בוצע שינוי קוד.** כל הממצאים מ-Vercel `list_deployments`/`get_deployment`/`get_runtime_errors`/`web_fetch_vercel_url` בזמן אמת, מ-`git log`, מ-`npm run build` מקומי, ומקריאת `SwingEdge_App.jsx`/`src/priceService.js`/`scripts/daily-digest.mjs`/`.github/workflows/*.yml`.

---

## 🔴 תקציר מנהלים — D19 קודם לכל השאר

**D19 עונה תשובה חד-משמעית: הקוד הממוזג (`2d95218`) *כן* פרוס בפרודקציה עכשיו, אבל הוא ⛔ לא היה פרוס במהלך כל חלון התצפית שניב הביא בגרף.** זו בדיוק ההתראה שניב ביקש: "אם D19 מגלה שהקוד לא נפרס — זו התשובה, עצור מיד ואמור." הקוד כן נפרס — רק **מאוחר יותר**, ולכן ה-"16=2×8" שנצפה הוא ⛔ לא כשל של המיזוג — הוא **הבאג הישן, ידוע-מראש**, שרץ לפני שהתיקון עלה.

**ציר זמן מדוד (UTC, מ-Vercel `list_deployments`):**

| שעה UTC | שעה IL | דיפלוימנט | קומיט | תוכן |
|---|---|---|---|---|
| 13:33:30 | 16:33 | `dpl_Ef748…` | `a6e3069` | **docs בלבד** ("Zero code touched — plan only") |
| **15:51–16:43** | **18:51–19:43** | **`dpl_Ef748…` (אותו)** | **`a6e3069`** | **⇐ חלון התצפית של ניב — קוד חי הוא עדיין הישן** |
| 17:35:02 | 20:35 | `dpl_B78yJ…` | **`2d95218`** | **🔴 התיקון עצמו עולה כאן** — כ-52 דק' *אחרי* שהתצפית של ניב הסתיימה |
| 17:48:06 | 20:48 | `dpl_GuUXg…` | `9a6ff12` | docs בלבד (הקומיט שלי מהחלק הקודם של השיחה) — **זה הדיפלוימנט הנוכחי, `swing-edge.com` מצביע עליו** |

בין `a6e3069` (13:33 UTC) ל-`2d95218` (17:35 UTC) **אין דיפלוימנט אחר** — נבדק מול כל 20 הרשומות שהחזיר `list_deployments`. הקוד-בפועל שרץ ב-15:51–16:43 UTC הוא עדיין מה שהיה חי מאז הקומיט-קוד האחרון שקדם לו: `21ef3a3` ("add console.error logging... no logic change") — כלומר **שני ה-`useEffect` הנפרדים, בלי איחוד ובלי visibility-gate**. זה בדיוק אותו באג שכבר תועד ב-`AUDIT-B171b.md` (D12): *"מפגש אחד, טאב אחד = עד 16 credits בבת אחת מול תקרה של 8"* — ממצא ישן, לא תגלית חדשה.

**⇒ שלוש המסקנות שניב כתב בפרומפט (§1–§3) כולן חדות יותר משהוצג:**
1. "האיחוד לא נכנס לתוקף" — **מדויק, אבל הסיבה שונה מ"נכשל": הוא עדיין לא היה קיים באותו רגע**, לא ש"נכנס לתוקף ונכשל".
2. "המרווח לא מכבד מצב שוק" — **הופרך**, ראה §2 למטה: השוק היה **פתוח**, לא סגור, וה-5 דק' שנמדדו תואמות בדיוק את הקוד (גם הישן וגם החדש — `getOverviewRefreshInterval` לא השתנה במיזוג).
3. "הטאב היה גלוי ⇒ visibilityState לא נבדק" — **לא רלוונטי לחלון הזה**: הקוד שרץ אז לא הכיל שום `visibilitychange` listener בכלל (זה בדיוק מה ש-`2d95218` הוסיף) — אין מה "לא לבדוק", התכונה עוד לא הייתה קיימת.

---

## תיקון-הנחות (נמדד מול הפרומפט, לא הונח)

⚠️ **שתי טענות בפרומפט המקורי אינן תואמות למדידה עצמאית — מדווח כנדרש (`feedback_verify_premises`):**

**(א) "בשעה 19:00 IL השוק סגור"** — ⛔ שגוי. 24.08.2026 הוא **יום שני**. 18:51–19:43 IL בקיץ (IDT=UTC+3) = **11:51–12:43 ET** (EDT=UTC-4) — בדיוק בתוך שעות המסחר הרגילות (9:30–16:00 ET). `getMarketState` (נקרא בקוד, `src/priceService.js:45-70`) אמור להחזיר `MARKET_OPEN` באותו חלון, ⛔ לא `CLOSED`. ⇒ הציפייה הנכונה ל-`getOverviewRefreshInterval` היא **5 דק'**, ⛔ לא 30.

**(ב) "כל ~2 דקות"** — ⛔ שגוי חשבונית. חישוב הפרשים מ-11 חותמות הזמן שניב עצמו רשם (06:51→07:41 pm): `[6,4,6,4,6,4,4,6,6,4]` דק' — **ממוצע 5.0 דק' בדיוק**, לא 2. זה **תואם באופן מושלם** את קצב 5-הדקות של שוק-פתוח, ⛔ לא סותר אותו.

**המשמעות המצטברת:** ברגע שמתקנים את שתי ההנחות, מסקנה §2 בפרומפט ("הטיימר לא מכבד את המרווח, יש setInterval נוסף") **מתהפכת** — המרווח שנמדד תואם בדיוק את הקוד (הישן וגם החדש). אין עדות למרווח שגוי או ל-timer חבוי.

---

## D19-הרחבה — אימות "בפרודקציה" מעבר לפי SHA בלבד

ניב ביקש במפורש "בדוק bundle hash מול הבנייה". בוצע, בשלוש שכבות:

1. **Alias דומיין:** `get_deployment` על `dpl_GuUXg3cXeyUyGLsJt5kn1hiTSxoq` (הדיפלוימנט האחרון) מחזיר `alias: ["swing-edge.com", "www.swing-edge.com", ...]` — **הדומיין הציבורי אכן מצביע על דיפלוימנט זה**, לא עותק ישן תקוע.
2. **SHA רשמי:** אותו דיפלוימנט נושא `githubCommitSha: 9a6ff129593f1363ac3e7ea88432acb272bf55c2` — HEAD של `main` היום, שלושה קומיטים אחרי `2d95218` (`git log --oneline`: `9a6ff12 → 2d95218 → e7436f2`), ומאותה שרשרת דיפלוימנטים רציפה (אין קפיצה/rollback ביניהם).
3. **Bundle hash בפועל:** `web_fetch_vercel_url` על `https://swing-edge.com` מחזיר `index.html` חי המפנה ל-`/assets/index-DeAGaU4_.js`. הרצתי `npm run build` פעמיים ברצף מ-HEAD מקומי נקי (`git status` נקי): שני הריצות המקומיות מייצרות **אותו** `index-BdsAjy1M.js` — כלומר הבנייה המקומית **דטרמיניסטית עצמה מול עצמה**, אבל **שונה** מהחי (`DeAGaU4_` ≠ `BdsAjy1M`). ⚠️ **זה לא סימן לקוד שונה** — שלושה chunks וקטוריים אחרים (`index-DL1LuIfv.css`, `date-fns-BPfDkTbk.js`, `sentry-N1B75qQV.js`, `recharts-BRzi68GW.js`) יצאו **זהים בייט-לביית** בין הבנייה המקומית לחי, מה שמוכיח גרסאות-תלות זהות ואין rollback. ה-chunk הראשי היחיד שהתפצל הוא ה-entry (`index-*.js`) — הסביר ביותר: פלאגין ה-Sentry (`sentry-vite-plugin`) מזריק release identifier שתלוי ב-`SENTRY_AUTH_TOKEN`; הבנייה המקומית שלי רצה בלי הטוקן (אזהרת build: "No auth token provided... Will not create release") בעוד ל-Vercel CI כנראה יש טוקן — הבדל **סביבתי**, לא הבדל **מקור**.

**⇒ מסקנה: לא הושג התאמת-hash מדויקת ל-100% (ולכן לא "אומת-מקור" מלא לפי §15), אבל שלוש הראיות המצטברות (alias+SHA+3/4 vendor-chunks זהים) הן ראייה חזקה שהקוד החי הוא אכן HEAD, לא deploy ישן.** אם ניב רוצה סגירה מוחלטת (100%, לא "חזקה") — צריך `SENTRY_AUTH_TOKEN` מקומי כדי לשחזר את אותו build בדיוק, שזה סוד ולכן מחוץ לגבולות שלי (§12).

---

## D19-ב — ספירת setInterval, מלאה מחדש (לא מהזיכרון)

```
grep -n "setInterval" SwingEdge_App.jsx src/**/*.jsx src/**/*.js
```

| קובץ:שורה | מטרה | נוגע ב-TwelveData/quote/overview? |
|---|---|---|
| `SwingEdge_App.jsx:1983` | רענון `marketState` (מחרוזת בלבד, בלי fetch) | ⛔ לא |
| `SwingEdge_App.jsx:2005` | מחירים חיים (`getRefreshInterval`, Finnhub) | ⛔ לא (TwelveData לא בשימוש כאן) |
| **`SwingEdge_App.jsx:2057`** | **Market Overview (`getOverviewRefreshInterval`)** | **✅ כן — היחיד** |
| `src/components/AdminPanel.jsx:1515` | health ping ל-Supabase | ⛔ לא |
| `src/components/AdminPanel.jsx:1687` | tick UI | ⛔ לא |
| `SwingEdge_App.jsx:2400` | tiltTick UI | ⛔ לא |
| `SwingEdge_App.jsx:2591` | tickerTape UI | ⛔ לא |
| `SwingEdge_App.jsx:2596` | pulse UI | ⛔ לא |
| `src/intelligence/ui/IntelligenceUI.jsx:329` | tick UI (1s) | ⛔ לא |

**⇒ בקוד הנוכחי (`9a6ff12`, כולל המיזוג) יש בדיוק setInterval אחד שנוגע ב-TwelveData.** בקוד הישן שרץ בזמן התצפית (`a6e3069`/`21ef3a3`) היו **שניים** (זה שהתמזג — `moRange` הפעיל + `days=30` חם, לפי הממצא הקיים ב-`AUDIT-B171b.md` D12) — וזה, לא timer סמוי נוסף, ההסבר ל"16=2×8".

**`getMarketState` נבדק ונמצא תקין**: משתמש ב-`Intl.DateTimeFormat` עם `timeZone: "America/New_York"` (מטפל ב-EDT/EST אוטומטית, `src/priceService.js:24-43`) — ⛔ לא ניחוש offset קבוע, ⛔ לא "מחזיר OPEN תמיד". לא נמצאה סיבה לחשוד בו; ההנחה שהובילה לחשד בו (§1 למעלה) הופרכה.

---

## הגל הקטן של 1-2 בין הספייקים

`get_runtime_errors` (חלון 24h, פרויקט הנוכחי) חושף **קבוצת-שגיאות נפרדת** התואמת:

> `Twelve Data health check failed: HTTP 429 ... You have run out of API credits for the day` — **count=85, users=33**, `first=2026-08-24T13:17:07Z, last=2026-08-24T19:51:16Z`, `lastDeployment=dpl_GuUXg3cXeyUyGLsJt5kn1hiTSxoq` (**הדיפלוימנט הנוכחי** — כלומר זה עדיין קורה **גם אחרי** התיקון).

זה `checkTwelveData` (`api/health.js:86-92`, פינג יחיד ל-`AAPL`, 1 credit) — מופעל ע"י Sentinel (`.github/workflows/sentinel.yml`, `cron: '20,50 * * * *'`, כל 30 דק') **וגם** ע"י כל מבקר שטוען `/api/health` ישירות (אין throttle על הroute עצמו). ⚠️ **לא הושגה התאמה מלאה של הקצב**: Sentinel לבדו הוא כל 30 דק', לא כל ~5 דק' כמו שהגל-הקטן מופיע בגרף. `scripts/daily-digest.mjs:131` גם קורא ל-`/api/health` — נבדק: `daily-digest.yml` רץ **פעם ביום בלבד** (`cron: '0 4 * * *'` = 07:00 IL), ⛔ לא תורם לדפוס חוזר. **⇒ המקור הסביר ביותר הוא `checkTwelveData`, אבל הרזולוציה המדויקת (מי בדיוק מפעיל אותו כל ~5 דק', לא רק Sentinel) לא נסגרה — נשאר ⚠️ לא-פתור, מתועד ולא מוסתר.**

**חשוב:** קבוצת-השגיאה הזו היא **מכסה יומית** (800/day, זה עדיין ה-B-171 המקורי, לא ה"16=2×8"), ⛔ לא "current minute". זו נמשכת גם היום, אחרי התיקון — עקבי עם מה שניב עצמו כבר תיעד בקומיט `a6e3069`: "the per-minute merge fix... is not guaranteed by itself to satisfy the 24h/0%-429 closure condition." זה **לא** ממצא חדש, זו המשך ידוע של הבעיה שהמיזוג לא נועד לפתור.

---

## איתות חיובי (זהיר, לא מסקנה סופית)

קבוצת-השגיאה **"current minute... 16 API credits... limit 8"** (D12 המקורי) מופיעה ב-`get_runtime_errors` עם `first=12:01:41Z, last=12:17:31Z` — **בלבד**, `lastDeployment=dpl_57QAAS4pDRxxTR1P1ooV8h1GkFZe` (`422beaf`, דיפלוימנט **ישן**, לפני התיקון). **מאז `2d95218` עלה (17:35 UTC) ועד רגע כתיבת שורה זו (~19:54 UTC, כ-2.3 שעות), לא נרשמה אף שגיאת "current minute" חדשה.** זה איתות מעודד, ⚠️ **אבל חלון קצר מדי בשביל קביעה** — לא תחליף לתנאי-הסגירה של 24 שעות/0%-429 שניב קבע במקום אחר.

---

## מה בוטל, ומה לא נבדק (לפי היקף מפורש בפרומפט)

- D17/D18/D20/D21 — **מבוטלים כמבוקש**, לא נבדקו.
- ⛔ לא נבדק: מנגנון מדויק שגורם ל"גל הקטן" כל ~5 דק' (רק Sentinel כל 30 דק' אותר בוודאות).
- ⛔ לא הושגה התאמת bundle-hash 100% (רק ראיה מצטברת חזקה — ראה D19-הרחבה).

---

## תוספת 24.08 (לאחר אישור ניב, `b1243aa`) — לקח מתועד + תנאי מדידה נקייה

**ניב אישר את שלוש ההפרכות, ובעצמו זיהה את הלקח המכריע — מתועד כאן כנדרש:**

> "הכרזתי 'התיקון לא עבד' על מדידה שנלקחה **לפני שהוא נפרס**. זו אינה טעות
> חישוב — זו מסקנה בלי לבדוק מתי הקוד עלה. §8.1 בצורתו הבסיסית."

🔴 **הלקח (לרישום, ⛔ לא לעריכה מחדש של הממצאים למעלה):** D19 ("האם התיקון
בכלל פרוס?") חייב להימדד **לפני** שמסיקים מסקנה על התנהגות התיקון — ⛔ לא
כתשובת-ביניים, ⛔ לא אחרי שכבר נכתבה מסקנה על "כשל". בסבב הזה D19 בוצע
כחלק מהאבחון (§8.1 מלא), וזה מה שחשף את הפער — אבל ההנחה המקורית בפרומפט
("האיחוד נכנס לתוקף וכשל") נכתבה **לפני** שנבדק מתי הקוד עלה. הכלל הנגזר:
**כל טענת "X לא עבד" הנשענת על תצפית בזמן-אמת חייבת לשאת חותמת-זמן של
התצפית + חותמת-זמן של הפריסה הנבדקת, ושתיהן מוצלבות במפורש**, ⛔ לפני
שהמסקנה נכתבת — לא רק כשהאבחון כבר רץ.

⇒ **`B-171` נשאר פתוח.** המדידה הקיימת (`AUDIT-B171b`+`AUDIT-B171c`) מוכיחה
שהקוד **תקין ופרוס**, ⛔ לא שהוא **פתר את בעיית ה-800/יום** — זה עדיין לא
נמדד חי על קוד חדש בלבד. תנאי-הסגירה, כפי שהוגדרו ע"י ניב:

**M1 — חלון 24 שעות** מ-20:35 IL (24.08, מועד פריסת `2d95218`) עד 20:35 IL
(25.08). ⛔ לא פחות. ניב מצלם דשבורד TwelveData אחרי 20:35 IL ב-25.08.

**M2 — המונה מתאפס בחצות UTC (03:00 IL), ⛔ לא בחצות IL.** ⇒ הצילום של
25.08 יראה מונה שהתחיל להצטבר ב-03:00 IL 25.08 — **17.5 שעות של קוד חדש
בלבד** (03:00→20:35), ⛔ לא 24. יש לציין זאת **במפורש** כשה-`C-032` נסגר,
⛔ לא לטעון "מדידת 24 שעות".

**M3 — סף הצלחה:** **<800/יום**. קו-בסיס (קוד ישן, המכסה המקורית שהובילה
ל-B-171): **2,389**. ⇒ אם המונה יורד ל-**~1,200** — האיחוד עבד (חצה את מספר
הבקשות המקבילות בערך פי-2) אבל הבעיה השארית היא **תדירות** (עדיין נדרש
צמצום נוסף). אם ל-**<800** — `B-171` נסגר. אם נשאר **~2,400** (ללא שינוי
מובהק) — התיקון ⛔ לא עבד באמת, וחוזרים לאבחון.

**M4 — הגל הקטן (1-2 קריאות בין הספייקים, כל ~5 דק').** ⚠️ לא סגור ב-D19-ב
למעלה: Sentinel (`.github/workflows/sentinel.yml`, `cron: '20,50 * * * *'`)
רץ כל **30 דק'** — ⛔ אינו יכול להסביר קצב של ~5 דק' לבדו. `checkTwelveData`
(`api/health.js:86-92`) הוא המועמד המוביל (ראה §"הגל הקטן" למעלה — קבוצת-
שגיאת 429-יומי תואמת), אבל **מי בדיוק קורא לו כל ~5 דק'** ⛔ לא זוהה
בוודאות. ⇒ **למדוד עד 25.08**, ⛔ לא להניח.

⇒ `C-032` עודכן (`docs/CHECKS.md`) עם M1-M4 כתנאי-גלישה מפורש. `B-171`
ב-`docs/STATE.md` עודכן עם ההפרכה ועם קישור לתוספת הזו. **⛔ מרשם בלבד —
אפס שינוי קוד בתוספת הזו.**

---

## עצרתי

⛔ אין קוד. ⛔ אין Plan Mode. ממתין להכרעת ניב.
