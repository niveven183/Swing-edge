# AUDIT Q1 — האם `B-184`·`B-185`·`B-186` ניתנים לתיקון בלי לגעת בשכבת התצוגה

> **שאלת ניב, 27.08:** «האם שלושתם ניתנים לתיקון **בלי לגעת** בשכבת התצוגה
> (`:972` · `:3946` · `tokens.css`)? מדוד את קבצי/שורות היעד של כל אחד.
> 🔴 אם אחד מהם נוגע באותן שורות כמו `B-246`/`B-254` — אמור זאת, כי אז הם גל אחד
> ⛔ ולא שניים. ⛔ **אל תחליט לאחד** — החזר את המדידה ואני מכריע.»
>
> **סוג:** מדידה read-only. ⛔ **0 קבצי קוד שונו · 0 שורות קוד נגעו.**
> **סיווג (§15):** `T1` — תשובות `לא`/`לא`/`לא`/`לא`/`לא`. (⚠️ שאלה 4 ⛔ אינה
> סופרת `docs`; שאלה 5 — הנחת הפרומפט **היא** מה שנמדד כאן.)
> ⛔ **הדוח ⛔ אינו מכריע אם לאחד את הגלים** — ההכרעה של ניב (§14).

---

## 0 · תיקון מדידה קודמת — ⛔ נמסרה ואינה נכונה

בסבב הקודם דיווחתי מיפוי בעלוּת-קומפוננטה שנתן `3946` · `4076` · `4503` · `6672`
**כולם** ל-`SectorThemeCard (:1134)`, וקראתי לזה «סורק שבור». **המדידה החוזרת מראה
ששתי המסקנות היו שגויות בכיוונים הפוכים:**

| | |
|---|---|
| **מבנה הקובץ בפועל** | `SwingEdge_App.jsx` = **8,189 שורות**. **36 הצהרות בעמודה 0** משתרעות `:16`–`:1134`. אחריהן **הצהרה אחת בלבד**: `export default function SwingEdge()` ב-**`:1159`**, שגופה נמשך עד סוף הקובץ. |
| **⇒** | **`:1159`–`:8189` = 7,031 שורות = 85.9% מהקובץ בקומפוננטה אחת.** |
| **⇒ מה שהיה שגוי בדיווח הקודם** | ⓵ הסורק ⛔ **לא** «לא מצא גבולות» — הוא צדק שאין הצהרת-עמודה-0 אחרי `:1134`; ⓶ אבל הוא **פספס את `export default function`** (הרגקס דרש `const\|function` מיד אחרי `export`) ⇒ ייחס את 7,031 השורות ל-`SectorThemeCard` במקום ל-`SwingEdge`. |
| **⇒ המסקנה שנשארת** | **בעלוּת-קומפוננטה ⛔ אינה יכולה להבדיל כאן.** `B-185`·`B-186`·`B-254` יושבים **כולם** בתוך `SwingEdge()` — כמו 86% מהקובץ. ⇒ **המבחן היחיד בעל כוח-הפלייה הוא חפיפת שורות**, ⛔ לא חפיפת קומפוננטה ו⛔ לא חפיפת קובץ. |

⚠️ **הדיווח הקודם ⛔ אינו «נמחק» — הוא נרשם כאן כי הוא נמסר.**

---

## 1 · שכבת התצוגה — הגדרת קבוצת-היעד, מ-`BACKLOG` ⛔ לא מהזיכרון

| פריט | קובץ:שורות שהתיקון **עורך** |
|------|------------------------------|
| **`B-246`** (דירוג) | `SwingEdge_App.jsx:954-984` (`StatCard`, כולל `:972`) · `SwingEdge_App.jsx:4339-4347` (שורת ה-`lg:grid-cols-6`) |
| **`B-254`** (Inter inline שובר RTL) | `SwingEdge_App.jsx:3946` (מעטפת האפליקציה) · `SwingEdge_App.jsx:3926` (מסך הטעינה) · `src/index.css:67` · `src/design/tokens.css:71` (`--font-hebrew`) |
| **`B-245`** (ניגודיות) | `src/design/tokens.css:29` (`--text-muted: #9CA89A`) + 27 צמתי-טקסט |

**קבוצת השורות המוגנת** = `SwingEdge_App.jsx:{954-984, 3926, 3946, 4339-4347}`
∪ `src/index.css:{67}` ∪ `src/design/tokens.css:{29, 71}`.

---

## 2 · `B-184` — טוקן סשן אל צד שלישי

| קובץ | שורות | מה יש שם |
|------|-------|-----------|
| `src/main.jsx` | **`:13`** | `import { inject } from "@vercel/analytics";` |
| `src/main.jsx` | **`:15`** | `inject();` — ⛔ ללא שער הסכמה, ⛔ ללא הצמדת `page_location` |
| `scripts/analytics-contract-test.mjs` | 201 שורות (`:167` אסרציה 12) | שער החוזה. שומר על **`gtag` בלבד** ⇒ ⛔ **אינו יכול** לראות את הערוץ השני |
| `index.html` | `:140-144` | **התקדים** — `gtag('config', …, { page_location: location.origin + location.pathname })` אחרי `INCIDENTS#12` |
| `src/lib/consent.js` | 13 ייצואים (`:87` `grantAnalytics` · `:110` `trackEvent`) | השער שהערוץ השני חייב להצטרף אליו |
| `package.json` | `:43` | `"@vercel/analytics": "^2.0.1"` |

**נמדד מכנית — כל ההפניות ל-`@vercel/analytics` בריפו (⛔ ללא `node_modules`/`dist`):**
`package.json:43` · `package-lock.json:14,1935` · `src/main.jsx:13,15`. **סה״כ 2 שורות קוד.**

> ### 🟢 `B-184`: **`SwingEdge_App.jsx` ⛔ אינו נפתח כלל.** ⛔ 0 חפיפה — ⛔ לא בשורה, ⛔ לא בקובץ.

---

## 3 · `B-185` — מחירים קשיחים תחת `🟢 LIVE`

**שני חצאים נפרדים, כפי שה-`NEXT` כבר אומר.**

**חצי א׳ — מסלול ה-fallback:**

| קובץ:שורה | מה יש שם |
|-----------|-----------|
| `SwingEdge_App.jsx:321-360` | `SCANNER_DATA` — **module scope**, מחירים קשיחים (`NVDA 142.30` · `PLTR 97.80` …) |
| `SwingEdge_App.jsx:1636` | `DEFAULT_WATCHLIST` מעתיק `s.price`/`s.change` מ-`SCANNER_DATA` **אל תוך state** |
| `SwingEdge_App.jsx:1799` · `:1840` | ⚠️ ה-state נשמר ל-Supabase **ול-`localStorage`** ⇒ **המחירים הקשיחים הופכים לנתון-משתמש שמור**, ⛔ לא לברירת מחדל בזיכרון |
| **`SwingEdge_App.jsx:6672-6673`** | `const price = lp?.price ?? s.price;` · `const changePct = lp?.changePct ?? s.change ?? 0;` |
| `SwingEdge_App.jsx:6701-6710` | הרינדור. ⚠️ **ענף `price != null` כבר קיים** ומרנדר `—`/ספינר (`:6710`) ⇒ מצב ה"⛔ לא ידוע" **כבר בנוי**; ה-`??` הוא מה שמונע ממנו להידלק |

**חצי ב׳ — התג:**

| קובץ:שורה | מה יש שם |
|-----------|-----------|
| **`SwingEdge_App.jsx:4076`** | `const badge = getMarketStateBadge(marketState);` |
| `SwingEdge_App.jsx:4083` | `opacity: marketState === MARKET_STATE.OPEN ? …` |
| `SwingEdge_App.jsx:4095-4097` · `:4883-4885` | `pricesLastUpdated` **כבר מוצג** — חותמת אמיתית לצד תג שאינו אמיתי |
| `src/priceService.js:443-449` | `getMarketStateBadge` — נגזר מ**שעון** (`:47-52` שעון NY), ⛔ לא ממדידת חיוּת |

**חפיפה לשורות המוגנות:** ⛔ **אפס.** המרחק הקטן ביותר הוא `:4076` מול `:4339` — **263 שורות**.

> ### 🟢 `B-185`: ⛔ 0 חפיפת שורות. **אבל שלוש נקודות-מגע שאינן חפיפה, ומדווחות במפורש:**
>
> ⓵ **`src/priceService.js:445` נושא את הליטרל `#10b981`** — **בדיוק** הצבע שב-`C1` נמדד `2.54:1` על לבן ואושש כפריט היחיד ששרד מ-`B-245`. ⇒ **אותו ערך, ⛔ קובץ ושורה אחרים.** נגיעה ב-`B-185` ⛔ אינה מחייבת נגיעה בו.
> ⓶ **`:6704` צורך `var(--v3-accent)`/`var(--v3-loss)`** = `tokens.css:122`/`:128` — **⛔ לא `:29`**, שהוא היעד של `B-245`. ⇒ קובץ משותף, שורות נבדלות.
> ⓷ **הכרעת ניב הפתוחה** («ערך אחרון עם חותמת, או `—`») נוחתת ב-`:6701-6710` ו-`:4076-4098` ⇒ **עבודת תצוגה, ⛔ לא שורות התצוגה הרשומות.**

---

## 4 · `B-186` — אחוז P&L חי ב-SHORT

| קובץ:שורה | מה יש שם |
|-----------|-----------|
| **`SwingEdge_App.jsx:4503`** | `? (tr.side === "LONG" ? ((currentPrice / tr.entry) - 1) * 100 : ((tr.entry / currentPrice) - 1) * 100)` |

**נמדד — האם האתר יחיד?** חיפוש על `entry / currentPrice` · `currentPrice / ` · `entry) - 1` · `/ tr.entry` בכל `src/` + `SwingEdge_App.jsx` החזיר **התאמה אחת בדיוק: `:4503`**.
`src/components/MobileTradeCard.jsx` — **0 התאמות** ל-`livePnl`/`livePnlPct` ⇒ ⛔ **אין אתר שני שיסחף**.
⚠️ `:4498` (סכום ה-P&L, ⛔ לא האחוז) **נכון** בשני הענפים — הבאג ב-`:4503` בלבד.

> ### 🟢 `B-186`: **שורה אחת. ⛔ 0 תלויות. ⛔ 0 חפיפה.** המרחק לשורה מוגנת: `:4503` מול `:4339` — **164 שורות**.

---

## 5 · תשובת Q1 — הצומת

**קבוצת השורות של `B-184` ∪ `B-185` ∪ `B-186` ∩ קבוצת השורות המוגנת = ∅.**

| | `SwingEdge_App.jsx` | `tokens.css` | `index.css` | `main.jsx` | `priceService.js` | `analytics-contract-test.mjs` | `index.html` |
|---|---|---|---|---|---|---|---|
| `B-246` | `954-984`·`4339-4347` | — | — | — | — | — | — |
| `B-254` | `3926`·`3946` | `71` | `67` | — | — | — | — |
| `B-245` | — | **`29`** | — | — | — | — | — |
| **`B-184`** | — | — | — | **`13`·`15`** | — | **✔** | **`140-144`** |
| **`B-185`** | `321-360`·`1636`·`1799`·`1840`·**`4076`**·**`6672-6673`**·`6701-6710` | — | — | — | **`443-449`** | — | — |
| **`B-186`** | **`4503`** | — | — | — | — | — | — |

**⇒ שלושתם ניתנים לתיקון בלי לגעת ב-`:972`, ב-`:3946` או ב-`tokens.css`. כן — שלושתם.**

### 5.1 ⚠️ מה שכן משותף, ומדווח כי נשאלתי

- **קובץ משותף:** `B-185`+`B-186` חולקים את `SwingEdge_App.jsx` עם `B-246`+`B-254`. אבל **86% מהקובץ הוא קומפוננטה אחת** (§0) ⇒ «אותו קובץ» כאן ⛔ אינו אות לכלום. `git` ⛔ אינו מתנגש — hunks נבדלים.
- **קובץ משותף:** `B-185` נוגע ב-`tokens.css:122`/`:128`; `B-245` ב-`:29`. **⛔ לא אותה שורה.**
- 🔴 **סחיפת מספרי שורה — הצימוד היחיד שנמדד ואינו אפס.** עריכות `B-254` (`:3926`·`:3946`) ו-`B-246` (`:954-984`) יושבות **מעל** כל יעדי ה-P0 (`:4076`·`:4503`·`:6672`). ⇒ **הגל שירוץ שני חייב לאתר את יעדיו מחדש לפי תוכן, ⛔ לא לפי המספרים הרשומים כאן.** ⚠️ זה ⛔ **אינו** נימוק לאיחוד ו⛔ אינו נימוק לפיצול — זו דרישת מדידה-מחדש שחלה על כל סדר שייבחר.

---

## 6 · מה הדוח הזה ⛔ אינו קובע

- ⛔ **אינו מכריע אם לאחד את הגלים** — הכרעת ניב (§14).
- ⛔ **אינו מכריע את השאלה הפתוחה ב-`B-185`** («ערך אחרון עם חותמת, או `—`»).
- ⛔ **אינו מתחיל דבר.** `B-184`·`B-185`·`B-186` — ⛔ לא נגעתי. `B-246`·`B-254` — ⛔ לא נגעתי.
