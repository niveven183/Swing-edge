# AUDIT — אירוע רשת ההתראות, 17.08

> **סוג:** אבחון read-only (§8.1 שלב 1). ⛔ **אפס תיקון בוצע.**
> **סיווג:** `T3` · תשובות: הפיכות=לא · אמון=**כן** · אבטחה=לא · רוחב=**כן** · ודאות=**כן** ⇒ 3/5, ושאלה 2 לבדה מספיקה.
> **אימות:** `אומת-מקור` — כל מספר בדוח נמדד מ-`gh api`/`gh run view` מול GitHub, ⛔ לא מהזיכרון ולא מהעץ המקומי.
> **HEAD בזמן האבחון:** `37e7f13` · עץ נקי · `git pull --ff-only` = `Already up to date`.
> **חלון האירוע:** 2026-08-17, **15:02Z–16:15Z** (18:02–19:15 מקומי, UTC+3).

---

## 0. תקציר — שלוש הנחות שהופרכו

| # | ההנחה (בפרומפט) | הממצא הנמדד |
|---|------------------|--------------|
| 1 | «Triage טיפל ושלח מייל» | ❌ **מופרך.** `Triage` 32041853172 = `failure`, **צעד אחד** (`Set up job`). ⛔ אף מייל לא נשלח. |
| 2 | «קומיט docs-only» מסביר את כשל ה-Smoke | ❌ **מופרך.** הכשל המוקדם 32041053330 הוא על `e5afdc7` = `feat(stats)`, קומיט **קוד**, זהה לחלוטין. |
| 3 | `class:config` ⇒ פגם בהגדרות הריפו | ❌ **מופרך.** השער התנהג **בדיוק כמתוכנן**; הסיבה חיצונית. |

**השאלה המכריעה של הפרומפט — «האם השער עבד או נשבר?» — ⛔ אף אחת מהשתיים.**
🔴 **מצב רביעי:** הג'וב מת ב-`Set up job`, **לפני צעד 1**. השער ⛔ מעולם לא רץ ⇒ ⛔ לא "עבד" ו⛔ לא "נשבר".

🔴 **השורש חיצוני:** `codeload.github.com` החזיר **429 Too Many Requests** חוצה-ספקים — כולל `actions/download-artifact` של **GitHub עצמה**. ⇒ ⛔ **לא ניתן למנוע · רק לזהות.** ההבחנה הזו קובעת מה התיקון יכול להיות בכלל.

---

## D1 — באיזה מסלול `exit 1` ירה? ⛔ באף אחד

**ריצה:** `Failure Alert` **32041897972** · 15:18:11Z · על `37e7f13` · נורתה מכשל **`Triage`**.

**מבנה הג'וב — נמדד ב-`gh api .../jobs`:**
```
job "alert" · conclusion=failure · started 15:18:14Z · completed 15:19:26Z (72s)
steps: [ { number:1, name:"Set up job", conclusion:"failure" } ]     ⇐ צעד אחד. זהו.
```

**הלוג — מצוטט מילה במילה, ⛔ ללא פרשנות:**
```
15:18:16.0705169Z Download action repository 'dawidd6/action-send-mail@v3' (SHA:4226df7daafa6fc901a43789c49bf7ab309066e7)
15:18:26.8305657Z ##[warning]Failed to download action 'https://codeload.github.com/dawidd6/action-send-mail/tar.gz/4226df7daafa6fc901a43789c49bf7ab309066e7'. Error: Response status code does not indicate success: 429 (Too Many Requests). 4C40:1E359A:1FB5FA:1FE60B:6A832638
15:18:26.8322008Z ##[warning]Back off 26.85 seconds before retry.
15:18:55.4365043Z ##[warning]Failed to download action 'https://codeload.github.com/dawidd6/action-send-mail/tar.gz/4226df7daafa6fc901a43789c49bf7ab309066e7'. Error: Response status code does not indicate success: 429 (Too Many Requests). 4C40:1E359A:28B4D5:28E599:6A83265D
15:18:55.4367401Z ##[warning]Back off 27.032 seconds before retry.
15:19:25.6653754Z ##[error]Response status code does not indicate success: 429 (Too Many Requests).
15:19:25.6667567Z ##[error]Failed to download archive 'https://codeload.github.com/dawidd6/action-send-mail/tar.gz/4226df7daafa6fc901a43789c49bf7ab309066e7' after 3 attempts.
```

**פסק דין על שלושת המסלולים שהפרומפט מנה:**

| מסלול | מה הוא | ירה? | למה לא |
|--------|---------|------|---------|
| `failure-alert.yml:83` | `SENTINEL_DISCORD_WEBHOOK not set` | ⛔ לא | הצעד `Report to Discord` ⛔ מעולם לא רץ |
| `:94` | `Discord rejected the alert (HTTP $code)` | ⛔ לא | אותו דבר |
| `:117` | `ההתראה ⛔ לא נמסרה באף ערוץ` | ⛔ לא | `Delivery gate` נושא `if: always()` — ו-`always()` ⛔ **אינו יכול לרוץ בג'וב שלא התחיל** |

⚠️ **הכותרת של `failure-alert.yml` עצמו (שורות 13–19) מונה בדיוק את מחלקת-הכשל הזו** — עבור `Restore Drill`:
> «`always()` cannot run in a job that never started: broken YAML, a postgres service container that never turns healthy, a cancelled run, or a timeout.»

🔴 **כשל הורדת action ⛔ אינו ברשימה הזו** — והוא המצב שהרג את **הצופה עצמו**. הרשימה נכתבה 15.08 כתיקון להצהרה קודמת שהכריזה מצב כבלתי-אפשרי בעודו אפשרי; היא עדיין ⛔ אינה ממצה.

---

## D2 — האם ההתראה הגיעה? ⛔ לא. 0/2, והשער ⛔ לא מדד

**מדידה ישירה:** ⛔ **אין שורת `discord POST -> HTTP`** בלוג · ⛔ **אין שורת `channels delivered:`**. הצעדים ⛔ לא רצו.

⚠️ ⛔ **זו אינה תקלת ערוץ** (ה-webhook וה-SMTP תקינים — ראה D4) ו⛔ **אינה באג בשער**. שני הערוצים היו בלתי-נגישים משום שלג'וב ⛔ לא היו צעדים כלל.

### שרשרת ההשתקה — שתי שכבות עמוק

| # | רכיב | רשימת הצפייה שלו | תוצאה |
|---|-------|-------------------|--------|
| 1 | `Smoke Tests` **32041651321** | — | `failure` 15:17:16Z |
| 2 | `Triage` **32041853172** | `["Smoke Tests", "Build", "Supabase Backup"]` ⇒ **תפס** | 🔴 **מת ב-`Set up job`** · אותו 429 · אותו action (15:17:29Z→15:18:06Z) |
| 3 | `Failure Alert` **32041897972** | `[…, "Triage", …]` ⇒ **תפס** | 🔴 **מת ב-`Set up job`** · אותו 429 |

⇒ 🔴 **כשל ה-Smoke דווח ל⛔אף אחד. 0 ערוצים, בשתי שכבות בלתי-תלויות שנפלו מאותה סיבה בדיוק.**

⚠️ **הנחה 1 מופרכת כאן:** `Triage` ⛔ לא "טיפל ושלח מייל" — מסקנתו `failure` עם צעד אחד.

### רוחב הפגיעה — ה-429 ⛔ אינו action אחד

| action | ריצות שנפגעו |
|--------|---------------|
| `dawidd6/action-send-mail@v3` | `Triage` 15:17 · `Failure Alert` 15:18 · `Failure Alert` 16:03 |
| `actions/download-artifact@v7` | `Sentinel` job `watch` 16:02–16:03 — ⚠️ **action של GitHub עצמה** |

⇒ 🔴 **`codeload.github.com` מוגבל-קצב חוצה-ספקים ⇒ תקלה בצד GitHub, ⛔ לא config של הריפו.**

**הזוג השני (16:01–16:05Z):** `Sentinel` **32044102616** — job `browser` **success** (13 צעדים, הושלם) · job `watch` **failure** ב-`Set up job`. `Failure Alert` **32044223847** נורה עליו ומת באותה דרך בדיוק.

### כל הכשלים של 17.08

```
15:02:21Z  32041053330  Smoke Tests    ⇐ e5afdc7, שער הפריסה
15:13:31Z  32041651321  Smoke Tests    ⇐ 37e7f13, שער הפריסה
15:17:21Z  32041853172  Triage         ⇐ 429
15:18:11Z  32041897972  Failure Alert  ⇐ 429
16:01:21Z  32044102616  Sentinel       ⇐ 429 (job watch בלבד)
16:03:47Z  32044223847  Failure Alert  ⇐ 429
19:08:08Z  32058659008  🐤 Canary Drill ⇐ ⚠️ אדום בכוונה — התרגיל של D4, ⛔ לא תקלה
```

---

## D3 — Smoke / `vercel-deploy-gate`: `class:config` מופרך

**הצעד שנכשל:** #6 `Wait for this commit's deploy to be live` (`smoke.yml:57`). 18 סקרים, כולם ריקים:
```
t=1s   sha=37e7f13 deployments=[] states=[ ]
t=12s  sha=37e7f13 deployments=[] states=[ ]
…  (16 שורות נוספות, כולן זהות)  …
t=188s sha=37e7f13 deployments=[] states=[ ]
##[error]No successful Production deployment for 37e7f13 within 180s. Failing on purpose:
         testing whatever is live would be a false ✅ against the previous deploy.
```

⚠️ **`api_error` ⛔ נעדר מכל 18 השורות** ⇒ נבחר הענף ה**נקי-ריק** (`smoke.yml:110`), כלומר «Vercel never got there» — ⛔ **לא** «we could not look» (`:108`). ההפרדה הזו נבנתה בכוונה ב-`smoke.yml:105-107`, והיא זו שמאפשרת את האבחנה הזו עכשיו.

### רשומות ה-deploy **כן** הגיעו — באיחור קיצוני

| קומיט | קומיט (UTC) | רשומת Production | פער |
|-------|--------------|-------------------|-----|
| `37e7f13` | 15:13:28 | 16:15:31 | 🔴 **+62מ׳ 03ש׳** |
| `e5afdc7` | 15:02:14 | 15:50:20 | 🔴 **+48מ׳ 06ש׳** |
| `b20693a` | 11:41:50 | 11:42:24 | +34ש׳ |
| `7139064` | 11:09:24 | 11:09:58 | +34ש׳ |
| `e68df2b` | 10:58:03 | 10:58:37 | +34ש׳ |
| `90ca42e` | 16.08 20:22:22 | 20:22:55 | +33ש׳ |
| `6082f60` | 16.08 19:57:00 | 19:58:13 | +73ש׳ |

**מכנה מוצהר:** 7 קומיטים רצופים. **5/7 מחוץ לחלון האירוע ⇒ בסיס 33–73 שניות. 2/7 בתוכו ⇒ 48–62 דקות = ~50–110× הבסיס.**

⇒ 🔴 **תקרת 180ש׳ היא 2.5–5.5× הבסיס הנמדד — ⛔ אינה סף שגוי.** השער סירב ל-✅ כוזב **בדיוק כפי שהערת ה-`smoke.yml:38-41` מתארת**: «A false green is worse than a red». ⛔ **אין כאן מה לתקן בהגדרות.**

**⚠️ תצפית נלווית, ⛔ לא חלק מהאירוע:** ל-`1fc6170` (16.08 19:56:01Z) ⛔ **אין** רשומת Production כלל; `6082f60` בא 59 שניות אחריו ⇒ ככל הנראה build שנדחק. **⛔ לא אובחן** — נרשם כתצפית בלבד.

---

## D4 — 🟢 הקנרית: הרשת **חיה כרגע**

**T-A** (`🐤 Canary Drill`, `mode=fail`) שוגר ב-**19:08:09Z** ⇒ קנרית **32058659008** = `failure` (מכוון) ⇒ `Failure Alert` **32058677953** = **`success`**:
```
discord POST -> HTTP 204
mail step outcome : success
discord delivered : true
channels delivered: 2/2
```

⚠️ **צעד המייל רץ** ⇒ `dawidd6/action-send-mail@v3` **ירד בהצלחה** ב-19:08 — **אותו action, אותו SHA** שנכשל שלוש פעמים ב-15:17–16:05.
⇒ 🔴 **ה-429 חלף. הכשל היה נקודתי (~15:17–16:05Z), ⛔ לא רשת שבורה.**

### `C-026` — שתי שכבות, נרשמות **בנפרד**

| שכבה | מה נמדד | מצב |
|-------|----------|------|
| **מכנית (המשגר)** | `HTTP 204` · `mail outcome: success` · `channels delivered: 2/2` | ✅ |
| **עין — מייל** | ניב צילם את התיבה 22:08 מקומי: `🚨 🐤 Canary Drill — התראת-בדיקה נכשל` + `runs/32058659008` | ✅ **אומת** |
| **עין — דיסקורד** | — | ⏳ **נותר** |

⚠️ **`2/2` ⛔ אינו «הגיע»** — הוא HTTP 204 מ-Discord ונושא-מייל שנבנה, ושתיהן מדידות של ה**משגר**. זו בדיוק ההבחנה ש-`C-026` קיים בשבילה (‹R-4›). ⇒ **`C-026` חצי-אומת: ערוץ המייל סגור בעין, דיסקורד פתוח.**

⚠️ **ריצה 32058659008 אדומה בכוונה** — היא התרגיל, ⛔ לא תקלה. מי שיקרא את רשימת הכשלים של 17.08 בלי השורה הזו יספור כשל שביעי שאינו קיים.

---

## D5 — `B-150`, מופע שני, ועכשיו **נמדד**

**המופע:** `Sentinel` **32047327001** (16:48:25Z) · `browser-findings-auth.json` · `severity: amber` · `fp: browser-auth|console_error`:
```
Failed to load resource: the server responded with a status of 404 ()
  @ https://fonts.gstatic.com/s/heebo/v28/NGScv5_NC0k9P_v6ZUCbLRAHxK1EiSz-eWFXF9ExWxtXqIFYL-WYL4o0.woff2
  @ https://fonts.gstatic.com/s/heebo/v28/NGScv5_NC0k9P_v6ZUCbLRAHxK1EiSz-eWFXF9ExWxtXqIFYL4mYL4o0.woff2
  @ https://fonts.gstatic.com/s/heebo/v28/NGScv5_NC0k9P_v6ZUCbLRAHxK1EiSz-eWFXF9ExWxtXqIFYL-eYLw.woff2
```

### תדירות — נמדדה, ⛔ לא הונחה

**14 ריצות `Sentinel` רצופות, 11:11:46Z → 18:59:58Z (חלון 7ש׳ 48ד׳):**

| מדד | תוצאה |
|------|--------|
| `Heebo` 404 | 🔴 **2/14** — 12:01:01Z · 16:48:25Z |
| `JetBrains Mono` 404 | **0/14** |
| `browser-findings.json` (מסך מנותק) | `[]` בשני המופעים ⇒ נצפה **רק במסך המחובר** |

**קבוצת ה-URL משתנה בין המופעים:** ב-12:01 — `…L4mYL4o0` · `…L-WYLw` · `…L-WYL4o0`; ב-16:48 — `…L-WYL4o0` · `…L4mYL4o0` · `…L-eYLw`. **2 מתוך 3 משותפים, אחד שונה בכל מופע.**

### מה זה משנה ב-`B-150`

1. 🔴 **משפחת הפונט ⛔ אינה המשתנה.** 15.08 = `JetBrains Mono`; 17.08 = `Heebo`. **אותו מארח** `fonts.gstatic.com` ⇒ הפגם הוא ב**מארח**. ⚠️ **זו מדידה, ⛔ לא הכרעה** — היא **מחזקת** את חלופה (א) self-host, ו⛔ אינה בוחרת בה.
2. 🔴 **ההשלכה גדולה מהרשום.** `Heebo` הוא פני ה-UI ה**עברי** ⇒ **כל הטקסט העברי** נופל לפונט חלופי, ⛔ לא רק הספרות (זו הייתה ההשלכה של `JetBrains Mono`).
3. ✅ **«אירוע יחיד · ⛔ לא נמדדה תדירות» מוחלף ב-2/14 בחלון מוצהר** ⇒ **ריצוד מאושש**, ⛔ לא מצב יציב.
4. ⚠️ **`R-4` שריר.** עדיין `amber` בלבד, ועדיין ⛔ אין שומר שמודד **נכונות רינדור** — `document.fonts.check()` מחזיר `true` גם בנפילה ל-fallback.

⛔ **לא תוקן — מדידה בלבד, לפי הוראה.**

---

## 1. המשמעות ל-`B-001` — מצב הכשל הרביעי

`D-036` (15.08) סגר את המקרה שבו `Failure Alert` **רץ** ולא הצליח למסור. **זה המקרה שבו הוא ⛔ לא הצליח להתחיל.**

🔴 **הפגם המבני:** `Delivery gate` — **כל מנגנון הפסיקה של הריצה** — יושב **בתוך הג'וב שאותו הוא אמור לשפוט**. ג'וב שמת ב-`Set up job` מייצר `conclusion: failure` ומגיע ל⛔אף אחד, ו-`if: always()` ⛔ אינו יכול להציל אותו — `always()` הוא תנאי על **צעד**, וצעדים ⛔ לא נולדו.

🔴 **והגיבוי היחיד הוא מתאם ההתראות ב-`fleet-daily`** — שרץ לאחרונה **06:38:02Z**, כלומר **~8.5 שעות לפני האירוע**, והבא ב-18.08 ⇒ **עד ~24 שעות עיוורון.** ⚠️ הפער הזה ⛔ אינו פריט נפרד — הוא **חלק מאותו מזהה**, כי הוא הגיבוי היחיד שקיים.

**⚠️ כיוונים — ⛔ אף אחד לא הוכרע:**

| כיוון | הערה נמדדת |
|--------|--------------|
| pinning ל-SHA | ⛔ **אינו פותר** — ההורדות שנכשלו כבר היו ל-SHA מפורש (`4226df7…`) |
| מתאם חיצוני | מחוץ ל-Actions ⇒ ⛔ אינו נופל עם codeload; אבל תלות חדשה |
| retry ברמת ה-workflow | ⛔ הכשל הוא ב-`Set up job` — לפני שמשהו שלנו יכול לרוץ |
| הפחתת תלות ב-actions צד-ג׳ | `curl` ל-SMTP במקום `dawidd6/…`; אבל `actions/download-artifact` נפל גם הוא ⇒ ⛔ אינו כיסוי מלא |

⛔ **הכרעה ⛔ לא נדרשת בדוח הזה** (§8.1 שלב 1). הכיוונים רשומים כדי ש**לא** ייולדו מחדש מהזיכרון.

---

## 2. מזהים שנולדו

| מזהה | מה | מקור |
|------|-----|-------|
| **`B-163`** | מצב הכשל הרביעי — ג'וב שמת ב-`Set up job`; מנגנון הפסיקה בתוך הג'וב שהוא שופט; + פער 24ש׳ במתאם `fleet-daily` | הדוח הזה |
| **`B-164`** | איחור רשומת ה-deploy של Vercel — 48–62ד׳ מול בסיס 33–73ש׳ | הדוח הזה |
| **`B-150`** | ⚠️ **עודכן** — Heebo 2/14 · אותו מארח · קבוצת URL משתנה · רק מסך מחובר · כל העברית | הדוח הזה |
| **`INCIDENTS#17`** | האירוע עצמו — 47מ׳ 44ש׳ שקט ברשת ההתראות | הדוח הזה |

⚠️ **תוקן בזמן הרישום: `#16` **תפוס** — `INCIDENTS.md:593` נושא את אירוע 61 תוויות המטבע (11.08). הדוח נכתב במקור «`#16`» **מהזיכרון ⛔ ולא מקריאת הקובץ**, וזו בדיוק המחלקה שהמרשם קיים בשבילה. המזהה בפועל: **`#17`**.

⛔ **אפס נגיעה בקוד. אפס נגיעה ב-workflows. `docs` בלבד.**
