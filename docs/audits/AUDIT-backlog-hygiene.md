# AUDIT — היגיינת מרשם · חוב רישום ⛔ לא-עיצובי

> **תאריך:** 2026-08-27 · **HEAD:** `d5c0cae` · **רמה:** T2 (תשובות 1–5: לא/לא/לא/לא/**כן**)
> **סוג:** אבחון read-only לפי §8.1 שלב 1. ⛔ **אפס תיקון · אפס נגיעה בקוד · אפס שינוי במרשם.**
> **מנדט:** כל מספר שהגיע בפרומפט נמדד מחדש. מדידה שסותרת נכתבת כנמדד, והטענה מוכרזת מופרכת.

---

## 0 · תקציר — מה הופרך

| # | הטענה שהגיעה | הנמדד | פסק |
|---|---------------|--------|------|
| E1 | 59 פריטים מצטטים קובץ שאינו קיים | **1/258** מסלול שבור אמיתי | ⛔ **הופרכה** |
| E1′ | «המספר האמיתי הוא **0**» (הטענה-שכנגד) | **1** (`B-209`, נתיב מקוצר ב-`…`) | ⚠️ **גם היא הופרכה — ב-1** |
| E2 | 54/61 ממצאי DEEP בלי הצלבה | **60/61 נושאים הצלבה**; 1 בלי (`B-244`) | ⛔ **הופרכה** |
| A3 | `grep -c D-056 DECISIONS.md` = **0** | **1** · ובנוסף `D-` חי ב-`DONE.md` (§14) | ⛔ **הופרכה — הקובץ השגוי נמדד** |
| M1 | **163** פתוחים | **175** פתוחים | ⛔ **הופרכה — מדידת 26.08** |
| M3 | 11 ❄️ — האם הנימוק עדיין תקף | **0 סתירות** ⇒ **0 נפתחו** | ✅ **אוששה** |
| M-003 | שימור שבוע 1 = **0/6** | **0/9** | ⚠️ **המכנה גדל, המונה נשאר 0** |

**שיטת מדידה:** פיצול שורות `| B-nnn |` על `|` לא-מוברח (כמו `registry-test.mjs:96`).
**אוכלוסייה:** 258 שורות `B-` · 258 מזהים נבדלים · **⛔ אפס כפילות**.

---

## A1 · ארבעה מיזוגים — הכרעה אחרי מדידה

כל שמונת הפריטים נקראו במלואם, וכל מיקום קוד שצוטט נבדק מול העץ ב-`HEAD`.

### 1 · `B-222` ⊃ `B-003` ⇒ **מיזוג**

**נמדד:** `vercel.json:18` הוא אכן שורת ה-CSP · `grep -c "report-uri\|report-to" vercel.json` = **0** ✓ ·
`unsafe-inline` נוכח ✓ (ב-`script-src` וב-`style-src`).

**נימוק:** `B-222` מכיל את `B-003` **במלואו** — אותו קובץ, אותה שורה, אותה עובדת «0 אספנים» —
ומוסיף שתי עובדות ש-`B-003` ⛔ אינו נושא: `'unsafe-inline'` בתוך המדיניות, והסשן ב-`localStorage`.
`B-222` מצהיר זאת בעצמו («מרחיב את `B-003`»), ושורה 335 ב-`BACKLOG` כבר ממפה `B-003` (⇒ `B-222`).

> 🔴 **תנאי למיזוג — ⛔ אל תמזג בלעדיו:** `B-003` נושא `חוסם? = כן — חוסם אכיפת CSP`;
> `B-222` נושא `לא`. מיזוג כפי שהוא **מוחק חוסם**. `B-222` חייב לרשת את הדגל,
> אחרת אחד מ-**3** החוסמים היחידים במרשם (ראה M1) נעלם בשקט.

### 2 · `B-206` ⊃ `B-161` ⇒ **אגד-לגל · ⛔ לא מיזוג**

**נמדד:**
- `tradingStats.js:182` ו-`:220` — **מדויקים**. שניהם `[...x].sort((a,b) => (realizedAt(a) ?? 0) - (realizedAt(b) ?? 0))`. ה-`?? 0` ממפה כל `NULL` ל-0 ⇒ שוויונות המוניים, וההכרעה ביניהם נופלת על סדר הקלט שאינו מקובע. הדיאגנוזה «משווה לא-טוטלי» נכונה.
- `B-161` צוטט `:50`; **בפועל `:54`** — `if (closed.length === 0) return EMPTY_STATS(capital);`, ו-`EMPTY_STATS` קובע `openTrades: 0` (`:378`) בזמן שהערך האמיתי מחושב ב-`:279`. המנגנון אושש.
- **צרכני `stats.openTrades` במוצר: 0.** `grep -rn "openTrades" src/` מחזיר רק מפתחות `i18n` (`openTradesCount`) ואת `openTrades` של `fifoMatch`/`buildImport` — **מושג אחר**. טענת «איש אינו קורא» אוששה.

**נימוק:** ⛔ **אינם אותו באג** — החשד שבפרומפט אושש. `B-206` הוא **סדר**; `B-161` הוא **שדה מת**.
שורשים שונים (`B-206`→‹R-1› דרך `B-042` · `B-161`→‹R-6›, שני מקורות-אמת). תיקון אחד ⛔ אינו סוגר את השני.
⇒ **מזהים נפרדים**, אך שניהם ב-`src/lib/tradingStats.js` ⇒ ריצת `test:tradingstats` אחת ובדיקה אחת של אותו קובץ.

### 3 · `B-208` ↔ `B-063` ⇒ **אגד-לגל** (הטענה אוששה)

**נמדד:**
- `GrowthTracker.js:101-102` — **מדויק**: `emotionAtEntry === "FOMO"` · `exitReason === "Fear"`.
- `MonthlyReport.js:223` — `/fomo|fear|greed|revenge|fomo/i`. **`fomo` אכן כפול** ✓.
- `B-063` צוטט `MonthlyReport.js:220` + `statisticalModels.js:257`; **בפועל `:229` ו-`:274`** — בשניהם `if (pnl === 0) continue;` ⇒ break-even ⛔ אינו שובר רצף. המנגנון **חי בדיוק כפי שתואר**.

**נימוק:** ⛔ **לא כפילות** — אוצר-מילים רגשי מול לוגיקת break-even, מנגנונים זרים.
אך **שניהם נוגעים ב-`MonthlyReport.js` במרחק 6 שורות** (`:223` מול `:229`) ⇒ שתי עריכות נפרדות
לאותו בלוק. ⇒ **אגד-לגל.** ⚠️ הערה: ה-`continue` ב-`:229` יושב **בלי הערת נימוק**, בקובץ שבו
`?? 0` הסמוך נושא נימוק בן שתי שורות — כלומר ⛔ אין ראיה שהוא מכוון.

### 4 · `B-209` ⊃ `B-072` ⇒ **מיזוג — בתנאי**

**נמדד:** `fifoMatch.js:104` — **מדויק**. `if (trade.side !== "SHORT")` דוחף כל עסקה שאינה שורט
לספר הלוטים; SHORT **צורך** לוטים. ⇒ קנייה אחרי שורט יתום נדחפת כלוט **חדש** ו⛔ לעולם אינה מכסה אותו.
`B-072` ו-`B-209`(צלע `A-15`) הם **אותו מנגנון באותה שורה**.

> 🔴 **הסתירה — היא הממצא:** `B-072` מצהיר **«לא מומש במכוון»** (מימוש בלי בקשה = חריגה
> מ«כללים נעולים מראש»). `B-209` מצהיר **באג** ודורש מיגרציה. **שני מסמכים סותרים על אותו קוד
> — ‹R-6› בדיוק.** ⇒ המיזוג מותר, אך `B-209` חייב לשאת ש**סגירתו היא הכרעת מוצר של ניב**
> (האם סבב שורט אמור להיסגר), ⛔ לא תיקון הנדסי. מיזוג שמוחק את «במכוון» הופך הכרעה לבאג.

**⛔ אפס שורות נמחקו.** כל ארבעת ההכרעות הן המלצה לרישום, ⛔ לא בוצעו.

---

## A2 · שתי טעויות הצ'אט — הנמדד

### E1 · «59 פריטים מצטטים קובץ שאינו קיים» ⇒ ⛔ **הופרכה**

נסרקו **258** שורות פריט · **530** אסימוני-קובץ.

| קטגוריה | כמות |
|-----------|-------|
| נתיב מדויק שקיים בריפו | **416** |
| שם-בסיס חשוף שמתרכז לקובץ קיים (`tradingStats.js` ⇒ `src/lib/…`) | **89** אסימונים נבדלים |
| ⛔ בלתי-פתיר | **15** אסימונים · **19** פריטים |

**פילוח 19 הפריטים הבלתי-פתירים — ⛔ רובם אינם פגם:**

| מקור | פריטים | הכרעה |
|-------|---------|--------|
| `SwingEdge-Master-Tasks.md` | **11** (`B-131`…`B-141`) | **נמחק במכוון** ב-`2382a98` («חיסול המרשם השני»). כותרת הסקציה (`BACKLOG:282`) **מצהירה** זאת ונושאת ⚠️ «לא נמדד מאז» ⇒ **מוצהר, ⛔ לא שבור** |
| `api/waitlist.js` | **1** (`B-106`) | **תוצאה חיובית-כוזבת** — הפריט מצטט אותו **מפני שאינו קיים** («נימוק: `api/waitlist.js` אינו קיים») |
| תוצרי בנייה / זמן-ריצה / צד-שלישי | **6** (`B-150`·`B-164`·`B-175`·`B-095`·`B-242`·`B-245`) | `dist/assets/*` · `.sentinel-state/state.json` · קובץ בשולחן העבודה · `gtag.js` ⇒ **⛔ אינם אמורים להיות בריפו** |
| `20260727180000_…schema.sql` | **1** (`B-209`) | 🔴 **פגם אמיתי** — הנתיב נכתב עם שלוש-נקודות (`…`) ⇒ ⛔ בלתי-פתיר מכנית. הקובץ: `supabase/migrations/20260727180000_document_trades_schema.sql` |

⇒ **מסלול ריפו שבור באמת: 1/258.**
**⛔ הטענה «59» הופרכה. ⚠️ וגם הטענה-שכנגד «0» הופרכה — היא נכונה עד כדי 1.**

### E2 · «54/61 ממצאי DEEP בלי הצלבה» ⇒ ⛔ **הופרכה**

נמדדו **61** שורות בסקציית DEEP:

| נושא | כמות | הערה |
|-------|------|-------|
| מזהה-ממצא-פאזה (`A-01`·`C-12`·`H-01`) בתא «מה» | **60/61** | |
| פרוזת הצלבה (`סוגר`/`מרחיב את`/`ראה`/`דפוס`) | **60/61** | |
| מזהה מרשם `B-nnn` בתא «מה» | **8/61** | ⬅️ **זה מה שהחיפוש הנאיבי ראה** |
| **נושא לפחות הצלבה אחת** | **60/61** | |
| ⛔ בלי שום הצלבה | **1** — `B-244` | |

**מקור הטעות:** החיפוש חופש `B-\d+` בלבד. הוא מדד **צורת מזהה**, ⛔ לא **קיום הצלבה** —
ולכן סימן 52 פריטים שנושאים `סוגר A-12+A-25+C-12` כ«בלי הצלבה».

### הכרעה על שורת `DECISIONS` — 🔴 `R-3` **כבר מכסה** ⇒ ⛔ **אל תכפיל**

`R-3` = «**אימות בודק שנכתב, ⛔ לא שנראה** — אסרציה על טקסט המקור».
E1 ו-E2 הם **אותו מנגנון פעמיים**: שניהם חיפוש על **הליטרל** (מחרוזת נתיב · תבנית `B-\d+`)
שהוצג כמדידה של ה**דבר** (קיום קובץ · קיום הצלבה). זהה גם לכלל שנחקק ב-`DECISIONS:188`
(27.08): «קריאת CSS סטטי ⛔ אינה קובעת מה מרונדר».

⇒ **⛔ אין לפתוח שורש שביעי ו⛔ אין לנסח מחדש את `R-3`.**
⇒ **מה שכן חסר בית:** שני המספרים המופרכים עצמם (`59` · `54/61`). מספר שנאמר ולא הופרך בכתב
חוזר להיקרא כעובדה — זה ‹R-1›. **המלצה: שורת `DECISIONS` אחת שרושמת את ההפרכה ומפנה ל-`R-3`**,
⛔ לא שורה שמתארת מנגנון חדש.

---

## A3 · `D-056` ⇒ ⛔ **הטענה הופרכה — נמדד הקובץ השגוי**

```
grep -c "D-056" docs/DECISIONS.md   →  1     (⛔ לא 0)
```

- `DECISIONS.md:188` — שורת 2026-08-27, כלל «קריאת CSS סטטי ⛔ אינה קובעת מה מרונדר».
- ⚠️ **ועיקר העניין:** לפי §14, מזהי `D-nnn` חיים ב-**`docs/DONE.md`**, ⛔ לא ב-`DECISIONS.md`.
  `D-056` יושב ב-**`DONE.md:86`**, ואחריו `D-057`·`D-058`·`D-059` ב-`:87`–`:89`.
- קומיט `1b17025` הצהיר «`D-056` נרשם עם ה-hash שהקומיט הקודם לא יכול היה לשאת» — **וכך אכן קרה**,
  ב-`DONE.md`, בדיוק לפי כלל `test:registry` §4.1.

⇒ **⛔ שום דבר אינו חסר. ⛔ אל תוסיף.**

---

## A4 · שלושה מדדים שלא נמדדו

### M1 · 175 פתוחים ⇒ **⛔ אין מנגנון בחירה מלבד `NEXT`**

**התפלגות סטטוס (מכנה = 258 שורות `B-`):**

| סטטוס | כמות |
|--------|------|
| פתוח | **175** |
| חסום-על-ניב | 23 |
| ממתין-לאות | 12 |
| ❄️ מוכרע-לא-לעשות | 11 |
| ✅ בוצע (⚠️ עדיין ב-`BACKLOG`) | 36 |
| ⚠️ חלקי | 1 |

**«163» אותר במדויק:** `git show 70123a0:docs/BACKLOG.md | grep -c "| פתוח |"` = **163**, בתאריך **26.08**.
המספר היה נכון אז והוא **מדידה בת יום**. מסלול: 163 (26.08) → 168 → 169 → 172 → 173 → **175** (27.08).

**מנגנוני בחירה שנמדדו:**

| מנגנון | כיסוי |
|---------|--------|
| `docs/NEXT.md` | **3/175** (חוזה: 3 בדיוק) |
| עמודת `חוסם? = כן` | **3/175** — `B-003` · `B-163` · `B-077` |
| קטגוריה/סקציה | גסה: 60 DEEP · 42 חוב · 26 ארכיון-גזום · 14 עיצוב · 13 חשוב · 13 master-tasks · 6 ❄️-שגוי · 2 חוסם |

`CLAUDE.md` §14 מצהיר: «⛔ `BACKLOG` נשאר בלי סדר **בכוונה** — ההכרעה חיה ב-`NEXT` ובשום מקום אחר».

⇒ 🔴 **ממצא (⛔ לא באג — תוצאה של החוזה):** 175 פתוחים · 3 מקומות ב-`NEXT` · 3 דגלי חוסם ⇒
**≈169 פריטים ⛔ בלי שום מסלול להיבחר** מלבד קריאה אנושית חוזרת של קובץ בן **232KB**.
המנגנון עומד בחוזה; **הקנה מידה** הוא הממצא.

### M2 · 23 חסום-על-ניב — שורה אחת לכל אחד

**✅ 17 ניתנים לניסוח בשורה אחת:**

| מזהה | מה בדיוק ניב מכריע |
|-------|---------------------|
| `B-005` | לאשר הודעה מוקדמת ל-`e403e391` לפני תיקון 13 שורות ת"א (חשיפה ×101.55) |
| `B-014` | לוותר על General Sans (גופן מותג) לטובת גופן מארח-Google |
| `B-041` | לאשר טבלת מדידת-עלות ל-`api/ocr.js` — מיגרציה (§12) |
| `B-084` | האם לשלוח מייל החזרה ל-12 הרדומים |
| `B-085` | לשלוח ל-`omrikapara1` את הודעת המסירה (§10.2 «טופל» דורש מסירה) |
| `B-087` | למזג או לסגור את PR #44 + ענף Dependabot |
| `B-088` | האם להריץ `why_stopped_r2` בהינתן ש-r1 נתן 5→0→0 |
| `B-091` | האם לשדרג ל-Supabase Pro (הגנת סיסמאות דלופות = Pro בלבד) |
| `B-094` | לבנות מלאי סודות עכשיו, או ביום שמשהו נשבר |
| `B-095` | להוציא חומרי שיווק ואז למחוק את `~/Desktop/Swing-edge` |
| `B-097` | לסמן או לנקות ≈906 `page_view` סינתטיים ב-GA4 |
| `B-114` | האם שני המיילים ששלחו מספקים את §10.2, או שנדרש שלישי עם hash `f375478` |
| `B-135` | לברר אם Stripe מחייב עוסק מורשה (חוסם את צלע Stripe ב-`B-092`) |
| `B-139` | להריץ `SELECT` ב-Supabase: האם `profiles` קיימת ומחזיקה נתוני משתמש |
| `B-148` | לבחור ספי `MAXAGE` — זמן הגילוי **בדיד**, ⛔ לא רציף |
| `B-176` | לאשר Deploy 2 (concurrency cap + LIVE badge) |
| `B-182` | **⚠️ זהה ל-`B-185`:** מה מוצג כשהספק נופל — ערך אחרון עם חותמת, או `—` |

> 🔴 **`B-182` ⛔ אינו מנסח הכרעה בגופו** — הוא מתואר כבאג. אך החוסם שלו הוא **בדיוק** החוסם
> הרשום ב-`NEXT` עבור `B-185`. ⇒ הכרעה **אחת** של ניב סוגרת את שניהם. הצלבה זו ⛔ אינה רשומה באף צד.

**⚠️ 5 מכולות — ⛔ אינם הכרעה אחת ⇒ ⛔ אינם «חסום על ניב», הם לא-מוגדרים:**

| מזהה | מה יש בפועל | נדרש |
|-------|--------------|-------|
| `B-086` | 3 פידבקים ללא מענה | פיצול ל-3 מזהים (§10.2 מחייב מצב לכל **פידבק**) |
| `B-090` | ≥5 אימותים ידניים לא-קשורים | פיצול · ומקומם ב-`CHECKS.md` (§14) |
| `B-092` | 4 תלויות חיצוניות (Stripe · GSC · Workspace · עו"ד) | פיצול ל-4 — ו-`B-135` כבר מוכיח שהוא חסר |
| `B-093` | 3 נכסי QA | פיצול; **ערכת ה-30 בלבד** חוסמת את `B-077`/`B-078`/`B-083` |
| `B-096` | 2 בקשות פיצ'ר | פיצול ל-2 |

**⛔ 1 ⛔ אינו חסום-על-ניב כלל:**
`B-181` — גופו מצהיר במפורש «תיעוד ביושר של פער-איכות-נתונים **⛔ לא הכרעה**».
סטטוס `חסום-על-ניב` סותר את גוף הפריט. מקומו: ⚠️ סיכון או שורת `METRICS` — ⛔ לא תור ההכרעות של ניב.

> ⚠️ **הצלבה לחלק ג׳:** ההכרעה של `B-014` כפי שנוסחה («לטובת **Inter**») **פסולה מראש** —
> `Inter` נמצא ברשימת הפסילות של גל העיצוב. ההכרעה חייבת להינסח מחדש לפני שניב יכול להכריע בה.

### M3 · 11 ❄️ — **0 סתירות ⇒ ⛔ 0 נפתחו**

| מזהה | נמדד היום | פסק |
|-------|------------|------|
| `B-105` | `react-router-dom ^7.18.1` (≫ 7.11.0) · הערת ה-pin נוכחת ב-`package.json` · **grep ל-RSC/server-actions: ריק** | ✅ **אושש במלואו** |
| `B-106` | `api/waitlist.js` עדיין אינו קיים ב-`api/` | ✅ אושש (⚠️ נשען על 1.46ש מ-`B-014`, ⛔ לא נמדד מחדש) |
| `B-108` | `brokerProfiles.js` — **0** התאמות ל-`time`/`hour` | ✅ אושש |
| `B-113` | **8 קבצי workflow · 9 מופעים** של `niveven183@gmail.com` קשיח | ✅ אושש — **הסחיפה שהפריט הזהיר מפניה ⛔ לא קרתה** |
| `B-115` | `AdminPanel.jsx:997` `value="—" sub="Mixed USD+ILS — no unit"` | ✅ אושש (שורה `:993`→`:997`) |
| `B-104` | **8,189 / 2,941** שורות מול **7,659 / 2,900** שנרשמו | ✅ הנימוק הוא **החלטה** ⇒ ⛔ לא ניתן להפרכה במדידה. ⚠️ אך `SwingEdge_App.jsx` גדל **+530 שורות** מאז 11.08 (7,504 ב-09.08 ⇒ **+9.1% ב-18 יום**) |
| `B-107` | `format-test.mjs:119` מצהיר בעצמו «Deliberately NOT the blanket» | ✅ הנימוק אושש. ⚠️ מניית «5 אתרים» ⛔ לא נמדדה במעבר הזה |
| `B-109` | `currency: capitalCurrency` — **`:2818`**, ⛔ לא `:2493` (**+325 שורות**) | ✅ הנימוק (החלטה) תקף. ⚠️ 61/45/16 דורשים DB |
| `B-110` | `api/ocr.js:226-230` מכיל היום לוגיקת `entrySource`/`stopTauto` — **⛔ לא את הפרומפט שתואר** | ⚠️ **העוגן נעלם.** ⛔ לא סתירה לנימוק ⇒ ⛔ לא נפתח, אך דורש עיגון מחדש |
| `B-111` | `Number(...) \|\| 0` ב-`DecisionCoach.js`: **6 מופעים**, בעוד הפריט מונה **4 שדות** | ⚠️ **היקף נסחף.** הפריט נכתב «כדי שהחיפוש הבא לא יגלה אותם מחדש כממצא» — מנייה שנסחפה מבטלת בדיוק את התכלית הזו |
| `B-112` | `Access-Control-Allow-Origin: *` ב-`api/health.js:171`, ⛔ לא `:160` | ✅ הנימוק תקף (⚠️ תוכן הגוף ⛔ לא נבדק במעבר הזה) |

⇒ **⛔ אף פריט לא נפתח.** 5 אוששו במלואם · 3 נימוקי-החלטה תקפים עם מספרים מיושנים · **3 עוגנים נסחפו**.

---

## A5 · שלושת המדדים הפגים — נמדדו מחדש

**מקור:** Supabase read-only `SELECT`, פרויקט `zicstkfkwhzvmdkzpidm`, 2026-08-27.
🔴 **⛔ לא הוארך שום `תוקף`.** המדידה החוזרת היא התיקון היחיד ל-`⏳ פג` (§14).

| מדד | ערך קודם | **נמדד 27.08** | תזוזה |
|------|-----------|-----------------|--------|
| `M-001` גודל אוכלוסייה | **46** שורות `auth.users` (11.08) | **51** שורות `auth.users` | **+5** |
| `M-002` כיסוי `user_settings` | **34/46 = 74%** ⇒ 12/46 בלי | **39/51 = 76.5%** ⇒ **12/51 = 23.5% בלי** | ⚠️ המונה עלה, אך **המספר המוחלט של חסרי-שורה נשאר בדיוק 12** |
| `M-003` שימור שבוע 1 | **0/6 = 0%** (06.08) | **0/9 = 0%** | ⚠️ **המכנה +50%, המונה עדיין 0** |

**`M-003` — הקוהורטה המלאה** (`scripts/retention.sql` §3, כשירים/פעילים):

| שבוע | כשירים | פעילים |
|-------|---------|---------|
| 0 | 11 | **9** |
| **1** | **9** | **0** |
| 2 | 9 | **1** |
| 3 | 8 | 0 |

⚠️ **שבוע 1 = 0/9 ⛔ אינו «אף אחד לא חוזר לעולם»** — משתמש אחד חזר בשבוע 2 (1/9).
המכנה הגדול יותר **מחזק** את הממצא: 9 משתמשים היו כשירים, אפס פתחו עסקה בשבוע הראשון.

### ⚠️ ממצא לוואי — `M-004` ו-`M-005` בתוקף ו**תוכנם מוכחש**

| מדד | מה שרשום | הנמדד 27.08 |
|------|-----------|--------------|
| `M-005` «0 הרשמות ב-77.1ש» · `max(created_at)=2026-08-20T10:42Z` · **✅ בתוקף** | | `max(created_at)` = **2026-08-27T15:15:32Z** — הרשמה **היום** |
| `M-004` «0 עסקאות ב-70.4ש» · `max("createdAt")=2026-08-20T17:24Z` | | `max("createdAt")` = **2026-08-25T14:47:17Z** |

⇒ 🔴 **הדגמה חיה של המשפט שב-`CLAUDE.md` §14: «§12 מודד טריוּת ⛔ ולא נכונות».**
שני מדדים יושבים **בתוך חלון ה-`תוקף`** ולכן `test:registry` §12 ירוק, בעוד תוכנם כבר שקרי.
⛔ **⛔ אין לתקן זאת בהארכת תוקף** — הכיוון הוא מדידה חוזרת (וזה מה שנעשה כאן).

---

## A6 · שיוך לגלים — 72/175 חד-משמעי · **103 ⛔ לא נוחשו**

**שיטה (auditable):** לכל פריט **פתוח** הופעלו שש תבניות סימנים על תא «מה».
**התאמה לגל אחד בדיוק** ⇒ שויך. **אפס** ⇒ «ללא גל». **שניים ומעלה** ⇒ ⚠️ לא-חד.
🔴 **⛔ לא נוחש שיוך** — לפי המנדט, ⚠️ הוא הפלט, ⛔ לא תשובה חסרה.

| גל | חד-משמעי | דוגמאות |
|-----|-----------|----------|
| W0 · מדידה | **8** | `B-010` `B-067` `B-068` `B-073` `B-141` `B-192` `B-244` |
| W1 · אבטחה | **15** | `B-003` `B-015` `B-038` `B-043` `B-175` `B-219`…`B-222` `B-240` |
| W2 · תצוגה+עיצוב | **11** | `B-033` `B-042` `B-046` `B-204` `B-228` `B-251`…`B-254` |
| W3 · אמון | **23** | `B-006` `B-021` `B-040` `B-060`…`B-063` `B-072` `B-158` `B-169` |
| W4 · שערים | **14** | `B-008` `B-028` `B-120` `B-189` `B-215` `B-229` `B-257` |
| W5 · משפטי | **1** | `B-076` |
| **סה"כ חד-משמעי** | **72/175 = 41%** | |
| ⚠️ **לא-חד (≥2 גלים)** | **44** | W4+W0 ×8 · W2+W3 ×7 · W3+W4 ×4 · W1+W2 ×3 |
| ⛔ **ללא גל (0 סימנים)** | **59** | רובם בסקציות הארכיון-הגזום ו-master-tasks, שתיאוריהן תמציתיים |

⇒ 🔴 **הממצא: טקסונומיית ששת הגלים ⛔ אינה מחלקת את המרשם.** **103/175 = 59%** מהפריטים
הפתוחים ⛔ אינם ניתנים לשיוך חד — 44 חוצים גלים ו-59 חסרי סימן.
בצירוף M1: הגלים ⛔ אינם מנגנון בחירה חלופי ל-`NEXT`, מפני שרוב הפריטים ⛔ אינם נכנסים לגל יחיד.

---

## B · ממצאים חדשים — ⛔ טרם נרשמו

> §10.1 «אין פריט יתום». **⚠️ `docs/STATE.md` עומד על 15,671/16,000 בתים (97.9%) —
> 329 בתים פנויים** ⇒ הוספת שורות ל-`STATE` תפיל את `test:registry` §3.
> ⇒ הממצאים נרשמים **כאן** ומחכים להכרעת ניב על אופן הקליטה. ⛔ אפס רישום בוצע.

### F1 · 🔴 שישה פריטים **פתוחים** יושבים תחת כותרת `❄️ מוכרע-לא-לעשות`

`B-118` · `B-120` · `B-121` · **`B-123`** · `B-124` · `B-125` — כולם `סטטוס = פתוח`,
כולם ממוקמים פיזית מתחת ל-`## ❄️ מוכרע-לא-לעשות`.

- **`B-123`** = «חשיפה פי-101.5455 בגודל הפוזיציה — נשארת פתוחה» ⇒ **מסלול כסף**.
- `B-124` = «13.9c ו-13.9d ⛔ מעולם לא נצפו אדומות» ⇒ אסרציות שלא הוכח שיש להן שיניים.
- `B-125` = «`TRUTH.md` #8 נמחק ולא הוחלף» ⇒ הפרת §10 סעיף 4.

**למה אף שער ⛔ אינו רואה זאת:** שער ה-`נימוק:` של `test:registry` בוחן פריטים שה**סטטוס** שלהם ❄️.
לשישה האלה סטטוס `פתוח` ⇒ השער ⛔ אינו מסתכל עליהם, ו⛔ אין שער שמשווה **סקציה מול סטטוס**.
⇒ מי שקורא את `BACKLOG` לפי סקציות קורא שישה פריטים חיים כ«הוחלט לא לעשות».
**זו בדיוק מחלקת 33 הפריטים שאבדו ב-12.08** — פריט נעלם בגלל **היכן שהוא יושב**, ⛔ לא מה שכתוב בו.

### F2 · 🔴 סחיפת מספרי שורות היא שיטתית ו⛔ אין שער שרואה אותה

| פריט | צוטט | נמדד | סחיפה |
|-------|-------|-------|--------|
| `B-109` | `:2493` | **`:2818`** | **+325** |
| `B-110` | `api/ocr.js:226-230` | תוכן **אחר לגמרי** | עוגן אבוד |
| `B-063` | `:257` · `:220` | `:274` · `:229` | +17 · +9 |
| `B-112` | `:160` | `:171` | +11 |
| `B-161` | `:50` | `:54` | +4 |
| `B-115` | `:993` | `:997` | +4 |

**התבנית:** פריטי DEEP (26.08) — `B-206`·`B-208`·`B-209`·`B-222` — **מדויקים ב-4/4**.
הפריטים הישנים (01.08–16.08) סחפו ב-6/6 שנבדקו.
**השורש:** מספר השורה במרשם הוא **עותק שני** של עובדה שבעלותה בקובץ המקור ⇒ ‹R-6›,
ו⛔ אין מנגנון שמסנכרן. ⚠️ `B-110` מראה את מצב הקצה: העוגן ⛔ לא זז — הוא **נעלם**.

### F3 · 🟠 שתי מניות בכותרות סקציה נסחפו — `test:registry` §8.6 ⛔ אינו רואה אותן

| כותרת | מצהירה | בפועל |
|--------|---------|--------|
| `🔬 DEEP-AUDIT 26.08 — 113 ממצאים ⇒ **60 פריטים**` | 60 | **61** |
| `🎨 AUDIT-design-system 26.08 — אימות 8 טענות ⇒ **5 פריטים**` | 5 | **14** |

§8.6 (`D-057`) נבנתה בדיוק נגד התבנית הזו — אך היא קוראת **רק** את פסקת שכבת-השורשים.
⇒ הרחבה ישירה של `B-250`/`B-258`: אותה סחיפה, מיקום אחר, ⛔ ואין שער.

### F4 · 🟠 36 שורות `✅ בוצע` עדיין חיות ב-`BACKLOG`

§14: «מזהה שנסגר עובר ל-`DONE.md` תחת מזהה `D-` חדש, **והשורה כאן נמחקת**».
36 שורות מפרות זאת: `B-001` `B-004` `B-009` `B-018` `B-047` `B-064` `B-065` `B-071` `B-089`
`B-116` `B-117` `B-119` `B-122` `B-126` `B-129` `B-131` `B-142`…`B-146` `B-149` `B-152` `B-153`
`B-156` `B-157` `B-160` `B-162` `B-164` `B-165` `B-170`…`B-173` `B-184` `B-250`.
⇒ המכנה «258» מנפח כל שיעור שנגזר ממנו — כולל `M-007` (**122/258**).

### F5 · 🟠 `B-209` מצטט נתיב מקוצר ב-`…`
ראה E1. ⇒ ⛔ בלתי-פתיר מכנית.

### F6 · ⚠️ `B-182` ≡ החוסם של `B-185` — ⛔ לא רשום באף צד (ראה M2)
### F7 · ⚠️ הכרעת `B-014` פסולה מראש (⛔ `Inter`) — דורשת ניסוח מחדש (ראה M2)

---

## C · מה ⛔ **לא** נעשה במסמך הזה

- ⛔ **אפס** שורה נמחקה, מוזגה או שונתה באיזשהו קובץ מרשם.
- ⛔ **אפס** נגיעה בקוד. `api/ocr.js` · `api/health.js` · `SwingEdge_App.jsx` נקראו **בלבד**.
- ⛔ **אפס** מזהה `B-`/`D-`/`M-` חדש נוצר.
- ⛔ **אפס** `תוקף` הוארך.
- ⚠️ **⛔ לא נמדד:** מניית «5 אתרים» של `B-107` · תוכן הגוף של `api/health.js` (`B-112`) ·
  פילוח 61/45/16 של `B-109` · טקסט `B-176` נקרא קטוע.

**הצעד הבא (§8.1 שלב 2):** הכרעת ניב על F1–F7 ועל ארבעת המיזוגים, ואז רישום בקומיט נפרד.
