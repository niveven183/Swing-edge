# AUDIT — אשכול "קוד מת": B-022 · B-024 · B-025 · B-050 · B-068

**תאריך:** 2026-08-26 · **HEAD בזמן המדידה:** `b50e0e2` · **מצב עץ:** נקי
**סוג:** אבחון read-only (§8.1 שלב 1) — ⛔ **אפס שינוי קוד**
**סיווג §15:** **T3** · תשובות: לא / לא / לא / **כן** / **כן**

> ⚠️ **ההערכה בכותרת הפרומפט (`T2`) שגויה — תוקנה לפי חמש השאלות:**
> **ש4 רוחב בקוד = כן** — הגל נוגע ב-≥4 קבצי קוד נפרדים.
> **ש5 ודאות = כן** — מדידות הבקלוג הן מ-01.08 · 09.08 · 12.08 ⇒ **14–25 יום**, ⛔ לא 48 שעות.
> 2+ ⇒ `T3` ⇒ נדרש אבחון read-only → עצירה לאישור. אימות שאחרי: **`אומת-מקור`** (אין מסך מושפע לפריטים שיימחקו).

---

## תקציר ההכרעה

| מזהה | טענת הבקלוג | מה נמדד | פסק דין |
|------|--------------|----------|----------|
| **B-022** | `src/localAI.js` — 0 מייבאים | 0 מייבאי-ריצה ✅ · **1 צרכן טקסט-מקור** בשרשרת verify | ⚠️ **⛔ לא מכני** — מחיקה דורשת עריכת `test:instrument` |
| **B-068** | `avgMaeMfe` — 1/1 = ההגדרה | **מאושש 1/1** · 0 בטסטים · 0 בבייסליין | ✅ **מת** — אך ⛔ **אפס אסרציה מאמתת** |
| **B-024** | `profile.defaults.commission` — מספר מת | ⛔ לא נקרא ✅ · **אך נשמר ל-localStorage ו-ל-DB** | 🔴 **⛔ לא S** — שינוי סכימה |
| **B-050** | TODO ב-`analyst.mjs:407` | **עבודה אמיתית — והסחיפה כבר קרתה (14 מול 25)** | 🔴 **לחווט, ⛔ לא למחוק** |
| **B-025** | `:412` placeholder · `:533` יחס 2:1 | **חי ופונה-משתמש** | ⛔ **יוצא מהגל** |

**⇒ מתוך 5 פריטים: 0 ניתנים למחיקה מכנית ללא הכרעה.**

---

## M1 — B-022 · `src/localAI.js`

**הטענה:** 0 מייבאים ⇒ קוד מת ⇒ מחיקה מכנית.

### מה נמדד

הקובץ: 136 שורות · 2 exports — `analyzeTradeLocal` (`:14`) · `analyzeTradeLocalText` (`:124`).

| ערוץ | פקודה | תוצאה |
|------|--------|--------|
| שם הקובץ/נתיב בקוד | `grep localAI --include=*.js/jsx/mjs/html/json/yml` | **2 מופעים — שניהם באותו קובץ בדיקה** |
| `analyzeTradeLocal` | grep מלא | **2/2 = ההגדרה + קריאה עצמית פנימית `:125`** ⇒ 0 חיצוניים |
| `analyzeTradeLocalText` | grep מלא | **1/1 = ההגדרה** |
| **dynamic `import()`** | `grep -E "(import\|require)\s*\(" \| grep -i localai` | **0** |
| **require מחרוזתי** | אותו ערוץ | **0** |

✅ **0 מייבאי-ריצה — הטענה מאוששת.**

### 🔴 מה שהטענה פספסה

```
scripts/instrument-currency-test.mjs:848:    const ai    = src("../src/localAI.js");
scripts/instrument-currency-test.mjs:849:    for (const [nm, s] of [["DecisionCoach", coach], ["localAI", ai]]) {
```

בלוק **13.11 · טהרה** מריץ **3 אסרציות** על תוכן הקובץ כ**טקסט**:
`⛔ אינו מייבא מ-fx.js` · `⛔ אין בו fxTable` · `⛔ אין בו async`.

`src` מוגדר ב-`:64` כ-`readFileSync(...)` ⇒ **זורק ENOENT** אם הקובץ נמחק.

⇒ **מחיקת הקובץ מפילה את `test:instrument`, וכך את `npm run verify` כולו.**

⚠️ **מדוע זה חשוב מעבר לתקלה טכנית:** האסרציות האלה מקבעות החלטה נעולה
(`DECISIONS` 12.08 — "`DecisionCoach`/`localAI` חייבים להישאר טהורים"). מחיקת הקובץ
מוחקת **חצי מהאוכלוסייה** ששומרת על הכלל הזה. `DecisionCoach` יישאר מכוסה;
הכלל עצמו יאבד את הצד השני שלו.

### מה נדרש בפועל

מחיקה = **2 קבצים**: `src/localAI.js` + עריכת `scripts/instrument-currency-test.mjs`
(הסרת `localAI` מהלולאה ב-`:849`) ⇒ **395 → 392 אסרציות**.

⇒ ⛔ **⛔ אינה "מחיקה מכנית".** נדרשת הכרעה: האם ויתור על 3 אסרציות טהרה מוצדק.

### ⚠️ תלות נוספת — `B-070`

`docs/BACKLOG.md:190` (`B-070`) מונה את `directionOk` (`localAI.js:35-36`) כאחד
מ**ארבעה** מימושי גיאומטריה, ורושם במפורש: **"B-022 עשוי למחוק את הרביעי"**.
⇒ מחיקה כאן **משנה את המכנה של `B-070` מ-4 ל-3** ומחייבת עדכון השורה באותו קומיט.

---

## M2 — B-068 · `avgMaeMfe`

**הטענה:** `grep` → 1/1 = ההגדרה עצמה.

### מה נמדד — הטענה מאוששת במלואה

| ערוץ | תוצאה |
|------|--------|
| `grep -rn avgMaeMfe` (ללא `docs/`) | **1/1** — `src/intelligence/utils/psychologyPatterns.js:134` |
| מייבאים | **0** |
| **בטסטים** (`scripts/*.mjs`) | **0** |
| **בבייסליין הקפוא** | **0** |

### 🔴 שאלת הבייסליין — נענתה במפורש

**`avgMaeMfe` ⛔ אינו בשום בייסליין קפוא ⇒ מחיקה ⛔ אינה מזיזה אף מספר.**

אימות עקיף: `psychologyPatterns.js` **כן** מיובא ע"י 4 מודולים —
`DecisionCoach` (`tradesToday`, `trailingLossRun`, `isRevengeWindow`) ·
`TradeDNA` (`disciplineRate`, `emotionPerformance`) ·
`TiltProtection` · `GrowthTracker` (`disciplineRate`).
⛔ **אף אחד מהם ⛔ אינו מייבא `avgMaeMfe`** ⇒ הבייסליין הקפוא של `test:dna`
(4 עסקאות מ-`D-041`) ⛔ אינו יכול להיות מושפע. **אומת בריצה: `test:dna` ✅.**

### ⚠️ מה שהמחיקה תמחק — ⛔ לא רק "export מת"

גוף הפונקציה נושא **תיקון מתועד של כלל §2** ("אפס מנה בלי מכנה"), עם הערה בת 12
שורות שמסבירה את הבאג שתוקן: המימוש הקודם קיבל עסקה על `maxAdverse != null ||
maxFavorable != null` ואז `Number(t.maxAdverse) || 0` **המציא 0** לצד החסר —
"אותו 0 נחת במונה בעוד המכנה ספר את כל המערך". התיקון נתן לכל מדד **מכנה משלו**
(`maeN`/`mfeN`), ו"שום דבר מדיד → `null`, לעולם לא 0".

⚠️ **וזה בדיוק מה ששורת `B-068` עצמה אומרת:** *"ובדיוק כך נולד המכנה השגוי מלכתחילה"*.

### ⚠️ הקשר רחב — הנתונים כן נאספים

`maxAdverse`/`maxFavorable` הם שדות **חיים**: נקלטים מהמשתמש
(`SwingEdge_App.jsx:8019,8024` טופס הסגירה · `EditTradeModal.jsx:338,349`),
נשמרים, ומוגנים ע"י `test:rcontract` §8 ("0 הוא ערך, ⛔ לא ריק").

⇒ **האפליקציה אוספת MAE/MFE מהמשתמש, מחזיקה מצרף נכון עבורם — ו⛔ אינה מציגה דבר.**
זו בדיוק המחלקה ש-`B-154` מתאר. מחיקת המצרף הופכת את **איסוף** הנתונים ליתום.

⇒ ההכרעה שהבקלוג הציע ("או שיחווט או שיימחק") **פתוחה באמת** ⛔ ואינה טכנית.

---

## M3 — B-024 · `profile.defaults.commission`

**הטענה:** מספר מת, אינו נקרא בשום מקום.

### מה נמדד — יש **שני** עותקים, ⛔ לא אחד

`src/components/OnboardingScreen.jsx`:

```
 :96   const commission = commissionFor(bucket);      ← נגזר מהדלי
 :193    commission,                                   ← עותק 1 — רמה עליונה
 :196    defaults: { riskPct, commission, capital, capitalCurrency: ccy },  ← עותק 2
 :510    …{CURRENCY_SYMBOL[profile.defaults.capitalCurrency]}{profile.commission}
                                                        ↑ מציג את **עותק 1**
```

| מפתח | נקרא? | היכן |
|------|-------|------|
| `profile.commission` (עליון) | 🔴 **כן — מוצג למשתמש** | `OnboardingScreen.jsx:510`, כרטיס "עמלה" |
| **`profile.defaults.commission`** | ✅ **⛔ לא נקרא** | — |

`SwingEdge_App.jsx` צורך מ-`defaults` **שלושה** מפתחות בלבד:
`capital` (`:1182`) · `riskPct` (`:1187`, `:1367`) · `capitalCurrency` (`:1197`).
⛔ `commission` ⛔ אינו ביניהם.

✅ **הטענה מאוששת — אך רק לגבי המפתח המקונן.**

### 🔴 שאלת ההתמדה — התשובה היא **כן, לשניהם**

```
OnboardingScreen.jsx:279
  localStorage.setItem("swingEdgeOnboarding", JSON.stringify(data));
                                              ↑ data.profile — האובייקט **כולו**
```

ומשם ל-**DB**:

```
src/lib/userSettings.js:23
  swingEdgeOnboarding: { field: "onboarding", parse: JSON.parse },
```

⇒ ה-blob נדחף ל-Supabase לעמודת `onboarding` (upsert, debounce 1000ms —
`SwingEdge_App.jsx:1750`, `AUDIT-2026-08-11-settings-persistence.md` §10).

### 🔴 פסק דין

**המפתח נשמר ל-localStorage ו-ל-DB ⇒ מחיקתו היא שינוי סכימה ⇒ ⛔ אינו `S`.**

⚠️ **ויש זנב לא-ממוגר:** משתמשים שכבר עברו אונבורדינג נושאים
`onboarding.defaults.commission` בעמודת ה-DB **שלהם**. הסרת השדה מהכותב ⛔ אינה
מסירה אותו מהשורות הקיימות ⇒ אוכלוסייה מעורבת (ישנים עם המפתח · חדשים בלי).
⛔ אינו מזיק — ⛔ אך גם ⛔ אינו "ניקוי".

⚠️ **ומלכודת ניסוח:** אם מישהו יקרא "B-024" כ"תמחק את `commission`" ויסיר גם את
`:193`, **`:510` יציג `₪undefined`** — ו-`test:fx` §8 ⛔ **לא יתפוס**: הוא בודק רק
שאין `$` ליטרלי לפני `profile.*` (אומת: האסרציה עוברת היום). ⇒ רגרסיה פונה-משתמש
שהשרשרת ⛔ אינה יכולה לראות.

---

## M4 — B-050 · `TODO(sync-check)` ב-`scripts/analyst.mjs:407`

**מספר השורה ⛔ לא נדד** — ה-TODO אכן ב-`:407` (יחיד בקובץ).

### מה ה-TODO אמור לעשות

```
// ⚠️ DRIFT RISK: MUST stay in sync with src/intelligence/knowledgeGlue.js:22. We were
// already burned by schema drift once — data-guardian idea: add an automated sync
// check (assert this map deep-equals knowledgeGlue's) so a future edit to one side
// can't silently diverge. TODO(sync-check): wire that assertion into data-guardian.mjs.
```

`SETUP_NAME_TO_KEY` ב-`analyst.mjs` הוא **כפילות מכוונת** של המפה ב-
`knowledgeGlue.js` — הוכפלה כדי לא לייבא את `knowledge.js` (שה-`import ... with
{type:"json"}` שלו רץ בטעינת מודול ו⛔ אינו ניתן ל-`catch` ⇒ CI-SAFE by design).
ההערה מצהירה שהצוות **כבר נכווה** מסחיפת סכימה פעם אחת.

### 🔴 המדידה — הסחיפה שה-TODO נכתב למנוע **כבר קרתה**

השוואת deep-equal בפועל בין שתי המפות:

```
analyst keys: 14 | knowledgeGlue keys: 25
only in analyst (0):  []
only in glue   (11):  ["Higher Low","Trend Continuation","Failed Breakout",
                       "EMA Bounce 50","Range Breakout","Breakdown","Retest",
                       "Post Earnings Strength","Cup and Handle",
                       "Pullback to 20 EMA","ORB Breakout"]
value mismatch (0):   []
DEEP-EQUAL: NO — DRIFTED
```

**14/25 מפתחות בלבד ב-`analyst.mjs` ⇒ 11/25 סטאפים חסרים = 44%.**
⛔ אין אי-התאמת ערכים — הסחיפה היא **חד-כיוונית**: `knowledgeGlue` גדל,
`analyst` ⛔ לא עודכן.

### ההשלכה

`analyst.mjs` הוא סוכן ה-CI שמצליב סטאפ מול קאנון ומחזיר פסק "מבני"/"שביר".
מפתח לא-ממופה ⇒ **⛔ אין פסק כלל**. ⇒ עסקאות ב-11 הסטאפים האלה מקבלות
**שקט**, ⛔ לא שגיאה. זהו **כשל שקט** (`CLAUDE.md` §2) שהיה פעיל בלי שאיש מדד אותו.

### 🔴 פסק דין

**⛔ אל תמחק.** ה-TODO ⛔ אינו חוב סגנוני — הוא מתאר עבודת בטיחות אמיתית,
**והכשל שהוא חזה כבר התממש**. ⇒ **לחווט.**

⚠️ **ומדובר בשני פריטים נפרדים, ⛔ לא אחד:**
1. **הסחיפה הקיימת** — 11 מפתחות חסרים. תיקון נתונים, נדרש **עכשיו**.
2. **`B-050` עצמו** — אסרציית deep-equal ב-`data-guardian.mjs` שתמנע הישנות.

`scripts/data-guardian.mjs` **קיים** (13,489 בתים) ⇒ יעד החיווט זמין.

---

## M5 — B-025 · `:412` placeholder · `:533` יחס `2:1`

### 🔴 מספרי השורות היו שגויים **כבר בזמן הרישום**

|  | הבקלוג (09.08) | בפועל ב-`13fd7da` (09.08) | היום (`b50e0e2`) |
|--|-----------------|----------------------------|-------------------|
| placeholder | `:412` | **`:393`** | **`:393`** |
| יחס `2:1` | `:533` | **`:514`** | **`:513`** |

⚠️ ⛔ **זו אינה סחיפה** (`B-146`) — הסחיפה בפועל היא 0–1 שורות ב-17 יום.
ההפניה הייתה **שגויה מלכתחילה** ב-≈19–20 שורות. ⇒ חיזוק לכלל §14:
הפניה נכתבת ל**תוכן/עוגן**, ⛔ לא למספר שורה.

### הזיהוי המדוד

**א. `placeholder="10000"` — `OnboardingScreen.jsx:393`**
זהו `placeholder` של **HTML input** (רמז בשדה ההון), ⛔ **לא "placeholder" במובן
של קוד לא-גמור**. ⛔ אינו קוד מת ו⛔ אינו באג.

**ב. `2:1` קשיח — `OnboardingScreen.jsx:513`** 🔴 **חי ומוצג למשתמש:**

```jsx
:512  <div …>Max R:R</div>
:513  <div className="…text-emerald-400">2:1</div>
```

יושב ב**כרטיס הפרופיל האישי**, בשורה אחת עם שני ערכים ש**כן** נגזרים מתשובות
המשתמש (`{profile.riskPct}%` `:506` · `{profile.commission}` `:510`) ⇒ הוא **נקרא
כאילו חושב עבורו**, בעוד הוא ליטרל.

**ו-`2:1` חי גם במנוע:**

| אתר | תפקיד |
|-----|--------|
| `DecisionCoach.js:97` | `if (rr >= 2)` — **סף אמיתי בחישוב** |
| `DecisionCoach.js:100,101,107,108` | טקסט פונה-משתמש |
| `i18n.js:547,1254` | `rrExcellent` |
| `OnboardingScreen.jsx:138` | טקסט המלצה |

✅ **נבדק ולא נמצא פגם:** `DecisionCoach` **עקבי פנימית** — `rr >= 2` משודך
למחרוזת "עובר את הרף של 2:1". ⛔ אין אי-התאמה בין הסף למה שנאמר.

### 🔴 פסק דין — **B-025 יוצא מהגל**

⛔ **אינו קוד מת בשום פירוש.** לפי הוראת הפרומפט המפורשת: 2:1 מוצג **וגם** משמש
בחישוב ⇒ יוצא, מזהה נפרד.

⚠️ **ממצא נלווה שלא תוקן (§11):** התווית ב-`:512` היא **"Max R:R"**, בעוד המנוע
מתייחס ל-2:1 כ**מינימום** ("עובר את הרף", "preferred floor", ו-`rr >= 3` = "מעולה").
⇒ התווית אומרת "מקסימום" על מה שהוא **רצפה**. פגם ניסוח פונה-משתמש, ⛔ לא תוקן כאן.

---

## M6 — מה `verify` מוכיח אחרי כל מחיקה

**בייסליין ירוק שנמדד היום (`b50e0e2`):**
`test:instrument` **395/395** · `test:fx` ✅ · `test:dna` ✅ · `test:registry` **30/30**

| פריט | אסרציה שנוגעת בו | מה verify יוכיח אחרי מחיקה |
|------|-------------------|------------------------------|
| **B-022** `localAI.js` | **3** (`test:instrument` 13.11) | 🔴 **קריסת ENOENT** — ⛔ לא "עבר". חייב לערוך גם את הטסט ⇒ 395→392 |
| **B-068** `avgMaeMfe` | **0** | ⛔ **כלום.** `build` ⛔ לא יזוז (0 מייבאים) ⇒ מחיקה **בלתי-מאומתת ע"י דבר** |
| **B-024** `defaults.commission` | **0** ישירות | ⛔ **כלום.** `test:fx` §8 בודק רק `$` ליטרלי ⇒ ⛔ עיוור לשדה חסר |
| **B-050** TODO | — | ⛔ לא נמחק |
| **B-025** `2:1` | `DecisionCoach` מכוסה ב-`test:coach` | ⛔ לא נמחק |

### 🔴 המסקנה שהתבקשה במפורש

**עבור `B-068` ו-`B-024` — ⛔ אין ולו אסרציה אחת שיכולה להבדיל בין "נמחק נכון"
לבין "נמחק יותר מדי".** שרשרת של 24 חוליות תחזור ירוקה בשני המקרים.
⇒ "`verify` עבר" ⛔ **אינו** הוכחה למחיקות האלה, וסגירתן דורשת
**`אומת-מקור`** (קריאת התוצר מ-`origin` + מדידת grep חוזרת עצמאית), ⛔ לא ירוק בשרשרת.

---

## סריקה סביבתית (§11) — דווח, ⛔ לא תוקן

| # | ממצא | היכן |
|---|-------|------|
| 1 | **`CLAUDE.md` §7 אומר `test:instrument` (252 assertions)** — הריצה בפועל היום: **395**. הפער ⛔ לא נבדק לגופו | `CLAUDE.md` §7 |
| 2 | `catch {}` ריק על כתיבת `localStorage` — כשל שקט (§2). ⛔ לא ה-`catch` של `B-021` | `OnboardingScreen.jsx:279` |
| 3 | תווית **"Max R:R"** על ערך שהמנוע מתייחס אליו כ**רצפה** | `OnboardingScreen.jsx:512-513` |
| 4 | `B-070` מונה 4 מימושי גיאומטריה; מחיקת `B-022` תוריד ל-3 ⇒ השורה תדרוש עדכון באותו קומיט | `docs/BACKLOG.md:190` |

---

## מה נדרש מניב — הכרעות פתוחות

⛔ **אפס מחיקה בוצעה. כל הפריטים ממתינים להכרעה.**

| # | הכרעה | הרקע המדוד |
|---|--------|-------------|
| **1** | **B-022** — למחוק את `localAI.js` **ולוותר על 3 אסרציות טהרה**, או להשאיר? | הקובץ מת בזמן ריצה, ⛔ אך משמש כאוכלוסייה לשער שמקבע `DECISIONS` 12.08 |
| **2** | **B-068** — למחוק את `avgMaeMfe`, או **לחווט** אותו למסך? | הנתונים נאספים מהמשתמש ו⛔ אינם מוצגים; הפונקציה היא תיקון §2 מתועד |
| **3** | **B-024** — האם שינוי סכימת `onboarding` ב-DB שווה את הסרת מפתח שאיש אינו קורא? | ⛔ לא `S`. זנב לא-ממוגר בשורות קיימות |
| **4** | 🔴 **הסחיפה של M4 — 11/25 מפתחות חסרים ב-`analyst.mjs`.** לתקן עכשיו כפריט נפרד? | כשל שקט **פעיל**, ⛔ לא היפותטי |
| **5** | **B-050** — לחווט deep-equal ל-`data-guardian.mjs` | היעד קיים; ⛔ אינו מחליף את #4 |
| **6** | **B-025** — לפצל למזהה חדש (2:1 קשיח פונה-משתמש) ולסגור כ"⛔ לא קוד מת"? | מוצג **וגם** בשימוש בחישוב |

---

**עצרתי. ⛔ אפס נגיעה בקוד · ⛔ אפס נגיעה ב-`api/` · ⛔ אפס נגיעה בבייסליין הקפוא · ⛔ `NEXT` לא שונה.**
