# AUDIT — מערכת העיצוב · אימות או הפרכה של 8 טענות

> **סוג:** אבחון read-only (§8.1 שלב 1) · **תאריך:** 26.08.2026 · **רמה:** `T2`
> **תשובות §15:** הפיכות `לא` · אמון `לא` · אבטחה `לא` · רוחב-קוד `לא` (`docs` אינם נספרים) · ודאות **`כן`**
> **⛔ 0 קבצי קוד שונו.** אפס תיקון · אפס טוקן חדש · אפס שינוי תצורה.
> **HEAD בזמן המדידה:** `26d99f0` · **ה-tarball שעליו נטענו הטענות:** `b66cceb` (14 קומיטים מאחור)

---

## 0. הפסק בשורה אחת

**ההשערה המרכזית — «הבעיה אינה עיצוב מכוער אלא היעדר מערכת טוקנים» — מופרכת במדידה.**

מערכת טוקנים **קיימת**: `src/design/tokens.css`, 227 שורות, **149 משתני CSS**, עם ערכת כהה
מלאה, סולם ריווח, סולם רדיוס, סולם צל וסולם תנועה. היא ⛔ אינה מדף — היא נצרכת
**1,167 פעמים** ב-JSX. **לוח הבקרה עצמו מטוקן ב-89%** (88 `var()` מול 11 ליטרלי הקס).

מתוך 8 הטענות: **0 אוששו במלואן · 3 אוששו חלקית · 5 הופרכו.**

השורש האמיתי, שאף אחד מ-14 המדדים לא חיפש, נמצא ב-**היררכיה** ולא בפלטה — ומפורט ב-§4.

---

## 1. פאזה 1 — אימות הספירות

### M1 · האם 92 צבעי הקס עומדים? — ⛔ **לא. הטענה שגויה ב-+53%.**

```bash
find . -type f \( -name '*.jsx' -o -name '*.tsx' -o -name '*.js' -o -name '*.ts' \
     -o -name '*.css' -o -name '*.html' \) \
  -not -path './node_modules/*' -not -path './dist/*' -not -path './build/*' \
  -not -path './test-results/*' -not -path './playwright-report/*' -not -path './.git/*' \
  | xargs grep -ohE '#[0-9a-fA-F]{6}\b|#[0-9a-fA-F]{3}\b' | tr 'A-F' 'a-f' | sort | uniq -c | sort -rn
```

```
file count: 134
UNIQUE: 141
TOTAL OCCURRENCES: 972
```

**המספר האמיתי על כל עץ המקור הוא 141 ייחודיים ב-972 מופעים, ⛔ לא 92.**
הטענה המקורית נמדדה על תת-קבוצה (`SwingEdge_App.jsx` + `src/*.js`) והוצגה כאילו היא הכל.

⚠️ **אבל הפער הזה הוא ⛔ לא הממצא.** ראה `M9`: המספר שרלוונטי לשדרוג הדאשבורד הוא **3**.

---

### M2 · הפרדה בין קוד חי לקוד שאינו נטען לדפדפן

```
A_live_src             unique=106   occurrences=443
A_live_rootjsx         unique=30    occurrences=176
A_live_indexhtml       unique=7     occurrences=7
A_tailwindcfg          unique=0     occurrences=0
B_scripts              unique=0     occurrences=0
B_tests                unique=0     occurrences=0
B_api                  unique=6     occurrences=7
B_emails               unique=12    occurrences=283
B_public               unique=22    occurrences=56
B_docs                 unique=0     occurrences=0
```

**איחוד קוד הדפדפן החי** (`src` + `SwingEdge_App.jsx` + `index.html`): **118 ייחודיים · 626 מופעים**.

🔴 **ההשערה «אם רוב הזנב בטסטים — הטענה קורסת» מופרכת, אבל בכיוון ההפוך מהצפוי:**
`scripts/` · `tests/` · `tests-sentinel/` · `docs/` תורמים **0 מופעים, אפס מוחלט**. הזנב ⛔ אינו
בטסטים — הוא בקוד חי.

⚠️ **מה שכן מזהם את הספירה הגלובלית:** `emails/` נושא **283 מופעים = 29% מכלל ה-972**,
ו⛔ אינו קוד דפדפן כלל (תבניות מייל HTML, §12 ב-`CLAUDE.md`). `public/` מוסיף עוד 56.
⇒ **כמעט שליש מהמספר הגלובלי מגיע ממשטח שאינו האפליקציה.**

**פילוח לפי קובץ, קוד חי, 15 העליונים:**

```
184 ./src/components/LandingPage.jsx
176 ./SwingEdge_App.jsx
 56 ./src/design/tokens.css
 22 ./src/components/MobileTradeCard.jsx
 19 ./src/components/EditTradeModal.jsx
 17 ./src/components/AdminPanel.jsx
 16 ./src/intelligence/ui/IntelligenceUI.jsx
 16 ./src/components/MonthlyReportTab.jsx
 14 ./src/components/WelcomeAnnouncement.jsx
 14 ./src/components/Logo.jsx
 12 ./src/components/AuthScreen.jsx
  8 ./src/components/GrowthPredictor.jsx
  7 ./index.html
  6 ./src/index.css
  6 ./src/components/ui/SmartSelect.jsx
```

🔴 **מקור ההקס הגדול ביותר בקוד החי הוא `LandingPage.jsx` (184) — דף השיווק, ⛔ לא הדאשבורד.**
הטענות נטענו על הדאשבורד. `184 + 56` (הגדרות הטוקנים) `= 240` מתוך 626 **= 38% מהקוד החי
אינו הדאשבורד ואינו רלוונטי לשדרוג שנתבקש.**

---

### M3 · 🔴 השאלה שמכריעה הכל — האם קיימת מערכת טוקנים?

## **כן. `src/design/tokens.css` — 227 שורות, 149 משתני CSS.**

```bash
grep -cE '^\s*--[a-z0-9-]+:' src/design/tokens.css   # → 149
grep -roh 'var(--[a-z0-9-]*)' src --include='*.jsx' --include='*.js' SwingEdge_App.jsx | wc -l
# → 1167
```

המערכת מכסה: משטחים · טקסט · אקסנטים · סמנטיקה · טיפוגרפיה · ריווח (`--space-1..16`) ·
רדיוס (`--radius-sm..pill`) · צללים (`--shadow-xs..glow`) · תנועה · `z-index` · **וערכת כהה מלאה**.

**ליטרל כהגדרת טוקן מול ליטרל מפוזר:**

| קבוצה | ייחודיים |
|--------|-----------|
| הקס בקוד החי, סה"כ | **118** |
| מתוכם — **ערך של טוקן מוגדר** | **41** |
| מתוכם — ליטרל שאינו ערך טוקן | **77** |

### 🔴 והממצא האמיתי שנמצא כאן — ⛔ לא היעדר מערכת אלא **שתיים**

`tokens.css:117-118` נושא, בכתב מפורש:

```css
/* ═══ Design System v3 (Robinhood-serif × Linear) — parallel layer ═══ */
/* מהגרים מסך-מסך. הישן נשאר עד שכל המסכים מומרים. */
```

⇒ **שכבת `--v3-*` מקבילה חיה לצד השכבה הישנה.** היא ⛔ אינה נטושה — היא הנצרכת ביותר:

```
161 var(--v3-accent)      133 var(--v3-text-lo)     120 var(--v3-loss)
 76 var(--v3-info)         70 var(--v3-bg-panel)     47 var(--v3-radius-chip)
 46 var(--v3-warn)         46 var(--v3-line)         45 var(--v3-text-mid)
 34 var(--v3-accent-glow)  30 var(--v3-purple)       11 var(--v3-text-hi)
 10 var(--v3-radius-card)   7 var(--v3-info-glow)     4 var(--v3-warn-glow)
  4 var(--v3-purple-glow)   2 var(--v3-radius-pill)   2 var(--v3-loss-glow)
```

⚠️ **ריבוי הצבעים ⛔ אינו סחיפה אקראית — הוא הגירה לא-גמורה בלי קריטריון סיום.**
זו אבחנה שונה לחלוטין, ומובילה לפעולה שונה לחלוטין (§5).

---

### M4 · הירוקים והאדומים — האם הם באמת «מתחרים»?

| הקס | מופעים בקוד חי | הגדרה | תפקיד | ברירת מחדל של Tailwind? |
|-----|-----------------|--------|--------|--------------------------|
| `#00c076` | **70** | `tokens.css:122` `--v3-accent` | אקסנט מערכת **v3** | ⛔ לא — צבע מותאם |
| `#10b981` | **29** | `tokens.css:180` `--accent-emerald` **בתוך `.dark`** | אקסנט הערכה **הישנה**, מצב כהה | ✅ `emerald-500` |
| `#16d687` | **20** | `tokens.css:35` `--accent-emerald-bright` | וריאנט בהיר; **14/20 ב-`LandingPage`** | ⛔ לא |
| `#f43f5e` | **28** | `tokens.css:128` `--v3-loss` **וגם** `:185` `--accent-rose` | הפסד — **שתי המערכות מסכימות** | ✅ `rose-500` |
| `#ef4444` | **22** | ⛔ **אינו ערך של אף טוקן** | — | ✅ `red-500` |
| `#06b6d4` | **31** | `tokens.css:130` `--v3-info` | ראה `M5` | ✅ `cyan-500` |

**T3 — «שלושה ירוקים מתחרים»:** הספירה כמעט נכונה (119 בקוד חי מול 112 שנטענו), אבל
**האבחנה שגויה.** אין כאן שלוש בחירות מתחרות אלא **שלושה תפקידים מוגדרים בשמות**: אקסנט v3 ·
אקסנט הערכה הישנה במצב כהה · וריאנט בהיר שחי כמעט כולו בדף הנחיתה.

**T4 — «שני אדומים מתחרים»:** מופרך בניסוח. `#f43f5e` הוא הטוקן ב**שתי** המערכות —
זו הסכמה, ⛔ לא תחרות. **`#ef4444` הוא הפריט היחיד מבין ששת הצבעים החמים שאינו טוקן כלל** —
`red-500` של Tailwind שנכתב ידנית ב-22 מקומות. ⇒ **טוקן אחד + יתום אחד**, ⛔ לא שני מתחרים.

⚠️ **וזה כן ממצא — אבל אחר:** `#10b981` · `#f43f5e` · `#ef4444` · `#06b6d4` הם **ברירות המחדל
של Tailwind**. הן ⛔ לא נבחרו — הן מה שקורה כשלא בוחרים. זה בדיוק הפרופיל של
«נראה AI», אבל המנגנון הוא **ברירת מחדל של ספרייה**, ⛔ לא היעדר טוקנים.

---

### M5 · מה טורקיז מסמן בפועל? — ⛔ **הטענה מופרכת. יש לו משמעות מוצהרת בקוד.**

`src/design/tokens.css:130-135`:

```css
--v3-info:    #06b6d4;   /* equity/live-data channel — distinct from accent */
--v3-purple:  #A78BFA;   /* setup-identity channel — distinct from accent/info */
--v3-warn:    #F59E0B;   /* caution tier — the middle step between accent (safe) and loss (danger) */
```

**31 המופעים אינם דקורציה.** הצבע נושא ערוץ סמנטי מתועד — הון/נתון-חי — ומופרד במפורש
מהאקסנט ומהאזהרה. פילוח: `SwingEdge_App.jsx` 21 · `MobileTradeCard.jsx` 3 · `tokens.css` 1.

⇒ **T5 מופרכת.** «במקום 3 בתדירות בלי סיבה» — הסיבה כתובה בהערה בשורת ההגדרה.

---

### M6 · glass וצללים — ⛔ **מופרכת עבור הדאשבורד, שעליו נטענה.**

```
backdrop-blur — 34 מופעים ב-19 קבצים
shadow-2xl 28 · shadow-xl 8 · shadow-lg 22 · shadow-md 6 · shadow-sm 14
```

המספרים הגלובליים **גבוהים** מהנטען (34 מול 5 · 58 מול 19). אבל:

```
=== M6: בבלוק הדאשבורד בלבד ===
  backdrop-blur: 0
  shadow-lg/xl/2xl: 1
```

🔴 **בדאשבורד: אפס glass, צל כבד אחד.** 17 מתוך 19 הקבצים עם `backdrop-blur` הם
מודאלים / שכבות-על / טוסטים / באנרים — `backdrop-blur` על רקע מודאל הוא שימוש **נכון**,
⛔ לא «רפלקס».

⚠️ **ויותר מזה — קיימת שכבת ריכוך שיטתית שלא נלקחה בחשבון** (`src/index.css:204-206`):

```css
.shadow-2xl, .shadow-xl { box-shadow: var(--shadow-lg); }
.shadow-lg { box-shadow: var(--shadow-md); }
.shadow-md { box-shadow: var(--shadow-sm); }
```

⇒ **כל `shadow-2xl` בקוד כבר מרונדר כ-`--shadow-lg`.** ספירת מחרוזות המקור ⛔ אינה מודדת
את מה שנראה על המסך — וזו בדיוק הטעות שהמדידה המקורית עשתה.

---

### M7 · רדיוסים — ⛔ **מופרכת, אבל כי היא מקטינה את הבעיה, לא כי אין בעיה**

```
rounded-full 93 · rounded-3xl 1 · rounded-2xl 59 · rounded-xl 168
rounded-lg 188 · rounded-md 6 · rounded-sm 0
rounded-[var(--v3-radius-chip)] 47 · rounded-[var(--v3-radius-card)] 10
rounded-[12px] 5 · rounded-[10px] 4 · rounded-[var(--v3-radius-pill)] 2
```

**ערכי `borderRadius` inline — 17 ערכים נומריים נבדלים:**

```
7·8·9·10·11·12·13·14·16·18·20·22·24·28·36·99·999
```

מול **9 טוקני רדיוס מוגדרים** (`--radius-sm/md/lg/xl/2xl/pill` + `--v3-radius-card/pill/chip`).

⇒ **הטענה «4 רדיוסים» מקטינה את הבעיה בערך פי 4.** אבל היא גם מפספסת את מיקומה: הכאוס
ב-`borderRadius` ה-inline (`9`, `11`, `13`, `22`, `36`), ⛔ לא במחלקות Tailwind.

**ובדאשבורד עצמו — יש היגיון:**

```
13 rounded-xl · 13 rounded-full · 3 rounded-2xl · 2 rounded-lg · 1 borderRadius: 8
```

כרטיס=`xl` · צ'יפ/כפתור עגול=`full`. ⇒ **בדאשבורד הטענה נחלשת מהותית.**

---

### M8 · Inter — ⛔ **מופרכת. הוא נטען ו⛔ אינו מרונדר.**

```
index.html:55  fonts.googleapis.com  → Frank Ruhl Libre, Inter(400..800), Heebo, JetBrains Mono
index.html:56  api.fontshare.com     → general-sans(400,500,600,700)
```

**`General Sans` נטען בפועל** (Fontshare), ובכל שרשרת הפונטים הוא **קודם** ל-Inter:

```css
--font-display: 'General Sans', 'Inter', system-ui, sans-serif;
--font-body:    'General Sans', 'Inter', system-ui, sans-serif;
--font-hebrew:  'Heebo', system-ui, sans-serif;
```

ובעברית — `src/index.css:67-69` מנתב את ה-`body` כולו ל-`--font-hebrew` = **Heebo**:

```css
[dir="rtl"] body, html[lang="he"] body { font-family: var(--font-hebrew); }
```

⇒ **Inter ⛔ אינו מרונדר לא בלטינית (General Sans מנצח) ולא בעברית (Heebo מנצח).**
`0` הצהרות `@font-face`, `0` קובצי פונט מקומיים.

⇒ **זהו חוב ביצועים (`<link>` חוסם-רינדור עם 5 משקלים), ⛔ לא ממצא עיצובי** — בדיוק
האבחנה ש-`M8` ביקשה. **ממצא ידוע, ⛔ לא נחקר מחדש:** `B-014` (fontshare חוסם-רינדור,
1.46ש מול 0.25ש) · `B-150` (404 מ-`fonts.gstatic.com`, תדירות `2/14`).

---

## 2. פאזה 2 — מה שלא נמדד כלל

### M9 · 🔴 מפת הדאשבורד בלבד — **המספר שרלוונטי הוא 3, ⛔ לא 92**

הגדרת המשטח: `SwingEdge_App.jsx:4302-4819` (הבלוק `tab === "dashboard"`, **518 שורות**)
\+ הקומפוננטות המשותפות `StatCard` (954-986) · `RibbonTicker` (1001-1037) ·
`MarketPulseCard`/`SectorThemeCard` (1088-1158) — **141 שורות**.

```
--- ייחודיים הקס בבלוק הדאשבורד ---
   5 #ef4444
   5 #10b981
   1 #0d1424
--- var(--*) בבלוק הדאשבורד ---
  31 var(--v3-loss)      20 var(--v3-accent)     12 var(--border-subtle)
   8 var(--v3-bg-panel)   8 var(--bg-elevated)    4 var(--v3-info)
   3 var(--v3-text-lo)    2 var(--v3-line)
```

| מדד | ערך |
|------|------|
| צבעי הקס ייחודיים בדאשבורד | **3** |
| מופעי הקס בדאשבורד | **11** |
| צריכות טוקן בדאשבורד | **88** |
| **שיעור טוקניזציה** | **88 / (88+11) = 89%** |

🔴 **הדאשבורד ⛔ אינו סובל מ-92 צבעים. הוא משתמש ב-3, ומטוקן ב-89%.**
**כל טענה `T1`/`T2` קורסת ברגע שהמשטח מוגדר נכון.**

---

### M10 · ניגודיות — 🔴 **הממצא החמור ביותר בדוח, וב⛔ לא במצב שנבדק**

חישוב WCAG 2.1 יחס-ניגודיות (`node`, נוסחת luminance יחסית) על צמדי הדאשבורד:

```
MODE  RATIO   AA-normal(4.5) AA-large(3.0)  PAIR
LIGHT  2.48  ** FAIL **     FAIL          StatCard label  slate-500 → --text-muted  [#9CA89A on #FFFFFF]
LIGHT  7.58  PASS           pass          StatCard sub    slate-600 גולמי           [#475569 on #FFFFFF]
LIGHT  4.10  ** FAIL **     pass          StatCard value  cyan → --accent-indigo     [#0284C7 on #FFFFFF]
LIGHT  2.54  ** FAIL **     FAIL          StatCard value  text-[#10b981]             [#10B981 on #FFFFFF]
LIGHT  3.76  ** FAIL **     pass          StatCard value  text-[#ef4444]             [#EF4444 on #FFFFFF]
LIGHT  2.54  ** FAIL **     FAIL          StatCard trend  ירוק ליטרלי                [#10B981 on #FFFFFF]
LIGHT 15.24  PASS           pass          body --text-primary on --bg                [#1B2419 on #F8FAF7]
LIGHT  4.65  PASS           pass          --text-tertiary on --bg-elevated           [#6B7868 on #FFFFFF]
DARK   6.19  PASS           pass          StatCard label / sub → --text-muted        [#8A97A8 on #0D1424]
DARK   2.43  ** FAIL **     FAIL          [לו M-01 לא היה קיים: slate-600 גולמי]     [#475569 on #0D1424]
DARK   6.16  PASS           pass          StatCard value cyan → --accent-indigo      [#818CF8 on #0D1424]
DARK   7.25  PASS           pass          StatCard value text-[#10b981]              [#10B981 on #0D1424]
DARK   4.88  PASS           pass          StatCard value text-[#ef4444]              [#EF4444 on #0D1424]
V3     5.14  PASS           pass          --v3-text-lo on --v3-bg-panel              [#7A8783 on #0A100D]
V3    12.19  PASS           pass          --v3-text-mid on --v3-bg-panel             [#C7D0CC on #0A100D]
V3     8.05 / 5.23 / 7.91 / 7.06 / 8.95   accent · loss · info · purple · warn — כולם עוברים
```

**המנה עם המכנה:** **5 מתוך 8** צמדי הדאשבורד ב**מצב בהיר** נופלים מתחת ל-4.5:1;
**0 מתוך 7** במצב כהה; **0 מתוך 8** בשכבת v3.

🔴 **המספר החמור:** ערך ה-P&L — **המספר החשוב ביותר בדאשבורד** — עומד על **2.54:1**
במצב בהיר. זה ⛔ לא רק כישלון AA לטקסט רגיל; זה כישלון גם ב-AA-large.

### השורש, נמדד ⛔ ולא משוער

`src/index.css:120-127` ממפה מחדש מחלקות Tailwind **בעלות שם** לטוקנים:

```css
.text-green-400, .text-green-500, .text-green-600,
.text-emerald-400, .text-emerald-500, .text-emerald-600 { color: var(--accent-emerald); }
```

אבל `StatCard:957,960,978` כותב **מחלקות שרירותיות**:

```jsx
green:  { border: "border-[#10b981]/25", iconColor: "text-[#10b981]", … },
red:    { border: "border-[#ef4444]/25", iconColor: "text-[#ef4444]", … },
```

⇒ **`text-[#10b981]` ⛔ אינו נתפס על ידי `.text-emerald-500`** ⇒ הוא **עוקף את שכבת
הערכות לחלוטין** ומרנדר את אותו הקס בדיוק בשני המצבים. במצב כהה זה במקרה עובד (7.25:1);
במצב בהיר זה נכשל (2.54:1).

⚠️ **וזה בדיוק אותו כשל שכבר נתפס ותוקן — בצד השני בלבד.** `src/index.css:112-118`:

```css
/* M-01: slate-600/700 as text render raw hex in dark (AA fail 1.7–2.5:1).
   Dark-scoped so light mode keeps the strong raw hex. bg/border classes untouched. */
.dark .text-slate-600, .dark .text-gray-600, … { color: var(--text-muted); }
```

⇒ **התיקון היה מוגבל ל-`.dark`. המקבילה במצב בהיר מעולם לא נמדדה.**

**ואישוש הצילום החי:** ווידג'ט «מצב שוק» (`IntelligenceUI.jsx:490-530`) — טקסט העצה
`text-[11px] text-slate-400`, ושורת המאמן `text-[10px] text-slate-500` ⇒ `--text-muted`
`#9CA89A` = **2.48:1 בגודל 10px** במצב בהיר. **התצפית «אפור-על-אפור וכמעט בלתי קריא»
מאוששת במספר.**

---

### M11 · סולם ריווח — ✅ **תקין. ⛔ אין ממצא.**

```
ערכי הסולם בשימוש בדאשבורד:  0.5 · 1 · 1.5 · 2 · 2.5 · 3 · 4 · 6
ערכי padding/gap/margin inline בפיקסלים:  (ריק — 0 מופעים)
```

סולם Tailwind של 4px, עקבי, **אפס ערכים שרירותיים**. זה החלק הבריא ביותר במערכת.

---

### M12 · 🔴 סולם טיפוגרפי — **ההשערה מאוששת, וזה הממצא המרכזי**

```
--- מחלקות גודל Tailwind בדאשבורד ---
  20 text-xs    8 text-sm    3 text-2xl    2 text-lg    1 text-xl    1 text-5xl
--- ערכים שרירותיים ---
  18 text-[10px]   10 text-[11px]   3 text-[9px]
--- inline ---
   3 fontSize: 11
```

⚠️ **`text-5xl` היחיד ⛔ אינו מספר — הוא אמוג'י:**

```jsx
<div className="text-5xl mb-3">👋</div>   // מצב ריק, /tmp/dashboard.jsx:6
```

⇒ **הטקסט הגדול ביותר בדאשבורד הוא קישוט במצב ריק.**

### 🔴 ולתשובה לשאלה שנשאלה — «האם למספר ההון יש גודל שונה?»

`SwingEdge_App.jsx:972` — **כל** ערך בכל כרטיס:

```jsx
<span className={`text-lg md:text-2xl font-bold font-mono ${iconColor}`}>{value}</span>
```

ובקריאה (`SwingEdge_App.jsx:4339-4347`, `lg:grid-cols-6`):

| # | כרטיס | גודל | `accent` | הצבע נושא משמעות? |
|---|--------|------|----------|--------------------|
| 1 | **הון החשבון** | `text-lg md:text-2xl` | `cyan` | ⛔ לא — קבוע |
| 2 | P&L נטו סגור | `text-lg md:text-2xl` | `green`/`red` לפי סימן | ✅ כן |
| 3 | אחוז הצלחה | `text-lg md:text-2xl` | `purple` | ⛔ לא — קבוע |
| 4 | R ממוצע | `text-lg md:text-2xl` | `amber` | ⛔ לא — קבוע |
| 5 | P&L יומי | `text-lg md:text-2xl` | `green`/`red` לפי סימן | ✅ כן |
| 6 | רצף | `text-lg md:text-2xl` | `green`/`amber` לפי סף | ✅ כן |

🔴 **שישה כרטיסים · גודל זהה · משקל זהה · פונט זהה · ריווח זהה · בשורה אחת של 6 עמודות.**
**הון החשבון ⛔ אינו נבדל ויזואלית מ"רצף".** ההיררכיה היחידה היא **גוון** —
ו**הגוון עצמו נושא שתי משמעויות סותרות**: ב-3 כרטיסים הוא סמנטי (ירוק/אדום לפי סימן),
ב-3 האחרים הוא דקורטיבי ונבחר ידנית באתר-הקריאה.

⚠️ **והקצה הנמוך מרוסק:** `9px` · `10px` · `11px` · `12px` — **4 מדרגות בטווח 3 פיקסלים**.
הן בלתי-נבדלות לעין ⇒ כל הטקסט המשני נקרא כמשקל אחד.

---

### M13 · מצב בהיר מול כהה — **שלושה מנגנונים מקבילים**

| # | מנגנון | היקף |
|---|---------|-------|
| 1 | `.dark` על ה-root ⇒ דריסת **ערכי טוקן** | `ThemeContext.jsx:58` `root.classList.toggle('dark', …)` · `tokens.css:160-226` |
| 2 | שכבת מיפוי גלובלית — מחלקות Tailwind ⇒ `var(--*)` | `src/index.css`, 845 שורות, **156** הפניות `var(--` |
| 3 | וריאנטים `dark:` מפוזרים בקומפוננטות | **538 מופעים** |

```
538 סה"כ dark:  ·  SwingEdge_App 139 · IntelligenceUI 42 · MonthlyReportTab 42
DayTradesModal 32 · AdminPanel 24 · TradeCalendar 22 · MonthlyReportModal 10 …
```

⚠️ **ומחלקות ההקס השרירותיות עוקפות את מנגנון 2 לגמרי** (`M10`) ⇒ בפועל יש **ארבעה**
מסלולים שקובעים צבע, ולאחד מהם אין ערכת נושא בכלל.

---

### M14 · 🔴 היקף השינוי — **החלפת פלטה ⛔ אינה זולה**

| מסלול | עלות |
|--------|-------|
| דרך טוקן מרכזי | **קובץ 1** (`tokens.css`), 149 משתנים — **זול** |
| ⛔ **אינו ניתן להשגה דרך טוקן** | |
| מחלקות Tailwind שרירותיות עם הקס | **224 מופעים · 34 נבדלים** |
| מופעי הקס באפליקציה (ללא `LandingPage` וללא `tokens.css`) | **386 מופעים ב-40 קבצים** |
| וריאנטים `dark:` שמקודדים צבע | **538** |
| `LandingPage.jsx` — משטח נפרד | **184 מופעים** |

```
 43 bg-[#0d1424]      24 border-[#00C076]   20 border-[#06b6d4]   18 bg-[#00C076]
 12 text-[#10b981]    11 text-[#ef4444]      8 border-[#F43F5E]    8 bg-[#F43F5E]
  8 bg-[#06b6d4]       7 border-[#A78BFA]    6 bg-[#131a2c]        6 bg-[#0a0f1e]
  5 text-[#F43F5E]     5 border-[#ef4444]    5 bg-[#ef4444]        4 text-[#00C076]
```

⇒ **`find-replace` על מאות ליטרלים — יקר ומסוכן.** מחלקה שרירותית אחת שנשכחת ⛔ אינה
שוברת את הבנייה ו⛔ אינה צועקת; היא פשוט נשארת בצבע הישן, ובמצב הלא-נכון נכשלת בניגודיות
**בשקט** — בדיוק כפי ש-`M10` מודד היום.

---

## 3. פאזה 3 — הפסק

| טענה | מה נמדד | פסק | הפער | מה זה משנה |
|-------|----------|------|-------|-------------|
| **T1** · 92 הקס ייחודיים | **141** בכל העץ · **118** בקוד חי · **3 בדאשבורד** | ⛔ **הופרכה** | +53% בעץ; והמשטח שנמדד ⛔ אינו זה שנטען | הדאשבורד משתמש ב-3 צבעים. הטענה ⛔ אינה נוגעת בו |
| **T2** · 4 העליונים = 34% | **30.0%** (188/626) בקוד חי | 🟡 **חלקית** | המספר קרוב; **המסקנה שגויה** — ⛔ לא נספרו 1,167 צריכות טוקן | «פיזור» מודד ליטרלים בלבד ומתעלם מ-89% המטוקן |
| **T3** · 3 ירוקים מתחרים = 112 | **119** בקוד חי | 🟡 **חלקית** | הספירה כמעט נכונה, **התפקידים נבדלים ומוגדרים בשם** | ⛔ לא סחיפה — **הגירה לא-גמורה** בין שתי מערכות |
| **T4** · 2 אדומים מתחרים | `#f43f5e` **28** (טוקן בשתי המערכות) · `#ef4444` **22** (⛔ לא טוקן) | 🟡 **חלקית** | טוקן אחד + יתום אחד, ⛔ לא שני מתחרים | היתום היחיד הוא `red-500` של Tailwind |
| **T5** · טורקיז 30, בלי סיבה | **31**; `--v3-info`, ערוץ הון/נתון-חי **מתועד בהערה** | ⛔ **הופרכה** | המשמעות כתובה בשורת ההגדרה | הצבע עקבי; ⛔ אינו דקורטיבי |
| **T6** · glass ×5 · צללים ×19 | גלובלי **34** · **58**; **בדאשבורד 0 ו-1** | ⛔ **הופרכה** | גבוה גלובלית, **אפס בדאשבורד**; ו-`index.css:204` כבר מרכך הכל | ספירת מחרוזות ⛔ אינה מודדת רינדור |
| **T7** · 4 רדיוסים | 7 מחלקות + **17 ערכי inline נבדלים** מול 9 טוקנים; בדאשבורד 4 עם היגיון | ⛔ **הופרכה** | **מקטינה את הבעיה פי ~4** וממקמת אותה במקום הלא-נכון | הכאוס ב-`borderRadius` inline, ⛔ לא ב-Tailwind |
| **T8** · Inter = tell #1 | נטען; `General Sans` **נטען וקודם לו**; עברית ⇒ `Heebo` | ⛔ **הופרכה** | Inter ⛔ אינו מרונדר כלל | חוב ביצועים (`B-014`/`B-150`), ⛔ לא ממצא עיצובי |

**סיכום: 0 אוששו · 3 חלקית · 5 הופרכו.**

---

### 🔴 מה הופרך, ובכמה — ⛔ בלי ריכוך

1. **הליבה קרסה.** «היעדר מערכת טוקנים» — יש **149 טוקנים** ו-**1,167 צריכות**. ⛔ לא חלקית שגוי; **הפוך**.
2. **T1 שגויה פי שניים:** גם המספר (92 מול 141) וגם המשטח (הדאשבורד = **3**).
3. **T6 מופרכת בדאשבורד** — 0 glass, צל 1 — **ו**שכבת ריכוך שיטתית לא נלקחה בחשבון.
4. **T8 מופרכת בבסיסה** — הפונט שנטען ⛔ אינו הפונט שמרונדר.
5. **T5 מופרכת מהערה בקוד** שהמדידה המקורית לא קראה.
6. **הזנב ⛔ אינו בטסטים** — `scripts`/`tests`/`docs` = **0 מוחלט**. אבל **29% מהמספר הגלובלי
   מגיע מ-`emails/`**, שאינו קוד דפדפן — ⇒ המספר נופח ממשטח לא רלוונטי.

### מה אושש וחזק מהנטען

- **T7 מקטינה את הבעיה** — 17 ערכי רדיוס inline נבדלים מול 9 טוקנים.
- **המספרים הגלובליים של T6 גבוהים פי ~3-7** מהנטען — אך במשטח הלא-נכון.

---

### ⚠️ מה שלא נשאל וצריך היה — הממצא שאף אחד מ-14 המדדים לא תפס

**כל 14 המדדים שואלים על מלאי: כמה צבעים · כמה רדיוסים · כמה צללים · איזה פונט.
⛔ אף אחד מהם לא שואל על דירוג — מה גדול ממה, ולמה.**

והתשובה נמדדה ב-`M12`: `StatCard:972` מרנדר את **שישה** מדדי ה-KPI בגודל, משקל, פונט
וריווח **זהים**, ב-`lg:grid-cols-6`. **הון החשבון נראה בדיוק כמו מונה הרצף.** ההבדל היחיד
הוא גוון — וגוון שנושא משמעות סמנטית ב-3 כרטיסים ודקורטיבית ב-3 האחרים.

🔴 **וזו הסיבה שארבעת ניסיונות השדרוג נכשלו, ובפרט «זה נראה אותו דבר»:**
פלטה חדשה שמוחלת על שישה תיבות זהות מייצרת **שישה תיבות זהות בצבעים חדשים.**
המבנה — ⛔ לא הצבע — הוא מה שנקרא «AI».

⚠️ **ממצא שני שלא נשאל:** `#10b981` · `#ef4444` · `#f43f5e` · `#06b6d4` הם **ברירות המחדל
המדויקות של Tailwind** (`emerald-500` · `red-500` · `rose-500` · `cyan-500`). קטלוג
ה-«AI slop tells» צודק בתוצאה ו⛔ לא במנגנון: המנגנון ⛔ אינו היעדר טוקנים, אלא **ערכי
ברירת-מחדל של ספרייה שהועלו לדרגת טוקן ⛔ בלי שנבחרו**.

---

### המלצה

**«היעדר מערכת טוקנים» ⛔ אינו שורש הבעיה. האבחון החטיא — והחטיא בכיוון שהיה מייצר עבודה
יקרה וחסרת תועלת** (עוד שכבת טוקנים מעל 149 קיימים, וגל find-replace על 224 מחלקות).

לפי מדידה, שלושה דברים נבדלים — ⛔ ואף אחד מהם ⛔ אינו «פלטה חדשה»:

1. **היררכיה** (`M12`) — הזול, ובעל ההשפעה הגדולה ביותר. `StatCard` הוא **קומפוננטה אחת,
   `SwingEdge_App.jsx:954-984`**. דירוג ההון כמספר-גיבור והורדת חמשת האחרים לדרגה משנית
   הוא שינוי במקום אחד. ⛔ **אינו דורש אף צבע חדש.**
2. **ניגודיות במצב בהיר** (`M10`) — **5/8 כשלי AA**, וה-P&L ב-2.54:1. זו נגישות
   ותקינות, ⛔ לא אסתטיקה. השורש: מחלקות הקס שרירותיות שעוקפות את שכבת הערכות.
3. **סיום ההגירה ל-v3** (`M3`) — או קביעת קריטריון סיום. שתי מערכות מקבילות ⛔ בלי תנאי
   עצירה הן `R-6` בהגדרתו, והן המקור האמיתי לריבוי הירוקים.

⛔ **הכרעה ⛔ אינה חלק מהדוח הזה** (§8.1 שלב 1). שלושת הכיוונים נרשמים ⛔ ולא נבחרים.

---

### עלות — החלפת פלטה

| ציר | מדידה |
|-----|--------|
| קבצים | **40** באפליקציה + `LandingPage.jsx` (184 מופעים) כמשטח נפרד |
| ליטרלים | **386** מופעי הקס · **224** מחלקות שרירותיות (34 נבדלות) · **538** וריאנטי `dark:` |
| הפיכות | ✅ הפיך ב-`git revert` — ⛔ אין DB, ⛔ אין מיגרציה |
| אמון | ⚠️ **כן** — הצבע נושא סימן P&L; היפוך ירוק/אדום הוא מספר שגוי בשקט |
| כשל שקט | 🔴 **גבוה** — מחלקה שנשכחת ⛔ אינה שוברת בנייה ו⛔ אינה צועקת |
| **סיווג** | **`T3`** — שאלה 2 (אמון) «כן» ⇒ טריגר-יחיד לפי §15 |

**פסק על שדרוג-פלטה כמשימה עצמאית: ✂️ פצל.**
ההיררכיה (`M12`) והניגודיות (`M10`) הן `T2` בקומפוננטה אחת, ומספקות את רוב השיפור
הנתפס. החלפת פלטה רוחבית היא `T3` נפרד, ואין לערבב ביניהם.

---

## 4. מגבלות הדוח — מוצהרות

- ⛔ **אין אימות עין.** האבחון קרא קוד וחישב יחסי ניגודיות אריתמטית. **מספרי `M10`
  מחושבים ⛔ ולא נצפו בדפדפן** — ⇒ אישוש מול המסך החי נדרש לפני כל תיקון.
- **`emails/`** (283 מופעים) · **`public/`** (56) נמדדו והוצאו מהאיחוד החי; ⛔ לא נותחו.
- **`dist/`** ⛔ לא נסרק ⇒ ⛔ לא נמדד מה Tailwind באמת פלט אחר purge.
- הגבולות ב-`M9` (`4302-4819`) נקבעו לפי מתגי ה-`tab` ⇒ קומפוננטות ילד שמרונדרות
  משם וחיות בקבצים אחרים ⛔ לא נספרו בבלוק הדאשבורד.
- ⛔ **מוחרג:** `B-176`/`B-178` (מוקפאים) — ⛔ לא נגענו ולא נמדדו.
