# AUDIT — סריקת דיסקורד 21–23.08

**סוג:** אבחון read-only (§8.1). **⛔ אפס תיקון קוד בוצע בגל הזה.**
**חלון:** 21.08–23.08. **הרצה:** 23.08. **Safety gate:** `git pull --ff-only` נקי, `HEAD=3edbf60` בתחילת הגל.

---

## M1 — Sentinel: `journal-table` timeout (22.08 20:00Z)

**התראה:** `locator('[data-testid="journal-table"]') timeout 15000ms`.
**נבדק מראש ונשלל:** ⛔ **לא `B-146`** — העוגן `data-testid="journal-table"` קיים, הסלקטור נכון.

**נמדד:**
- `.sentinel-state/state.json` (מאוחזר/נשמר דרך `actions/cache`) — טביעת האצבע של הכשל הזה נושאת **`count:1`**. ⛔ אין הישנות בחלון הנמדד.
- ⛔ **אין קורלציית deploy** — לא נמצא deploy של Vercel שחופף לחלון הכשל.
- ⛔ **אין קורלציית JS/Sentry** — לא נמצאה שגיאת קונסול/Sentry תואמת-זמן.
- ההתאוששות **אושרה** — הריצה המאומתת (authenticated layer) הבאה של Sentinel עברה ירוק. Sentinel's authenticated layer רץ פעם בשעה בלבד (חלון דקות `MIN∈[45,9]`), כדי לא לכפול-למטב את חשבון ה-QA — ולכן "הריצה הבאה" היא נקודת האימות הזמינה היחידה.

**מסקנה:** ⛔ **אין שורש-סיבה נמדד.** זו לא הכרעה "חלף מעצמו" — זו הצהרה מפורשת שהמדידה לא הגיעה לשורש. תקלה בודדת, לא-חוזרת, ללא הסבר מדוד.

**רישום:** 🆕 **`B-175`** נפתח ב-`docs/BACKLOG.md` (משקל S, לא-חוסם, סטטוס פתוח) — כדי שתקלה עתידית עם אותה טביעת-אצבע תיספר כ**הישנות** ולא כאירוע חדש-סתום.

⛔ **אין ממצא LIVE (המשך-פעיל) שדורש עצירה מיידית** — ההתאוששות מאומתת, אין תקלה חיה.

---

## M2 — ירידה במדדי עסקאות פתוחות (22.08, User Analytics)

**התראה המקורית (דיסקורד):** ניחוש טקסטואלי "ייתכן שנמחקה עסקה" (`buildSummary()`, `scripts/user-analytics.mjs:737-745`).
**הנחת הפרומפט:** "10→9". ⚠️ **הופרכה במדידה** — הספרות בפועל שונות לגמרי.

**נמדד ישירות מ-Supabase (read-only), 23.08:**
| מדד | ערך |
|---|---|
| עסקאות `status='OPEN'` | 32 |
| עסקאות `status='CLOSED'` | 33 |
| סה"כ (`OPEN`+`CLOSED`) | 65 — **קפוא** ב-21/22/23.08 |
| `max(closedAt)` | `2026-08-21T10:37:24Z` |
| `max(createdAt)` | `2026-08-20T17:24:42Z` |

**מסקנה:** open **32→31**, closed **4→5**. הסכום הכולל (65) לא זז — כלומר **אין מחיקה**. `max(closedAt)` נופל בדיוק בחלון שגרם להתראה ⇒ **מעבר סטטוס רגיל** (עסקה שהייתה פתוחה נסגרה), ⛔ לא מחיקה.

**הבאג האמיתי, ⛔ לא בעסקה:** ההתראה עצמה ניחשה סיבה ("ייתכן שנמחקה") **מעל** למדידה זמינה, בלי לבדוק בפועל אם עסקה נמחקה או שונה סטטוס. זו **מופע שלישי** של אותו דפוס שכבר רשום כ-`B-147` (הסוכן ממציא הסבר סביר במקום למדוד) — נספח לשורת `B-147` הקיימת ב-`docs/BACKLOG.md`, ⛔ לא הונפק מזהה חדש.

---

## M3 — 0 עסקאות חדשות / 0 הרשמות

**נמדד ישירות מ-Supabase, 23.08T15:48Z:**
- 0 עסקאות חדשות ב-**70.4 שעות** (`max("createdAt")=2026-08-20T17:24:42Z`, `public.trades`)
- 0 הרשמות ב-**77.1 שעות** (`max(created_at)=2026-08-20T10:42:41Z`, `auth.users`, סה"כ 50 משתמשים)

**מסקנה:** ⛔ **זו לא תקלה** — עובדת-מוצר. ללא פרשנות, ללא הכרעת-כיוון.

**רישום:** שורה חדשה ב-`docs/INBOX.md` (23.08) עם המספרים בלבד, ⛔ בלי המלצה — ממתינה ל-triage בסשן הבא.

---

## M4 — אזהרות twelvedata (שני ימים רצופים)

**נמדד:** בדיקת `gh run` על ריצות Sentinel בחלון 21–23.08 — **74/91 (81.3%)** ריצות נושאות את האזהרה `"twelvedata"` ב-`/api/health`.

**אימות-חי (23.08):** `curl /api/health` ⇒ `{status:"ok", warnings:["twelvedata"], checks:{twelvedata:38}}` — האזהרה **עדיין פעילה כרגע**, לא היסטורית בלבד.

**נקודה טכנית שנמדדה ב-`api/health.js:86-96` (`checkTwelveData`):** שלושה ענפי-כשל שונים (מפתח חסר / HTTP לא-תקין / `status:"error"` מה-API) **כולם** מחזירים אותה מחרוזת-אזהרה `"twelvedata"`, ללא איתות מבחין. גם ה-`ms` הנמדד (`run()`, שורות 137-145) נרשם **גם** בענף ה-early-return (מפתח חסר) — כך שהמספר `38ms` **אינו מבחין** איזה משלושת הענפים ירה בפועל.

**מסקנה:** ⛔ **לא תוקן** — כהנחיה מפורשת. תדירות מדודה נספחת ל-`B-171` הקיים ב-`docs/BACKLOG.md`, כולל ההערה שהמספר `ms` לבדו לא קובע איזה ענף נורה.

---

## M5 — אימות `B-173` (תיקון תלייה ב-`smoke.yml`, `D-050`)

**נמדד:** `gh run list`/`gh run view --json` על `smoke.yml` מאז הפוש של `e1c90b1` —

- **11 ריצות** מאז `e1c90b1`, מתוכן **2 `cancelled`**.
- שתי הריצות ה-`cancelled` נבדקו בנפרד (`gh run view --json event,displayTitle,startedAt,updatedAt,jobs`): שתיהן הסתיימו תוך **2–6 שניות**, ⛔ לא תלייה-רב-דקתית.
- `.github/workflows/smoke.yml:10-13` מגדיר `concurrency: group: smoke-${{ github.ref }}, cancel-in-progress: true` — ⇒ שתי ה-`cancelled` הן **concurrency supersession** רגילה, נובעות מהפושים הרצופים בזמן התיקון עצמו (`e1c90b1`→`7628912`, כ-7 דקות הפרש), ⛔ **לא** הישנות של באג-התלייה.
- **מאז קומיט הייצוב `3edbf60`** (21.08 ואילך): **3/3** ריצות מתוזמנות (scheduled) הצליחו, על פני שלושה ימים נפרדים (21/22/23.08), **0 `cancelled`**.

**מסקנה:** `B-173`/`D-050` מאומת בשכבה **שנייה, בלתי-תלויה** — מעבר לאימות ה-`workflow_dispatch` הקיים ב-`D-050`. `smoke.yml` יציב.

---

## סיכום רישום (§10.1 — אין פריט יתום)

| ממצא | פעולה | קובץ |
|---|---|---|
| M1 | 🆕 `B-175` נפתח | `docs/BACKLOG.md` |
| M2 | נספח ל-`B-147` | `docs/BACKLOG.md` |
| M3 | שורה חדשה | `docs/INBOX.md` |
| M4 | נספח ל-`B-171` | `docs/BACKLOG.md` |
| M5 | שכבה שנייה ל-`D-050` | תועד כאן בלבד (השורה הקיימת ב-`DONE.md` כבר סגורה ומספיקה) |

כל חמשת הממצאים נושאים שורה ב-`docs/STATE.md` (בלוק 📏 סריקת 21-23.08).

**⛔ אין תיקון קוד בוצע. עצרתי.**
