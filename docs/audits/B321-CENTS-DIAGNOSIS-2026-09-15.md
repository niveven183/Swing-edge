# B-321 — אבחון read-only: «אגורות רפאים»

**תאריך:** 2026-09-15 · **HEAD בזמן המדידה:** `a55c8d6` · **רמה:** `T3`
**שלב:** 1 מתוך 2 (§8.1) — ⛔ **אפס תיקון**. כל מספר כאן נמדד, ⛔ לא הוסק.

---

## 0. תקציר ההכרעה

| מה | הפרוזה ב-`BACKLOG` (14.09) | **נמדד 15.09** | פסק |
|----|---------------------------|----------------|------|
| מופעי `Math.round(` ב-`SwingEdge_App.jsx` | `37` | **`43`** (על `38` שורות) | ⛔ נסחף |
| שומרי-אגורות `Math.round(x*100)/100` באפליקציה | `5` | **`7`** | ⛔ נסחף |
| אתרי המחלקה | `8` מופעים על `7` שורות | **`8` מופעים על `7` שורות** — ✅ תואם, **אבל ההגדרה הייתה צרה מדי** | ⚠️ |
| **גודל המחלקה האמיתי** | `7` | **`14`** | 🔴 |

🔴 **הפרוזה מנתה `grep -c` (שורות) מול `grep -o` (מופעים) בלי לומר זאת.** `38` שורות
נושאות `43` מופעים. כל מספר כאן מסומן במפורש — **מופעים** או **שורות**.

🔴 **הגידול `37 → 43` הוא הצלחה, ⛔ רגרסיה** — `Math.round(x*100)/100` הוא ה**תיקון**,
והוא מוסיף מופע. `B-297` הוסיף שניים (`5 → 7`).

---

## 1. ① המחלקה — הגדרה מדודה

**ההגדרה שהייתה:** `fmt$(Math.round(X))` ערום.
**ההגדרה המדודה:** ערך כספי שעבר `Math.round` ערום ומגיע ל**מעצב שכופה
`minimumFractionDigits: 2`** — **בין אם ישירות ובין אם דרך payload של גרף.**

**מה מכריע — המעצב, ⛔ הצורה.** נמדד ב-`src/utils.js`:

| מעצב | ספרות | `fmt(Math.round(1234.47))` | כופה `.00`? |
|------|-------|---------------------------|-------------|
| `fmt$` = `fmtMoney` (`:196`) | `min 2 / max 2` | `+$1,234.00` | 🔴 **כן** |
| `fmtBalance` (`:280`) | `min 2 / max 2` | `$1,234.00` | 🔴 **כן** |
| `fmtAccountAmount` → `fmtMoney` (`:207`) | `min 2 / max 2` | `+$1,234.00` | 🔴 **כן** |
| `fmt$0` = `fmtMoney0` (`:197`) | `min 0 / max 0` | `$1,234` | ✅ לא |
| `fmtPrice` (`:240`) | `min 0 / max 2` | `$1,234` | ✅ לא |
| `.toLocaleString("en-US")` ערום | ברירת מחדל | `1,234` | ✅ לא |

⚠️ **זה מפריך שני אתרים שהצורה הפלילה אותם.** `fmtPrice(Math.round(avgWin))`
ב-`:825`/`:830` **תואם את הצורה בדיוק** ו⛔ **אינו במחלקה** — `fmtPrice` הוא
`min 0`, והמחרוזת יוצאת `$1,234`. ⇒ **② אושש: מדדנו, ⛔ הסקנו מהצורה.**

---

## 2. ① מנייה מלאה לפי קטגוריה — `43/43` מופעים ב-`SwingEdge_App.jsx`

| # | קטגוריה | מופעים | במחלקה? |
|---|----------|--------|---------|
| 1 | 🔴 **מחלקה — ישיר:** round ערום ← `fmt$`/`fmtAcct` | **`6`** | **כן** |
| 2 | 🔴 **מחלקה — עקיף:** round ב-payload ← tooltip/תא `fmt$`/`fmtBalance` | **`8`** | **כן** |
| 3 | ✅ שומר-אגורות `Math.round(x*100)/100` — ה**תיקון** | `7` | לא |
| 4 | ✅ שלם-מכוון: `Math.round(x).toLocaleString()` | `9` | לא (③) |
| 5 | ✅ `fmtPrice(Math.round(…))` — המעצב ⛔ כופה | `4` | לא |
| 6 | ✅ אחוזים (`winRate` · `lossRate`) | `6` | לא |
| 7 | ✅ פיקסלים (`MAX_EDGE_PX`) | `1` | לא |
| 8 | ⚪ `acctRefusalText(t, Math.round(pnl))` — הסכום ⛔ מרונדר, מזין **הכרעה** | `1` | לא (אך תאום של A2) |
| 9 | ⚪ `pnl: Math.round(pnl)` ב-`:383` — שדה payload ש⛔ **נצרך בשום מקום** | `1` | לא (קוד מת) |
| | **סה"כ** | **`43/43`** | **`14` במחלקה** |

**מחוץ לקובץ:** `116` מופעים ב-`src/` + `SwingEdge_App.jsx` על `22` קבצים.
`73/116` יושבים ב-`src/intelligence/` ו-`src/components/` והם **אחוזים · ימים ·
מילישניות · פיקסלים · ציוני ביטחון** — ⛔ אף אחד מהם אינו כסף שנכנס למעצב דו-ספרתי.

---

## 3. ② + ③ — כל אתר במחלקה, מה המשתמש רואה

`X = 1234.47`. «נראה» = המחרוזת בפועל, נמדדה ע"י הרצת הביטוי המחולץ מהבייטים.

### 3א · ישיר (`6`)

| מזהה | שורה | מה מוצג | מסך | נראה | ③ מכוון? |
|------|------|---------|-----|------|-----------|
| `A1` | `:3009` | toast «רווח … נסגר בהצלחה» | כל סגירת עסקה | `+$1,234.00` | ⛔ **לא** — ההערה בקוד אומרת «נשמר בדיוק כפי שהיה» (שימור התנהגות בגל ג׳), ⛔ **אינה** טוענת ששלם הוא הכוונה |
| `A2` | `:4727` | תא P&L בטבלת מנטי | פאנל מנטור | `+$1,234.00` | ⛔ לא |
| `A3` | `:5112` | כרטיס «רווח ממוצע» | ג'ורנל | `+$1,234.00` | ⛔ לא |
| `A4` | `:5116` | כרטיס «הפסד ממוצע» | ג'ורנל | `-$1,234.00` | ⛔ לא |
| `A5` | `:5136` | כרטיס «Max Drawdown» | ג'ורנל | `-$1,234.00` | ⛔ לא |
| `A6` | `:6334` | «היום הטוב ביותר» | אנליטיקה | `+$1,234.00` | ⛔ לא |

### 3ב · עקיף (`8`) — ה**תת-מחלקה ש⛔ נראתה באף מדידה קודמת**

הערך מעוגל ב-payload של הגרף; ה-tooltip/התא מעצב אותו ב-`fmt$`/`fmtBalance`.

| מזהה | עיגול | צרכן | מה מוצג | מסך | נראה |
|------|-------|------|---------|-----|------|
| `B1` | `:383` | tooltip `:6171` `fmtBalance` | עקומת הון | אנליטיקה | `$1,234.00` |
| `B2` | `:523` | tooltip `:4611` `fmtBalance` | עקומת הון מנטי | מנטור | `$1,234.00` |
| `B3` | `:6190` | tooltip `:6194` `fmt$` | P&L לפי עסקה | אנליטיקה | `+$1,234.00` |
| `B4` | `:6234` | tooltip `:6248` `fmt$` | P&L לפי יום בשבוע | אנליטיקה | `+$1,234.00` |
| `B5` | `:6447` | tooltip `:6534` `fmt$` | P&L לפי חודש | אנליטיקה | `+$1,234.00` |
| `B6` | `:6457` | tooltip `:6561` `fmt$` | P&L לפי רגש | אנליטיקה | `+$1,234.00` |
| `B7` | `:6476` | **תא טבלה** `:6667` `fmt$` | P&L לפי setup | אנליטיקה | `+$1,234.00` |
| `B8` | `:6484` | tooltip `:6721` `fmt$` | פיזור החזקה↔P&L | אנליטיקה | `+$1,234.00` |

⚠️ **`B7` ⛔ הוא גרף** — הוא **תא בטבלה**. מי שיקרא את `B1`–`B8` כ«רק tooltips»
ויתקן גיאומטריה בלבד ישאיר אותו שבור.

### 3ג · ③ — איפה העיגול **כן** מכוון (אושש)

`9` אתרי `Math.round(x).toLocaleString()` — `:406` (ציר Y) · `:951` · `:4465` ·
`:4467` · `:4468` · `:4890` · `:6150`×2 · `:7941`. כולם **כותרות/תקצירי הון**
(«התחלת ב-$10,000»). ⛔ **אינם במחלקה** — הם כבר מדפיסים `$10,000` בלי `.00`,
כלומר העיגול **מתממש** ואינו מבטיח אגורות. **⛔ לגעת בהם.**

---

## 4. ④ — `fmt$0` מול `fmt$`

`fmt$0 = fmtMoney0 = money(n, 0)` — **אותה פונקציה, `0` ספרות** (`utils.js:197`).
⇒ **אתר שרוצה שלם צריך `fmt$0(X)`, ⛔ `fmt$(Math.round(X))`.** `fmt$0` כבר מיובא
ב-`SwingEdge_App.jsx:55` ונצרך ב-3 קבצים (`TradeCalendar` · `WeeklyReviewTab` ·
`MonthlyReportTab`) — ⛔ אין צורך בכלי חדש.

---

## 5. האסרציה — נצפתה אדומה ⛔ לפני ירוק

**ניסוח אחיד לכל שורה:** ערך עם אגורות ⇒ המחרוזת המרונדרת **⛔ מסתיימת ב-`.00`**.
⛔ **הביטוי מחולץ מהבייטים של `SwingEdge_App.jsx` לפי עוגן ומורץ** — ⛔ מועתק ידנית
(`R-6`). **כשל חילוץ = אדום קשה, ⛔ דילוג** (`B-272`); נאכף בשער-מטא «עוגן ≠ 1».

```
── CLASS (expect RED on current tree) ──
  ✗ A1 toast (fmtAcct)               :3009  -> "+$1,234.00"
  ✗ A2 journal cell (fmtAcct)        :4727  -> "+$1,234.00"
  ✗ A3 journal avgWin                :5112  -> "+$1,234.00"
  ✗ A4 journal avgLoss               :5116  -> "-$1,234.00"
  ✗ A5 journal maxDD                 :5136  -> "-$1,234.00"
  ✗ A6 best day                      :6334  -> "+$1,234.00"
  ✗ B1 equity curve -> fmtBalance    :383   -> "$1,234.00"
  ✗ B2 mentee equity -> fmtBalance   :523   -> "$1,234.00"
  ✗ B3 pnl-by-trade -> fmt$          :6190  -> "+$1,234.00"
  ✗ B4 pnl-by-weekday -> fmt$        :6234  -> "+$1,234.00"
  ✗ B5 pnl-by-month -> fmt$          :6447  -> "+$1,234.00"
  ✗ B6 emotion totalPnL -> fmt$      :6457  -> "+$1,234.00"
  ✗ B7 setup totalPnL -> fmt$        :6476  -> "+$1,234.00"
  ✗ B8 hold scatter -> fmt$          :6484  -> "+$1,234.00"

── CONTROL ARM (expect GREEN on current tree) ──
  ✓ K1 fmtPrice(round) avgWin        :825   -> "$1,234"
  ✓ K2 fmtPrice(round) avgLoss       :825   -> "$1,234"
  ✓ K3 emotion winRate (pct)         :6458  -> 62
  ✓ K4 setup winRate (pct)           :6473  -> 62
  ✓ K5 cents-FIX form (totalPnL)     :4469  -> "+$1,234.47"
  ✓ K6 whole-$ toLocaleString        :4890  -> 1234

meta-gate failures: 0
CLASS   red: 14/14   (want 14/14)
CONTROL green: 6/6   (want 6/6)
```

⚠️ **זרוע הביקורת היא מה שמגדיר את הרוחב.** `K1`/`K2` הם `fmtPrice(Math.round(…))` —
**אותה צורה בדיוק** כמו המחלקה. הם **ירוקים** ⇒ ההגדרה נתלית ב**מעצב**, ⛔ בצורה.
`K5` הוא שומר-האגורות מ-`B-297` ⇒ מוכיח שהשער מכיר את התיקון ו⛔ יורה עליו.
⇒ **`14/14` אדום · `6/6` ירוק · `0` שערי-מטא. ההגדרה ⛔ רחבה מדי ו⛔ צרה מדי.**

⚠️ **`K1` ירה שער-מטא בריצה הראשונה** («עוגן `fmtPrice(` התאים 2×») — ⛔ תוקן
ע"י ריכוך השער אלא ע"י הידוק העוגן. זו ההוכחה ששערי-המטא חיים.

---

## 6. מה **⛔** נמדד כאן — גבול האבחון

- ⛔ **רינדור**: JSX · React · recharts · דפדפן אמיתי · RTL · פרודקשן.
  ההרצה היא של **ביטוי מחולץ** ב-Node ⇒ היא מוכיחה **ערך**, ⛔ שהמשתמש רואה.
  אותו גבול בדיוק של `C-036`·`C-038`·`C-039`·`C-041`·`C-043` ⇒ **חייב `C-` חדש.**
- ⛔ **`src/components/`**: `MobileTradeCard` · `DayTradesModal` · `MonthlyReportTab`
  ⛔ נסרקו לתבנית «payload מעוגל». `116` המופעים נמנו, `14` המחלקה היא של
  `SwingEdge_App.jsx` **בלבד**.
- ⛔ **`api/` · DB · `dailyPnL` · `i18n` · `fmt$` עצמו** — מחוץ לתחום בהוראה.

---

## 7. פריטים שנולדו כאן (§10.1 · §11)

| # | ממצא | הצעה |
|---|------|------|
| 1 | שורת `B-321` ב-`BACKLOG` נושאת `7` אתרים · `37` מופעים · שורות `:5108`/`:5132`/`:6330` — **כולם נסחפו** | תיקון המדידה בשורה, ⛔ פתיחת מזהה חדש |
| 2 | `:383` `pnl: Math.round(pnl)` — שדה payload ש⛔ נצרך בשום מקום (**קוד מת**) | `B-` חדש |
| 3 | תת-המחלקה העקיפה (`8` אתרים) ⛔ נראתה ב-`D-068` ו⛔ ב-`B-297` — **הגדרה צרה מדי היא איך שמחלקה נסגרת פעמיים ו⛔ נסגרת** | לקח ל-`DECISIONS` |
| 4 | `src/components/` ⛔ נסרק לתבנית העקיפה | `B-` חדש |
