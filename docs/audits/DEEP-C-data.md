# DEEP · פאזה C — דיוק נתונים מקצה לקצה

**תאריך:** 2026-08-26 · **סוג:** אבחון read-only (§8.1 שלב 1) · **רמה:** T3
**היקף:** חישוב מחדש ידני של 3 משתמשי פרודקשן · עקביות DB · מצבי-קצה · מטבע מעורב ·
אידמפוטנטיות · אזור-זמן · מקור ההון · סנכרון localStorage↔DB.

⛔ **אפס שינוי קוד, קונפיג, DB או הגדרות.** כל SELECT בלבד.
⛔ **B-176 / B-178 לא נגעתי** (קפואים עד 26.08 14:18Z).
⛔ **אפס שמות משתמשים** — מזהים מקוצרים ל-8 תווים בלבד.

**ספירה:** P0=2 · P1=4 · P2=13 · P3=1 · **נבדק ונקי: 8**

---

## 0. מתודולוגיה — שתי מימושים בלתי-תלויים

הפאזה לא נשענת על קריאת קוד. לכל משתמש חושבו כל מספרי-הכותרת **פעמיים**:

1. **מסלול א׳ — SQL.** אריתמטיקה משלי ישירות על השורות הגולמיות ב-Postgres.
2. **מסלול ב׳ — קוד האפליקציה עצמו.** `computeTradingStats` · `calcTradeMetrics` ·
   `calculateTradeDNA` · `calculateGrowthScore` יובאו מהריפו והורצו על **אותן**
   שורות בדיוק (סקריפט ב-`/tmp`, ⛔ מחוץ לריפו, כדי שלא ייקומט לעולם).

כל פער בין השניים הוא ממצא. זו התשובה הישירה למשפחת `R-3` — אסרציה שמאמתת את
מה ש**נכתב** ולא את מה ש**נראה**.

**אוכלוסייה:** 14 משתמשים עם עסקאות · **66 עסקאות** · 11 טבלאות.
שלושת הנבדקים נבחרו לפי נפח עסקאות סגורות:

| משתמש | סה״כ | סגורות | פתוחות | `_capitalAtEntry` |
|--------|------|--------|---------|-------------------|
| `ad1a0494` | 22 | 22 | 0 | 3,000 |
| `eb61d502` | 16 | 7 | 9 | 10,000 |
| `1ad72482` | 5 | 3 | 2 | 10,000 |

---

## 1. תוצאת ההצלבה — הליבה החשבונית **נקייה**

`computeTradingStats` תואם את החישוב העצמאי שלי **בכל מספר כותרת, בשלושת
המשתמשים, עד 6 ספרות אחרי הנקודה**:

| מדד | `1ad72482` | `ad1a0494` | `eb61d502` |
|------|-----------|-----------|-----------|
| n סגורות | 3 | 22 | 7 |
| W/L/BE | 1/2/0 | 5/17/0 | 7/0/0 |
| winRate | 33.333333% | 22.727273% | 100% |
| **סכום שלושת השיעורים** | **100.000000** | **100.000000** | **100.000000** |
| totalPnL | −520 | −279.5696 | 11,368.01 |
| grossWin / grossLoss | 130 / 650 | 144.4204 / 423.99 | 11,368.01 / 0 |
| profitFactor | 0.200000 | 0.340622 | `Infinity` |
| avgR · rSample | −1.500000 · 2 | `null` · **0** | 16.414894 · 7 |

**האינוריאנטה `winRate + lossRate + beRate === 100` מתקיימת במדויק** בשלושתם —
⛔ לא מעוגלת, לא בקירוב.

`profitFactor = Infinity` אצל `eb61d502` (7 ניצחונות, אפס הפסדים) הוא **סנטינל
מוצהר** ב-`src/intelligence/utils/statisticalModels.js:184-188`, ⛔ ואינו ממצא —
ה-`null` שהתקבל אצלי נבע מ-`nullif` שלי. פער צפוי ומוסבר.

`avgR = null` עם `rSampleSize = 0` אצל `ad1a0494` הוא **התנהגות נכונה**: 22/22
שורותיו נטולות `stop`, ולכן R אינו ניתן למדידה. חוזה ה-`null` כובד כאן.

---

## 2. ממצאים

### 🔴 P0

---

**`C-01` · `src/intelligence/core/TradeDNA.js:106` + `:157` · עסקה בלי סטופ נמדדת כסטופ ב-אפס**

**מה נמדד.** שני האתרים מריצים:

```js
const stop = Number(t.stop);
if (!Number.isFinite(entry) || !Number.isFinite(stop) || !Number.isFinite(shares)) return 0;
const riskPerShare = Math.abs(entry - stop);
```

`Number(null) === 0`, ו-`Number.isFinite(0) === true`. השער ⛔ אינו יכול לתפוס
סטופ חסר. התוצאה: `riskPerShare = |entry − 0| = entry` ⇒ המנוע קורא את **שווי
הפוזיציה המלא** כסיכון, ואז `.filter(r => r > 0)` מכניס אותו פנימה בשמחה.

מדידה מספרית (שורה `eeac9048` — entry 222.805 · stop `NULL` · shares 6 · הון 3,000):

```
Number(null) = 0 · isFinite = true
riskPerShare = 222.805 → risk fraction = 0.44560999999999995
```

המנוע קורא **44.6% סיכון תיקי** על עסקה שאין לה סטופ בכלל.

**התוצאה הנמדדת** — `calculateTradeDNA` על 22 השורות האמיתיות של `ad1a0494`
(22/66 מהשורות בפרודקשן, **23/66 בסך הכול נטולות סטופ**):

```
scores.risk      = 0     ⇐ הציון הגרוע ביותר האפשרי
style.aggression = 100   ⇐ "אגרסיבי לחלוטין"
```

שני המספרים נגזרו מ**היעדר** נתון.

**שורש.** ⛔ לא "סטופ חסר" — אלא **שער שבודק שהערך הוא מספר סופי במקום לבדוק
שהוא נמדד** (משפחת `R-4`). ו-`B-156` הוסיף ב-`:163` שער `null` נכון
(`risks.length ? … : null`) — אבל השער הזה **⛔ אינו יכול לירות**, מפני שהמלכודת
שתי שורות מעליו מייצרת מערך לא-ריק מתוך סטופים ריקים. שער שנחסם מלירות אינו שער.

**ההוכחה שזו טעות ולא החלטה** — **אותו באג נמצא, הובן, תועד ותוקן בקובץ אחד
משמאל.** `src/intelligence/core/GrowthTracker.js:44-53`:

> `A trade without a stop has no measurable risk. Math.abs(entry - null) is entry, so including it charged the score the FULL POSITION VALUE as risk — a positive number, which the > 0 filter below happily let through.`

והתיקון שם: `closed.filter(t => t.stop != null && t.shares > 0 && …)`.
⇒ על **אותו ג׳ורנל בדיוק**, `GrowthTracker.riskManagement` מחזיר `null` (נמנע),
בעוד `TradeDNA.scores.risk` מחזיר `0` (מאשים). שני מנועים, אותן שורות, שתי
תשובות סותרות — `R-6`.

**התיקון הזול ביותר.** להעתיק את הפרדיקט שכבר קיים ב-`GrowthTracker.js:51` לשני
האתרים ב-`TradeDNA.js` — סינון `t.stop != null` **לפני** ה-`map`.

**⛔ מה אסור בתיקון.**
⛔ אסור להחליף `Number(t.stop)` ב-`Number(t.stop ?? NaN)` בלבד — זה מתקן את
הסימפטום באתר אחד ומשאיר את הפרדיקט מפוזר בשלושה קבצים (`R-6` חוזר).
⛔ אסור לייחס לעסקה בלי סטופ "סיכון ברירת מחדל" כלשהו — זו בדיוק ההמצאה
ש-`B-156` בא לחסל.
⛔ אסור להזיז את השער הקפוא של `test:dna` כדי שיעבור — מספר שזז שם **עוצר**.

**תלות.** `C-04` (בלוק ה-`style` חסר שער `null` לגמרי).

---

**`C-02` · `SwingEdge_App.jsx:1305-1306` · מכנה ההון מגיע מ-localStorage, וסותר את מה ששמור בעסקה**

**מה נמדד.**

```js
const [capital, setCapital] = useState(() => {
  try { return parseFloat(localStorage.getItem("swingEdgeCapital")) || DEFAULT_CAPITAL; } catch { return DEFAULT_CAPITAL; }
});
```

`DEFAULT_CAPITAL = 2500` (`src/utils.js:6`). המשתנה הזה הוא ה**מכנה** של
`returnPct` · `currentEquity` · `maxDrawdown` — דרך
`src/hooks/useTradingStats.js:23`. הוא נדרס מההגדרות מאוחר יותר
(`SwingEdge_App.jsx:1727`), אבל ⛔ רק אם יש רשומת הגדרות.

**שתי מדידות בפרודקשן:**

1. **`1ad72482` — סתירה חיה.** `user_settings.capital = 2500`, בעוד **כל
   חמש** עסקאותיו נושאות `_capitalAtEntry = 10000`. הרצתי את
   `computeTradingStats` על אותן שורות בדיוק, פעם עם כל הון:

   | | הון 10,000 | הון 2,500 |
   |---|---|---|
   | `currentEquity` | 9,480.00 | **1,980.00** |
   | `returnPct` | **−5.200000%** | **−20.800000%** |
   | `maxDrawdown` | **6.416584%** | **24.714829%** |

   אותו ג׳ורנל, פי-4 בשני מספרי הכותרת של תשואה וסיכון. ובו-זמנית מנועי
   ה-DNA שוקלים את אותן עסקאות מול `_capitalAtEntry = 10000` ⇒ **שני הונים
   שונים למשתמש אחד על מסך אחד.**

2. **3 משתמשים עם עסקאות ו⛔ בלי רשומת `user_settings` כלל:** `6eb77488`
   (עסקאות מול הון 500) · `a617018a` (10,000) · `cc0e76bd` (7,000).
   אצלם `setCapital(s.capital)` ⛔ לעולם אינו יורה. בדפדפן חדש או אחרי ניקוי
   אחסון, ההון נופל ל-`DEFAULT_CAPITAL = 2500` ⇒ `a617018a`, שסידר עסקאות מול
   10,000, יקבל תשואה מחושבת על 2,500: **הגזמה פי 4.**

**שורש.** מצב קריטי — מכנה של מספר כספי — חי באחסון נדיף, עם ברירת מחדל
שמחליפה נתון אמיתי (`R-5` + `R-2`). `_capitalAtEntry` נשמר **בכל שורה** ב-DB
(66/66 לא-null, ⛔ אפס אפסים), כלומר מקור-האמת קיים ואינו בשימוש במסלול הזה.

**התיקון הזול ביותר.** לגזור את ההון ההתחלתי מ-`_capitalAtEntry` של העסקה
הראשונה כרונולוגית כשאין רשומת הגדרות, ולהציג `null` כשאין אף מקור — ⛔ לא 2,500.

**⛔ מה אסור בתיקון.**
⛔ אסור "לתקן" בכתיבת ההון חזרה ל-`user_settings` — זו כתיבה על סמך ניחוש,
וזו פעולה בלתי-הפיכה על נתוני משתמש (§12).
⛔ אסור להשאיר `|| DEFAULT_CAPITAL` במסלול. מספר מנוחש במכנה של תשואה הוא
בדיוק "ברירת מחדל שמחליפה נתון אמיתי" (§2).
⛔ אסור לגעת ב-`B-176`/`B-178` תוך כדי — קפואים.

**תלות.** `C-01` (שני המנועים כבר קוראים `_capitalAtEntry`; זה יאחד את המקור).

---

### 🟠 P1

---

**`C-03` · `src/intelligence/core/GrowthTracker.js:101-102` · ציון "שליטה רגשית" הוא 100/100 לכל משתמש בבסיס הנתונים**

**מה נמדד.**

```js
const fomo  = closed.filter(t => t.emotionAtEntry === "FOMO").length;
const fear  = closed.filter(t => t.exitReason === "Fear").length;
const share = (fomo + fear) / closed.length;
return to100(1 - Math.min(1, share * 3));
```

שני הקלטים אפס בפרודקשן — אבל **מסיבות שונות**, וההבחנה היא הממצא:

| קלט | מצב | מדידה |
|------|-----|--------|
| `"FOMO"` | ✅ ערך **חוקי** ב-`src/data/tradeEnums.js:56` | 0/66 שורות — אוכלוסייה, ⛔ לא פגם |
| `"Fear"` | ⛔ **אינו קיים** ברשימת הכתיבה | 0/66 — **בלתי-ניתן להגעה מבנית** |

מסלול הכתיבה היחיד ל-`exitReason` הוא ה-`<select>` ב-
`src/components/EditTradeModal.jsx:322-332`, שנשען על
`EditTradeModal.jsx:11`:

```js
const EXIT_REASONS = ["Hit Target", "Hit Stop", "Manual Exit", "Trailing Stop", "Other"];
```

⛔ אין "Fear". אוצר-המילים בפועל ב-DB מאשר: `Hit Target`×14 · `Manual Exit`×3 ·
`Other`×1 · `Target Hit`×1 · `NULL`×47. ⇒ `fear` הוא **קבוע 0 לנצח**.

**התוצאה הנמדדת** — `calculateGrowthScore` על שלושת המשתמשים האמיתיים:

```
1ad72482 → {discipline:100,  riskManagement:25,   consistency:76,   edgeUtilization:null, emotionalControl:100}
ad1a0494 → {discipline:null, riskManagement:null, consistency:null, edgeUtilization:null, emotionalControl:100}
eb61d502 → {discipline:83,   riskManagement:100,  consistency:0,    edgeUtilization:null, emotionalControl:100}
```

השורה האמצעית היא ההוכחה: אצל `ad1a0494` **ארבעת** תת-הציונים האחרים מחזירים
`null` — האוכלוסייה אינה נמדדת, והחוזה כובד. **`emotionalControl` לבדו מחזיר
100 מושלם** על ג׳ורנל שבו 22/22 השורות נושאות `emotionAtEntry = ""` ואפס
`exitReason`. ⇒ ציון מושלם שנגזר מאפס נתונים.

**שורש.** אות התנהגותי מחווט למחרוזת שמסלול הכתיבה ⛔ אינו יכול להפיק
(`R-4`) — ולכן `share = 0` הוא "לא נמדד" שמתחזה ל"מצוין". ⛔ שער ה-`null`
בראש הפונקציה (`if (!closed.length) return null`) בודק שהג׳ורנל אינו ריק,
⛔ ולא שהאות נמדד.

⚠️ **סתירה מתועדת.** `CLAUDE.md` §7 מצהיר ש-`test:dna` (27 אסרציות) "בודק
ש-4 ציוני ה-DNA ו**חמשת** תת-הציונים של ה-Growth מחזירים `null` על אוכלוסייה
לא-נמדדת ⛔ ולא `50`/`100` מומצא (B-156)". המדידה מראה שתת-הציון החמישי ⛔ אינו
מקיים זאת. השער קיים ואינו תופס את המקרה הזה.

**התיקון הזול ביותר.** להחזיר `null` כאשר אף אחד משני האותות אינו ניתן למדידה
על האוכלוסייה — כלומר כאשר ⛔ אין ולו שורה אחת עם `emotionAtEntry` **או**
`exitReason` שנרשמו. ובנפרד: או להוסיף `"Fear"` ל-`EXIT_REASONS`, או להסיר
את הקריאה אליו — ⛔ אבל לא להשאיר את שניהם.

**⛔ מה אסור בתיקון.**
⛔ אסור להחזיר `50` כ"אמצע" — זו ההמצאה ש-`B-156` חיסל.
⛔ אסור להוסיף `"Fear"` לרשימה **בלי** להחליט מה קורה ל-14 שורות `Hit Target`
הקיימות — הוספת אופציה אינה מסווגת רטרואקטיבית.
⛔ אסור לשנות `value` של מחרוזת קיימת ב-`tradeEnums.js` — מחרוזות load-bearing
(§13), והן מושוות ישירות בארבעה מודולים.

**תלות.** `C-04` · `C-07`.

---

**`C-04` · `src/intelligence/core/TradeDNA.js:93-96` · אותו אובייקט מחזיר `discipline: null` ו-`discipline: 50` בו-זמנית**

**מה נמדד.** `calculateTradeDNA` על 22 השורות של `ad1a0494`:

```
scores = {risk: 0,          discipline: null, consistency: null, growth: null}
style  = {aggression: 100,  discipline: 50,   patience: 50,      tilt: 0}
```

**המילה `discipline` מופיעה פעמיים באותו אובייקט מוחזר עם שתי תשובות שונות
לאותה שאלה על אותן שורות** — `null` ("לא נמדד") ו-`50` ("בדיוק ממוצע").

הסיבה מדודה: `computeScores` תוקן ב-`B-156` (`TradeDNA.js:168-170`,
`dRate == null ? null : to100(dRate)`, עם הערה מפורשת ש"`== null ? 50` היה
עצמו התרגום השגוי מ'לא נמדד' ל'בדיוק ממוצע'"), אבל `inferStyle` — הבלוק המקביל
— **⛔ לא תוקן**, ופותח ב-`TradeDNA.js:95` ב-
`return { aggression: 50, patience: 50, discipline: 50, tilt: 0 }`.

`patience: 50` אצל `ad1a0494` מגיע לאותו מקום בדרך אחרת:
`to100((planCloses − fearCloses) / closed.length + 0.5)` עם `planCloses = 0`
ו-`fearCloses ≡ 0` (ראה `C-03`) ⇒ `to100(0.5) = 50` — הפעם דרך חישוב **אמיתי**,
כך ששער-`null` ⛔ אינו יכול לתפוס אותו.

**שורש.** `R-6` — שני עותקים של אותה שאלה, תיקון שהוחל על אחד בלבד.

**התיקון הזול ביותר.** להחיל את חוזה ה-`null` על ארבעת שדות `style` באותה צורה
בדיוק שבה הוא הוחל על `scores`.

**⛔ מה אסור בתיקון.** ⛔ אסור לתקן את `style` בלי להרחיב את `test:dna` — אחרת
זהו בדיוק `R-3` (אסרציה שמאמתת את מה שנכתב). ⛔ אסור לאחד את `scores` ו-`style`
לאובייקט אחד במסגרת התיקון — זה ריפקטור, ⛔ לא תיקון.

**תלות.** `C-01` · `C-03`.

---

**`C-05` · `src/lib/tradingStats.js:335` · `planAdherence` — מכנה שאינו יכול להכיל את מה שנספר**

**מה נמדד.**

```js
planAdherence: closed.length ? (planYes.length / closed.length) * 100 : 0,
```

המכנה הוא **כל** העסקאות הסגורות. המונה הוא `isFollowedPlan(m.followedPlan)`.
אוצר-המילים כפי שהוא שמור, כפי שנמדד בפועל על 43 השורות של שלושת המשתמשים:

```
"null"       n=35  isFollowedPlan=false  isOffPlan=false
"true"       n=7   isFollowedPlan=true   isOffPlan=false
"Partially"  n=1   isFollowedPlan=false  isOffPlan=false
```

⇒ **35/43 (81.4%) מהשורות ⛔ מעולם לא נענו**, ונספרות כ"לא עמד בתוכנית".
בנוסף `"Partially"` יושב במכנה ו⛔ אינו יכול להיכנס למונה לעולם.

התוצאה הנמדדת אצל `ad1a0494` (22/22 `NULL`): **`planAdherence = 0.000000`** —
"אף פעם לא עקבת אחרי התוכנית", כשהאמת היא "מעולם לא רשמת".

זו בדיוק השאלה של `CLAUDE.md` §2: *"האם המכנה יכול בכלל להכיל את מה שנספר?"*
כאן — לא. המספר מודד את **גודל האוכלוסייה**, ⛔ לא התנהגות.

⚠️ ההערה ב-`tradingStats.js:246` מכנה את `planAdherence` "the denominator's
public form" — ושתי השורות מתחתיה (`planFollowedWR` · `planIgnoredWR`) ⛔ אכן
מחזירות `null` על דלי ריק. השורה שהוגדרה כצורתו הפומבית של המכנה היא היחידה
שאינה מכבדת את הכלל.

**למה P1 ולא P0.** `grep` על כל `src/` · `api/` · `scripts/`: **⛔ אין ולו צרכן
UI אחד**. `planAdherence` מחושב ונבדק, ⛔ ואינו מרונדר. הוא מלכודת דרוכה במרחק
רינדור אחד מ-P0.

⚠️ **והשער ⛔ אינו יכול לתפוס זאת.** ארבע האסרציות ב-
`scripts/tradingstats-test.mjs:467` · `:608` · `:618` · `:755` מציבות
`followedPlan` **מפורשות בכל שורת fixture** (`true` / `false` / `"true"` /
`"false"`). ⛔ אין ולו fixture אחד עם `followedPlan` חסר — כלומר החבילה ⛔ אינה
יכולה להבחין בין "חרג מהתוכנית" לבין "לא נשאל", וזה המצב של 81.4% מהפרודקשן.
`R-3` בצורתו הנקייה.

**התיקון הזול ביותר.** מכנה = `planYes.length + planNo.length` (האוכלוסייה
ש**נענתה**), ו-`null` כשהיא ריקה — בדיוק כמו שתי השורות שמעליה.

**⛔ מה אסור בתיקון.**
⛔ אסור לספור `"Partially"` כ"עקב" או כ"חרג" — זו הכרעת מוצר, ⛔ לא ריפקטור.
⛔ אסור לתקן בלי להוסיף fixture עם `followedPlan` **חסר** — אחרת השער נשאר עיוור.

**תלות.** אין.

---

**`C-06` · שני מנועים, אותו ג׳ורנל, שתי תשובות ל-`profitFactor`**

**מה נמדד.** על 7 השורות הסגורות של `eb61d502` (7 ניצחונות, 0 הפסדים):

| מקור | ערך |
|------|-----|
| `computeTradingStats(...).profitFactor` | `Infinity` |
| `calculateTradeDNA(...).metrics.profitFactor` | `null` |

`Infinity` הוא סנטינל **מוצהר ומתועד** ב-
`src/intelligence/utils/statisticalModels.js:184-188`, עם נימוק כתוב שהוא ⛔ לא
`null` מפני ש-`isFinite(null) === true`. ⇒ `TradeDNA` ⛔ אינו מכבד את הסנטינל
שהמודול המשותף מגדיר.

**שורש.** `R-6` — שני מסלולים לאותה שאלה, ⛔ בלי בעלים אחד.

**התיקון הזול ביותר.** ש-`TradeDNA.metrics` יקרא ל-`profitFactorFromPnls`
המשותף במקום לגזור בעצמו.

**⛔ מה אסור בתיקון.** ⛔ אסור "לנרמל" את `Infinity` ל-`null` במודול המשותף כדי
ליישר קו — הנימוק הכתוב שם הוא הסיבה שהסנטינל נבחר, וההיפוך ישבור צרכנים
שכבר בודקים `isFinite`.

**תלות.** אין.

---

### 🟡 P2

---

**`C-07` · `exitReason` — טקסט חופשי בלי CHECK, ושתי איות לאותו מושג**

`pg_get_constraintdef` על `public.trades` מחזיר ארבעה CHECK בלבד:
`currency` · `currency_source` · `horizon` · `source`. ⛔ **אין CHECK על
`exitReason`.** אוצר-המילים בפועל: `Hit Target`×14 · `Manual Exit`×3 ·
`Other`×1 · **`Target Hit`×1** · `NULL`×47.

`"Target Hit"` ⛔ אינו ברשימת `EXIT_REASONS` — כלומר הוא הגיע ממסלול כתיבה
**אחר** מה-`<select>`. שתי איות לאותו מושג בעמודה שמושווית ישירות בקוד (§13).
**תיקון:** CHECK על אוצר-המילים + מיפוי חד-פעמי של השורה החורגת (מיגרציה
בקובץ `.sql` בלבד — ניב מריץ, §12).

---

**`C-08` · שדה זמן-סגירה מאוכלס על 9 שורות פתוחות**

`select count(*) from trades where status='OPEN' and "exitReason" is not null` → **9**.
בשלושת הנבדקים: `f6184950` · `1b01ab61` · `2f31bc5f` · `a97521d8` · `3ebf389e` ·
`80fbb2ca` · `e3d83661` — כולם `"Hit Target"`. המקור מדוד:
`EditTradeModal.jsx:428` מאתחל `exitReason: "Hit Target"` בטופס עסקה **חדשה**,
והערך נשמר גם כשלא הייתה יציאה. ⇒ עסקה פתוחה נושאת הצהרה שיצאה ברווח.
**תיקון:** לאפס `exitReason` כשה-`status` אינו `CLOSED` במסלול הכתיבה.
⛔ **אסור** לנקות רטרואקטיבית ב-UPDATE בלי אישור — כתיבה על נתוני משתמש.

---

**`C-09` · תווית מטבע בלי תעודת-מקור על 52/66 שורות (78.8%)**

`currency` מאוכלס ב-66/66, אך `currency_source` הוא `NULL` ב-**52/66** ו-
`manual_capital` ב-14/66. ⚠️ נמדד ש-`manual_capital` הוא ערך **חוקי** ב-CHECK
(אחד מחמישה: `broker_arithmetic` · `file_cell` · `account_default` ·
`literal_fallback` · `manual_capital`) — ⛔ אין כאן הפרת אילוץ. הממצא הוא שגל
`B-129` הוסיף את עמודות התעודה, ו-78.8% מהשורות קודמות לו ⇒ תווית מטבע
שמרונדרת עם סמל, בלי דרך לדעת מאין הגיעה (`R-1`).

---

**`C-10` · `source` ריק על 52/66 · `import_batch_id` על 0/66**

אותה אוכלוסייה בדיוק כמו `C-09`. `source` = `manual`×14 · `NULL`×52.
⇒ ⛔ אי-אפשר להבדיל בין עסקה שהוקלדה ידנית לעסקה שיובאה, ב-78.8% מהג׳ורנל.

---

**`C-11` · `horizon` מאוכלס על 0/66 שורות**

הפיצ׳ר נושא **140 אסרציות** ב-`test:horizon` (`CLAUDE.md` §7) ו-CHECK פעיל
בשרת — ו⛔ אפס נתוני פרודקשן. ⛔ אין כאן באג בקוד; הממצא הוא שהשער היקר ביותר
בשרשרת שומר על מסלול שמעולם לא נכתב אליו. ראוי להכרעה: לחווט את מסלול הכתיבה,
או להוריד את הפיצ׳ר.

---

**`C-12` · `inserted_at` ריק על 52/66 — ⛔ אינו מפתח מיון**

העמודה מוגדרת `default now()` והשרת בעליה (`src/supabaseClient.js:78-80`,
`B-042`), אבל 52 השורות שקדמו למיגרציה נושאות `NULL`. ⇒ ⛔ אסור להסתמך עליה
לסדר כרונולוגי. `createdAt` לעומתה מאוכלס ב-66/66 (נמדד: `createdAt_null = 0`).

---

**`C-13` · מחרוזת ריקה במקום `NULL` — 23 שורות נופלות ל־"Unknown"**

`emotionAtEntry` בפועל: `Neutral`×33 · **`""`×23** · `Confident`×3 · `Calm`×3 ·
`Hesitant`×2 · `Patient`×2. כל 22 השורות של `ad1a0494` נושאות `""` ב-`setup`,
`emotionAtEntry` ו-`marketCondition`.

`groupAndAnalyze` ממפה `(raw ?? "").toString().trim() || "Unknown"` ⇒ המדידה
מאשרת: `bySetup = [["Unknown", 22, −279.5696]]`, ו-
`metrics.emotions = {"Unknown": {count: 22, …}}`. ⛔ אין כאן חישוב שגוי — הדלי
"Unknown" הוא הצגה כנה. הממצא הוא ש**שני ייצוגים** ל"לא נרשם" (`""` ו-`NULL`)
חיים באותה עמודה.

---

**`C-14` · `avgLoss` מוחזר כגודל חיובי, `worstLoss` מוחזר עם סימן**

`src/lib/tradingStats.js:164` מול `:170`. נמדד אצל `1ad72482`:
`avgLoss = 325.000000` (חיובי) · `worstLoss = −350.000000` (שלילי). שני שדות
שכנים באותו אובייקט, שני מוסכמות סימן. ⚠️ `src/utils.js:130`
(`isNegativeValue`) ו-`money`/`fmtMoney` מסתמכים על הסימן לצביעה ⇒ `avgLoss`
עלול להיצבע כרווח. **דורש אימות עין בפאזה D** לפני שנקבע אם זה P2 או P1.

---

**`C-15` · `avgWin` · `avgLoss` · `bestWin` · `worstLoss` מחזירים `0` על דלי ריק, ⛔ לא `null`**

`tradingStats.js:163-170` — כולם `… ? … : 0`. נמדד אצל `eb61d502` (אפס הפסדים):
`avgLoss = 0.000000` · `worstLoss = 0.000000`. ⇒ "ההפסד הממוצע שלך הוא 0" על
אוכלוסייה **ריקה**. ⚠️ ההערה ב-`tradingStats.js:363-365` מנמקת במפורש ש-
`bestWin: 0` ⛔ אינו "לא נמדד" מפני ש-`pnl > 0` בהגדרה — הנימוק תקף ל-`bestWin`,
⛔ ואינו מסביר את `avgLoss`. `EMPTY_STATS` (`:386-390`) כן מחזיר `null` בכולם ⇒
שתי תשובות לאותה שאלה, תלוי אם הג׳ורנל ריק לגמרי או רק הדלי.

---

**`C-16` · `user_settings.settings` הוא JSONB בלי סכימה — 9 / 11 / 13 מפתחות**

הטבלה היא `(user_id, settings jsonb, updated_at)`. ספירת מפתחות בפועל על 38
הרשומות: **9** (2 רשומות) · **11** (16) · **13** (20). ⛔ אין CHECK, ⛔ אין
מיגרציה שמיישרת. ⇒ הון המשתמש — המכנה מ-`C-02` — חי בבלוב לא-מסוכמן.

---

**`C-17` · `ae9aa7f9` — יציאה בסטופ המסומנת "Hit Target"**

entry 95,000 · stop 88,000 · exit **88,000** · shares 0.05 (BTC-USD).
`calcTradeMetrics` מחזיר `pnl = −350` · `rMultiple = −1` בדיוק. היציאה במחיר
הסטופ המדויק, ו-`exitReason` קורא `"Hit Target"`. ⛔ לא באג חישובי — ה-R נכון.
נתון קלט שסותר את עצמו, ומזין את `bySetup`/`byEmotion` כאילו היה יעד שהושג.

---

**`C-18` · `eb61d502` — חשיפה פתוחה גדולה מההון המוצהר**

`303fb0c2` META 15×541 = **$8,115** · `f6184950` SOFI 430×14.92 = **$6,415.60**
⇒ שתי פוזיציות בלבד = **$14,530.60** מול הון מוצהר של **$10,000**, ועוד 7
פוזיציות פתוחות. ⛔ אין באג — אין בקוד שער שמשווה חשיפה פתוחה מצטברת להון.
פער יכולת, ⛔ לא פער חישוב.

---

**`C-19` · `numeric` לא-מוגבל ⇒ float64 ⇒ שארית בינארית גלויה**

הסכימה: `entry` · `stop` · `target` · `exit` · `shares` · `maxFavorable` ·
`maxAdverse` · `_capitalAtEntry` — **כולם `numeric` בלי דיוק/סקאלה**.
⚠️ **נמדד ולא הונח:** `jsonb_typeof(to_jsonb(entry))` → `"number"` (דגימת חוט:
`95000 | 0.05`). ⇒ הערכים מגיעים ל-JS כ-double אמיתי, ⛔ לא כמחרוזת.
זו **מדידה שהפריכה השערה**: אילו הגיעו כמחרוזות, `trade.entry > trade.stop`
היה משווה לקסיקוגרפית — P0. ⛔ אינו קורה.
השארית עצמה גלויה בפלט: `totalPnL = −279.56960000000004` ·
`bySetup = [["Breakout", 7, 11368.010000000002]]`. ברמת התצוגה זה נבלע בעיגול;
הממצא נרשם לפאזה H (דיוק/כספים) ולא לכאן.

---

### 🔵 P3

---

**`C-20` · `settings->>'currency'` ריק ב-38/38 הרשומות**

⛔ אף רשומת הגדרות ⛔ אינה נושאת מטבע. ⇒ מטבע החשבון נגזר במקום אחר בכל טעינה.
קוסמטי היום מפני שכל 66 השורות דולריות פרט לאחת (`C-נקי-8`).

---

## 3. נבדק ו**נקי** — עם הראיה המכריעה

⛔ ממצא שלא נמצא הוא תוצאה, ⛔ ולא היעדר עבודה. שמונה בדיקות חזרו נקיות:

| # | מה נבדק | הראיה המכריעה |
|---|----------|----------------|
| 1 | הליבה החשבונית | `computeTradingStats` = החישוב העצמאי שלי ב-SQL, **6 ספרות, 3 משתמשים, כל מדד** |
| 2 | חוק שלוש התוצאות | `winRate+lossRate+beRate = 100.000000` **בשלושתם**, במדויק |
| 3 | סוג-חוט של `numeric` | `jsonb_typeof(to_jsonb(entry))` = `"number"` ⇒ השערת ההשוואה הלקסיקוגרפית **הופרכה** |
| 4 | אידמפוטנטיות במנוחה | `(user_id, ticker, date, entry, shares)` כפול → **0 שורות** |
| 5 | 26 אינוריאנטות מבניות | ⛔ אפס הפרות: closed↔exit · open↔closedAt · shares>0 · entry>0 · side∈{LONG,SHORT} · status∈{OPEN,CLOSED} · user_id · date · צד-הסטופ |
| 6 | `manual_capital` | ערך **חוקי** ב-`trades_currency_source_check` ⇒ ⛔ לא הפרת אילוץ |
| 7 | שלמות ההון בשורה | `_capitalAtEntry` null-או-אפס → **0/66** · `createdAt` null → **0/66** |
| 8 | המשתמש רב-המטבעי | `92a06c0c` — 2 שורות **פתוחות** בלבד (`ILS`/NVDA · `USD`/BE) ⇒ ⛔ אפס סגורות ⇒ ⛔ אינו נכנס לאף חישוב כספי. הנתיב רב-המטבעי ⛔ **אינו מכוסה בפרודקשן** ומחייב בדיקה סינתטית |

⚠️ שורה 8 היא **מגבלת האבחון**, ⛔ ולא אישור: מסלול ה-FX — `pnlByCurrency` ·
`fxUnconverted` · `mixedCurrency` — לא נבחן על נתון אמיתי, מפני ש⛔ אין כזה.
`fxUnconvertedCount = 0` בשלושת הנבדקים, כלומר האוכלוסייה שהדגל נועד לתפוס
ריקה. `currencies=["USD"]` · `mixedCurrency=false` בשלושתם.

⚠️ **תיקון להנחה שלי במהלך העבודה, נרשם כדי שלא יחזור:** בריצה הראשונה קיבלתי
`currencies=[]` והתחלתי לרשום ממצא. הסיבה האמיתית הייתה ש-fixture שלי ⛔ לא כלל
`ticker`, ו-`deriveInstrumentCurrency` נשען עליו (`tradingStats.js:133`).
אחרי הוספת הטיקרים האמיתיים: `pnlByCurrency = {"USD": …}` ·
`tradesByCurrency = {"USD": …}` — **תקין**. ⇒ ארטיפקט מדידה, ⛔ לא באג.

---

## 4. מה ⛔ **לא** נבדק בפאזה הזו

⛔ אבחון שמצהיר על כיסוי שאין לו הוא בדיוק הכשל שהוא בא לתפוס.

- **`B-176` / `B-178`** — ⛔ לא נגעתי. קפואים עד 26.08 14:18Z.
- **מרוצי רשת · ספקי מחיר חיים · cache** — דורשים דפדפן חי ⇒ **פאזה D**.
- **סנכרון localStorage↔DB בזמן ריצה** — `C-02` מתעד את ה**מקור** מקריאת קוד;
  חלון הריצה בפועל (הפרש בין הצביעה הראשונה לטעינת ההגדרות) ⇒ **פאזה D**.
- **גבולות אזור-זמן** — חלון 00:00–02:59 IDT מתועד כבר ב-`src/utils.js:30`;
  ⛔ לא שוחזר כאן, נדרשת שעה מזויפת ⇒ **פאזה D**.
- **`C-14`** (סימן `avgLoss`) — ⛔ אינו נסגר בלי אימות עין ⇒ **פאזה D**.
- **דיוק float ברמת התצוגה** (`C-19`) ⇒ **פאזה H**.

---

## 5. הצעת סדר תיקון — ⛔ לביצוע בנפרד, אחרי אישור

⛔ **הפאזה הזו ⛔ אינה מתקנת דבר** (§8.1 שלב 1). הסדר לפי "כמה משתמשים רואים
מספר שגוי, חלקי כמה עולה התיקון":

1. **`C-01`** — התיקון כבר כתוב ומנומק ב-`GrowthTracker.js:51`; העתקה לשני
   אתרים. משפיע על 23/66 שורות ועל שני ציונים מרונדרים.
2. **`C-02`** — נוגע בכסף. 3 משתמשים בלי הגדרות + 1 בסתירה חיה.
3. **`C-03`** + **`C-04`** — אותה משפחה, ראוי לגל אחד.
4. **`C-05`** — לפני שמישהו ירנדר את השדה.
5. השאר לפי `BACKLOG`.

---

## 6. רישום

הממצאים כאן נושאים מזהי-דוח מקומיים `C-01`…`C-20`. ⛔ **אינם מזהי מרשם.**
המרשם מחזיק `B-001`…`B-183`; ההקצאה למזהי `B-` אמיתיים תתבצע ב-`DEEP-SUMMARY.md`
בסוף האודיט, **מ-`B-184` והלאה** (§14 — פריט חי בקובץ אחד בלבד).
