# DEEP · פאזה D — דפדפן חי

**סוג:** אבחון read-only (§8.1 שלב 1) · **רמה:** T3 · **תשובות:** 1-לא / 2-כן / 3-כן / 4-לא / 5-כן
**HEAD בתחילת האבחון:** `121aa51` · עץ נקי · **0 קבצים שונו**
**מוחרג:** `B-176`/`B-178` (מוקפא עד 26.08 14:18Z)
**סביבה:** `vite-dev` על `localhost:5173`, Chromium 146 (macOS 26.5.2), viewport 1470×956 ו-390×844
**⛔ לא נוצר משתמש בפרודקשן.** `.env` המקומי נושא Supabase URL/key **ריקים** (אורך 0/0) ⇒ מסלול ההרשמה החי לא נבדק. ראה `D-04`, שהוא **תוצר** של אותה עובדה.

---

## מנייה

| # | חומרה | אתר | כותרת | תלות |
|---|--------|-----|--------|-------|
| D-01 | **P0** | `src/main.jsx:15` | `inject()` של Vercel Analytics שולח את ה-URL המלא כולל `#access_token` — ללא שער הסכמה וללא הצמדה | — |
| D-02 | **P1** | `src/utils.js:14-16,45-51` | `holdDays`/`realizedDayKey` תלויים באזור הזמן של ה**קורא** — אותה שורה נותנת 1 או 2 ימי החזקה | — |
| D-03 | **P1** | `src/components/TickerLogo.jsx:18` | רשימת הטיקרים של המשתמש נשלחת לצד שלישי (FMP) בכל רינדור, ללא שער הסכמה | D-01 |
| D-04 | **P1** | `SwingEdge_App.jsx:3937` | שער האימות נכשל-פתוח: `isSupabaseConfigured && !session` | — |
| D-05 | **P2** | `SwingEdge_App.jsx:1448` | טיהור שקט בזמן טעינה — נכתב חזרה ל-localStorage בלי שהמשתמש יודע | — |
| D-06 | **P2** | `scripts/analytics-contract-test.mjs` | שער החוזה שומר על gtag בלבד ו⛔ אינו יכול לראות ערוץ אנליטיקה שני | D-01 |
| D-07 | **P3** | `SwingEdge_App.jsx` (FAB) | ב-390px כפתור ה-FAB מסתיר שני ציוני DNA | — |

**מנייה: P0 = 1 · P1 = 3 · P2 = 2 · P3 = 1 · סה"כ 7 · נקי = 6**

**נקי (נמדד, לא הונח):** `D-N1` קונסולה · `D-N2` אריתמטיקה על המסך · `D-N3` הצמדת `page_location` ב-GA4 · `D-N4` כיסוי i18n 652/652 · `D-N5` 100 עסקאות · `D-N6` Sentry.

**⛔ לא נבדק — מגבלת כלי, לא ממצא:** `D7` (Safari/Firefox) ו-`D8` (Slow 3G). כלי ה-preview הוא Chromium בלבד ואין בו ויסות רשת. ⛔ **אין להסיק מכאן שהמסלולים תקינים.** שורה ב-`CHECKS`.

---

## D-01 · **P0** · `src/main.jsx:15`

**מה נמדד.** ניווט יזום ל-
`http://localhost:5173/app?utm_probe=1#access_token=FAKE_TOKEN_AUDIT_PROBE&refresh_token=FAKE_REFRESH`
הפיק בקונסולה:

```
[Vercel Web Analytics] [view] http://localhost:5173/app?utm_probe=1#access_token=FAKE_TOKEN_AUDIT_PROBE&refresh_token=FAKE_REFRESH  /_vercel/insights/view
```

**באותה טעינת עמוד בדיוק**, GA4 שלח `dl=http%3A%2F%2Flocalhost%3A5173%2Fapp` — בלי query ובלי fragment.
שני ספקי אנליטיקה, אותו URL, אחד מוצמד ואחד לא.

**שורש.** `main.jsx:13-15` מייבא ומריץ `inject()` ברמת המודול — קריאה יחידה, ללא ארגומנטים, **ללא שער הסכמה**. ברירת המחדל של `@vercel/analytics` היא לדווח את `location.href`. זהו **אותו זוג ברירות-מחדל** של `INCIDENTS#12`, שלקחו ממנו היה: *"ברירת מחדל שקוראת את הסביבה היא קלט שלא הצהרת עליו"*. התיקון של #12 הוצמד ל-gtag **בלבד** (`index.html:140-144`), וערוץ שני שנולד לפניו נשאר פתוח.

**תנאי-קדם — אומתו, לא הונחו:**
- `node_modules/@supabase/auth-js/dist/main/GoTrueClient.js:24` — `flowType: 'implicit'` הוא ברירת המחדל (v2.110.8); `src/supabaseClient.js:10-14` ⛔ אינו דורס אותה ⇒ החזרת OAuth נוחתת על `/#access_token=…&refresh_token=…`.
- `AuthScreen.jsx:136-138` `redirectTo: window.location.origin` · `ForgotPassword.jsx:35` `redirectTo: …/?reset=true`.

**חלון החשיפה — נמדד ב-`git merge-base --is-ancestor`:** `ee2c00d` (04.07) **קודם** ל-GA4 (27.07) ולתיקון #12 (`08ca710`, 05.08). ⇒ **חלון 10 הימים הרשום ב-`INCIDENTS#12` שגוי לגבי הערוץ הזה**; החלון נפתח 04.07 ועודנו פתוח — **53 יום**.

**חמור מ-GA4 בשני צירים:** ⛔ אינו מותנה בהסכמה (`readConsent()` אינו נקרא באף מסלול של `inject()`) ⇒ **100% מהמשתמשים**, ⛔ לא רק המסכימים; ו-`analytics-contract-test.mjs` ⛔ אינו יכול לראותו (`D-06`).

**⚠️ סייגים — נמדד ב-dev בלבד.** ב-dev החבילה במצב debug ו**מדפיסה** את מה שהייתה שולחת ⛔ ואינה שולחת. שידור בפועל בפרודקשן ⛔ **לא נצפה** — הוא דורש session אמיתי. הדליפה גם **תלוית-מרוץ**: `detectSessionInUrl` מסיר את ה-fragment אסינכרונית, ולכן היא לסירוגין — מה שמקשה על גילוי ⛔ ואינו מקטין את החומרה.

**התיקון הזול ביותר.** להצמיד את הדיווח לנתיב במקום ל-href, ולשער אותו ב-`readConsent()` — אותה תבנית בדיוק שכבר קיימת ב-`consent.js:122-131`.

**⛔ מה אסור בתיקון.** ⛔ אין להסתמך על "Supabase מנקה את ה-hash ממילא" — זהו בדיוק המרוץ. ⛔ אין להסיר את `inject()` בשקט בלי החלטה מתועדת. ⛔ אין להרחיב את `denyUrls` של Sentry כתחליף — ערוץ אחר לגמרי.

---

## D-02 · **P1** · `src/utils.js:14-16,45-51`

**מה נמדד.** אותן 5 שורות בדיוק, ארבעה אזורי זמן, `TZ=… node`:

| trade | `closedAt` | Asia/Jerusalem | UTC | America/New_York | Pacific/Auckland |
|-------|-----------|----------------|-----|------------------|------------------|
| aud-1 | `2026-08-06T17:00Z` | key 08-06 · hold **1** | 08-06 · **1** | 08-06 · **1** | 08-07 · **2** |
| aud-5 | `2026-08-20T21:30Z` | 08-21 · **1** | 08-20 · **0** | 08-20 · **0** | 08-21 · **1** |

**שורש.** צימוד א-סימטרי. `localDayKey` (`utils.js:14`) מחזיר מחרוזת בת 10 תווים **כמות שהיא** — `trade.date` הוא יום קלנדרי חסר-אזור-זמן. אך `closedAt` הוא **רגע**, ו-`:15-16` ממיר אותו ליום ה**מקומי של הקורא**. `holdDays` (`:45-51`) מחסר יום-מקומי ממחרוזת-צפה ⇒ התוצאה תלויה במי שמסתכל, ⛔ לא במה שקרה.

⚠️ **הפרכה של הנחה שלי.** שיערתי שמחרוזת `YYYY-MM-DD` נפרסת כחצות UTC ותיסוג ביום שלם ממערב לגריניץ׳. `utils.js:14` **מונע** זאת במפורש. ⛔ ההנחה הופרכה לפני שנכתבה כממצא.

**מדוע P1 ולא P0.** למשתמש באזור זמן קבוע הקרוב לשוק שלו המספר יציב. הוא מתפוצץ בתנאים ספציפיים: נסיעה, סגירה סמוכה לחצות, או ג׳ורנל שיובא מברוקר באזור אחר. זהו כשל **שקט** — `avgHoldDays` מוצג ואין דבר שיסמן שהשתנה.

**התיקון הזול ביותר.** לגזור את יום הסגירה מ**אותו** בסיס כמו יום הכניסה. ההכרעה היא **מוצרית ⛔ לא ריפקטור** ולכן אינה שייכת לאבחון.

**⛔ מה אסור בתיקון.** ⛔ אין להחליף ל-`toISOString()` — זו בדיוק שגיאת-הראי המתועדת ב-`utils.js:26-28`. ⛔ אין לשמור `holdDays` נגזר ב-DB (§14 בלוק 9). ⛔ אין לגעת בפיקסצ'רים לפני הכרעה — `B-159` כבר קבע שהם עיוורים ל-`avgHold`, וריכוך שם יסתיר את זה.

**תלות.** נפרד מ-`B-159` (עיוורון **פיקסצ'ר**); זה מספר שגוי על **מסך**.

---

## D-03 · **P1** · `src/components/TickerLogo.jsx:18`

**מה נמדד.** בטעינת `/app` עם 5 עסקאות, לוג הרשת מכיל 5 בקשות לצד שלישי — אחת לכל טיקר מוחזק:

```
GET https://financialmodelingprep.com/image-stock/{GOOG,TSLA,NVDA,MSFT,AAPL}.png → 200
```

**שורש.** `<img src={`https://financialmodelingprep.com/image-stock/${cleanTicker}.png`}>` — ה-URL עצמו **הוא** הנתון. הרכב התיק המלא של המשתמש נמסר ל-FMP (יחד עם IP ו-Referer) בכל רינדור, ⛔ ללא שער הסכמה. ⚠️ **הפרדה:** `consent.js` מקפיד שאירועי אנליטיקה ⛔ לעולם לא יישאו טיקר (`:104-105`) — ואותו טיקר עוזב דרך תגית `<img>` שהחוזה אינו רואה.

**⚠️ תקדים חלקי בלבד.** `DECISIONS.md:21` (27.07) נוגע ב-FMP — אך בסינון 404 ב-CI, ⛔ לא בציר הפרטיות. ב-`BACKLOG`/`INCIDENTS`: **0 התאמות**.

**התיקון הזול ביותר.** proxy דרך `api/` או נכס מקומי; לחלופין `referrerPolicy="no-referrer"` כמזעור **חלקי** — ⛔ אינו מסתיר את הטיקר, שהוא ה-path עצמו.

**⛔ מה אסור בתיקון.** ⛔ אין להסתפק ב-`no-referrer` ולסמן כסגור. ⛔ אין להוסיף מפתח API — הבעיה היא היעד, לא האימות.

---

## D-04 · **P1** · `SwingEdge_App.jsx:3937`

**מה נמדד.** `if (isSupabaseConfigured && !session) { return <AuthScreen />; }`. עם `.env` הנוכחי (URL/key **ריקים**, אורך 0/0 — נמדד), `isSupabaseConfigured` הוא `false` (`supabaseClient.js:3-6`), ולכן `/app` רינדר את אשף ה-onboarding ואת הג׳ורנל המלא מ-localStorage — **בלי מסך התחברות**. זהו המצב שבו בוצעו כל מדידות הפאזה.

**שורש.** השער מודד **נוכחות תצורה**, ⛔ לא **אמת הרשאה** — משפחת `R-4`. `false` בהיעדר תצורה פותח, במקום לסגור.

**התיקון הזול ביותר.** לכשול-סגור: היעדר תצורה הוא שגיאת הפעלה צועקת, ⛔ לא מצב שקט.

**⛔ מה אסור בתיקון.** ⛔ אין להפוך ל-`!session` לבד בלי מסלול מפורש ל-dev — זה שובר את `vite-dev-noauth`, שקיים בכוונה (`.claude/launch.json`).

---

## D-05 · **P2** · `SwingEdge_App.jsx:1448`

**מה נמדד.** `localStorage.setItem("swingEdgeTrades", JSON.stringify(purgeInvalidTrades(cleanTrades(parsed))))` רץ בכל טעינה, בתוך `useState` initializer. אומת שאינו הרסני על התוכן שנבדק: 100/100 שורות שנזרעו שרדו.

**שורש.** מסלול טעינה שגם **כותב**. שורה שתיפסל תיעלם מהדיסק בלי הודעה ובלי דרך חזרה — כשל שקט (§2). ⛔ לא נמדדה שורה שנמחקה בפועל; הממצא הוא ה**מבנה**.

**⛔ מה אסור בתיקון.** ⛔ אין להסיר את הטיהור — הוא מגן על הרינדור. יש **לדווח** מה נופל, לא להשתיק.

---

## D-06 · **P2** · `scripts/analytics-contract-test.mjs`

**מה נמדד.** `grep -E "inject|@vercel/analytics|insights"` → **0 התאמות**. הקובץ מצהיר שתפקידו למנוע קבצים ש"מגיעים ל-gtag ישירות ושולחים אירוע שלא עבר `readConsent()`" — ובדיוק זה קורה בערוץ שני שהשער אינו יודע על קיומו. 4 מופעי `assert` בלבד.

**שורש.** השער מוגדר סביב **ספק** (gtag) ולא סביב **התנהגות** (יציאת נתונים החוצה). `INCIDENTS#12` כבר רשם זאת כפתוח: *"אין בדיקה אוטומטית שמוודאת שדבר הדומה לטוקן אינו מגיע לקריאת אנליטיקה יוצאת"*.

**תלות.** `D-06` הוא הסיבה ש-`D-01` שרד 53 יום.

---

## D-07 · **P3** · FAB ב-390px

**מה נמדד.** `getBoundingClientRect` — FAB ב-`[24,764]–[80,820]`, `z-index: 40`. חופף לשני ערכים מספריים: `100` (משמעת) ב-`[33,752]–[55,768]` ו-`36` (צמיחה) ב-`[33,791]–[47,807]` — השני **מכוסה במלואו**. תלוי-גלילה ולכן חולף.

**⚠️ הפרכה של קריאה מוקדמת שלי.** בצילום הראשון קראתי "משמעת 20" ורשמתי חשד לאי-התאמה בין תווית לערך ב-RTL. זום הראה `ניהול סיכון 20` · `משמעת 100` · `עקביות 58` — תואם ל-Node **בדיוק**. ⛔ החשד הופרך ואינו ממצא.

---

## נקי — נמדד

**D-N1 · קונסולה.** מסע מלא + 100 עסקאות: **0 שגיאות, 0 אזהרות React**. `preview_console_logs level=warn` → "No console logs".

**D-N2 · אריתמטיקה על המסך.** כל ערך חושב ביד ותאם: `+$80.00` · `40%` (2W/2L/1BE) · `-0.35R` · `רצף שיא 1` · `רצף נוכחי 0` (האחרונה הפסד). המכנה של R **מוצהר על המסך**: *"ל-4 מתוך 5 עסקאות סגורות היה סטופ"* — §2 מקוים.

**D-N3 · הצמדת GA4.** באותה בקשת probe: `dl=…%2Fapp` — ה-fragment וה-query **הוסרו**. תיקון `INCIDENTS#12` עומד. ⚠️ GA4 **כן** שולח פינגים חסרי-עוגייה בהסכמה שלילית (`gcs=G100&npa=1`) — התנהגות Consent Mode v2 מתועדת ב-`index.html:98-104`, ⛔ לא באג. ציר ה-GDPR ⇒ פאזה H.

**D-N4 · i18n.** 5 שפות (he·en·es·pt·ar), 2 RTL. **652/652 מפתחות בכל אחת · 0 חסרים · 0 ריקים.** ל-`ar` 3 מפתחות `_two` נוספים (יחיד-זוגי) — כולם מגיעים דרך `plural()` (`SwingEdge_App.jsx:4693,4698,5001`), ⛔ לא קוד מת. `plural()` נכשל-פתוח והמגבלה (11+ בערבית) **מתועדת בקוד**.

**D-N5 · 100 עסקאות.** נזרעו ב-dev בלבד. `DOMContentLoaded 323ms` · `load 1408ms` · 100/100 שורות · **5,970 צמתי DOM** · גובה גלילה 16,550px · 0 שגיאות. הדף מגיב. ⛔ **אין וירטואליזציה** — ב-100 זה בסדר; זה הציר שיכאב באלף. ⇒ פאזה E.

**D-N6 · Sentry.** `main.jsx:19` `enabled: import.meta.env.PROD` — ב-dev לא נשלח דבר.

---

## מגבלות ורטרקציות

**⛔ לא נבדק (מגבלת כלי):** `D7` דפדפנים חוצי-פלטפורמה · `D8` רשת איטית. כלי ה-preview הוא Chromium בלבד וללא ויסות רשת. ⛔ אין להסיק תקינות. שורה ב-`CHECKS`.

**⛔ לא נבדק (החלטה):** מסע הרשמה חי — דורש Supabase מוגדר. ⛔ לא נוצר משתמש בפרודקשן, כפי שהוגדר בפרומפט.

**ארבע רטרקציות — כשל **בכלי שלי**, ⛔ לא במוצר.** נרשמות כי אבחון שמדווח רק ממצאים מסתיר את שיעור הטעות של עצמו:

1. **`preview_click`** דיווח הצלחה על באנר ההסכמה בלי להפעיל את ה-handler של React. `.click()` נייטיב הוכיח: `null → {"v":1,"analytics":"denied",…}`. כמעט נרשם כבאג.
2. **`calculateTradeDNA(trades, 2500)`** — הפרמטר השני הוא `capitalCurrency`, ⛔ לא הון (`TradeDNA.js:217`). `2500` רוקן את אוכלוסיית הסיכון. אומת ש-**פאזה C לא הושפעה** (`/tmp/deepc-dna.mjs:16` קורא בארגומנט אחד).
3. **פער 92 מול 90.** המסך הראה `92`, ה-harness `90`. השערתי `edgeReport` ומדדתי: `calculateGrowthScore(trades, findEdges(trades), "USD")` → **92 · measured 5/5**; עם `null` → **90 · 4/5**. ⇒ האפליקציה **צודקת**, ה-harness שלי היה חסר.
4. **timeout של 30 שניות** במעבר טאב נראה ככשל ביצועים חמור. בדיקה חוזרת: 100 שורות · 5,970 צמתים · הדף מגיב. ה-Promise ב-`preview_eval` לא נפתר — ⛔ לא המוצר.

⚠️ **בשלושה מהארבעה כמעט נכתב ממצא שגוי.** מה שעצר: סירוב לדווח סטייה שלא הוסברה עד שהוסברה.

**תיקון חוצה-פאזות ל-`A-05`.** `SwingEdge_App.jsx:303` — `const MOCK_TRADES = [];` **מערך ריק**. ⇒ `catch { return MOCK_TRADES; }` (`:1450`) ⛔ **אינו** מזריק נתונים מומצאים. `A-05` **מורד**: הכשל הוא בליעת השגיאה, ⛔ לא המצאת עסקאות.

---

## מה נשאר ואינו ברפו (§14 · §10.1)

- `D-01`…`D-07` — נרשמים כ-`B-` ב-`BACKLOG` ב-`DEEP-SUMMARY`.
- `D7`/`D8` — פריט `CHECKS`.
- דחיות פאזה B: גישה חוצת-משתמשים עם anon key · `script.innerHTML`.
- דחיות פאזה C: ספקי מחיר חיים · מרוצי רשת · cache.
- **מצב dev:** `localStorage` נושא כרגע **100 עסקאות סינתטיות** (`perf-0`…`perf-99`) ו-`swingEdgeConsent: denied`. ⛔ dev בלבד — פרודקשן לא נגעה.
