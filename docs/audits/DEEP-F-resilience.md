# DEEP · פאזה F — עמידות בכשל (Error Boundary · ספק שנופל)

**סוג:** אבחון read-only (§8.1 שלב 1) · **רמה:** T3 · **תשובות:** 1-לא / 2-לא / 3-לא / 4-כן / 5-כן
**HEAD בתחילת האבחון:** `d61d490` · עץ נקי · **0 קבצי קוד שונו**
**מוחרג:** `B-176`/`B-178` (מוקפא עד 26.08 14:18Z)
**מכשיר:** `vite preview` על `dist/` המקומי (`localhost:4173`), חסימה בצד-לקוח
(patch ל-`window.fetch` ול-`document.createElement('script')`), + קריאת קוד.
⛔ **פרודקשן לא שונתה.** כל הפאצ'ים הוסרו בסוף (אומת: `fetchPatched:false ·
createElementPatched:false`).

**סיכום מנייה:** **P0=1 · P1=4 · P2=4 · P3=0 · סה"כ 9** · ובנוסף **9 מסלולים
שנמדדו ועברו** (§פאס). ⚠️ אודיט עמידות שמדווח רק כשלים אינו מדידה — הרשימה
הירוקה היא חלק מהתוצאה, לא נימוס.

---

## F1 · Error Boundary

### מה נמדד

`grep -rln "componentDidCatch\|getDerivedStateFromError\|ErrorBoundary"` על
`SwingEdge_App.jsx` + `src/` מחזיר **קובץ אחד**: `src/main.jsx`.

```
src/main.jsx:76   <Sentry.ErrorBoundary fallback={<p>משהו השתבש. רענן את הדף.</p>}>
src/main.jsx:94   </Sentry.ErrorBoundary>
```

בין 76 ל-94 יושבים `ThemeProvider` → `ToastProvider` → `ConfirmProvider` →
`BrowserRouter` → **כל** ה-`Routes` + `RouteTracker` + `ConsentBanner`.
⇒ **גבול אחד, בשורש, לכל האפליקציה.** אין גבול לכל טאב, לכל מסך, ולא סביב
אף ווידג'ט צד-שלישי.

### הניסוי החי

עסקה אחת ב-`localStorage` קיבלה `ticker` בצורת אובייקט (`{sym:"AAPL",
exch:"NASDAQ"}`) — הצורה שבאג ייבוא/OCR מייצר בפועל, ⛔ לא ערך אקראי.
גיבוי ל-`__deepf_backup`, שוחזר ונמחק בסוף (אומת: `idx3_ticker:"MSFT" ·
tempKeys:[]`).

| שלב | נמדד |
|-----|-------|
| לוח בקרה | **נטען כרגיל** — והעסקה הפגומה נכללת בסטטיסטיקה: `400 עסקאות סגורות · $9,910.95 · −$89.05 · ▼0.9%` |
| לחיצה על "יומן" | `#root` מוחלף כולו ב-`<p>משהו השתבש. רענן את הדף.</p>` |
| DOM | **28,829 → 56 צמתים · 0 כפתורים · 0 קישורים** |
| צבעים | `rgb(248,250,252)` על `rgb(10,15,30)` ⇒ הטקסט **נראה** — ⛔ לא "מסך לבן" |
| רענון | חוזר ללוח הבקרה |
| לחיצה שנייה על "יומן" | **קורס שוב, זהה** |

---

### F-01 · [P1] · `src/main.jsx:76` · גבול יחיד בשורש מפיל את כל האפליקציה

**מה נמדד:** קריסה של קומפוננטת טאב אחת מוחקת את העץ כולו — כולל הניווט.
28,829 → 56 צמתים, **0 כפתורים, 0 קישורים**. המשתמש נשאר בלי שום פעולה זמינה,
גם לא מעבר ללשונית עובדת.

**שורש:** ה-`ErrorBoundary` עוטף את `BrowserRouter` מבחוץ (`:76`–`:94`). ⛔ זו
אינה "חוסר גבולות" — זו **מיקום** של גבול: הוא נמצא מעל הניווט, ולכן כל כשל
מתחתיו לוקח גם את הניווט.

**התיקון הזול והלא-הרסני:** גבול **שני**, פנימי, סביב תוכן הטאב בלבד
(`SwingEdge_App.jsx` — ה-`{tab === "..."}` בלוקים), עם fallback שמשאיר את סרגל
הטאבים חי. הגבול בשורש נשאר כרשת אחרונה.

**⛔ מה אסור בתיקון:** ⛔ לא לעטוף כל קומפוננטה בגבול משלה — גבול לכל צומת הופך
כל באג לפגיעה מקומית ושקטה, וזה בדיוק הכשל השקט שהגבול בא למנוע. ⛔ ולא להחליף
את הגבול בשורש; הוא הרשת שתופסת קריסה בזמן mount.

**תלות:** `F-02` (הטקסט של אותו fallback) · `F-03` (ייחוס ה-stack).

---

### F-02 · [P1] · `src/main.jsx:76` · ה-fallback מורה על פעולה שנמדדה כלא-עוזרת

**מה נמדד:** הטקסט הוא `"משהו השתבש. רענן את הדף."`. רענון אכן מחזיר את
האפליקציה — **ללוח הבקרה**. לחיצה חוזרת על "יומן" מפילה שוב, זהה. הכשל אינו
חולף; הוא נתון. ⇒ ההוראה היחידה שהמשתמש מקבל היא זו שנמדדה כלא-פותרת.

בנוסף: הודעה אחת, בעברית קשיחה (⛔ לא עוברת ב-`t`/`i18n` — האפליקציה תומכת
he/en/es/pt), בלי כפתור, בלי "דווח", ובלי שום רמז **איזו** עסקה פגומה. משתמש
עם 400 עסקאות אינו יכול למצוא אותה.

**שורש:** ה-fallback הוא ליטרל `<p>` ולא קומפוננטה. ל-`Sentry.ErrorBoundary`
יש `fallback={({error, resetError}) => ...}` ו-`eventId` — שניהם לא בשימוש.

**התיקון הזול והלא-הרסני:** להחליף את ה-`<p>` בקומפוננטת fallback שמקבלת
`resetError` ומציגה: כפתור "חזרה ללוח הבקרה" (ניווט + `resetError`), מזהה
האירוע מ-Sentry להעתקה, וטקסט דרך `t`.

**⛔ מה אסור בתיקון:** ⛔ לא לרכך את הטקסט ל"נסה שוב מאוחר יותר" בלי פעולה —
זה מחליף הוראה שגויה בהוראה ריקה. ⛔ ולא להוסיף retry אוטומטי: הכשל דטרמיניסטי
בנתון, ו-retry עליו הוא לולאה.

**תלות:** `F-01`.

---

### F-03 · [P2] · `dist/assets/recharts-*.js` · כל stack ב-Sentry מיוחס לספריית הגרפים

**מה נמדד** — הקונסול ברגע הקריסה:

```
[error] Error: Minified React error #31; ... args[]=object with keys {sym, exch}
    at vl (http://localhost:4173/assets/recharts-BsRpwJDZ.js:22:6312)
[error] TypeError: (e || "").replace is not a function
    at ys (http://localhost:4173/assets/index-C1E4LHcf.js:1142:43758)
    at Xf (http://localhost:4173/assets/recharts-BsRpwJDZ.js:22:17331)
```

**כל** מסגרת של React/react-dom נושאת את השם `recharts-*.js` — בקריסה שאין לה
שום קשר לגרף.

**שורש:** ⛔ לא באג ב-recharts. `manualChunks` ב-`vite.config.js:36-40` מקצה
`recharts` לצ'אנק משלו, ו-rollup מצרף לשם גם את התלות המשותפת `react-dom`
(127.9 kB, נמדד ב-`E-01`). שם הצ'אנק הופך לשם הקובץ בכל stack.

**התיקון הזול והלא-הרסני:** אותו תיקון של `E-01` — `manualChunks` כפונקציה
שמוציאה `react`/`react-dom` לצ'אנק `vendor-react` נפרד. ⛔ אין כאן עבודה נוספת.

**⛔ מה אסור בתיקון:** ⛔ לא להוסיף `denyUrls`/`ignoreErrors` על `recharts` ב-
`src/main.jsx:17-55` — זה יסתום בדיוק את הדיווחים האמיתיים.

**תלות:** **זהה לשורש של `E-01`.** תיקון אחד סוגר את שניהם.

---

## F2 · ספק שנופל

שיטה: כל ספק נחסם בנפרד ונמדד מה המשתמש **רואה** — ⛔ לא מה הקוד מתכוון.

---

### F-04 · [P0] · `SwingEdge_App.jsx:6672-6673` · רשימת המעקב מציגה מחירים קשיחים כמחירים חיים

**מה נמדד** — במסך אחד, באותו רגע, עם `/api/quote` ללא תשובה:

| אזור במסך | מה הוצג |
|-----------|----------|
| לוח בקרה (באנר) | `⚠️ ההון הוא סכום חלקי … אין מחיר חי ל-100 מתוך 100 עסקאות פתוחות` ✅ |
| רשימת מעקב (מודיעין שוק) | `AMD · $118.40 · −1.40%` · `AVGO · $221.50 · +1.90%` ❌ |

הערכים ברשימת המעקב זהים תו-בתו ל-`SCANNER_DATA`:

```
SwingEdge_App.jsx:326   { ticker: "AMD",  price: 118.40, change: -1.4,  ... }
SwingEdge_App.jsx:324   { ticker: "AVGO", price: 221.50, change: +1.9,  ... }
```

והם מוצגים ב**אותה** גופן, **אותו** צבע, **אותו** חץ ירוק/אדום שבהם מוצג מחיר
אמיתי. אין סימן, אין חותמת זמן, אין "—".

**שורש** — שלוש שורות, בסדר הזה:

```
SwingEdge_App.jsx:321-330   SCANNER_DATA = [ { ticker:"NVDA", price:142.30, change:+3.2, ... }, ... ]   ← טבלה ליטרלית
SwingEdge_App.jsx:1636      DEFAULT_WATCHLIST = SCANNER_DATA.map(s => ({ ..., price: s.price, change: s.change }))
SwingEdge_App.jsx:6672      const price      = lp?.price      ?? s.price;
SwingEdge_App.jsx:6673      const changePct  = lp?.changePct  ?? s.change ?? 0;
```

⚠️ **הקוד כבר יודע את החוזה הנכון.** `handleAddWatchlist` (`:3431`) כותב
לכל טיקר שהמשתמש מוסיף **`price: null, change: null`** — במפורש. ⇒ זו אינה
מחלוקת עיצובית; זו טבלה ליטרלית אחת שסותרת את החוזה של שאר הקובץ.
`:6701` (`price != null`) ⛔ אינו יכול להבחין — קבוע וציטוט חי הם שניהם `number`.

**האוכלוסייה, עם מכנה:** **8 מתוך 10** שורות ברשימת המעקב המוגדרת כברירת מחדל
(NVDA · PLTR · AVGO · META · AMD · MSTR · SMCI · TSLA). BTC ו-ETH כבר `null`
(`:1637-1638`) ומציגים "—" כראוי. **חלון החשיפה:** כל טעינת עמוד עד שהציטוט
הראשון חוזר · **וכל משך תקלה בספק** · לכל משתמש שלא ערך את רשימת המעקב.

**⛔ מה כן נבדק ואינו נפגע:** מחשבון הפוזיציה קורא `getLivePrice(...)`
(`:5737`) ומקבל `null` — ⇒ **גודל הפוזיציה אינו מושפע.** `MarketPulseCard`
(`:1080-1085`) מחזיר `"—"` לכל ערך לא-מספרי. `?? 0` ב-`:6673` ⛔ **אינו** יורה
בפועל: כשאין `lp` וגם אין `s.change`, גם `price` הוא `null` והענף כולו מרנדר
"—". ⛔ לא נטען כאן שהוא יורה.

**התיקון הזול והלא-הרסני:** ב-`:1636` להעביר `price: null, change: null` —
בדיוק כמו `handleAddWatchlist:3431`. שורה אחת, מיישרת את הקובץ עם עצמו.

**⛔ מה אסור בתיקון:** ⛔ לא לעדכן את המספרים ב-`SCANNER_DATA` ל"מחירים נכונים
יותר" — קבוע שנכון היום שקרי מחר וממחזר את הבאג עם תאריך תפוגה. ⛔ ולא להוסיף
תווית "מחיר משוער" לצדם — נתון בלי מקור אינו הופך לנתון עם תווית.

**תלות:** `F-05` (שני הביטויים של אותה שאלה: "מה המשתמש רואה כשאין ציטוט").

---

### F-05 · [P1] · `SwingEdge_App.jsx:4076` · תג `🟢 LIVE` הוא שעון, ⛔ לא מדידת חיוּת

**מה נמדד:** עם **אפס** ציטוטים שחזרו, הכותרת הציגה `🟢 LIVE` לכל אורך האבחון.

**שורש:** `getMarketStateBadge(marketState)` (`:4076`), ו-`marketState` נגזר
מהשעון בלבד (`getMarketState()`, `:1981`). ⛔ אף רכיב בתג אינו נוגע ב-
`pricesLastUpdated` או ב-`livePrices`. הסימן היחיד לתקלה הוא **היעדר** חותמת
הזמן שלידו (`:4095`, `pricesLastUpdated && …`) — היעדר, ⛔ לא הצהרה — והיא
ממילא `hidden md:inline` (`:4096`), כלומר **אינה קיימת במובייל כלל**.

**התיקון הזול והלא-הרסני:** להתנות את מצב ה"חי" של התג ב-`pricesLastUpdated`
טרי (למשל בתוך `2 × getRefreshInterval(marketState)`); אחרת אותו תג בצבע מושתק
+ מילה שאומרת שהנתון אינו מתעדכן.

**⛔ מה אסור בתיקון:** ⛔ לא להסתיר את התג כשאין נתון — היעלמות היא בדיוק
"היעדר במקום הצהרה" שיצר את הבאג.

**תלות:** `F-04`.

---

### F-06 · [P1] · `SwingEdge_App.jsx:1690-1692` · כשל **קריאה** מול Supabase שקט לחלוטין

**מה נמדד** — ספירה על כל מסלולי `.from(...)` ב-`SwingEdge_App.jsx`:

| מסלול | שורה | `toast.error` | `console.error` |
|-------|------|----------------|------------------|
| `trades.select` (טעינת היומן) | 1671 | ❌ | ✅ |
| `trades.select` (טיזר חניכים) | 3296 | ❌ | ❌ |
| `mentor_notes.select` | 3366 | ❌ | ❌ |
| 4 מסלולי `select` נוספים | 3207·3208·3332·3386 | ✅ | — |

⇒ **3 מתוך 7 מסלולי `SELECT`** אינם מוסרים למשתמש דבר. ⛔ המנייה היא על
`SwingEdge_App.jsx` בלבד.

**שורש** — אסימטריה בתוך אותה פונקציה:

```
SwingEdge_App.jsx:1676-1686   if (error) { console.error(...); ← נופל חזרה ל-localStorage }
SwingEdge_App.jsx:1690-1692   catch (e) { console.error("Supabase load failed:", e); }   ← ⛔ בלי fallback, בלי UI
```

⚠️ **תקלת רשת נופלת ל-`catch`, ⛔ לא ל-`error`.** כלומר בדיוק התרחיש "Supabase
נפל" הוא זה שאין לו נפילה-לאחור. בפועל `trades` נשאר על הערך ההתחלתי
(`:1443-1451`, קריאה מ-`localStorage`), ולכן על מכשיר מוכר המשתמש רואה עותק
מקומי; **על מכשיר חדש / אחרי ניקוי אחסון הוא רואה יומן ריק** — ללא הבדל
נראה מ"עדיין לא הזנת עסקאות".

⚠️ **מה נבדק ונשלל:** `MOCK_TRADES` (`:303`) הוא `[]` ריק. ⛔ אין נתון מפוברק
בערך ההתחלתי. הבדיקה נעשתה כדי לשלול, לא כדי לאשש.

⚠️ **`3296` ו-`3366` חמורים יותר מהראשון:** `if (cancelled || error || !data)
return;` ו-`if (!error) setMenteeNotesByTrade(...)` — ⛔ אפילו לא `console`.
מנטור שהשאילתה שלו נכשלה רואה "אין הערות", ⇒ בדיוק כמו "לא כתבת הערות".

**התיקון הזול והלא-הרסני:** ב-`:1690` — אותו fallback ל-`localStorage` שכבר
כתוב ב-`:1678-1684`, ובנוסף `toast.error` על שני הענפים (מסלולי ה**כתיבה**
כבר עושים בדיוק את זה, `:2892`). ב-`3296`/`3366` — `console.error` לכל הפחות.

**⛔ מה אסור בתיקון:** ⛔ לא להציג יומן ריק אחרי כשל קריאה בלי לומר שהקריאה
נכשלה — משתמש שיסיק "המידע שלי נמחק" עלול לייבא הכול מחדש וליצור כפילויות.

---

### F-07 · [P1] · `src/components/AuthScreen.jsx:438,449,97` · Cloudflare חסום ⇒ הרשמה בלתי-אפשרית, בלי הסבר

**מה נמדד** (קריאת קוד; ראה §"מה לא ניתן היה למדוד"):

```
AuthScreen.jsx:438   script.addEventListener("load", render);      ← ⛔ אין מאזין "error"
AuthScreen.jsx:449   return <div ref={ref} className="... min-h-[65px] ..." />;   ← ⛔ אין תוכן, אין timeout
AuthScreen.jsx:97    if (isTurnstileConfigured && !captchaToken) { setError(t("errCaptcha")); return; }
AuthScreen.jsx:38    errCaptcha: { he: "יש להשלים את בדיקת האבטחה." }
```

**שרשרת הכשל:** `challenges.cloudflare.com` חסום (חומת-אש ארגונית · חוסם
פרסומות · תקלה אצל Cloudflare) ⇒ `load` לעולם לא יורה ⇒ `render()` לא רץ ⇒
`captchaToken` נשאר `""` ⇒ המשתמש לוחץ "הרשמה" ורואה **"יש להשלים את בדיקת
האבטחה"** מעל **קופסה ריקה בגובה 65px שאין בה מה להשלים**. אין timeout, אין
מסלול חלופי, אין דיווח.

**היקף, במדויק:** ה-`Turnstile` מרונדר רק במצב **הרשמה** (`:250`, בתוך טופס
ה-signup). ⇒ משתמשים קיימים **ממשיכים להתחבר**; ⛔ הפגיעה היא ב**רישום חדש
בלבד** — ולכן גם השקטה ביותר: אף אחד לא מתלונן, הוא פשוט עוזב.

**⛔ מה כן עובד ונמדד:** `/api/verify-turnstile` **נכשל-פתוח** במפורש —
`:110` חוסם רק על `400` מובהק, ו-`:112` מתעד `5xx/429/רשת → fail open`. ⇒
הצד שלנו כבר תוכנן נכון; החסר הוא בטעינת הסקריפט של הצד השלישי.

**התיקון הזול והלא-הרסני:** `script.addEventListener("error", …)` לצד
`:438`, + timeout (~8s) שמסמן `turnstileUnavailable`; במצב הזה — טקסט מפורש
בקופסה במקום ריק. ההחלטה אם לפתוח את ההרשמה ללא captcha היא החלטת מוצר
(`DECISIONS`), ⛔ לא פרט מימוש.

**⛔ מה אסור בתיקון:** ⛔ לא לוותר על ה-captcha כברירת מחדל בשקט — זה פותח
רישום המוני. ⛔ ולא להשאיר את `errCaptcha` כהודעה היחידה: היא מפנה לפעולה
שאינה קיימת על המסך.

**תלות:** `E-08` (אותו `:438` — שם כמאזין שאינו מוסר, כאן כמסלול כשל).
**ניטור:** `api/health.js` מנטר 5 ספקים (`:131-137`) — Cloudflare ⛔ אינו אחד
מהם. ⇒ נמסר ל-**פאזה G**.

---

### F-08 · [P2] · `src/components/TradingViewWidgets.jsx:11-16` · `SwingEdge_App.jsx:1864-1883` · ווידג'ט חסום ⇒ שטח ריק בלי מילה

**מה נמדד:** `s3.tradingview.com` נחסם, טאב "מודיעין שוק" נפתח:

```
{ containerFound: true, box: { w: 702, h: 518 }, innerText: "", childCount: 2 }
{ box: { w: 736, h: 74 }, innerText: "" }                    ← ticker-tape
anyErrorTextOnPage: false
```

**702×518 פיקסלים ריקים**, ללא טקסט, ללא סימן — במרכז הטאב.

**שורש:** שני אתרי הטעינה יוצרים `<script>` ומצרפים אותו בלי `onerror`:
`TradingViewWidgets.jsx:11-16` ו-`SwingEdge_App.jsx:1864-1883`. המכל אותחל
ל-`innerHTML = ""` (`:1849` · `:10`) ולכן נשאר ריק.

**התיקון הזול והלא-הרסני:** `script.onerror` בשני האתרים → כתיבת הודעה קצרה
לתוך המכל ("הגרף אינו זמין כרגע").

**⛔ מה אסור בתיקון:** ⛔ לא להעתיק את הטיפול פעמיים — שני האתרים כבר עותק
כפול של אותה לוגיקה (`R-6`); התיקון צריך לאחד אותם ל-`useTradingViewScript`
אחד, אחרת התיקון הבא ייעשה שוב רק בחצי.

---

### F-09 · [P2] · כל הלקוח · אין שום זיהוי מצב offline

**מה נמדד:**

```
grep -rn "navigator.onLine|'offline'|\"offline\"|addEventListener(\"online\"" src/ SwingEdge_App.jsx
→ 0 תוצאות
```

**שורש:** אין מנגנון. ⇒ ניתוק רשת מתפרק ל-N כשלים נפרדים במקום סיבה אחת:
מחירים קופאים (עם באנר ✅) · כתיבות נכשלות (עם toast ✅) · קריאות שותקות
(`F-06` ❌) · ווידג'טים ריקים (`F-08` ❌). המשתמש רואה ארבע תקלות ולא "אין
רשת".

**⚠️ הדירוג P2 ⛔ ואינו P1 בכוונה:** רוב המסלולים **כן** מדווחים בנפרד. החסר
הוא איחוד לסיבה אחת, ⛔ לא היעדר גילוי.

**התיקון הזול והלא-הרסני:** מאזיני `online`/`offline` + פס עליון יחיד.

**⛔ מה אסור בתיקון:** ⛔ לא להשתמש ב-`navigator.onLine` כשער שחוסם פעולות —
הוא מדווח "מחובר" גם מאחורי captive portal (`R-4`: מדידת נוכחות ולא אמת).
הוא תווית בלבד.

---

### F-10 · [P2] · `SwingEdge_App.jsx:16-33` · כשל טעינת צ'אנק בניסיון השני מפיל את כל האפליקציה

**מה נמדד:** `lazyWithRetry` מזהה `ChunkLoadError`, טוען מחדש **פעם אחת**
(שומר דגל ב-`sessionStorage`, `:24-28`) ובניסיון השני `throw err` (`:26`) ⇒
הגבול בשורש ⇒ `F-01`.

**שורש:** ⚠️ ההגנה מפני לולאת רענון היא **נכונה** ואינה הבאג. הבאג הוא היעד:
`throw` מגיע לגבול היחיד שקיים, ולכן כשל בצ'אנק **אחד** מוחק את **הכול**.
ההסתברות עולה כי `dist/assets/*` מוגשים עם `public, max-age=0,
must-revalidate` (נמדד ב-`E-05`), ⇒ דפדפן שמחזיק `index.html` ישן אחרי deploy
יבקש hash שכבר אינו קיים.

**היקף:** `AdminPanel` הוא ה-`import()` הדינמי **היחיד** בכל הריפו (נמדד
ב-`E-02`: 1 מתוך 1) ⇒ הפגיעה כיום היא במסלול האדמין בלבד.

**התיקון הזול והלא-הרסני:** `F-01` (גבול פנימי) פותר גם את זה. ⛔ אין עבודה
נוספת.

**⛔ מה אסור בתיקון:** ⛔ לא להסיר את דגל ה-`sessionStorage` כדי "לנסות שוב" —
זו לולאת רענון אינסופית מול deploy.

**תלות:** `F-01` · `E-05`.

---

## ✅ פאס — מסלולים שנמדדו ועמדו

⚠️ אלה **מדידות**, ⛔ לא רושם כללי. כל שורה נצפתה או נקראה בשורה מסוימת.

| # | מסלול | מה נמדד |
|---|--------|----------|
| 1 | **FX נופל** (`/api/fx` נחסם, 4 קריאות נדחו) | באנר מפורש `אין שער חליפין כרגע — המספרים מוצגים במטבע המקורי שלהם`, ו-**0 סימני ₪** בדף מול 532 סימני `$` ⇒ שום מספר לא הומר בשער מנוחש. `SwingEdge_App.jsx:4331` |
| 2 | `src/lib/fx.js:239-251` | `catch` מחזיר טבלה מימים **בלתי-משתנים** בלבד, בלי spot ⇒ ערכי הווה מדווחים "אין שער" ⛔ ולא שואלים שער מהעבר. `null` פירושו "אין", ⛔ לעולם לא `1` |
| 3 | **ציטוטים נופלים** | באנר עם מונה **ומכנה**: `אין מחיר חי ל-100 מתוך 100 עסקאות פתוחות` (`:4357-4378`) |
| 4 | מסלול **כתיבה** נכשל | `SwingEdge_App.jsx:2880-2897`: rollback של השורה האופטימית · הטופס מוחזר עם הערכים · `toast.error` עם סיבת השרת |
| 5 | `/api/verify-turnstile` נופל | `AuthScreen.jsx:108-114` — נכשל-פתוח על `5xx/429/רשת`, חוסם רק על `400` מובהק |
| 6 | לוגו FMP נכשל | `TickerLogo.jsx:23` — `onError` → תג גרדיאנט עם 2 אותיות. ⛔ אף פעם לא תמונה שבורה |
| 7 | `/api/ocr` נופל | `SwingEdge_App.jsx:3487-3496` — `AbortController` ב-15s + `setOcrStatus({status:"error"})` גלוי. ⛔ אין תלייה |
| 8 | `/api/feedback` נופל | `FeedbackTab.jsx:183-202` — ערוץ שני (EmailJS) + `detail` מפורט למשתמש |
| 9 | `/api/quote` מחזיר **200 עם HTML** | `priceService.js:110-117` — `res.ok` עובר, `res.json()` זורק, ה-`catch` מחזיר `{}` ⇒ מתדרדר נכון גם על תשובה בצורה שגויה, ⛔ לא רק על סטטוס שגוי |

---

## ⛔ מה לא ניתן היה למדוד — ואינו ממצא

| נושא | למה |
|-------|------|
| **Turnstile חי בדפדפן** (`F-07`) | ה-build המקומי נבנה **בלי** `VITE_SUPABASE_URL` (אומת: 0 מופעים של ה-project-ref ב-`dist/assets/index-*.js`) ⇒ `isSupabaseConfigured === false` ⇒ `AuthScreen` לעולם אינו מרונדר (`SwingEdge_App.jsx:3937`). הממצא נשען על קריאת קוד בלבד, והוא **מסומן ככזה** |
| **Supabase נופל תוך כדי סשן מאומת** | אותה סיבה. `F-06` נמדד בקוד ובספירה, ⛔ לא בדפדפן |
| **חסימה ברמת הרשת** (DevTools blocking) | הכלי אינו חושף CDP; החסימה נעשתה בשכבת JS (`fetch` + `script.src`). ⇒ הסימולציה נאמנה ל"הסקריפט/הבקשה לא הגיעו", ⛔ אך אינה זהה לניתוק TCP |
| **התנהגות אחרי deploy אמיתי** (`F-10`) | דורש שני builds בפרודקשן. הנימוק נשען על `E-05` (כותרות cache) ⛔ ולא על תצפית |

---

## סיכום

| # | חומרה | קובץ | בשורה אחת |
|---|-------|------|-------------|
| F-04 | **P0** | `SwingEdge_App.jsx:6672` | 8/10 שורות ברשימת המעקב מציגות קבועים קשיחים כמחיר חי |
| F-01 | P1 | `src/main.jsx:76` | גבול יחיד בשורש → 56 צמתים, 0 כפתורים |
| F-02 | P1 | `src/main.jsx:76` | ה-fallback מורה "רענן" — ונמדד שרענון אינו פותר |
| F-05 | P1 | `SwingEdge_App.jsx:4076` | `🟢 LIVE` נגזר מהשעון, ⛔ לא ממחיר שהגיע |
| F-06 | P1 | `SwingEdge_App.jsx:1690` | 3/7 מסלולי `SELECT` שותקים; `catch` בלי fallback |
| F-07 | P1 | `AuthScreen.jsx:438` | Cloudflare חסום ⇒ הרשמה חסומה בשקט |
| F-03 | P2 | `vite.config.js:36` | כל stack ב-Sentry מיוחס ל-recharts |
| F-08 | P2 | `TradingViewWidgets.jsx:11` | 702×518 ריק בלי מילה |
| F-09 | P2 | כל הלקוח | 0 זיהוי offline |
| F-10 | P2 | `SwingEdge_App.jsx:26` | כשל צ'אנק שני → הגבול בשורש |

**נמסר לפאזות הבאות:** Cloudflare אינו מנוטר ב-`api/health.js:131-137` → **G**.
`F-03`+`F-10` נשענים על `E-01`/`E-05` — **תיקון אחד סוגר שלושה ממצאים**.

**⛔ 0 קבצי קוד/קונפיג/DB שונו. ⛔ 0 סודות הודפסו. ⛔ פרודקשן לא נגענה.**
מצב הפריוויו הוחזר: `swingEdgeAccountCurrency` נמחק · `__deepf_backup` נמחק ·
כל הפאצ'ים הוסרו. ⚠️ ב-`localStorage` של הפריוויו נותרו 500 עסקאות סינתטיות
מפאזה E — מקומי בלבד, ⛔ לא בפרודקשן ולא בריפו.
