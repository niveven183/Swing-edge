# אבחון · שער ההידרציה בסנטינל המאומת · 20.09.2026

**סיווג:** `T3` · תשובות `כן/כן/לא/כן/כן` (4/5). שאלה 1 (כתיבת DB ב-`B-350`) היא
טריגר-יחיד. §8.1 ⇒ אבחון read-only נדחף לפני כל נגיעה בקוד.

**מפעיל:** 12 התרעות סנטינל ב-3 שעות (20.09, מ-01:44 ומ-07:06 UTC), כולן זהות:

```
הון התחלתי $10,000 — הסימן שההגדרות נטענו מה-DB
מוצג: $3,358 (טקסט מלא: התחלה $3,358)
אזור ההון קיים ומציג ערך שאינו 10,000 גם אחרי 25 שניות
```

⛔ **אפס תיקון בשלב הזה.** הדוח מודד; התיקון בקומיט נפרד שאחריו.

---

## 1 · מה השער בודק, ומתי נכתב

`tests-sentinel/sentinel-auth.spec.js:666-705`, בלוק `// ---- 2. hydration gate`.

```js
const capital = page.locator('[data-tour="equity"]')
  .locator('span')
  .filter({ hasText: /(התחלה|Started at)\s*\$/ })
  .first();
...
await expect(capital).toContainText('10,000', { timeout: HYDRATION_TIMEOUT });
```

**נולד:** `1a60d70`, 26.07.2026. שונה ב-`54713d3` באותו יום (8s → `HYDRATION_TIMEOUT`,
פיצול לכידת-הראיה לשני ענפים). ⇒ שבעה שבועות **לפני** `B-142` (15.08,
`resolveEquityBase`) ולפני כל עבודת המטבע.

**הוא מניח `capitalCurrency === accountCurrency === USD` — בשתי שכבות נפרדות:**

| שכבה | הבייטים | ההנחה |
|---|---|---|
| locator | `/(התחלה\|Started at)\s*\$/` | סימן `$` **ליטרלי**. תצוגת `₪` ⇒ אפס התאמות ⇒ ענף **כתום** (`hydration-timeout`), ⛔ אדום |
| ערך | `toContainText('10,000')` | המספר ה**גולמי**. כל `reason` שאינו `identity` מייצר מספר אחר **בהכרח מבני** |

`readCapital` (`:158-171`) נושא את אותה הנחה: `text.match(/\$\s*([\d,]+(?:\.\d+)?)/)`
ומחזיר `` `מוצג: $${m[1]}` `` עם `$` **קבוע**.

---

## 2 · שתי ההשערות, והמדידה שהכריעה

הרינדור (`SwingEdge_App.jsx:4488`) נגזר מ-`:2207-2246`:

```js
const fxOk = capitalCurrency === accountCurrency || (displayCapital != null && equityBaseD.ok);
const dispCcy = fxOk ? accountCurrency : capitalCurrency;
const equityBase = fxOk ? equityBaseD.value : capital;
```

| | H1 — שער מיושן, מוצר תקין | H2 — ההגדרות השתנו |
|---|---|---|
| `capital` | `10000` | `3358` |
| `capitalCurrency` | `ILS` | `USD` |
| מה מוצג | `$3,358` (מומר) | `$3,358` (גולמי) |

שתיהן מייצרות **אותם בייטים בדיוק** על המסך. המסך ⛔ יכול להפריד.

### השאילתות (READ-ONLY, הורצו באישור מפורש של ניב)

```sql
select s.settings->>'capital'         as capital,
       s.settings->>'capitalCurrency' as capital_currency,
       s.settings->>'accountCurrency' as account_currency,
       s.updated_at
  from public.user_settings s
  join auth.users u on u.id = s.user_id
 where u.email = '<<QA email>>';
```
```
capital  capital_currency  account_currency  updated_at
10000    ILS               USD               2026-09-20 10:12:32.579+00
```

```sql
select min(coalesce(t."closedAt"::date, t.date)) as equity_base_day,
       count(*) as closed_rows
  from public.trades t join auth.users u on u.id = t.user_id
 where u.email = '<<QA email>>' and t.status = 'CLOSED';
```
```
equity_base_day  closed_rows
2026-06-10       3
```

### שרשרת ההוכחה — התאמה מלאה, ⛔ «≈»

| שלב | מקור | ערך |
|---|---|---|
| ההון | `user_settings` | `10000` **ILS** |
| יום הבסיס | `equityBaseDayKey` = `min(closedAt)` על `CLOSED` | `2026-06-10` |
| הקיבוע | `GET /api/fx?base=ILS&symbols=USD&date=2026-06-10` → `{"rates":{"USD":0.33575}}` | `0.33575` |
| `convert` (`fx.js:84`) | `10000 × 0.33575` | `3357.5` |
| `Math.round` (`:4488`) | | `3358` |
| `.toLocaleString()` | | `"3,358"` |
| `dispSym` | `fxOk=true ⇒ dispCcy=USD` | `"$"` |
| הרינדור | `` `${t.startedAt} ${dispSym}…` `` | `התחלה $3,358` |
| **דיווח הסנטינל** | | `מוצג: $3,358 (טקסט מלא: התחלה $3,358)` |

**⇒ H2 מופרכת:** היא דרשה `capital = 3358` ו-`capitalCurrency = USD`.
**⇒ H1 מאושרת:** המוצר תקין; `resolveEquityBase` מחזיר `reason: "converted"` בקיבוע
יום-הבסיס, בדיוק כפי שהחוזה ב-`equityBase.js:60-82` מתחייב. **השער מיושן.**

### גבול ההוכחה

`updated_at = 10:12 UTC` הוא **אחרי** פתיחת חלון ההתרעות (01:44 UTC) ⇒ העמודה
⛔ יכולה לתארך מתי `capitalCurrency` הפך ל-`ILS`, ואיני טוען שהיא כן. מה שתומך
בכך שהמצב קודם ל-10:12: טקסט ההתרעה **זהה בכל 12 הירי**. ⛔ אין audit trail
ב-`user_settings` ⇒ **תאריך השינוי לא נמדד**.

---

## 3 · ממצא שאינו תלוי בשום השערה — 8 שלבים מעוורים

`:703-704`:
```js
record(diag);
return; // אין להמשיך: אימות מול ברירות מחדל = דיווח שקר
```

ה-`return` מדלג על **שמונה** שלבים במסע המאומת:

| שורה | שלב |
|---|---|
| `:707` | פתיחת ג'ורנל + סחיפת נתונים ישנים |
| `:749` | רינדור הג'ורנל |
| `:774` | באנר ההסכמה (כולל חפיפה ל-FAB) |
| `:809` | יצירת עסקה דרך הטופס |
| `:833` | מתקן `B-305` (`riskPct === null`) |
| `:855` | טאבים + גבול השגיאה (`B-304`) |
| `:871` | מחיקה דרך ה-UI |

⚠️ **שש המרות ה-`count()` ⇒ שער-שמסוגל-להיכשל שנחתו ב-`afb0d47` (19.09) ⛔ רצו
אף פעם מאז שנחתו** — הן נולדו לתוך קטע מת.

---

## 4 · הנחה שהמדידה הפריכה

הפרומפט הניח: «ב-19.09 הוגדר בחשבון ה-QA הון ₪10,000 עם `capitalCurrency=ILS`».
⛔ **אין לכך עיגון בריפו:**

- ה-`₪10,000` ב-`B-348` וב-`DECISIONS` 19.09 הוא **טקסט ציפייה נעול של `C-055` ⓷**,
  שחושב מ-`src/lib/positionSizing.js` ⛔ נקרא מה-DB.
- `C-041` ⓺ (06.09) מתעד `accountCurrency = capitalCurrency = USD` (**זהות**), עם
  «המטבע ⛔ **לא שונה** (איסור מפורש)».
- `D-088` מתעד את תצפית 19.09 כ**דריסת SHARES ידנית**, ⛔ שינוי מטבע.
- `git log --since=2026-09-18` — אף קומיט לא נגע במטבע חשבון ה-QA.

**ניב אישש 20.09:** ה-₪10,000 הוגדר ב**חשבון האישי**, ⛔ ב-QA. ⇒ H1 נכונה
**מסיבה אחרת** מזו שניתנה. מתי חשבון ה-QA עבר ל-ILS — **לא נמדד**.

---

## 5 · פסק דין

| | |
|---|---|
| **ההכרעה** | **3-א** — התנהגות תקינה + שער מיושן ⇒ תקן את **השער**, ⛔ את המוצר |
| **0-א** | שער ההידרציה יאמת **שלושה** תנאים, כולם נדרשים: סימן מטבע מ-`[$₪]` · `≠ DEFAULT_CAPITAL` (**מיובא** מ-`src/utils.js`, ⛔ ליטרל — מחלקת `B-324`) · `> 0`. ⛔ ריכוך ל«יש מספר כלשהו» — זה היה עובר על `2,500` |
| **0-ב** | `readCapital` (`:158-171`) → `[$₪]`, והמחרוזת נושאת את הסימן **שנמצא**. מחלקת `B-335` |
| **0-ג** | ממצא §3 ⇒ פריט **חוסם** חדש. מדד סגירה: ריצת סנטינל מאומתת אחת שהגיעה ל-`:871` — ⛔ הפריסה |
| **⛔ אסור** | לתקן את ההון ב-QA · כל `UPDATE` · לרכך את השער |

**מה ההתרעה תראה אחרי:** `$3,358` הוא `≠ 2,500`, נושא `$`, וחיובי ⇒ **ירוק**.
ה-`return` מפסיק לירות ושמונת השלבים רצים בפעם הראשונה. ⚠️ **צפוי גל ממצאים
חדש** — שערים שמעולם לא הורצו יכולים להיות אדומים מיד. זו **תוצאה של חשיפה**,
⛔ רגרסיה שנגרמה בגל הזה.
