# PLAN 2026-08-05 — מסלול המצלמה באנדרואיד (`capture="environment"`)

**סטטוס:** awaiting approval
**HEAD בעת הכתיבה:** `86a9e1f`
**סיכון ב-STATE:** 🔴 הראשון (`docs/STATE.md:77-82`)
**פידבק מקור:** `omrikapara1` ×4 · אנדרואיד 406px · 31.07

---

## 0 · Safety gate

```
$ git remote -v | head -1
origin	https://github.com/niveven183/Swing-edge (fetch)

$ git pull origin main && git status && git log -1 --oneline
Already up to date.
On branch main
Your branch is up to date with 'origin/main'.
nothing to commit, working tree clean
86a9e1f docs(decisions): מלכודת התחילית העברית + צבר מעורב
```

עבר.

---

## 1 · אימות הממצא (קריאה בלבד)

`SwingEdge_App.jsx:5784` הוא הקלט **היחיד בכל הריפו** הנושא `capture=`:

```
$ grep -rn 'capture=' . --include='*.jsx' --include='*.js' --include='*.html' \
    --exclude-dir=node_modules --exclude-dir=.git
SwingEdge_App.jsx:5784:  <input ref={chartFileRef} type="file" accept="image/*" capture="environment"
                          onChange={handleChartFileFallback} className="hidden"
                          aria-hidden="true" tabIndex={-1} />
```

### למה לשלושת האחרים אין `capture`

| שורה | מסך | צורה | סמנטיקה |
|------|------|------|----------|
| `:4667` | Analyzer | `<label>` גלוי, `t.uploadChart` | "העלה תמונה" — פעולת **העלאה**, המשתמש כבר בחר להעלות קובץ |
| `:6481` | Playbook | `<label>` גלוי, `t.uploadImage` | זהה |
| `:6979` | Journal | `<label>` גלוי, `t.uploadChart` | זהה |
| `:5784` | **כלים / צ'ארט** | קלט **מוסתר**, נורה תוכניתית | fallback אחרי כשל שיתוף מסך — לא בחירה של המשתמש |

שלושת הראשונים הם קלט שהמשתמש לוחץ עליו במפורש כדי להעלות קובץ. `:5784` נורה
מ-`openChartFallback` (`:3139-3143`) בעקבות `handleChartAiExtract`, ומגיעים אליו
**רק** כאשר:

```jsx
// SwingEdge_App.jsx:3171-3177
const md = navigator.mediaDevices;
if (!md || typeof md.getDisplayMedia !== "function") {
  openChartFallback();
  return;
}
```

באנדרואיד `getDisplayMedia` אינו קיים, ולכן המסלול **תמיד** נופל לענף הזה.
`capture="environment"` מורה לדפדפן לפתוח מצלמה אחורית במקום בורר קבצים —
משתמש שמנסה להעלות **סקרין שוט** לא יכול לבחור אותו. כלומר: הענף היחיד שנחשף
רק למובייל הוא היחיד שכופה מצלמה.

### תיקון פרמיסה

הפרומפט מפנה ל-`STATE:1064`. `docs/STATE.md` הוא **100 שורות** (נגזם ב-`c2f75f1`,
1636→100). הטענה יושבת כיום ב-`docs/STATE.md:77-82`. התוכן זהה, ההפניה ישנה בלבד.

---

## 2 · שלב 1 — ההכרעה

### אפשרות א — הסרת `capture` לחלוטין

| פלטפורמה | התנהגות אחרי הסרה |
|-----------|--------------------|
| **אנדרואיד Chrome** | בורר המערכת נפתח עם "מצלמה / קבצים / גלריה". **שתי הדרכים זמינות** — המשתמש בוחר סקרין שוט מהגלריה, או מצלמה אם ירצה |
| **iOS Safari** | action sheet: "ספריית תמונות / צלם תמונה או וידאו / בחר קובץ". **שתי הדרכים** |
| **דסקטופ** | **ללא שינוי.** דפדפני דסקטופ מתעלמים מ-`capture` ממילא; המסלול נחשף רק כשמשתמש מבטל את דיאלוג שיתוף המסך (`:3187-3192`), ואז נפתח דיאלוג קבצים רגיל — כמו היום |

### אפשרות ב — שני כפתורים מפורשים ("בחר תמונה" / "צלם")

| פלטפורמה | התנהגות |
|-----------|----------|
| אנדרואיד / iOS | אותה תוצאה סופית כמו א', אחרי **הקשה נוספת** |
| דסקטופ | "צלם" חסר משמעות — צריך להסתיר אותו, כלומר ענף UI נוסף לפי פלטפורמה |

**עלות:** קלט שני עם `capture`, גיליון בחירה חדש ב-406px, מחרוזות i18n חדשות
(he+en), וענף הסתרה לדסקטופ.

### המלצה: **א**

שלוש סיבות, לפי סדר משקל:

1. **ב' מכפילה בידיים בחירה שמערכת ההפעלה כבר נותנת** — מתורגמת, מקומית,
   מוכרת למשתמש, ובלי קוד. בורר הקבצים של אנדרואיד ושל iOS *הוא* התפריט
   "גלריה או מצלמה". בניית תפריט משלנו מעליו היא שכפול נחות.
2. **§3 (מטרת-העל) —** ב' מוסיפה הקשה קבועה לכל משתמש מובייל בנתיב הראשי.
   פתרון שמוסיף צעד ידני קבוע הוא פתרון גרוע גם אם הוא נכון טכנית.
3. **שרשרת מחוות המשתמש —** ההערה ב-`:3173-3174` מציינת מפורשות שהפתיחה חייבת
   לרוץ **סינכרונית בתוך מחוות הקליק** כדי שהדפדפן ירשה לדיאלוג להיפתח. כל
   שכבת UI ביניים נוגעת בשרשרת הזו. א' לא נוגעת בה בכלל.

**א' היא מחיקה של מאפיין אחד.** מחזירה מצלמה **וגם** גלריה בכל פלטפורמה, בלי UI
חדש, בלי מחרוזות חדשות, בלי סיכון רגרסיה בדסקטופ.

⛔ בשתי האפשרויות: `capture="environment"` לא נשאר על המסלול הזה בשום צורה.

---

## 3 · שלב 2 — הבדיקה שנצפתה נכשלת

הרצה **על הקוד הישן**, לפני כל שינוי (HEAD `86a9e1f`):

```
$ node -e '<האסרציה>'
input found:
  <input ref={chartFileRef} type="file" accept="image/*" capture="environment"
   onChange={handleChartFileFallback} className="hidden" aria-hidden="true" tabIndex={-1} />

✗ FAIL: chart fallback input carries capture= — Android opens the camera, gallery unreachable
exit code 1
```

**האסרציה נכשלת על הקוד הקיים.** תרחיש שלא נצפה נכשל אינו שער — זה נצפה.

**בית קבוע:** `scripts/ocr-contract-test.mjs` (`test:ocr`), הבית של מסלול ה-OCR
מהצ'ארט ו-`api/ocr.js`.

**דפוס:** סריקת מקור, קיים כבר בריפו — `rContract-test.mjs:138` ו-`engine-test.mjs:276`
קוראים `SwingEdge_App.jsx` כטקסט ומאשרים דפוסים.

**עיגון:** האסרציה מאתרת את הקלט לפי `ref={chartFileRef}` ולא לפי מספר שורה, כדי
שלא תישבר מהזזת קוד ולא תעבור בשקט אם הקלט יעבור מקום.

---

## 4 · שלב 3 — ⚠️ ציד: למה זה לא נתפס

### השורש אינו "בדקנו 406px"

`playwright.config.js:26` מגדיר project **אחד**:

```js
projects: [
  { name: 'chromium', use: { ...devices['Desktop Chrome'] } },
],
```

אין project מובייל. כל עשר בדיקות שרשרת ה-`verify` הן Node טהור בלי DOM.
מכאן: **שום בדיקה בריפו לא רצה אי-פעם תחת UA מובייל או תחת דפדפן שחסר לו
API של דסקטופ.**

חלון דסקטופ מוקטן ל-406px הוא אותו מנוע ואותו משטח API — יש בו `getDisplayMedia`,
ולכן הוא **לעולם לא מגיע** לענף `:3172`. **נבדק רוחב, לא פלטפורמה.** זו הסיבה
המבנית, והיא חלה על כל השורות שלהלן, לא רק על הבאג הזה.

### מסלולים שמסתעפים לפי יכולת דפדפן ולא לפי רוחב, ואינם מכוסים בשום בדיקה

רשימה בלבד — **לא מתקן בפרומפט הזה** (§11).

| # | מיקום | ההסתעפות | מה קורה במובייל / WebView אמיתי | כיסוי |
|---|-------|-----------|--------------------------------|-------|
| 1 | `SwingEdge_App.jsx:3171` | `!md \|\| typeof md.getDisplayMedia !== "function"` → `openChartFallback()` | **הבאג עצמו.** רק מובייל מגיע לענף | אין |
| 2 | `SwingEdge_App.jsx:2606` | `navigator.clipboard.writeText` — **בלי בדיקת קיום**, רק `try{}catch{}` ריק | הקשר לא-מאובטח / WebView של אנדרואיד: `navigator.clipboard` הוא `undefined` → זריקה → נבלעת בשקט. קוד ההזמנה לא הועתק, המשתמש לא יודע. `catch` עם הערה בלבד — §2 "אפס כשל שקט" | אין |
| 3 | `src/contexts/ThemeContext.jsx:42` | `const mq = window.matchMedia('(prefers-color-scheme: dark)')` — **לא מוגן**, בניגוד ל-`:31` באותו קובץ שכן עטוף ב-`try/catch` | WebView ישן בלי `matchMedia` → זריקה בתוך `useEffect` → ThemeProvider נופל, ואיתו כל העץ | אין |
| 4 | `src/components/IOSInstallBanner.jsx:9,13,20` | `navigator.userAgent` + `navigator.platform === "MacIntel"` + `navigator.maxTouchPoints` + `navigator.standalone` | הקומפוננטה כולה iOS-only. Desktop Chrome **לעולם לא מרנדר אותה** — כלומר היא מעולם לא הורצה בשום בדיקה | אין |
| 5 | `src/components/FeedbackTab.jsx:49` | `parseUA()` — זיהוי דפדפן/OS למטא-דאטה של הפידבק | תיוג OS שגוי בדיווח הוא בדיוק מה שמסווה באג פלטפורמה. אם דיווח אנדרואיד מתויג "Unknown", הסיווג לפי פלטפורמה בלתי אפשרי | אין |
| 6 | `SwingEdge_App.jsx:1304` · `OnboardingTour.jsx:37` | `prefers-reduced-motion`. **ה-Tour מוגן** (`window.matchMedia ? … : false`), **`:1304` לא** — `window.matchMedia(...).matches` חשוף | סיכון נמוך (matchMedia נתמך רחב), אך חוסר העקביות בין שני האתרים הוא הדפוס עצמו | אין |

**המכנה המשותף של 1–6:** כולם מסתעפים על קיום API או על UA. אף אחד מהם אינו
נגזרת של רוחב מסך. אימות ויזואלי ברוחב מוקטן אינו יכול לגעת באף אחד מהם, בהגדרה.

---

## 5 · ניתוח השלכות (§8)

**רמה 1 — פילטר שקט:**

| שאלה | תשובה |
|-------|--------|
| משנה DB? | לא |
| נוגע בכסף או במיילים? | לא |
| מוסיף סוד? | לא |
| רץ אוטומטית בפרודקשן? | לא |
| בלתי הפיך? | לא — מחיקת מאפיין אחד, `git revert` מחזיר |

כל התשובות "לא" → **אין טבלת רמה 2.**

---

## 6 · הביצוע — רק אחרי אישור

1. מחיקת `capture="environment"` מ-`SwingEdge_App.jsx:5784`. **שינוי הקוד היחיד.**
2. הוספת האסרציה מסעיף 3 ל-`scripts/ocr-contract-test.mjs`.
3. `npm run verify` — **פלט מלא מודבק בדיווח**, לא סיכום (§7).
4. באותו קומיט (§10):
   - `docs/STATE.md` — סגירת ה-🔴, ושש שורות ⚠️ לממצאי סעיף 4 (§10.1: אין פריט יתום).
   - `docs/DECISIONS.md` — שורה מתוארכת: בורר הקבצים של מערכת ההפעלה גובר על
     תפריט בחירה ידני; `capture` אסור על קלט fallback שנורה תוכניתית.
5. `git push origin main` — רגיל. **בלי force, בלי `--no-verify`.**
6. דיווח: hash + סיימתי ✅

---

## 7 · מה התוכנית הזו לא עושה

- **לא** מוסיפה project מובייל ל-Playwright. זו המסקנה המתבקשת מסעיף 4, אבל היא
  משימה נפרדת בהיקף אחר, והיא תיכנס ל-`docs/STATE.md` כ-⏭️ ולא תבוצע כאן (§11).
- **לא** נוגעת בשורות 2–6 מטבלת הציד. רשימה להכרעת ניב.
- **לא** נוגעת בשלושת הקלטים האחרים (`:4667` · `:6481` · `:6979`) — אין להם `capture`
  ואין להם בעיה.
