# PLAN — `F1` · סעיף מול סטטוס, ושער שיכול לראות כותרות

> **תאריך:** 2026-08-27 · **HEAD בזמן הכתיבה:** `b866d30`
> **רמה:** T3 · **תשובות 1–5:** לא / לא / לא / **כן**⚠️ / **כן**
> **אימות-אחרי:** `אומת-מקור` (⛔ אין מסך מושפע) — קריאת התוצר מ-`origin` + מדידה עצמאית חוזרת
> **מצב:** ⏸️ **awaiting approval** — §9 שלב 2. ⛔ אפס נגיעה בקוד עד אישור שלב 3.

**נימוק הסיווג:** שאלה 4 = «כן» **מספק** — `scripts/registry-test.mjs` הוא קובץ קוד יחיד
ו⛔ אינו מסלול כתיבה, אבל הוא השער החוסם של **כל** `push` ⇒ ספק ⇒ כן (§15).
שאלה 5 = «כן» — `F1`/`F4` הגיעו כמספרים שנויים במחלוקת ולא נמדדו ב-48 שעות.

---

## 0 · מקור התוכנית — מה נמדד, ומה ⛔ שרד את המדידה

התוכנית נגזרת מ-`docs/audits/AUDIT-backlog-hygiene.md` (`b866d30`) וממדידות
read-only שנוספו בסשן הזה. **⛔ אף סעיף כאן אינו נשען על מספר שלא נמדד מחדש.**

### 0.1 `F1` = **6** — ⛔ לא 8

תחת הכותרת `## ❄️ מוכרע-לא-לעשות` (`BACKLOG.md:254`) יש **18 שורות `B-`**:

| סטטוס | כמות | מזהים |
|--------|------|--------|
| `❄️ מוכרע-לא-לעשות` | 11 | `B-104`…`B-115` |
| **`פתוח`** | **6** | `B-118` · `B-120` · `B-121` · `B-123` · `B-124` · `B-125` |
| מצבה `✅ בוצע → D-025` | 1 | `B-122` |

**הפרסר הגס שוחזר והחזיר 8 בדיוק.** הוא סורק `פתוח` בכל מקום בשורה:

| מזהה | סטטוס אמיתי | הטקסט שנתפס |
|------|--------------|---------------|
| `B-105` | `❄️ מוכרע-לא-לעשות` | בגוף: «GHSA-qwww-vcr4-c8h2 (high) **נשאר פתוח** — סיכון מקובל» |
| `B-107` | `❄️ מוכרע-לא-לעשות` | בגוף: «**מה שנשאר פתוח** כתוצאה:» |

⇒ שניהם **פרוזה**, ⛔ לא סטטוס. **`F1` = 6.**
⚠️ וההפרש עצמו הוא `R-3` — grep על טוקן מילולי שמתחזה למשמעות. **נכנס ל-`DECISIONS`
כהפרכה שביעית** (הכרעת ניב 27.08), ⛔ לא מרוכך.

### 0.2 המנגנון — השער עיוור מבנית לכותרות

- `scripts/registry-test.mjs` ⛔ **אינו מפרסר אף כותרת `#`** — 0 מופעים.
- §7.1 מסנן `❄️` **בתא** (`d.cells.some(c => c.includes("❄️"))`), ⛔ לא בכותרת
  ⇒ ששת הפריטים **⛔ אינם נספרים בו כלל**.
- ⇒ ⛔ **אין שער בריפו שיכול לראות סתירת סעיף↔סטטוס.** `R-4`.

### 0.3 המקור — בלוק יתום, ⛔ לא החלטה

כל שמונת השורות (`B-118`…`B-125`) נוספו בקומיט **אחד**: `3b1a1cf` (12.08).
`git show 3b1a1cf -- docs/BACKLOG.md` ⛔ **אינו מכיל אף שורת `+## `** ⇒ הבלוק
נוסף בלי כותרת משלו ויורש את `❄️` **לפי מיקום בלבד**. ⛔ איש לא החליט שהם קפואים.

### 0.4 🔴 `F4` — **מופרך. הממצא שלי, ואני מפריך אותו.**

`F4` = **36** (⛔ לא 40 — 4 ההפרש הם ✅ בפרוזת הגוף של `B-150`·`B-166`·`B-168`·`B-114`).
**אבל המספר אינו הבעיה — הממצא עצמו שגוי.** שלוש מדידות:

1. **`scripts/registry-test.mjs:263-266`**, בהערה בקוד עצמו:
   «שורת **מצבה** היא קיום חוקי לכל דבר… ⛔ זו אינה עקיפה — זה **הפתרון** ל-`B-126`».
2. **§6.1 (`:269`)** — «מזהה אינו נעלם ואינו ממוחזר». מחיקת שורת `B-` **יורה אדום**,
   והודעת הכשל אומרת: «פריט שהוכרע נשאר כ**מצבה** `✅ בוצע → D-nnn`».
3. **`docs/METRICS.md:41`, שורת `M-007` עצמה** — «⛔ המכנה הוא **כל** פריטי `B-`,
   **כולל מצבות**» ⇒ המכנה ⛔ אינו מנופח; הוא **מוצהר עם שם האוכלוסייה** (§2).

⇒ 36 המצבות הן **המנגנון המתוכנן**. ביצוע `F4` היה **שובר את `B-126`** ומאדים את §6.1.

> 🔴 **לרישום מפורש, בהוראת ניב 27.08:** `F4` הגיע ל**אישור ביצוע** ועבר אותו.
> **האישור היה שגוי, והממצא היה שלי.** מה שעצר אותו הוא מדידה שנעשתה **אחרי**
> האישור, ⛔ לא האישור. ⇒ אישור ⛔ אינו תעודת-מקור לממצא ‹R-1›.
> **פעולה: ⛔ אפס.** `F4` מבוטל.

### 0.5 ששת פריטי `F1` — הסטטוס צודק, ⛔ לא הסעיף

| מזהה | נמדד 27.08 | פסק |
|------|--------------|------|
| `B-118` | משימת סקירה · ⛔ אפס ראיה לביצוע | **פתוח** |
| `B-120` | משימת סריקה · ⛔ אפס ראיה | **פתוח** |
| `B-121` | `minorUnit`: 14 מופעים ב-**2 קבצים** (המודול + הטסט) ⇒ **0 אתרי ייצור**. `src/utils.js:134`: `CURRENCY_SYMBOL = { USD:"$", ILS:"₪" }` — **⛔ אין `ILA`** | **פתוח · 🔴 חוסם** |
| `B-123` | `src/lib/instrumentCurrency.js:64` עדיין מתעד 101.5455× | **פתוח · מסלול כסף · ⛔ לא נסגר** |
| `B-124` | `13.9c` (`:700`) · `13.9d` (`:735`) קיימות; «נצפו אדומות» ⛔ אינו נמדד מהקוד | **פתוח** |
| `B-125` | 🔴 **מופרך** — `docs/TRUTH.md:50` נושא את #8 במלואו | **מצבה** (§4.3) |

### 0.6 `B-125` נולד שקרי

| קומיט | זמן | מה |
|--------|------|-----|
| `636b490` | 12.08 **18:00:32** | TRUTH #8 **הוחלף** («מעודכן 12.08») |
| `3b1a1cf` | 12.08 **23:25:58** | `B-125` נרשם: «#8 נמחק ו⛔ לא הוחלף» |

`git merge-base --is-ancestor 636b490 3b1a1cf` ⇒ **0** (קדם).
⇒ הפריט **שקרי מרגע לידתו**, 15 יום, ומקורו שורת `STATE` מיושנת. ‹R-1› + ‹R-5›.

### 0.7 ממצא נלווה — `|` חשוף

`docs/BACKLOG.md:180` (`B-175`) נושא `|` **לא מוברח** בתוך הגוף
(``browser-auth|journal-open``) ⇒ השורה מתפצלת ל-**8 תאים במקום 7**.
⛔ **אין אסרציה שבודקת רוחב עמודות ב-`BACKLOG`** — §5.2 בודקת **רק** את `CHECKS`.
⚠️ העמודה האחרונה עדיין נקראת נכון היום, ולכן זה עובר **בשקט**; שדה שיתווסף
בעתיד יזוז בלי שאיש יראה.

### 0.8 תקרת `STATE` — 🟢 נפתרת ⛔ בלי העלאת תקרה

`## 🔴 עכשיו` = **7,652 בתים = 49%** מהקובץ, ב-8 בולטים.
**6 מהם נושאים `D-` שכבר סגור ב-`DONE.md`** ⇒ **עותק שני** ‹R-6›:

| בולט | בתים | `D-` |
|-------|------|------|
| `C1` / `B-245` | 815 | `D-058` |
| `C2` / `B-250` | 391 | `D-057` |
| DEEP-AUDIT | 360 | `D-053` |
| גיזום `STATE` | 594 | `D-054` |
| אימות אבחון העיצוב | 679 | `D-056` |
| `METRICS` | 463 | `D-055` |
| **סה"כ ניתן לגיזום** | **3,302** | |

| | בתים | פנוי |
|---|------|------|
| היום | 15,671 | **329** |
| אחרי גיזום | **12,369** | **3,631** (22.7%) |

⚠️ שני הבולטים הנותרים (מסלול העיצוב 1,386ב · `Q1` 1,006ב) ⛔ **אינם נגזמים** —
⛔ אין להם `D-`, הם מצב חי.
⚠️ **`D-059` (`B-184`) ⛔ אינו מופיע ב-`🔴 עכשיו` כלל** — הסעיף מפגר בגל שלם.

⚠️ **תיקון לדיווח חלק א':** אמרתי «`F1`–`F7` ⛔ לא ניתנים לרישום בגלל התקרה».
**שגוי.** §14 מציב פריט פתוח שאינו פעיל ב-`BACKLOG`, ש**⛔ אין לו תקרה**.
התקרה מעולם לא חסמה רישום.

### 0.9 החוסם `B-121` → `B-005` הוא חד-כיווני

`B-121` נושא `חוסם? = חוסם את B-005`.
`B-005` נושא `חוסם? = כן — 13/60 שורות` · סטטוס `חסום-על-ניב` · ⛔ **ואינו מזכיר
`B-121` כלל** (`/B-121/.test(row)` = `false`). ⇒ הקשר קיים בכיוון אחד בלבד ‹R-6›.

---

## 1 · מה התוכנית עושה — ומה ⛔ אינה עושה

| ✅ עושה | ⛔ ⛔ אינה עושה |
|---------|------------------|
| מוסיפה כותרת סעיף ל-6 שורות יתומות | ⛔ משנה סטטוס של פריט כלשהו |
| הופכת את `B-125` למצבה | ⛔ מוחקת שורת `B-` — **אף אחת** |
| מוסיפה 2 אסרציות לשער | ⛔ מבצעת את `F4` |
| גוזמת 6 בולטים כפולים מ-`STATE` | ⛔ מעלה את תקרת ה-16,000 |
| מוסיפה שורת `DECISIONS` אחת | ⛔ נוגעת בקוד מוצר |
| מודדת `M-007` מחדש עם שושלת | ⛔ מאריכה תוקף של מדידה |
| רושמת חוסם הדדי ב-`B-005` | ⛔ פותחת פריט `❄️` מחדש |

**⛔ אסור מוחלט (רשימת ניב, 27.08):** קוד מוצר · `B-185` · `B-186` · `main.jsx` ·
LegalPages · `tokens.css` · `index.css` · `api/` · הארכת תוקף · העלאת תקרה ·
מחיקת שורת `B-` · ביצוע `F4`.

---

## 2 · השער החדש — `test:registry` §13

**קובץ:** `scripts/registry-test.mjs` · **מיקום:** אחרי §12 · **מונה:** 36 → **38**.

### 2.1 · §13.1 — כותרת-סעיף חייבת להסכים עם עמודת הסטטוס

**זו הפעם הראשונה שהשער מפרסר `#`.** נוסף פרסר כותרות שמייחס לכל שורת `B-`
את הכותרת האחרונה שקדמה לה.

**הכלל — מינימלי ובר-הפרכה:**
> שורה שהכותרת שמעליה נושאת `❄️` חייבת שהסטטוס שלה יישא `❄️` **או** יהיה **מצבה**.

⚠️ **למה לא כלל גורף על כל הסעיפים:** סעיפים כמו `🟠 חשוב` / `⚪ חוב` הם **עדיפות**,
⛔ לא סטטוס, ולכן אין להם התאמה מוגדרת. שער שיאדים עליהם היה מודד רעש ומאלץ
ריכוך — וריכוך כדי לעבור הוא `R-4`. **הכלל מכסה את הסעיף היחיד שהוא הכרעה.**

**צפי אדום:** **6 שורות** — `B-118` · `B-120` · `B-121` · `B-123` · `B-124` · `B-125`.
`B-122` (מצבה) ו-11 שורות `❄️` ⛔ אינן נופלות.

### 2.2 · §13.2 — רוחב עמודות ב-`BACKLOG` = 7

> כל שורת `B-` מתפצלת ל-**7 תאים בדיוק** על `|` לא-מוברח.

⚠️ קיים ל-`CHECKS` (§5.2, `CHECK_CELLS = 5`) ו⛔ **לא קיים ל-`BACKLOG`** — ולכן
`B-175` עבר בשקט. **צפי אדום: שורה 1** — `B-175` (8 תאים).

### 2.3 · 🔴 סדר הריצה — ⛔ אין דילוג

```
1. כתיבת §13.1 + §13.2 בלבד   ⇒ ⛔ אפס תיקון בנתונים
2. npm run test:registry       ⇒ 🔴 ⛔ חייב לצאת אדום · הפלט מודבק בדיווח
3. רק אז — התיקונים (§3, §4)
4. npm run test:registry       ⇒ 🟢 38/38 · הפלט מודבק
```

⚠️ **«צפייה שלא רצה ⛔ אינה צפייה»** — אם אחת האסרציות יוצאת **ירוקה בשלב 2**,
זה אומר שהיא ⛔ אינה מודדת את מה שנטען, וזו **עצירה**, ⛔ לא תיקון ציפייה.

---

## 3 · תיקון `F1` — כותרת, ⛔ לא הזזת פריטים

**קובץ:** `docs/BACKLOG.md` · **מיקום:** בין `:270` (`B-115`) ל-`:272` (`B-118`).

נוספת כותרת חדשה + כותרת-טבלה. הנוסח המוצע:

```markdown
## 🧪 חוב שנמדד בגל ג׳ (12.08) — ⚠️ ⛔ לא הוכרע-לא-לעשות

> ⚠️ שבע השורות כאן נוספו ב-`3b1a1cf` **בלי כותרת משלהן** וירשו את `❄️`
> **לפי מיקום בלבד** (`AUDIT-backlog-hygiene.md` §F1 · הכותרת נוספה 27.08).
> ⛔ אף אחת מהן לא הוכרעה כ"לא לעשות". `test:registry` §13.1 חוסם חזרה.
```

- ⛔ **אפס שורה זזה. אפס סטטוס משתנה. אפס מזהה נוצר או נמחק.**
- `B-122` (מצבה) עוברת יחד עם הבלוק — היא שייכת לאותו גל, ומצבה חוקית בכל סעיף.
- ⚠️ נבדק ש-§7.1 (`frozen.length > 0`) נשאר ירוק: 11 שורות `❄️` ⛔ אינן זזות.

---

## 4 · `B-125` → מצבה (הכרעת ניב 27.08)

### 4.1 · השורה ב-`BACKLOG`

הסטטוס `פתוח` → `✅ בוצע → D-060`. **גוף הפריט ⛔ אינו נמחק** — נוספת לו סיפא
שמתעדת שההנחה הופרכה, כדי שהמזהה יישאר בר-ציטוט עם ההקשר המלא.

### 4.2 · השורה ב-`DONE`

מזהה חדש **`D-060`**, עם ה-hash של הקומיט ש**באמת** תיקן:

| שדה | ערך |
|------|-----|
| מזהה | `D-060` |
| מה | `B-125` — «`TRUTH.md` #8 נמחק ולא הוחלף». **הנחה מופרכת** |
| תאריך | 27.08 |
| hash | **`636b490`** |
| איך אומת | `docs/TRUTH.md:50` נושא את #8 («מעודכן 12.08»). `636b490` = 12.08 18:00:32; `B-125` נרשם ב-`3b1a1cf` = 12.08 23:25:58, מתוך `STATE` מיושן. `git merge-base --is-ancestor 636b490 3b1a1cf` ⇒ `0`. **שקרי 15 יום.** ‹R-1›+‹R-5› |
| `T` | T3 · `אומת-מקור` |

> 🔴 **למה `636b490` ולא ה-hash של הגל הזה:** §14 קובע שקומיט ⛔ אינו יכול לשאת את
> ה-hash של עצמו, ו-§8.4 דורש שמצבה תצביע ל-`D-` **שקיים באותו קומיט**. הפתרון
> ⛔ אינו עקיפה — הוא **מדויק יותר**: מה שהפך את `B-125` ללא-רלוונטי הוא `636b490`,
> ⛔ לא הגל הזה. הגל הזה רק **גילה** זאת.

⚠️ הנימוק «פריט שקרי במרשם גרוע מפריט חסר» הוא הכרעת ניב, 27.08 — מתועד ב-`DECISIONS`.

---

## 5 · גיזום `STATE` — ⛔ רק אחרי אימות דלתא

**לכל אחד מששת הבולטים, לפי הסדר, ⛔ אין קיצור:**

```
1. קרא את הבולט ב-STATE במלואו
2. קרא את שורת ה-D- המקבילה ב-DONE במלואה
3. רשום עובדה-עובדה: מה יש בבולט ואין בשורה?
4. דלתא ריקה  ⇒ גזום
   דלתא קיימת ⇒ העבר את הדלתא ל-DONE (או ל-DECISIONS אם היא הכרעה) קודם,
                ורק אז גזום
```

⛔ **אפס מחיקת תוכן.** אם דלתא כלשהי ⛔ אינה מוצאת בית — **עצירה ודיווח**,
⛔ לא גיזום «כי זה כמעט אותו דבר».

**בנוסף:** נוסף בולט קצר ל-`🔴 עכשיו` שמתעד את `D-059` (`B-184`) ואת הגל הזה —
הסעיף כרגע מפגר בגל שלם.

**יעד נמדד:** `wc -c docs/STATE.md` < 16,000, עם הפרש מדווח מול 15,671.
⛔ **התקרה ⛔ אינה זזה.** `registry-test.mjs:184` ⛔ אינו נערך.

---

## 6 · שורת `DECISIONS` — שבע הפרכות

**שורה אחת** (append-only, ⛔ אפס עריכה של שורה היסטורית), 27.08:

| # | הטענה | הנמדד |
|---|--------|--------|
| 1 | 59 פריטים מצטטים קובץ שאינו קיים | **1/258** |
| 2 | «המספר האמיתי 0» (הטענה-שכנגד) | **1** — `B-209`, נתיב עם `…` |
| 3 | 54/61 DEEP בלי הצלבה | **60/61 נושאים** · רק 8/61 עם `B-nnn` |
| 4 | `grep -c D-056 DECISIONS.md` = 0 | **1** · ו-`D-` חי ב-`DONE.md` (§14) |
| 5 | 163 פתוחים | **175** — 163 הוא `70123a0` מ-26.08 |
| 6 | **`F4`: 36 ✅ מפרות את §14** | **מצבות = הפתרון ל-`B-126`** (`:263-266` · §6.1 · §8.4 · `METRICS:41`) |
| 7 | **`F1` = 8** | **6** — `B-105`/`B-107` נתפסו על `פתוח` **בפרוזה** |

**המנגנון המשותף — `R-3`:** כל שבע נובעות מ-grep על **טוקן מילולי** שעומד במקום
משמעות. **`R-1`** מסביר למה הן שרדו: מספר שנמסר ⛔ בלי תעודת-מקור נקרא כעובדה.
⛔ **אין שורש שביעי** — `R-3` ו-`R-1` מכסים, וניסוחם ⛔ אינו משוכתב.

⚠️ **ו-#6 נרשמת עם ההודאה:** הממצא היה של Code, ניב **אישר אותו לביצוע**, ומה
שעצר היה מדידה שנעשתה **אחרי** האישור. ⇒ אישור ⛔ אינו תעודת-מקור.

---

## 7 · `M-007` — מדידה חוזרת עם שושלת

- הערך הישן יורד ל**שושלת** ‹§14›. ⛔ **`M-` חדש אינו נפתח.**
- המכנה נשאר **כל** פריטי `B-` **כולל מצבות** — ⛔ ההגדרה אינה משתנה.
- ⚠️ אם `B-259` נוצר (סעיף 8) המכנה זז `258 → 259` ⇒ **ארבעת המספרים בפרוזת
  `BACKLOG` חייבים לזוז עמו**, אחרת §8.6 יורה אדום. זה **רצוי** — זה בדיוק
  מה ש-§8.6 נבנתה בשבילו (`D-057`).
- ⛔ **`תוקף` ⛔ אינו מוארך.** `נמדד` = 27.08.

---

## 8 · פריטים שהגל ⛔ אינו מבצע — §10.1

⛔ אף אחד מאלה אינו מתוקן בגל הזה. כולם מקבלים שורת `B-` ב-`BACKLOG`
(⛔ **לא** ב-`STATE` — §14: `BACKLOG` הוא בית פריט פתוח שאינו פעיל, ו⛔ אין לו תקרה):

| מזהה | מה | שורש |
|------|-----|-------|
| `B-259` | **חוסם חד-כיווני** — `B-121` מצהיר «חוסם את `B-005`»; `B-005` ⛔ אינו מזכירו. ⛔ אין אסרציה לדו-כיווניות חוסמים | ‹R-6› |
| `B-260` | **סחיפת מספרי שורה שיטתית** (`F2`) — פריטי DEEP (26.08) מדויקים 4/4; ישנים סחפו 6/6, גרוע: `B-109` +325, `B-110` העוגן **נעלם** | ‹R-6› |
| `B-261` | **מוני כותרות-סעיף מיושנים** (`F3`) — DEEP «60» מול **61** · עיצוב «5» מול **14**. §8.6 ⛔ אינה רואה אותם (קוראת רק את פסקת השורשים) | ‹R-4› |
| `B-262` | **`M-004`/`M-005` «טריים ושקריים»** — בתוך חלון התוקף ⇒ §12 ירוק, בזמן שהתוכן כבר שקרי (הרשמה `2026-08-27T15:15:32Z` · עסקה `2026-08-25T14:47:17Z`) | ‹R-4› |
| `B-263` | **5 מכלים ב«חסום-על-ניב»** — `B-086`·`B-090`·`B-092`·`B-093`·`B-096` ⛔ אינם ניתנים לניסוח בשורה ⇒ דורשים פיצול. **`B-181` ⛔ אינו חסום על ניב** כלל | ‹R-5› |
| `B-264` | **`B-182` ≡ `B-185`** — אותה הכרעה בדיוק (מה להציג כשהספק נכשל), ⛔ אין קישור. **`B-014`: החלופה «Inter במקום General Sans» פסולה** (`B-254`) | ‹R-6› |
| `B-265` | **הטקסונומיה W0…W5 ⛔ אינה מנגנון בחירה** — **72/175 = 41%** בגל יחיד · 44 חוצים · 59 בלי אות ⇒ **103/175 = 59% לא שויכו**. ⛔ אין מנגנון בחירה מלבד `NEXT` (3 פריטים) ל-175 פתוחים | ‹R-5› |

⚠️ **הרשימה הזו היא סעיף §10.1 של הגל.** ⛔ פריט שיעלה בביצוע ואינו כאן — **נוסף
לפני ה-push**, ⛔ לא «נטפל בזה אחר כך».

---

## 9 · סדר הקומיטים

| # | תוכן | הודעה |
|---|-------|--------|
| 1 | **הקובץ הזה** | `docs(plan): F1 — סעיף מול סטטוס — awaiting approval` |
| — | ⏸️ **עצירה — §9 שלב 3.** ניב קורא מ-`origin` ומאשר | |
| 2 | §13.1 + §13.2 בלבד | `test(registry): §13 — כותרת↔סטטוס ורוחב עמודות (נצפה אדום)` |
| 3 | תיקוני §3·§4·§5·§6·§7·§8 | `fix(registry): F1 — כותרת לבלוק היתום · B-125 מצבה · גיזום STATE` |
| 4 | שורת `DONE` של הגל | `docs(registry): הגל נסגר → D-061` |

⚠️ **קומיט 2 נדחף אדום בכוונה?** ⛔ **לא.** `npm run verify` חוסם `push`.
⇒ קומיטים 2 ו-3 נדחפים **יחד** אחרי שהשער ירוק, ו**הפלט האדום של שלב 2 מודבק
בדיווח** כראיה. ⛔ הראיה היא הפלט, ⛔ לא קומיט שבור ב-`main`.
⚠️ קומיט 4 נפרד מפני שקומיט ⛔ אינו יכול לשאת את ה-hash של עצמו (§14).

**לפני כל push:** `npm run verify` — 23 חוליות ואז `build`. הפלט **המלא** מודבק (§7).

---

## 10 · הפיכות

| מה | איך חוזרים | תוך כמה |
|-----|-------------|----------|
| כותרת ב-`BACKLOG` | `git revert` | דקה |
| `B-125` מצבה + `D-060` | `git revert` — ⛔ אף מזהה לא נמחק | דקה |
| §13.1/§13.2 | `git revert` — 38 → 36 | דקה |
| גיזום `STATE` | `git revert` — התוכן ב-`DONE`/`DECISIONS` ממילא | דקה |

⛔ **אפס כתיבת DB · אפס מייל · אפס סוד · אפס workflow · אפס קוד מוצר**
⇒ §8 רמה 1: כל התשובות «לא» ⇒ ⛔ אין טבלת השלכות.

---

## 11 · מדדי סגירה — ⛔ בלי אחד מהם הגל ⛔ אינו נסגר

1. `npm run test:registry` **אדום** בשלב 2, עם **6** נפילות ב-§13.1 ו-**1** ב-§13.2 — פלט מודבק.
2. `npm run test:registry` **ירוק** בשלב 4: **38/38** — פלט מודבק.
3. `npm run verify` ירוק במלואו — פלט מודבק.
4. `wc -c docs/STATE.md` < 16,000, עם ההפרש מול 15,671.
5. 0 שורות `B-` נמחקו: `grep -c "^| B-" docs/BACKLOG.md` ≥ 258.
6. `git push` שהפלט שלו מכיל `..HEAD -> main` (§5.4).
7. **`אומת-מקור`**: אחרי ה-push — `git show origin/main:docs/BACKLOG.md` ומדידה
   עצמאית חוזרת של `F1` (צפוי **0**) ורוחב העמודות (צפוי **0**), ⛔ **לא** מהעץ המקומי.

---

**⏸️ awaiting approval — §9 שלב 2. עצרתי.**
