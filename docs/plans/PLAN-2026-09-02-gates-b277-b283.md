# W-GATES · שני שערים שקרנים — `B-277` + `B-283`

**רמה: T3 · תשובות: לא / לא / לא / כן / כן**
(הפיכות=לא · אמון=לא · אבטחה=לא · **רוחב=כן** — 3 קבצי קוד · **ודאות=כן** — ההנחות נמדדו מחדש היום).
⛔ שאלות 1·3 = "לא" ⇒ ⛔ אין טריגר-יחיד; `T3` מגיע מ-2+ תשובות "כן".

---

## 0 · 🔴 מדידה שהפריכה את הפרומפט — ואת `B-283` שאני עצמי כתבתי

**הכלל של ניב הופעל על הממצא של Code, ⛔ לא רק על מספרי הפרומפט.**
`B-283` מצהיר: «חבר ברשימת שורש ש⛔ אינו קיים כפריט **מדולג בשקט**, ו-§8.1 סופר אותו»
ו«⛔ **לא נתפס ע"י שער** — ב-`grep` ידני». **שחזרתי את המצב המדויק ומדדתי — זה שגוי.**

שחזור: הסרתי את שורת `B-282` מ-`BACKLOG` (השארתי אותו ברשימת החברים של `R-6`) והרצתי:

```
8 · שכבת השורשים והמצבה
❌ 8.1  6 שורשים · 158 שיוכים
      שיוך לפריט שאינו מוגדר: R-6→B-282
✅ 8.2  6 שורשים מוגדרים · כל תג ‹R-n› מפנה לשורש קיים
✅ 8.3  שיוך דו-כיווני — רשימת השורש ותגי הפריטים מסכימים
❌ 8.6  הפרוזה של שכבת השורשים נושאת את המספרים המדודים (שיוכים=158)
      "סה״כ פריטים" — הפרוזה אומרת 283, המדידה 282
```
(הקובץ הוחזר מגיבוי מיד; `git status` נקי.)

| טענה ב-`B-283` | פסק |
|---|---|
| §8.3 «דו-כיוונית» **ירוקה** על מזהה-רפאים · `backlogIds.has(b) &&` הוא הסיבה | ✅ **אושש** |
| §8.1 **סופרת** את הרפאים במספר המודפס (`158` במקום `157`) | ✅ **אושש** |
| הרפאים «**מדולג בשקט**» · «⛔ לא נתפס ע"י שער» | 🔴 **הופרך** — §8.1 **אדומה** ונוקבת בשם: `R-6→B-282` |

⚠️ **המשמעות לגל:** הכיסוי ⛔ לא היה חסר — **הייחוס** היה שגוי. תפסתי את זה ב-`grep`
ידני **ו**השער תפס במקביל, ואני זקפתי את הקרדיט לידני. ⇒ `B-283` יורד מ-🔴 לפגם
**שם-וספירה**, ⛔ ואינו «חור בכיסוי». **התיקון עדיין נדרש** — שער ששמו מצהיר
«דו-כיווני» ומדלג על מחלקה שלמה הוא `R-4` גם כשמישהו אחר מכסה אותו במקרה; כיסוי
במקרה ⛔ אינו כיסוי. אבל הוא נכתב כ**אמת מדודה**, ⛔ לא כדרמה.

---

## 1 · P1 · `B-277` — חלוקה שלמה שמזיזה כל סף בשעה

**המנגנון אושש במקור ובחשבון, ובשני אתרים ⛔ ולא אחד:**

| אתר | שורה |
|---|---|
| `.github/workflows/watchdog.yml:87` | `age_h=$(( (now_epoch - upd_epoch) / 3600 ))` → `:88` `[ "$age_h" -gt "$max_h" ]` |
| `.github/workflows/fleet-daily.yml:256` | `age_h=$(( ( $(date -u +%s) - upd_epoch ) / 3600 ))` → `:257` `[ "$age_h" -gt "$MAX_H" ]` |

`$(( ))` קוטמת כלפי אפס. `-gt` על מספר קטום ⇒ השער יורה **רק** כש-`now-upd ≥ (max_h+1)·3600`.
⇒ **הסף האפקטיבי הוא `max_h + 1` שעות בדיוק, בכל 12 השורות בטבלה.** `3` מתנהג כ-`4`.

**התיקון — השוואה בשניות, ⛔ בלי לגעת באף סף:**
```bash
age_s=$(( now_epoch - upd_epoch ))
if [ "$age_s" -gt "$(( max_h * 3600 ))" ]; then
```
`age_h` נשאר מחושב **לתצוגה בלבד** (המחרוזות ב-`:89` · `:259` · `:262` נשארות
byte-identical). ⛔ **אף ערך ב-`MAXAGE` ⛔ אינו זז**, ⛔ אין סף חדש, ⛔ אין ערך שנמחק.

### 1.1 · ⚠️ המדידה החוזרת שניב דרש — **הסף הקיים כבר נכשל היום**

חלון: `27.08 04:05:05Z` (הריצה המוצלחת הראשונה במשטר המידרדר) → `02.09 20:05:03Z`.
**`160.00` שעות · `33` ריצות `success` · `33` פערים.**

| מדד | ערך |
|---|---|
| חציון פער | **`4.50`ש'** |
| פער מרבי | **`11.81`ש'** |
| ממוצע | `4.85`ש' |
| **סף מוצהר `3`ש'** | חורג **`68.88/160.00` שעות = `43.1%`** מזמן-הקיר · **`23/33`** פערים |
| **סף אפקטיבי `4`ש'** | חורג **`48.27/160.00` שעות = `30.2%`** מזמן-הקיר · **`18/33`** פערים |
| **הפרש שהקטימה מסתירה** | **`12.9` נקודות אחוז** |

🔴 **הפסק, כפי שניב הורה לומר ו⛔ לא לתקן:** הסף `3`ש' ⛔ אינו סף שיתחיל להיכשל
אחרי התיקון — הוא **נכשל כבר עכשיו על `43.1%` מזמן-הקיר**. ⇒ התיקון ⛔ **אינו יוצר
התראה חדשה — הוא מרחיב התראה שכבר יורה, ב-`12.9` נ"א.** ⛔ **הסף ⛔ אינו משתנה בגל הזה.**

⚠️ **ומספרי 29.08 שלי מוחלפים:** מדדתי אז `60.6%` / `48.4%` / `12.2` נ"א על חלון
`65.5`ש'. החלון היום ארוך פי 2.4 והשלושה ימים האחרונים טובים יותר ⇒ **הערכים לעיל
הם הקובעים, והישנים יורדים לשושלת ב-`METRICS`.**

⚠️ **ותצפית שאינה הגל הזה:** קצב הסנטינל במשטר = **`4.95` ריצות/יום מתוך `48` מתוזמנות
= `10.3%`**. `M-010` רשום `5.6%`. ⇒ `M-010` **⏳ ראוי למדידה חוזרת**, ⛔ אבל `B-275`
⛔ אינו נפתח כאן ו⛔ אין שינוי מדיניות-תדר.

### 1.2 · מה קורה **עכשיו** לכל 12 השורות (⇒ כמה התיקון מזיז ברגע זה)

נמדד `02.09 20:05Z`: `sentinel 1.11h/3` · `daily-digest 11.63/30` · `fleet-daily 9.77/30` ·
`user-analytics 8.53/30` · `smoke 11.75/30` · `data-guardian 34.34/80` · `fleet-weekly 79.58/192` ·
`analyst 80.81/192` · `arch-auditor 81.75/192` · `backup 83.20/192` · `restore-drill 35.12/840` ·
`watchdog 7.62/30`.
⇒ **`0/12` ברצועה המהפכת-הכרעה ברגע זה** — הפסק היום זהה לפני ואחרי. ⚠️ **וזו בדיוק
הסיבה שאסור להסתמך על צילום-רגע:** על פני `160`ש' הרצועה הייתה חיה `12.9%` מהזמן.

---

## 2 · P2 · `B-283` — §8.3 · ושני תיקונים מובחנים

**`scripts/registry-test.mjs`, שני שינויים שאין לערבב:**

**2a · §8.3 (`:318`) — הסרת `backlogIds.has(b) &&`.** חבר שאינו ב-`BACKLOG` יפול
בכיוון «חבר ⛔ אינו נושא את התג». ⚠️ **ואומרים בגלוי מה זה נותן:** אות **שני** על אותו
מצב שכבר תופסת §8.1 — ⛔ **אפס כיסוי חדש**. הערך הוא ששמה של §8.3 יחדל לשקר.
⇒ נכתב בהערה בקוד: «⛔ אינו מוסיף כיסוי — §8.1 תופסת ממילא; מסיר הצהרה כוזבת».

**2b · §8.1 (`:298-299`) — המספר המודפס מנפח.** `roots.reduce(... members.length)` סופר
**תגים כולל רפאים** (`158` בשחזור מול `157` אמיתיים), ו-§8.6 משווה את הפרוזה **למספר
המנופח הזה**. 🔴 **זה הפגם היחיד כאן שאינו מכוסה בשום מקום אחר.** התיקון: §8.1 תדפיס
ותייצא **שיוכים קיימים בלבד**, והרפאים יימנו בנפרד בהודעת הכשל.
⇒ **המספר ש-§8.6 שומרת עליו יהפוך לספירת שיוכים, ⛔ לא ספירת תגים** — בדיוק מה
ש-`B-283` דרש בשורת «התיקון המוצע».

⚠️ **הסכמת מונה↔שער (דרישת ניב):** אחרי 2b, במצב תקין `§8.1 = §8.6 = 158`; במצב רפאים
`§8.1` מדפיסה `157` ⛔ ואינה מנפחת את המכנה של §8.6. ⇒ המונה והשער מסכימים **בשני** המצבים.

---

## 3 · P3 · אדום **לפני** ירוק — ⛔ צפייה שלא רצה ⛔ אינה צפייה

| # | אסרציה | איך נצפית אדומה |
|---|---|---|
| 1 | `B-277` · `watchdog` — פער `3h27m` נחשב חורג מסף `3` | סקריפט-בדיקה מבודד מריץ את **בלוק ה-bash עצמו** על `upd_epoch` סינתטי (`now-12420s`). **לפני:** `age_h=3` ⇒ 🟢 שקרי. **אחרי:** `age_s=12420 > 10800` ⇒ 🟠 |
| 2 | `B-277` · **גבול** — `2h59m` נשאר ירוק · `3h00m01s` אדום | אותו רתם. שומר שהסף ⛔ **לא** הוחמר בטעות |
| 3 | `B-277` · `fleet-daily` — אותו זוג על `MAX_H=30` (`30h30m`) | אתר שני, ⛔ לא הנחה שהוא זהה |
| 4 | `B-283` · §8.3 אדומה על חבר-רפאים | שחזור §0 — הסרת שורה, הרצת `test:registry`, פלט מודבק |
| 5 | `B-283` · §8.1 מדפיסה `157` ⛔ ולא `158` באותו מצב | אותו שחזור |

⚠️ **הפלט המלא של כל חמשת האדומים מודבק בדיווח לפני שהתיקון נוחת** (§2 · בסיס קפוא).
⚠️ **ואומרים במפורש:** אסרציות 1–3 ⛔ **אינן** בשרשרת `verify` — ⛔ אין ב-`package.json`
חוליה שקוראת `.github/workflows/`. ⇒ הן רתם חד-פעמי בדיווח, ⛔ **לא הגנה מתמשכת.**
🔴 **וזה עצמו פריט `B-` חדש** (`R-4`: אין שער על ה-workflows), ⛔ ולא נסגר כאן.

---

## 4 · P4 · רולבק · ומה `verify` ⛔ אינו מוכיח

**רולבק:** 3 קבצים · ⛔ אין סכימה · ⛔ אין מיגרציה · ⛔ אין סוד · ⛔ אין כתיבת DB.
`git revert` לקומיט אחד מחזיר במדויק. **הפיכות: מלאה, שניות.**

**מה `verify` **כן** מוכיח:** `test:registry` מוכיח את §8.3 ואת §8.1 — הוא **מריץ** אותן
על הקבצים האמיתיים. זה אמיתי ומלא.

🔴 **מה `verify` ⛔ אינו מוכיח — ⛔ כלום מ-`B-277`:**
⛔ **אפס** מ-25 החוליות נוגע ב-`.github/workflows/`. `test:syntax` פורס סוכני CI ב-`scripts/`
וב-`api/` — ⛔ **לא YAML של workflows**. ⇒ תיקון `age_s` ⛔ **אינו מוגן ברגרסיה** אחרי הגל.
⛔ **וגם ריצת ה-workflow עצמה ⛔ אינה ראיה נצפית:** `gh run view --log` מהדהד את **מקור**
הסקריפט; הפסק המחושב יוצא ל-Discord ⛔ ולא ללוג. ⇒ ההוכחה היא **הרתם המבודד** (P3 1–3)
+ החשבון הידני, ⛔ ולא «הריצה הבאה תהיה ירוקה».

⇒ **אימות `T3` (§15):** ⛔ אין מסך מושפע ⇒ **`אומת-מקור`** — קריאת שלושת הקבצים
מ-`origin` אחרי ה-push (⛔ לא מהעץ המקומי) + הרצת `test:registry` על התוצר שנמשך.

---

## 5 · קבצים

| קובץ | שינוי |
|------|--------|
| `.github/workflows/watchdog.yml` | `:87-88` — השוואה בשניות · `age_h` לתצוגה בלבד. ⛔ `MAXAGE` ⛔ ללא נגיעה |
| `.github/workflows/fleet-daily.yml` | `:256-257` — אותו תיקון · `MAX_H` ⛔ ללא נגיעה |
| `scripts/registry-test.mjs` | §8.3 `:318` הסרת הפילטר · §8.1 `:298-299` ספירת שיוכים קיימים |
| `docs/BACKLOG.md` | `B-277`·`B-283` → `נסגר` · **גוף `B-283` נכתב מחדש לאמת המדודה (§0)** · 🆕 `B-284` |
| `docs/DONE.md` | ⛔ **לא בקומיט הזה** — קומיט אינו נושא את ה-hash של עצמו (§14) ⇒ שורה בקומיט הבא. `D-063` שמור ל-`W-CAP` |
| `docs/STATE.md` | שורת הגל · **תקציב** |
| `docs/METRICS.md` | `M-010` ⏳ · מדד פערי הסנטינל — ערכי 29.08 → שושלת |
| `docs/audits/AUDIT-refutations-2026-08.md` | 🔴 תיקון שורה 11: «⛔ לא נתפס ע"י שער» **הופרך** |

## 6 · מרשם (§10.1 · §14)

- 🆕 `B-284` ‹R-4› — ⛔ **אין שער על `.github/workflows/`**. `B-277` חי בשני אתרים
  ו-25 חוליות `verify` ⛔ לא יכלו לראות אף אחד. ⛔ **לא נסגר בגל הזה.**
- `B-283` — הגוף **נכתב מחדש**: §8.3 אושש · «מדולג בשקט» **הופרך** · הפגם הנותר הוא
  **ספירת תגים במקום שיוכים**. ⚠️ **הפחתת דרמה על בסיס מדידה ⛔ אינה ריכוך `R-4`** —
  ריכוך הוא הזזת **סף כדי לעבור**; זו **תיקון עובדה**.
- `AUDIT-refutations` §7.3 שורה 11 — מתוקנת באותו קומיט. ⚠️ **ההפרכה של `B-283` היא
  הפרכה מספר `22` של אוגוסט, והיא `R-3` — ⛔ אימות שנכתב ולא נראה.** ⇒ `18/22 = 81.8%`.
- `M-010` (`5.6%`) — ⏳ **סומן לפג-תוקף**, נמדד `10.3%` (`4.95/48` ריצות/יום, `160`ש').
  ⛔ **`B-275` ⛔ אינו נפתח, ⛔ אין שינוי תדר.**
- ⚠️ **`STATE` ב-`15,937/16,000`** ⇒ `63` בתים פנויים. **מקום ייווצר בגיזום עותקים בלבד**,
  ⛔ **לא במחיקת תוכן ו⛔ לא בהעלאת תקרה.** אם ⛔ אין מקום בגיזום עותקים — **עוצרים
  ומדווחים**, ⛔ לא דוחסים.

## 7 · ⛔ מה הגל הזה ⛔ אינו נוגע בו

⛔ אף ערך סף · ⛔ `W-CAP` · ⛔ `C-036` · ⛔ קוד מוצר (`SwingEdge_App.jsx` · `src/` · `api/`) ·
⛔ `B-185` · ⛔ `B-186` · ⛔ דמו · ⛔ כתיבת DB · ⛔ תקרת `STATE` · ⛔ `B-275`/מדיניות תדר.

## 8 · אימות

`npm run verify` — 25 חוליות + `build`, פלט מלא מודבק. `test:registry` לפני (§8.3+§8.1
נצפות אדומות בשחזור) ואחרי. הרתם המבודד ל-`B-277` — פלט אדום ואז ירוק, מודבק.
ואז **`אומת-מקור`** לפי P4. ⛔ **אין סגירה בלי שלושת הפלטים.**

---

## 9 · הצעד הראשון והיחיד אחרי אישור (§9 שלב 2)

כתיבת התוכנית הזו ל-`docs/plans/PLAN-2026-09-02-gates-b277-b283.md`, קומיט **נפרד**
`docs(plan): B-277 + B-283 — שערים שמדווחים ירוק בלי למדוד — awaiting approval`,
push. ⛔ אפס נגיעה בקוד · ⛔ אפס workflow · ⛔ אפס `npm` בשלב הזה.
ואז **שלב 3** — משיכה מ-`origin`, קריאת הקובץ **שנדחף** (⛔ לא מהזיכרון), ורק אז שלב 4.

---

## 10 · תוספת האישור — ניב, `02.09` (⛔ נדחפה באותו קומיט של התוכנית)

**אושר.** ניב אימת מ-`origin` את ארבעת האתרים ואישר את ההפרכה העצמית של `B-283`.
🔴 **הפרכה `22` של אוגוסט, ואותו כשל שהדוח מנה `17` פעמים — `R-3`.**

**R1 · חמש האסרציות נצפות אדומות עם פלט מודבק.** ⚠️ ו-1–3 הן רתם חד-פעמי
⛔ **ולא בשרשרת** ⇒ **נאמר בסגירה במפורש, ⛔ בלי ריכוך.** `B-284` ‹R-4› **נרשם ⛔ ולא נסגר.**

**R2 · תקציב `STATE` = `63` בתים.** גיזום **עותקים בלבד**. ⛔ אין מקום ⇒ **עצור ודווח.**
⛔ אל תעלה תקרה · ⛔ אל תמחק תוכן.

**⚠️ הנחיה על הורדת החומרה של `B-283`:** «⛔ אל תרכך את הורדת החומרה — רשום במפורש
שהיא **ירידה, ⛔ לא ביטול**.» ⇒ מה שנשאר **נכון** נשמר: §8.1 סופרת **תגים ולא שיוכים**.

**⚠️ הנחיה על 29.08:** המספרים הישנים (`60.6%` / `48.4%` / `12.2` נ"א) **מוחלפים**
⛔ **ולא נטענים מעליהם** ⇒ נרשמים ב**שושלת** של שורת ה-`M-` ב-`METRICS`.

### 10.1 · 🆕 משימת מדידה א' — מספרי שורות שנסחפו · ⛔ מדידה בלבד

מספרי השורות שבגוף `B-245` · `B-246` · `B-254` **נסחפו**. `B-246` מצטט `:4339` שהוא
היום `<textarea>` (`Δ+77`). ⇒ **מדוד מחדש ועדכן בגוף הפריטים.**
⛔ **אל תשנה סטטוס · ⛔ אל תתקן את הממצא · ⛔ אל תיגע בקוד.**
⚠️ **תנאי מקדים להדגמה** — מספר שגוי יפיל אותה שוב. זה בדיוק `R-6`: מספר שורה הוא
עותק שני של מיקום, ובעל-הבית שלו הוא הקובץ ⛔ ולא הפריט.

### 10.2 · 🆕 משימת מדידה ב' — המוצר זז · ⛔ מדידה, ⛔ ללא פרשנות

ניב מסר מ-Admin Console `02.09`: `57` משתמשים · `73` עסקאות · `7` פעילים (30D) ·
`7` עסקאות השבוע (מול `51`/`66`/`6`/`1` ב-29.08).
⇒ **נמדד עצמאית מול ה-DB ונרשם ב-`METRICS`** (⛔ כלל «⛔ אל תסמוך על מספר מהפרומפט»
חל גם על מספר של ניב). ⛔ **אל תפרש** — ⛔ אין מסקנת מוצר, ⛔ אין המלצה, ⛔ אין שינוי `NEXT`.
⚠️ המשמעות היחידה שנרשמת היא ש«`0` עסקאות ב-`48`ש'» היה **חלון**, ⛔ לא מצב קבוע —
וזו בדיוק הסיבה ש-`METRICS` קיים: מספר נמדד שוב, ⛔ לא נקרא מחדש.
