# PLAN — `B-186` · אחוז P&L חי ב-SHORT מחלק במחיר הנוכחי

**תאריך:** 2026-09-04 · **גל:** `W1b` · **מצב:** ⏸️ awaiting approval
**רמה: `T3`** · תשובות `1..5`: **לא · כן · לא · כן · כן**
— ⓶ משתמש מקבל אחוז שגוי בלי לדעת · ⓸ מסלול שצרכנים רבים תלויים בו · ⓹ מספרי
השורה בתיעוד נמדדו 27.08. ⛔ **שלב 1 בלבד: מדידה + תוכנית · 0 שינויי קוד מוצר.**

> ⚠️ **מסמך זה נכתב מהמדוד, ⛔ לא מההשערות.** כל מספר בו נושא את הפקודה שהפיקה
> אותו. שלוש טענות שעמדו בבסיס הפריט **הופרכו** — §2 · §4 · §7.1.

---

## §0 · שער בטיחות

```
git pull --ff-only && git status --porcelain
Already up to date.        (⛔ אפס פלט מ-porcelain — עץ נקי)
git rev-parse --short HEAD ⇒ 5f30280        (צפוי 5f30280 ✓)
```

---

## §1 · A — האתר אותר **לפי תוכן**, ⛔ לא לפי מספר שורה

```
$ grep -rn "entry *\/ *currentPrice" --include="*.jsx" --include="*.js" . | grep -v node_modules
SwingEdge_App.jsx:4581

$ grep -rn "livePnlPct" --include="*.jsx" --include="*.js" . | grep -v node_modules
SwingEdge_App.jsx:4580   ← ההגדרה
SwingEdge_App.jsx:4623   ← שער רינדור  livePnlPct !== null
SwingEdge_App.jsx:4626   ← הצרכן היחיד  Math.min(Math.abs(livePnlPct) * 10, 100)%
```

| | ערך |
|---|---|
| **מספר שורה מאומת** | `SwingEdge_App.jsx:4580-4581` |
| **הרשום ב-`BACKLOG` `B-186`** | `SwingEdge_App.jsx:4503` |
| **דלתא** | **`+78` שורות** — הקובץ זז מאז 26.08 |
| **מספר האתרים** | **`1/1`** — ⛔ אין אתר שני |

**חיפוש אתר שני — שלוש תבניות עצמאיות, כולן שליליות:**

```
$ grep -rn "\- 1) \* 100\|-1) \* 100" --include="*.jsx" --include="*.js" . | grep -v node_modules
SwingEdge_App.jsx:4581                          ← האתר עצמו
src/intelligence/core/MarketRegime.js:67        ← pctChange על מדד שוק, ⛔ אין side
$ grep -rn "/ *tr\.entry\|/ *trade\.entry" …
SwingEdge_App.jsx:4581
src/intelligence/core/DecisionCoach.js:65       ← מרחק סטופ, |entry−stop|/entry — תקין
$ grep -n "currentPrice\|getLivePrice" src/components/MobileTradeCard.jsx
(⛔ אפס התאמות — מסלול המובייל ⛔ אינו מחשב אחוז חי כלל)
```

⇒ **אתר יחיד.** ⛔ אין שני אתרים ⇒ ⛔ אין «תיקון של אחד בשקט».

---

## §2 · B — 🔴 ההפרכה **אוששה**: מדד הסגירה ב-`NEXT` ⛔ אינו יכול לירות

**הטענה שנבדקה:** «`livePnlPct` ⛔ אינו מרונדר כמספר; צרכנו היחיד הוא רוחב פס».

### 2.1 · אוששה — במדידה, ⛔ לא בקריאה

`grep` על `livePnlPct` מחזיר **3** אתרי צריכה, וכולם נספרו: `:4580` הגדרה ·
`:4623` שער `!== null` · `:4626` רוחב הפס. ⛔ **אף אחד ⛔ אינו מדפיס את הערך.**
המספר שכן מרונדר ליד הפס הוא `livePnl` (**דולרים**, `:4621`) — והוא מחושב
בשורה `:4576` בנוסחה **התקינה** `(tr.entry − currentPrice) × tr.shares`.

⇒ 🔴 **הפרכה נלווית — הנמקת `NEXT:16» «ומספר כסף על המסך» ⛔ אינה מדויקת:**
מספר הכסף **תקין**. מה ש-`B-186` שובר הוא **פיקסלים של רוחב פס בלבד**.

### 2.2 · מדד הסגירה הנוכחי — למה ⛔ אינו יכול לירות

> `docs/NEXT.md:19` — «**מדד סגירה:** עסקת SHORT פתוחה — האחוז מחושב ידנית
> ומושווה למסך (⛔ לא "נראה סביר"); בייסליין קפוא נצפה אדום לפני התיקון.»

⛔ **אין אחוז על המסך שאפשר להשוות אליו.** «מחושב ידנית ומושווה למסך» מניח
צומת טקסט שאינו קיים ⇒ המדד **ירוק בהכרח**, בדיוק ‹R-4› (שומר שמודד נוכחות
ולא אמת). ⛔ **ולא שכתבתי אותו ו⛔ לא ריככתי אותו** — שתי חלופות ל§8, **הכרעת ניב**.

### 2.3 · הדלתא ה**נראית** — כומתה, ⛔ לא הונחה

הפס: `width = min(|pct| × 10, 100)%`. `E=100`, מדוד ב-`node`:

| `C` | מצב | buggy % | fixed % | פס buggy | פס fixed | **דלתא נראית** |
|---|---|---|---|---|---|---|
| `95` | SHORT ברווח | `5.26` | `5.00` | `52.63%` | `50.00%` | **`+2.63` נ״א** |
| `92` | SHORT ברווח | `8.70` | `8.00` | `86.96%` | `80.00%` | **`+6.96` נ״א** |
| `110` | SHORT בהפסד | `−9.09` | `−10.00` | `90.91%` | `100.00%` | **`−9.09` נ״א** ← מרבי |
| **`125`** | **הפסד `>±10%`** | `−20.00` | `−25.00` | `100.00%` | `100.00%` | **`0.00`** ⇐ **החיתוך משווה** |
| **`75`** | **רווח `>±10%`** | `33.33` | `25.00` | `100.00%` | `100.00%` | **`0.00`** ⇐ **החיתוך משווה** |

**סריקה `C∈[50,200]` בצעד `0.01`:** דלתא מרבית **`9.09` נ״א** ב-`C=110`.

🔴 **וזו הפרכה שלישית — `NEXT:16` «האחוז שגוי תמיד, ⛔ לא בקצה»:**
נכון על ה**ערך**; ⛔ **שגוי על הפיקסל.** מחוץ לרצועה `|pct| ≲ 11%` שני הפסים
**חתוכים ל-`100%`** ⇒ הדלתא הנראית היא **בדיוק `0`**. ⇒ הבאג **תמידי בערך
ונראה ברצועה בלבד** — ו⛔ אין לתאר אותו כשגוי-תמיד-על-המסך.

---

## §3 · C — 🔴 האסרציה נצפתה **אדומה בפועל** על העץ שלפני התיקון

⚠️ **⛔ לא נוצרה חוליה חדשה בשרשרת בשלב הזה, ו⛔ לא נוסף קובץ לריפו.**
הרתם פותח כ-`/tmp/short-pnl-pct-test.mjs` והורץ **מול הבייטים של הריפו**
(`--app /Users/nivhareven/Swing-edge/SwingEdge_App.jsx`). **מקורו המלא ב-§8.2**,
וב-שלב 2 הוא נכנס כ-`scripts/short-pnl-pct-test.mjs` + `test:shortpct`.

**שיטה — חילוץ לפי עוגן, תקדים `test:hydration`:** 61 `import` ברמה העליונה
חוסמים ייבוא ל-Node ⇒ ההצהרה מחולצת מהקובץ ומורצת ב-`new Function` עם
`currentPrice`/`tr` מוזרקים. ⛔ **כשל חילוץ = אדום קשה** (`M1`…`M4`, דוקטרינת
`B-272`) ⛔ **ולעולם לא דילוג.**

### 3.1 · הפלט האדום כלשונו — על `5f30280`

```
$ node /tmp/short-pnl-pct-test.mjs --app /Users/nivhareven/Swing-edge/SwingEdge_App.jsx
M1 עוגן `const livePnlPct =` מופיע בדיוק פעם אחת: 1 ✓
M2 צרכן הפס `Math.abs(livePnlPct)` מופיע בדיוק פעם אחת: 1 ✓
M3 סוגריים מאוזנים בהצהרה שחולצה: מאוזן ✓
M4 ההצהרה מסתיימת ב-`: null;`: כן ✓

— נוסחה שחולצה מ-SwingEdge_App.jsx —
const livePnlPct = currentPrice && tr.entry
                      ? (tr.side === "LONG" ? ((currentPrice / tr.entry) - 1) * 100 : ((tr.entry / currentPrice) - 1) * 100)
                      : null;

A1 SHORT ברווח (E=100·C=95) ⇒ +5.000%: 5.2632% ✗ RED
A2 SHORT בהפסד (E=100·C=105) ⇒ −5.000%: -4.7619% ✗ RED
A3 SHORT ברווח (E=50·C=40) ⇒ +20.000%: 25.0000% ✗ RED
A4 LONG ברווח (E=100·C=110) ⇒ +10.000%: 10.0000% ✓
A5 LONG בהפסד (E=100·C=90) ⇒ −10.000%: -10.0000% ✓
A6 סימטריה E=100·C=95: LONG === −SHORT: LONG=-5.0000 · SHORT=5.2632 ✗ RED
A7 האחוז עקבי עם ה-$ (E=100·C=105·10 מניות ⇒ −50$ על 1000$): -4.7619% מול -5.0000% ✗ RED
A8 entry = 0 ⇒ null: null ✓
A9 entry = null ⇒ null: null ✓
A10 currentPrice = null ⇒ null: null ✓
A11 currentPrice = 0 ⇒ null (⛔ ולא Infinity): null ✓
B1 רוחב פס SHORT E=100·C=95 ⇒ 50.00%: 52.63% ✗ RED
B2 רוחב פס SHORT E=100·C=110 ⇒ 100.00% (חיתוך): 90.91% ✗ RED
B3 רוחב פס SHORT E=100·C=92 ⇒ 80.00%: 86.96% ✗ RED

10 עברו · 8 נכשלו — אדומות: A1, A2, A3, A6, A7, B1, B2, B3
EXIT=1
```

**`8/18` אדומות · `10/18` ירוקות.** ⛔ **הפריט ⛔ לא הופרך** — האסרציה ⛔ לא
יצאה ירוקה לפני התיקון.

### 3.2 · והיא **יכולה** להיות ירוקה — ⛔ לא אסרציה בלתי-ניתנת-להפרכה

התיקון הוחל על **עותק** (`/tmp/App-fixed.jsx`, ⛔ הריפו ⛔ לא נגע):

```
$ node /tmp/short-pnl-pct-test.mjs --app /tmp/App-fixed.jsx
… ? (tr.side === "LONG" ? ((currentPrice / tr.entry) - 1) * 100 : ((tr.entry - currentPrice) / tr.entry) * 100)
A1 … 5.0000% ✓   A2 … -5.0000% ✓   A3 … 20.0000% ✓   A6 … LONG=-5.0000 · SHORT=5.0000 ✓
A7 … -5.0000% מול -5.0000% ✓   B1 … 50.00% ✓   B2 … 100.00% ✓   B3 … 80.00% ✓
18 עברו · 0 נכשלו
EXIT=0
```

⇒ **אדום ← ירוק, שניהם נצפו.** `A4`/`A5` (LONG) ירוקות **בשני** העצים —
⚠️ ⇒ הן **אינווריאנטות אי-רגרסיה** ו⛔ **אינן ראיה לתיקון**; קריאתן כראיה היא
בדיוק עיוורון אסרציה 12 ב-`test:analytics`.

---

## §4 · D — 🔴 מדוד בפרודקשן: **`0/40` עסקאות SHORT פתוחות**

Supabase MCP **קריאה בלבד**, פרויקט `zicstkfkwhzvmdkzpidm`, 04.09. ⛔ אפס כתיבה.

**ראשית — האם המכנה יכול בכלל להכיל את הנספר (CLAUDE.md §2)?**

```sql
select coalesce(status,'<null>') status, coalesce(side,'<null>') side,
       count(*) n, count(*) filter (where coalesce(is_demo,false)=false) real_n
from public.trades group by 1,2 order by n desc;
```
| status | side | n | real_n |
|---|---|---|---|
| `OPEN` | `LONG` | 40 | 40 |
| `CLOSED` | `LONG` | 34 | 34 |
| `CLOSED` | `SHORT` | **1** | 1 |

⇒ **כן.** `side` ∈ {`LONG`,`SHORT`} ו-`status` ∈ {`OPEN`,`CLOSED`} — אוצר-מילים
סגור, ו-`SHORT` **קיים** בטבלה. ⇒ `0` הוא **היעדר מדוד**, ⛔ לא מכנה עיוור.

```sql
select count(*) open_all,
       count(*) filter (where side='SHORT') open_short, …
from public.trades where status='OPEN';
```
⇒ `open_all=40` · **`open_short=0`** · `open_short_real=0` · `users_short_real=0`.

| מדד | ערך · מכנה · אוכלוסייה |
|---|---|
| SHORT פתוחות | **`0/40` = `0%`** מהעסקאות ב-`status='OPEN'` |
| משתמשים מושפעים **היום** | **`0`** |
| SHORT בכל הטבלה | **`1/75` = `1.3%`** — ו**סגורה** ⇒ ⛔ אינה מרנדרת פס |

🔴 **⇒ הפרכה של הנמקת הדחיפות ב-`NEXT:16`:** «מספר כסף על המסך» מתאר
**`0` פוזיציות היום**. ⛔ **הבאג ⛔ לא בוטל** — הקוד שגוי, והוא יירה ברגע
שמישהו יפתח SHORT. אבל ⛔ **אין כאן משתמש שרואה עכשיו מספר שגוי.**

⚠️ **הרצועה `|pct| < 11%` ⛔ לא נמדדה ו⛔ אינה ניתנת למדידה:** האחוז נגזר
ממחיר **חי** שמגיע מהספק בזמן רינדור ו⛔ אינו נשמר ב-DB (⛔ אין עמודת מחיר
נוכחי — נקרא מ-`information_schema`). עם `0` פוזיציות פתוחות השאלה **מנוונת**.

⚠️ **המספר נרשם כ-`M-018` עם `תוקף`** — «`0` SHORT פתוחות» הוא **חלון**, ⛔ לא
מצב; זה בדיוק הלקח מ-`M-011` («0 עסקאות» שהיה חלון שקט ⛔ ולא מוצר מת).

---

## §5 · E — רולבק · ומה `verify` **⛔ אינו** מוכיח

**רולבק:** `git revert <hash>` — קומיט **אחד**, ביטוי אחד בשורה אחת + קובץ טסט
חדש. ⛔ אפס מיגרציה · ⛔ אפס נתון שמור · ⛔ אפס סוד · ⛔ אפס שינוי `workflow`.
**זמן חזרה = deploy אחד של Vercel.** ⚠️ **הפיכות מלאה:** הגזירה רצה בזמן קריאה
ו⛔ לעולם אינה נשמרת ⇒ ⛔ אין שארית ב-DB לנקות.

**מה `verify` מוכיח על השינוי הזה — ⛔ כמעט כלום:**

| חוליה | מה היא אומרת על `:4581` |
|---|---|
| `build` | ⛔ **קומפילציה בלבד.** `(a−b)/a` ו-`(a/b)−1` שניהם מתקמפלים. |
| `test:hydration` | ⛔ אינה נוגעת — היא מחלצת שלושה `useEffect`, ⛔ לא את `openTrades.map`. |
| `test:instrument` | ⛔ אינה נוגעת — האחוז הוא **יחס בין שני מחירים באותו מטבע** ⇒ חסין-מטבע ו⛔ אינו מומר (ההערה ב-`:4578-4579`). |
| 22 החוליות הנותרות | ⛔ **אפס** קוראות את `SwingEdge_App.jsx` בהקשר הזה. |

🔴 **⇒ עד שתיווצר `test:shortpct`, `verify` ⛔ אינו מוכיח דבר מעבר לכך
שהקובץ מתקמפל.** זו הסיבה ש-§3 היא **תנאי** ולא נלווה.
⛔ **ו-`test:shortpct` עצמה ⛔ אינה מכסה JSX** — שהפס מרונדר · שהרוחב מוחל ·
דפדפן אמיתי · פרודקשן. אותו גבול בדיוק כמו `C-036` ל-`test:hydration`.

---

## §6 · F — תקציב `STATE`

```
לפני:  wc -c docs/STATE.md ⇒ 15986 / 16000   (14 בתים פנויים)
```

**מה נגזם:** `docs/STATE.md:46` — פריט `⏭️` מס' 3 החזיק **ערך מדוד**
(«מתוזמן `48`/יום ומבוצע `2-3`/יום») בתוך מקטע שחוזהו בשורה `:42` הוא
**«כאן מזהה בלבד; ההנמקה … חיה שם ⛔ ולא כאן»**.

🔴 **⇒ עותק שני ‹R-6›, ו⛔ לא היפותטי — הוא כבר נסחף לשלושה ערכים שונים:**
`STATE:46` = `2-3`/יום · `NEXT:28` = `5.6%` · `M-010` (מקור-האמת) = **`33/336
= 9.8%`**, ו-`M-010` עצמו רושם ש-`5.6%` **הופרך**. ⇒ `STATE` ציטט מספר
שהפנקס כבר סימן כמופרך.

**לאן:** ⛔ **לשום מקום — ⛔ לא נמחק תוכן.** הערך חי ב-`METRICS` `M-010` (מקור
אמת) ובשושלת שלו. ⛔ **אף פריט פתוח ⛔ לא הועבר** — `B-275` נשאר `⏭️` מס' 3,
מזהה והנמקה ללא שינוי. ⛔ **לא ל-`ARCHIVE`. ⛔ לא הועלתה תקרה.**

🔴 **וגיזום שני נדרש — מפני שהראשון היה כמעט אפס-בתים, וזה נמדד ⛔ ולא שוער.**
הסרת שני המספרים מ-`:46` שחררה `~20` בתים בלבד, וסמן ה-‹R-6› שמסמן שנגזמו
**עולה יותר ממה שהוא חוסך** ⇒ הניסוח הראשון הביא את הקובץ ל-`16287`, כלומר
**`287` בתים מעל התקרה.** ⛔ **התקרה ⛔ לא הועלתה** (וזה בדיוק היה הפיתוי `R-4`).
⇒ נגזמו **בנוסף**, שניהם ב-`:24`:

1. **סמן-הגיזום של `age_h`** — «וקטימת `age_h` שעמדה כאן נגזמה 03.09 — היא
   `B-277`, ש**נסגר** ב-`D-065`». ⚠️ זהו **סמן על פריט סגור** בתוך מקטע 🔴 **עכשיו**;
   מקור-האמת שלו הוא `DONE`+`D-065`, שם הוא כבר רשום ⇒ ‹R-6›.
2. **`48`/יום** מגוף `B-275` — באותה שורה שמצהירה «הערך והמכנה ב-`M-010` ⛔ ולא
   כאן» ובכל זאת נוקבת בערך. ⇒ הסתירה נפתרה לטובת מה שהשורה עצמה מצהירה.

```
אחרי:  wc -c docs/STATE.md ⇒ 15831 / 16000   (169 בתים פנויים)
דלתא:  −155 בתים · מרווח 14 → 169
```
⚠️ **שורת `HEAD` עודכנה ל-`5f30280`** באותו קומיט (כלל `HEAD`, `STATE:12`) —
⛔ **לא ל-hash של הקומיט הזה**: קומיט ⛔ אינו יכול לשאת את ה-hash של עצמו.

---

## §7 · G — חוב רישום · ⛔ אפס קוד

### 7.1 · `docs/BOOTSTRAP.md` — 🔴 **הנחת הפרומפט הופרכה**

**הטענה:** «`docs/BOOTSTRAP.md` ⛔ אינו קיים בעוד **הוראות הפרויקט מפנות אליו**».

**מדוד 04.09 — החצי הראשון אושש, החצי השני הופרך:**

```
$ ls docs/BOOTSTRAP.md            ⇒ No such file or directory          ← אושש
$ grep -rn "BOOTSTRAP" . --exclude-dir=node_modules --exclude-dir=.git
(⛔ אפס התאמות)                                                        ← הופרך
```

⇒ ⛔ **אף קובץ בריפו ⛔ אינו מפנה ל-`BOOTSTRAP`** — ⛔ לא `CLAUDE.md` · ⛔ לא
`CONTEXT.md` · ⛔ לא `docs/` · ⛔ לא `scripts/` · ⛔ לא `.githooks/`.
ההתאמה **היחידה** במכונה כולה היא **תמליל השיחה הזו עצמה**
(`~/.claude/projects/…/…jsonl`) — כלומר **הפרומפט**, ⛔ לא הריפו.

⇒ ⛔ **אין כאן «מפנה שיישבר»** — ⛔ אין מפנה כלל. ⇒ **`B-288` נרשם על מה
שנמדד** (⛔ אין הפניה · ⛔ אין קובץ ⇒ **הכרעת ניב**: לכתוב, או להצהיר שאינו
נדרש), ⛔ **ולא על הטענה כפי שנוסחה.** ⛔ **הקובץ ⛔ לא נוצר.**

### 7.2 · `METRICS` — המנייה **מפלט השער**, ⛔ לא מהזיכרון

```
$ npm run test:registry
✅ 12.4  1/17 מדדים פגי-תוקף, וכולם מכריזים ⏳ פג
✅ registry: 38/38 assertions passed — 405 מזהים, פריט חי בקובץ אחד.
```

⇒ **`1/17`, ⛔ לא «מדדים» ברבים.** הפג היחיד: **`M-003`** — שימור שבוע 1,
`0/6 = 0%`, נמדד `06.08`, תוקף `14` יום ⇒ **פג מאז 20.08, כ-`15` ימים.**
⇒ **`B-289`** למדידה חוזרת. ⛔ **⛔ לא הוארך תוקף לאף מדד** — הארכה כדי לעבור
היא ‹R-4›, בדיוק כתקרת ה-100 שורות.

### 7.3 · סריקה סביבתית (§11) — ⛔ **לא תוקן**, ⛔ ⛔ ולא נפתח מזהה חדש

`CLAUDE.md` §7 מצהיר «`test:registry` (**36** assertions)»; השער מדד **`38/38`**.
⇒ **`B-257` כבר פתוח על התבנית הזו בדיוק** (`test:write` `25→29` ·
`test:instrument` `252→395`) ⇒ זו **הופעה שלישית**, ⛔ **ולא פריט חדש** —
מזהה שני לאותו פגם הוא ‹R-6› מתוצרת עצמנו. ⛔ **⛔ ולא תוקן כאן:** §11 מתיר
תיקון-בדרך רק כשהוא **קשור למשימה**, ו⛔ אינו. `B-274` נשאר הפריט שסוגר את
המחלקה (שער שגוזר את המספר מ-`package.json`).

---

## §8 · שלב 2 — מה ירוץ **אחרי** אישור. ⛔ ⛔ לא לפניו

### 8.1 · השינוי — ביטוי אחד, שורה אחת

`SwingEdge_App.jsx:4581`, ענף ה-`SHORT` **בלבד**:

```diff
- : ((tr.entry / currentPrice) - 1) * 100)
+ : ((tr.entry - currentPrice) / tr.entry) * 100)
```

⚠️ **המכנה הוא ההון שהושם בסיכון — מחיר הכניסה — והוא ⛔ אינו זז עם השוק.**
⛔ **ענף ה-`LONG` ⛔ אינו נגע.** ⛔ **⛔ ואין `|| 1` ו⛔ אין `?? 1` בשום שער**
(`CLAUDE.md` §7): השומר `currentPrice && tr.entry` נשאר כפי שהוא ⇒ `null`
כשאין מחיר, ⛔ ולא `0` מומצא ‹R-2›.

### 8.2 · החוליה — `test:shortpct`, חוליה **26** בשרשרת

`scripts/short-pnl-pct-test.mjs` (המקור המלא = הרתם שהורץ ב-§3, ללא שינוי) +
`"test:shortpct": "node scripts/short-pnl-pct-test.mjs"` + שרשור ל-`verify`
**לפני** `build`. ⚠️ **ובאותו קומיט בדיוק** — `CLAUDE.md` §7 מחייב שרשימת
החוליות תתעדכן בקומיט שמשנה את השרשרת, וזה הכלל ש-`B-257` מתעד כמופר **פעמיים**.

### 8.3 · 🔴 מדד סגירה — **הוכרע 04.09: חלופה א׳ (`אומת-מקור`)**

🔴 **חלופה ב׳ נפסלה בהכרעת ניב** — היא דורשת פתיחת עסקת SHORT אמיתית, כלומר
**כתיבה ל-DB בפרודקשן**: עסקה מומצאת בג'ורנל אמיתי היא בדיוק מה ש-§8.5 אוסר,
ו-`0/40` SHORT פתוחות (`M-018`) אומר ש⛔ **אין מסך מושפע** להסתכל עליו.

#### 🔴 תיקון 2 — הניסוח הקודם של חלופה א׳ **הופרך במדידה**

הטבלה נשאה: «אימות ש**הבייטים** נושאים `(tr.entry - currentPrice) / tr.entry`».
⛔ **זו הייתה אסרציה שאינה יכולה לירות.** נמדד 04.09 על ה-bundle החי:

```
$ grep -o 'assets/index-[A-Za-z0-9._-]*\.js'   ⇒ assets/index-B6YE6JDv.js
...(x.entry-L)*x.shares:null,J=L&&x.entry?x.side==="LONG"
?(L/x.entry-1)*100:(x.entry/L-1)*100:null...
```

⇒ **המקטין ממפה `tr`→`x` · `currentPrice`→`L` · `livePnlPct`→`J`.**
`grep -c "tr.entry - currentPrice"` על ה-bundle = **`0` היום**, ו-**`0` גם
אחרי התיקון** ⇒ 🔴 **השער היה עובר בשני המצבים** — ‹R-3› בדיוק, אסרציה על
טקסט-המקור במקום על מה שנשלח. ⚠️ **שמות מקומיים ⛔ אינם שורדים מִזעור**, וזו
הכללה של אותו לקח: `test:hydration` מחלץ מ**המקור** ולכן שם מותר לו לסמוך על
שמות; קריאת **תוצר** ⛔ אינה יכולה.

#### האסרציה המתוקנת — **שתי בדיקות צורה**, שתיהן נמדדו

⚠️ **ה-hash של ה-bundle משתנה בכל deploy** ⇒ מחלצים אותו, ⛔ **לא קובעים קשיח**:

```bash
BUNDLE=$(curl -sL https://swing-edge.com/ \
  | grep -o 'assets/index-[A-Za-z0-9._-]*\.js' | head -1)
curl -sL "https://swing-edge.com/$BUNDLE" -o /tmp/se-bundle.js

# בדיקה 1 — הצורה הבאגית.   לפני: 1 · אחרי: 0
grep -oE '\.entry/[A-Za-z_$]{1,3}-1\)\*100' /tmp/se-bundle.js | wc -l
# בדיקה 2 — הצורה המתוקנת.  לפני: 0 · אחרי: 1
grep -oE '\.entry-[A-Za-z_$]{1,3}\)/[A-Za-z_$]{1,3}\.entry\*100' /tmp/se-bundle.js | wc -l
```

| | בדיקה 1 | בדיקה 2 |
|---|---|---|
| **פרודקשן `index-B6YE6JDv.js` · נמדד 04.09** | **`1`** (`.entry/L-1)*100`) | **`0`** |
| **הצורה המתוקנת אחרי מִזעור · נמדד 04.09** | **`0`** | **`1`** |

🔴 **⇒ קו-הבסיס האדום קיים ו⛔ נצפה — ⛔ לא היפותטי**, ובשני הכיוונים.
⚠️ **והשורה השנייה ⛔ אינה השערה על המקטין:** הביטוי המתוקן הועבר דרך
`esbuild --minify` בתוך פונקציה (כדי לכפות שינוי-שם מקומי) והפלט **נצפה**:
`(n.entry-l)/n.entry*100`. ⛔ **בלי הצעד הזה «אחרי: 1» היה ניחוש** — בדיוק
המחלקה של `B-285`, שער שמדווח על מצב שלא מדד.

⚠️ **מה שתי הבדיקות ⛔ אינן מוכיחות:** הן מוכיחות ש**הביטוי הנכון נשלח
לדפדפן** ⛔ **ואינן מוכיחות שהפס מרונדר** — JSX · React · דפדפן אמיתי נשארים
מחוץ, **אותו גבול בדיוק כמו `C-036`** ו-`B-273`. ⛔ אין לקרוא `אומת-מקור`
כאילו הוא אימות עין.

#### מה נכתב ל-`docs/NEXT.md:19` — **בקומיט הביצוע**

⇒ המדד הבלתי-יָריק («האחוז מחושב ידנית ומושווה למסך») **מוחלף** ב:
`test:shortpct` `18/18` (מתוכן `8` **נצפו אדומות** על `5f30280`) **וגם** שתי
בדיקות הצורה על ה-bundle החי, `1→0` ו-`0→1`. ⛔ **וזו הקלה בראיה ⛔ ולא בסף** —
`test:shortpct` ⛔ אינו מרוכך באסרציה אחת.

### 8.4 · סדר הביצוע

1. `git pull --ff-only` · אימות `HEAD`.
2. `scripts/short-pnl-pct-test.mjs` נכנס לריפו · `package.json` (סקריפט + `verify`) · `CLAUDE.md` §7 (רשימת החוליות) — **קומיט אחד**.
3. `npm run test:shortpct` ⇒ **חייב לצאת `8` אדומות** על הקוד שטרם תוקן. ⛔ ירוק כאן = עצירה.
4. `:4581` מתוקן.
5. `npm run test:shortpct` ⇒ `18/18`. ואז `npm run verify` **מלא** — הפלט מודבק בשלמותו, ⛔ לא «עבר».
6. 🆕 **`docs/BACKLOG.md` `B-186` — מספר השורה נמדד מחדש לפי תוכן ומעודכן באותו קומיט.** הרשום שם הוא `:4503` ⛔ ואינו נכון. ⛔ **אין להעתיק מספר משום מסמך — כולל מהתוכנית הזו:** הקובץ יזוז שוב עד אז, וזו בדיוק הסחיפה שהפריט הזה נולד ממנה.
7. `docs/DONE.md` (עמודת `T` = `T3` · סימון **`אומת-מקור`**) · `docs/CHECKS.md` (`C-0nn` — פס SHORT) · `docs/STATE.md` · **`docs/NEXT.md:19` משוכתב** לפי §8.3.
8. `git push origin main` — «סיימתי» תקף **רק** עם `..HEAD -> main` בפלט.
9. 🆕 **אחרי שה-deploy נחת** — שתי בדיקות הצורה מורצות שוב על ה-bundle ה**חדש** (ה-hash מחולץ מחדש, ⛔ לא `B6YE6JDv`) וחייבות להחזיר **`0`** ו-**`1`**. ⛔ **זהו האימות עצמו ⛔ ולא פורמליות** — עד שהוא רץ, הראיה היחידה היא העץ המקומי, ו-`verify` ⛔ אינו מוכיח דבר מעבר לקומפילציה (§5).

### 8.5 · ⛔ מה ⛔ אינו בגל הזה

⛔ `B-246` · `B-254` · `B-185` · `B-275` (ולא לפי מספר שורה — לפי תוכן:
`StatCard`/accents · `Inter` inline · מחירים קשיחים+תג `LIVE` · `sentinel.yml`) ·
⛔ כל כתיבה ל-DB · ⛔ דמו/עיצוב · ⛔ סגירת `W-CAP`/`C-036` · ⛔ העלאת תקרת
`STATE` · ⛔ `--no-verify` · ⛔ force-push · ⛔ יצירת `docs/BOOTSTRAP.md` ·
⛔ הארכת `תוקף` ל-`M-003`.

---

## §9 · פריטים שנוצרו בגל הזה

| מזהה | מה | קובץ |
|---|---|---|
| `B-288` | ⛔ אין `docs/BOOTSTRAP.md` ו⛔ **אין מפנה אליו** — הכרעת ניב | `BACKLOG` |
| `B-289` | `M-003` פג `15` יום — מדידה חוזרת של שימור שבוע 1 | `BACKLOG` |
| `M-018` | `0/40` SHORT פתוחות בפרודקשן · `1/75` בטבלה כולה | `METRICS` |

⚠️ **`B-257` ⛔ לא נפתח מחדש** (§7.3) · ⚠️ **`B-274` נשאר פתוח** — הוא הפריט
שסוגר את מחלקת «מספר בפרוזה שאינו נאכף».

---

## §10 · ⏸️ עצירה

⛔ **הקומיט הזה ⛔ אינו אישור לבצע** (`CLAUDE.md` §9 שלב 2).

✅ **שלב 3 בוצע 04.09** — ניב קרא את הקובץ מ-`origin`, אימת עצמאית
(`registry 38/38` · `STATE 15,831` · `diff` הגיזום ⇒ `0` פריטים פתוחים אבדו),
**אישר `§1`–`§7` · `§8.1` · `§8.2` · `§8.4` · `§8.5`**, והכריע על §8.3.
🔴 **ושני תיקונים ניתנו, שניהם על מדידה שגויה שלי:** מספר השורה בדיווח הצ'אט
(התוכנית עצמה נשאה את הנכון) · ואסרציית ה-bundle שלא יכלה לירות — §8.3.

⏸️ **עצירה חוזרת.** שלב 4 (ביצוע) מתחיל רק באישור מפורש נוסף.
