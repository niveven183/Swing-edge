# PLAN 2026-09-05 — `B-185` · (ב)+(ג)+(ד) — awaiting approval

**מקור ההכרעה:** `docs/audits/AUDIT-2026-09-04-b185-live-fallback.md` (נקרא מהריפו, ⛔ לא מהזיכרון).
**HEAD בזמן הכתיבה:** `3ce2327` · עץ נקי.
**הכרעת ניב:** **(ב)+(ג)+(ד) יחד. ⛔ לא (א). ⛔ לא (ה).**
⛔ **הגל הזה הוא תוכנית בלבד — אפס קוד.**

**סיווג (§15) — `לא/כן/לא/כן/לא` ⇒ `T3`.**
· הפיכות — **לא** (⛔ אפס migration · אפס `UPDATE` · אפס `DELETE`; ראה §5)
· אמון — **כן** (מספר שהמשתמש רואה ופועל עליו)
· אבטחה — **לא**
· רוחב-קוד — **כן** (`SwingEdge_App.jsx` + harness חדש; שלושה אתרי כתיבה בקובץ אחד)
· ודאות — **לא** (הכול נמדד היום)
⇒ שתי «כן» ⇒ `T3`. שלב 1 (אבחון read-only) בוצע ונדחף ב-`22aad8c`; זהו שלב 2.

---

## 1 · המדידה שנפתחה בגל הזה — ה-`SELECT`

הורץ כפי שנכתב באודיט Q4, **read-only, אגרגטיבי, ⛔ אפס תוכן אישי, ⛔ אפס כתיבה.**
פרויקט `zicstkfkwhzvmdkzpidm` · `public.user_settings`.

```
contaminated_users = 46 · users_with_watchlist = 46 · user_settings_rows = 46
```

🔴 **`46/46` שורות `user_settings` נושאות לפחות ליטרל אחד מ-`SCANNER_DATA`** —
מונה **46**, מכנה **46**, אוכלוסייה **`public.user_settings` בפרודקשן**.
`users_with_watchlist` = **46/46** ⇒ ⛔ **אין ולו שורה אחת בלי `watchlist`.**

⚠️ **זהו חסם עליון, ⛔ לא ספירה — וזה הממצא, ⛔ לא פגם בשאילתה.**
‹`R-1`› מאושש: הצורה השמורה ⛔ **אינה נושאת `ts`/`source`/`asOf`**, ולכן
⛔ **אין שום דרך מכנית להבחין** בין `142.30` שהוא ליטרל מ-2024 לבין מחיר
שנמדד ובמקרה התלכד. השאילתה מתאימה **ליטרל מדויק**:
· **מנפחת** — מחיר אמיתי שהתלכד ל-`142.30` נספר;
· **מכווצת** — ליטרל שנדרס בערך אמיתי ⛔ לא נספר.
⇒ הערך האמיתי הוא **≤ 46**. ⚠️ וכיוון שהמונה **רווי** (`46 = ` המכנה),
כיוון ההנפחה הוא היחיד שנותר פתוח.

⚠️ **ומה המספר ⛔ אינו אומר:** `46/46` הוא בדיוק מה שצפוי מהשרשרת שאומתה
באודיט (הידרציה כותבת את `DEFAULT_WATCHLIST` לכל משתמש שלא נגע ברשימה) ⇒
הוא מודד **את גודל האוכלוסייה שנחשפה למנגנון**, ⛔ **לא כמה משתמשים פעלו
על מספר מומצא**. הכלל של `CLAUDE.md` §2 («האם המכנה יכול בכלל להכיל את מה
שנספר?») חל כאן במלואו.

⛔ **שאילתת העומק (Q4, `seed_literals` per-user) ⛔ לא הורצה** — היא מחזירה
`user_id` per-row ו⛔ אינה אגרגטיבית; האישור בפרומפט היה ל-`SELECT` האחד.

---

## 2 · 🔴 הפרכה של הנחה בפרומפט — חובה לקרוא לפני אישור

הפרומפט קובע ש-`?? 0` ב-`:6751` «הוא אותו ‹`R-2`› בדיוק, בשורה צמודה».
**מדדתי. הדפוס זהה; ההשלכה ⛔ אינה.**

`src/priceService.js:136`, כלשונו:

```js
const changePct = prevClose ? (change / prevClose) * 100 : 0;
```

🔴 **ההמצאה של `0` נולדת בשכבת הספק, ⛔ לא בשכבת הרינדור.** `:175` מחזיר
`changePct` **תמיד כמספר** ⇒ כש-`lp` קיים, `changePct` ⛔ לעולם אינו `undefined`.

**שתי מסקנות מדודות:**

1. ⚠️ **הסרת `?? 0` מ-`:6751` היא no-op בזמן ריצה** בהינתן חוזה הספק הנוכחי:
   אחרי (ב), `changePct` נופל ל-nullish **בדיוק כאשר** `lp` nullish — ואז
   `price` גם הוא nullish, הרינדור נכנס לענף `—`, ושורת האחוז ⛔ אינה מרונדרת כלל.
2. 🔴 **ולכן (ב) ⛔ אינה סוגרת את הסימפטום `+0.00%` בירוק.** מי שיקרא
   «`?? 0` הוסר» כ«המשתמש כבר לא רואה אפס מומצא» יטעה. הליטרל `: 0`
   ב-`priceService.js:136` **שורד את הגל הזה במלואו**.

⇒ **אני עדיין מבצע את (ב) על `:6751`** — הוא ‹`R-2`› בצורתו, הוא מסיר תלות
בחוזה של מודול אחר, והשארתו הופכת את `:6750` ל«תוקן» ואת שכנו ל«נשאר».
⛔ **אך התוכנית ⛔ לא תטען שהוא מתקן מסך.** הליטרל ב-`priceService.js:136`
נרשם ב-§7 כפריט פתוח.

---

## 3 · שלושת החלקים

### (ג) · `SwingEdge_App.jsx:1642` — הזרע ⛔ לא יישא `price`/`change`

| | |
|---|---|
| **מה נשמר** | `SCANNER_DATA` (`:321-330`) ⛔ **לא נגע** · `ticker` · `setup` · `chartSym` · סדר הפריטים · 10 פריטים |
| **מה משתנה** | ה-`map` ב-`:1642` מפסיק להעתיק `price: s.price, change: s.change`. שתי שורות הקריפטו (`:1643-1644`) מיישרות לאותה צורה — הן נושאות היום `price: null, change: null`, כלומר את **המפתחות** בלבד |
| **רולבק** | `git revert` של הקומיט. ⛔ אפס מצב מתמשך — הליטרל חוזר לזרוע פריטים חדשים בלבד |

⚠️ **⛔ אינה צורה חדשה.** `:1643` (`BTC`) הוא העוגן: פריט בלי מחיר שמיש
**כבר מרונדר `—` בפרודקשן היום**, דרך הענף שכבר בנוי ב-`:6785-6787`
(כולל ספינר `pricesLoading`). (ג) מרחיבה מצב **קיים ומוכח** משני פריטים לעשרה.

⚠️ **`SCANNER_DATA` נמדד בעל צרכן אחד בדיוק — `:1642`.** אחרי (ג) שדות
`price`/`change` שבו הופכים לליטרלים **מתים**. ⛔ **⛔ לא מוסרים אותם בגל הזה**
(§11 — ⛔ לא קשור למשימה, ומחיקה יוצרת דיף רחב יותר). נרשם ב-§7.

⛔ **(ג) לבדה ⛔ אינה מספיקה** — היא עוצרת **זרימה חדשה** בלבד. `46/46` השורות
הקיימות ממשיכות להיטען בהידרציה עם המחיר שבתוכן.

### (ב) · `SwingEdge_App.jsx:6750-6751` — הרינדור מפסיק לנחש

| | |
|---|---|
| **מה נשמר** | המסלול החי (`lp?.price` ⇒ מחיר אמיתי מרונדר כרגיל) · ענף ה-`—` וספינר `pricesLoading` (`:6785-6787`) — **קיימים, ⛔ לא נבנים** · המיון (`:6740-6741`) קורא `lpA`/`lpB` בלבד ו⛔ אינו נוגע ב-`s.price` |
| **מה משתנה** | `const price = lp?.price ?? s.price;` → `const price = lp?.price;`<br>`const changePct = lp?.changePct ?? s.change ?? 0;` → `const changePct = lp?.changePct;` |
| **רולבק** | `git revert`. ⛔ אפס מצב מתמשך |

⛔ **אין `\|\| 0` ואין `?? 0`** — ערך חסר ⇒ `—`. `null` הוא הודאה, `0` הוא המצאה (‹`R-2`›).

🔴 **החלטה פתוחה לניב — שורת האחוז ב-JSX.** שורת האחוז (`:6781-6784`)
יושבת **בתוך** ענף `price != null`, ו-`changePct>=0` על `null` הוא `false`
⇒ חץ אדום + `null%`. **מדדתי שהמצב הזה ⛔ אינו ניתן להגעה** בחוזה הספק
הנוכחי (§2). שתי דרכים:

- **ב1 — בלי שומר.** מינימלי. נשען על אינווריאנטה שחיה ב-`priceService.js`
  ו⛔ אין שער שאוכף אותה ⇒ ‹`R-6`› בהמתנה.
- **ב2 — שומר `{changePct != null && (…)}`.** תנאי אחד. ממיר תלות
  חוצת-מודול לערובה מקומית. ⚠️ שינוי JSX ⇒ ⛔ **אינו מכוסה ב-harness**
  (אותו גבול של `C-036`/`C-038`).

**המלצת Code: ב2. ⛔ ההכרעה של ניב** — ⛔ לא אבחר בשקט.

### (ד) · אפקט ההתמדה (`:1831`) — הצורה הנשמרת ⛔ לא תישא `price`/`change`

| | |
|---|---|
| **מה נשמר** | `ticker` · `setup` · `chartSym` · מספר הפריטים · סדרם · כל שאר מפתחות ה-`patch` (`:1830-1837`) · `saveSettings` · הסכימה (`settings` הוא jsonb — ⛔ **אפס migration**) |
| **מה משתנה** | `watchlist: watchlistItems` → `watchlist: watchlistItems.map(({ ticker, setup, chartSym }) => ({ ticker, setup, chartSym }))` |
| **רולבק** | `git revert`. שורות שכבר נכתבו בצורה הרזה נטענות בהידרציה ללא שגיאה גם בקוד הישן — הישן קורא `s.price` שאינו קיים ⇒ `undefined` ⇒ ענף `—`. **הרולבק ⛔ אינו משבש נתונים** |

🔴 **הוכחת סובלנות הקורא — שורה ישנה שכן נושאת `price`/`change` ⛔ אינה נשברת.**
השרשרת אומתה בקוד:

1. הידרציה (`:1777-1780`) עושה `setWatchlistItems(s.watchlist)` — **מילולית**,
   ⛔ ללא סינון, ⛔ ללא ולידציה, ⛔ ללא גישה למפתחות.
2. הרינדור אחרי (ב) קורא מהפריט **ארבעה** מפתחות בלבד: `ticker` (`:6763`),
   `setup`/`chartSym` (`:6762`), ו-`s` כמפתח `key`. ⛔ **`s.price` ו-`s.change`
   ⛔ אינם נקראים כלל** ⇒ מפתח עודף הוא נתון מת, ⛔ לא שגיאה.
3. הכתיבה החוזרת (ד) עושה destructuring של שלושה מפתחות ⇒ מפתח עודף **מושמט**,
   ⛔ לא מתנגש.

⇒ שורה ישנה **נקראת, מרונדרת `—`, ונכתבת חזרה רזה.** מכוסה ב-`D1`–`D4`.

🔴 **וכך `46/46` השורות מתנקות — ⛔ בלי migration ו⛔ בלי `UPDATE` ידני.**
אפקט ההתמדה יורה על כל שינוי ב-`watchlistItems`/`capital`/`lang`/…
(מערך התלויות ב-`:1848`) ⇒ הכתיבה הרזה הראשונה של המשתמש דורסת את השורה שלו.
⚠️ **מוצהר במפורש: משתמש שלא יחזור ⛔ לא יתנקה.** הליטרל יישאר ב-jsonb שלו,
⛔ **אך ⛔ לא יוצג** — (ב) חוסמת את הרינדור ללא תלות בתוכן ה-DB. ⇒ החשיפה
נסגרת ב-`46/46` מיידית; **הניקוי** הוא הדרגתי.

---

## 4 · 🔴 אסרציה אדומה לפני ירוקה — `test:watchlist`, חוליה 27

`scripts/watchlist-fallback-test.mjs`, באותה תבנית חילוץ-לפי-עוגן של
`test:hydration` ו-`test:shortpct`: 61 `import` ברמה העליונה חוסמים ייבוא
ל-Node ⇒ ה-harness מחלץ ארבעה מקטעים לפי עוגן ומריץ ב-`new Function`.
⛔ **כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`).

**שערי-מטא (`M1`–`M6`) — `exit 1` מיידי:**
`M1` עוגן `const price = lp?.price` == 1 · `M2` עוגן `const changePct = lp?.changePct` == 1 ·
`M3` עוגן `const DEFAULT_WATCHLIST = [` == 1 · `M4` עוגן `watchlist: watchlistItems` == 1 ·
`M5` סוגריים מאוזנים בארבעת המקטעים · `M6` הצהרת `price` מסתיימת ב-`;`.

**בלוק A — הזרע (ג):** `A1` 10 פריטים · `A2` ⛔ אף פריט ⛔ אינו נושא מפתח `price` ·
`A3` ⛔ אף פריט ⛔ אינו נושא `change` · `A4` `NVDA` שומר `setup`+`chartSym` ·
`A5` `BTC` קיים (העוגן שכבר מרנדר `—`).

**בלוק B — הרינדור (ב):** `B1` 🔴 `lp=null` + `s.price=142.30` ⇒ המחיר **nullish**,
⛔ **לא `142.30`** · `B2` `lp=null` + פריט בלי מחיר ⇒ nullish · `B3` `lp={price:150}`
⇒ `150` (המסלול החי ⛔ לא נשבר) · `B4` 🔴 `lp=null` + `s.change=+3.2` ⇒ nullish,
⛔ **לא `0`** ו⛔ לא `3.2` · `B5` המקטע ⛔ אינו מכיל `s.price` · `B6` המקטע ⛔ אינו
מכיל `?? 0` ⛔ ולא `\|\| 0`.

**בלוק C — הצורה הנשמרת (ד):** `C1` ⛔ אין `price` ב-`patch.watchlist` ·
`C2` ⛔ אין `change` · `C3` `ticker`/`setup`/`chartSym` שרדו · `C4` מספר הפריטים נשמר.

**בלוק D — 🔴 סובלנות לשורה ישנה (ד):** `D1` שורה ישנה עם `price:142.30`
דרך הרינדור ⇒ nullish · `D2` אותה שורה ⇒ `ticker`/`setup`/`chartSym` נקראים ·
`D3` אותה שורה דרך הכתיבה ⇒ `price`/`change` מושמטים · `D4` מערך מעורב
ישן+חדש ⇒ האורך נשמר, ⛔ אפס זריקה.

**⚪ אינווריאנטות — מודפסות `⚪` ו⛔ לא `✓`, ו⛔ אינן ראיה לתיקון:**
`W1` `priceService.js:136` עדיין `: 0` (§2) · `W2` `SCANNER_DATA` עדיין נושא
את הליטרלים (מתים אחרי (ג)).

⇒ **25 אסרציות + 2 ⚪.**

🔴 **הרצה על העץ הנוכחי, ⛔ לפני התיקון — צפי: 10 אדומות**
(`A2`·`A3`·`B1`·`B4`·`B5`·`B6`·`C1`·`C2`·`D1`·`D3`).
⚠️ **הצפי הזה ⛔ אינו התוצאה** — הפלט המדוד יודבק בדיווח.
⛔ **ירוק מראש ⇒ עצירה** ודיווח לניב: אם `B1` ירוקה לפני התיקון, ה-harness
⛔ אינו מודד את מה שהוא מתיימר למדוד.

⚠️ **`CLAUDE.md` §7 — `26 → 27` באותו קומיט**, בבלוק **וגם** בפרוזה.
🔴 **`B-274` נשאר פתוח** — זו ההזזה-ביד הרביעית, ⛔ והיא ⛔ אינה סוגרת אותו.

---

## 5 · מדד הסגירה

⛔ **⛔ לא הומצא — מועתק מ-`D-066`, שהוא התקדים היחיד בריפו.**

1. `npm run test:watchlist` ⇒ **10 אדומות לפני · 25/25 אחרי**, פלט מודבק.
2. `npm run verify` ⇒ **27** חוליות + `build`, `EXIT=0`, פלט מלא מודבק.
3. **שתי בדיקות צורה על ה-bundle החי** — ה-hash **מחולץ ב-`curl` בכל מדידה,
   ⛔ לא מוקשח** (`curl -s https://swing-edge.com/` ⇒ `assets/index-*.js`):
   - **בדיקה 1 — (ב):** מונה `??` שאגף ימין שלו הוא `.price`.
     צפי **`1` לפני ⇒ `0` אחרי**.
   - **בדיקה 2 — (ד):** מונה `watchlist:` שאחריו `.map(`.
     צפי **`0` לפני ⇒ `1` אחרי**.
   ⚠️ **צורה, ⛔ לא טקסט מקור** — ה-מזעור ממפה שמות משתנים; **שמות מאפיינים
   שורדים**. זו בדיוק הסיבה ש-§8.3 ב-`D-066` שוכתב אחרי שנמדד `0` לפני
   **וגם** אחרי (‹`R-3`› — שער שעובר בשני המצבים).
   ⚠️ **(ג) ⛔ אינה מכוסה ישירות** בשתי הבדיקות — היא מכוסה ב-`A1`–`A5`
   וטרנזיטיבית בבדיקה 2. **מוצהר, ⛔ לא מוסתר.**
4. **`SELECT` חוזר** (אותה שאילתה, §1) ⇒ שורה ל-`METRICS` עם שושלת.
   ⚠️ **⛔ ולא יירד ל-0 מיד** — הניקוי הדרגתי (§3, (ד)).

🔴 **מה זה ⛔ אינו מוכיח — מוצהר, ⛔ לא מרומז:**
· ⛔ **שהמשתמש רואה `—` על המסך.** JSX · React · דפדפן אמיתי ⛔ אינם מכוסים —
  אותו גבול בדיוק של `C-036` ו-`C-038`.
· ⛔ **שהמשתמש כבר לא רואה `+0.00%`** — `priceService.js:136` שורד (§2).
· ⛔ **ששורה בפרודקשן התנקתה** — רק שהקוד שכותב אותה רזה.
⇒ **פריט `CHECKS` חדש (`C-039`) ⛔ לפני שהגל נסגר** (§14): פאנל רשימת המעקב
בדפדפן אמיתי, משתמש שלא נגע ברשימה, בשני מצבים — ספק חי וספק מנותק.
**סיווג `T3` ⇒ ⛔ אין סגירה בלי אימות עין** (§15).

---

## 6 · טיוטת הודעה מוקדמת למשתמשים — ⛔ לא נשלחת

⚠️ **טיוטה בלבד. ⛔ לא נשלחה, ⛔ לא תישלח בגל הזה. ההחלטה אם לשלוח = ניב.**
⚠️ ואם תישלח — HTML ממותג מ-`emails/`, טוקני v3, RTL, CTA עם utm (§12).
⛔ **לעולם לא טקסט חשוף.** הטקסט למטה הוא **תוכן**, ⛔ לא פורמט.

> **נושא:** רשימת המעקב — מה משתנה, ולמה זה לטובה
>
> היי,
>
> ברשימת המעקב שלך הופיעו עד היום מחירים לשמונה סימולים מוכרים גם כשלא
> הצלחנו למשוך ציטוט חי. המספרים האלה הגיעו מערכי ברירת מחדל שנקבעו בזמן
> בניית המערכת — הם ⛔ לא היו מדידה, והם התיישנו.
>
> תיקנו את זה. מעכשיו, כשאין לנו מחיר חי לסימול — נציג `—`.
>
> זה אומר שתראו לפעמים קו מקף במקום מספר. **זו הכוונה.** עדיף שנגיד «אין
> לנו את זה כרגע» מאשר שנציג מספר שנראה חי ואינו.
>
> ⛔ שום נתון שלך לא נמחק, ורשימת המעקב שלך נשארת כפי שהיא.
>
> צוות Swing Edge

---

## 7 · מה נשאר פתוח — נרשם, ⛔ לא מתוקן (§10.1 · §11)

| # | מה | לאן |
|---|-----|-----|
| 1 | **(ה) — תג חיוּת אמיתי + `Radio` ב-`:6708` ללא תנאי.** התג נגזר מהשעון בלבד; ה-`Radio` ⛔ אינו מתייעץ בכלום | `B-` חדש ב-`BACKLOG` ‹`R-2`› |
| 2 | 🔴 **`priceService.js:136` — `changePct` ממציא `0` כש-`prevClose` falsy.** ההמצאה ששרדה את (ב) (§2) | `B-` חדש ‹`R-2`› |
| 3 | **`SCANNER_DATA` `price`/`change` — ליטרלים מתים אחרי (ג)** | `BACKLOG` |
| 4 | **`NEXT` §2 נושא `200/200`** — עותק **שלישי** של `M-010` שכבר הופרך. ⛔ **⛔ לא נגעתי** | ‹`R-6`› — הכרעת ניב |
| 5 | **`B-264` — הכרעת ניב: נסגר עם מצבה** המפנה ל-Q5 באודיט. **מופרך במדידה** — `B-182`/`B-185` חולקים שאלה, ⛔ ולא אתר/ערך/צרכן/תיקון | `BACKLOG` → מצבה |
| 6 | **`B-182` נשאר עומד בנפרד** — ⛔ אינו נסגר ו⛔ אינו מאוחד | `BACKLOG`, ללא שינוי |
| 7 | **`B-274` נשאר פתוח** — `26→27` ביד היא המופע הרביעי | `NEXT` §3 |

---

## 8 · תקציב `STATE`

לפני: **15,604 / 16,000 בתים** (`wc -c`).
תוצאת ה-`SELECT` היא **מדידה** ⇒ שורת **`M-`** ב-`docs/METRICS.md` (§14),
⛔ **לא ב-`STATE`**. שורת ⏸️ הקיימת («⛔ לא הורץ — ממתין לאישור ניב»)
מוחלפת במצביע קצר ⇒ **צפי לירידה נטו.**
⛔ **⛔ לא גיזום פריט פתוח · ⛔ לא `ARCHIVE` · ⛔ לא העלאת תקרה.**
המדידה בפועל תדווח.

---

## 9 · סדר הביצוע — לגל הבא, אחרי אישור

1. `test:watchlist` **תחילה** ⇒ ריצה על העץ הלא-מתוקן ⇒ **צפי 10 אדומות**, פלט מודבק.
2. (ג) `:1642-1644` · (ב) `:6750-6751` (+ הכרעת ב1/ב2) · (ד) `:1831`.
3. `test:watchlist` ⇒ `25/25`.
4. `package.json` + `CLAUDE.md` §7 `26→27` — **אותו קומיט**.
5. `C-039` ב-`CHECKS` · שבעת הפריטים ב-§7 · `STATE`/`METRICS` (§10.1).
6. `npm run verify` מלא ⇒ פלט מודבק.
7. push ⇒ `..HEAD -> main` (§5.4) ⇒ שתי בדיקות הצורה על ה-bundle החי.
8. `DONE` **בקומיט הבא** (קומיט ⛔ אינו יכול לשאת את ה-hash של עצמו).

⛔ **מחוץ לגל:** (א) · רענון `s.price` · (ה)/`Radio`/עיצוב · `B-245` · `B-254` ·
`B-182` · `B-274` · `B-275` · סגירת `W-CAP`/`C-036` · `BOOTSTRAP` ·
`--no-verify` · force · amend · השתקת שער.
