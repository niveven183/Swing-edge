# PLAN 2026-09-09 — `B-305` + `B-306`: סנטינל שרואה את מה שקרה

**סטטוס:** awaiting approval · **רמה: T3** · תשובות `1..5`: **לא / כן / כן / כן / כן**
**בסיס:** `21fd1e6` · עץ נקי · **⛔ אפס שינוי קוד בוצע בגל האבחון הזה** (§8.1 שלב 1)

---

## 0. מה נמדד — ⛔ ולא הונח

כל מספר כאן נגזר מפקודה שרצה על העץ `21fd1e6` או על `51a12d2`. פקודות מצוטטות.

### ① `firstSeen:-35m` — ⛔ **המדידה החיה חסומה**, וזו הצהרה ⛔ לא הנחה

**מה שנמדד — הבייטים:**

```
$ sed -n '378,395p' .github/workflows/sentinel.yml
--data-urlencode "query=is:unresolved firstSeen:-35m"
```

**מה ש⛔ נמדד — התשובה של Sentry.** הפרומפט דורש להריץ את שתי הגרסאות מול
ה-issue של 07.09 ולראות `0` מול `1`. **⛔ לא בוצע, ו⛔ לא נוחש.** הסיבה מדודה:

| מקור טוקן | נמדד | מסקנה |
|-----------|------|--------|
| `.env` מקומי | `VITE_SUPABASE_URL` · `VITE_SUPABASE_ANON_KEY` · `VITE_SENTRY_DSN` | **DSN הוא מפתח ingest** — כותב אירועים, ⛔ **אינו** קורא את `/issues/` |
| `gh secret list` | `SENTRY_AUTH_TOKEN` קיים (`2026-07-21`) | סודות GitHub הם **write-only** ⇒ ⛔ בלתי-קריא |
| §12 | — | «Code לעולם לא מזין ולא מדפיס סודות» ⇒ ⛔ **אסור לי גם אילו יכולתי** |

⚠️ **וזה ⛔ ממצא חדש — זה בדיוק מה ש-`INCIDENTS#20` כבר רשם:**
«the alert rules could ⛔ not be read (⛔ no Sentry MCP, ⛔ no token, §12) and were
⛔ **not guessed**». **ו-`firstSeen = 2026-07-24T06:12:31Z` עצמו ⛔ לא נמדד מה-API**
— הוא נלקח מ**מייל Sentry ב-Gmail** (`AUDIT-2026-09-07` `:54`, עמודת מקור =
`Gmail`). ⇒ **⛔ אף אחד ⛔ לא שאל את Sentry דרך ה-API בשום שלב.**

🔴 **המשמעות התכנונית — ⛔ ולא עיכוב בלבד:** `lastSeen` ⛔ **אינו** «התיקון
הנכון» עד שנמדד. `is:regression` ⛔ **לא אומת שהוא טוקן חוקי** בחיפוש issues של
Sentry, וכתיבתו לתוך workflow בלי מדידה מייצרת בדיוק את הכשל של `B-306` שוב:
שאילתה תחבירית-תקינה שמחזירה `0` **לנצח, בשקט**. ⇒ **שלב `א` בתוכנית הוא שער
מדידה, ⛔ לא שינוי קוד** (§2א).

### ② מה **בדיוק** מייצר `riskPct === null` — נמדד מול המודולים האמיתיים

⛔ **לא שוכתב.** `deriveInstrumentCurrency` · `matchesCapital` · `riskInCapital`
יובאו כפי שהם והורצו ב-Node על צורת השורה של הלוח
(`SwingEdge_App.jsx:4757-4764`), עם `capitalCurrency='USD'` וטבלת FX **תקינה**:

```
ticker             state          reason                 aggr   matchesCap  conv.reason            riskPct
AAPL               assumed        no_evidence_against    true   true        identity               2.00%
NVDA               assumed        no_evidence_against    true   true        identity               2.00%
BTC-USD            measured       pair_quote             true   true        identity               2.00%
SNTNL              assumed        no_evidence_against    true   true        identity               2.00%
SNTNL1             ambiguous      unrecognized_ticker    false  false       unverified_instrument  null  <-- hasStop=true
SNTNL_X            ambiguous      unrecognized_ticker    false  false       unverified_instrument  null  <-- hasStop=true
1010               ambiguous      numeric_ticker         false  false       unverified_instrument  null  <-- hasStop=true
SNTNL/ILS-label    contradicted   ils_never_measured     false  false       unverified_instrument  null  <-- hasStop=true
```

**התשובה לשאלה «`contradicted`? `no_rate`? שניהם?» — ⛔ אף אחד מהם אינו הכרחי.**
התנאי המדויק לקריסת 07.09 הוא **צירוף של שניים**:

> `hasStop === true` (כלומר `t.stop != null && t.shares > 0`)
> **וגם** `riskInCapital(...).value == null`

והשני מתפצל לשני מסלולים **בלתי-תלויים** ב-`amountAt`
(`src/hooks/useFxRates.js:168-187`):

| מסלול | תנאי | דורש הון לא-דולרי? | דורש FX שבור? |
|-------|------|-------------------|---------------|
| **A · `unverified_instrument`** | `!isAggregatable(derived)` — נבדק **לפני** `from === displayCurrency` (`:176` לפני `:178`) | ⛔ **לא** | ⛔ **לא** |
| **B · `no_rate` / `loading`** | `status !== "ready" \|\| !table`, או `convert` מחזיר `null` | ✅ **כן** — אחרת `identity` חוסם ב-`:178` | ✅ כן |

🔴 **מסלול A הוא הבחירה, וזו מדידה ⛔ לא העדפה:** הוא יורה בחשבון QA **דולרי
לגמרי עם טבלת FX תקינה**, ולכן ⛔ אינו דורש לגעת ב-`capitalCurrency` של חשבון
ה-QA. מסלול B היה מחייב להפוך את ההון לשקלי — מה שמזיז את שער ההידרציה
(`sentinel-auth.spec.js:344`, שמצפה ל-`"2,500"`) ומייצר בדיקה **מהבהבת** התלויה
בטריות טבלת ה-FX. ⇒ **B נדחה כספק, ⛔ לא כתופעה.**

**ו-`contradicted` ⛔ אינו נדרש:** `ils_never_measured` אכן מייצר `null` (שורה
אחרונה בטבלה), אבל הוא דורש תווית `currency='ILS'` על טיקר אלפביתי — כלומר נגיעה
בהעדפת המטבע של החשבון. `unrecognized_ticker` מגיע לאותו `conv.reason` בדיוק
(`unverified_instrument`) **בלי** לגעת בשום הגדרה.

**המועמד:** `SNTNL1`. הנימוק ⛔ אינו אסתטי — `ALPHA_TICKER = /^[A-Z][A-Z.\-]*$/`
ו-`NUMERIC_TICKER = /^\d+$/` (`instrumentCurrency.js:134-135`); ספרה בתוך מחרוזת
אלפבתית נופלת בין שניהם ⇒ `unrecognized_ticker`. ⚠️ **והוא מכיל את המחרוזת
`SNTNL`** ⇒ **כל** מנגנוני הצמצום והניקוי הקיימים בספק (`deleteSntnlRow` ·
`restCleanup` · סריקת השאריות ב-`:385-403`, כולם `hasText: 'SNTNL'`) ממשיכים
להתאים לו **בלי שינוי**. `1010` נדחה: טיקר מספרי עלול להתאים לטקסט של מחיר
בשורה אחרת ולשבור צמצום.

### ③ אסרציית «⛔ `משהו השתבש` על המסך» — **⛔ אינה קיימת. זו החסימה השלישית.**

```
$ for f in tests-sentinel/*.js; do printf "%s %s\n" "$f" "$(grep -c 'משהו השתבש' "$f")"; done
tests-sentinel/sentinel-auth.spec.js       0
tests-sentinel/sentinel-public.spec.js     0

$ grep -rln "משהו השתבש" . --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=docs
scripts/panel-boundary-test.mjs      # S8 — מוודא שה-fallback ה**ישן** ⛔ נעדר. ⛔ לא רץ מול פרודקשן
scripts/boundary-render-probe.mjs    # זרוע הבקרה. ⛔ לא רץ מול פרודקשן
src/main.jsx                         # מקור
src/components/LandingPage.jsx       # ⚠️ התנגשות — ראה למטה
```

⇒ **`0/2` ספקי הסנטינל אומרים את המחרוזת. אישור: זו חסימה שלישית, בלתי-תלויה
בשתיים, ו⛔ לא נמדדה עד כה.**

🔴 **ושלושה ממצאים שמשנים את **צורת** האסרציה — ⛔ לא רק את קיומה:**

1. **`B-304` (09.09) פיצל את הסימפטום.** לפני 09.09 היה **גבול אחד**
   (`main.jsx:97`) ⇒ קריסה בלוח הסיכון החליפה את **כל** `/app` ב«משהו השתבש».
   היום יש **חמישה** גבולות מוכלים, ו-`risk` (`:4737`) ו-`journal` (`:5004`) הם
   **שניים נפרדים**. ⇒ קריסת `riskPct` היום מותירה את היומן **מרונדר ותקין**,
   ומחליפה **רק** את לוח הסיכון בכרטיס שגיאה. ⚠️ **⇒ בדיקה 4 (רינדור היומן),
   שהאבחון של 07.09 מנה כאחת מ-`3` האדומות, תישאר היום ⛔ ירוקה.** אסרציה שתאמר
   רק «`משהו השתבש` ⛔ בגוף המסמך» תהיה **ירוקה בזמן שפאנל מת** — `R-4` בתחפושת.
2. **הכרטיס נושא עוגן מכני** — `role="alert"` ו-`data-boundary={name}`
   (`PanelBoundary.jsx:77-78`) ⇒ **זהו יעד האסרציה הנכון**, ⛔ לא מחרוזת עברית.
   הפאנל השורשי נושא `role="alert"` (`main.jsx:109`) ו-`<h1>משהו השתבש</h1>`
   (`:126-128`) ⛔ **ללא** `data-`.
3. ⚠️ **התנגשות מחרוזת:** `LandingPage.jsx:101` נושא
   `"משהו השתבש. נסה שוב עוד רגע."` לשגיאת רשימת המתנה. `sentinel-public.spec.js`
   רץ על הלנדינג ⇒ אסרציית-מחרוזת שם היא **תפיסה כוזבת בהמתנה**.

### ④ תקרת ההרצה — מצוין, ⛔ לא מתוקן כאן

`6/37 = 16.2%` מהמשבצות בחלון 07.09 · `M-010` = **`33/336 = 9.8%`** (27.08–02.09) ·
כיסוי דגימת Sentry **`210/1,114.67` דק' = `18.8%`**. ⇒ **גם סנטינל מושלם רץ ~16%
מהזמן.** `B-275` ⛔ **אינו בתוכנית הזו** ו⛔ אינו נסגר בה. כל מה שנבנה כאן חי
מתחת לתקרה הזו.

---

## 1. שתי הפרכות — ⛔ סעיף חובה, ⛔ לא הערת שוליים

### הפרכה 1 🔴 `sentinel.yml` ⛔ **אינו יכול להסתיים ב-`failure`. בשום חומרה.**

`B-306` רשם: «`amber` ⇒ **כתום ⛔ ולא אדום** ⇒ ⛔ לא היה מגיע ל-`failure-alert.yml`
(שמותנה ב-`conclusion == 'failure'`)». **הניסוח הזה מרמז ש-`red` כן היה מגיע.**
נמדד:

```
$ grep -c "exit 1" .github/workflows/sentinel.yml
0
$ grep -n "exit" .github/workflows/sentinel.yml
197: # (הערה על openssl)   219/409/417: awk "BEGIN{exit !(...)}"
```

⇒ **`0/0` מסלולי יציאה לא-אפס.** `overall=red` (`:599`) מזין **טקסט להודעה**,
⛔ לא קוד יציאה. ⇒ `failure-alert.yml` ⛔ **בלתי-נגיש מכל תקלת סנטינל, אדומה
כמו כתומה** — ותיקון החומרה ל-`red` לבדו ⛔ **לא** היה פותח את הערוץ.

⚠️ **וההשלכה ההפוכה חשובה לא פחות — הערוץ שכן חי:** `post_embed` (`:505`) שולח
לדיסקורד ישירות עבור `red` **וגם** `amber`, עם דה-דופ של **3 שעות** לכל
`fingerprint` (`:536-540`). ⇒ **ספק אדום מהדפדפן ⛔ אינו נופל לרצפה** — הוא מגיע
לדיסקורד. ⇒ **⛔ אין צורך לגעת ב-`exit` בגל הזה**, וההנחה «צריך להפוך את Sentry
לאדום כדי שמישהו יידע» ⛔ **שגויה**. הפער האמיתי הוא ש-⛔ **אף ספק ⛔ לא ייצר את
התקלה מלכתחילה** (`B-305`).

### הפרכה 2 ⚠️ «`3` בדיקות היו אדומות» — נכון ל-`51a12d2`, ⛔ **לא להיום**

האבחון של 07.09 מנה `pageerror` · רינדור-היומן · בדיקה 6. אחרי `B-304` (09.09)
היומן חי בגבול נפרד ⇒ **בדיקה 4 ⛔ אינה יכולה עוד ליפול מקריסת לוח הסיכון**.
⇒ המספר `3` ⛔ אינו מספר של היום, וסגירת הפריט על סמכו הייתה ראיה שפגה.

---

## 2. התוכנית

### א. השאילתה — **שער מדידה תחילה, ⛔ לא שינוי**

⛔ **⛔ אין כתיבת שאילתה חדשה לפני שנמדדה.** צעד א׳ הוא ריצה יחידה, ידנית,
ע"י **ניב** (§12 — Code ⛔ אינו נוגע בטוקן), על ה-issue של 07.09:

```bash
# 1 — הישנה. חייבת להחזיר 0.
curl -sS -G "https://sentry.io/api/0/organizations/swingedge/issues/" \
  --data-urlencode "query=is:unresolved firstSeen:-35m" \
  -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" | jq length

# 2 — המועמדת. חייבת להחזיר ≥1 בחלון שבו האירועים נצברו.
curl -sS -G "https://sentry.io/api/0/organizations/swingedge/issues/" \
  --data-urlencode "query=is:unresolved lastSeen:-35m" \
  -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" | jq length

# 3 — האם `is:regression` הוא טוקן חוקי בכלל? שגיאה ⇒ נפסל.
curl -sS -G "https://sentry.io/api/0/organizations/swingedge/issues/" \
  --data-urlencode "query=is:regression" \
  -H "Authorization: Bearer $SENTRY_AUTH_TOKEN" -w '\n%{http_code}\n' | tail -3
```

**כללי ההכרעה — נכתבים כאן מראש כדי שלא ייגזרו מהתוצאה:**
- (1) מחזירה `>0` ⇒ 🔴 **הפרכה** של `B-306`. **עצור ודווח.** ⛔ אין המשך.
- (3) מחזירה `400` ⇒ `is:regression` **נפסל**, ⛔ ולא נכתב לקובץ.
- רק אם (1)=`0` **וגם** (2)`>0` — `lastSeen:-35m` נכנס ל-`sentinel.yml:382`.

⚠️ **והמחיר של `lastSeen` מוצהר מראש, ⛔ לא מתגלה אחר כך:** `lastSeen` מתאים
לכל issue **פתוח שנראה לאחרונה** — כולל תקלה כרונית שתדליק כל ריצה לנצח.
**ההגנה קיימת כבר**: דה-דופ 3 השעות לכל `fingerprint` (`:536`). ⚠️ אבל
ה-`fingerprint` הנוכחי הוא `"sentry|new_issue"` — **קבוע לכל ה-issues** ⇒ שני
issues שונים יתמזגו לאירוע אחד. **התיקון הנלווה:** `fp` שנושא את מזהה ה-issue.
⛔ **ואסור לרכך את הדה-דופ כדי «לא להציף»** — זה `R-4`.

### ב. הספק — `SNTNL1`, בחשבון QA בלבד

- מוסיפים **עסקה שנייה** במסלול היצירה הקיים של הספק (`:472`, יצירה דרך ה-UI
  כמו משתמש), עם `ticker='SNTNL1'`, `stop` מלא ו-`shares > 0`.
- ⛔ **⛔ לא נוגעים ב-`FIXED_TRADES`** — שלוש עסקאות הקבע נשארות בדיוק כפי שהן;
  הן שער `fmtR` חי (`AAPL` סגורה ללא stop) ו⛔ אינן קשורות לגל.
- ⛔ **⛔ אין נגיעה ב-`capitalCurrency` · בהגדרות החשבון · בטבלת FX.** מסלול A
  (§0②) יורה בלעדיהם.
- הניקוי: הצמצום הקיים `hasText: 'SNTNL'` תופס גם את `SNTNL1` ⇒ ⛔ אין מנגנון
  שני. ⚠️ **אבל `deleteSntnlRow` מסרב לפעול אלא אם **בדיוק שורה אחת** מתאימה
  (`:216`)** ⇒ שתי שורות `SNTNL*` ישברו אותו. **חייב** לעבור לצמצום מדויק
  (`hasText: /^SNTNL1?$/` או `data-testid` פר-שורה), ו⛔ **לא** לרכך את בדיקת
  ה«בדיוק אחת» — היא ההגנה שמונעת מחיקה בעיוורון.
- ⚠️ **חשבון QA ⛔ אינו «לא-אמיתי»** — זו שורת DB תחת RLS. הכתיבה עוברת במסלול
  ה-UI הרגיל, נמחקת באותה ריצה, ו-`restCleanup` (`:232`) הוא רשת הביטחון.

### ג. האסרציה — **`[data-boundary]` ראשי, מחרוזת משנית**

בבדיקה חדשה שרצה **אחרי** טעינת `/app` ו**לפני** היציאה, בלשונית ברירת המחדל
(`dashboard`, `:1410-1417`):

| # | נבדק | חומרה |
|---|-------|--------|
| `E1` | `page.locator('[data-boundary]').count() === 0` | 🔴 `red` |
| `E2` | גוף המסמך ⛔ מכיל `משהו השתבש` **תחת `/app`** | 🔴 `red` |
| `E3` | `[data-boundary]` ⛔ קיים גם אחרי מעבר בין שלוש הלשוניות (בדיקה 6) | 🔴 `red` |

⛔ **`red` ⛔ ולא `amber`** — Error Boundary פתוח הוא **כשל קשה**. הוא מגיע
לדיסקורד דרך `post_embed` (הפרכה 1) ⇒ הערוץ נסגר בלי לגעת ב-`exit`.
⛔ **ואסור למקם את `E2` ב-`sentinel-public.spec.js`** — התנגשות הלנדינג (§0③.3).
⚠️ `E1` הוא העיקר; `E2` הוא **גיבוי** למקרה שהקריסה תעקוף פאנל ותגיע לשורש.

### ד. 🔴 מבחן הירי — **מה בוצע, ומה ⛔ בוצע**

#### ד.1 ✅ **ברמת הערך — בוצע ועבר.**

שני השומרים חולצו מילולית משני העצים והורצו על השורה המדודה מ-②:

```
SNTNL  (הספק הנוכחי)
   riskPct = 2 | hasStop = true | conv.reason = identity
   51a12d2 guard (t.hasStop)      -> OK  2.00%      <-- ⛔ אינו יורה
   HEAD    guard (t.riskPct!=null)-> OK  2.00%

SNTNL1 (המועמד)
   riskPct = null | hasStop = true | conv.reason = unverified_instrument
   51a12d2 guard (t.hasStop)      -> THROW TypeError: Cannot read properties of null (reading 'toFixed')
   HEAD    guard (t.riskPct!=null)-> OK  —
```

`51a12d2:4959` נקרא ישירות מהאובייקט של גיט (`git show 51a12d2:SwingEdge_App.jsx`)
⛔ ולא משוחזר מהזיכרון. **הזריקה זהה בהודעתה לחתימת `SWING-EDGE-A`.**
⇒ **הספק הנוכחי מת בשני העצים; המועמד יורה בעץ שקרס ושותק בעץ המתוקן.** זו
בדיוק צורת «זרוע-בקרה שחייבת להישאר מתה» מ-`probe:boundary`.

#### ד.2 ⛔ **ברמת הדפדפן — ⛔ לא בוצע. חסום, ⛔ ולא נדחה.**

הריצה המלאה של `sentinel-auth.spec.js` מול build של `51a12d2` דורשת
`SENTINEL_QA_EMAIL` + `SENTINEL_QA_PASSWORD` — **סודות GitHub, §12.**
⛔ **⛔ לא נוחש ו⛔ לא הוחלף בקירוב שיוצג כאותו דבר.**

⚠️ **ו-ד.1 ⛔ **אינו** תחליף:** הוא מוכיח שהערך זורק, ⛔ **לא** שהסנטינל נצבע
אדום. בין השניים יושבים React · ה-Error Boundary · Playwright · והשאלה
ה⛔פתוחה האם שגיאה ש**נתפסה** ע"י גבול פולטת `pageerror` בכלל (React 19 מנתב
שגיאה תפוסה ל-`onCaughtError`, ⛔ לא ל-`reportError`) — וזו בדיוק הסיבה
ש-`E1` נשען על **DOM** ⛔ ולא על `pageerror`.

**⇒ תנאי סף לביצוע, ⛔ לא «נחמד לעשות»:** לפני ש-`ב`+`ג` נדחפים, הספק המורחב
ירוץ מול build מקומי של `51a12d2` — **חייב אדום**; ואז מול `HEAD` — **חייב
ירוק**. ירוק בראשון ⇒ **הספק ⛔ מייצג ⇒ עצור ודווח.** מריץ: ניב, או Code עם
הרשאה נקודתית ל-QA. ⛔ **סגירה בלי זה אסורה** — «שכבה ⛔ קיימת עד שנצפתה יורה».

---

## 3. ניתוח השלכות (§8)

**רמה 1 — הפילטר תפס:** «רץ אוטומטית בפרודקשן» = **כן** (הסנטינל פוגע ב-
`swing-edge.com` ובחשבון QA חי כל 30 דק'). «משנה DB» = **כן, QA בלבד**.

| ציר | הערכה |
|-----|--------|
| משתמשים | ⛔ **0 משתמשי קצה.** ⛔ אפס שינוי ב-`src/` המוצרי · `api/` · DB סכימה |
| נתונים | שורת `trades` אחת נוספת בחשבון QA, נוצרת ונמחקת באותה ריצה. **הפיך** · RLS מגביל לחשבון עצמו · `restCleanup` הוא הרשת |
| עלות | +~10 שנ' לריצת דפדפן (`3/6` מהריצות) · +2 קריאות Sentry API ידניות · ⛔ אפס דקות Actions נוספות |
| תקרות ספק | Sentry API ⛔ לא נצרך יותר מהיום (אותה קריאה, שאילתה אחרת) · Finnhub/Gmail ⛔ לא נוגעים |
| אבטחה | ⛔ **אפס סוד חדש.** `SENTRY_AUTH_TOKEN` ⛔ לא נקרא ולא מודפס ע"י Code (§12). ה-QA טיקר `SNTNL1` ⛔ אינו סימבול אמיתי |
| תחזוקה (§3) | **מקטין ידני:** היום התשובה ל«האם המוצר חי» מגיעה ממייל בתיבה של ניב. ⚠️ **⛔ ואינו מאפס** — תקרת `6/37` (§0④) נשארת |
| הפיכות | `git revert` על 3 קבצים. ⛔ אין מיגרציה · ⛔ אין סוד · ⛔ אין שינוי סכימה |
| **כשל שקט** | 🔴 **הציר המסוכן.** ספק שנשבר בשקט (`SNTNL1` שמפסיק לייצר `null`, למשל אם `ALPHA_TICKER` ישתנה) יחזיר את הסנטינל לירוק-כוזב **בלי שאיש ידע**. ⇒ ראה §4 «שער נגד» |

**פסק דין: ⚠️ בצע עם הגנה** — בכפוף לשער ①, לשער ד.2, ולשער-הנגד ב-§4.

---

## 4. מה נשמר · מה זז · שער-נגד · רולבק

**⛔ ⛔ לא נוגעים — נאכף בעצמי לפני push:**
`src/` מוצר · `api/` · הון/מטבע · סכימת DB · חשבון אמיתי · `env`/סודות ·
`Q8` · `B-297`/`B-298` · `B-275` (מצוין בלבד, §0④) · `--no-verify` · force · amend.
⛔ **ו⛔ לא נוגעים ב-`exit` של `sentinel.yml`** — הפרכה 1 מראה שאין בכך צורך,
ושינוי שם היה הופך כל תקלה כתומה לכישלון workflow.

**מה זז — 3 קבצים בדיוק:**
1. `.github/workflows/sentinel.yml` — שורה `382` (שאילתה) + `fp` של `sentry|new_issue`
2. `tests-sentinel/sentinel-auth.spec.js` — עסקת `SNTNL1` · צמצום מדויק ב-`deleteSntnlRow` · `E1`–`E3`
3. `docs/` — `BACKLOG`/`STATE`/`DONE`/`INCIDENTS`/`CHECKS` לפי §10

🔴 **שער-נגד — חובה, נגד «הכשל השקט» מ-§3.** אסרציה ב-`scripts/panel-boundary-test.mjs`
(או חוליה חדשה) שמריצה את `deriveInstrumentCurrency('SNTNL1')` ומחייבת
`isAggregatable === false`. ⇒ ביום שבו הרגקס ישתנה והספק יפסיק לייצג,
**`verify` נופל** ⛔ ולא הסנטינל משתתק. ⚠️ **בלי השער הזה הגל מייצר בדיוק את
התבנית שהוא בא לסגור.**

**רולבק:** `git revert` יחיד. ⛔ אין מצב ביניים — אם השאילתה מציפה, החזרת
`firstSeen` מחזירה את השקט הידוע.

---

## 5. חסום על ניב — ⛔ ⛔ אין ביצוע לפני

| # | מה | למה |
|---|-----|------|
| `G1` | הרצת **3 השאילתות** של §2א והדבקת הפלט | §12 · ⛔ בלי זה `lastSeen` הוא ניחוש |
| `G2` | מבחן הירי ד.2 מול build של `51a12d2` | ⛔ שכבה ⛔ קיימת עד שנצפתה יורה |
| `G3` | אישור: האם `B-306` בבקלוג מתוקן **עכשיו** (הפרכה 1) או בביצוע | §10.1 — ⛔ אין פריט יתום |

**⛔ עצירה. ⛔ אפס נגיעה בקוד עד אישור ניב.**
