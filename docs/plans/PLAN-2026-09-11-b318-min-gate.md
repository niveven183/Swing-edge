# PLAN — B-318 · גדר ה-`MIN` נסחפה — awaiting approval

**תאריך:** 2026-09-11 · **HEAD בזמן המדידה:** `70455d6` · **מצב:** ⏸️ ממתין להכרעת ניב
**סיווג הגל הזה (מדידה + מסמך):** `T1` · תשובות `לא/לא/לא/לא/לא`
**סיווג הביצוע המתוכנן:** 🔴 `T3` · תשובות **`כן`**/לא/**`כן`**/כן/לא —
שאלות 1 ו-3 הן טריגר-יחיד: הגדר שולטת במוטציה על **חשבון QA חי** (הפיכות)
ורצה תחת סודות ב-CI (אבטחה). ⇒ `T3` לבד, ⛔ בלי צבירה.

⛔ **המסמך הזה ⛔ ממליץ.** שלוש חלופות, לכל אחת מה מובטח · מה נשבר · מה קורה
כש-`B-275` יתוקן · רולבק. **ההכרעה של ניב.**

---

## 0. מה נמדד — ⛔ מה הונח

כל המספרים כאן נמדדו ב-11.09 מ-`gh api` על **חלון עצמאי ורחב יותר** מזה של
אבחון `B-318`: **`57` ריצות `schedule` + `2` `workflow_dispatch` = `59`**,
03–10.09 (האבחון המקורי מדד `50`, 03–09.09). האוכלוסייה: כל ריצות
`sentinel.yml` בחלון. **המדידה משחזרת את הממצא המקורי ⛔ ולא מסתמכת עליו.**

---

## 1. ① מה בדיוק `MIN` מודד? — 🔴 **אושש**

`sentinel.yml:81` קורא `date -u +%M` **בזמן ביצוע השלב**, ⛔ בזמן הסלוט המתוכנן.

נמדד — הפרש בין `run_started_at` לבין `started_at` של שלב הגדר, על `57/57`:

```
gate steps found: 57/57
gate-step offset from run_started_at  min/median/max sec: 18 / 24 / 408
gate minute != run_start minute: 24/57 (boundary crossings)
```

⇒ הגדר יורה **24 שניות (חציון)** אחרי תחילת הריצה. כלומר `MIN` הוא, למעשה,
הדקה של **תחילת הריצה בפועל** — ו⛔ של `:20`/`:50`.

התפלגות הדקה שהגדר עצמה חישבה, `57` ריצות, שישה סלי-עשר-דקות:

```
10-min bins: [6, 13, 10, 11, 9, 8]   chi2=3.11  (df=5, crit .05=11.07)
auth=1 : 19/57 = 33.3%   window=41.7%   z=-1.28
```

`chi2 = 3.11` מול סף `11.07` ⇒ ⛔ **אי-אפשר לדחות אחידות.** הדקה מתפלגת
אחיד על פני השעה, ו-`19/57 = 33.3%` מול רוחב-חלון `41.7%` (`z = −1.28`)
**עקבי עם הטלת מטבע**. ⇒ **① אושש: זהו השורש.**

⚠️ **דיוק אחד שצריך להיאמר:** «`202` דק׳ (חציון)» ב-`B-318` הוא **החציון של
המרווח בין ריצות עוקבות** (מדדתי `202.1` — זהה), ⛔ **ולא חציון העיכוב**.
העיכוב האמיתי ⛔ **ניתן למדידה כלל** — ראה §2. ההבחנה חשובה כי היא בדיוק
הסיבה שאין עוגן.

---

## 2. ② האם GitHub חושף את הסלוט המתוכנן? — 🔴 **הופרך כעוגן, בצורתו הנוכחית**

**`created_at` מול `run_started_at` — נמדד על `59/59`:**

```
rows where created_at != run_started_at: 0 / 59
```

⇒ **`0/59`.** GitHub מדווח את שני השדות **זהים** לריצות `schedule`. ⛔ **אין
חותמת-תור** להיאחז בה, ולכן העיכוב אינו ניתן לחישוב מה-API. שני השדות ⛔
עוגן.

**`github.event.schedule`:** השדה מחזיר את **מחרוזת ה-cron שהפעילה את הריצה**.
`sentinel.yml:16` נושא **שורת cron אחת** — `'20,50 * * * *'` — המכסה את שני
הסלוטים ⇒ הוא יחזיר את **אותה מחרוזת בדיוק** ב-`:20` וב-`:50`. **בצורתו
הנוכחית הוא ⛔ יכול להבחין.**

🆕 **אבל זה ניתן לשינוי, וזו החלופה (א):** פיצול לשתי שורות cron הופך את
`github.event.schedule` למזהה סלוט **דטרמיניסטי**, בלתי-תלוי בעיכוב.

⚠️ **הנחה שטרם אומתה, ומוצהרת ככזו:** נמדד ב-repo —

```
--- event.schedule usage:  (none)
--- multi-line cron:  אף workflow מבין 12 ⛔ נושא יותר משורת cron אחת
```

⇒ `github.event.schedule` ⛔ **בשימוש באף מקום בריפו**, ולאף workflow ⛔ יש
cron רב-שורתי. **⛔ ניתן לאמת מקומית** — נדרשת ריצת CI אמיתית. זו **ההנחה
היחידה במסמך הזה שלא נמדדה**, והיא חוסמת את (א) עד לאימות.

---

## 3. ③ מה הגדר נועדה למנוע — והאם יש מנגנון אחר? — 🔴 **יש שניים, ו⛔ אחד מהם יכול להיות ההגנה**

**הכוונה המוצהרת** (`sentinel.yml:69-73`, מצוטט): «The `:50` run owns it»,
והגבול `≤09` קיים כדי שריצת `:20` מאוחרת ⛔ תוכל «to also claim the slot and
mutate twice in one hour».

**שני מנגנוני `concurrency` קיימים בקובץ:**

| מיקום | קבוצה | `cancel-in-progress` |
|--------|--------|----------------------|
| רמת-workflow `:24-26` | `sentinel` | 🔴 `true` |
| רמת-job על `browser` `:43-45` | `sentinel-auth` | `false` |

🔴 **⛔ אחד מהם יכול להיות ההגנה מפני «פעמיים באותה שעה», ונמדד למה:**
`concurrency` מונע **חפיפה**, ⛔ **חזרה**. שתי ריצות במרווח של 30 דק׳ ⛔
חופפות, ולכן הקבוצה ⛔ נוגעת בהן — הריצה השנייה פשוט תרוץ ותשנה שוב.

**נמדד — חפיפה ⛔ הייתה אפשרית ולו פעם אחת בחלון:**

```
run duration sec  min/median/max: 38 / 53 / 433      (מקסימום = 7.2 דק')
consecutive gaps <=30min: 0/56
consecutive gaps <=60min: 0/56
gap min/median/max min: 87.8 / 202.1 / 392.3          (מינימום = 87.8 דק')
```

מקסימום ריצה `7.2` דק׳ מול מרווח מינימלי `87.8` דק׳ ⇒ **קבוצת `sentinel-auth`
⛔ עצרה דבר מעולם.** היא רדומה, ⛔ לא פעילה.

⚠️ **וממצא סריקה סביבתית (§11) — ⛔ תוקן, ⛔ ודאי ב-100%, ולכן מדווח בלבד:**
`cancel-in-progress: true` ברמת ה-workflow **גובר** על `false` שברמת ה-job —
ביטול ברמת-workflow הורג את **כל** הריצה, כולל `browser` באמצע מוטציה, וזה
בדיוק מה שההערה ב-`:41-42` מצהירה שאסור. 🔴 **⛔ נצפה בפועל:** `59/59
success`, `0` ביטולים ⇒ זו **סכנה רדומה, ⛔ תקלה מדודה**. ⛔ **ולא נגעתי** —
`.github/` ברשימת האסור לגל הזה. ⇒ דורש שורה במרשם או הכרעה מפורשת של ניב.

---

## 4. ④ כמה מוטציות כפולות **היו קורות** לו `auth=1` תמיד? — 🔴 **`0`**

⛔ הערכה — ספירה. שעת-שעון עם `2+` ריצות היא מוטציה כפולה לו הגדר הייתה
`auth=1` תמיד:

```
Q4 -> hours with 2+ runs if auth always 1: 0 / 57
distinct clock-hours containing >=1 schedule run: 57
hours with 2+ actual auth=1: 0 / 19
```

🔴 **`0/57`.** שום שעת-שעון ⛔ הכילה שתי ריצות. ⇒ **אפילו הסרה מוחלטת של
הגדר הייתה מייצרת `0` מוטציות כפולות בחלון הזה.**

⚠️ **המסקנה, והיא המסקנה המרכזית של המסמך:** ההגנה בפועל ⛔ הגדר ו⛔
ה-`concurrency` — **היא שיעור הנשירה של `B-275`.** המרווח המינימלי `87.8` דק׳
הוא מה שמבטיח `0/57`, ⛔ שום דבר בקובץ. **⇒ תיקון `B-275` מסיר את ההגנה
היחידה בדיוק ברגע שהוא עובד** — בדיוק כפי ש-`B-318` קובע, וכעת מדוד עצמאית
על `57` ריצות.

---

## 5. האסרציה — 🔴 **נצפתה אדומה על העץ הנוכחי**

⚠️ **מוצהר: זהו מכשיר מקומי (`/tmp/b318-gate-rig.sh`), ⛔ בשרשרת `verify`,
⛔ ב-CI.** הוא ⛔ ניתן למימוש ב-CI בגל הזה (יצירת workflow ברשימת האסור).
הוא **מחלץ את בלוק הגדר האמיתי מ-`sentinel.yml` לפי עוגן ומריץ אותו** —
⛔ מחקה אותו — באותה תבנית של `test:hydration`/`test:shortpct`:
**כשל חילוץ הוא אדום קשה ⛔ ולעולם לא דילוג** (`B-272`), ואוכפים זאת ארבעה
שערי-מטא (עוגן-פתיחה ≠ 1 · עוגן-סגירה ≠ 1 · סדר עוגנים · סוגריים לא מאוזנים).

**האסרציה `A1`:** שתי ריצות באותה שעת-שעון ⇒ **⛔ יותר מ-`auth=1` אחת.**
ממצה על כל `C(60,2)` זוגות הדקות.

```
meta: 4/4 extraction gates green — block = lines 81-89 of .github/workflows/sentinel.yml
--- extracted gate ---
MIN="$((10#$(date -u +%M)))"
auth=0
{ [ "$MIN" -ge 45 ] || [ "$MIN" -le 9 ]; } && auth=1
if [ "${GH_EVENT:-}" = "workflow_dispatch" ]; then
  auth=0
  [ "${RUN_AUTH:-true}" = "true" ] && auth=1
fi
echo "SENTINEL_AUTH=$auth" >> "$GITHUB_ENV"
echo "auth=$auth" >> "$GITHUB_OUTPUT"
----------------------

A1  two runs in the same clock hour => at most one auth=1
    pairs examined : 1770  (all C(60,2) minute pairs)
    pairs where BOTH got auth=1 : 300
    first counterexample : :0 + :1  -> auth=1 AND auth=1
    RESULT: 🔴 RED — the guard described at sentinel.yml:69-73 does NOT hold
```

🔴 **אדום — `300/1770`.** ⛔ ירוק-מראש ⇒ הגדר אכן ⛔ עובדת.

⚠️ **`300` ⛔ מספר שרירותי — הוא `C(25,2)` בדיוק**, כאשר `25` הוא רוחב החלון
`[45,59]∪[0,9]`. ההתאמה האנליטית המדויקת היא ההוכחה שהמכשיר מריץ את **הגדר
האמיתית**, ⛔ העתק שלה.

⚠️ **ובדיקת-מכנה:** הריצה הראשונה החזירה `1772` זוגות, ⛔ `1770`. `seq 60 59`
ב-BSD/macOS **סופר אחורה** ⇒ שני זוגות רפאים. ⛔ **המספר לא רוכך ולא עוגל** —
המכשיר תוקן ורץ מחדש (§2, «אפס מנה בלי מכנה»).

---

## 6. שלוש החלופות — ⛔ **המלצה**

### (א) עוגן על הסלוט המתוכנן במקום על `date +%M`

פיצול `cron: '20,50 * * * *'` לשתי שורות, וגדר על `github.event.schedule`.

- **מה מובטח:** דטרמיניזם מלא. זהות הסלוט מגיעה מ**אירוע ההפעלה**, ⛔ משעון
  הביצוע ⇒ העיכוב הופך ללא-רלוונטי. **בדיוק מה שההערה ב-`:69-73` מתארת.**
- **מה נשבר:** `github.event.schedule` ⛔ בשימוש באף מקום בריפו (§2) ⇒ **הנחה
  לא-מאומתת**. ⛔ **וחייב לזעוק, לא ליפול ל-`auth=0`** — מחרוזת לא-מוכרת חייבת
  `exit 1`, אחרת זו גדר שנכבית לנצח **בשקט** (`R-2` · §2).
- **כש-`B-275` יתוקן ⇒ `48` ריצות/יום:** `24` מוטציות/יום, אחת לשעה, בסלוט
  `:50` — **גם אם שתי הריצות נוחתות באותה שעת-שעון**, כי הזהות ⛔ מהשעון.
- **רולבק:** קובץ אחד, קומיט אחד, חזרה לשורת cron יחידה. מיידי.

### (ב) `concurrency` group שמבטיח ריצה מאומתת אחת בשעה

- **מה מובטח:** 🔴 **לצורך `B-318` — ⛔ כלום, ונמדד למה (§3).** `concurrency`
  מונע **חפיפה**, ⛔ **חזרה**: הוא מתייג ריצה שנייה ⇒ היא תרוץ ותשנה שוב.
  ⛔ **אין ב-`concurrency` סמנטיקת «דלג».** מקסימום ריצה `7.2` דק׳ מול מרווח
  מינימלי `87.8` דק׳ ⇒ הקבוצה הקיימת ⛔ ירתה מעולם.
- **מה נשבר:** ⛔ כלום — אבל גם ⛔ נסגר כלום. ⚠️ **החלופה הזו כן סוגרת את
  ממצא §3 הרדום** (התנגשות `true`/`false`), וזה פריט **אחר**.
- **כש-`B-275` יתוקן:** מרווח `30` דק׳ מול תקרת job `12`+`5` דק׳ ⇒ עדיין ⛔
  חפיפה ⇒ עדיין ⛔ הגנה.
- **רולבק:** טריוויאלי.

### (ג) נעילה בצד ה-DB (ה-QA עצמו)

שורת-נעילה לשעה (`UNIQUE` על דלי-שעה); המוטציה רצה רק אם ה-`INSERT` ניצח.

- **מה מובטח:** 🔴 **הבטחה חזקה ביותר** — הדרה הדדית אמיתית **במקום שבו
  המוטציה קורית**, בלתי-תלויה ב-cron · בעיכוב · ב-`workflow_dispatch` · ואף
  בריצה ידנית מקבילה. החלופה היחידה שמגנה גם על מסלול ה-dispatch.
- **מה נשבר:** דורשת **מיגרציה** ⇒ §12: Code כותב `.sql` **בלבד**, ניב מריץ.
  נוגעת ב-spec של Playwright (**מסלול קוד**, ⛔ YAML). ⚠️ **ומוסיפה מצב-כשל
  חדש:** נעילה תקועה מריצה שקרסה חוסמת את השעה הבאה — **חובה TTL/תפוגה**,
  אחרת החלפנו «משנה פעמיים» ב«⛔ משנה לעולם», שהוא כשל **שקט** יותר.
- **כש-`B-275` יתוקן ⇒ `48` ריצות/יום:** נכון — דלי-שעה אחד מנצח, השנייה
  מדלגת **ומדווחת** «skipped · lock held» (⛔ בשקט).
- **רולבק:** איטי יותר — `DROP` + החזרת ה-spec; שינוי DB ⛔ הפיך בקומיט.

⚠️ **(א) ו-(ג) ⛔ סותרות** — (א) קובעת **מי** אמור לשנות, (ג) אוכפת **שרק אחד**
משנה בפועל. **הצירוף ⛔ מיותר, וההכרעה אם לצרף היא של ניב.**

---

## 7. מדד סגירה (לכל חלופה שתיבחר)

1. `A1` המקומית עוברת מ-🔴 ל-🟢 על העץ החדש — **ורצה אדומה על הישן**.
2. מדידה חוזרת אחרי `≥N` ריצות: **`0/N`** סלוטי `:20` עם `auth=1`
   **ו-`N/N`** סלוטי `:50` עם `auth=1` — עם מונה, מכנה ושם אוכלוסייה.
3. שורת `M-` ב-`METRICS` עם `נמדד` + `תוקף`, ⛔ מספר חופשי בפרוזה.
4. 🔴 **הגל נסגר ⛔ לפני `B-275`(א)** — §4 מוכיח שהנשירה היא ההגנה היחידה.

---

## 8. פריטים שעלו ו⛔ בוצעו (§10.1 · §11)

| # | ממצא | מצב |
|---|-------|------|
| 1 | התנגשות `cancel-in-progress` `true` (workflow) מול `false` (job) — §3 | ⏸️ **חסום על ניב** — `.github/` ברשימת האסור; ⛔ נצפה בפועל (`0/59` ביטולים) ⇒ סכנה רדומה. דורש `B-` חדש או הכרעה |
| 2 | «`202` דק׳ חציון» ב-`B-318` הוא **מרווח**, ⛔ **עיכוב** — §1 | ⏭️ תיקון ניסוח בשורת `B-318` בגל הביצוע |
| 3 | העיכוב האמיתי ⛔ **ניתן למדידה** מה-API (`created_at == run_started_at`, `0/59`) — §2 | ⏭️ לתעד כאילוץ, ⛔ כחוב |

⛔ **`docs/STATE.md` ⛔ נגע בגל הזה** — `15,941/16,000` בתים, `59` פנויים.
שלושת הפריטים נרשמים **בגל הביצוע**, עם הגיזום שהם מחייבים.

---

## 9. מה ⛔ נגעתי

`src/` · `api/` · `scripts/` · `.github/` · DB · `B-275` · `B-315` · `C-044` ·
⛔ נוצר workflow · ⛔ `--no-verify` · ⛔ force · ⛔ amend.
**הגל הזה: קריאה בלבד + מסמך אחד.**
