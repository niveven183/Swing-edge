# PLAN · B-321 — שארית `D-068`: «אגורות רפאים» — awaiting approval

**תאריך:** 2026-09-15 · **HEAD:** `a55c8d6` · **מזהה:** `B-321` ‹R-2›
**אבחון (§8.1 שלב 1):** `docs/audits/B321-CENTS-DIAGNOSIS-2026-09-15.md`

---

## 0. סיווג (§15)

**רמה: `T3` · תשובות: `לא`/`כן`/`לא`/`כן`/`כן`** — 3 מתוך 5.

| # | שאלה | תשובה | נימוק |
|---|------|-------|--------|
| 1 | הפיכות | **לא** | ⛔ DB · ⛔ מייל · ⛔ מחיקה. שינוי תצוגה, הפיך ב-`revert` |
| 2 | **אמון** | 🔴 **כן** | המשתמש רואה `$1,234.00` כשהערך `1234.47` — **מספר שגוי בלי לדעת** |
| 3 | אבטחה | **לא** | ⛔ `auth` · ⛔ RLS · ⛔ `api/` |
| 4 | **רוחב** | 🔴 **כן** | `SwingEdge_App.jsx` + `scripts/` + `package.json` ⇒ >קובץ קוד אחד |
| 5 | **ודאות** | 🔴 **כן** | הפרוזה ב-`BACKLOG` אמרה `7`/`37`; נמדד `14`/`43` |

⚠️ **הערכת הכותרת בפרומפט לא נמסרה — הסיווג נגזר מחמש השאלות.**
⇒ נדרש: אבחון read-only ✅ (בוצע) · עצירה לאישור ✅ (כאן) · **אימות עין/דפדפן**
לפני סגירה — ⛔ אין סגירה בלעדיו.

**§8 רמה 1 — פילטר:** DB `לא` · כסף/מייל 🔴 **כן** (כסף על המסך) · סוד `לא` ·
פרודקשן-אוטומטי `לא` · בלתי-הפיך `לא` ⇒ **הפילטר תפס.**

**§8 רמה 2:**

| ציר | תשובה |
|-----|--------|
| משתמשים | כל משתמש עם עסקה סגורה שערכה ⛔ שלם — `14` אתרים על `4` מסכים (ג'ורנל · אנליטיקה · מנטור · toast) |
| נתונים | ⛔ נגיעה. תצוגה בלבד; ⛔ מיגרציה |
| עלות | `0` — ⛔ קריאת API · ⛔ build נוסף. חוליית `verify` אחת (~שניות) |
| תקרות ספק | ⛔ רלוונטי |
| אבטחה | ⛔ סוד · ⛔ RLS · ⛔ קלט |
| תחזוקה (§3) | ✅ **מקטין** — שער מכני מחליף «לזכור לא לכתוב `fmt$(Math.round(` » |
| הפיכות | `git revert` על קומיט אחד; דקות |
| **כשל שקט** | 🔴 **זה הכשל השקט עצמו.** `.00` **נראה נכון**. ⇒ השער הוא התיקון האמיתי, ⛔ העריכה |

**פסק דין: ⚠️ בצע עם הגנה** — כל אתר מקבל אסרציית **ערך** שנצפתה אדומה, ובנוסף
`C-` לאימות עין. ⛔ **אין עריכה לפני שהשער קיים ואדום.**

---

## 1. הממצא בשורה אחת

`Math.round(X)` ערום שנכנס למעצב שכופה `minimumFractionDigits: 2` מייצר `.00` —
**הבטחת דיוק שלא נמדדה.** `fmt$` · `fmtBalance` · `fmtAccountAmount` כופים `2`;
`fmt$0` · `fmtPrice` · `.toLocaleString()` ⛔ כופים.

🔴 **המחלקה היא `14`, ⛔ `7`** — `6` ישירים + **`8` עקיפים** (payload מעוגל שנצרך
ב-`fmt$`/`fmtBalance`). התת-מחלקה העקיפה ⛔ נראתה ב-`D-068` ו⛔ ב-`B-297`.

🔴 **`:825`/`:830` (`fmtPrice(Math.round(…))`) — ⛔ במחלקה**, למרות שהצורה זהה.

---

## 2. הטבלה — `14/14` אתרים

⛔ **כל שורה נקראה בהקשרה. ⛔ תיקון-המוני.**
**המסך מושפע — בכולם `כן`** ⇒ כולם נכנסים ל-`C-` אחד.

### 2א · ישיר (`6`)

| מזהה | קובץ:שורה | מה מוצג | המשתמש רואה | תיקון מוצע |
|------|-----------|---------|--------------|-------------|
| `A1` | `SwingEdge_App.jsx:3009` | toast סגירת עסקה | `+$1,234.00` | הסר `Math.round` ⇒ `fmtAcct(closedTrade, pnl)` |
| `A2` | `:4727` | תא P&L · טבלת מנטי | `+$1,234.00` | הסר `Math.round` ⇒ `fmtAcct(t, pnl)` |
| `A3` | `:5112` | «רווח ממוצע» | `+$1,234.00` | הסר `Math.round` |
| `A4` | `:5116` | «הפסד ממוצע» | `-$1,234.00` | הסר `Math.round` |
| `A5` | `:5136` | «Max Drawdown» | `-$1,234.00` | הסר `Math.round` |
| `A6` | `:6334` | «היום הטוב ביותר» | `+$1,234.00` | הסר `Math.round` |

⚠️ **`A2` נושא תאום:** `acctRefusalText(t, Math.round(pnl))` באותה שורה. הסכום שם
⛔ מרונדר — הוא מזין **הכרעה**. ⛔ אך חייב לזוז **יחד**, אחרת שני הצדדים של אותה
שורה מכריעים על שני מספרים שונים.

### 2ב · עקיף (`8`)

| מזהה | עיגול | צרכן | מה מוצג | תיקון מוצע |
|------|-------|------|---------|-------------|
| `B1` | `:383` | `:6171` `fmtBalance` | עקומת הון | הסר `Math.round` מ-`equity:` |
| `B2` | `:523` | `:4611` `fmtBalance` | עקומת הון מנטי | הסר `Math.round` |
| `B3` | `:6190` | `:6194` `fmt$` | P&L לפי עסקה | הסר `Math.round` |
| `B4` | `:6234` | `:6248` `fmt$` | P&L לפי יום | הסר `Math.round` |
| `B5` | `:6447` | `:6534` `fmt$` | P&L לפי חודש | הסר `Math.round` |
| `B6` | `:6457` | `:6561` `fmt$` | P&L לפי רגש | הסר `Math.round` |
| `B7` | `:6476` | **`:6667` תא טבלה** | P&L לפי setup | הסר `Math.round` |
| `B8` | `:6484` | `:6721` `fmt$` | פיזור החזקה↔P&L | הסר `Math.round` |

⚠️ **`B7` הוא תא טבלה ⛔ גרף** — מי שיקרא «tooltips» יפספס אותו.
⚠️ **הסרת `Math.round` מ-payload ⛔ משנה גיאומטריה באופן נראה** — דלתא < `$1`
על ציר שמכסה אלפים. ⛔ **אין כאן `|| 0`** — הערך עובר כמות שהוא.

### 2ג · ⛔ לגעת (`23` מופעים)

`7` שומרי-אגורות (`*100)/100`) · `9` שלם-מכוון (`toLocaleString`) ·
`4` `fmtPrice(round)` · `1` `acctRefusalText` · `1` payload מת · `1` פיקסלים.
**③ אושש:** `9` השלם-מכוון הם כותרות/תקצירי הון שכבר מדפיסים `$10,000` בלי `.00`.

---

## 3. השער — `test:cents`

**קובץ חדש:** `scripts/test-cents.mjs` · **חוליה:** `package.json` ⇒ `test:cents`,
⛔ **והרשימה ב-`CLAUDE.md` §7 נגזרת מ-`package.json` ומתעדכנת באותו קומיט**
(`29 → 30`). ⚠️ זו ההזזה ה**שביעית** ו⛔ **אינה סוגרת את `B-274`**.

**חוזה:** לכל אתר — ערך עם אגורות ⇒ המחרוזת **⛔ מסתיימת ב-`.00`**.
**הביטוי מחולץ מבייטים לפי עוגן ומורץ ב-`new Function`** (תבנית `test:hydration` ·
`test:shortpct` · `test:watchlist`) — ⛔ מועתק ידנית (`R-6`).
⛔ **כשל חילוץ = אדום קשה, ⛔ דילוג** (`B-272`), נאכף בשער-מטא «עוגן ≠ 1».

**מנייה:** `14` מחלקה + `6` זרוע-ביקורת + `n` שערי-מטא.

⚠️ **זרוע הביקורת היא מה שמגדיר את הרוחב, ⛔ קישוט.** `K1`/`K2` הם
`fmtPrice(Math.round(…))` — **אותה צורה** — ו**חייבים להישאר ירוקים**.
`K5` הוא שומר-האגורות של `B-297` ⇒ מוכיח שהשער ⛔ יורה על התיקון.
**ביקורת שנהייתה אדומה ⇒ ההגדרה רחבה מדי ⇒ עצור.**

### הפלט שנצפה על העץ הנוכחי (`a55c8d6`)

```
CLASS   red: 14/14   (want 14/14)
CONTROL green: 6/6   (want 6/6)
meta-gate failures: 0
```

⇒ **אדום `14/14` לפני ירוק.** הפירוט המלא באבחון §5.

---

## 4. סדר ביצוע

| # | צעד | קומיט |
|---|-----|--------|
| 0 | תיקון שורת `B-321` ב-`BACKLOG` למדידה הנכונה (`14`/`43`) + `3` פריטים חדשים (§7 באבחון) | `docs(registry)` |
| 1 | `scripts/test-cents.mjs` + `test:cents` ב-`package.json` + §7 ב-`CLAUDE.md` `29→30`. **⛔ נגיעה ב-`SwingEdge_App.jsx`.** הדבק פלט אדום `14/14` + ירוק `6/6` | `test(B-321)` |
| 2 | `14` העריכות. `npm run verify` מלא, פלט מודבק | `fix(B-321)` |
| 3 | `C-` חדש ב-`CHECKS` — **לפני** שהגל נסגר (§14) | באותו קומיט של 2 |
| 4 | **אימות עין** — `T3` ⛔ נסגר בלעדיו | — |
| 5 | `DONE` עם hash + «איך אומת» + עמודת `T` | קומיט **הבא** (§14) |

⚠️ **צעד 1 ⛔ נבלע בצעד 2.** שער שנולד באותו קומיט של התיקון ⛔ נצפה אדום.

---

## 5. אימות עין (`C-` חדש)

⛔ **הערך ⛔ הוא המסך.** השער מריץ ביטוי ב-Node ⇒ מוכיח **ערך**, ⛔ רינדור.
JSX · React · recharts · RTL · דפדפן · פרודקשן — ⛔ מכוסים. אותו גבול בדיוק של
`C-036`·`C-038`·`C-039`·`C-041`·`C-043`.

**מה נבדק בעין — `4` מסכים:**

1. **ג'ורנל** — `A3`/`A4`/`A5`: «רווח ממוצע» · «הפסד ממוצע» · «Max Drawdown»
   נושאים אגורות אמיתיות, ו**החיבור נבדק ידנית** (§15: «מחשב את המספר בעצמו»).
2. **אנליטיקה** — `A6` + `B3`–`B8`: ריחוף על `6` גרפים + **תא `setup`** (`B7`).
3. **מנטור** — `A2`/`B2`.
4. **toast** — `A1`: סגירת עסקה שערכה ⛔ שלם.

⚠️ **מקרה קצה שחייב להיבדק:** ערך ש**כן** שלם (`$1,200.00`) חייב להמשיך להציג
`.00` — ⛔ `$1,200`. `fmt$` כופה `2` ⇒ זה אמור להישמר; ⛔ **אמור ≠ אומת.**

---

## 6. מה ⛔ בתוכנית

`fmt$` עצמו · `i18n` · `api/` · DB · `dailyPnL` · `B-322` · `B-298` · `C-046` ·
`C-047` · `C-044` · `src/components/` (נרשם כפריט נפרד).
⛔ `--no-verify` · ⛔ force · ⛔ amend.

---

## 7. ⏸️ ממתין לאישור ניב

1. **הרחבת המחלקה `7 → 14`** — התת-מחלקה העקיפה ⛔ הייתה ב-`B-321` כפי שנוסח.
   האם היא נכנסת לגל הזה, או נפתחת כ-`B-` נפרד?
2. **`A1` (toast)** — ההערה בקוד אומרת «`Math.round` נשמר בדיוק כפי שהיה».
   ההסרה **מבטלת** החלטה מגל ג׳ במפורש. מאשר?
3. **צעד 0** (תיקון שורת `BACKLOG`) — §9 אומר שקומיט התוכנית הוא התוכנית בלבד,
   ולכן השורה ⛔ תוקנה כאן. מאשר לתקן בקומיט נפרד לפני צעד 1?
