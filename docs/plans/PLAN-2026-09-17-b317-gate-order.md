# PLAN · `B-317` — סדר הבדיקה ב-`markerVerdict`

> **אבחון read-only (§8.1 שלב 1) + תוכנית (§9 שלב 2).** ⛔ **אפס תיקון** —
> `scripts/registry-test.mjs` ⛔ שונה. **HEAD בעת המדידה: `f420aa0`.**
> **רמה: T2** · תשובות `לא/לא/לא/לא/כן` (ודאות — ההנחה שבפרומפט נמדדה, ⛔ הונחה).
> **פסק:** 🔴 **הפרומפט צודק על המנגנון ו⛔ על המופע.** ענף ה-`C-0xx` אכן חסום
> מאחורי ההחזרה המוקדמת — ונמדד שהוא **מת ב-`0/42`**. ⛔ **אבל `D-085` ⛔ פסק
> שגוי שנגרם מכך**, ו**שורש שני** נמדד. ⛔ **אין המלצה כאן** — ⑦ להכרעת ניב.

---

## 0 · המצב הנוכחי של השער — נאמר לפני כל דבר אחר

```
❌ 12.4  6/19 מדדים פגי-תוקף, וכולם מכריזים ⏳ פג
      M-010 — נמדד לפני 8 יום, תוקף 7 ⇒ ⛔ אינו עובדה, וסטטוסו "✅ בתוקף"
      M-011 — נמדד לפני 8 יום, תוקף 7 ⇒ ⛔ אינו עובדה, וסטטוסו "✅ בתוקף"
      M-013 — נמדד לפני 4 יום, תוקף 3 ⇒ ⛔ אינו עובדה, וסטטוסו "✅ בתוקף"
❌ registry: 1/41 assertion(s) failed.
```

⚠️ **`test:registry` **אדום** על העץ הנוכחי — ו⛔ בגלל `B-317`.** §12.4 נופלת על
`3` מדדים פגי-תוקף. זה **קדם לגל הזה**, ⛔ נגרם ממנו, ו⛔ תוקן כאן. **§11 עצמה
ירוקה:**

```
✅ 11.1  85/85 שורות DONE נושאות רמת T
✅ 11.2  42 שורות T3 לא-רטרו · כולן נושאות אימות עין/מקור **שבוצע**
✅ 11.3  13/13 fixtures קפואים · המסווג מכריע נכון בשני הכיוונים
```

---

## 1 · ① האוכלוסייה — מה השער **מצהיר** שהוא בודק, ומה הוא בודק

**הכותרת של §11.2 מצהירה:** «`42` שורות `T3` לא-רטרו · כולן נושאות אימות
עין/מקור **שבוצע**». המילה `שבוצע` היא טענת **מדידה** — היא נבדקת **אך ורק**
בענף ה-`C-0xx` (`performed.get(r)`). נמדד:

| מדד | ערך | אוכלוסייה |
|-----|-----|------------|
| שורות `DONE` | `85` | `docs/DONE.md` |
| שורות `T3` לא-רטרו | **`42`** | המכנה של §11.2 עצמה |
| הוכרעו בלולאת `EYE` | **`42/42`** | 100% |
| הגיעו לווטו `DENIED` | **`0/42`** | ⛔ הענף לא רץ |
| **הגיעו לענף `C-0xx`** | 🔴 **`0/42`** | **ענף מת על הקורפוס החי** |

⇒ **תשובת ①:** האוכלוסייה שהשער **מצהיר** שהוא בודק היא `42`; האוכלוסייה שבה
`שבוצע` **נמדד בפועל** היא **`0/42`**. ⛔ **הפער אינו «חלק מהשורות»** — הוא
**כל** השורות. ענף ה-`C-0xx` חי ב-`11.3` (fixtures) **בלבד**.

**הפילוח בתוך ה-`42`:**

- **`23/42`** שורות מזכירות ≥1 מזהה `C-0xx` בתא האימות (`36` אזכורים סה"כ).
- 🔴 **`11/42`** מזכירות `C-` שענף ה-`C-` **⛔ היה מאשר** לו נשאל:

```
D-036 — C-026=לא-בוצעה      D-067 — C-039=לא-בוצעה
D-042 — C-028=לא-בוצעה      D-068 — C-040·C-036·C-038·C-039 = כולן הוכחשו בשורה
D-044 — C-091=⛔ אינה קיימת  D-071 — C-042=הוכחשה
D-047 — C-030=לא-בוצעה      D-075 — C-043=לא-בוצעה
D-052 — C-032=לא-בוצעה      D-082 — C-048=לא-בוצעה
D-066 — C-038=לא-בוצעה
```

---

## 2 · ② `D-085` — **מופרך.** ההחזרה המוקדמת ⛔ גרמה שם לפסק שגוי

**הטענה שנבדקה:** «`D-085` ירוקה ב-§11.2 על הסמן, ו-`C-050` ב-`2/5` ⛔ נקרא ⇒
פסק שגוי בפועל».

**החצי הראשון אושש.** `markerVerdict(D-085)` מחזיר
`{ok:true, why:"סמן מפורש: אומת בעין"}` בתוך לולאת `EYE`, ושני האזכורים
(`C-050` · `C-049`) ⛔ נקראים.

🔴 **החצי השני מופרך — נמדד, ⛔ הוסק:**

```
D-085 · C-050 @494 · denied(±90)=false · performed=true
D-085 · C-049 @657 · denied(±90)=false · performed=false
```

**`performed.get('C-050') === true`** — למרות ש-`docs/CHECKS.md:100` מצהיר
במפורש `🟡 «2/5 תת-פריטים נענו · 3/5 נותרו פתוחים — ⛔ 5/5, ו⛔ «עבר»»`.

⇒ **לו ההחזרה המוקדמת הייתה מוסרת, `D-085` הייתה נשארת ירוקה.** ההחזרה
המוקדמת היא תנאי **מספיק** ל«לא נקרא», ⛔ **הכרחי** לפסק. **הפסק לא היה
מתהפך.**

### 2.1 · השורש השני — `performedFrom` ⛔ יודע לקרוא שלמות

`performedFrom` (`:580-588`) בודק **ארבעה** תנאים: תאריך תקין · ⛔`טרם/⏳/ממתין`
בתא «נבדק» · תא «מה נמצא» ⛔ ריק ו⛔ `PENDING` · מצביע `D-` חי. ⛔ **אף אחד
מהם ⛔ קורא שלמות.** `2/5` ו-`5/5` **בלתי-נבדלים** עבורו.

**נמדד: `6/50` שורות `CHECKS` נושאות `performed=true` ובו-זמנית מצהירות על
עצמן כלא-שלמות:**

| בדיקה | ההצהרה בתא «מה נמצא» |
|-------|----------------------|
| `C-036` | 🟡 חלקית — (1)+(3) בלבד; ⛔ הפריט נשאר פתוח |
| `C-041` | 🟡 חלקית — ⓵⓶⓷ עברו · 🔴 ⓹ נכשל; ⛔ הפריט נשאר פתוח |
| `C-042` | 🔴 נכשל, ו⛔ לא על ידינו |
| `C-044` | 🔴 ⛔ אין ממצא על המסך — הפריט נפתח פתוח |
| `C-047` | 🟡 `3` תצפיות מתוך `8` סעיפים — ⛔ `8/8` |
| `C-050` | 🟡 `2/5` תת-פריטים נענו · `3/5` נותרו פתוחים |

> ⚠️ **המכנה נאמר ביושר:** רגקס-הסריקה תפס `7/50`; `C-027` הוא **ארטיפקט** —
> «נשאר פתוח» שם מתייחס לחוזה JSON ב-`B-080`, ⛔ לבדיקה עצמה. **`6/50` הוא
> המספר אחרי בדיקה ידנית של כל השבע.**

🔴 **ורדיוס-הפגיעה של השורש השני נמדד:** `6/42` שורות `T3` ירוקות **אך ורק**
משום ש-`performedFrom` קורא בדיקה חלקית כ«בוצעה» — `D-063` · `D-069` · `D-072` ·
`D-073` · `D-081` · **`D-085`**. (הדלתא בין `perf` נוכחי ל-`perf` מחמיר.)

⇒ **`B-317` בניסוחו ⛔ מכסה את השורש הזה.** הוא שואל «מי חוסם את הענף»; זה
שואל «מה הענף היה עונה לו נשאל». **פריט נפרד, ⛔ הרחבה של `B-317`.**

---

## 3 · ③ חלופה ב׳ — **⛔ פותרת את ענף ה-`C-`. נמדד, ⛔ הונח**

חלופה ב׳ = `TIGHT_NEG` + קריאת הסמנים מהתא ה**גולמי** (`cell`) במקום מ-`strip(cell)`.

```
ב׳ לבדה · אדומות: 0/42
ב׳ לבדה · ענף C-0xx הגיע לביצוע: 0/42  ⇒ הענף נשאר מת
```

⚠️ **ב׳ משנה את ה*קלט* של לולאת `EYE`, ⛔ את *מיקום ההחזרה*.** ההחזרה החיובית
נשארת בתוך הלולאה ⇒ כל מה שאחריה — הווטו **וגם** ענף ה-`C-` — נשאר בלתי-נגיש
בדיוק כמו קודם.

🔴 **וההוכחה החזקה יותר:** `V5` (ב׳ **בתוספת** צירוף) מניבה מספרים **זהים
בדיוק** ל-`V4` (צירוף **בלי** ב׳) בכל מדידה — `11/42` · `17/42` · `13/13`
fixtures. ⇒ **הצירוף הוא מה שמגיע לענף ה-`C-`; ב׳ תורמת `0`.**

⇒ **תשובת ③:** ב׳ פותרת את **הווטו בלבד**. היא **ניצבת ואורתוגונלית** לענף
ה-`C-`, ומחירה — הרפיית `strip()`, שהיא הגנת `B-120` — **⛔ קונה כאן דבר**.

---

## 4 · ④ הסדרים — כל אחד תומחר על `42` השורות **ועל `13` ה-fixtures**

⛔ **אף אחד לא הוחל על עץ העבודה.** נמדד על עותקי-מוטנט ב-`/tmp`.

| # | הסדר | אדומות (`perf` נוכחי) | אדומות (`perf` מחמיר) | fixtures |
|---|------|----------------------|----------------------|----------|
| `V0` | **baseline** — `EYE` → `DENIED` → `C-` | `0/42` | `0/42` | ✅ `13/13` |
| `V1` | **ווטו ראשון** — `DENIED` → `EYE` → `C-` | `0/42` | `0/42` | ❌ `12/13` |
| `V2` | **`C-` ראשון** — `C-` → `EYE` → `DENIED` | `11/42` | `17/42` | ❌ `12/13` |
| `V3` | **צירופי-כל** — סמן ∧ **כל** `C-` בוצע | `11/42` | `16/42` | ✅ `13/13` |
| `V4` | **צירופי-אחד** — סמן ∧ **≥1** `C-` בוצע | `11/42` | `17/42` | ✅ `13/13` |
| `V5` | **ב׳ + צירופי-אחד** | `11/42` | `17/42` | ✅ `13/13` |

**מה שנשבר, מפורשות:**

- 🔴 **`V1` (ווטו ראשון) — `0/42` שינוי, ושובר את fixture `12`.** הוא **no-op
  מוחלט על הקורפוס החי** (הווטו ממילא אינו מגיע ל-`0/42`), ומפיל את
  ⟪הכחשה רחוקה ב-`DENIED` ⛔ אינה פוסלת⟫ — **הכרעת ניב 19.08 שהוקפאה ב-`B-162`.**
  ⇒ **משלם בהחלטה נעולה ומקבל אפס.**
- 🔴 **`V2` (`C-` ראשון) — שובר את fixture `13`.** ⟪הכחשה מפורשת גוברת על מצביע
  `C` רחוק שבוצע⟫ מתהפך ⇒ **היפוך ישיר של הכרעת ניב 19.08** («הודאה מפורשת
  גוברת על ראיה עקיפה»), וכנות בתא נעשית חסרת-משקל.
- ✅ **`V3`/`V4`/`V5` שומרים `13/13`** — משום ש⛔ **אינם סדר כלל.** הם משאירים את
  `EYE` ראשון ואת הווטו במקומו, ומתנים את ההחזרה החיובית.

⇒ **תשובת ④: ⛔ אין «סדר נכון».** כל **הזזה** של הסדר או משלמת בהכרעה נעולה
(`V2`) או קונה אפס (`V1`). מה שמגיע לענף ה-`C-` הוא **תנאי**, ⛔ סדר.

---

## 5 · 🔴 החוסם שנמדד ⛔ הוצג בפרומפט — `0/36`

לפני שמתמחרים תיקון, נמדד **מה יש בתאים בפועל**:

```
שורות T3 עם ≥1 מזהה C- בתא האימות:        23/42
סה"כ מזהי C- שהוזכרו בתאי האימות:          36
מתוכם — נושאים אידיום ראיה («אומת מול»):   0/36
```

🔴 **`0/36`.** ⛔ **אף אזכור `C-` בקורפוס החי ⛔ כתוב בצורה ש-13 ה-fixtures
מניחים** (`אומת מול \`C-091\``). ה-`36` הם **פרוזה**, ובדגימה ידנית של `6`
שורות הם כמעט כולם **ההפך מראיה**:

| שורה | ההקשר של האזכור | מה זה באמת |
|------|------------------|-------------|
| `D-036` | «✅ ואומת בעין 15.08 — ניב צילם את ערוץ הדיסקורד (`C-026`)» | ✅ **ראיה אמיתית** |
| `D-066` | «JSX · React · דפדפן אמיתי **נשארים** ב-`C-038`» | 🔴 **הצהרת מגבלה** |
| `D-068` | «וזה **טרם בוצע**: **נפתח** `C-040` … כמו `C-036`/`C-038`/`C-039`» | 🔴 **פתיחת פער** |
| `D-082` | «⛔ `5/8` תת-פריטי `C-048` ⛔ נענו» | 🔴 **הצהרת אי-שלמות** |
| `D-085` | «`C-049` … ⛔ **נספרים כאן**» | 🔴 **החרגה מפורשת** |
| `D-044` | «why: אזכור בדיקה שבוצעה: **`C-091`**» | 🔴🔴 **`B-120` חי** |

🔴 **`D-044` הוא `B-120` בהתגלמותו:** התא **מתעד את ה-fixture של הטסט עצמו**
(`C-091`, קבוע שמוגדר ב-`registry-test.mjs:635` ו⛔ קיים ב-`CHECKS`). כל צירוף
שקורא `C-0\d\d` מהפרוזה **סופר את התיעוד של הטסט כראיה של המרשם**. ⚠️ **זו
הפעם החמישית ש-`B-120` חוזר** — והפעם **מתוך** התיקון המוצע, ⛔ מתוך הבאג.

⇒ **מסקנה שחייבת להיאמר:** `V3`/`V4`/`V5` **⛔ בשלים**. `11/42` האדומות שלהם
⛔ `11` תאים שקרניים — הן ברובן תאים **כנים** שמצהירים על פערים, וההחמרה הייתה
**מענישה בדיוק את הכנות** שהווטו נבנה להגן עליה (הכרעת 19.08). ⛔ **והתיקון
⛔ «לערוך `11` תאים»** — זו הרפיית-מציאות כדי לעבור ‹R-4›.

---

## 6 · החלופות — ⛔ **אין המלצה.** להכרעת ניב

| # | החלופה | אדומות | עריכת תאים | מה נשבר ב-`B-120` | רולבק |
|---|---------|--------|------------|---------------------|--------|
| **א** | **⛔ לתקן — לתעד ולסגור את הצהרת §11.2** | `0/42` | `0` | ⛔ כלום | ⛔ אין מה להחזיר |
| **ב** | **`V4` — צירופי-אחד, מיידי** | `11/42` | 🔴 `11` | 🔴 `D-044` סופר `C-091` של הטסט | `git revert` קומיט יחיד |
| **ג** | **אידיום ראיה תחילה, צירוף אחריו** | `0/42` היום | `0` מיידי | ✅ נסגר בהגדרה | `revert` ×2 |
| **ד** | **`performedFrom` מחמיר לבדו** | `0/42` | `0` | ⛔ כלום | `git revert` קומיט יחיד |

**א — «נמדד ומוכל», בהמשך ישיר להכרעת 15.09.**
⛔ נגיעה ב-`markerVerdict`. **המעשה היחיד:** תיקון **הכותרת** של §11.2 כך
שתאמר מה שהיא מודדת — «`42` שורות `T3` נושאות **סמן** אימות עין/מקור» — ⛔
«**שבוצע**». **עלות:** `0` אדומות, `0` עריכות, `0` סיכון. **מה זה קונה:** הכזב
בהצהרה מפסיק. **מה זה ⛔ קונה:** ענף ה-`C-` נשאר מת `0/42`.
⚠️ **זו ⛔ «לא לעשות כלום»** — היום ההצהרה **שקרית ב-`42/42`**, וזה ‹R-3›
בצורתו הטהורה (אימות שבודק שנכתב ⛔ שנמדד).

**ב — `V4` מיידי.** `13/13` fixtures שורדים, הווטו ⛔ זז, `B-162` שלם.
🔴 **המחיר המדוד:** `11/42` אדומות שרובן **תאים כנים**, ⛔ תאים שגויים; `D-044`
מחזיר את `B-120`; והסגירה מחייבת **עריכת `11` תאים היסטוריים** — בדיוק מה ש-⑧
של 15.09 אסרה. **רולבק:** `git revert` של קומיט אחד; ⛔ יש מצב DB ו⛔ פריסה.

**ג — שני שלבים, ⛔ אחד.**
`ג1` — **שער צורה** שדורש אידיום ראיה מפורש לציטוט `C-` כראיה
(`אומת מול \`C-0xx\``), ו**מנטרל** כל אזכור אחר. נמדד היום: `0/36` נושאים אותו
⇒ **`0/42` אדומות מיידית**, ו-`B-120` נסגר **בהגדרה** (ציטוט בפרוזה ⛔ ראיה).
`ג2` — הצירוף (`V4`) מופעל **רק** על אזכורים שעברו את `ג1`.
🔴 **מה שזה עולה, בקול:** עד ששורה תיכתב באידיום, הצירוף חל על `0` שורות ⇒
**ענף ה-`C-` נשאר מת עד שמישהו יכתוב באידיום** — כלומר `ג` **⛔ סוגרת את `B-317`
היום**, היא מייצרת **נתיב** לסגירתו. ⚠️ **ואידיום חדש הוא חוזה חדש למרשם** —
דורש שורה ב-`CLAUDE.md` §14, ⛔ רק קוד.

**ד — `performedFrom` מחמיר.** מתייחס ל**שורש השני** (§2.1) ⛔ ל-`B-317`.
`performedFrom` דוחה בדיקה שמצהירה על עצמה כחלקית ⇒ `6/50` הופכות ל-`false`.
**`0/42` אדומות היום** — משום שענף ה-`C-` מת ⇒ ⛔ צרכן. ⚠️ **ולכן ⛔ ניתן
לצפייה-אדומה על הקורפוס** בלי ב׳/ג׳ — הוא ניתן לצפייה-אדומה **ב-fixtures בלבד**,
וזו בדיוק הצורה ש-15.09 פסלה כ«אסרציה שאינה קיימת».

> 🔴 **צירוף שנשקל ונדחה מראש — «חתך תאריך»:** להחיל את הצירוף רק על שורות
> מ-`D-086` והלאה. ⛔ **זהו ‹R-4› בתחפושת** — ריכוך המכנה כדי לעבור, בדיוק כמו
> תקרת ה-100 שורות ב-`STATE`. **נרשם כאן כדי שלא ייבחר בשקט.**

---

## 7 · האסרציה — **אדום לפני ירוק, ונצפתה**

**דרישת הפרומפט:** שורה `T3` · סמן «אומת בעין» · `C-` ב-`0/N` ⇒ השער **חייב
אדום**; על העץ הנוכחי ⇒ **חייב ירוק** (זה הבאג). **בקרה:** אותה שורה עם `C-`
שבוצעה ⇒ **חייבת להישאר ירוקה**.

**נמדד** (`FIX_CHECKS`: `C-091`=בוצעה · `C-092`=⛔ בוצעה):

```
תא                                             | ציפייה | V0 (העץ הנוכחי) | V4 (טיפול)
🔴 טיפול · סמן «אומת בעין» + C- שלא בוצעה      | אדום   | ירוק ❌           | אדום  ✅
🔴 בקרה  · סמן «אומת בעין» + C- שבוצעה         | ירוק   | ירוק ✅           | ירוק ✅
🔴 בקרה  · סמן «אומת בעין» בלי C- כלל          | ירוק   | ירוק ✅           | ירוק ✅
```

✅ **הטיפול ירוק על העץ הנוכחי — זה הבאג, ונצפה.**
✅ **שתי הבקרות נשארו ירוקות בשני העצים** ⇒ האדום מודד את **התנאי**, ⛔ רוחב יתר.
⚠️ **בקרה `3` (סמן בלי `C-` כלל) היא מה ששומר על `19/42` השורות** שאין בהן
מצביע — בלעדיה כל צירוף היה מוחק את הרוב.

**לפי §9 שלב 4 — האסרציה הזו נכתבת לקובץ רק אחרי אישור ניב.** ⛔ **⛔ נכתבה.**

---

## 8 · מה נשאר פתוח — ⛔ פריט יתום (§10.1)

| # | הפריט | לאן |
|---|--------|------|
| ① | 🔴 **שורש שני: `performedFrom` ⛔ קורא שלמות.** `6/50` בדיקות `performed=true` מצהירות על עצמן כלא-שלמות; `6/42` שורות `T3` ירוקות רק בזכותו (`D-063`·`D-069`·`D-072`·`D-073`·`D-081`·`D-085`) | ⏭️ **דורש `B-` חדש.** ⛔ `B-317`, ⛔ נפתח בגל הזה (§9 — קומיט התוכנית בלבד) |
| ② | 🔴 **`0/36` אזכורי `C-` נושאים אידיום ראיה**, ו-`D-044` מצטט את `C-091` — קבוע ה-fixture של הטסט ⇒ `B-120` חוזר **מתוך** התיקון | גוף חלופה **ג**; נשאר ⏸️ להכרעה |
| ③ | 🟠 **`test:registry` אדום על §12.4** — `M-010`·`M-011`·`M-013` פגי-תוקף. **קדם לגל**, ⛔ נגרם ממנו | ⏭️ מדידה חוזרת (⛔ הארכת תוקף ‹R-4›) |
| ④ | ⚠️ **שם הקובץ נושא `2026-09-17` והיום `18.09`** — נכתב כך בפקודה מפורשת בפרומפט | לידיעה |

⛔ **`docs/STATE.md` ⛔ נגע** — `15,989/16,000`, `11` בתים.
⛔ **`B-331` · `B-327`–`B-332` · `C-050` ⛔ נגעו.**

---

## 9 · הצעד הבא

**⛔ אין המלצה.** ארבע החלופות תומחרו; הבחירה ביניהן היא **הכרעת `T3`** בדיוק
כפי שנקבע 15.09 — היא מכריעה מה המרשם **מצהיר**, ⛔ איך הוא ממומש.

**ההכרעה הנדרשת מניב:**
1. **א · ב · ג · ד** — או צירוף (`ד` **אורתוגונלית** לשלוש האחרות וניתנת לבדה).
2. אם **ב** — האם `11` עריכות התאים מאושרות, ⛔ ‹R-4›?
3. אם **ג** — האם אידיום ראיה חדש נכנס ל-`CLAUDE.md` §14?
4. האם השורש השני (§2.1) נפתח כ-`B-` נפרד, ומתי?
