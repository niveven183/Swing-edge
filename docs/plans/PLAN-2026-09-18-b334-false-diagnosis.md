# PLAN — `B-334` · שכבת ההתראות מייצרת אבחון שקרי — awaiting approval

**תאריך:** 18.09 · **קומיט בסיס:** `2058cf0` · **רמה: `T2`** · תשובות: `לא/לא/לא/לא/כן`
(⑤ בלבד — הפרומפט נושא הנחות שלא נמדדו ב-48 שעות. ⛔ הפיכות · ⛔ אמון · ⛔ אבטחה ·
⛔ רוחב-קוד, כי גל זה ⛔ נוגע בקוד.)

> ⛔ **גל אבחון בלבד.** ⛔ קוד · ⛔ DB · ⛔ מיגרציה · ⛔ `api/` · ⛔ `SwingEdge_App.jsx` ·
> ⛔ `tradingStats.js` · ⛔ הרצה חוזרת של workflow. כל מספר כאן נמדד בבייטים
> על `2058cf0`, ⛔ הוסק.

---

## §0 — מה נמדד, ומה **אינו** מה שהפרומפט הניח

הפרומפט הצהיר: «`:437` ירה משער הכניסה ⇒ `:444`/`:447` ⛔ הורצו ⇒ `handleDeleteTrade`
ו-`ConfirmProvider` ⛔ רצו». **אושש בבייטים** — `sentinel-auth.spec.js:437` זורק
`throw new Error(...)` לפני `:444` (`click` על כפתור המחיקה), ולכן הדיאלוג ⛔ נפתח
ו-`handleDeleteTrade` ⛔ נקרא. ההתראה ב-`:801-806` הצמידה לזה
`'בדוק את handleDeleteTrade ואת ConfirmProvider'`. ⇒ **ההאשמה מופנית לקוד שלא רץ.**

🔴 **וההיקף רחב מהאתר הבודד.** זו ⛔ תקלה ב-`:801` אלא **תבנית** שחוזרת ב-
`13/33` אתרי ה-`add()` בשני קבצי הסנטינל, ולכן היא מזהה נפרד (`B-335`) ו⛔ שורה
ב-`B-334`: `B-334` שואל «למה השער ירה», וזה שואל «למה הדוח *אמר* משהו אחר».
תיקון `:436` לבדו משאיר את `B-335` בחיים ב-`12` אתרים נוספים, ולהפך.

⚠️ **והתיקון ⛔ הסרת הטקסט** — ראה §5(ב): שדות `reason`/`fix` נצרכים ב-`jq`
ב-`sentinel.yml:571-572`, ומחיקתם היא שינוי workflow (§12).

---

## §1 — ① המנייה: `catch` שמצמיד `reason`/`fix` **קבועים** לשגיאה רב-שורשית

**האוכלוסייה:** כל אתרי ה-`add()` בשני קבצי הסנטינל — `27` ב-`sentinel-auth.spec.js`
+ `6` ב-`sentinel-public.spec.js` = **`33`**.
(המנייה ⛔ כוללת את `29` אתרי `add_fault` ב-`sentinel.yml` — אותה מחלקה בדיוק,
בשכבת ה-shell, ונמנית בנפרד ב-§7 כדי ש⛔ ייבלע מכנה בתוך מכנה.)

### שכבה א׳ — `reason`/`fix` מרותכים (זו האוכלוסייה שנשאלה): **`11/33`**

| # | אתר | מה מרותך | מדוע רב-שורשי |
|---|------|-----------|----------------|
| 1 | `auth:801` `delete-failed` | `fix`: «בדוק את `handleDeleteTrade` ואת `ConfirmProvider`» | 🔴 **נמדד שקרי 17.09 20:35** |
| 2 | `auth:785` `tab-switch` | `reason`: «טאב לא נטען» | `:428` click נכשל **או** `:429` timeout |
| 3 | `auth:768` `fixture-failed` | `fix`: «בדוק את `EditTradeModal` (:148-152, :402-405)» | `:459` שער-כניסה זורק **לפני** פתיחת המודל |
| 4 | `auth:747` `create-failed` | `fix`: «בדוק את `handleSubmit` ואת הכתיבה ל-Supabase» | `4` `fill` + FAB click + `toHaveCount` — `6` שורשים |
| 5 | `auth:688` `journal-render` | `fix`: «בדוק `fmtR` ו-`calcTradeMetrics`» | `:680` ספירה חד-פעמית · שורה חסרה ב-DB · `fmtR` |
| 6 | `auth:669` `sweep-failed` | `reason`: «שורת בדיקה **נשארה** ביומן» | `:657` `count()` עצמו זרק ⇒ ⛔ אושרה שורה כלל |
| 7 | `auth:646` `journal-open` | `fix`: «בדוק pageerror ורינדור הטבלה» | `:428` click **או** `:429` timeout |
| 8 | `auth:535` `ui-delete-incomplete` | `reason`: «ה-UI מסיר מה-state אך המחיקה ⛔ נשמרה» | `removed n` סופר **שני** טיקרים; `deleteRow` נגע ב**אחד** |
| 9 | `auth:394` `boundary-check-failed` | `fix`: «ודא ש-`data-boundary` עדיין נכתב» | ⚠️ **החלשה** — ה-`reason` שם **כן** כנה |
| 10 | `public:159` `render_landing` | `reason`+`fix` | `goto` · `header#top` · `h1.se-serif` |
| 11 | `public:179` `render_app` | `reason`+`fix` | `goto` · `input[email]` · `current-password` |

### שכבה ב׳ — 🔴 **`got` מרותך**, והיא **חמורה יותר**: **`5/33`**

`got` הוא השדה שה-Discord מתייג «**מה חזר**» (`sentinel.yml:570`) — כלומר הוא מציג
את עצמו כ**מדידה**. ליטרל שם הוא המצאה שאינה ניתנת להבחנה ממדידה (‹R-2› בצורתו
הטהורה), בעוד `reason` מתויג «סיבה **סבירה**» ונושא סייג מובנה.

| אתר | ה-`got` בפועל | הכשל |
|------|----------------|-------|
| `public:159` | `'הרכיבים לא נראו תוך 15 שניות'` | 🔴 `catch {` — **`e` נזרק כליל**. אם `goto` נפל, המתנה זו ⛔ רצה |
| `public:179` | `'טופס ההתחברות לא נראה תוך 15 שניות'` | 🔴 `catch {` — **`e` נזרק כליל** |
| `auth:591` | `` `סרגל הטאבים לא נראה אחרי הכניסה: ${e.message}` `` | קידומת מרותכת; אם `goto` נפל, ⛔ הייתה «כניסה» |
| `auth:646` | `` `הטבלה לא נראתה תוך 15 שניות: ${e.message}` `` | אם `:428` נפל, ה-15 שניות ⛔ נמדדו |
| `auth:658` | `'נמצאה שורת בדיקה מריצה קודמת'` | ⛔ מדפיס כמה · איזה טיקר · מאיזו ריצה |

**איחוד שתי השכבות: `13/33` אתרים נושאים לפחות שדה אחד מרותך.**
`2/33` (`public:159`·`:179`) **משמידים את אובייקט השגיאה** ⇒ השורש ⛔ ניתן לשחזור
מהארטיפקט גם בדיעבד.

### מה **לא** נכנס, ולמה — זרוע הביקורת של המנייה

`20/33` נמצאו תקינים, ו⛔ במקרה:
· **`5` אתרי `record()`** (`:208`·`:216`·`:224`·`:232`·`:240`) — ה-`got` נושא את
  הטקסט שנלכד בפועל, וה-`reason` הוא **ניסוח מחדש של המדידה** ⛔ ייחוס.
· **`:232`** מונה שורשים מפורשות: «401/403 = RLS/מפתח, 429 = rate limit, 5xx = תקלת שירות».
· **`:511`/`:543`** — ה-`reason` הוא **תוצאה** («ה-spec לא יכול לנקות אחרי עצמו»),
  נכונה בכל שורש, וה-`fix` מונה.
· **`:403`/`:417`/`:711`** — מדידה **חיובית** (`n>0`, גיאומטריה); ה-`reason`
  טאוטולוגי עם הצורה שנמדדה.
· **`:394` (`reason` בלבד) ו-`:723`** נושאים את הניסוח ה**נכון**:
  «הבדיקה עצמה נכשלה — ⛔ בהכרח המוצר». **זה התקן שאליו שאר האתרים צריכים לזוז.**

⚠️ **קיום `20` תקינים הוא מה שהופך את `13` לממצא ⛔ לסגנון.** אילו כולם היו מרותכים,
זו הייתה מוסכמה; ההבדל נמדד **בתוך אותו קובץ**.

---

## §2 — ② האם הטקסט **יכול** להיות שקרי — דוגמה קונקרטית לכל

1. **`:801` — ⛔ «יכול», אלא *היה*.** 17.09 20:35, ריצה `35271690173` על `f420aa0`:
   `:437` זרק, ההתראה אמרה «בדוק את `handleDeleteTrade`», ו-`handleDeleteTrade`
   ⛔ נקרא. במקביל `restCleanup` הדפיס `removed 1` ⇒ **השורה הייתה ב-DB**.
2. **`:747`** — שינוי שם של `#log-stop` ⇒ `:740` `fill` זורק. ההתראה: «בדוק את
   `handleSubmit` ואת כתיבת `trades` ל-Supabase». הטופס ⛔ הוגש; `handleSubmit`
   ⛔ רץ; Supabase ⛔ נגעה. ⇒ יום חיפוש במסלול כתיבה תקין.
3. **`:688`** — `:680` הוא `await rows.count()` חד-פעמי מיד אחרי `openJournal`.
   קריאה בזמן commit מחזירה `2` ⇒ `n < 3` ⇒ ההתראה: «ייתכן ערך `null` שמפיל את
   החישוב · בדוק `fmtR` (`utils.js:106`) ו-`calcTradeMetrics` (`:38`)». ⇒ ההתראה
   שולחת ישירות לקוד ה**כסף**, על מירוץ ב-harness.
4. **`:768`** — `:459` הוא אותו שער חד-פעמי של `:436`. הוא זורק **לפני**
   `:461` (click על «עריכה») ⇒ ההתראה «העריכה נשברה · בדוק את `EditTradeModal`
   (:148-152, :402-405)» מצביעה על מודל שמעולם ⛔ נפתח.
5. **`:785`/`:646`** — `:428` הוא `click` בלי המתנה. כשלו ⇒ `got` מצהיר
   «הטבלה לא נראתה תוך 15 שניות», המתנה ש⛔ התחילה.
6. **`:535`** — `CLEANUP_FILTER` הוא `ticker=in.(SNTNL,SNTNL1)` (`:487`), ואילו
   `deleteRow` נקרא על **טיקר אחד** (`:798`). מסע שקרס בין ה-create ל-rename
   משאיר `SNTNL` בעוד `SNTNL1` נמחק ב-UI ⇒ `uiDeleteOk=true` **וגם** `n=1`
   ⇒ ההתראה «ה-UI מסיר את השורה מה-state אך המחיקה ב-Supabase לא נשמרה»
   מאשימה את `handleDeleteTrade` על שורה שה-UI **מעולם ⛔ כיוון אליה**.
7. **`public:159`** — `/` מחזיר `504` מ-Vercel ⇒ `page.goto` זורק. ההתראה:
   «הרכיבים לא נראו תוך 15 שניות — הדף לא רונדר · **בדוק שה-JS נטען**».
   ⛔ היה JS, ⛔ היה דף, ⛔ היו 15 שניות. ⚠️ **ו-`e` נזרק** ⇒ ה-`504` ⛔ מופיע
   בשום מקום בארטיפקט.
8. **`:394`** — הליטרל היחיד שם הוא ה-`fix`. שגיאה מ-`page.locator().count()`
   יכולה לנבוע מ-context סגור או מניווט באמצע; ההנחיה «ודא ש-`data-boundary`
   עדיין נכתב» היא שורש אחד מתוך שלושה. ⚠️ **הקלה ביותר, ומובאת כדי שהמנייה
   ⛔ תנופח** — ה-`reason` שם כבר כתוב נכון.

---

## §3 — ③ מנייתם של `count()` חד-פעמיים — מחלקת `B-334`

**`15` אתרי `.count()`** בשני הקבצים (`:87` היא הערה ו⛔ נספרת), מול **`4`**
בלבד של `expect(...).toHaveCount(...)` החוזר (`:448`·`:474`·`:475`·`:744`).

| תת-קבוצה | מנייה | אתרים |
|-----------|-------|--------|
| מדידה/לוג בלבד — ⛔ שער | `4` | `:310` `:311` `:312` `:315` |
| **שער שמכריע** ⇒ מחלקת `B-334` | **`11`** | להלן |

### `11` השערים, מפוצלים לפי **כיוון הכשל**

| כיוון | מנייה | אתרים | מה קורה כשה-DOM ⛔ התייצב |
|-------|-------|--------|---------------------------|
| 🔴 **אדום-שקרי** | `5` | `:436` `:442` `:459` `:680` `:684` | זורק ⇒ התראה על באג שאינו קיים |
| ⚠️ **ירוק-שקרי** | `5` | `:392` `:416` `:657` `:665` `:703` | **⛔ מדווח כלום** ⇒ הבדיקה מדולגת בשקט |
| מעורב | `1` | `:362` | `0` ⇒ «הטאב אינו קיים» (צהוב) **וגם** הגבול ⛔ נבדק |

🔴 **החצי המסוכן הוא ה*ירוק*, והוא היחיד שאין לו תסמין.** `:392` (`data-boundary`)
ו-`:416` (`role="alert" h1`) הם **כל** בדיקת קריסת-הגבולות שנולדה ב-`B-304`:
ספירה חד-פעמית שרצה לפני שהפאנל התייצב מחזירה `0`, `0` הוא התשובה הירוקה,
ו-`sweepBoundaries` ממשיך הלאה. ⇒ `5` פאנלים «ירוקים» בלי שנמדד ולו אחד.
⚠️ **זו בדיוק צורת `07.09`** שבגללה `B-304` נבנה, ובדיוק ההערה ב-`:357`
(«הסריקה ראיה **רק** בזמן שהמתקן חי»).
⚠️ **`:703` באותה צורה** — `card.count()===0` מדלג על **כל** בדיקת באנר ההסכמה,
כולל ההשוואה ל-FAB, בלי ממצא.

⚠️ **`:442` ⛔ יכול לירות עצמאית** — הוא קורא את אותו תת-עץ ש-`:436` כבר פתר
באותו tick. נספר בצורה ⛔ בסיכון.
⚠️ **`:684` נמדד בטוח מ-`B-312`** — הוא `hasText: sym` (substring), והצורה
שנשברה ב-`B-312` הייתה `\b…\b`; תג ה-`TickerLogo` ⛔ שובר substring.
הסיכון שלו הוא **תזמון בלבד**.

---

## §4 — ④ האם `:429` צריכה להמתין לשורה הראשונה

**⛔. והתשובה חשובה יותר מהשאלה.**

`:429` ממתינה ל-`[data-testid="journal-table"]`. נמדד ב-`SwingEdge_App.jsx`:
`:5246` מרנדר **מסך-ריק במקום הטבלה** כש-`trades.length === 0`, ולכן «הטבלה
נראית» **כן** גורר `trades.length > 0`. ⇒ ההמתנה ⛔ עיוורת לגמרי.

🔴 **אבל היא נכשלת מסיבה אחרת:** הג'ורנל נושא `3` עסקאות קבע
(`AAPL`·`NVDA`·`BTC-USD`) **תמיד**. ⇒ הטבלה נראית מהשלוש הללו בלבד, בזמן
ש-`SNTNL1` עוד לא נכנס ל-`sortedFilteredTrades` (`:5326`). ⇒ «הטבלה נראתה»
נושאת **אפס מידע** על השורה ש-`:436` עומד לחפש.

⚠️ **ולכן «המתן לשורה הראשונה» הוא אותו כשל בתחפושת** — הוא ייענה ע"י `AAPL`.
**הדפוס הנכון הוא להמתין ל*שורה שעליה עומדים לפעול*, ⛔ לשורה כלשהי** — כלומר
`expect(rowsFor(page, sym)).toHaveCount(1, { timeout })` **במקום** `:436`,
שהיא חלופה (א). ⇒ ④ ⛔ מוסיפה עבודה מעבר ל-(א); היא **מסבירה למה (א) היא
הצורה הנכונה** ולמה חיזוק `:429` לבדו ⛔ סוגר כלום.

---

## §5 — החלופות · ⛔ בוצעה אף אחת

### (א) `toHaveCount(1, { timeout })` ב-`:436`

| ציר | תשובה |
|-----|--------|
| **מה ההתראה תאמר ב-20:35** | ⚠️ **ברוב המקרים — כלום**, כי `15s` retry היו בולעים את המירוץ ⇒ ריצה ירוקה. בכשל אמיתי: `Expected: 1 / Received: 0` של Playwright, ו-`reason`/`fix` **⛔ משתנים** ⇒ עדיין «בדוק את `handleDeleteTrade`» |
| **מה נשבר** | תקציב הזמן. `test.setTimeout(180_000)` (`:559`), ו-`deleteRow` נקרא עד `3`× (`:665` בלולאת הסחף ×2 + `:798`) ⇒ עד `+45s` בכשל. ⚠️ timeout שחורג **קוטע את הממצאים** (ההערה ב-`:556-557` מתעדת בדיוק את זה) |
| **רולבק** | שורה אחת. מיידי |
| **פסק** | ⚠️ **נחוצה ו⛔ מספיקה** — היא מסלקת את ה**טריגר** ומשאירה את **מנגנון** האבחון השקרי חי ב-`12` אתרים |

### (ב) `reason`/`fix` ⛔ קבועים ⇒ ההתראה מדווחת **מה נמדד** בלבד

| ציר | תשובה |
|-----|--------|
| **מה ההתראה תאמר ב-20:35** | «מה נבדק: מחיקת עסקת הבדיקה · **מה חזר**: `expected exactly 1 SNTNL1 row, found 0` — השער ב-`:436` זרק; **המחיקה ⛔ נוסתה** · **מועמדים**: (1) השורה ⛔ רונדרה (2) הכתיבה ⛔ נחתה (3) העוגן זז». 🔴 **בניסוח הזה ⛔ הייתי בונה פרומפט על `handleDeleteTrade`.** |
| **מה נשבר** | 🔴 `sentinel.yml:571-572` קורא `.reason`/`.fix` ב-`jq` לתוך embed קבוע. **מחיקת השדות ⇒ בולטים ריקים בדיסקורד ⇒ שינוי workflow (§12)**. ⚠️ **ולכן הצורה המומלצת היא «לגזור, ⛔ למחוק»**: השדות נשארים, ה-`reason` מצהיר מה נמדד ומה ⛔, וה-`fix` מונה מועמדים ⇒ **ה-yml ⛔ נוגע** |
| **רולבק** | revert ל-spec בלבד. מיידי |
| **פסק** | ✅ **הרחבה הגדולה ביותר ליחידת מאמץ** — `13/33` אתרים, ⛔ `1` |

### (ג) שניהם

🔴 **המומלץ — ובסדר `ב׳ ← א׳`.**
**למה ב׳ ראשונה:** (א) לבדה הופכת את הריצה לירוקה ⇒ **מוחקת את הראיה** ל-(ב)
ומשאירה את `12` האתרים האחרים לייצר את ההתראה הבאה. (ב) ראשונה מבטיחה שהאזעקה
ה**באה**, מאיזה אתר שתגיע, תהיה קריאה — גם אם (א) תתעכב.
**מה ההתראה תאמר ב-20:35 תחת (ג):** ⛔ כלום (המירוץ נבלע), ובכשל אמיתי —
הניסוח של (ב) עם `15s` מדודות מאחוריו.

⚠️ **ו⛔ אחת מהשלוש נוגעת ב-`11` שערי ה-`count()` של §3** — היא מתקנת `1` מתוך
`5` האדומים-שקריים ו-`0` מתוך `5` הירוקים-שקריים. **החצי הירוק נשאר פתוח**
(§7, `B-334`).

---

## §6 — האסרציה: מה **ניתן** לצפות אדום ומה ⛔

🔴 **הפרומפט צדק בחצי, והחצי השני הוא ההבדל.** צריך להפריד בין שני דברים
שקל לבלבל:

| מה | ניתן לצפייה אדומה? | למה |
|-----|---------------------|------|
| ה**מירוץ** של 20:35 (ה-DOM החזיר `0`) | ⛔ **לא — חסום** | תלוי במהירות runner · רשת · תזמון commit של React. ⛔ דטרמיניסטי |
| ה**פגם בקוד** (שער ⛔-חוזר · פרוזה מרותכת) | ✅ **כן, דטרמיניסטית** | תכונה של הבייטים בקובץ |

**⇒ ⛔ נכתבת «אסרציה» שתמיד ירוקה, ו⛔ מומצא שחזור למירוץ.** במקום זה:

**`A-מירוץ` — «⛔ נצפה אדום — חסום».** הכיוון שהוצע (הזרקת השהיה ברינדור)
דורש דפדפן אמיתי ⇒ מקומו ב-`probe:` **מחוץ לשרשרת**, בדיוק כמו `probe:boundary`
ו-`test:smoke`. ⛔ נבנה בגל הזה, ונרשם ככזה.

**`A-צורה` — ✅ אדומה היום.** לדג׳ר דו-כיווני בתבנית `test:cents`: `13` טביעות-אצבע,
כל אחת מצהירה `מרותך` / `נגזר`; ההרנס קורא את הבייטים ומודד. **היום `13/13`
מרותכים ⇒ אדום.** אחרי (ב) ⇒ ירוק, ומי שמתקן אתר בלי להזיז את שורת הלדג׳ר
**מקבל אדום**.
⚠️ **ומה שהיא ⛔ יכולה לדעת:** האם הטקסט הנגזר הוא אבחון *טוב*. היא מודדת
**צורה** — בדיוק אותו גבול של `C1`–`C12` ב-`test:equitystate`. ⛔ לקרוא ירוק
שלה כ«ההתראות אמינות».
⚠️ **והיא ניתנת למשחק** ב-`` reason: `${e.message}` `` (כפילות של `got`)
⇒ הלדג׳ר חייב לפנות ל**טביעת אצבע**, ⛔ ל«יש אינטרפולציה».

**`A-שער` — ✅ אדומה היום.** `:436` הוא `await rows.count()` ⇒ אדום;
`toHaveCount` ⇒ ירוק. ⚠️ **ו-`11` השערים של §3 נכנסים ללדג׳ר באותו כיוון הפוך**,
כך שהחצי ה**ירוק-שקרי** ⛔ נשכח.

⚠️ **עלות שרשרת שיש להכריע:** חוליה חדשה מעלה `31 → 32`, והמספר ב-`CLAUDE.md` §7
נכתב **ביד** ⇒ זו ההזזה ה**תשיעית** ו⛔ סוגרת את `B-274`. ⚠️ **וקבצי הסנטינל
יושבים ב-`tests-sentinel/`** — `test:syntax` פורס `scripts/` ו-`api/` בלבד
⇒ ⛔ מכסה אותם היום.

---

## §7 — רישום · §10.1 «אין פריט יתום»

⛔ **`docs/STATE.md` ⛔ נגע** — `15,989/16,000` בתים, `11` פנויים.
⇒ הרישום ב-`BACKLOG` (⛔ תקרה, §14), וזו ⛔ עקיפה: §14 קובע שפריט פתוח שאינו
פעיל חי שם.

| פריט | מנגנון | יעד |
|------|---------|------|
| `B-334` — `:436` שער ⛔-חוזר · `:429` ממתינה לטבלה | ‹R-2› | **שורה קיימת** — מורחבת ב-§3/§4: `11` שערים, `5`+`5`+`1` |
| 🆕 `B-335` — `13/33` אתרי `add()` נושאים פרוזה מרותכת | ‹R-2› | **חדש** |
| 🆕 `B-336` — «סטאפ הכי טוב» בניתוח-ביצועים עדיין מקבל `Unknown` | ‹R-2› | **חדש** |
| `29` אתרי `add_fault` ב-`sentinel.yml` | ‹R-2› | נמדד ⛔ סווג — **שורה ב-`B-335`**, מחוץ למכנה `33` |
| `B-331` — ⑤ המתקן המתוקן · ⑥ `7` אתרי argmax · ⑦ חסימה | — | **כבר רשום** ב-18.09, ראה §8 |

### 🆕 `B-336` — ממצא **חדש** מסריקה סביבתית (§11), ⛔ תוקן

`B-326` ⓖ הוסיף סינון `Unknown` ב-`SwingEdge_App.jsx:766-769` — ב-
`generateSmartLessons` **בלבד**. נמדד היום ב-`:6316-6319`, כרטיס «הסטאפ הכי טוב»
בניתוח-הביצועים:
```
const bestSetup = [...stats.bySetup]
  .filter(s => s.count > 0)
  .sort((a, b) => (b.winRate - a.winRate) || (b.count - a.count))[0];
```
`count > 0` ⛔ מחריג את `Unknown` — הליטרל ש-`tradingStats.js:417` מייצר.
הרינדור (`:6356`) הוא `labelFor("setup", bestSetup.setup, lang)` ⇒
🔴 **הכרטיס יכול להציג `Unknown` כסטאפ החזק ביותר — היום, בפרודקשן.**
⚠️ **וההערה ב-`:6317-6318` מצהירה «so both name the same setup»** ⇒ ההצהרה
הפכה **שקרית** ב-`8d631ec`: הכרטיסים כעת יכולים לנקוב בשני סטאפים שונים.
⚠️ **`B-336` ⛔ `B-331`** — `B-331` הוא **סדר** (argmax לפני שער), וזה
**אוכלוסייה** (`Unknown` מועמד). ⚠️ **ו⛔ `B-326`**, שנסגר על אתר אחד.

---

## §8 — ⑤⑥⑦ של `B-331`: **כבר רשומים** — ⛔ נכתבו שוב

⚠️ **§14 אוסר עותק שני** («פריט חי בקובץ אחד בלבד») ⇒ להלן **הצבעה**, ⛔ שכפול.

- **⑤ — המתקן המתוקן.** כבר רשום פעמיים, שתיהן מ-18.09:
  `BACKLOG:828` (סוף עמודת המדידה) ו-`PLAN-2026-09-18-b331-argmax-gate.md:20-26`
  ו-`:141-162`. שני המקומות נושאים במפורש:
  `[{פולבק,n:1,wr:100},{Unknown,n:2,wr:0}]` ⇒ `null` ב**שני** העצים (⇒ ⛔ שמיש),
  מול `[{פריצה,n:5,wr:80},{פולבק,n:1,wr:100}]` ⇒ `null` היום · `פריצה` אחרי.
  🔴 **ו-`:26` של התוכנית כבר מצהיר שהמתקן שנדרש היה היפוך `B-326`.**
  ⇒ **⛔ נדרשת כתיבה. ⛔ כתבתי.**
- **⑥ — מניית ה-argmax.** כבר רשומה ב-`PLAN-…-b331-argmax-gate.md:48-59` כטבלה:
  **`7` אתרים · `1` משתיק · `3` בלי שער · `3` בוררים**.
  ✅ **אומת עצמאית היום** בקריאת בייטים על `4/7` שב-`SwingEdge_App.jsx`:
  `:770→:772` (משתיק, `count>=2` על הנבחר) · `:6309` (`.sort(totalPnL)[0]`, ⛔ שער) ·
  `:6316` (`filter(count>0)`, ⛔ שער מדגם) · `:6326` (רשימה-לבנה, ⛔ מדגם).
  ⇒ **`1` משתיק · `3` בלי שער — תואם.** `3` הבוררים (`EdgeFinder.js:93/146` ·
  `AdaptiveLessons.js:128`) ⛔ נמדדו מחדש בגל הזה.
  🆕 **ומה ש⛔ היה שם: `Unknown` חי ב-`:6316`** ⇒ `B-336` (§7).
- **⑦ — `B-331` פתוח וחסום.** `BACKLOG:828` כבר נושא «🔴 **פתוח — וחוסם את ⓑ של
  `B-326`**». **החסימה על `B-334` היא חדשה ונכתבת בגל הזה** — נימוקה:
  `B-331` יסגר באסרציית **ערך** על ג'ורנל סינתטי, ואילו `C-050` (אימות-עין,
  `2/5`) דורשת מסך פרודקשן; כל עוד הסנטינל מייצר אבחון שקרי, **תצפית מהסנטינל
  ⛔ יכולה לשמש כאישוש** לאף אחד מהשניים.

---

## §9 — מה נדרש מניב

1. 🔴 **איזו חלופה — א׳ · ב׳ · ג׳ (§5).** ההמלצה: **(ג), בסדר `ב׳ ← א׳`**.
2. 🔴 **החצי ה*ירוק-שקרי* של §3 (`5` אתרים) — האם בגל הזה או נפרד?** הוא ⛔ הפיק
   התראה מעולם, ולכן ⛔ יופיע כתסמין. ⚠️ **`:392`/`:416` הם *כל* ההוכחה של `B-304`.**
3. **`B-335` — האם הלדג׳ר (`A-צורה`) נכנס לשרשרת?** עלות: `31 → 32` ⇒ הזזה ידנית
   תשיעית ב-§7 (`B-274`), ו-`test:syntax` ⛔ מכסה `tests-sentinel/`.
4. **`B-336` — דירוג.** הוא פונה-משתמש **עכשיו** ו⛔ תוקן.
5. **`A-מירוץ`** — האם לבנות `probe:sentinel-race` (מחוץ לשרשרת) בכלל.
   ⛔ **⛔ נבנה. ⛔ הורץ workflow.**

---

## §10 — טבלת §8 רמה 2

**פילטר רמה 1:** DB ⛔ · כסף/מיילים ⛔ · סוד ⛔ · פרודקשן-אוטומטי ⛔ · בלתי-הפיך ⛔.
⇒ **חמש «לא» — טבלת רמה 2 ⛔ נדרשת**, וגל זה ⛔ מציג אותה.
⚠️ **ההמלצות עצמן (§5) נוגעות בקוד שרץ בפרודקשן אוטומטית כל שעה** ⇒ הפילטר
ייתפס ב**גל הביצוע**, ⛔ בזה.
