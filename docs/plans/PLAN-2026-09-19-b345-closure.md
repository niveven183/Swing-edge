# PLAN 2026-09-19 — סגירת `B-345` + `B-347` + `B-348`

**רמה: `T3` · תשובות: לא / כן / לא / לא / כן** (2/5 ⇒ `T3`; ⛔ נורה טריגר-יחיד).
אין מסך מושפע — הגל הוא **docs בלבד** ⇒ האימות הוא **אומת-מקור** (§15).

**שער בטיחות:** `PULL_EXIT=0` · `git status --porcelain` ריק ·
`npm run test:instrument` `519/519` `EXIT=0`.

**אושר ע"י ניב 19.09 עם שלושה תיקונים**, שכולם מוטמעים כאן.

---

## §1 — המדידות שעליהן התוכנית נשענת

כל המדידות read-only, מהמודולים ה**אמיתיים** (⛔ סטאבים).

### 1.1 · מאיזה משתנה נגזר כל מספר בכרטיס

שני המספרים חוזרים מ**קריאה אחת**, `SwingEdge_App.jsx:2729-2734`:

```js
const sizing = sizePosition({ entry: form.entry, stop: form.stop, capital, riskPct,
  sharesOverride: form.shares ?? "", rate: formRate, refusalReason: formRefusal });
```

| מספר | מקור |
|---|---|
| `posSize` / `suggestedShares` | `src/lib/positionSizing.js:70` · `:84` — `floor((capital × riskPct/100) / riskPerShare)` |
| אחוז-הסיכון-בפועל | `src/lib/positionSizing.js:93` — `effRiskPct = (effPotLoss / capital) × 100` |
| השער שמדליק את האזהרה | `SwingEdge_App.jsx:2769` — `isOverRisk` |
| הרינדור | `SwingEdge_App.jsx:8052` |

### 1.2 · המרת מטבע — ⛔ אסימטריה

שניהם חולקים את אותו `capital` ואת אותו
`riskPerShare = |entry−stop| × rate` (`positionSizing.js:67-68`).
⛔ **קיים מסלול שבו אחד מומר והשני ⛔.**

### 1.3 · ההנחה «שני בסיסים באותו כרטיס» — **הופרכה**

`effShares = posSize` ⇒ `posSize × rps ≤ תקציב` ⇒ `effRiskPct ≤ riskPct` **תמיד**.
נמדד על **109,500 אוכלוסיות** (הון `100`…`200,000` × 5 צמדי מחיר × 5 אחוזי סיכון
× 3 שערים): `isOverRisk` נכון **0/109,500** בלי דריסה · יחס
`effRiskPct/riskPct` מקסימלי `1.000000`.

⇒ **`6` על המסך הוא דריסה ידנית, ⛔ ההמלצה.** ההמלצה הייתה **`1`**.
**ניב אישר בכתב 19.09** שהקליד `6` ידנית.

שני שחזורים שמייצרים את הצילום בדיוק (`SHARES 6` · `MAX RISK $129.24` · `3.9%`):

| תרחיש | הון | `rate` | `posSize` | MAX RISK | `effRiskPct` |
|---|---|---|---|---|---|
| A | `$3,314` | `1` | **1** | `$129.24` | `3.90%` |
| B | `₪10,000` | `3.0333` | **1** | `$129.24` | **`3.92%`** |

### 1.4 · התג «מטבע לא מאומת» — **שורש שני, ⛔ אותו שורש**

`UnverifiedCcyChip` (`SwingEdge_App.jsx:892-893`) נדלק **רק** על `isUnverified`.
נמדד מ-`deriveInstrumentCurrency` ה**אמיתי**:

| שורה | `state` | `reason` | שבב |
|---|---|---|---|
| בלי `currency` | `assumed` | `no_evidence_against` | `false` |
| `currency: 'USD'` | `assumed` | `no_evidence_against` | `false` |
| `currency: 'ILS'` | `contradicted` | `ils_never_measured` | **`true`** |

⇒ השורש הוא **`B-340`** (`:2918` חותם `currency: capitalCurrency`).
⚠️ **והתג מכריע בין A ל-B** — הוא מוכיח `capitalCurrency === 'ILS'` ⇒ **תרחיש B**.
⇒ **שניים, ⛔ אחד.**

### 1.5 · תיקון 3 — הציפייה הנעולה ב-`C-055` ⓷

`C-055` ⓷ מצפה ל-`6` בתרחיש `NBIS` `217.99`/`202`/**`₪10,000`**/`1%`.
`riskPerShare` (נייר) `= 15.99`. נמדד:

| תצורה | `riskPerShare` בהון | `posSize` |
|---|---|---|
| `₪10,000` · `rate 3.0333` | `48.50` | **`2`** |
| `₪10,000` · `rate 3.70` | `59.16` | **`1`** |
| `$10,000` · `rate 1` | `15.99` | **`6`** |

⇒ **`6` מושג רק ב-`rate = 1`, כלומר הון `$10,000` — והטקסט נוקב `₪`.**

🔴 **וההשערה «מיושן פוסט-`B-339`» הופרכה:** `B-339` (`a7a7d5a`) היה **תצוגה בלבד**
ו-`positionSizing.js` ⛔ נגעה — ההמרה-לפני-החלוקה היא **סיבת קיום המודול**
(`positionSizing.js:15-27`) וקדמה ל-`C-055`. ⇒ זו **שגיאת יחידה בציפייה עצמה
בזמן הכתיבה**, ⛔ סחיפה שנגרמה מ-`B-339`.

---

## §2 — חמשת הקבצים

⛔ **קוד מוצר ⛔ נוגע בגל הזה.**

| # | קובץ | תוכן |
|---|---|---|
| 1 | `docs/audits/B-347-shares-override-2026-09-19.md` | דוח אבחון §8.1 — כל §1 לעיל |
| 2 | `docs/DONE.md` | `D-088` · `B-345` · hash **`d41cf18`** · `T3` · **אומת-מקור** |
| 3 | `docs/BACKLOG.md` | `B-345` יורד · **`B-347`** · **`B-348`** נפתחים |
| 4 | `docs/CHECKS.md` | `C-055` — עדכון מניין בלבד; **הטקסט הנעול ⛔ נוגע** |
| 5 | `docs/DECISIONS.md` | שורת 19.09 — על מה `B-345` נסגר |

### 2.1 · מה בדיוק ייכתב על `C-055` (תיקונים 1+2)

**המניין: `2/5` נענו · `1/5` נצפה.** ⛔ `3/5`.

| סעיף | מצב |
|---|---|
| ⓵ | **נענה** — מכוסה ברמת **ערך בלבד** (`test:instrument` 21.1 `A1`; `CA-1` נצפתה אדומה) · ⛔ **מסך** |
| ⓶ | **נענה ו⛔נצפה** — 19.09 11:14, שדה `6` לפני שמירה · שורת `NBIS` בג'ורנל `6`. שוויון |
| ⓷ | 🔴 **⛔ נבדק כלל** — ה-`6` שנצפה היה **דריסה ידנית**, ⛔ ההמלצה. ⓷ דורשת שה**המלצה** תשרוד «העתק לטופס»; זה ⛔ קרה. ⛔ **«חלקית»** |
| ⓸ | **נענה בערך** (21.4 `K2`/`K4`; `CA-2` אדומה) — ⛔ נספר במניין המסך |
| ⓹ | ⛔ **נבדק** |

⚠️ **המניין `2/5` מונה את ⓵+⓶ — שני הסעיפים של מדד הסגירה.**

### 2.2 · `B-347` — ‹R-1›

**הדריסה ⛔ נבדלת מההמלצה על המסך.** `:7968` מרנדר
`sharesOverrideStr !== "" ? sharesOverrideStr : String(suggestedShares)` —
**אותו סטיילינג לשני המצבים**, וההמלצה עצמה (`1`) ⛔ מוצגת לצד הדריסה (`6`).
`hasSharesOverride` (`:2759`) קיים ומרונדר ב-`:8001` כקישור «חזור להמלצה»
ש⛔ נוקב **מה** ההמלצה.

⇒ נתון (`6`) נוסע בלי **תעודת מקור** — «זו המלצה» מול «זה מה שהקלדת» —
והמשתמש קורא דריסה כעצה. **`R-1`.**

**מדד סגירה:** אסרציית **ערך** שנצפתה אדומה **וגם** `C-` לאימות-עין.
**קישורים, ⛔ מיזוג:** `B-340` (התג) · `B-343` (תרחיש B מסביר «$ על הון ₪»
כהתנהגות **תקינה** פוסט-`B-339`) · `B-339` (סגור).

### 2.3 · `B-348` — הציפייה הנעולה סותרת מדידה

`C-055` ⓷ נוקבת `₪10,000 ⇒ 6`; מדידה נותנת `2` (`rate 3.0333`) או `1`
(`rate 3.70`). `6` דורש `rate = 1` ⇒ הון `$10,000`.
**מדד סגירה:** הכרעת ניב — או תיקון היחידה בטקסט הנעול, או מדידה חוזרת
שמראה שהערך תקין. ⛔ **הטקסט הנעול ⛔ נוגע בגל הזה.**

---

## §3 — מה ⛔ ייגע

⛔ קוד מוצר · ⛔ `docs/STATE.md` · ⛔ `docs/NEXT.md` ·
⛔ שחרור טקסט נעול ב-`C-055` · ⛔ מיזוג `B-347`/`B-348` ל-`B-339`/`B-343`.

## §4 — CLOSE

`npm run test:registry` **ערום** · `npm run verify` **ערום** · שניהם `EXIT=0`
נלכד ב-`; echo "EXIT=$?"` (⛔ צינורות, `INCIDENTS#25`) · commit + `git push
origin main` · ⛔ **force**.

## §5 — חוב ידוע (אושר ע"י ניב כחוב)

1. **§14 מחייב עדכון `docs/NEXT.md` בסגירת גל, והפרומפט אוסר** ⇒ `NEXT` יישאר
   מצביע על `B-345` שנסגר.
2. **`docs/STATE.md` = 15,989/16,000 בתים (11 פנויים)** ⇒ `B-347`/`B-348`
   ינחתו ב-`BACKLOG` בלבד, ו-`STATE` ⛔ ידע על סגירת `B-345`.
