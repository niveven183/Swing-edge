# PLAN-2026-09-20 — מחיקת טאב המחברת

**סטטוס:** ⏸️ awaiting approval (§9 שלב 2 — נכתב, נדחף, ועוצר).
**רמה: T3 · תשובות: לא / כן / לא / כן / לא** (הפיכות · אמון · אבטחה · רוחב · ודאות).

- **הפיכות — לא.** ⛔ אין `drop table`, ⛔ אין מחיקת שורות. הטבלה `public.journal_notes`
  נשארת **בדיוק כפי שהיא**; מה שנמחק הוא קוד, והוא חוזר ב-`git revert`.
- **אמון — כן.** `FeedbackTab.jsx:87` הוא `t[TAB_LABEL_KEYS[originTab]] || originTab` —
  מסלול שנופל ל**מחרוזת גולמית** בלי להודיע. הסרה חלקית (i18n בלי המפה) מייצרת תווית
  אנגלית גולמית למשתמש עברי, **בשקט**.
- **אבטחה — לא.** ⛔ `auth` · ⛔ RLS · ⛔ `api/` · ⛔ סוד.
- **רוחב — כן.** **6** קבצי קוד + מחיקת קובץ שביעי.
- **ודאות — לא.** כל הנחה נמדדה היום (20.09) בבייטים; הסעיף הבא הוא המדידה.

⚠️ **§8 רמה 1 — הפילטר ⛔ תפס דבר:** ⛔ שינוי DB · ⛔ כסף/מיילים · ⛔ סוד · ⛔ ריצה
אוטומטית בפרודקשן · ⛔ בלתי-הפיך ⇒ **⛔ טבלת רמה 2**, לפי הכלל.
⚠️ **`T3` ⛔ יורד** (§15) ⇒ סגירה מחייבת **אימות עין** בדפדפן, ⛔ `build` ירוק.

---

## 1 · מה נמדד (⛔ הונח)

### שאילתה (א) — `public.journal_notes` בפרודקשן, 20.09

| שדה | ערך |
|------|------|
| `notes_total` | **0** |
| `users_with_notes` | **0** |
| `first_note` | `null` |
| `last_note` | `null` |
| `notes_last_30d` | **0** |

**היחס: `0/57` משתמשים רשומים אחרי סינון חשבונות בדיקה = 0%.**

🔴 **ולכן תבנית `B-005`/`B-121` — הודעה מוקדמת לבעלי רשומות לפני כל `drop` — ⛔ מופעלת.**
⛔ **אין למי להודיע:** `0` שורות, `0` משתמשים, `0` ב-30 הימים האחרונים. ⚠️ **וזה ⛔ מרשה
`drop`** — הטבלה נשארת (§3), וההודעה המוקדמת תידרש ביום שבו תישקל מחיקתה, ⛔ היום.

### משטח המחיקה — נמדד בבייטים, 20.09

| קובץ | מה | כמה |
|------|-----|------|
| `src/components/NotebookTab.jsx` | **הקובץ כולו** — הצרכן ה⛔-יחיד של `public.journal_notes` (`:37-38` select · `:53-54` insert · `:68-69` update · `:80` delete) | **194** שורות |
| `SwingEdge_App.jsx` | `:72` אייקון `NotebookPen` · `:127` `import NotebookTab` · `:1061` רשומת `NAV_KEYS` · `:1090` שלב הסיור · `:7763-7766` ענף הרינדור | **6** שורות |
| `src/i18n.js` | `35` מפתחות `nb_*` (`12-18` · `722-728` · `1460-1466` · `1921-1927` · `2382-2388`) + `5` `notebookTab` (`:10` · `:720` · `:1458` · `:1919` · `:2380`) + `4` `tourNotebook*` (`:617-618` en · `:1324-1325` he) | **44** מפתחות |
| `src/lib/consent.js` | `:156` — `"notebook"` יוצא מ-`SCREEN_NAMES` | `12 ⇒ 11` |
| `scripts/analytics-contract-test.mjs` | `:50` `EXPECTED_SCREENS` · 🔴 `:134` המחרוזת `"the 12 measured screens"` | `12 ⇒ 11` **בשני האתרים** |
| `src/components/FeedbackTab.jsx` | `:44` — `notebook: "notebookTab"` ב-`TAB_LABEL_KEYS` | **1** שורה |
| `CONTEXT.md` | `:48` (היסטוריית B3) · `:52-54` (פרוזת `NAV_KEYS`) · `:203` · 🔴 `:398` כלל קוד 6 «**current 8**» · `:626` (QA) | **5** אתרים |

### חמש מדידות שהפריכו הנחה

1. ⚠️ **`tourNotebookTitle`/`Body` קיימים ב-`en` וב-`he` בלבד** — `es`/`pt`/`ar` ⛔ נושאים
   אותם כלל ונופלים לאנגלית דרך `tk = (k) => t[k] ?? enT[k]` (`:1077`). ⇒ `4` מפתחות, ⛔ `10`.
2. ⚠️ **`FeedbackTab` ⛔ נושא נתון היסטורי** — `originTab` מגיע מ-`feedbackOriginRef.current`
   (`SwingEdge_App.jsx:7784`), כלומר הטאב ה**חי** שממנו נפתח הפידבק. ⇒ אחרי המחיקה `"notebook"`
   ⛔ יכול להגיע לשם כלל, ורשומות פידבק ישנות ⛔ נקראות דרך המפה הזו. **ההסרה בטוחה.**
3. ⚠️ **מונה הסיור `{i + 1}/{total}` נגזר** — `OnboardingTour.jsx:29` הוא
   `const total = steps.length;` ⇒ `15 ⇒ 14` **זז לבדו**. ⛔ מספר ידני בסיור.
4. ⚠️ **`arch-auditor` ⛔ יסחף** — רשומת ה-`ACKNOWLEDGED` ל-`i18n.js` (`:139`) היא
   `/^קובץ ענק \(\d+ שורות\): src\/i18n\.js$/` — `\d+` הוא תו-כללי ⇒ הורדת `44` שורות ⛔ מזיזה אותה.
5. ⚠️ **הסנטינל ⛔ סופר טאבים** — `sentinel-auth.spec.js:390-392` הוא
   `expect.poll(() => page.locator('[data-tour-tab]').count()).toBeGreaterThan(0)` — טענה
   **חיובית בלי מספר קבוע**. ⇒ `0` הפניות ל-`notebook` בשני קבצי הסנטינל, ו⛔ נדרש בהם שינוי.

---

## 2 · ארבעת הפריטים שנרשמו (§10.1 — ⛔ פריט יתום)

נרשמו ב-`docs/BACKLOG.md` בקומיט הזה, ⛔ מטופלים בגל הזה:

| מזהה | שורש | מה |
|------|-------|-----|
| `B-350` | ‹R-5› | `tourDone` נכתב ל-`localStorage` **בלבד**; הגעתו ל-DB היא תופעת-לוואי של אפקט שמסתנכרן «בהזדמנות הבאה» ⇒ הסיור חוזר במכשיר שני |
| `B-351` | ‹R-6› | **שני** אלמנטים נושאים `data-tour="add-trade"` (`:5270` · `:8406`); `querySelector` לוקח את הראשון בסדר ה-DOM, והוא **מותנה-מצב** (`:5264` — `trades.length === 0`) ⇒ שלבי סיור 6+7 מאירים אלמנט שתלוי בג׳ורנל |
| `B-352` | ⛔ בלי שורש | ניווט חצים ⛔ מתהפך ב-RTL — `isRTL` בחתימה (`:22`) ו⛔ נקרא במאזין (`:95-102`, תלויות `[]`), והערת הכותרת (`:11`) מצהירה את ההפך |
| `B-353` | ⛔ בלי שורש | `onNavigate` מקבל כל מחרוזת בלי ולידציה מול `NAV_KEYS` ⇒ `~1.2s` תקיעה ואזור תוכן ריק. **מחלקה, ⛔ מופע** — `0` כיסוי |

⚠️ **`B-352` ו-`B-353` ⛔ שויכו בכוח.** שניהם חולקים צורה — «מקור-האמת קיים והצרכן ⛔ מתייעץ
בו» — ש⛔ אחת משש התבניות מתארת. **שורש שביעי ⛔ הוכרע כאן**, ותיוג כדי שהמספרים יסתדרו
הוא בדיוק `R-4`.

🔴 **`B-353` הוא הפריט שהגל הזה ⛔ מתקן ו*כן* נוגע בו** — שלב הסיור ל-`notebook` נמחק,
ולכן **המופע** נעלם. ⛔ **המחלקה נשארת פתוחה**, ואת זה הגל ⛔ סוגר.

---

## 3 · מה במפורש **⛔ בתחום**

| ⛔ | למה |
|----|------|
| `drop table public.journal_notes` | **החלטת ניב, ⛔ שלי.** `0` שורות היום ⛔ פירושו «בטוח למחוק» — פירושו «⛔ נדרשת הודעה מוקדמת». הטבלה נשארת חסרת-צרכן, וזו שורת `⚠️` ב-`STATE`, ⛔ פעולה |
| `supabase/migrations/20260712100000_journal_notes.sql` | 🔴 **הקובץ יוצר גם את `weekly_reviews`, שחי.** מחיקתו שוברת שחזור-מאפס. ⛔ נגיעה |
| `weeklyReviewTab` | טאב **נפרד** שנשאר. ⛔ בתחום |
| `B-350`/`B-351`/`B-352`/`B-353` | נרשמו, ⛔ תוקנו |

---

## 4 · ביצוע — קומיט אחד, קוד בלבד

⚠️ **הכול בקומיט אחד** — כל פיצול משאיר את העץ במצב שבו `i18n` ו-`FeedbackTab` ⛔ מסכימים,
וזה בדיוק מסלול הכשל השקט שסעיף «אמון» מתאר.

| # | קובץ | פעולה |
|---|------|--------|
| 1 | `src/components/NotebookTab.jsx` | `git rm` — `194` שורות |
| 2 | `SwingEdge_App.jsx` | הסרת `NotebookPen` מרשימת האייקונים (`:72`) · `import` (`:127`) · רשומת `NAV_KEYS` (`:1061`) · שלב הסיור (`:1090`) · ענף הרינדור + הערת הכותרת (`:7763-7766`) |
| 3 | `src/i18n.js` | הסרת `44` המפתחות בחמש השפות |
| 4 | `src/lib/consent.js` | `"notebook"` יוצא מ-`SCREEN_NAMES` (`:156`) |
| 5 | `scripts/analytics-contract-test.mjs` | `"notebook"` יוצא מ-`EXPECTED_SCREENS` (`:50`) **וגם** `"the 12 measured screens"` ⇒ `11` (`:134`) |
| 6 | `src/components/FeedbackTab.jsx` | הסרת `notebook: "notebookTab"` (`:44`) |
| 7 | `CONTEXT.md` | `:52-54` פרוזת `NAV_KEYS` · 🔴 `:398` «current **8**» ⇒ «current **7**» + הסרת `notebook` מהרשימה · `:626` פריט QA · `:203` · `:48` מקבל הערת «הטאב נמחק 20.09, הטבלה נשארה» |

⛔ **`:48` ⛔ נמחק** — היסטוריית ספרינט היא רשומה של מה ש**קרה**, ומחיקתה היא שכתוב.

---

## 5 · מה תופס כל שבירה — ומה ⛔ נתפס

### ⑤א · שבירות שיש להן שער

| שבירה | השער שתופס | איך הוא נכשל |
|--------|--------------|----------------|
| `import NotebookTab` נשאר אחרי מחיקת הקובץ | **`build`** (חוליה 33) | כשל resolve — **קשה ורועש** |
| `SCREEN_NAMES` ⛔ מסכים עם `EXPECTED_SCREENS` | `test:analytics` אסרציה 7 | `JSON.stringify` השוואה — **אדום** |
| `docs/BACKLOG.md` — `B-350`…`B-353` בלי `‹R-n›` דו-כיווני | `test:registry` §8.3 | רשימת השורש מול התגים — **אדום** |
| ארבעת מספרי שכבת השורשים בפרוזה | `test:registry` §8.6 | פרוזה מול מדוד — **אדום** |
| שורת `DONE` בלי `hash`/עמודת `T` | `test:registry` §4.1 · §11.1 | **אדום** |
| `docs/STATE.md` מעל `16,000` בתים | `test:registry` §3 | **אדום** |
| `arch-auditor` — `i18n.js` מאבד `44` שורות | `test:arch` | ⚪ `\d+` תו-כללי ⇒ **⛔ זז** (נמדד, ⛔ הונח) |
| סנטינל — טאב נעלם משורת הניווט | `sentinel-auth:390` | ⚪ `toBeGreaterThan(0)` ⇒ **⛔ זז** (נמדד) |

### ⑤ב · 🔴 שבירות ש⛔ שום שער תופס — ונאמר במפורש

| שבירה | למה ⛔ נתפסת | מה עושים במקום |
|--------|----------------|------------------|
| 🔴 **`"the 12 measured screens"` ב-`:134`** | המחרוזת **כתובה ביד** ו⛔ נגזרת מ-`EXPECTED_SCREENS.length`. אסרציה 7 תעבור עם התווית `12` מעל אוכלוסייה של `11` | **מחלקת `B-324`.** השינוי ידני, **באותו קומיט**, ונרשם כאן כדי שיהיה מי שיבדוק |
| 🔴 **`CONTEXT.md:398` — «current 8»** | ⛔ שער קורא את המספר הזה | ידני, באותו קומיט. ⚠️ ⛔ נפתח `B-` חדש: זו מחלקת `B-324` הפתוחה |
| 🔴 **`44` מפתחות `i18n` יתומים** | ⛔ שער בריפו מודד מפתח שאיש ⛔ צורך. `test:copy` מודד `tooltips.js` מול `glossary.json` — ⛔ את `i18n.js` | הרשימה בסעיף 1 היא המדידה. ⚠️ אם מפתח יישאר — **⛔ יישבר דבר**, וזו בדיוק הסיבה שאיש ⛔ ישים לב |
| 🔴 **הטאב נעלם מהמסך, המשתמש רואה `7` פריטי ניווט** | `build` מוכיח **קומפילציה**, ⛔ רינדור. ⛔ harness ל-JSX של שורת הניווט | **`C-056`** — אימות עין, ⛔ אופציונלי (`T3`, §15) |
| 🔴 **הסיור רץ `14` שלבים ברצף** | `OnboardingTour` ⛔ מכוסה: `grep -rln "OnboardingTour\|buildTourSteps" scripts/ tests/ tests-sentinel/` מחזיר **ריק** ⇒ `0` כיסוי (נמדד 20.09, וזה `B-353`) | **`C-056`** — הרצת הסיור מקצה לקצה בדפדפן |
| 🔴 **`FeedbackTab` — תווית גולמית** | `t[undefined] \|\| originTab` ⛔ זורק ו⛔ מתריע | מדידה ⑤ב־2 בסעיף 1 מוכיחה ש-`originTab` חי-בלבד ⇒ **המסלול מת אחרי המחיקה**. ⛔ ראיה שהוא מת **לפניה** |

⚠️ **ההבחנה שמחזיקה את הסעיף הזה:** `build` ירוק מוכיח ש⛔ נשאר `import` שבור.
הוא ⛔ מוכיח ששורת הניווט נראית תקינה, שהסיור מגיע לסופו, ושאיש ⛔ נתקע `1.2` שניות
מול מסך ריק. ⛔ **ולכן `T3` ⛔ נסגר על `verify` ירוק.**

---

## 6 · שערים והוכחה

```
npm run test:analytics     # 17 assertions — חובה, נוגעים ב-consent.js (§7)
npm run test:arch          #  8 assertions — חובה, נוגעים ב-arch-auditor surface (§7)
npm run test:registry      # 41 assertions — חובה לפני כל push (§14)
npm run verify             # 32 חוליות ואז build — הפלט המלא מודבק, ⛔ סיכום
```

⛔ **⛔ pipe לפני push מותנה** (`INCIDENTS#25`) — כל שער רץ **חשוף**, ו-`pre-push` ⛔ מריץ
את `verify` (`B-337`).

---

## 7 · חובות מרשם בסגירה (§10 · §14)

| קובץ | מה |
|------|-----|
| `docs/DONE.md` | שורה עם **hash** · «איך אומת» · עמודת `T` = `T3`. ⚠️ בקומיט ה**בא** — קומיט ⛔ נושא את ה-hash של עצמו |
| `docs/CHECKS.md` | **`C-056`** — אימות עין: `7` פריטי ניווט · הסיור `14/14` · ⛔ `1.2s` תקיעה · RTL |
| `docs/STATE.md` | ⚠️ שורת סיכון: **`public.journal_notes` חסרת-צרכן** — קיימת, `0` שורות, ⛔ נמחקת בלי הכרעת ניב |
| `docs/TRUTH.md` | שינוי יכולת פונה-משתמש (§10.4) — הסרת «מחברת» |
| `docs/NEXT.md` | עדכון בסגירת הגל (§14) |
| `docs/BACKLOG.md` | ✅ `B-350`…`B-353` — **נרשמו בקומיט הזה** |

---

## 8 · מה נדרש מניב

1. **אישור המחיקה** כפי שמתוארת — קוד בלבד, הטבלה נשארת.
2. **הכרעה על `public.journal_notes`**: נשארת חסרת-צרכן (⇒ שורת `⚠️` ב-`STATE`),
   או נפתח `B-` למחיקתה בגל נפרד עם הודעה מוקדמת. ⛔ ברירת מחדל — צריך הכרעה.
3. **הכרעה על `B-352`/`B-353`**: לתקן בגל הזה (הם בקוד שממילא נוגעים בו) או להשאיר
   ב-`BACKLOG`. ⚠️ **המלצתי: להשאיר** — `B-353` דורש אסרציה שנצפתה אדומה, וזה גל בפני עצמו.
4. **האם §8.1 דורש `docs/audits/` נפרד.** המדידה כולה יושבת בסעיף 1 של המסמך הזה.
   אם התשובה «כן» — נכתב לפני שורת קוד ראשונה.
