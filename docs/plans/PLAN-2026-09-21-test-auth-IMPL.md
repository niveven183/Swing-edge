# PLAN — `probe:eye` · תוכנית **מימוש** — ⏸️ awaiting approval

**מקור:** `docs/plans/PLAN-2026-09-21-test-auth.md` §0–§7 **במלואה**, כולל §6.1
המעודכן. **סיווג:** `T2` · תשובות `לא/לא/לא/לא/כן` (ודאות — מוסכמות
`scripts/`/`tests-sentinel/` נמדדו ולא הונחו).

⛔ **⛔ קוד.** זו פריסה לשלבי בנייה: **קובץ · פונקציה/מקטע · סדר**.
⛔ **⛔ סוגר אף `C-`** ו⛔ **נוגע ב-`STATE`/`NEXT`/`BACKLOG`/`CHECKS`**.

---

## 0. ההפרדה מול תוכנית-האבחון

`PLAN-2026-09-21-test-auth.md` עונה «**מה** צריך לקרות ו**למה**». הקובץ הזה עונה
«**איפה** זה נכתב, **באיזה סדר**, ו**מה נמדד** לפני שנכתבה שורה».

⛔ **הכרעות נעולות ⛔ נפתחות כאן.** ⓽ (חלון-אחיזה — אדום-שווא חד-פעמי
בסנטינל **מקובל**, ⛔ נעילה הדדית, ⛔ נגיעה ב-`sentinel.yml`) · שער-מצב מפורש
⓹-⓺-⓻ · כשל `gh` ⇒ **נעילה** · עיצוב הגיבוי §6.1ב׳ · אזהרת-סיסמה בלבד ·
השם `probe:eye` · ⛔ ניקוי אוטומטי, `--reset` נפרד · **ההרנס עוצר ו⛔ מכריע**.

---

## 1. מבנה הקבצים — **נמדד, ⛔ הומצא**

### 1.1 מה נמדד ב-`scripts/`

| מדידה | ערך | פקודה |
|--------|------|--------|
| קבצים ב-`scripts/` | `48` (+`2` תת-ספריות) | `ls scripts/` |
| תת-ספריות ב-`scripts/` | `fixtures` · `import-fixtures` **בלבד** | `ls scripts/` |
| ספריית `probe/` בשורש | ⛔ **⛔ קיימת** | `ls` |
| ערכי `probe:*` ב-`package.json` | `5` — `boundary`·`dailypnl`·`deploychunk`·`firing`·`count` | `grep '"probe:' package.json` |
| מהם מצביעים ל-`scripts/*.mjs` | `5/5` — כולם שטוחים | כנ"ל |
| מהם בשם `*-probe.mjs` | `4/5` (`boundary-render`·`deploy-chunk`·`sentinel-firing`·`sentinel-count`) | כנ"ל |
| החריג | `probe:dailypnl` → `daily-pnl-label-**test**.mjs` | כנ"ל |
| ייבוא-צולב בין `scripts/*.mjs` | **`0`** — כל קובץ עצמאי | `grep 'from "\./' scripts/*.mjs` |
| תקדים לייבוא מתת-ספריה | `import-test.mjs:8` → `import "./import-fixtures/generate.mjs"` | `grep fixtures scripts/*.mjs` |
| קבצים ב-`tests-sentinel/` | `2` — `sentinel-auth.spec.js` · `sentinel-public.spec.js`, **`2/2` ספקים של ה-runner** | `ls tests-sentinel/` |

### 1.2 ההכרעה שנגזרת

🔴 **`probe/eye.mjs` — הומצא, ונדחה.** ⛔ קיימת ספריית `probe/`, ו-`5/5` ערכי
ה-`probe:*` הקיימים מצביעים ל-`scripts/` **שטוח**. פתיחת ספריה חדשה היא
מוסכמה שנייה — `‹R-6›` על מבנה הריפו.

⇒ **הקובץ: `scripts/eye-verification-probe.mjs`** — נגזר מ-`4/5` הרוב
(`*-probe.mjs`), ⛔ מהחריג היחיד.

⇒ **קובץ אחד, עצמאי.** `0/48` קבצי `scripts/` מייבאים זה את זה. פריסה
ל-`probe/lib/*.mjs` הייתה המוסכמה החדשה ה**שנייה** באותו גל.

⇒ **`tests-sentinel/` ⛔ הבית.** `2/2` הקבצים שם הם `.spec.js` שה-workflow מריץ
דרך ה-runner; `probe:eye` ⛔ spec ו⛔ רץ ב-CI (§2.3).

⇒ **הזרעים — נתונים, ⛔ קוד.** אם טבלת `SEEDS` (§6) חוצה `~120` שורות היא
עוברת ל-`scripts/eye-fixtures/seeds.mjs` — **תקדים מדוד**: `scripts/import-fixtures/`
ו-`scripts/fixtures/`. ⛔ **אחרת היא נשארת inline**; פיצול מוקדם הוא המצאה.

### 1.3 קובץ הגיבוי

`.eye-backup/<ISO>.json` — **מחוץ לריפו בפועל**, לפי התקדים המדוד
ב-`.gitignore`: `analytics-report.txt` + `.analytics-state/` נושאים את ההערה
«must never land in a public repo». ⇒ שתי שורות חדשות ב-`.gitignore`
(`.eye-backup/`) **עם אותה הערה**, בקומיט המימוש.

---

## 2. `package.json`

שורה אחת, **אחרי `probe:count`** (הערכים מקובצים; `probe:*` יושבים כגוש):

```
"probe:eye": "node scripts/eye-verification-probe.mjs",
```

⛔ **`verify` ⛔ זזה.** `probe:eye` דורש דפדפן headed, סודות ופרודקשן — בדיוק
כמו `probe:boundary`/`probe:firing`/`probe:count`, ש-`3/3` ⛔ בשרשרת. ⇒ **מניית
`32` החוליות ב-`CLAUDE.md` §7 ⛔ זזה**, ו-`test:registry` §14 נשאר ירוק.

---

## 3. שלבי הבנייה — `S1`…`S9`

כל שלב: **קובץ · מקטע/פונקציה · מה נדרש כדי לסמנו גמור.**
⚠️ **הסדר הוא סדר בנייה, ⛔ סדר ריצה** (§4).

| # | מקטע בקובץ | פונקציה | גמור כאשר |
|---|-------------|----------|------------|
| **S1** | `// ── דגלים · env · אזהרות` | `parseArgv()` · `readEnv()` · `warnPassword()` | `--restore-only`/`--reset`/`--prove-restore`/`--only=<C>` נקראים; `SENTINEL_QA_EMAIL`/`SENTINEL_QA_PASSWORD`/`SUPABASE_URL`/`SUPABASE_ANON_KEY` נקראים מ-`process.env` **בלבד** (§2.2); אזהרת §6.1ג׳ מודפסת ונוקבת ב-`updated=2026-07-25T20:07:07Z` המדוד; ⛔ חוסמת |
| **S2** | `// ── ⓹⓺⓻ שער-המצב` | `sentinelGate()` | `gh run list --workflow=sentinel.yml --limit 5 --json databaseId,event,status,conclusion,startedAt`; **רשימה-לבנה** `status === "completed"` בלבד; `[]` ⇒ **נעילה**; `gh` שנכשל ⇒ **נעילה** עם `stderr` + `exit code` מלאים, ומבדיל `⛔ מותקן`/`⛔ מאומת`/`timeout`/`rate-limit` |
| **S3** | `// ── גיבוי` | `backupAll()` · `verifyBackupReadBack()` | `GET /rest/v1/trades?select=*` **וגם** `GET /rest/v1/user_settings?select=*` (§7.2) ⇒ קובץ ⇒ **נקרא בחזרה ומונה `N` שורות לכל טבלה**. ⛔ **`DELETE` ⛔ נשלח לפני שזה עבר** |
| **S4** | `// ── הוכחת round-trip` | `proveRestore()` | `--prove-restore` בלבד: `SELECT`→`DELETE`→`INSERT`→`SELECT` **ללא דפדפן**, והשוואת-שדות על `id` · `created_at` · עמודות `20260812150000_be_row_currency_label` ו-`20260816120000_trades_provenance`. **ירוק כאן הוא תנאי-סף לכל ריצה אחרת** (§6.1ב׳5) |
| **S5** | `// ── שכבת הזרעים` | `seedJournal(state)` · `writeSettings(patch)` | `5` מצבי-היומן של §6 נזרעים דרך REST; כל זריעה **מאומתת ב-`GET` חוזר** לפני שממשיכים; `capital`/`lang`/`tourDone`/`betaWelcome` נכתבים ל-`user_settings` |
| **S6** | `// ── הדפדפן` | `openBrowser()` · `login()` · `gotoTab(name)` | `chromium.launch({ headless: false })`, **פרופיל חד-פעמי**; התחברות עם אישורי ה-QA; ⛔ מדפיס סיסמה; כשל התחברות מבדיל `login-failed` מ**כשל-עוגן** (§6.1ג׳) |
| **S7** | `// ── נקודות העצירה` | `stop(meta)` | מדפיס את הבלוק הקבוע (§5) וממתין ל-stdin; `Enter` ⇒ הבא · `r` ⇒ שחזר ועצור · `Ctrl-C` ⇒ שחזר וצא. ⛔ **⛔ פולט `✓`/`✗`** (§0 של תוכנית-האבחון) |
| **S8** | `// ── שחזור` | `restoreAll()` · `installExitHooks()` | `4` מסלולים — תקין · `SIGINT` · `SIGTERM` · `uncaughtException`. 🔴 **הדפדפן נסגר לפני כתיבת ה-DB** (§7.3) |
| **S9** | `// ── רשתות ביטחון` | `restoreOnly()` · `resetOnly()` | `--restore-only` קורא את קובץ הגיבוי **האחרון** ומחזיר, ללא שער וללא דפדפן; `--reset` הוא ניקוי **מפורש ונפרד**, ⛔ אוטומטי |

---

## 4. סדר הריצה (המסלול המלא, ללא דגלים)

```
 ① אזהרת סיסמה (§6.1ג׳)            — מדפיס, ⛔ חוסם
 ② שער-המצב  sentinelGate()         — נכשל ⇒ יציאה 1, ⛔ נגע בדבר
 ③ backupAll() + verifyBackupReadBack()
 ④ openBrowser() + login()
 ⑤ לולאה על 5 מצבי-היומן (§6):
       seedJournal(Jn) → GET אימות → [stop(), stop(), …]
 ⑥ closeBrowser()                   — 🔴 לפני ⑦, ⛔ אחריו
 ⑦ restoreAll()                     — trades + user_settings
 ⑧ סיכום: כמה עצירות · כמה שורות הוחזרו · היכן קובץ הגיבוי
```

⚠️ **השער נבדק ב-①-② בלבד.** אחיזה חוזרת באמצע ⛔ מוסיפה ידיעה — חלון-האחיזה
**חסום-מעצם-העיצוב** (ההרנס ממתין לאדם), וזו בדיוק ⓽ שננעלה.

---

## 5. נקודת-העצירה — מה **בדיוק** מודפס

בלוק קבוע, זהה בכל `16` העצירות:

```
────────────────────────────────────────────────────────────
⏸️  עצירה 4/16   ·   C-053 ⓶   ·   מצב-יומן J3   ·   locale he
    מסך:   מאמן → «לקחים חכמים»
    נזרע:  SETUP-A ×5 (4W/1L) · SETUP-B ×1 (1W)   [GET אימות: 6/6]
    בדוק בעין:
      ⓵ `Unknown` ⛔ מופיע בכרטיס
      ⓶ הכרטיס נוקב ב-SETUP-A  (⛔ SETUP-B)
    ⛔ ההרנס ⛔ קובע ✓/✗ — ההכרעה שלך, ב-`docs/CHECKS.md`
    [Enter] הבא   ·   [r] שחזר ועצור   ·   [Ctrl-C] שחזר וצא
────────────────────────────────────────────────────────────
```

**מה שחייב להופיע ומה ⛔:** ✅ מספר עצירה ומכנה · הפריט והסעיף · מצב-היומן
ו-locale · **הנזרע עם מניית אימות** · רשימת מה-לבדוק **המועתקת מ-§1.1**.
⛔ **⛔ «עבר»** · ⛔ **⛔ «נראה תקין»** · ⛔ **⛔ מספר שההרנס חישב במקום ניב** —
`C-049`⓺ הוא «חיבור ידני לסנט», והרנס שמדפיס את הסכום מוחק את הבדיקה.

---

## 6. תיאום הזרעים — **ריצה אחת, ⛔ שש** (נמדד)

### 6.1 המדידה שמכריעה

**כל המשטח המשתנה של `6` הפריטים הוא שתי קבוצות-שורות:**

| טבלה | מה נמדד | מקור |
|-------|----------|-------|
| `trades` | שורות ה-QA | `sentinel-auth.spec.js:611-613` — `DELETE /rest/v1/trades` |
| `user_settings` | **שורה אחת**, `{user_id, settings jsonb, updated_at}` | `src/lib/userSettings.js:93-98` |

ו-`user_settings.settings` הוא **blob יחיד** הנושא `capital`·`riskPct`·
`onboarding`·`tourDone`·`watchlist`·`playbook`·`priceAlerts`·**`lang`**·
`accountCurrency`·`capitalCurrency` (`userSettings.js:19-32`).

⇒ **locale · `capital` · `tourDone` — ⛔ דורשים חשבון שני, ⛔ דפדפן שני,
⛔ ריצה שנייה.** כולם `PATCH` לאותה שורה + רענון. ⇒ **גיבוי אחד · סשן-דפדפן
אחד · שחזור אחד · `16` עצירות.**

### 6.2 מה ⛔ ניתן למיזוג — וזה מדוד

🔴 **`C-049`⓪ הוא תכונת-יומן *גלובלית*.** `avgWin`⊕`avgLoss`⊕`maxDD` הם
אגרגטים על **כל** היומן ⇒ כל זריעה של `C-050`/`C-053` **משנה אותם**. לכן
`C-049` מקבל מצב משלו, וה-⓪ מאומת **אחרי** החישוב, ⛔ לפניו — זה בדיוק הכשל
שביטל את ניסיון 16.09.

🔴 **`C-056` ⛔ רץ על יומן ריק.** `B-351` מדד ש-`data-tour="add-trade"` יושב גם
על כפתור מצב-ריק שמרונדר רק כש-`trades.length === 0` ⇒ סיור על יומן ריק מצביע
על אלמנט **אחר**. זה מה ש-`C-057`⓵ בא למדוד, ולכן ערבובם מכשיל את שניהם.

✅ **`C-053`(ב) ⊇ `C-050`(ב)** — הרכב `A:5` / `B:1` מקיים «סטאפ אמיתי `n ≥ 2`».
מיזוג **מוכח**, ⛔ משוער.

⚠️ **`C-050` — `2` עצירות, ⛔ `6`.** §1 מודד ש-`2/5` נענו 17.09 (⓵·⓸) ושהפתוחים
הם ⓶·⓷·⓹. `3/3` מהם נענים על מצב (ב): ⓶ ו-⓷ ב-`he`, ⓹ ב-`en`. מצבים (א)/(ג)
משרתים את ⓵/⓸ ש**כבר נענו** ⇒ נכללים כ**זרוע-ביקורת אופציונלית** (`+2` עצירות)
ו⛔ במסלול ברירת-המחדל. ⚠️ **ו-§1.1 מונה (א) «יומן ללא סטאפ» ו-(ג) «`0` סטאפים»
כשני מצבים — הם נראים זהים בניסוח.** ⛔ **⛔ מוזגו בשקט**: זו שאלה ל-§1.1,
⛔ הכרעת מימוש.

### 6.3 חמשת הזרעים והמסלול

| סדר | מצב | מה נזרע | עצירות | משרת |
|-----|------|----------|---------|-------|
| `1` | **J3** | `SETUP-A ×5` (`4W/1L`) · `SETUP-B ×1` (`1W`) | `9` | `C-050`⓶⓷ (`he`) · `C-050`⓹ (`en`) · `C-053`⓶ ×2 אתרים · `C-052` · `C-056` · `C-057`⓶⓸⓹ ×2 · `C-057`⓷ |
| `2` | **J5** | חלק מהעסקאות **ללא** סטאפ | `2` | `C-053`⓵ — «לקחים חכמים» ואז אריח `Best Setup` |
| `3` | **J6** | כל הסטאפים בני-`1` | `2` | `C-053`⓷ ×2 אתרים |
| `4` | **J1** | ≥`1` סגורה, `avgWin`⊕`avgLoss`⊕`maxDD` ⛔ שלמים | `2` | `C-049` — תא P&L, ואז `4` כרטיסי אנליטיקס |
| `5` | **J0** | **יומן ריק** | `1` | `C-057`⓵ |
| | | **סה"כ** | **`16`** | |

⚠️ **`C-052` מחזיר את `capital` מיד אחרי עצירתו**, ⛔ בסוף הריצה: `capital`
יושב באותו blob שממנו `C-049` נגזר, ודליפה בין הפריטים הייתה מרעילה את `J1`.

⚠️ **סדר `J0` אחרון ⛔ שרירותי** — הוא המצב היחיד שבו היומן **ריק**, ולכן
כל טעות-רצף אחריו נראית מיד.

### 6.4 ⓒ — **נפתר בפועל**

`C-057`⓵ ו-`C-057`⓷ סווגו **ⓒ** («⛔ מתחיל בלי הכרעה») כי שניהם דורשים
**הרס מצב פרודקשן**: ⓵ יומן ריק, ⓷ איפוס `tourDone`+`betaWelcome`.

🔴 **§6.1ב׳ הכריע את זה.** הגיבוי-לפני-כתיבה, השחזור בארבעת מסלולי-היציאה
ו-`--restore-only` הם בדיוק ההגנה שחסרה. ⇒ **`C-057`⓵ ו-⓷ עוברים ⓒ → ⓐ**,
נכנסים לטבלה §6.3 (`J0` ו-`J3`), ו⛔ **נשארים «הכרעה פתוחה»**.
⚠️ **`C-046` נשאר ⓑ** — הוא נקרא מלוג ה-Actions, ו⛔ נוגע בדפדפן כלל.

---

## 7. הנחות שהופרכו מול הקיים

| הנחה | מדידה | מסקנה |
|-------|---------|--------|
| «`probe/eye.mjs` או דומה» | `0` ספריות `probe/`; `5/5` ערכי `probe:*` → `scripts/` שטוח | ⛔ **הופרכה** ⇒ `scripts/eye-verification-probe.mjs` |
| «פריסה למודולים» | `0/48` קבצי `scripts/` מייבאים זה את זה | ⛔ **הופרכה** ⇒ קובץ אחד |
| «`gh` הוא כלי מבוסס בסקריפטים» | `0` קריאות `gh` ב-`scripts/`; הגישה היחידה ל-GitHub היא `daily-digest.mjs:33` `fetch("https://api.github.com")` | ⚠️ **הופרכה** ⇒ `S2` הוא **הקריאה הראשונה**; ענף «`gh` ⛔ מותקן» הוא מציאותי, ⛔ תיאורטי |
| «`headless:false` הוא הצורה המקובלת» | `0` מופעים של `headless` בריפו; `boundary-render-probe.mjs:143` הוא `chromium.launch()` סתם | ⚠️ **הופרכה חלקית** ⇒ headed הוא **חדש**, והוא מוצהר |
| «יש `import` אחד ל-playwright» | **שתי** צורות: `from "playwright"` (`boundary-render-probe`) ו-`from '@playwright/test'` **ללא ה-runner** (`sentinel-count-probe:39`) | ⇒ שתיהן תקדים; נבחרת `from "playwright"` — ⛔ גוררת את ה-runner |
| «דגלים — צורה חופשית» | `8` קבצים: `process.argv.slice(2)` + `indexOf`/`includes` | ✅ **אושרה** ⇒ `--restore-only`/`--reset` באותה צורה |
| «הגיבוי הוא של ה*יומן*» (§6.1ב׳1) | `capital`·`lang`·`tourDone`·`betaWelcome` ⛔ ביומן — כולם ב-`user_settings.settings` | 🔴 **הורחב** ⇒ הגיבוי חייב **שתי** טבלאות. זו **הרחבה נגזרת**, ⛔ פתיחת ההכרעה |
| «שחזור ה-DB יכול לרוץ בכל עת» | `userSettings.js:161-175` — `upsert` **בהשהיית `1000ms`**, ועוד flush ב-unload | 🔴 **הופרכה** ⇒ **סוגרים דפדפן, ואז כותבים** — אחרת הדף מעלה את ערכי ההרנס **מעל** השחזור |
| «שחזור DB משאיר mirror מורעל» | `loadSettings` `ok` קורא `writeMirror(data.settings)` (`userSettings.js:136`) | ✅ **⛔ חוסם** — ה-mirror מתרפא בקריאה מוצלחת; ובכל מקרה הפרופיל חד-פעמי |
| «`6` פריטים ⇒ `6` ריצות» | המשטח כולו = `trades` + שורת `user_settings` אחת | ⛔ **הופרכה** ⇒ **ריצה אחת**, `5` זרעים, `16` עצירות |
| «`C-050` דורש `3` מצבים ×`2` locale = `6`» | `2/5` סעיפים נענו 17.09; `3/3` הפתוחים נענים על מצב (ב) | ⚠️ **צומצמה** ⇒ `2` עצירות + `2` אופציונליות |

---

## 8. מה השלב הזה ⛔ כולל

⛔ קוד · ⛔ `sentinel.yml` · ⛔ workflows · ⛔ סודות · ⛔ שינוי סיסמה ·
⛔ התחברות לחשבון ה-QA · ⛔ הרצה מול פרודקשן · ⛔ נעילה הדדית עם הסנטינל ·
⛔ `STATE`/`NEXT`/`BACKLOG`/`CHECKS` · ⛔ סגירת אף `C-`.

⚠️ **פריטים שנולדו כאן ו⛔ נרשמו בריפו — נכנסים לפי §10.1 בקומיט המימוש:**
העמימות בין `C-050`(א) ל-(ג) ב-§1.1 · שתי השורות ל-`.gitignore` ·
היות `S2` הקריאה הראשונה ל-`gh` מתוך `scripts/`.
