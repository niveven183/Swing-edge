# PLAN 2026-09-21 — `test:auth`: הרנס שמכין תנאים לאימות-עין ועוצר

**סטטוס:** ⏸️ **awaiting approval** — ⛔ מומש דבר. ⛔ `package.json` ⛔ נגע ·
⛔ קובץ הרנס ⛔ נוצר · ⛔ `tests-sentinel/` ⛔ נגע · ⛔ workflow ⛔ שונה ·
⛔ סוד ⛔ נגע · `STATE`/`NEXT`/`CHECKS` ⛔ נגעו · ⛔ `B-` ⛔ נפתח ·
⛔ התחברות לחשבון ה-QA · ⛔ הרצה מול פרודקשן.

**סיווג: `T3` · תשובות `לא`/`לא`/**`כן`**/`לא`/**`כן`**.**

- **ש1 הפיכות — `לא`.** התוצר הוא קובץ `.md` אחד. ⛔ DB · ⛔ מייל · ⛔ פרסום.
- **ש2 אמון — `לא`.** ⛔ מספר פונה-משתמש נגזר כאן.
- **ש3 אבטחה וגישה — `כן`.** הגל הזה ⛔ נוגע בסוד, אבל **התוצר קובע איך הרנס
  מקבל את אישורי ה-QA ומה נראה על המסך בזמן שהוא מקליד אותם**. ⚠️ **וזה ⛔
  תיאורטי:** `INBOX` 20.09 מתעד שהסיסמה נחשפה **בצילום מסך**, והרנס המתוכנן הוא
  **headed** ⇒ אותו ערוץ דליפה בדיוק, בדרגת חשיפה גבוהה יותר. §15: «ספק ⇒ כן»,
  ו-ש3 היא **טריגר-יחיד** ⇒ `T3` לבד.
- **ש4 רוחב בקוד — `לא`.** `0` קבצי קוד. `docs` ⛔ נספרים (§15).
- **ש5 ודאות — `כן`.** **מצב ה-DB של חשבון ה-QA ⛔ נמדד** — אין מדידה של
  `tourDone`/`betaWelcome`/`welcomeSeen`, ⛔ של קיום עסקה סגורה עם אגורות,
  ⛔ של `bySetup`. ⚠️ **וזו בדיוק האוכלוסייה שממנה `C-049`⓪ · `C-050`⓶ ·
  `C-053`⓶ · `C-057`⓵⓷ נגזרים** ⇒ הנחה פתוחה, ⛔ מרוככת.

⚠️ **`T3` בלי מסך מושפע ⇒ האימות הוא `אומת-מקור`** (§15) — קריאת הקובץ
מ-`origin` ו⛔ מהעץ המקומי. **הקובץ הזה הוא שלב 1 של §8.1** (אבחון read-only,
⛔ אפס תיקון); המימוש הוא שלב 2 ו⛔ מתחיל בלי אישור.

**שער בטיחות:** `git pull --ff-only origin main` ⇒ `Already up to date` ·
`git status --porcelain` ריק · `HEAD = 94ff240`.

---

## 0. מה זה ⛔ ומה זה כן

🔴 **הרנס ⛔ מסמן «עבר».** הוא **מכין מצב** ו**עוצר**; התשובה היא של ניב, בעין,
על המסך. ⚠️ **זו ⛔ החמרה סגנונית** — היא השורש שבגללו `7` הפריטים פתוחים:
כל אחד מהם נושא בשורתו את אותו משפט («⛔ מה ש-`test:X` מדד הוא … ⛔ המסך»),
והמכשול ⛔ היה חוסר-ידיעה מה לבדוק אלא **עלות ההכנה** — התחברות, seed, locale,
מטבע, מצב-יומן. הרנס מסיר את העלות הזו ו⛔ את ההכרעה.

⛔ **ולכן הרנס ⛔ פולט `✓`/`✗` על סעיף `C-`.** הוא פולט **מצב מוכן + נקודת-עצירה
+ מה לבדוק**. הרנס שמדפיס «עבר» הוא `R-4` בצורתו הטהורה — שומר שמודד נוכחות
(«הדף נטען») במקום אמת («המספר נכון»), וזה בדיוק מה ש-`7` השורות מזהירות מפניו.

⚠️ **התכנון הקודם נעשה בשיחה ו⛔ נשמר** (`INBOX` 21.09) ⇒ **⛔ הונח דבר.**
כל מה שלמטה נגזר מ-`docs/CHECKS.md` כלשונו ומ**מדידה** שבוצעה בסשן הזה.

---

## 1. שבעת הפריטים — ציטוט, סעיפים פתוחים, סיווג

**מקרא הסיווג:** ⓐ הרנס יכול להכין תנאים עד רגע העין · ⓑ ⛔ דפדפן כלל
(לוג Actions / DB / מכשיר שני) · ⓒ דורש החלטת ניב לפני שהרנס נוגע.

### C-046 — `CHECKS.md:96`

> «**ריצת `schedule` אמיתית — האם `github.event.schedule` קיים, ונושא את שורת
> ה-cron שירתה.** נולד מ-`B-318` (11.09) … ⛔ **ההנחה ⛔ ניתנת לאימות מקומי**»

**סעיפים:** `6` — ⓵ השדה קיים ו⛔ ריק · ⓶ התוכן מדויק · ⓷ ההתאמה נכונה
(`[20 …]`⇒`auth=0`, `[50 …]`⇒`auth=1`, **שתי** הריצות נצפו) · ⓸ `SENTINEL_AUTH`
בשלב ה-`browser` תואם · ⓹ ⛔ ריצה נפלה על `exit 1` · ⓺ `workflow_dispatch` שרד.
**מצב:** `0/6`, «⛔ טרם רץ».

**סיווג: ⓑ — ⛔ דפדפן כלל, ו⛔ שייך ל-`test:auth`.** ⚠️ **החשד המוקדם אושר
במלואו, ותוספת מדודה הפריכה את הנחת-החסימה** — ראה §3.1. ⛔ **הכללתו בהרנס היא
`R-6`**: הרנס פותח דפדפן מול `swing-edge.com`, וכל `6` הסעיפים נקראים מלוג
GitHub Actions. הוא **⛔ נכנס לתוכנית המימוש.**

### C-049 — `CHECKS.md:99`

> «**`5` האתרים שתוקנו ב-`B-321` — האם המספר על המסך נושא **אגורות אמיתיות**,
> ו⛔ `.00` עגול.** … ⓪ **תנאי-סף לכל שאר הסעיפים — הג'ורנל שעליו מצלמים חייב
> לשאת עסקה סגורה שערכיה נושאים אגורות** … ⛔ **תצפית `.00` ⛔ סוגרת את `C-049`
> לעולם**»

**סעיפים:** `6` + **⓪ תנאי-סף ש⛔ נספר** — ⓵ תא P&L ביומן + `title` · ⓶ «רווח
ממוצע» · ⓷ «הפסד ממוצע» (הענף השלילי) · ⓸ `Max Drawdown` (היחיד עם שומר
`== null`) · ⓹ «היום הטוב ביותר» (**ריווח** המפריד `·`) · ⓺ חיבור ידני לסנט.
**מצב:** `0/6`; ניסיון 16.09 **בוטל** — `avgWin 130` · `avgLoss 325` · `maxDD 650`
שלושתם שלמים ⇒ `Math.round` פעולת-זהות ⇒ ירוק בשני העצים.

**סיווג: ⓐ — ו⓪ הוא כל העניין.** 🔴 **החסם היחיד הוא האוכלוסייה**, והרנס קיים
כדי לייצר אותה: seed של עסקה סגורה שממנה `avgWin`/`avgLoss`/`maxDD` נופלים על
אגורות. ⚠️ **ה-seed חייב להיבדק *אחרי* החישוב ו⛔ לפניו** — עסקה שערכיה נושאים
אגורות ⛔ מבטיחה שהממוצע יישא אותן (`(300+350)/2 = 325` בדיוק).

### C-050 — `CHECKS.md:100`

> «**כרטיס «הסטאפ החזק ביותר» ב«לקחים חכמים» — האם המסך ⛔ מציע להגדיל פוזיציה,
> ו⛔ משתמש בלי סטאפים רואה מסך שבור.**»

**סעיפים:** `5`, **`2/5` נענו 17.09** (ניב, פרודקשן, 19:15, עברית) — ⓵ ✅ הכרטיס
נעלם · ⓸ ✅ ⛔ קריסה. **`3/5` פתוחים:** ⓶ ג'ורנל עם סטאפ אמיתי (`n ≥ 2`) —
**זרוע הביקורת ⛔ הופעלה כלל** · ⓷ טקסט ה-`action` — **ריקה-אמיתית**, נצפתה על
מסך שבו הכרטיס נעדר · ⓹ מסך `en` ⛔ נפתח ⇒ `es`/`pt`/`ar` ⛔ כוסו.

**סיווג: ⓐ.** ⚠️ **ושלושת הפתוחים הם בדיוק מה שהרנס פותר** — כולם דורשים
**מצב-התחלה אחר** (ג'ורנל עם סטאפ · locale אחר), ו⛔ תצפית נוספת על אותו מסך.

### C-052 — `CHECKS.md:102`

> «**כרטיס גודל הפוזיציה בטופס הוספת עסקה — האם המסך עדיין נוקב בהוראת קנייה
> שהוא עצמו פוסל.** … ⓵ 🔴 **תנאי-סף לכל השאר — הזן את הצירוף שנצפה:** `NBIS` ·
> כניסה `220.02` · סטופ `202.48` · הון `$821` · `1%`, **ועזוב את שדה המניות
> ריק**. ⛔ **הקלדה בשדה היא דריסה**»

**סעיפים:** `6` — ⓵ תנאי-סף · ⓶ `SHARES 0` + ⛔ התרה · ⓷ הבאנר · ⓸ «שמור»
⛔ שומרת · ⓹ `MAX RISK`/`POS. VALUE` באותו מספר ספרות · ⓺ **בקרה שחייבת להישאר
ירוקה** (`100`/`95`/`10,000`/`1%` ⇒ `SHARES 20`, ⛔ באנר · ⛔ חסימה).
**מצב:** `0/6`, «⛔ טרם».

**סיווג: ⓐ — הנקי מכולם.** ⛔ **⛔ seed של עסקאות** — הכל קלט-טופס.
⚠️ **חריג אחד: `הון $821` הוא הגדרת-משתמש ו⛔ שדה-טופס** ⇒ הרנס **כותב**
`capital` ל-`user_settings` של חשבון ה-QA, וחייב להחזירו. זו ⛔ פעולה ניטרלית.

### C-053 — `CHECKS.md:103`

> «**שני אתרי «הסטאפ החזק ביותר» — האם `Unknown` נעלם מ**שניהם**, והאם סטאפ
> אמיתי שנקבר מתחת לקבוצה בת-עסקה-אחת חזר לפני השטח.**»

**סעיפים:** `4` — ⓵ `Unknown` נעלם **גם** מאריח `Best Setup` באנליטיקס (האתר
ה**שני**, ש-`C-050` ⛔ מדד) · ⓶ **תרחיש `B-331` בדיוק** — סטאפ `5` עסקאות
ב-`80%` מול סטאפ `1` עסקה ב-`100%` ⇒ הכרטיס נוקב ב**ראשון** · ⓷ כל הסטאפים
בני-עסקה-אחת ⇒ הכרטיס ⛔ מופיע (**וזה נכון**) · ⓸ האריח והכרטיס יכולים להצביע
על סטאפים שונים — **⛔ באג**, ו⛔ «לתקן בעין». **מצב:** `0/4`, «⛔ טרם».

**סיווג: ⓐ — והתובעני ביותר ב-seed.** ⓶ דורש **הרכב מדויק** של קבוצות: `5`
עסקאות בסטאפ A מהן `4` מנצחות, ועסקה בודדת מנצחת בסטאפ B. ⓷ דורש ג'ורנל
**שלישי**. ⇒ `3` מצבי-יומן נפרדים בפריט אחד.

### C-056 — `CHECKS.md:106`

> «**אחרי מחיקת טאב המחברת — האם שורת הניווט נושאת `7` פריטים, והאם הסיור רץ
> `14/14` בלי תקיעה.** … 🔴 **ו-`OnboardingTour` נושא `0` כיסוי טסטים** …
> ⇒ **הסעיפים כאן הם הבדיקה ה⛔יחידה שקיימת לסיור.**»

**סעיפים:** `5` — ⓵ `7` פריטי ניווט בדיוק · ⓶ הסיור `1/14`→`14/14` · ⓷ ⛔ תקיעה
(`POLL_TRIES × 100ms ≈ 1.2s` + בועה מעל אזור ריק) · ⓸ RTL ⛔ חריגה/חיתוך ·
⓹ טאב «פידבק» מציג **שם** טאב ⛔ מזהה גולמי, מכל `7`. **מצב:** `0/5`.
⚠️ **השורה מצהירה מפורשות: «Code ⛔ הריץ את הסיור ו⛔ טוען שעבר».**

**סיווג: ⓐ.** ⚠️ **ו-⓵ קיבל עוגן מדוד** — `SwingEdge_App.jsx:1057-1065` מגדיר
`NAV_KEYS` בן `7` בדיוק, ו-`:4275-4277` מוסיף `mentoring` **רק** כש-
`myMentees.length > 0`. חשבון ה-QA הוא בלי מנטי ⇒ `7` הוא הצפי הנכון **עליו**.
🔴 **וזו תלות שברירית — ראה §4.**

### C-057 — `CHECKS.md:107`

> «**אחרי גל `B-350`…`B-353` — האם ארבעת התיקונים נראים על המסך, והאם הסיור לא
> נשבר בדרך.** … ⓷ 🔴 **`B-350` — הסיור ⛔ חוזר במכשיר שני.** … ⚠️ **זו הבדיקה
> היחידה שיכולה להבחין בין `localStorage` ל-DB**»

**סעיפים:** `5` — ⓵ `B-351`, שלבים `6`/`7` מאירים אלמנטים **שונים** (תנאי-סף:
יומן `0` עסקאות) · ⓶ `B-352`, RTL `ArrowLeft` מקדם / LTR הפוך — **⛔ לבדוק שפה
אחת** · ⓷ `B-350`, מכשיר שני · ⓸ `B-353`, «דלג»⇒לוח · «הבנתי»⇒יומן, ⛔ שגיאת
console · ⓹ **בקרה** — `1/14`→`14/14` בלי תקיעה. **מצב:** `0/5`.

**סיווג: מפוצל — ⓐ ל-⓶⓸⓹ · ⓒ ל-⓵ ו-⓷.** ⚠️ **החשד המוקדם («2 מכשירים ⇒ ⓑ/ⓒ»)
הופרך בחלקו** — ראה §3.2.

### 1.1 הטבלה

| `C-` | סיווג | מצב-התחלה נדרש | נקודת-עצירה של הרנס | מה **ניב** בודק בעין |
|------|--------|-----------------|----------------------|-----------------------|
| **C-046** | **ⓑ** | ⛔ — ריצת `schedule` שכבר קיימת בלוג | ⛔ **⛔ בהרנס.** `gh run view <id> --log` | `4/6` קריאים **היום** מהלוג (§3.1); ⓹⓺ — ראה §5 |
| **C-049** | **ⓐ** | חשבון QA · locale `he` · מטבע הון קבוע · **seed: ≥1 עסקה סגורה שממנה `avgWin`⊕`avgLoss`⊕`maxDD` ⛔ שלם** (⓪) | דפדפן headed פתוח על **טאב היומן**, גלול לתא ה-P&L; `4` הכרטיסים באנליטיקס במסך שני | ⓵ תא P&L + tooltip · ⓶ רווח ממוצע · ⓷ הפסד ממוצע (**המינוס**) · ⓸ MaxDD · ⓹ **המפריד `·`** · ⓺ **חיבור ידני לסנט**. ⛔ `.00` ⛔ סוגר |
| **C-050** | **ⓐ** | `3` מצבים: (א) יומן ללא סטאפ · (ב) **seed: סטאפ אמיתי `n ≥ 2`** · (ג) `0` סטאפים; **×2 locale** (`he` + `en`) | עוצר על טאב «לקחים חכמים» בכל מצב; מחליף locale ועוצר שוב | ⓶ הכרטיס **כן** מופיע (**זרוע ביקורת** — היעלמות ⇒ **עצור**) · ⓷ ה-`action` הוא «חפש עוד סטאפים של X.» **ותו לא** · ⓹ `en` ⛔ «increase position size» |
| **C-052** | **ⓐ** | חשבון QA · **`capital = $821`** (כתיבה ל-`user_settings` + שחזור) · `1%` · טופס ריק | טופס הוספת עסקה מלא: `NBIS`/`220.02`/`202.48`, **שדה מניות ריק ו⛔ נגוע**; ⛔ לוחץ «שמור» | ⓶ `SHARES 0` + ⛔ התרה · ⓷ נוסח הבאנר · ⓸ **ניב** לוחץ «שמור» ⇒ ⛔ נשמרת + toast · ⓹ `MAX RISK` אגורות · ⓺ **בקרה** `SHARES 20` ⇒ אדום = **עצור** |
| **C-053** | **ⓐ** | `3` מצבי-יומן: (א) חלק מהעסקאות ללא סטאפ · (ב) **A: `5` עסקאות `80%` + B: `1` עסקה `100%`** · (ג) כל הסטאפים בני-1 | עוצר **פעמיים בכל מצב** — «לקחים חכמים» ואז אריח `Best Setup` באנליטיקס | ⓵ `Unknown` נעדר מ**שניהם** · ⓶ הכרטיס נוקב ב**A** · ⓷ הכרטיס ⛔ מופיע · ⓸ פער אריח↔כרטיס = **⛔ באג, ⛔ לתקן** |
| **C-056** | **ⓐ** | חשבון QA **בלי מנטי** (§4) · locale `he` · `localStorage.swingEdgeTourDone` מוסר ⇒ `startTour()` | עוצר עם **הסיור פתוח על `1/14`**; ⛔ מתקדם | ⓵ **`7`** פריטי ניווט · ⓶ **ניב** מריץ `1/14`→`14/14` · ⓷ ⛔ תקיעה ~1.2s / בועה מעל ריק · ⓸ RTL ⛔ חיתוך · ⓹ פידבק מכל `7` ⇒ **שם** |
| **C-057** ⓶⓸⓹ | **ⓐ** | כנ"ל, **×2 locale** (`he` + `en`) | עוצר עם הסיור פתוח, **פעמיים** — `he` ואז `en` | ⓶ `he`: ←מקדם / `en`: הפוך · ⓸ «דלג»⇒לוח · «הבנתי»⇒יומן · ⛔ console · ⓹ **בקרה** `14/14` |
| **C-057** ⓵ | **ⓒ** | **יומן `0` עסקאות** בחשבון ה-QA | ⛔ — **⛔ מתחיל בלי הכרעה** (§5) | ⓵ שלבים `6`/`7` מאירים אלמנטים **שונים** |
| **C-057** ⓷ | **ⓒ** | **איפוס `tourDone`+`betaWelcome` ב-`user_settings`** | ⛔ — **⛔ מתחיל בלי הכרעה** (§5) | ⓷ הסיור ⛔ נפתח בהקשר השני |

---

## 2. החלטות ארכיטקטורה — והנימוק

### 2.1 סביבה: **פרודקשן** (`https://swing-edge.com`)

⛔ **local · ⛔ preview.** שלוש סיבות **מדודות**:

1. **תנאי-הגלישה עצמם נוקבים בפריסה.** `C-056`/`C-057` גולשים «**פעם אחת אחרי
   הפריסה**», `C-052` «**פעם אחת אחרי הפריסה של גל `B-338`**». בדיקה על בייטים
   שלא נפרסו ⛔ עונה על תנאי-הגלישה — היא עונה על שאלה אחרת.
2. **שבע השורות מונות `⛔ פרודקשן` כפער במפורש.** הרשימה החוזרת היא
   «⛔ JSX · ⛔ React · ⛔ Tailwind · ⛔ RTL · ⛔ ניגודיות · ⛔ קורא מסך ·
   ⛔ דפדפן אמיתי · **⛔ פרודקשן**». הרצה מקומית סוגרת `7/8` ומשאירה את השמיני —
   ואז «נבדק» ייקרא כסגירה. זה `R-4`.
3. 🔴 **`local` הוא הגרוע משני העולמות.** `npm run dev` מגיש בייטים **לא-פרוסים**
   אבל מדבר מול **אותו** Supabase (`VITE_SUPABASE_URL`) ⇒ ה-seed נוחת בפרודקשן
   בכל מקרה. ⇒ הסיכון של פרודקשן **בלי** התועלת שלה.

⚠️ **ולכן `TEST_URL` נשאר המתג**, בדיוק כמו ב-`playwright.config.js:4` וב-
`playwright.sentinel.config.js:11` — ברירת המחדל `https://swing-edge.com`,
ועקיפה מפורשת אפשרית ל-preview. ⛔ **אין ליטרל URL בהרנס.**

### 2.2 אישורי QA: `SENTINEL_QA_EMAIL` / `SENTINEL_QA_PASSWORD` מ-`process.env`

⛔ **סוד חדש · ⛔ ליטרל בקוד · ⛔ קובץ `.env` חדש.** **מדוד:**
`tests-sentinel/sentinel-auth.spec.js:44-45` כבר קורא בדיוק את שני השמות האלה,
ו-`:652-658` כבר מטפל בהיעדרם כ-🟡 מוצהר. ⇒ **שימוש חוזר בשמות, ⛔ ערוץ שני.**

מקומית: ניב מייצא אותם ל-shell לפני ההרצה. ⛔ **הרנס ⛔ מדפיס אותם**, ⛔ כותב
אותם לקובץ, ו⛔ מעביר אותם ב-argv (שנראה ב-`ps`).

🔴 **ההשלכה של הדליפה (`INBOX` 20.09) על הרנס שרץ מקומית — שני חלקים:**

- **א׳ — חלון ההחלפה.** ברגע שניב מחליף את הסיסמה בחשבון ה-QA ומעדכן את
  `SENTINEL_QA_PASSWORD` ב-secrets, **הייצוא המקומי בשל ניב מתיישן בשקט**.
  הרנס ייפול ב-`login-failed` — ⚠️ **באותה צורה בדיוק שבה ייפול אם עוגני הטופס
  זזו או אם Supabase דחה**, כלומר **ההודעה ⛔ תבדיל**. ⇒ **ההחלפה מחייבת עדכון
  ב-`3` מקומות באותו חלון: החשבון · ה-secret · ה-shell של ניב.**
- **ב׳ — 🔴 `headed` הוא ערוץ הדליפה עצמו.** הסיסמה דלפה **בצילום מסך**. הרנס
  המתוכנן פותח חלון גלוי, מקליד את הסיסמה לשדה, וניב מצלם את התוצאה. ⇒ **חובה:
  ההרנס ⛔ עוצר בשלב הכניסה, ⛔ מותיר את טופס הכניסה על המסך, ונקודת-העצירה
  הראשונה היא תמיד *אחרי* שהסשן נוצר.** ⚠️ שדה `type="password"` מסתיר תווים,
  אבל **שדה האימייל ⛔** — וזהות חשבון ה-QA היא מידע שאין סיבה לפרסם בריפו ציבורי.

### 2.3 האם זו כפילות ‹R-6›? — **⛔, והמדידה מראה למה**

| שכבה | מה היא מריצה | דפדפן | מי מכריע | בשרשרת |
|------|---------------|--------|-----------|---------|
| `test:tour` (`D-091`) | בייטים ב-`new Function` | ⛔ | הרנס (`57/57`) | ✅ חוליה 33 |
| `probe:boundary` | bundle סינתטי ב-Chromium | ✅ headless | הרנס (`26`) | ⛔ |
| `tests-sentinel/` | האפליקציה בפרודקשן | ✅ headless, CI | הרנס (findings JSON) | ⛔ |
| **המוצע** | האפליקציה בפרודקשן | ✅ **headed** | 🔴 **ניב, בעין** | ⛔ |

⚠️ **הציר החדש הוא העמודה הרביעית, ⛔ השלישית.** כל הקיים **מכריע בעצמו**;
המוצע **⛔ מכריע** — הוא מכין ועוצר. ⛔ אין היום שום דבר בריפו שעושה זאת.

**מה שנעשה בו שימוש חוזר ו⛔ נכתב מחדש:** התלות ב-`playwright` · שמות משתני
הסביבה (§2.2) · תבנית ה-seed/cleanup ב-REST מול Supabase
(`sentinel-auth.spec.js:580-642` — `auth/v1/token` ואז `rest/v1/trades`) ·
תבנית ה-`chromium.launch` של `scripts/boundary-render-probe.mjs:143`.

🔴 **מה ש⛔ נעשה בו שימוש חוזר, בכוונה: `@playwright/test` כ-runner.** הרנס הזה
חייב **להשאיר את הדפדפן פתוח** אחרי שסיים; ה-runner סוגר context בסוף כל `test`
ומבנה עצמו סביב `expect`. ⇒ **הצורה הנכונה היא `scripts/*.mjs` עם
`chromium.launch({ headless: false })`**, בדיוק כמו `probe:boundary` — ⛔ spec
תחת `tests-sentinel/` ו⛔ config שלישי.

### 2.4 השם — **החלטה פתוחה לניב** (§6)

⚠️ **`test:auth` הוא שם מטעה בשני מובנים:** הוא ⛔ טסט (⛔ מכריע), והוא ⛔ על
`auth` (הכניסה היא אמצעי). ⚠️ **והתקדים בריפו מפוצל:** `test:smoke` הוא `test:*`
ו**⛔** בשרשרת, אבל `probe:boundary`·`probe:firing`·`probe:count` הם `probe:*`
ו⛔ בשרשרת. ⇒ **`probe:eye` מתאים לתבנית המאוחרת יותר.**

🔴 **ובשני המקרים — מנייַן השרשרת ⛔ זז.** `test:registry` §14.1 גוזר את החוליות
מ-`scripts.verify` (`CLAUDE.md` §7), וסקריפט שאינו ב-`verify` ⛔ נספר ⇒ «33
חוליות ואז `build`» נשאר נכון ו-`B-324` ⛔ מחמיר. **נמדד:** `verify` הנוכחי מונה
`33` חוליות ו-`test:auth` ⛔ קיים ב-`package.json`.

---

## 3. חשדות מוקדמים — מה שנמדד

### 3.1 «C-046 = לוג schedule ⇒ ⓑ?» — ✅ **אושר, ו⛔ הנחת-החסימה הופרכה**

**הסיווג ⓑ אושר:** `6/6` הסעיפים נקראים מלוג Actions. ⛔ דפדפן · ⛔ DOM ·
⛔ חשבון QA. ⇒ **הפריט ⛔ שייך לתוכנית המימוש.**

🔴 **אבל ההנחה ש«⛔ טרם רץ» פירושה «חסום» — הופרכה במדידה.** `INBOX` 20.09 מתעד
`8` סלוטים רצופים שלא ירו, ו-`C-046` נושא «נדרשת ריצת `schedule` אמיתית».
**נמדד היום (21.09 10:34Z) ב-`gh run list --workflow=sentinel.yml`:** `8/8`
הריצות האחרונות הן `event=schedule` ו-`conclusion=success`. **ושתי המחרוזות
כבר בלוג:**

```
run 35568403188 (2026-09-21T06:25:36Z) → schedule=[20 * * * *] event=schedule auth=0
run 35540084047 (2026-09-20T21:55:11Z) → schedule=[50 * * * *] event=schedule auth=1
```

⇒ **⓵ (השדה קיים ו⛔ ריק) · ⓶ (`20 * * * *` / `50 * * * *` **בדיוק**) · ⓷
(`[20…]`⇒`auth=0` **וגם** `[50…]`⇒`auth=1`, **שתי** הריצות נצפו) — `3/6`
קריאים **היום**, בלי לחכות לדבר.** ⓸ קריא באותו לוג (`SENTINEL_AUTH` נמצא בשלב
ה-`browser`) ⇒ **`4/6`**.

⚠️ **ו⛔ סגרתי אותם** — §15 קובע שהאימות הוא של ניב; זו **מדידת זמינות ראיה**,
⛔ הכרעת הפריט. **הפעולה הנכונה: ניב קורא את שני הלוגים ומסמן `4/6` — ⛔ הרנס.**

### 3.2 «C-057 = 2 מכשירים ⇒ ⓑ/ⓒ?» — ⚠️ **הופרך חלקית, ואז אושר מסיבה אחרת**

**מה שהופרך:** «מכשיר שני» ⛔ דורש חומרה שנייה. **נמדד בקוד:**
`SwingEdge_App.jsx:1820-1821` — שער ההידרציה קורא `s.tourDone === true` מה-DB
וכותב `swingEdgeTourDone` ל-`localStorage`; `:1338` — `dismissBetaWelcome` בודק
את אותו מפתח. ⇒ **המשתנה היחיד שהופך «מכשיר שני» למכשיר שני הוא
`localStorage` ריק מול אותו `authUser.id`.** הקשר Playwright שני עם
storage-state נקי הוא **שווה-ערך מלא** לכך במסלול הזה. ⇒ **הרנס *יכול* להכין
את ⓷.**

🔴 **מה שאושר, מסיבה ש⛔ הייתה בחשד:** ⓷ ⛔ יכול **לירות** על מצב ה-DB הנוכחי.
המסלול המלא נמדד: `:1824-1825` כותב גם את `betaWelcome` מה-DB, ו-`:1291` מגדיר
את אפקט ה-betaWelcome ב-`hydrationDone`. ⇒ אם `tourDone`+`betaWelcome` כבר
`true` בחשבון ה-QA, הסיור ⛔ נפתח בהקשר השני — **בשני העצים, הישן והחדש.**
⚠️ **זו בדיוק תבנית האינווריאנטות ⚪ של `test:hydration` ו-`.00` של `C-049`:
ירוק בשני העצים ⛔ ראיה לתיקון.** ⇒ כדי ש-⓷ **יוכל** להבחין, צריך **לאפס**
`tourDone`+`betaWelcome` ב-`user_settings` של חשבון ה-QA — **כתיבה למצב חשבון
בפרודקשן** ⇒ **ⓒ, הכרעת ניב.** ⛔ **⛔ סיווג ⓑ.**

⚠️ **ו-`:7735` ⛔ תחליף** — כפתור «הפעל סיור מחדש» מסיר `localStorage` וקורא
`startTour()` **ישירות**, כלומר עוקף את מסלול ה-auto-open ⇒ הוא מודד את ⓹
(הבקרה) ו**⛔ את ⓷**. שימוש בו כראיה ל-⓷ הוא `R-4`.

---

## 4. `B-360` — האם חוסם? **⛔ חוסם, ו🔴 מצמיד**

**מדוד.** `B-360` הוא ש-`mentoring` ⛔ נמדד בשכבה המאומתת כי הטאב ⛔ מרונדר
לחשבון ה-QA (`myMentees.length > 0`). **⛔ אחד** מ-`7` הפריטים נוגע ב-`mentoring`
⇒ **⛔ חוסם אף פריט.**

🔴 **אבל הוא **מצמיד** ל-`C-056`⓵, וזה ממצא ו⛔ הערה.** נמדד
`SwingEdge_App.jsx:4275-4277`: שורת הניווט היא `NAV_KEYS` (`7`, `:1057-1065`)
**ועוד** `mentoring` **רק** כש-`myMentees.length > 0`. ⇒ **הצפי «`7` בדיוק»
של `C-056`⓵ תקף רק על חשבון בלי מנטי.**

⚠️ **ולכן: ברגע שניב ייתן לחשבון ה-QA מנטה כדי לסגור את `B-360`, `C-056`⓵ הופך
ל-`8` — והשורה ב-`CHECKS.md` תמשיך לדרוש `7` בשקט.** ⇒ **הפריטים ⛔ עצמאיים,
ו⛔ קיים שער שיראה את ההצמדה.** ⛔ **⛔ נפתח `B-` כאן** (הפרומפט אוסר); זו
**שורה שחייבת להיכנס ל-`CHECKS.md`/`BACKLOG` בשלב 2**, לפי §10.1.

---

## 5. מה חוסם כל פריט ⓑ/ⓒ

| פריט | חוסם | מה נדרש כדי לשחרר |
|------|-------|--------------------|
| **C-046** ⓵⓶⓷⓸ | 🔴 **⛔ חסום** — הראיה בלוג **היום** (§3.1) | **ניב** קורא `gh run view 35568403188 --log` + `35540084047` ומסמן `4/6`. ⛔ הרנס · ⛔ המתנה |
| **C-046** ⓹ | ⛔ ריצה נפלה על `exit 1` — נדרשת סקירת `conclusion` על חלון ריצות | `gh run list --workflow=sentinel.yml` על חלון מוגדר. ⚠️ **ה-`8` סלוטים שלא ירו (`INBOX` 20.09) ⛔ «נפילה»** — הם היעדר ריצה, ו-`B-357`/`B-358` מודדים אותם |
| **C-046** ⓺ | דורש ריצת `workflow_dispatch` עם `run_auth=true` | **הכרעת ניב** — dispatch ידני יוצר+מוחק עסקת `SNTNL` בחשבון ה-QA. ⛔ Code מריץ |
| **C-057** ⓵ | **יומן `0` עסקאות** בחשבון ה-QA | 🔁 **עודכן 21.09 — (א) חשבון QA שני **נדחה** בידי ניב ⇒ נותר (ב) ריקון+שחזור.** העיצוב, והתנגשותו עם `C-049`⓪/`C-050`⓶ שדורשים יומן **מלא** — **§6.1ב׳**. ⛔ **⛔ מנוסח כאן פעם שנייה** ‹R-6› |
| **C-057** ⓷ | **איפוס `tourDone`+`betaWelcome` ב-`user_settings`** | 🔴 **הכרעת ניב** — כתיבה למצב חשבון בפרודקשן. בלעדיה ⓷ **⛔ יכול לירות** (§3.2) |

---

## 6. מה **⛔ הוכרע** — פתוח לניב

⚠️ **עודכן 21.09 אחרי הכרעת ניב: חשבון QA שני — נדחה.** `probe:eye` רץ על
**אותו** חשבון שהסנטינל משתמש בו. ⛔ **ההכרעה ⛔ מוזילה — היא סוגרת פריט אחד
ופותחת שניים**, ושניהם **נמדדו** ב-§6.1 ו⛔ הונחו.

1. ✅ **חשבון QA אחד או שניים? — הוכרע: אחד, משותף עם הסנטינל.**
   🔴 **מה שזה מבטל:** המסלול הנקי שהתוכנית תיארה — חשבון ריק, `tourDone=false`
   **מובנה**, ⛔ מחיקה ו⛔ שחזור — **⛔ קיים יותר**. `C-057`⓵ דורש יומן `0`
   עסקאות (`CHECKS.md:107`, «תנאי-סף») בזמן ש-`C-049`⓪·`C-050`⓶·`C-053`⓶
   דורשים יומן **מלא ובהרכב מדויק** באותו חשבון ⇒ **ריקון+שחזור הוא כעת
   היחיד, ⛔ המועדף.**
2. 🔴 **האם הרנס רשאי לכתוב `trades`/`user_settings`? — הפך מ«פתוח» ל«כפוי», והרוחב גדל.**
   התקדים של הסנטינל הוא `ticker=in.(SNTNL,SNTNL1)` — **רשימה סגורה, ⛔ תבנית**
   (`sentinel-auth.spec.js:573-578`, מנומק שם במפורש). `C-057`⓵ דורש **מחיקת
   כל השורות** ⇒ זו בדיוק הפעולה שהערה `:573-578` נכתבה כדי לאסור. ⛔ **התקדים
   ⛔ מכסה את זה, וההרחבה חייבת להיות מוצהרת ⛔ מולבשת בשקט.** ⚠️ נשאר פתוח
   לניב — §6.1ב׳ מתאר את ה**עיצוב**, ⛔ נותן אישור.
3. ✅ **השם — הוכרע: `probe:eye`.** תבנית `probe:*` (§2.4). המנייַן `33` ⛔ זז.
4. 🔁 **«ניקוי אחרי» — הוחלף ב«שחזור חובה».** ⛔ `--reset` אופציונלי, ⛔ הכרעת
   ניב בסיום: **גיבוי לפני ריקון ושחזור אוטומטי כחלק מהריצה** (§6.1ב׳).
   🔴 **והמתח שסעיף זה נשא ⛔ נעלם — הוא התחדד:** נקודת-העצירה היא *לפני*
   אימות-העין, והשחזור חייב לקרות *אחריו* ⇒ **ההרנס ⛔ יכול לשחזר «בסוף
   הריצה», כי הריצה ⛔ נגמרת מעצמה.** זה עיצוב, ⛔ פרט מימוש — §6.1ב׳.
5. **`C-049`⓪ — מי מגדיר את ערכי ה-seed?** התוכנית ⛔ נוקבת במספרים בכוונה:
   הם חייבים להיגזר כך ש**הממוצע** יישא אגורות, ⛔ רק העסקה.
6. 🔴 **חלון-הזמן — חוסם חדש, נמדד ו⛔ מונח.** ⛔ **קיים חלון שעון-קיר שניתן
   להוכיח שהוא נקי מריצת `auth` של הסנטינל** (§6.1א׳). ⛔ **⛔ הומצא פתרון** —
   הפריט מדווח כחוסם והכרעתו של ניב.

---

## 6.1 — סיכוני שיתוף חשבון עם Sentinel

שלוש מדידות. **כולן על העץ ב-`d0931a8`, נמדדו 21.09 17:42Z.**

### א׳ — חלון-זמן בטוח: 🔴 **⛔ קיים. חוסם.**

**מה נמדד.** `gh run list --workflow=sentinel.yml --event=schedule --limit 12`
ואז `gh run view <id> --log` על **כל אחת** מ-12 הריצות, לחילוץ שורת השער
`schedule=[…] event=… auth=…` (`sentinel.yml:113`).

**⓵ רק `:50` נוגע בחשבון ה-QA.** `sentinel.yml:102-109`: `'50 * * * *'` ⇒
`auth=1`, `'20 * * * *'` ⇒ `auth=0`. ⇒ **החפיפה היא מול סלוטי `:50` בלבד**,
`6/12` מהריצות שנמדדו. `6/6` סלוטי `:50` נשאו `auth=1`, `0/6` סלוטי `:20`.

**⓶ זמן שעון-הקיר ⛔ נושא זהות-סלוט.** זמני ההתחלה בפועל של `6/6` סלוטי `:50`:

| ריצה | נומינלי | התחלה בפועל | עיכוב (חסם תחתון) |
|------|----------|--------------|---------------------|
| 35540084047 | 20.09 21:50 | 21:54:47 | 4מ׳47שנ׳ |
| 35531113261 | 20.09 18:50 | 19:04:42 | 14מ׳42שנ׳ |
| 35592488743 | 21.09 10:50 | 11:08:42 | 18מ׳42שנ׳ |
| 35630549802 | 21.09 16:50 | 17:12:45 | 22מ׳45שנ׳ |
| 35564959922 | 21.09 04:50 | 05:33:24 | 43מ׳24שנ׳ |
| 35548094288 | 20.09 23:50 | 00:33:51 | 43מ׳51שנ׳ |

🔴 **והראיה החותכת היא דווקא בסלוט האחר:** ריצה `35523960714` היא סלוט
**`:20`** שהתחילה ב-`16:50:16` — כלומר **בדיוק על שעון `:50`**. ⇒ שעון-הקיר
`:50` ⛔ אומר «ריצת `auth`», ושעון-קיר `:05` ⛔ אומר «נקי».

**⓷ החלון שהפרומפט שיער — הופרך ישירות.** ההשערה הייתה «15+ דק׳ אחרי `:50`,
לפני `:20` הבא» ⇒ חלון `[:05, :20]`. **`2/6` מריצות ה-`auth` שנמדדו נחתו בדיוק
בתוכו** — `11:08:42` ו-`17:12:45`. ⛔ **זו ⛔ תקלה בהשערה — זה מה שהמדידה מראה.**

**⓸ ו⛔ קיים חלון אחר.** דקות-הנחיתה של ריצות ה-`auth`: `04 · 08 · 12 · 33 ·
33 · 54`. הפער הגדול ביותר שאינו מכוסה הוא `~21` דק׳ — ⛔ **הוא ⛔ ראיה**:
⚠️ **העיכובים בטבלה הם *חסמי תחתון* ו⛔ מדידות.** GitHub **מפיל** סלוטים
(`12` ריצות על `~49` סלוטים נומינליים בחלון של `24`ש׳ `22`דק׳), ולכן ⛔ ניתן
להבחין בין «`04:50` באיחור `43` דק׳» לבין «`04:50` **הופל** וזו `03:50`
באיחור `103` דק׳». ⇒ **זנב ההתפלגות ⛔ נמדד, ו-`n=6` ⛔ יכול לחסום אותו.**
⛔ **«פער בין `6` נקודות» ⛔ «חלון בטוח» — זה בדיוק «מנה בלי מכנה» (§2).**

⛔ **ולכן ⛔ מוצע כאן תזמון.** ⇒ **הוכרע 21.09: שער מפורש על מצב הסנטינל,
⛔ חלון-זמן.** ⓹–⓾ להלן. ⚠️ **וזה גובר על §6 פריט `6`**, שנוסח כשעדיין
⛔ הייתה הכרעה — הפריט שם נושא כעת פסק-דין מיושן.

---

**⓹ תנאי-השער המדויק — רשימה-לבנה, ⛔ רשימה-שחורה.**

```
gh run list --workflow=sentinel.yml --limit 5 \
  --json databaseId,event,status,conclusion,startedAt
```

**מריצים אם ורק אם `status == "completed"` בכל שורה שחזרה.** כל ערך אחר —
`queued` · `in_progress` · `waiting` · `requested` · `pending` · **וכל ערך
ש-GitHub יוסיף בעתיד** — ⇒ **סירוב**.

🔴 **הכיוון הזה הוא העיקר.** שער שחוסם `in_progress|queued` הוא **רשימה
שחורה**, ומצב חדש בצד GitHub הופך אותו ל**ירוק בשקט**. ⚠️ **זו בדיוק התבנית
ש-`sentinel-auth.spec.js:573-578` נימק במפורש** — `in.(SNTNL,SNTNL1)` סגור
במקום `like.SNTNL*` — ו-`sentinel.yml:105-107` שיוצא `exit 1` על
`github.event.schedule` לא-מוכר במקום לנחש. **אותו עיקרון, שכבה שלישית.**

**⓺ ⛔ מסננים לפי `event`. נמדד.** ריצה `35605093675` (21.09 13:21:31Z) היא
`event=workflow_dispatch` · `schedule=[]` · **`auth=1`** — כלומר **נגעה בחשבון
ה-QA**. `sentinel.yml:26` הוא `default: true` ו-`:100` הוא `${RUN_AUTH:-true}`
⇒ **dispatch ללא קלט מפורש הוא `auth=1`.** ⚠️ **ולכן `--event=schedule`
בשער היה עיוור לריצה הזו.** ⛔ **והפקודה שבפרומפט חסרה `databaseId` ו-`event`**
⇒ הפלט ⛔ ניתן לייחוס והודעת-הסירוב ⛔ יכולה לנקוב בריצה. **הוספתי את שניהם.**

**⓻ `--limit 5` ו⛔ `3`.** `sentinel.yml:28-35` הוא `concurrency: group:
sentinel` עם `cancel-in-progress: false` ⇒ ריצה חדשה **מצטרפת לתור** במקום
לבטל ⇒ יותר מריצה אחת יכולה להיות לא-`completed` בו-זמנית. הריצות ממוינות
לפי `createdAt` יורד, ולכן `3` **מספיק בפועל** — `5` הוא מרווח, ⛔ הכרח.

**⓼ «ריצה שהסתיימה אך עדיין בתוך `auth`» — ⛔ קיים. נמדד, ו⛔ נדרשת בדיקה נוספת.**
`sentinel.yml:47` הוא `timeout-minutes: 12` ⇒ **GitHub הורג את ה-job בתקרה
קשיחה**, ו-`completed` פירושו ש⛔ נותר תהליך שכותב. ⚠️ **מה שכן יכול להישאר
הוא שורות `SNTNL` יתומות** — `test.afterAll` (`:946-948`) ⛔ רץ כשהרנר נהרג,
וה-spec עצמו נוקב בכך ב-`:783` («הריצה הקודמת נקטעה בטיימאאוט»).
⇒ **אבל זה ⛔ סיכון ל-`probe:eye`:** הגיבוי/שחזור (§6.1ב׳) הוא **אגנוסטי
לתוכן** — יתומים מגובים ומשוחזרים **זהים**, וריצת הסנטינל הבאה מנקה אותם
ממילא. ⇒ **⛔ נדרש תנאי-שער נוסף על `conclusion`.**

**⓽ 🔴 «חלון בטיחות קדימה» — ⛔ יכול להתקיים, ⛔ כי הוא קצר.**
משך ריצות `auth=1` מקצה לקצה, `8/8` שנמדדו:
`52 · 60 · 61 · 63 · 73 · 147 · 149 · 150` שניות ⇒ **מקסימום `2`מ׳`30`שנ׳**,
וחסם קשיח `12` דק׳ (`:47`). **⛔ אלה ⛔ המספר הרלוונטי.**
⚠️ **`probe:eye` **עוצר לאימות-עין** ⇒ **זמן ההחזקה שלו הוא אנושי ובלתי-חסום
מעצם העיצוב.** היומן ריק כל עוד ניב מסתכל. ⇒ **השער סוגר את מרוץ ה*התחלה*
ו⛔ את חלון ה*החזקה*.**
**הסדר גודל, עם מכנה:** `8` ריצות `auth` בחלון `20.09 16:31:20Z` →
`21.09 17:12:45Z` (`1,481` דק׳) ⇒ ריצת `auth` אחת לכל `~185` דק׳.
⇒ החזקה של `20` דק׳ חופפת ריצת `auth` בסדר גודל של `20/185 ≈ 11%`.
🔴 **ונמדד שהמרווח התהדק:** הערת `sentinel.yml:32-33` קובעת «min gap 87.8 min»
(נמדד `59` ריצות, 03–10.09); **בחלון שנמדד כאן המרווח המינימלי בין שתי ריצות
הוא `18`מ׳`56`שנ׳** (`16:31:20`→`16:50:16`, 20.09) — **פי `~4.6` צפוף יותר.**
⛔ **⛔ נגעתי ב-`sentinel.yml`** (הפרומפט אוסר) — זה ממצא להכרעת ניב.

**⓾ כשל `gh` ⇒ 🔴 נעילה. הרנס מסרב לרוץ.** שלושה נימוקים, ⛔ בחירה שקטה:
1. **⛔ ידוע ⛔ «בטוח».** המשך אחרי כשל מתרגם «⛔ מדדתי» ל«אין ריצה» — זהו
   `‹R-2›` בצורתו הטהורה, בדיוק כמו `suggestedShares = posSizeTooSmall ? 1 : …`.
2. **אסימטריה.** סירוב עולה לניב **הרצה חוזרת**; המשך מסתכן ב**מחיקת יומן
   הפרודקשן במקביל לריצת סנטינל** ⇒ פיקסצ׳ר פגום **ו**אדום כוזב ב-`6` הריצות
   הבאות. ⛔ **הצדדים ⛔ שקולים.**
3. **תקדים בריפו, ⛔ המצאה.** `sentinel.yml:105-107` יוצא `exit 1` על סלוט
   לא-מוכר; `restCleanup` (`:581-588`) מצהיר 🟡 במקום לדלג בשקט.

**והסירוב חייב להיות רועש:** מדפיסים את **`stderr` המדויק ואת קוד היציאה**
של `gh`, ⛔ «הסנטינל אולי פעיל». ⚠️ **ומבחינים בין המקרים** — `gh` לא מותקן ·
⛔ מאומת · timeout רשת · rate-limit — כי «בדוק את `gh`» הוא בדיוק הניסוח
ש-`B-335` נבנה כדי לאסור.
⚠️ **ומקרה נוסף שנועל: `gh` מחזיר `0` עם מערך ריק.** נמדד ש-`≥14` ריצות
קיימות ⇒ `[]` פירושו ששם ה-workflow או השאילתה נסחפו, ⛔ ש«נקי».

**מה שנשאר להכרעת ניב:** ⓽ **בלבד** — חלון ההחזקה. השער (⓹–⓼) ו-⓾ מוכרעים.

### ב׳ — שחזור: **חובה, ⛔ אופציונלי.** עיצוב:

**למה זה קריטי.** היומן של חשבון ה-QA הוא ה**פיקסצ׳ר** של `C-049`⓪·`C-050`⓶·
`C-053`⓶ ו**גם** הקלט של ריצת `:50` השעתית. ⇒ **הרנס שמת בין ה-`DELETE`
ל-`INSERT` ⛔ «משאיר בלגן» — הוא מוחק פיקסצ׳ר פרודקשן ומרעיל את `6` הריצות
הבאות של הסנטינל באדום כוזב.**

1. **גיבוי לפני כל כתיבה.** `GET /rest/v1/trades?select=*` ⇒ קובץ JSON על
   הדיסק, **ואז אימות שהקובץ נקרא בחזרה ומונה `N` שורות**. ⛔ **`DELETE`
   ⛔ נשלח לפני שהאימות הזה עבר.**
2. 🔴 **הקובץ ⛔ בריפו.** הריפו **ציבורי** (`project_public_repo_constraint`),
   והגיבוי נושא יומן מסחר שלם. התקדים הקיים ב-`.gitignore` הוא
   `analytics-report.txt` / `.analytics-state/` עם ההערה המפורשת «must never
   land in a public repo» ⇒ **אותה צורה בדיוק**, ⛔ קובץ עירום תחת `docs/`.
3. **שחזור בכל מסלול יציאה.** יציאה תקינה · `SIGINT` · `SIGTERM` ·
   `uncaughtException` — **ארבעה**, ⛔ רק הראשון. ⚠️ **וזו הנקודה שבה §6.4
   התחדד:** ההרנס **עוצר** לאימות-עין ו⛔ נגמר מעצמו ⇒ השחזור מופעל
   ב**המשך מפורש של ניב** (מקש בטרמינל), ⛔ ב«סוף הסקריפט».
4. **שחזור עצמאי כרשת-ביטחון.** `probe:eye --restore-only` שקורא את קובץ
   הגיבוי האחרון ומחזיר — למקרה שכל שאר המסלולים מתו. ⛔ **הוא ⛔ מייתר את
   ⓷ — הוא מה שקיים כשהוא נכשל.**
5. 🔴 **נאמנות ה-round-trip ⛔ מונחת.** `id` · `created_at` · והעמודות שנוספו
   במיגרציות `20260812150000_be_row_currency_label` ו-`20260816120000_trades_provenance`
   חייבים לחזור **זהים**. ⇒ **דרישת-סף למימוש: הוכחת `SELECT`→`DELETE`→
   `INSERT`→`SELECT` עם השוואת שדות, ⛔ «נראה שחזר».** ⛔ **⛔ נמדדה כאן** —
   היא דורשת חיבור לחשבון ה-QA, ש§7 אוסר.

### ג׳ — סיסמה דלופה: **נמדד — ⛔ הוחלפה.**

`gh secret list` (שמות ותאריכים בלבד — ⛔ ערכים):
`SENTINEL_QA_PASSWORD  updated=2026-07-25T20:07:07Z`. הדליפה תועדה ב-`INBOX`
**20.09** ⇒ הסוד **⛔ סובב מאז** (`25.07` קודם ל-`20.09` ב-`57` יום).

⇒ **ההתנהגות הנדרשת, כפי שהוכרעה:** ההרנס קורא את `SENTINEL_QA_PASSWORD`
ה**קיים** מ-`process.env` (⛔ שינוי · ⛔ סוד חדש · ⛔ ליטרל), **ומדפיס אזהרה
מפורשת בפלט** שהוא רץ על חשבון שסיסמתו נחשפה. ⛔ **⛔ מחסום ריצה — דיווח בלבד.**

⚠️ **והדבר ⛔ מבטל את §2.2א׳** — חלון ההחלפה בן `3` המקומות (החשבון · ה-secret ·
ה-shell של ניב) נשאר פתוח, ומרגע שניב מחליף, **הייצוא המקומי מתיישן בשקט**
וההרנס ייפול ב-`login-failed` בלי להבדיל מכשל-עוגן. ⇒ האזהרה בפלט צריכה לנקוב
ב-`updated=` שנמדד, כדי שהפער יהיה **נראה** ⛔ מוסק.

---

## 7. מה התוכנית הזו ⛔ כוללת

⛔ קוד · ⛔ `package.json` · ⛔ `tests-sentinel/` · ⛔ workflows · ⛔ סודות ·
⛔ `STATE`/`NEXT`/`CHECKS`/`BACKLOG` · ⛔ פתיחת `B-` · ⛔ סגירת אף `C-` ·
⛔ הרצה מול פרודקשן · ⛔ התחברות לחשבון ה-QA.

⚠️ **פריטים שעלו ו⛔ נרשמו בריפו — נכנסים ב-שלב 2 לפי §10.1:** הצמדת
`B-360`↔`C-056`⓵ (§4) · העדר שער על הצמדה זו · השם המטעה `test:auth` (§2.4) ·
חלון ההחלפה בן `3` המקומות של סיסמת ה-QA (§2.2א׳).
