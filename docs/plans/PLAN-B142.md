# PLAN-B142 — ההון הסגור מפסיק להיות סכום בין-יחידות

> ⛔ **Awaiting approval.** אפס קוד נכתב. הקובץ הזה הוא התוצר היחיד של שלב 2.
> מבוסס על `docs/audits/AUDIT-B142.md` (קומיט `3901578`) ועל הכרעת ניב מ-14.08.

**סיווג §15 — רמה: T3 · תשובות: 1/1/0/1/1**
(1 הפיכות: ⛔ לא — אין כתיבת DB · **2 אמון: כן** — המשתמש קורא הון שגוי בלי לדעת ·
3 אבטחה: לא · **4 רוחב: כן** — `useFxRates.js` + `equityBase.js` + `SwingEdge_App.jsx`
+ `i18n.js` · **5 ודאות: כן** — נמדד בשלב 1). ⚠️ שאלה 2 היא טריגר-יחיד ⇒ `T3` לבד.
אימות-אחרי: אסרציות ערך שנצפו **אדומות** + **אימות עין בדפדפן בחיבור**.

---

## 1 · הפגם במשפט אחד

`stats.currentEquity = capital + Σ pnl` (`src/lib/tradingStats.js:221-224`).
כשאיבר בסכום ⛔ אינו נקוב במטבע שהכותרת מודפסת בו, החיבור מחבר יחידות שונות —
ו**שום סמל ⛔ אינו הופך אותו לנכון**. ⇒ יישור סמלים (`NEXT` #1) הוא **הסתרה**,
⛔ לא תיקון. נדחה בהכרעת ניב 14.08.

## 2 · האינווריאנטה שהגל אוכף

> **הכותרת נקובה ב-`dispCcy`. כל איבר שמטבעו ≠ `dispCcy` ו⛔אינו ניתן להמרה
> ל-`dispCcy` — ⛔ אינו נכנס לסכום, ו**נספר**.**

זו בדיוק התבנית ש-`B-119` קנה לצד ה**חי** (`openPnL.unconvertedCount`,
`SwingEdge_App.jsx:2345-2360`). הגל הזה מחיל אותה על הצד ה**סגור**.
⇒ שני צדי אותה כותרת מתנהגים **זהה**, וזו דרישת ניב Q3.

**מה זה נותן, במספרים** (מ-`/tmp/b142-probe.mjs`, שלב 1):

| מצב | היום | אחרי | מה נספר |
|-----|------|------|----------|
| **S5** הון ILS · חשבון USD · ⛔ אין שער | `₪10,100` (=10,000 ILS + 100 USD) | **`₪10,000`** | 1/1 סגורות · 1/1 פתוחות |
| **S7** נייר לא-מאומת, `fxOk=true` | `₪20,300` | **`₪20,200`** | 1/2 סגורות |
| **S8** אין fixing ליום המימוש | `₪20,300` | **`₪20,200`** | 1/2 סגורות |
| **S1·S2·S3·S4** | — | **byte-identical** | 0 |
| **S6** (הון USD · חשבון ILS · אין שער) | ⛔ אינו יורה | ⛔ אינו יורה | 0 |

⚠️ **S6 ⛔ אינו נכלל בכוונה.** האסימטריה נמדדה בשלב 1: הוא עקבי-פנימית
(`capitalCurrency === PAPER_BASE`) ⇒ ⛔ אין בו חיבור בין-יחידות. סירוב גורף היה
מוחק את ההבחנה הזו, ולכן היא נעולה כאסרציה (§6.4).

## 3 · היקף — הכרעת ניב (ב)

**בהיקף:** S5 · S7 · S8 · `equityBase` הלא-מוצהר · עקומת ההון (אותו בסיס, אותו
`calcFn`) · `curEquityReturnPct`.
**⛔ מחוץ להיקף, ומקבל מזהה בקומיט הסוגר:**

- **`B-143`** — F6: 28 אתרי `statsCcy` + `stats.totalPnL` + `stats.returnPct`
  (`:4160`, `:5735`, `:5771`) + ייצוא ה-PDF (`:7242`, שמדפיס את **אותו** מספר תחת
  סמל **אחר**). ⚠️ אותה הנמקה ש-`B-119` השתמש בה כדי לדחות: תיקון סמל-מצרף הוא
  משפחה שלמה, וערבובה בגל הזה היה מונע אימות-עין ממוקד. עולה ל-`NEXT`.
- **`menteeStats` · `monthStats` · `journalStats`** — **נמדד בשלב 2 כדרישת ניב #3**:
  שלושתם מדפיסים `totalPnL` · `winRate` · `avgWin` · `avgLoss` · `maxDD` ·
  `profitFactor` · `avgR` · `total` · `currentStreak` · `totalTrades`, ו⛔**אף אחד
  מהם ⛔ אינו מדפיס `currentEquity` או `returnPct`**. ⇒ ⛔ אינם בכותרת ההון.
  זיהום ה-`totalPnL` שלהם הוא **אותו** F4 ⇒ נכנס ל-`B-143`, ⛔ לא נשאר יתום (§10.1).

## 4 · העיצוב — שישה שינויים

### C1 · הסימון בתפר — `src/hooks/useFxRates.js:252` (Q1)

```
- if (d.reason !== "converted") return m;
+ if (d.reason !== "converted") return d.ok ? m : { ...m, fxUnconverted: d.reason };
```

- **הסימון הוא תוצר-לוואי של הכרעה שכבר התקבלה שם** (`d`), ⛔ לא מעבר שני.
- ⛔ **אינו מתייג מטבע מחדש** ו⛔**אינו משנה ערך**: `identity` (`d.ok === true`)
  מחזיר את **אותו אובייקט** `m`; מסלול `converted` ⛔ לא נגע; מסלול הסירוב מחזיר
  את כל השדות הקיימים כפי שהם ומוסיף שדה **חדש** בלבד.
- **תנאי העצירה של ניב ⛔ לא הופעל — נבדק ונמצא בר-ביצוע:**
  - `13.9b‴` (`scripts/instrument-currency-test.mjs:564-593`) קורא **שדות בשם**
    (`.rMultiple` · `.pnl` · `.currency`) ⛔ ולא `deepStrictEqual` על האובייקט ⇒
    ⛔ אינו יכול לראות שדה נוסף. **נשאר ירוק byte-identical.**
  - `scripts/fixtures/tradingstats-baseline.json` **כן** מכיל `closedMetrics`
    ותחת `assert.deepStrictEqual` — אבל 8/8 התרחישים נבנים עם `calcTradeMetrics`
    **הגולמי**, ⛔ לעולם לא עם `makeConvertingCalc` ⇒ הסימון ⛔ אינו יכול להגיע לשם.

### C2 · התפר מוזן ב-`dispCcy` — `SwingEdge_App.jsx:2094`

```
- makeConvertingCalc(paperAcctTable, accountCurrency, paperAcctStatus)
+ makeConvertingCalc(paperAcctTable, dispCcy,         paperAcctStatus)
```

זו **כל** הסיבה ש-S5 מתוקן. `dispCcy` הוא המטבע שהכותרת **מדפיסה**; `accountCurrency`
הוא מה שהמשתמש **ביקש**, והשניים נפרדים בדיוק כשההגנה של `:2047` פועלת.
⚠️ זו אותה הזרקה בדיוק ש-`livePnlAmount` כבר מקבלת (`:2131`, ההערה ב-`:2126-2128`).

⛔ **אין סירוב גורף.** בניגוד ל-`livePnlAmount` שמסרבת `fx_fallback` על כל
`dispCcy !== accountCurrency`, כאן ההכרעה נשארת **פר-עסקה**: נייר שמטבעו **שווה**
ל-`dispCcy` נכנס לסכום ביושר (`identity` **מאומת**, ⛔ לא התווית השמורה). זה ההבדל
בין תיקון לבין מחיקת פיצ'ר.

⚠️ **תופעת לוואי מוצהרת:** ב-S5 הטבלה `paperAcctTable` נקובה `PAPER_BASE→accountCurrency`,
ולכן המרה ל-`dispCcy` שונה ⛔ אינה אפשרית ⇒ `no_table`/`no_rate` ⇒ סירוב מסומן.
זו **אותה** מכניקה שמייצרת את ה-`fx_fallback` בצד החי, ⛔ לא כלל חדש.

### C3 · `resolveEquityBase` מחזיר **הכרעה** — `src/lib/equityBase.js:65-82` (Q2)

```
{ ok:true,  reason:"identity"|"converted"|"spot", value, currency:<accountCurrency> }
{ ok:false, reason:"no_rate"|"no_spot_rate",      value:<ההון הגולמי>, currency:<capitalCurrency> }
```

⚠️ **`value` ⛔ לעולם אינו `null`** — בניגוד ל-`amountAt`. לעקומה **חייבת** להיות
ראשית; החוזה בראש הקובץ (`:27-33`) קובע שהחוסר מוחזר כהון **גולמי** ו⛔ לעולם לא
spot, וזה ⛔ לא זז. מה ש**כן** זז: הקורא לומד ש-`value` נקוב ב-`capitalCurrency`
במקום לנחש. היום זה **שקט לחלוטין**.

**הקורא — `SwingEdge_App.jsx`. 🔴 תיקון עובדתי, 15.08 (ניב P1):** הנוסח הקודם
כאן ("עולה מעל") נקרא כתיאור מצב קיים והוא **שגוי**. נמדד מחדש מ-`origin`
בקומיט `8f8fa18`:

```
:1995  const realTrades      ⟵ תלות
:2038  const displayCapital  ⟵ תלות
:2046  const fxOk
:2047  const dispCcy
:2063  const equityBase      🔴 **מתחת** ל-fxOk, ⛔ לא מעליו
```

⇒ `C3` ⛔ **אינו** "קריאה לשדה קיים" אלא **הזזת ה-`useMemo` עצמו** מ-`:2063`
אל **מעל** `:2046`. היתכנות אומתה: שתי התלויות (`realTrades:1995` ·
`displayCapital:2038`) כבר קודמות ל-`:2046`, ו-`resolveEquityBase` ⛔ אינו מקבל
`dispCcy` ⇒ ⛔ אין תלות מעגלית.

⚠️ **חובה הערה בת-שורה בקוד** מעל ה-`useMemo` המוזז: **למה** הוא יושב שם
ו⛔**למה אסור** להחזירו מתחת ל-`fxOk`. בלעדיה הריפקטור הבא מחזיר אותו, ו-`fxOk`
קורא `equityBaseD` לפני שהוא קיים. ואז:

```
- const fxOk = capitalCurrency === accountCurrency || displayCapital != null;
+ const fxOk = capitalCurrency === accountCurrency || (displayCapital != null && equityBaseD.ok);
  const dispCcy = fxOk ? accountCurrency : capitalCurrency;
  const equityBase = equityBaseD.value;
```

🔴 **המצב שזה סוגר, וטרם היה לו שם:** `fxOk === true` (spot עבד) אבל ⛔ **אין
fixing ליום הבסיס** ⇒ הבסיס נשאר `ILS` בעוד `dispCcy === "USD"`. הוא מודפס תחת
`dispSym` ב-`:4156` **וגם** משמש מכנה ל-`curEquityReturnPct` (`:2383`). זהו
בדיוק באג T10 ("יחס של שתי יחידות") חוזר דרך דלת אחורית. אחרי השינוי — `dispCcy`
נופל ל-`capitalCurrency` והבסיס והסמל ⛔ אינם יכולים להיפרד.

⚠️ `capitalShown` (`:2048`) ⛔ **לא זז** — הוא ערך **הווה** ב-spot בכוונה
(`DECISIONS` 2026-08-09).

### C4 · הסכימה החלקית + המונה — `SwingEdge_App.jsx` (Q3)

תאום מדויק ל-`openPnL` (`:2345-2360`), על אותם שדות:

```
const closedPnL = useMemo(() => {
  let value = 0, unconvertedCount = 0;
  for (const m of stats.closedMetrics) {
    if (m.fxUnconverted) { unconvertedCount++; continue; }   // ⛔ לא נוחש, ⛔ לא נשמט בשקט
    value += m.pnl || 0;
  }
  return { value, unconvertedCount };
}, [stats.closedMetrics]);

const curEquity = useMemo(() => equityBase + closedPnL.value + openPnL.value, [...]);
```

⚠️ `stats.closedMetrics` כבר מיוצא (`tradingStats.js:261`) ⇒ ⛔ **אין מעבר שני על
העסקאות ו⛔אין חישוב מקביל** — קוראים את תוצר-הלוואי של C1.
⛔ **`computeTradingStats` ⛔ אינו נוגע** — נתיב א׳ נדחה (ההערה ב-`:221-223` אוסרת),
ו-`stats.currentEquity` נשאר כפי שהוא לצרכני `B-143`.

🔴 **P2 (ניב, 15.08) — `stats.currentEquity` הופך ליתום.** אחרי `C4` הצרכן
ה**יחיד** שלו (`SwingEdge_App.jsx:2366`) חדל לקרוא לו, והוא נשאר **מזוהם ובלי
בעלים** — וזה בדיוק מה שייצר את `B-142` מלכתחילה. ⇒ הערה בת שורתיים **מעליו**
ב-`src/lib/tradingStats.js:221-224`:

> ⚠️ סכום בין-יחידות כשההמרה נכשלת. הכותרת ⛔ אינה צורכת אותו יותר (`B-142`).
> בבעלות `B-143`.

⛔ **בלי לשנות את הערך** — הוא נשאר `capital + totalPnL` byte-identical.

**עקומת ההון** — `generateEquityCurve` (`SwingEdge_App.jsx:342-360`) עושה
`balance += calcFn(t).pnl`. איבר מסומן מדולג שם באותה שורה, אחרת הגרף והכותרת
מציגים שני מספרים שונים — ⚠️ בדיוק משפחת הבאגים ש**ההערה של הפונקציה עצמה**
(`:339-341`) קיימת כדי לסגור.

### C5 · באנר אחד מאוחד, שני מונים — `SwingEdge_App.jsx:4167-4182`

שני ה-`div` הצמודים (`missingPriceWarn` · `unconvertedPnlWarn`) מוחלפים ב**באנר
אחד** שנושא את שלושת המונים. **⛔ לא `—` חשוף · ⛔ לא 0 · ⛔ לא שני באנרים סמוכים**
(דרישת ניב Q3).

**עם מכנה, §2:** `"{n} מתוך {m}"` — סגורות מול `closedTrades.length`, פתוחות מול
`openTrades.length`. מפתחות חדשים ב-`src/i18n.js` ב-**5/5 השפות** (he · en · es ·
pt · ar) — נמדד: `missingPriceWarn` מופיע 5 פעמים.

### C6 · `MixedCurrencyBanner` מקבל מונה — `SwingEdge_App.jsx:841-857`

הבאנר מונה מטבעות ו⛔אינו אומר **כמה עסקאות** בכל אחד ⇒ אחוז בלי מכנה בתחפושת.
תיקון בן-שורה **בתוך** הגל (הכרעת ניב), מ-`stats.pnlByCurrency` שכבר קיים.

---

## 5 · תזוזות מוצהרות — ⚠️ דורשות אישור מפורש

| # | קובץ:שורה | הישן | החדש | למה ⛔ אינו ריכוך |
|---|-----------|------|------|-------------------|
| M1 | `instrument-currency-test.mjs:735-736` | `/makeConvertingCalc\(paperAcctTable, accountCurrency, paperAcctStatus\)/` | `/makeConvertingCalc\(paperAcctTable, dispCcy, paperAcctStatus\)/` | **הנושא של האסרציה נשמר מילה במילה**: היא נועדה לנעול שהתפר מוזן בטבלת **נייר→חשבון** ⛔ ולא `fxTable`, וזה ⛔ לא זז (וגם `!/makeConvertingCalc\(fxTable/` נשאר). רק הארגומנט ש-C2 מזיז במכוון נכתב מחדש, **ושני הערכים נרשמים בהערה** בתבנית גל ג׳ |
| M2 | `fx-test.mjs:240-241·267·271` | `resolveEquityBase({…})` | `resolveEquityBase({…}).value` | ⚠️ עודכנה ה**קריאה**, ⛔ **לא הציפייה** — 5/5 הערכים הצפויים (`59,999*0.3270` · `calm===moved` · `59,999*0.4` · `59,999`) נשארים **מספר-במספר**. הערה מצהירה מה זז |

⛔ **13.9b · 13.9b′ · 13.9b″ · 13.9b‴ · A1–A8 ⛔ אינם נגעים.** ההוראה המקורית
("הסרה מפורשת של 13.9b") **בוטלה** ע"י ניב ב-14.08 על סמך ממצא F1: היא נשענה על
שורת `BACKLOG` שגויה — 13.9b **כבר** אסרציית-הסרה שמקבעת התנהגות **נכונה**.

---

## 6 · אסרציות חדשות — בלוק 15, `scripts/instrument-currency-test.mjs`

⚠️ כל אחת **תיצפה אדומה** לפני התיקון ותודבק בדיווח. אסרציית **ערך**, ⛔ לא מקור.

| # | מצב | אדום היום | ירוק אחרי |
|---|-----|-----------|-----------|
| 15.1 | **S5** — הון ILS · חשבון USD · טבלה ריקה | `Σ = 10,100` | `Σ = 10,000` · `fxUnconverted === "no_table"` · מונה `1` |
| 15.2 | **S7** — תווית ILS על AAPL, `fxOk=true` | `Σ = 20,300` | `Σ = 20,200` · `"unverified_instrument"` · מונה `1/2` |
| 15.3 | **S8** — אין `byDay` ליום המימוש | `Σ = 20,300` | `Σ = 20,200` · `"no_rate"` · מונה `1/2` |
| 15.4 | **S1 · S2 · S6** — הקו הקפוא | — | ⛔ `fxUnconverted` **אינו קיים** · `Object.is` על `pnl`·`rMultiple`·`currency` · מונה `0` · האובייקט הוא **אותו** אובייקט (`identity`) |
| 15.5 | `resolveEquityBase` — 4 מסלולים | — | `identity`·`converted`·`spot`·`no_rate` · **בסירוב: `value` = ההון הגולמי ו-`currency === capitalCurrency`** · ⛔ לעולם `null` · ⛔ לעולם spot |
| **15.5b** | 🔴 **P3** — אין fixing ליום ה**בסיס** בעוד `fxOk` היה `true` | הבסיס `ILS` תחת `$`, והמכנה של `curEquityReturnPct` ביחידה אחרת מהמונה | `ok:false` ⇒ `dispCcy` נופל ל-`capitalCurrency` ⇒ **הבסיס והסמל ⛔ אינם יכולים להיפרד**, והמונה והמכנה באותה יחידה |
| 15.6 | **שיניים** — S3/S4 (`converted`) | — | ⛔ **אפס** סימונים ⇒ סירוב **גורף** ⛔ אינו יכול לעבור את הבלוק |
| 15.7 | עקומת ההון | הנקודה האחרונה ≠ הכותרת ב-S7 | הנקודה האחרונה **=** `equityBase + closedPnL.value` |

`15.4` ו-`15.6` יחד הן המכנה: בלעדיהן "תיקון" שמסרב לכל דבר היה עובר.

⚠️ **P3 — `S8` נושא שני תפקידים שונים ו⛔אסור שיתערבבו:**

| | איזה יום חסר | על מה זה יושב | האסרציה |
|-|---------------|----------------|----------|
| `15.3` | יום ה**מימוש** (`realizedDayKey`) | P&L של עסקה **סגורה** | `no_rate` ⇒ האיבר יוצא מהסכום ונספר |
| `15.5b` | יום ה**בסיס** (`equityBaseDayKey`) | **`equityBase`** עצמו | `ok:false` ⇒ `dispCcy` נופל, הסמל והמכנה ננעלים יחד |

⇒ **שתי אסרציות נפרדות**, ⛔ לא אחת. אסרציה אחת שמכסה את שתיהן ⛔ אינה יכולה
לדעת מי משתיהן נפלה.

---

## 7 · `C-023` — תנאי גלישה, ⛔ לא תדירות

חוזר ל-`docs/CHECKS.md` עם **תנאי**: "כל שינוי ב-`useFxRates.js` · `equityBase.js` ·
כותרת ההון". ⚠️ `AUDIT-B142.md` §8 מצהיר שהמצבים האלה **⛔ לא צולמו**. הבדיקה היא
**בחיבור** (⛔ לא "נראה סביר"), 3 מצבים, בדפדפן, לפני סגירת הגל:

| מצב | הרכבה | מה מחברים בעין | מה חייב להיראות |
|-----|--------|------------------|------------------|
| S5 | הון ILS · חשבון USD · חסימת `/api/fx` | `10,000 + 0` | `₪10,000` · באנר `1/1` סגורות + `1/1` פתוחות |
| S7 | עסקה עם `currency:"ILS"` על AAPL | `20,000 + 200` | `₪20,200` · באנר `1/2` |
| S8 | עסקה שנסגרה ביום ללא fixing | `20,000 + 200` | `₪20,200` · באנר `1/2` |

⛔ אין סגירה בלי אימות-עין (§15, `T3`). Code+Chrome מצלם ו**מחשב את הסכום בעצמו**;
ניב מאשר את הצילום.

---

## 8 · ⛔ אסור לגעת

`livePnlAmount` · לולאת `openPnL` (המונים **נצרכים**, ההכרעה ⛔ לא זזה) ·
`accountAmount` · `spotAmount` · `amountAt` · `acctDecision`/`fmtAcct` (אתרי שורה) ·
`capitalShown` · `computeTradingStats` · `13.9b`·`13.9b′`·`13.9b″`·`13.9b‴` ·
`A1`–`A8` · `tradingstats-baseline.json` · `docs/NEXT.md` (עד סגירת הגל).

---

## 9 · ניתוח השלכות §8 — הפילטר תפס (כסף)

| ציר | תשובה |
|-----|--------|
| משתמשים | 43/46 ביומן דולרי — `dispCcy === accountCurrency` ⇒ **byte-identical**. החשופים: מי שהונו וחשבונו נפרדים **ובזמן** שהשער חסר |
| נתונים | ⛔ אפס כתיבה. הגזירה בזמן קריאה ⛔ ואינה נשמרת |
| עלות | ⛔ אין קריאות רשת חדשות |
| אבטחה | ⛔ ללא נגיעה |
| תחזוקה (§3) | מוריד עבודה: הכותרת מפסיקה לדרוש פרשנות ידנית |
| הפיכות | revert של קומיט אחד |
| **כשל שקט** | **זה בדיוק מה שנסגר.** הסיכון החדש: סירוב **גורף** שמאפס את הכותרת בשקט ⇒ נחסם ב-`15.4`+`15.6` |

**פסק דין: ⚠️ בצע עם הגנה** — האסרציות והאימות-עין הן ההגנה.

---

## 10 · רישום בקומיט הסוגר

1. `docs/BACKLOG.md` `B-142` — **3 תיקוני מקור**: `$10,500.00` שגוי (F2 — המסך
   מדפיס **₪**) · "13.9b קו קפוא" שגוי (F1) · "רק כשאין שער" שגוי (F4 — S7/S8
   יורים ב-`fxOk === true`).
2. `docs/NEXT.md` — #1 מתוקן (יישור `statsCcy`←`dispCcy` **אינו מספיק**), ו-`B-143`
   עולה. ⚠️ **בסגירת הגל בלבד.**
3. `docs/BACKLOG.md` — **`B-143`** חדש (F6 + שלושת הצרכנים המשניים).
4. `docs/STATE.md` · `docs/CHECKS.md` (`C-023` תנאי גלישה) · `docs/DECISIONS.md`
   (שורה מתוארכת: `dispCcy` הוא מטבע הכותרת, ⛔ לא `accountCurrency`).
5. `docs/DONE.md` — `B-142` עם **hash · איך אומת · עמודת `T`**, ⚠️ בקומיט **הבא**
   (קומיט ⛔ אינו יכול לשאת את ה-hash של עצמו).
6. `npm run test:registry` לפני כל push · `npm run verify` מלא, פלט מודבק.

---

## 11 · סדר ביצוע

`15.1-15.7 אדומות` → C1 → C3 → C2 → C4 → C5/C6 → `test:instrument` → `test:fx` →
`test:tradingstats` → `npm run verify` → **אימות עין** → קומיט + push → `DONE` בקומיט הבא.

## 12 · שתי השאלות — **הוכרעו 15.08 (ניב)**

1. **M1** — ✅ **מאושר.** הנושא נשמר (טבלת נייר→חשבון ⛔ לא `fxTable`), השומר
   `!/makeConvertingCalc\(fxTable/` נשאר, ושני הערכים נרשמים בהערה בתבנית גל ג׳.
2. **עקומת ההון** — ✅ **בהיקף.** גרף וכותרת חלוקים הם בדיוק משפחת הבאגים
   שההערה ב-`:339-341` קיימת כדי לסגור. **`15.7` חובה.**

## 13 · תנאי סגירה — ⛔ אין ויתור

- `15.1`–`15.7` (+`15.5b`) **נצפות אדומות**, פלט מודבק.
- `npm run verify` — 23 חוליות + `build`, פלט מודבק.
- אימות עין `S5`/`S7`/`S8` בדפדפן — Code **מחשב את הסכום בעצמו** ומצרף צילום.
  ⛔ לא "נראה סביר".
- §10 במלואו · `DONE` בקומיט הבא.
- ⚠️ אם `13.9b‴` או `tradingstats-baseline.json` **בכל זאת** נופלים — **עצור
  ושאל**. ⛔ אל תרכך.
